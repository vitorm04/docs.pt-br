<Type Name="SemanticResultKey" FullName="System.Speech.Recognition.SemanticResultKey">
  <TypeSignature Language="C#" Value="public class SemanticResultKey" />
  <TypeSignature Language="ILAsm" Value=".class public auto ansi beforefieldinit SemanticResultKey extends System.Object" />
  <TypeSignature Language="DocId" Value="T:System.Speech.Recognition.SemanticResultKey" />
  <AssemblyInfo>
    <AssemblyName>System.Speech</AssemblyName>
    <AssemblyVersion>4.0.0.0</AssemblyVersion>
  </AssemblyInfo>
  <Base>
    <BaseTypeName>System.Object</BaseTypeName>
  </Base>
  <Interfaces />
  <Attributes>
    <Attribute>
      <AttributeName>System.Diagnostics.DebuggerDisplay("{_semanticKey.DebugSummary}")</AttributeName>
    </Attribute>
  </Attributes>
  <Docs>
    <summary>Associa uma cadeia de caracteres chave com valores <see cref="T:System.Speech.Recognition.SemanticResultValue" /> para definir objetos <see cref="T:System.Speech.Recognition.SemanticValue" />.</summary>
    <remarks>
      <format type="text/markdown"><![CDATA[  
  
## Remarks  
 É a unidade básica de expressão de semântica em Speech o <xref:System.Speech.Recognition.SemanticValue>, que é um par chave/valor.  
  
 Usando <xref:System.Speech.Recognition.SemanticResultKey> objetos, você marca <xref:System.Speech.Recognition.SemanticResultValue> instâncias contidas em <xref:System.Speech.Recognition.GrammarBuilder> objetos e cadeias de caracteres de forma que os valores podem ser acessados facilmente de <xref:System.Speech.Recognition.SemanticValue> instâncias no reconhecimento.  
  
 Você pode usar <xref:System.Speech.Recognition.SemanticResultValue> e <xref:System.Speech.Recognition.SemanticResultKey> objetos, em conjunto com <xref:System.Speech.Recognition.GrammarBuilder> e <xref:System.Speech.Recognition.Choices> objetos, para definir a estrutura de semântica de uma gramática de reconhecimento de fala. Para acessar as informações de semânticas em um resultado de reconhecimento, obtenha uma instância de <xref:System.Speech.Recognition.SemanticValue> por meio de <xref:System.Speech.Recognition.RecognizedPhrase.Semantics%2A> propriedade <xref:System.Speech.Recognition.RecognizedPhrase>.  
  
 Para obter mais informações sobre como usar <xref:System.Speech.Recognition.SemanticResultValue> e <xref:System.Speech.Recognition.SemanticResultKey> objetos, consulte [SemanticResultValue de compreensão e objetos SemanticResultKey](http://msdn.microsoft.com/en-us/0fea1236-5261-4608-89b3-9ce9ffc22a1c) e [usando um SemanticResultKey para extrair um SemanticResultValue](http://msdn.microsoft.com/en-us/198e29b8-845f-4cec-a25e-f55c0eaf46aa) no [guia de programação de fala de sistema para o .NET Framework 4.0](http://msdn.microsoft.com/en-us/610116c7-3817-40ff-857b-5d41e8511043).  
  
   
  
## Examples  
 O exemplo a seguir cria um <xref:System.Speech.Recognition.Grammar> para reconhecer a entrada de senha do formulário "minha senha for...", onde a entrada real é correspondida com um caractere curinga.  
  
 O caractere curinga é marcado com uma chave semântica e o <xref:System.Speech.Recognition.Grammar.SpeechRecognized> manipulador verifica a presença da marca para verificar que ocorreu uma entrada de senha.  
  
```csharp  
private void pwdGrammar()   
{  
  GrammarBuilder pwdBuilder = new GrammarBuilder("My Password is");  
  GrammarBuilder wildcardBuilder = new GrammarBuilder();  
  wildcardBuilder.AppendWildcard();  
  SemanticResultKey wildcardKey= new SemanticResultKey("Password", wildcardBuilder);  
  pwdBuilder+=wildcardKey;  
  Grammar grammar = new Grammar(pwdBuilder);  
  grammar.Name = "Password input";  
  
  grammar.SpeechRecognized += delegate(object sender, SpeechRecognizedEventArgs eventArgs)   
  {  
    SemanticValue semantics = eventArgs.Result.Semantics;  
    RecognitionResult result=eventArgs.Result;  
  
    if (!semantics.ContainsKey("Password"))   
    {  
      SpeechUI.SendTextFeedback(eventArgs.Result, "No Password Provided", false);  
    }  
    else   
    {  
      RecognizedAudio pwdAudio = result.GetAudioForWordRange(result.Words[3], result.Words[result.Words.Count - 1]);  
      MemoryStream pwdMemoryStream = new MemoryStream();  
      pwdAudio.WriteToAudioStream(pwdMemoryStream);  
      if (!IsValidPwd(pwdMemoryStream))   
      {  
        string badPwd = System.IO.Path.GetTempPath() + "BadPwd" + (new Random()).Next().ToString() + ".wav";  
        FileStream waveStream = new FileStream(badPwd, FileMode.Create);  
        pwdAudio.WriteToWaveStream(waveStream);  
        waveStream.Flush();  
        waveStream.Close();  
        SpeechUI.SendTextFeedback(eventArgs.Result, "Invalid Password", false);  
  
      }  
    }  
  };  
  grammar.Enabled = true;  
  _recognizer.LoadGrammar(grammar);  
  UpdateGrammarTree(_grammarTreeView, _recognizer);  
  
}  
```  
  
 ]]></format>
    </remarks>
  </Docs>
  <Members>
    <MemberGroup MemberName=".ctor">
      <AssemblyInfo>
        <AssemblyName>System.Speech</AssemblyName>
        <AssemblyVersion>4.0.0.0</AssemblyVersion>
      </AssemblyInfo>
      <Docs>
        <summary>Constrói uma instância de <see cref="T:System.Speech.Recognition.SemanticResultKey" /> e associa a chave de componentes de gramática.</summary>
        <remarks>
          <format type="text/markdown"><![CDATA[  
  
## Remarks  
 Os construtores de <xref:System.Speech.Recognition.SemanticResultKey> especificar uma marca de texto (a chave semântica) e um conjunto de componentes de gramática para adicionar a uma gramática de reconhecimento de fala.  
  
 A gramática de componentes podem ser especificado como uma matriz de <xref:System.Speech.Recognition.GrammarBuilder> objetos, ou como uma matriz de <xref:System.String> instâncias.  
  
 Se os componentes de gramática são usados no reconhecimento, você poderá acessar retornado <xref:System.Speech.Recognition.SemanticValue> usando a marca de texto fornecida para o construtor de <xref:System.Speech.Recognition.SemanticResultKey> como uma chave semântica. O <xref:System.Speech.Recognition.SemanticValue.Value%2A> propriedade o <xref:System.Speech.Recognition.SemanticValue> instância será determinada pelos componentes de gramática usados na definição de <xref:System.Speech.Recognition.SemanticResultKey>.  
  
 ]]></format>
        </remarks>
      </Docs>
    </MemberGroup>
    <Member MemberName=".ctor">
      <MemberSignature Language="C#" Value="public SemanticResultKey (string semanticResultKey, params System.Speech.Recognition.GrammarBuilder[] builders);" />
      <MemberSignature Language="ILAsm" Value=".method public hidebysig specialname rtspecialname instance void .ctor(string semanticResultKey, class System.Speech.Recognition.GrammarBuilder[] builders) cil managed" />
      <MemberSignature Language="DocId" Value="M:System.Speech.Recognition.SemanticResultKey.#ctor(System.String,System.Speech.Recognition.GrammarBuilder[])" />
      <MemberType>Constructor</MemberType>
      <AssemblyInfo>
        <AssemblyName>System.Speech</AssemblyName>
        <AssemblyVersion>4.0.0.0</AssemblyVersion>
      </AssemblyInfo>
      <Parameters>
        <Parameter Name="semanticResultKey" Type="System.String" />
        <Parameter Name="builders" Type="System.Speech.Recognition.GrammarBuilder[]">
          <Attributes>
            <Attribute>
              <AttributeName>System.ParamArray</AttributeName>
            </Attribute>
          </Attributes>
        </Parameter>
      </Parameters>
      <Docs>
        <param name="semanticResultKey">A marca a ser usado como uma chave semântica para acessar o <see cref="T:System.Speech.Recognition.SemanticValue" /> instância associada do <see cref="T:System.Speech.Recognition.GrammarBuilder" /> objetos especificados pelo <c>construtores</c> argumento.</param>
        <param name="builders">Uma matriz de componentes de gramática que será associado com um <see cref="T:System.Speech.Recognition.SemanticValue" /> objeto acessível com a marca definida no <c>semanticResultKey</c>.</param>
        <summary>Atribui uma chave semântica a uma ou mais <see cref="T:System.Speech.Recognition.GrammarBuilder" /> objetos usados para criar uma gramática de reconhecimento de fala.</summary>
        <remarks>
          <format type="text/markdown"><![CDATA[  
  
## Remarks  
 Devido a conversões implícitas, o `builders` argumento dá suporte a <xref:System.Speech.Recognition.SemanticResultValue>, <xref:System.Speech.Recognition.SemanticResultKey>, <xref:System.Speech.Recognition.Choices>, e <xref:System.String> objetos. Para obter mais informações sobre conversões implícitas, consulte <xref:System.Speech.Recognition.GrammarBuilder.op_Implicit%2A>.  
  
 Ao executar uma operação de reconhecimento, o <xref:System.Speech.Recognition.GrammarBuilder> objetos fornecidos no `builders` argumento são tratados como sequencial. Por exemplo, se o seguinte <xref:System.Speech.Recognition.SemanticResultValue> é usado para construir um <xref:System.Speech.Recognition.Grammar>, entrada para o mecanismo de reconhecimento deve conter as palavras "a raposa marrom" na sequência a ser reconhecido.  
  
```csharp  
SemanticResultKey stringTest=new SemanticResultKey(  
    "stringTest", new GrammarBuilder[] {  
    new GrammarBuilder("the"),  
    new GrammarBuilder("quick"),  
    new GrammarBuilder("brown"),  
    new GrammarBuilder("fox")});  
```  
  
 O `semanticResultKey` argumento contém a marca usada para acessar o <xref:System.Speech.Recognition.SemanticValue> que pode ser retornado.  
  
 O <xref:System.Speech.Recognition.SemanticValue.Value%2A> do <xref:System.Speech.Recognition.SemanticValue> é determinado pelo <xref:System.Speech.Recognition.GrammarBuilder> instâncias fornecidas pelo `builders` parâmetro.  
  
 Se o <xref:System.Speech.Recognition.GrammarBuilder> objetos não conter nenhuma instância de definição de <xref:System.Speech.Recognition.SemanticResultValue>, o valor da <xref:System.Speech.Recognition.SemanticValue> é `null`.  
  
 Se o <xref:System.Speech.Recognition.GrammarBuilder> objetos fornecidos no `builders` parâmetro fornecem um sem marcas (não associado a um <xref:System.Speech.Recognition.SemanticResultKey> objeto) <xref:System.Speech.Recognition.SemanticResultValue> instância que é usada pela lógica de reconhecimento, aquela instância do <xref:System.Speech.Recognition.SemanticResultValue> definirá o <xref:System.Speech.Recognition.SemanticValue.Value%2A> propriedade do <xref:System.Speech.Recognition.SemanticValue> que é produzido.  
  
 Deve haver um e apenas um, sem marcas <xref:System.Speech.Recognition.SemanticResultValue> de instância de <xref:System.Speech.Recognition.GrammarBuilder> objetos especificados pelo `builders` parâmetro. Se várias instâncias sem marcas <xref:System.Speech.Recognition.SemanticResultValue> estão associados a <xref:System.Speech.Recognition.SemanticResultKey>, cada tentará o conjunto de valor da <xref:System.Speech.Recognition.SemanticValue> produzido no resultado de reconhecimento. Isso não é permitido e o reconhecedor gerará uma exceção ao tentar usar um <xref:System.Speech.Recognition.Grammar> criado usando como uma <xref:System.Speech.Recognition.SemanticResultKey> instância.  
  
 Instâncias do <xref:System.Speech.Recognition.SemanticResultValue> contidas a <xref:System.Speech.Recognition.GrammarBuilder> objetos especificados pelo `builders` parâmetro e já está associado a outro <xref:System.Speech.Recognition.SemanticResultKey> não têm nenhum efeito no atual <xref:System.Speech.Recognition.SemanticResultKey> instância.  
  
   
  
## Examples  
 O exemplo a seguir cria um <xref:System.Speech.Recognition.Grammar> para reconhecer a entrada de senha do formulário "minha senha for...", onde a entrada real é correspondida com um caractere curinga.  
  
 O caractere curinga é identificado por um <xref:System.Speech.Recognition.SpeechRecognizer> cujo valor da chave é "Password". O <xref:System.Speech.Recognition.Grammar.SpeechRecognized> manipulador verifica a presença da marca, obtém a entrada de áudio da senha e verifica a senha.  
  
```csharp  
private void pwdGrammar()   
{  
  GrammarBuilder pwdBuilder = new GrammarBuilder("My Password is");  
  GrammarBuilder wildcardBuilder = new GrammarBuilder();  
  wildcardBuilder.AppendWildcard();  
  SemanticResultKey wildcardKey= new SemanticResultKey("Password", wildcardBuilder);  
  pwdBuilder+=wildcardKey;  
  Grammar grammar = new Grammar(pwdBuilder);  
  grammar.Name = "Password input";  
  
  grammar.SpeechRecognized +=   
    delegate(object sender, SpeechRecognizedEventArgs eventArgs)   
    {  
      SemanticValue semantics = eventArgs.Result.Semantics;  
      RecognitionResult result=eventArgs.Result;  
  
      if (!semantics.ContainsKey("Password"))   
      {  
        SpeechUI.SendTextFeedback(eventArgs.Result, "No Password Provided", false);  
      }  
      else   
      {  
        RecognizedAudio pwdAudio = result.GetAudioForWordRange(  
                  result.Words[3],  
                  result.Words[result.Words.Count - 1]);  
                  MemoryStream pwdMemoryStream = new MemoryStream();  
                  pwdAudio.WriteToAudioStream(pwdMemoryStream);  
        if (!IsValidPwd(pwdMemoryStream))   
        {  
          string badPwd = System.IO.Path.GetTempPath() + "BadPwd" + (new Random()).Next().ToString() + ".wav";  
          FileStream waveStream = new FileStream(badPwd, FileMode.Create);    
          pwdAudio.WriteToWaveStream(waveStream);  
          waveStream.Flush();  
          waveStream.Close();  
          SpeechUI.SendTextFeedback(eventArgs.Result, "Invalid Password", false);      
        }  
      }  
    };  
  
  grammar.Enabled = true;  
  _recognizer.LoadGrammar(grammar);  
  UpdateGrammarTree(_grammarTreeView, _recognizer);  
  
}  
```  
  
 ]]></format>
        </remarks>
      </Docs>
    </Member>
    <Member MemberName=".ctor">
      <MemberSignature Language="C#" Value="public SemanticResultKey (string semanticResultKey, params string[] phrases);" />
      <MemberSignature Language="ILAsm" Value=".method public hidebysig specialname rtspecialname instance void .ctor(string semanticResultKey, string[] phrases) cil managed" />
      <MemberSignature Language="DocId" Value="M:System.Speech.Recognition.SemanticResultKey.#ctor(System.String,System.String[])" />
      <MemberType>Constructor</MemberType>
      <AssemblyInfo>
        <AssemblyName>System.Speech</AssemblyName>
        <AssemblyVersion>4.0.0.0</AssemblyVersion>
      </AssemblyInfo>
      <Parameters>
        <Parameter Name="semanticResultKey" Type="System.String" />
        <Parameter Name="phrases" Type="System.String[]">
          <Attributes>
            <Attribute>
              <AttributeName>System.ParamArray</AttributeName>
            </Attribute>
          </Attributes>
        </Parameter>
      </Parameters>
      <Docs>
        <param name="semanticResultKey">A marca a ser usado o acesso a <see cref="T:System.Speech.Recognition.SemanticValue" /> instância associada a <see cref="T:System.String" /> objetos especificados pelo <c>frases</c> argumento.</param>
        <param name="phrases">Um ou mais <see cref="T:System.String" /> objetos, cujo texto concatenado será associado com um <see cref="T:System.Speech.Recognition.SemanticValue" /> objeto acessível com a marca definida no <c>semanticResultKey</c>.</param>
        <summary>Atribui uma chave semântica a uma ou mais <see cref="T:System.String" /> instâncias usadas para criar uma gramática de reconhecimento de fala.</summary>
        <remarks>
          <format type="text/markdown"><![CDATA[  
  
## Remarks  
 Ao executar uma operação de reconhecimento, o <xref:System.String> objetos usados no `phrases` parâmetro são tratados como sequencial. Por exemplo, se o seguinte <xref:System.Speech.Recognition.SemanticResultValue> é usado para construir um <xref:System.Speech.Recognition.Grammar>, entrada para o mecanismo de reconhecimento deve conter as palavras "a raposa marrom" na sequência a ser reconhecido.  
  
```csharp  
SemanticResultKey stringTest=new SemanticResultKey("stringTest",   
                                new string[] {  
                                               "the",  
                                               "quick",  
                                               "brown",  
                                               "fox"});  
```  
  
 O `semanticResultKey` argumento determina a chave usada para acessar o <xref:System.Speech.Recognition.SemanticValue> que pode ser retornado.  
  
 Se você construir um <xref:System.Speech.Recognition.Grammar> usando um <xref:System.Speech.Recognition.GrammarBuilder> objeto que contém uma chave semântica com uma matriz de objetos de cadeia de caracteres, o <xref:System.Speech.Recognition.SemanticValue.Value%2A> do <xref:System.Speech.Recognition.SemanticValue> produzido por uma operação de reconhecimento será a cadeia de caracteres usada no reconhecimento. No exemplo anterior, isso significa que <xref:System.Speech.Recognition.SemanticValue.Value%2A> seria "a raposa marrom".  
  
   
  
## Examples  
 O exemplo a seguir cria um <xref:System.Speech.Recognition.Grammar> de um <xref:System.Speech.Recognition.GrammarBuilder> objeto que usa um <xref:System.Speech.Recognition.SemanticResultKey>, que é definido por uma matriz de <xref:System.String> objetos.  
  
 Um mecanismo de reconhecimento usando o <xref:System.Speech.Recognition.Grammar> criado será reconhecer a frase "cor vermelha zero azul verde". A semântica do <xref:System.Speech.Recognition.RecognizedPhrase> retornado pelo reconhecimento conterá um <xref:System.Speech.Recognition.SemanticValue> com um <xref:System.Speech.Recognition.SemanticValue.Value%2A> de "vermelho verde azul". Você pode acessar o <xref:System.Speech.Recognition.SemanticValue> usando a marca "código".  
  
 Devido a `SemanticResultValue("zero", 5)` anexado ao <xref:System.Speech.Recognition.GrammarBuilder>, raiz <xref:System.Speech.Recognition.SemanticValue> objeto o <xref:System.Speech.Recognition.RecognizedPhrase> terá um valor de 5.  
  
```csharp  
private void keyTest()   
{  
  // Say "color red green blue zero"  
  GrammarBuilder gb = new GrammarBuilder("color") +  
                        new SemanticResultKey("code",   
                          (new string[] {"red", "green", "blue"})) +  
                        new SemanticResultValue("zero", 5);  
  Grammar g = new Grammar(gb);  
  g.Name = "keyTest";  
  _recognizer.LoadGrammar(g);  
}  
```  
  
 ]]></format>
        </remarks>
      </Docs>
    </Member>
    <Member MemberName="ToGrammarBuilder">
      <MemberSignature Language="C#" Value="public System.Speech.Recognition.GrammarBuilder ToGrammarBuilder ();" />
      <MemberSignature Language="ILAsm" Value=".method public hidebysig instance class System.Speech.Recognition.GrammarBuilder ToGrammarBuilder() cil managed" />
      <MemberSignature Language="DocId" Value="M:System.Speech.Recognition.SemanticResultKey.ToGrammarBuilder" />
      <MemberType>Method</MemberType>
      <AssemblyInfo>
        <AssemblyName>System.Speech</AssemblyName>
        <AssemblyVersion>4.0.0.0</AssemblyVersion>
      </AssemblyInfo>
      <ReturnValue>
        <ReturnType>System.Speech.Recognition.GrammarBuilder</ReturnType>
      </ReturnValue>
      <Parameters />
      <Docs>
        <summary>Retorna uma instância de <see cref="T:System.Speech.Recognition.GrammarBuilder" /> construído a partir de atual <see cref="T:System.Speech.Recognition.SemanticResultKey" /> instância.</summary>
        <returns>To be added.</returns>
        <remarks>
          <format type="text/markdown"><![CDATA[  
  
## Remarks  
 O uso de <xref:System.Speech.Recognition.SemanticResultValue.ToGrammarBuilder%2A> é equivalente a usar o <xref:System.Speech.Recognition.GrammarBuilder> construtor que aceite <xref:System.Speech.Recognition.SemanticResultKey> como um argumento (<xref:System.Speech.Recognition.GrammarBuilder.%23ctor%28System.Speech.Recognition.SemanticResultKey%29>).  
  
   
  
## Examples  
 O exemplo a seguir cria um <xref:System.Speech.Recognition.Grammar> objeto que dá suporte a comandos para alterar a cor do plano de fundo.  
  
 Um <xref:System.Speech.Recognition.Choices> objeto (`colorChoice`) que contém a lista de opções para as cores de plano de fundo é preenchido usando o <xref:System.Speech.Recognition.Choices.Add%28System.Speech.Recognition.GrammarBuilder%5B%5D%29> método com <xref:System.Speech.Recognition.GrammarBuilder> instâncias. O <xref:System.Speech.Recognition.GrammarBuilder> instâncias são obtidas por meio de <xref:System.Speech.Recognition.SemanticResultKey.ToGrammarBuilder> método no <xref:System.Speech.Recognition.SemanticResultValue> objetos criados a partir de cadeias de caracteres de cor.  
  
 Um <xref:System.Speech.Recognition.GrammarBuilder> , em seguida, é obtida chamando <xref:System.Speech.Recognition.SemanticResultKey.ToGrammarBuilder> em uma <xref:System.Speech.Recognition.SemanticResultKey> instância, que será usada para as opções semânticas de chave `colorChoice`.  
  
```csharp  
  
private Grammar CreateGrammarBuilderRGBSemantics()   
{  
  
  // Create a set of choices, each a lookup from a color name to RGB.  
  // Choices constructors do not take SemanticResultValue parameters, so cast   
  // the SemanticResultValue to GrammarBuilder.  
  Choices colorChoice = new Choices();  
  foreach (string colorName in System.Enum.GetNames(typeof(KnownColor)))   
  {  
    SemanticResultValue colorValue=new SemanticResultValue(colorName, Color.FromName(colorName).ToArgb());  
  
    // Use implicit conversion of SemanticResultValue to GrammarBuilder.  
    colorChoice.Add(colorValue.ToGrammarBuilder());      
  }  
  SemanticResultKey choiceKey = new SemanticResultKey("rgb", colorChoice);  
  GrammarBuilder choiceBuilder = choiceKey.ToGrammarBuilder();  
  
  // Create two intermediate grammars with introductory phrase and the color choice.  
  GrammarBuilder makeBackgroundBuilder = "Make background";  
  makeBackgroundBuilder.Append(choiceBuilder);  
  
  GrammarBuilder configureBackgroundBuilder = new GrammarBuilder("Configure background as");  
  configureBackgroundBuilder.Append((new SemanticResultKey("rgb", colorChoice)).ToGrammarBuilder());  
  
  // Create the Grammar object, which recognizes either intermediate grammar.  
  Grammar grammar = new Grammar(new Choices(new GrammarBuilder[] {makeBackgroundBuilder, configureBackgroundBuilder}));  
  grammar.Name = "Make Background /Configure background as";  
  
  return grammar;  
}  
  
```  
  
 ]]></format>
        </remarks>
      </Docs>
    </Member>
  </Members>
</Type>
