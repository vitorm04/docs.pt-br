---
title: Visão geral dos namespaces-LINQ to XML
description: Saiba mais sobre nomes XML, namespaces XML e prefixos de namespace XML e sobre as classes XName e XNamespace.
ms.date: 07/20/2015
ms.assetid: 16283322-8238-4918-ab11-802ac6748eb7
ms.openlocfilehash: 81fe678a8d6be32461c675cc527595033e826165
ms.sourcegitcommit: 0c3ce6d2e7586d925a30f231f32046b7b3934acb
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/08/2020
ms.locfileid: "89551926"
---
# <a name="namespaces-overview-linq-to-xml"></a><span data-ttu-id="50425-103">Visão geral de namespaces (LINQ to XML)</span><span class="sxs-lookup"><span data-stu-id="50425-103">Namespaces overview (LINQ to XML)</span></span>

<span data-ttu-id="50425-104">Este artigo apresenta *nomes XML*, *namespaces XML*, *prefixos de namespace XML*e <xref:System.Xml.Linq.XName> <xref:System.Xml.Linq.XNamespace> classes e.</span><span class="sxs-lookup"><span data-stu-id="50425-104">This article introduces *XML names*, *XML namespaces*, *XML namespace prefixes*, and the <xref:System.Xml.Linq.XName> and <xref:System.Xml.Linq.XNamespace> classes.</span></span>

<span data-ttu-id="50425-105">Os nomes XML são geralmente uma fonte de complexidade na programação XML.</span><span class="sxs-lookup"><span data-stu-id="50425-105">XML names are often a source of complexity in XML programming.</span></span> <span data-ttu-id="50425-106">Um nome de XML consiste em um namespace XML (também chamado um URI de um namespace XML) e um nome local.</span><span class="sxs-lookup"><span data-stu-id="50425-106">An XML name consists of an XML namespace (also called an XML namespace URI) and a local name.</span></span> <span data-ttu-id="50425-107">Um namespace XML é semelhante a um namespace em um programa .NET.</span><span class="sxs-lookup"><span data-stu-id="50425-107">An XML namespace is similar to a namespace in a .NET program.</span></span> <span data-ttu-id="50425-108">Ele permite qualificar exclusivamente os nomes de elementos e atributos para evitar conflitos de nome entre várias partes de um documento XML.</span><span class="sxs-lookup"><span data-stu-id="50425-108">It enables you to uniquely qualify the names of elements and attributes to avoid name conflicts between various parts of an XML document.</span></span> <span data-ttu-id="50425-109">Quando você declarou um namespace XML, você pode selecionar um nome local que só precisa ser exclusivo dentro desse namespace.</span><span class="sxs-lookup"><span data-stu-id="50425-109">When you've declared an XML namespace, you can select a local name that only has to be unique within that namespace.</span></span>

<span data-ttu-id="50425-110">Outro aspecto dos nomes XML são os prefixos de namespace XML, que causam a maior parte da complexidade de nomes XML.</span><span class="sxs-lookup"><span data-stu-id="50425-110">Another aspect of XML names is XML namespace prefixes, which cause most of the complexity of XML names.</span></span> <span data-ttu-id="50425-111">Esses prefixos permite que você crie um atalho para um namespace XML, que faz o documento XML mais concisas e legível.</span><span class="sxs-lookup"><span data-stu-id="50425-111">These prefixes enable you to create a shortcut for an XML namespace, which makes the XML document more concise and understandable.</span></span> <span data-ttu-id="50425-112">No entanto, o significado de um prefixo XML depende do contexto, o que adiciona complexidade.</span><span class="sxs-lookup"><span data-stu-id="50425-112">However, the meaning of an XML prefix depends on context, which adds complexity.</span></span> <span data-ttu-id="50425-113">Por exemplo, o prefixo XML `aw` pode ser associado a um namespace XML em parte de uma árvore XML e com um namespace diferente em outra parte.</span><span class="sxs-lookup"><span data-stu-id="50425-113">For example, the XML prefix `aw` could be associated with one XML namespace in part of an XML tree, and with a different namespace in another part.</span></span>

<span data-ttu-id="50425-114">Uma das vantagens de usar o LINQ to XML com o C# é que você não precisa usar prefixos XML.</span><span class="sxs-lookup"><span data-stu-id="50425-114">One of the advantages of using LINQ to XML with C# is that you don't have to use XML prefixes.</span></span> <span data-ttu-id="50425-115">Quando LINQ to XML carrega ou analisa um documento XML, cada prefixo XML é resolvido para seu namespace XML correspondente.</span><span class="sxs-lookup"><span data-stu-id="50425-115">When LINQ to XML loads or parses an XML document, each XML prefix is resolved to its corresponding XML namespace.</span></span> <span data-ttu-id="50425-116">Após isso, quando você trabalha com um documento que usar namespaces, você acessa quase sempre namespaces com o URI de namespace, e não com o prefixo de namespace.</span><span class="sxs-lookup"><span data-stu-id="50425-116">After that, when you work with a document that uses namespaces, you almost always access the namespaces through the namespace URI, and not through the namespace prefix.</span></span> <span data-ttu-id="50425-117">Quando os desenvolvedores trabalham com nomes XML em LINQ to XML eles sempre trabalham com um nome XML totalmente qualificado (ou seja, um namespace XML e um nome local).</span><span class="sxs-lookup"><span data-stu-id="50425-117">When developers work with XML names in LINQ to XML they always work with a fully-qualified XML name (that is, an XML namespace and a local name).</span></span> <span data-ttu-id="50425-118">No entanto, o LINQ to XML permite que você trabalhe e controle os prefixos de namespace conforme necessário.</span><span class="sxs-lookup"><span data-stu-id="50425-118">However, LINQ to XML does allow you to work with and control namespace prefixes as needed.</span></span>

<span data-ttu-id="50425-119">Ao usar LINQ to XML com literais Visual Basic e XML, você deve usar prefixos de namespace ao trabalhar com documentos em namespaces.</span><span class="sxs-lookup"><span data-stu-id="50425-119">When using LINQ to XML with Visual Basic and XML literals, you must use namespace prefixes when working with documents in namespaces.</span></span>

<span data-ttu-id="50425-120">No LINQ to XML, a classe que representa os nomes XML é <xref:System.Xml.Linq.XName> .</span><span class="sxs-lookup"><span data-stu-id="50425-120">In LINQ to XML, the class that represents XML names is <xref:System.Xml.Linq.XName>.</span></span> <span data-ttu-id="50425-121">Os nomes de XML aparecem frequentemente em toda a API de LINQ to XML e sempre que um nome XML é necessário, você encontrará um <xref:System.Xml.Linq.XName> parâmetro.</span><span class="sxs-lookup"><span data-stu-id="50425-121">XML names appear frequently throughout the LINQ to XML API, and wherever an XML name is required, you will find an <xref:System.Xml.Linq.XName> parameter.</span></span> <span data-ttu-id="50425-122">No entanto, raramente você trabalha diretamente com um <xref:System.Xml.Linq.XName>.</span><span class="sxs-lookup"><span data-stu-id="50425-122">However, you rarely work directly with an <xref:System.Xml.Linq.XName>.</span></span> <span data-ttu-id="50425-123"><xref:System.Xml.Linq.XName> contém uma conversão implícita de cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="50425-123"><xref:System.Xml.Linq.XName> contains an implicit conversion from string.</span></span>

<span data-ttu-id="50425-124">Para obter mais informações, consulte <xref:System.Xml.Linq.XNamespace> e <xref:System.Xml.Linq.XName>.</span><span class="sxs-lookup"><span data-stu-id="50425-124">For more information, see <xref:System.Xml.Linq.XNamespace> and <xref:System.Xml.Linq.XName>.</span></span>
