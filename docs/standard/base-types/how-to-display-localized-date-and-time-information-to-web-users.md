---
title: Como exibir informações localizadas de data e hora para usuários da Web
ms.date: 03/30/2017
ms.technology: dotnet-standard
helpviewer_keywords:
- formatting [.NET Framework], dates
- parsing strings [.NET Framework], date and time strings
- localization [.NET Framework], date and time displays
- formatting [.NET Framework], localized data
- displaying date and time data
- localized date displays [.NET Framework]
ms.assetid: 377fe93c-32be-421a-a30a-be639a46ede8
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 27e9306164e3d0e008f38f2d94e1f9c11c0d7d3d
ms.sourcegitcommit: 64f4baed249341e5bf64d1385bf48e3f2e1a0211
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/07/2018
ms.locfileid: "44085219"
---
# <a name="how-to-display-localized-date-and-time-information-to-web-users"></a><span data-ttu-id="ddbf4-102">Como exibir informações localizadas de data e hora para usuários da Web</span><span class="sxs-lookup"><span data-stu-id="ddbf4-102">How to: Display Localized Date and Time Information to Web Users</span></span>
<span data-ttu-id="ddbf4-103">Como uma página da Web pode ser exibida em qualquer lugar no mundo, operações que analisam e formatam valores de data e hora não devem depender de um formato padrão (que geralmente é o formato da cultura local do servidor Web) ao interagir com o usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-103">Because a Web page can be displayed anywhere in the world, operations that parse and format date and time values should not rely on a default format (which most often is the format of the Web server's local culture) when interacting with the user.</span></span> <span data-ttu-id="ddbf4-104">Em vez disso, formulários da Web que lidam com cadeias de caracteres de data e hora inseridas pelo usuário devem analisar as cadeias de caracteres usando a cultura preferencial do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-104">Instead, Web forms that handle date and time strings input by the user should parse the strings using the user's preferred culture.</span></span> <span data-ttu-id="ddbf4-105">Da mesma forma, os dados de data e hora devem ser exibidos para o usuário em um formato compatível com a respectiva cultura.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-105">Similarly, date and time data should be displayed to the user in a format that conforms to the user's culture.</span></span> <span data-ttu-id="ddbf4-106">Este tópico mostra como fazer isso.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-106">This topic shows how to do this.</span></span>  
  
### <a name="to-parse-date-and-time-strings-input-by-the-user"></a><span data-ttu-id="ddbf4-107">Para analisar a data e a hora em cadeias de caracteres de entrada do usuário</span><span class="sxs-lookup"><span data-stu-id="ddbf4-107">To parse date and time strings input by the user</span></span>  
  
1.  <span data-ttu-id="ddbf4-108">Determine se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> é populada.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-108">Determine whether the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> property is populated.</span></span> <span data-ttu-id="ddbf4-109">Se não for, vá para a etapa 6.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-109">If it is not, continue to step 6.</span></span>  
  
2.  <span data-ttu-id="ddbf4-110">Se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A> for populada, recupere o primeiro elemento.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-110">If the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property is populated, retrieve its first element.</span></span> <span data-ttu-id="ddbf4-111">O primeiro elemento indica o idioma e a região padrão ou preferenciais do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-111">The first element indicates the user's default or preferred language and region.</span></span>  
  
3.  <span data-ttu-id="ddbf4-112">Crie uma instância de um objeto <xref:System.Globalization.CultureInfo> que represente a cultura preferencial do usuário do chamando o construtor <xref:System.Globalization.CultureInfo.%23ctor%28System.String%2CSystem.Boolean%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-112">Instantiate a <xref:System.Globalization.CultureInfo> object that represents the user's preferred culture by calling the <xref:System.Globalization.CultureInfo.%23ctor%28System.String%2CSystem.Boolean%29?displayProperty=nameWithType> constructor.</span></span>  
  
4.  <span data-ttu-id="ddbf4-113">Chame o método `TryParse` ou o `Parse` do tipo <xref:System.DateTime> ou <xref:System.DateTimeOffset> para tentar a conversão.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-113">Call either the `TryParse` or the `Parse` method of the <xref:System.DateTime> or <xref:System.DateTimeOffset> type to try the conversion.</span></span> <span data-ttu-id="ddbf4-114">Use uma sobrecarga do método `TryParse` ou `Parse` com um parâmetro `provider` e passe para ele um dos seguintes itens:</span><span class="sxs-lookup"><span data-stu-id="ddbf4-114">Use an overload of the `TryParse` or the `Parse` method with a `provider` parameter, and pass it either of the following:</span></span>  
  
    -   <span data-ttu-id="ddbf4-115">O objeto <xref:System.Globalization.CultureInfo> criado na etapa 3.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-115">The <xref:System.Globalization.CultureInfo> object created in step 3.</span></span>  
  
    -   <span data-ttu-id="ddbf4-116">O objeto <xref:System.Globalization.DateTimeFormatInfo> que é retornado pela propriedade <xref:System.Globalization.CultureInfo.DateTimeFormat%2A> do objeto <xref:System.Globalization.CultureInfo> criado na etapa 3.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-116">The <xref:System.Globalization.DateTimeFormatInfo> object that is returned by the <xref:System.Globalization.CultureInfo.DateTimeFormat%2A> property of the <xref:System.Globalization.CultureInfo> object created in step 3.</span></span>  
  
5.  <span data-ttu-id="ddbf4-117">Se a conversão falhar, repita as etapas 2 a 4 para cada elemento restante na matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-117">If the conversion fails, repeat steps 2 through 4 for each remaining element in the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property.</span></span>  
  
6.  <span data-ttu-id="ddbf4-118">Se a conversão ainda falhar ou se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A> estiver vazia, analise a cadeia de caracteres usando a cultura invariável, que é retornada pela propriedade <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-118">If the conversion still fails or if the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property is empty, parse the string by using the invariant culture, which is returned by the <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType> property.</span></span>  
  
### <a name="to-parse-the-local-date-and-time-of-the-users-request"></a><span data-ttu-id="ddbf4-119">Para analisar a data e a hora local da solicitação do usuário</span><span class="sxs-lookup"><span data-stu-id="ddbf4-119">To parse the local date and time of the user's request</span></span>  
  
1.  <span data-ttu-id="ddbf4-120">Adicione um controle <xref:System.Web.UI.WebControls.HiddenField> a um formulário da Web.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-120">Add a <xref:System.Web.UI.WebControls.HiddenField> control to a Web form.</span></span>  
  
2.  <span data-ttu-id="ddbf4-121">Crie uma função JavaScript que manipula o eventos `onClick` de um botão `Submit` gravando a data e a hora atuais e o deslocamento do fuso horário local do UTC (Tempo Universal Coordenado) para a propriedade <xref:System.Web.UI.WebControls.HiddenField.Value%2A>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-121">Create a JavaScript function that handles the `onClick` event of a `Submit` button by writing the current date and time and the local time zone's offset from Coordinated Universal Time (UTC) to the <xref:System.Web.UI.WebControls.HiddenField.Value%2A> property.</span></span> <span data-ttu-id="ddbf4-122">Use um delimitador (como ponto e vírgula) para separar os dois componentes da cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-122">Use a delimiter (such as a semicolon) to separate the two components of the string.</span></span>  
  
3.  <span data-ttu-id="ddbf4-123">Use o evento <xref:System.Web.UI.Control.PreRender> do formulário de Web para injetar a função no fluxo de saída de HTML, passando o texto do script para o método <xref:System.Web.UI.ClientScriptManager.RegisterClientScriptBlock%28System.Type%2CSystem.String%2CSystem.String%2CSystem.Boolean%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-123">Use the Web form's <xref:System.Web.UI.Control.PreRender> event to inject the function into the HTML output stream by passing the text of the script to the <xref:System.Web.UI.ClientScriptManager.RegisterClientScriptBlock%28System.Type%2CSystem.String%2CSystem.String%2CSystem.Boolean%29?displayProperty=nameWithType> method.</span></span>  
  
4.  <span data-ttu-id="ddbf4-124">Conecte o manipulador de eventos ao evento `onClick` do botão `Submit`, fornecendo o nome da função JavaScript ao atributo `OnClientClick` do botão `Submit`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-124">Connect the event handler to the `Submit` button's `onClick` event by providing the name of the JavaScript function to the `OnClientClick` attribute of the `Submit` button.</span></span>  
  
5.  <span data-ttu-id="ddbf4-125">Crie um manipulador para o evento <xref:System.Web.UI.WebControls.Button.Click> do botão `Submit`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-125">Create a handler for the `Submit` button's <xref:System.Web.UI.WebControls.Button.Click> event.</span></span>  
  
6.  <span data-ttu-id="ddbf4-126">No manipulador de eventos, determine se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> é populada.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-126">In the event handler, determine whether the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> property is populated.</span></span> <span data-ttu-id="ddbf4-127">Se não for, vá para a etapa 14.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-127">If it is not, continue to step 14.</span></span>  
  
7.  <span data-ttu-id="ddbf4-128">Se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A> for populada, recupere o primeiro elemento.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-128">If the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property is populated, retrieve its first element.</span></span> <span data-ttu-id="ddbf4-129">O primeiro elemento indica o idioma e a região padrão ou preferenciais do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-129">The first element indicates the user's default or preferred language and region.</span></span>  
  
8.  <span data-ttu-id="ddbf4-130">Crie uma instância de um objeto <xref:System.Globalization.CultureInfo> que represente a cultura preferencial do usuário do chamando o construtor <xref:System.Globalization.CultureInfo.%23ctor%28System.String%2CSystem.Boolean%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-130">Instantiate a <xref:System.Globalization.CultureInfo> object that represents the user's preferred culture by calling the <xref:System.Globalization.CultureInfo.%23ctor%28System.String%2CSystem.Boolean%29?displayProperty=nameWithType> constructor.</span></span>  
  
9. <span data-ttu-id="ddbf4-131">Passe a cadeia de caracteres atribuída à propriedade <xref:System.Web.UI.WebControls.HiddenField.Value%2A> para o método <xref:System.String.Split%2A> para armazenar a representação de cadeia de caracteres de data local do usuário e a representação de cadeia de caracteres do deslocamento de fuso horário local do usuário em elementos de matriz separada.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-131">Pass the string assigned to the <xref:System.Web.UI.WebControls.HiddenField.Value%2A> property to the <xref:System.String.Split%2A> method to store the string representation of the user's local date and time and the string representation of the user's local time zone offset in separate array elements.</span></span>  
  
10. <span data-ttu-id="ddbf4-132">Chame o método <xref:System.DateTime.Parse%2A?displayProperty=nameWithType> ou <xref:System.DateTime.TryParse%28System.String%2CSystem.IFormatProvider%2CSystem.Globalization.DateTimeStyles%2CSystem.DateTime%40%29?displayProperty=nameWithType> para converter a data e a hora da solicitação do usuário em um valor <xref:System.DateTime>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-132">Call either the <xref:System.DateTime.Parse%2A?displayProperty=nameWithType> or <xref:System.DateTime.TryParse%28System.String%2CSystem.IFormatProvider%2CSystem.Globalization.DateTimeStyles%2CSystem.DateTime%40%29?displayProperty=nameWithType> method to convert the date and time of the user's request to a <xref:System.DateTime> value.</span></span> <span data-ttu-id="ddbf4-133">Use uma sobrecarga do método com um parâmetro `provider` e passe a ele um dos seguintes itens:</span><span class="sxs-lookup"><span data-stu-id="ddbf4-133">Use an overload of the method with a `provider` parameter, and pass it either of the following:</span></span>  
  
    -   <span data-ttu-id="ddbf4-134">O objeto <xref:System.Globalization.CultureInfo> criado na etapa 8.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-134">The <xref:System.Globalization.CultureInfo> object created in step 8.</span></span>  
  
    -   <span data-ttu-id="ddbf4-135">O objeto <xref:System.Globalization.DateTimeFormatInfo> que é retornado pela propriedade <xref:System.Globalization.CultureInfo.DateTimeFormat%2A> do objeto <xref:System.Globalization.CultureInfo> criado na etapa 8.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-135">The <xref:System.Globalization.DateTimeFormatInfo> object that is returned by the <xref:System.Globalization.CultureInfo.DateTimeFormat%2A> property of the <xref:System.Globalization.CultureInfo> object created in step 8.</span></span>  
  
11. <span data-ttu-id="ddbf4-136">Se a operação de análise na etapa 10 falhar, vá para a etapa 13.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-136">If the parse operation in step 10 fails, go to step 13.</span></span> <span data-ttu-id="ddbf4-137">Caso contrário, chame o método <xref:System.UInt32.Parse%28System.String%29?displayProperty=nameWithType> para converter a representação de cadeia de caracteres do deslocamento de fuso horário do usuário em um número inteiro.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-137">Otherwise, call the <xref:System.UInt32.Parse%28System.String%29?displayProperty=nameWithType> method to convert the string representation of the user's time zone offset to an integer.</span></span>  
  
12. <span data-ttu-id="ddbf4-138">Crie uma instância de <xref:System.DateTimeOffset> que represente a hora local do usuário chamando o construtor <xref:System.DateTimeOffset.%23ctor%28System.DateTime%2CSystem.TimeSpan%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-138">Instantiate a <xref:System.DateTimeOffset> that represents the user's local time by calling the <xref:System.DateTimeOffset.%23ctor%28System.DateTime%2CSystem.TimeSpan%29?displayProperty=nameWithType> constructor.</span></span>  
  
13. <span data-ttu-id="ddbf4-139">Se a conversão na etapa 10 falhar, repita as etapas 7 a 12 para cada elemento restante na matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-139">If the conversion in step 10 fails, repeat steps 7 through 12 for each remaining element in the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property.</span></span>  
  
14. <span data-ttu-id="ddbf4-140">Se a conversão ainda falhar ou se a matriz de cadeia de caracteres retornada pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A> estiver vazia, analise a cadeia de caracteres usando a cultura invariável, que é retornada pela propriedade <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-140">If the conversion still fails or if the string array returned by the <xref:System.Web.HttpRequest.UserLanguages%2A> property is empty, parse the string by using the invariant culture, which is returned by the <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="ddbf4-141">Repita as etapas de 7 a 12.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-141">Then repeat steps 7 through 12.</span></span>  
  
 <span data-ttu-id="ddbf4-142">O resultado é um objeto <xref:System.DateTimeOffset> que representa a hora local do usuário da página da Web.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-142">The result is a <xref:System.DateTimeOffset> object that represents the local time of the user of your Web page.</span></span> <span data-ttu-id="ddbf4-143">Em seguida, você pode determinar o UTC equivalente chamando o método <xref:System.DateTimeOffset.ToUniversalTime%2A>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-143">You can then determine the equivalent UTC by calling the <xref:System.DateTimeOffset.ToUniversalTime%2A> method.</span></span> <span data-ttu-id="ddbf4-144">Você também pode determinar a data e hora equivalentes no servidor Web chamando o método <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29?displayProperty=nameWithType> e passando um valor <xref:System.TimeZoneInfo.Local%2A?displayProperty=nameWithType> como o fuso horário para converter a hora.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-144">You can also determine the equivalent date and time on your Web server by calling the <xref:System.TimeZoneInfo.ConvertTime%28System.DateTimeOffset%2CSystem.TimeZoneInfo%29?displayProperty=nameWithType> method and passing a value of <xref:System.TimeZoneInfo.Local%2A?displayProperty=nameWithType> as the time zone to convert the time to.</span></span>  
  
## <a name="example"></a><span data-ttu-id="ddbf4-145">Exemplo</span><span class="sxs-lookup"><span data-stu-id="ddbf4-145">Example</span></span>  
 <span data-ttu-id="ddbf4-146">O exemplo a seguir contém o código-fonte HTML e o código para um formulário da Web do ASP.NET que pede ao usuário um valor de data e hora de entrada.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-146">The following example contains both the HTML source and the code for an ASP.NET Web form that asks the user to input a date and time value.</span></span> <span data-ttu-id="ddbf4-147">Um script do lado do cliente também grava informações sobre o local data e a hora da solicitação do usuário e o deslocamento de fuso horário do usuário de UTC em um campo oculto.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-147">A client-side script also writes information on the local date and time of the user's request and the offset of the user's time zone from UTC to a hidden field.</span></span> <span data-ttu-id="ddbf4-148">Em seguida, essas informações são analisadas pelo servidor, que retorna uma página da Web que exibe a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-148">This information is then parsed by the server, which returns a Web page that displays the user's input.</span></span> <span data-ttu-id="ddbf4-149">Também exibe a data e hora da solicitação do usuário usando a hora local do usuário, a hora no servidor e UTC.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-149">It also displays the date and time of the user's request using the user's local time, the time on the server, and UTC.</span></span>  
  
 [!code-aspx-csharp[Formatting.HowTo.ParseDateInput#1](../../../samples/snippets/csharp/VS_Snippets_CLR/Formatting.HowTo.ParseDateInput/cs/GetDateInfo.aspx#1)]
 [!code-aspx-vb[Formatting.HowTo.ParseDateInput#1](../../../samples/snippets/visualbasic/VS_Snippets_CLR/Formatting.HowTo.ParseDateInput/vb/GetDateInfo.aspx#1)]
  
 <span data-ttu-id="ddbf4-150">O script do lado do cliente chama o método JavaScript `toLocaleString`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-150">The client-side script calls the JavaScript `toLocaleString` method.</span></span> <span data-ttu-id="ddbf4-151">Isso produz uma cadeia de caracteres que segue as convenções de formatação da localidade do usuário, que tem maior probabilidade de ser analisada com êxito no servidor.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-151">This produces a string that follows the formatting conventions of the user's locale, which is more likely to be successfully parsed on the server.</span></span>  
  
 <span data-ttu-id="ddbf4-152">A propriedade <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> é populada com os nomes de cultura que estão contidos em cabeçalhos `Accept-Language` incluídos em uma solicitação HTTP.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-152">The <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> property is populated from the culture names that are contained in `Accept-Language` headers included in an HTTP request.</span></span> <span data-ttu-id="ddbf4-153">No entanto, nem todos os navegadores incluem cabeçalhos `Accept-Language` em suas solicitações, e os usuários também podem suprimir totalmente os cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-153">However, not all browsers include `Accept-Language` headers in their requests, and users can also suppress the headers completely.</span></span> <span data-ttu-id="ddbf4-154">Por isso, é importante ter uma cultura de fallback ao analisar a entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-154">This makes it important to have a fallback culture when parsing user input.</span></span> <span data-ttu-id="ddbf4-155">Normalmente, a cultura de fallback é a cultura invariável retornada por <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-155">Typically the fallback culture is the invariant culture returned by <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="ddbf4-156">Os usuários também podem fornecer ao Internet Explorer nomes de cultura que inserem em uma caixa de texto, o que cria a possibilidade de que os nomes de cultura não sejam válidos.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-156">Users can also provide Internet Explorer with culture names that they input in a text box, which creates the possibility that the culture names may not be valid.</span></span> <span data-ttu-id="ddbf4-157">Assim, é importante usar o tratamento de exceções ao instanciar um objeto <xref:System.Globalization.CultureInfo>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-157">This makes it important to use exception handling when instantiating a <xref:System.Globalization.CultureInfo> object.</span></span>  
  
 <span data-ttu-id="ddbf4-158">Quando recuperada de uma solicitação HTTP enviada pelo Internet Explorer, a matriz <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> é populada por ordem de preferência do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-158">When retrieved from an HTTP request submitted by Internet Explorer, the <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> array is populated in order of user preference.</span></span> <span data-ttu-id="ddbf4-159">O primeiro elemento na matriz contém o nome da cultura/região principal do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-159">The first element in the array contains the name of the user's primary culture/region.</span></span> <span data-ttu-id="ddbf4-160">Se a matriz contiver itens adicionais, o Internet Explorer atribuirá arbitrariamente a eles um especificador de qualidade, que é delimitado do nome da cultura por um ponto e vírgula.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-160">If the array contains any additional items, Internet Explorer arbitrarily assigns them a quality specifier, which is delimited from the culture name by a semicolon.</span></span> <span data-ttu-id="ddbf4-161">Por exemplo, uma entrada para a cultura fr-FR pode ter o formato `fr-FR;q=0.7`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-161">For example, an entry for the fr-FR culture might take the form `fr-FR;q=0.7`.</span></span>  
  
 <span data-ttu-id="ddbf4-162">O exemplo chama o construtor <xref:System.Globalization.CultureInfo.%23ctor%2A> com o parâmetro `useUserOverride` definido como `false` para criar um novo objeto <xref:System.Globalization.CultureInfo>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-162">The example calls the <xref:System.Globalization.CultureInfo.%23ctor%2A> constructor with its `useUserOverride` parameter set to `false` to create a new <xref:System.Globalization.CultureInfo> object.</span></span> <span data-ttu-id="ddbf4-163">Isso garante que, se o nome de cultura for o nome de cultura padrão no servidor, o novo <xref:System.Globalization.CultureInfo> criado pelo construtor de classe de objeto conterá as configurações padrão de uma cultura e não refletirá configurações substituídas usando o aplicativo **Opções Regionais e de Idioma** do servidor.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-163">This ensures that, if the culture name is the default culture name on the server, the new <xref:System.Globalization.CultureInfo> object created by the class constructor contains a culture's default settings and does not reflect any settings overridden by using the server's **Regional and Language Options** application.</span></span> <span data-ttu-id="ddbf4-164">Os valores de configurações substituídas no servidor provavelmente não existem no sistema do usuário nem são refletidos na entrada do usuário.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-164">The values from any overridden settings on the server are unlikely to exist on the user's system or to be reflected in the user's input.</span></span>  
  
 <span data-ttu-id="ddbf4-165">Como este exemplo analisa duas representações de cadeia de caracteres de data e hora (uma entrada do usuário e outra armazenada para o campo oculto), ele define os possíveis objetos <xref:System.Globalization.CultureInfo> que podem ser necessários com antecedência.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-165">Because this example parses two string representations of a date and time (one input by the user, the other stored to the hidden field), it defines the possible <xref:System.Globalization.CultureInfo> objects that may be required in advance.</span></span> <span data-ttu-id="ddbf4-166">Ele cria uma matriz de objetos <xref:System.Globalization.CultureInfo> que é maior do que o número de elementos retornados pela propriedade <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-166">It creates an array of <xref:System.Globalization.CultureInfo> objects that is one greater than the number of elements returned by the <xref:System.Web.HttpRequest.UserLanguages%2A?displayProperty=nameWithType> property.</span></span> <span data-ttu-id="ddbf4-167">Ele instancia um objeto <xref:System.Globalization.CultureInfo> para cada cadeia de caracteres de idioma/região e também instancia um objeto <xref:System.Globalization.CultureInfo> que representa <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-167">It then instantiates a <xref:System.Globalization.CultureInfo> object for each language/region string, and also instantiates a <xref:System.Globalization.CultureInfo> object that represents <xref:System.Globalization.CultureInfo.InvariantCulture%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="ddbf4-168">O código pode chamar o método <xref:System.DateTime.Parse%2A> ou <xref:System.DateTime.TryParse%2A> para converter a representação de cadeia de caracteres do usuário de uma data e hora em um valor <xref:System.DateTime>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-168">Your code can call either the <xref:System.DateTime.Parse%2A> or the <xref:System.DateTime.TryParse%2A> method to convert the user's string representation of a date and time to a <xref:System.DateTime> value.</span></span> <span data-ttu-id="ddbf4-169">As chamadas repetidas para um método de análise podem ser necessárias para uma única operação de análise.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-169">Repeated calls to a parse method may be required for a single parsing operation.</span></span> <span data-ttu-id="ddbf4-170">Como resultado, o método <xref:System.DateTime.TryParse%2A> é melhor porque retorna `false` se uma operação de análise falha.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-170">As a result, the <xref:System.DateTime.TryParse%2A> method is better because it returns `false` if a parse operation fails.</span></span> <span data-ttu-id="ddbf4-171">Por outro lado, manipular as exceções repetidas que podem ser geradas pelo método <xref:System.DateTime.Parse%2A> pode ser uma proposta muito cara em um aplicativo Web.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-171">In contrast, handling the repeated exceptions that may be thrown by the <xref:System.DateTime.Parse%2A> method can be a very expensive proposition in a Web application.</span></span>  
  
## <a name="compiling-the-code"></a><span data-ttu-id="ddbf4-172">Compilando o código</span><span class="sxs-lookup"><span data-stu-id="ddbf4-172">Compiling the Code</span></span>  
 <span data-ttu-id="ddbf4-173">Para compilar o código, crie uma página da Web [!INCLUDE[vstecasp](../../../includes/vstecasp-md.md)] sem code-behind.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-173">To compile the code, create an [!INCLUDE[vstecasp](../../../includes/vstecasp-md.md)] Web page without a code-behind.</span></span> <span data-ttu-id="ddbf4-174">Copie o exemplo para a página da Web para que ele substitua todo o código existente.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-174">Then copy the example into the Web page so that it replaces all the existing code.</span></span> <span data-ttu-id="ddbf4-175">A página da Web [!INCLUDE[vstecasp](../../../includes/vstecasp-md.md)] deve conter os seguintes controles:</span><span class="sxs-lookup"><span data-stu-id="ddbf4-175">The [!INCLUDE[vstecasp](../../../includes/vstecasp-md.md)] Web page should contain the following controls:</span></span>  
  
-   <span data-ttu-id="ddbf4-176">Um controle <xref:System.Web.UI.WebControls.Label>, que não é referenciado no código.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-176">A <xref:System.Web.UI.WebControls.Label> control, which is not referenced in code.</span></span> <span data-ttu-id="ddbf4-177">Defina a propriedade <xref:System.Web.UI.WebControls.TextBox.Text%2A> como "Digite um Número:".</span><span class="sxs-lookup"><span data-stu-id="ddbf4-177">Set its <xref:System.Web.UI.WebControls.TextBox.Text%2A> property to "Enter a Number:".</span></span>  
  
-   <span data-ttu-id="ddbf4-178">Um controle <xref:System.Web.UI.WebControls.TextBox> chamado `DateString`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-178">A <xref:System.Web.UI.WebControls.TextBox> control named `DateString`.</span></span>  
  
-   <span data-ttu-id="ddbf4-179">Um controle <xref:System.Web.UI.WebControls.Button> chamado `OKButton`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-179">A <xref:System.Web.UI.WebControls.Button> control named `OKButton`.</span></span> <span data-ttu-id="ddbf4-180">Defina sua propriedade <xref:System.Web.UI.WebControls.Button.Text%2A> como "OK".</span><span class="sxs-lookup"><span data-stu-id="ddbf4-180">Set its <xref:System.Web.UI.WebControls.Button.Text%2A> property to "OK".</span></span>  
  
-   <span data-ttu-id="ddbf4-181">Um controle <xref:System.Web.UI.WebControls.HiddenField> chamado `DateInfo`.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-181">A <xref:System.Web.UI.WebControls.HiddenField> control named `DateInfo`.</span></span>  
  
## <a name="net-framework-security"></a><span data-ttu-id="ddbf4-182">Segurança do .NET Framework</span><span class="sxs-lookup"><span data-stu-id="ddbf4-182">.NET Framework Security</span></span>  
 <span data-ttu-id="ddbf4-183">Para impedir que um usuário insira script no fluxo de HTML, a entrada do usuário nunca deve ser ecoada diretamente de volta na resposta do servidor.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-183">To prevent a user from injecting script into the HTML stream, user input should never be directly echoed back in the server response.</span></span> <span data-ttu-id="ddbf4-184">Em vez disso, ela deve ser codificada usando o método <xref:System.Web.HttpServerUtility.HtmlEncode%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="ddbf4-184">Instead, it should be encoded by using the <xref:System.Web.HttpServerUtility.HtmlEncode%2A?displayProperty=nameWithType> method.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="ddbf4-185">Consulte também</span><span class="sxs-lookup"><span data-stu-id="ddbf4-185">See also</span></span>

- [<span data-ttu-id="ddbf4-186">Executando operações de formatação</span><span class="sxs-lookup"><span data-stu-id="ddbf4-186">Performing Formatting Operations</span></span>](../../../docs/standard/base-types/performing-formatting-operations.md)  
- [<span data-ttu-id="ddbf4-187">Cadeias de caracteres de formato de data e hora padrão</span><span class="sxs-lookup"><span data-stu-id="ddbf4-187">Standard Date and Time Format Strings</span></span>](../../../docs/standard/base-types/standard-date-and-time-format-strings.md)  
- [<span data-ttu-id="ddbf4-188">Cadeias de caracteres de formato de data e hora personalizado</span><span class="sxs-lookup"><span data-stu-id="ddbf4-188">Custom Date and Time Format Strings</span></span>](../../../docs/standard/base-types/custom-date-and-time-format-strings.md)  
- [<span data-ttu-id="ddbf4-189">Analisando cadeias de caracteres de data e hora</span><span class="sxs-lookup"><span data-stu-id="ddbf4-189">Parsing Date and Time Strings</span></span>](../../../docs/standard/base-types/parsing-datetime.md)
