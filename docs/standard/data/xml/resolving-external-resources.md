---
title: Resolvendo recursos externos
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-standard
ms.tgt_pltfrm: 
ms.topic: article
ms.assetid: ad3fa320-4b8f-4e5c-b549-01157591007a
caps.latest.revision: "4"
author: mairaw
ms.author: mairaw
manager: wpickett
ms.openlocfilehash: 824a35ee5d4ecafc45167ff3f4bc89802af4ed96
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2017
---
# <a name="resolving-external-resources"></a><span data-ttu-id="4d1a2-102">Resolvendo recursos externos</span><span class="sxs-lookup"><span data-stu-id="4d1a2-102">Resolving External Resources</span></span>
<span data-ttu-id="4d1a2-103">O **XmlResolver** propriedade o **XmlDocument** é usado pelo **XmlDocument** classe para localizar recursos que não são embutidos em dados XML, como o tipo de documento externo DTDs (definições), entidades e esquemas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-103">The **XmlResolver** property of the **XmlDocument** is used by the **XmlDocument** class to locate resources that are not inline in the XML data, such as external document type definitions (DTDs), entities, and schemas.</span></span> <span data-ttu-id="4d1a2-104">Esses itens podem ser localizados em uma rede ou em uma unidade local, e são identificáveis por Uniform Resource Identifier (URI).</span><span class="sxs-lookup"><span data-stu-id="4d1a2-104">These items can be located on a network or on a local drive, and are identifiable by a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="4d1a2-105">Isso permite que o **XmlDocument** resolver **EntityReference** nós que estão presentes no documento e validar o documento de acordo com o esquema ou DTD externo.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-105">This allows the **XmlDocument** to resolve **EntityReference** nodes that are present in the document and validate the document according to the external DTD or schema.</span></span>  
  
## <a name="fully-trusted-xmldocument"></a><span data-ttu-id="4d1a2-106">XmlDocument totalmente confiável</span><span class="sxs-lookup"><span data-stu-id="4d1a2-106">Fully-Trusted XmlDocument</span></span>  
 <span data-ttu-id="4d1a2-107">O **XmlResolver** propriedade afeta a funcionalidade do **XmlDocument.Load** método.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-107">The **XmlResolver** property affects the functionality of the **XmlDocument.Load** method.</span></span> <span data-ttu-id="4d1a2-108">A tabela a seguir mostra como o **XmlDocument.XmlResolver** propriedade funciona quando o **XmlDocument** objeto é totalmente confiável.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-108">The table below shows how the **XmlDocument.XmlResolver** property works when the **XmlDocument** object is fully trusted.</span></span> <span data-ttu-id="4d1a2-109">A tabela a seguir mostra o **XmlDocument.Load** métodos quando a entrada para a carga for um **TextReader**, **cadeia de caracteres**, **fluxo**, ou  **URI**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-109">The following table shows the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="4d1a2-110">Esta tabela não é válido para o **carga** método se a **XmlDocument** é carregado de um **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-110">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="4d1a2-111">Propriedade de XmlResolver</span><span class="sxs-lookup"><span data-stu-id="4d1a2-111">XmlResolver property</span></span>|<span data-ttu-id="4d1a2-112">Função</span><span class="sxs-lookup"><span data-stu-id="4d1a2-112">Function</span></span>|<span data-ttu-id="4d1a2-113">Observações</span><span class="sxs-lookup"><span data-stu-id="4d1a2-113">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="4d1a2-114">A propriedade é definida como um **XmlResolver** classe que foi criado anteriormente e possui propriedades já definidas pelo usuário.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-114">The property is set to an **XmlResolver** class that has been previously created and has properties already set on it by the user.</span></span>|<span data-ttu-id="4d1a2-115">O **XmlDocument** usa o **XmlResolver** que é fornecido para resolver nomes de arquivo, para resolver referências a recursos externos como DTDs, entidades e esquemas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-115">The **XmlDocument** uses the **XmlResolver** that is given to resolve file names, to resolve references to external resources such as DTDs, entities, and schemas.</span></span><br /><br /> <span data-ttu-id="4d1a2-116">O **XmlResolver** também é usado quando Resolvendo recursos externos que são necessários para adicionar ou editar nós o **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-116">The **XmlResolver** is also used when resolving external resources that are needed when adding or editing nodes in the **XmlDocument**.</span></span>|<span data-ttu-id="4d1a2-117">O **XmlResolver** fornecido para o **XmlDocument** é o resolvedor é usado sempre que os recursos externos precisam ser localizado e resolvidos.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-117">The **XmlResolver** given to the **XmlDocument** is the resolver that is used whenever external resources need to be located and resolved.</span></span>|  
|<span data-ttu-id="4d1a2-118">A propriedade é definida como **nulo** (**nada** no Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="4d1a2-118">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="4d1a2-119">Os recursos que exigem um recurso externo não são suportados, como localizar um esquema externo, ou o DTD.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-119">Features that require an external resource are not supported, such as locating an external schema, or DTD.</span></span> <span data-ttu-id="4d1a2-120">Nem as entidades externos serão resolvidas, e executar funções de edição, como inserir os nós de entidade que exigem resolução, não é suportado.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-120">Nor will external entities be resolved, and performing editing functions, such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="4d1a2-121">O **XmlDocument** carregamentos de arquivos como anônimos e não tenta resolver todos os outros recursos.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-121">The **XmlDocument** loads files as anonymous and does not attempt to resolve any other resources.</span></span>|  
|<span data-ttu-id="4d1a2-122">A propriedade não está definida, mas esquerda em seu estado padrão.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-122">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="4d1a2-123">Um **XmlUrlResolver** com NULL credenciais serão criadas e usadas pelo **XmlDocument** durante a resolução de nomes de arquivo, localizando DTDs, entidades e esquemas externos e **null** as credenciais são usadas durante a edição de nós.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-123">An **XmlUrlResolver** with NULL credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>||  
  
 <span data-ttu-id="4d1a2-124">A tabela a seguir mostra o **XmlDocument.Load** método quando a entrada para o **carga** é um **XmlReader** e **XmlDocument** é totalmente confiável.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-124">The following table shows the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader** and the **XmlDocument** is fully trusted.</span></span>  
  
|<span data-ttu-id="4d1a2-125">Propriedade de XmlResolver</span><span class="sxs-lookup"><span data-stu-id="4d1a2-125">XmlResolver property</span></span>|<span data-ttu-id="4d1a2-126">Função</span><span class="sxs-lookup"><span data-stu-id="4d1a2-126">Function</span></span>|<span data-ttu-id="4d1a2-127">Observações</span><span class="sxs-lookup"><span data-stu-id="4d1a2-127">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="4d1a2-128">O **XmlResolver** classe usada pelo **XmlDocument** é a mesma classe que está sendo usada pelo **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-128">The **XmlResolver** class used by the **XmlDocument** is the same class being used by the **XmlReader**.</span></span>|<span data-ttu-id="4d1a2-129">O **XmlDocument** usa o **XmlResolver** que foi atribuído para o **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-129">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="4d1a2-130">O **XmlDocument.Resolver** propriedade não pode ser definido, independentemente do **XmlDocument** nível, de confiança porque ele está ficando um **XmlResolver** do  **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-130">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="4d1a2-131">Não é possível tentar substituir as configurações do **XmlReaders**' **XmlResolver** definindo o **XmlResolver** propriedade o **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-131">You cannot attempt to override the settings of the **XmlReaders**' **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="4d1a2-132">O **XmlReader** pode ser o **XmlTextReader**, **XmlValidatingReader**, ou um leitor personalizados.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-132">The **XmlReader** can be the **XmlTextReader**, **XmlValidatingReader**, or a custom-written reader.</span></span> <span data-ttu-id="4d1a2-133">Se o leitor usado oferece suporte a resolução de entidade, as entidades externos são resolvidas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-133">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="4d1a2-134">Se o leitor passado não oferece suporte a referências a entidades, então as referências a entidades não são resolvidas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-134">If the reader passed does not support entity references, then entity references are not resolved.</span></span>|  
  
## <a name="semi-trusted-xmldocument"></a><span data-ttu-id="4d1a2-135">XmlDocument de confiança parcial</span><span class="sxs-lookup"><span data-stu-id="4d1a2-135">Semi-Trusted XmlDocument</span></span>  
 <span data-ttu-id="4d1a2-136">A tabela a seguir mostra como o **XmlDocument.XmlResolver** propriedade funciona quando o objeto é de confiança parcial.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-136">The following table shows how the **XmlDocument.XmlResolver** property works when the object is semi-trusted.</span></span> <span data-ttu-id="4d1a2-137">Esta tabela se aplica ao **XmlDocument.Load** métodos quando a entrada para a carga for um **TextReader**, **cadeia de caracteres**, **fluxo**, ou  **URI**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-137">This table applies to the **XmlDocument.Load** methods when the input to the Load is a **TextReader**, **String**, **Stream**, or **URI**.</span></span> <span data-ttu-id="4d1a2-138">Esta tabela não é válido para o **carga** método se a **XmlDocument** é carregado de um **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-138">This table does not apply to the **Load** method if the **XmlDocument** is loaded from an **XmlReader**.</span></span>  
  
|<span data-ttu-id="4d1a2-139">Propriedade de XmlResolver</span><span class="sxs-lookup"><span data-stu-id="4d1a2-139">XmlResolver property</span></span>|<span data-ttu-id="4d1a2-140">Função</span><span class="sxs-lookup"><span data-stu-id="4d1a2-140">Function</span></span>|<span data-ttu-id="4d1a2-141">Observações</span><span class="sxs-lookup"><span data-stu-id="4d1a2-141">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="4d1a2-142">No cenário de confiança parcial, o **XmlResolver** propriedade não pode ser definida como algo diferente de null.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-142">In the semi-trusted scenario, the **XmlResolver** property cannot be set to anything other than null.</span></span>|<span data-ttu-id="4d1a2-143">Um **XmlUrlResolver** com **nulo** credenciais serão criadas e usadas pelo **XmlDocument** durante a resolução de nomes de arquivo, localizando externos DTDs, entidades, e esquemas e **nulo** as credenciais são usadas durante a edição de nós.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-143">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="4d1a2-144">Esse comportamento é idêntico ao comportamento quando o **XmlResolver** propriedade não é definida, mas deixada no estado padrão.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-144">This behavior is identical to the behavior when the **XmlResolver** property is not set, but left in its default state.</span></span><br /><br /> <span data-ttu-id="4d1a2-145">O **XmlDocument** usa permissões anônimas para todas as ações.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-145">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
|<span data-ttu-id="4d1a2-146">A propriedade é definida como **nulo** (**nada** no Microsoft Visual Basic .NET).</span><span class="sxs-lookup"><span data-stu-id="4d1a2-146">The property is set to **null** (**Nothing** in Microsoft Visual Basic .NET).</span></span>|<span data-ttu-id="4d1a2-147">Nenhum recurso que requer um recurso externo é suportado, como localizar um esquema externo ou um DTD.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-147">No features that require an external resource are supported, such as locating an external schema or DTD.</span></span> <span data-ttu-id="4d1a2-148">Nem as entidades externos serão resolvidas, e executar funções de edição como inserir os nós de entidade que exigem resolução, não é suportado.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-148">Nor will external entities be resolved, and performing editing functions such as inserting entity nodes that require resolution, are not supported.</span></span>|<span data-ttu-id="4d1a2-149">Quando a propriedade for **nulo**, o comportamento é o mesmo independentemente do **XmlDocument** é totalmente confiável ou confiança parcial.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-149">When the property is **null**, the behavior is the same regardless if the **XmlDocument** is fully trusted or semi-trusted.</span></span>|  
|<span data-ttu-id="4d1a2-150">A propriedade não está definida, mas esquerda em seu estado padrão.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-150">The property is not set, but left in its default state.</span></span>|<span data-ttu-id="4d1a2-151">Um **XmlUrlResolver** com **nulo** credenciais serão criadas e usadas pelo **XmlDocument** durante a resolução de nomes de arquivo, localizando externos DTDs, entidades, e esquemas e **nulo** as credenciais são usadas durante a edição de nós.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-151">An **XmlUrlResolver** with **null** credentials will be instantiated and used by the **XmlDocument** when resolving file names, locating external DTDs, entities, and schemas, and **null** credentials are used when editing nodes.</span></span>|<span data-ttu-id="4d1a2-152">O **XmlDocument** usa permissões anônimas para todas as ações.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-152">The **XmlDocument** uses anonymous permissions for all actions.</span></span>|  
  
 <span data-ttu-id="4d1a2-153">Esta tabela se aplica ao **XmlDocument.Load** método quando a entrada para o **carga** é um **XmlReader**e o **XmlDocument** é confiança parcial.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-153">This table applies to the **XmlDocument.Load** method when the input to the **Load** is an **XmlReader**, and the **XmlDocument** is semi-trusted.</span></span>  
  
|<span data-ttu-id="4d1a2-154">Propriedade de XmlResolver</span><span class="sxs-lookup"><span data-stu-id="4d1a2-154">XmlResolver property</span></span>|<span data-ttu-id="4d1a2-155">Função</span><span class="sxs-lookup"><span data-stu-id="4d1a2-155">Function</span></span>|<span data-ttu-id="4d1a2-156">Observações</span><span class="sxs-lookup"><span data-stu-id="4d1a2-156">Notes</span></span>|  
|--------------------------|--------------|-----------|  
|<span data-ttu-id="4d1a2-157">O **XmlResolver** classe usada pelo **XmlDocument** é o mesmo que está sendo usado pelo **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-157">The **XmlResolver** class used by the **XmlDocument** is the same one being used by the **XmlReader**.</span></span>|<span data-ttu-id="4d1a2-158">O **XmlDocument** usa o **XmlResolver** que foi atribuído para o **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-158">The **XmlDocument** uses the **XmlResolver** that was assigned to the **XmlReader**.</span></span><br /><br /> <span data-ttu-id="4d1a2-159">O **XmlDocument.Resolver** propriedade não pode ser definido, independentemente do **XmlDocument** nível, de confiança porque ele está ficando um **XmlResolver** do  **XmlReader**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-159">The **XmlDocument.Resolver** property cannot be set, regardless of the **XmlDocument** trust level, because it is getting an **XmlResolver** from the **XmlReader**.</span></span> <span data-ttu-id="4d1a2-160">Não é possível tentar substituir as configurações do **XmlReaders** **XmlResolver** definindo o **XmlResolver** propriedade o **XmlDocument**.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-160">You cannot attempt to override the settings of the **XmlReaders** **XmlResolver** by setting the **XmlResolver** property of the **XmlDocument**.</span></span>|<span data-ttu-id="4d1a2-161">O **XmlReader** pode ser o **XmlTextReader**, validando <xref:System.Xml.XmlReader>, ou um leitor personalizados.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-161">The **XmlReader** can be the **XmlTextReader**, validating <xref:System.Xml.XmlReader>, or a custom-written reader.</span></span> <span data-ttu-id="4d1a2-162">Se o leitor usado oferece suporte a resolução de entidade, as entidades externos são resolvidas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-162">If the reader used supports entity resolution, external entities are resolved.</span></span> <span data-ttu-id="4d1a2-163">Se o leitor passado não oferece suporte a referências a entidades, então as referências a entidades não são resolvidas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-163">If the reader passed in does not support entity references, then entity references are not resolved.</span></span>|  
  
 <span data-ttu-id="4d1a2-164">Defina o XmlResolver para conter as credenciais corretas permite acesso aos recursos externos.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-164">Setting the XmlResolver to contain the correct credentials allows access to external resources.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="4d1a2-165">Não é possível recuperar o **XmlResolver** propriedade.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-165">There is no way to retrieve the **XmlResolver** property.</span></span> <span data-ttu-id="4d1a2-166">Isso ajuda a impedir que um usuário de reutilizar um **XmlResolver** em que credenciais foram definidas.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-166">This helps to prevent a user from reusing an **XmlResolver** on which credentials have been set.</span></span> <span data-ttu-id="4d1a2-167">Além disso, se um **XmlTextReader** ou validar <xref:System.Xml.XmlReader> é usado para carregar o **XmlDocument** e **XmlDocument** tem um resolvedor de tiver sido definido, os resolvedores de Esses leitores não são armazenados em cache pelo **XmlDocument** depois que o **carga** fase, já que isso também apresenta um risco de segurança.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-167">Also, if an **XmlTextReader** or validating <xref:System.Xml.XmlReader> is used to load the **XmlDocument** and the **XmlDocument** has a resolver that has been set, the resolvers from these readers are not cached by the **XmlDocument** after the **Load** phase, since this also presents a security risk.</span></span>  
  
 <span data-ttu-id="4d1a2-168">Para obter mais informações, consulte a seção de Comentários da página de referência <xref:System.Xml.XmlResolver>.</span><span class="sxs-lookup"><span data-stu-id="4d1a2-168">For more information, see the Remarks section of the <xref:System.Xml.XmlResolver> reference page.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="4d1a2-169">Consulte também</span><span class="sxs-lookup"><span data-stu-id="4d1a2-169">See Also</span></span>  
 [<span data-ttu-id="4d1a2-170">XML Document Object Model (DOM)</span><span class="sxs-lookup"><span data-stu-id="4d1a2-170">XML Document Object Model (DOM)</span></span>](../../../../docs/standard/data/xml/xml-document-object-model-dom.md)
