---
title: Funções duráveis do Azure – aplicativos sem servidor
description: As funções duráveis do Azure estendem o tempo de execução do Azure Functions para permitir que os fluxos de trabalho com monitoração de estado no código.
author: cecilphillip
ms.author: cephilli
ms.date: 06/26/2018
ms.openlocfilehash: f7ee74926d6658042120113b49dc763383881423
ms.sourcegitcommit: 8699383914c24a0df033393f55db3369db728a7b
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/15/2019
ms.locfileid: "65643411"
---
# <a name="durable-azure-functions"></a><span data-ttu-id="b6f3e-103">Funções duráveis do Azure</span><span class="sxs-lookup"><span data-stu-id="b6f3e-103">Durable Azure functions</span></span>

<span data-ttu-id="b6f3e-104">Ao criar aplicativos sem servidor com o Azure Functions, suas operações normalmente serão criadas para executar de maneira sem monitoração de estado.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-104">When creating serverless applications with Azure Functions, your operations will typically be designed to run in a stateless manner.</span></span> <span data-ttu-id="b6f3e-105">O motivo para essa escolha de design é porque, como as escalas de plataforma, torna-se difícil saber quais servidores o código está em execução no.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-105">The reason for this design choice is because as the platform scales, it becomes difficult to know what servers the code is running on.</span></span> <span data-ttu-id="b6f3e-106">Também é difícil saber quantas instâncias estão ativas em qualquer momento determinado.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-106">It also becomes difficult to know how many instances are active at any given point.</span></span> <span data-ttu-id="b6f3e-107">No entanto, há classes de aplicativos que exigem o estado atual de um processo conhecido.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-107">However, there are classes of applications that require the current state of a process to be known.</span></span> <span data-ttu-id="b6f3e-108">Considere o processo de enviar um pedido para uma loja online.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-108">Consider the process of submitting an order to an online store.</span></span> <span data-ttu-id="b6f3e-109">A operação de check-out pode ser um fluxo de trabalho é composto de várias operações que precisam saber o estado do processo.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-109">The checkout operation might be a workflow that is composed of multiple operations that need to know the state of the process.</span></span> <span data-ttu-id="b6f3e-110">Essas informações podem incluir o estoque de produto, se o cliente tiver quaisquer créditos em sua conta e também os resultados do processamento de cartão de crédito.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-110">Such information may include the product inventory, if the customer has any credits on their account, and also the results of processing the credit card.</span></span> <span data-ttu-id="b6f3e-111">Essas operações pode ser facilmente seus próprios fluxos de trabalho internos ou até mesmo os serviços de sistemas de terceiros.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-111">These operations could easily be their own internal workflows or even services from third-party systems.</span></span>

<span data-ttu-id="b6f3e-112">Vários padrões de hoje existem que ajudam com a coordenação do estado do aplicativo entre sistemas internos e externos.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-112">Various patterns exist today that assist with the coordination of application state between internal and external systems.</span></span> <span data-ttu-id="b6f3e-113">É comum ao se deparar com soluções que dependem de sistemas centralizados de enfileiramento de mensagens, repositórios de chave-valor distribuídos ou bancos de dados compartilhados para gerenciar esse estado.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-113">It's common to come across solutions that rely on centralized queuing systems, distributed key-value stores, or shared databases to manage that state.</span></span> <span data-ttu-id="b6f3e-114">No entanto, esses são todos os outros recursos que agora precisam ser provisionados e gerenciados.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-114">However, these are all additional resources that now need to be provisioned and managed.</span></span> <span data-ttu-id="b6f3e-115">Em um ambiente sem servidor, seu código pode se tornar complicado tentando coordenar com esses recursos manualmente.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-115">In a serverless environment, your code could become cumbersome trying to coordinate with these resources manually.</span></span> <span data-ttu-id="b6f3e-116">O Azure Functions oferece uma alternativa para a criação de funções com estado chamadas de funções duráveis.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-116">Azure Functions offers an alternative for creating stateful functions called Durable Functions.</span></span>

<span data-ttu-id="b6f3e-117">As funções duráveis é uma extensão para o tempo de execução de funções do Azure que permite a definição de fluxos de trabalho com monitoração de estado no código.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-117">Durable Functions is an extension to the Azure Functions runtime that enables the definition of stateful workflows in code.</span></span> <span data-ttu-id="b6f3e-118">Ao dividir os fluxos de trabalho em atividades, a extensão funções duráveis pode gerenciar o estado, criar pontos de verificação de progresso e lidar com a distribuição das chamadas de função em servidores.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-118">By breaking down workflows into activities, the Durable Functions extension can manage state, create progress checkpoints, and handle the distribution of function calls across servers.</span></span> <span data-ttu-id="b6f3e-119">Em segundo plano, ele torna o uso de uma conta de armazenamento do Azure para manter o histórico de execução, agendam funções de atividade e recuperar respostas.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-119">In the background, it makes use of an Azure Storage account to persist execution history, schedule activity functions and retrieve responses.</span></span> <span data-ttu-id="b6f3e-120">Seu código sem servidor nunca deve interagir com as informações persistentes na conta de armazenamento e normalmente não é algo com que os desenvolvedores precisam interagir.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-120">Your serverless code should never interact with persisted information in that storage account, and is typically not something with which developers need to interact.</span></span>

## <a name="triggering-a-stateful-workflow"></a><span data-ttu-id="b6f3e-121">Disparar um fluxo de trabalho com monitoração de estado</span><span class="sxs-lookup"><span data-stu-id="b6f3e-121">Triggering a stateful workflow</span></span>

<span data-ttu-id="b6f3e-122">Fluxos de trabalho com monitoração de estado nas funções duráveis podem ser divididos em dois componentes intrínseco; gatilhos de orquestração e a atividade.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-122">Stateful workflows in Durable Functions can be broken down into two intrinsic components; orchestration and activity triggers.</span></span> <span data-ttu-id="b6f3e-123">Gatilhos e associações são os principais componentes usados pelas funções do Azure para habilitar funções sem servidor para ser notificado quando iniciar, receber de entrada e retornam os resultados.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-123">Triggers and bindings are core components used by Azure Functions to enable your serverless functions to be notified when to start, receive input, and return results.</span></span>

### <a name="working-with-the-orchestration-client"></a><span data-ttu-id="b6f3e-124">Trabalhando com o cliente de orquestração</span><span class="sxs-lookup"><span data-stu-id="b6f3e-124">Working with the Orchestration client</span></span>

<span data-ttu-id="b6f3e-125">Orquestrações são exclusivas em comparação com outros estilos de operações de disparadas no Azure Functions.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-125">Orchestrations are unique when compared to other styles of triggered operations in Azure Functions.</span></span> <span data-ttu-id="b6f3e-126">As funções duráveis permitem a execução de funções que podem levar horas ou mesmo dias para ser concluída.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-126">Durable Functions enables the execution of functions that may take hours or even days to complete.</span></span> <span data-ttu-id="b6f3e-127">Esse tipo de comportamento vem com a necessidade de capaz de verificar o status de uma orquestração em execução, preventivamente encerrar ou enviar notificações de eventos externos.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-127">That type of behavior comes with the need to able to check the status of a running orchestration, preemptively terminate, or send notifications of external events.</span></span>

<span data-ttu-id="b6f3e-128">Para tais casos, a extensão de funções duráveis fornece o `DurableOrchestrationClient` orquestrada de classe que permite que você interaja com funções.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-128">For such cases, the Durable Functions extension provides the `DurableOrchestrationClient` class that allows you to interact with orchestrated functions.</span></span> <span data-ttu-id="b6f3e-129">Obter acesso ao cliente de orquestração usando o `OrchestrationClientAttribute` associação.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-129">You get access to the orchestration client by using the `OrchestrationClientAttribute` binding.</span></span> <span data-ttu-id="b6f3e-130">Em geral, você deve incluir esse atributo com outro tipo de gatilho, como um `HttpTrigger` ou `ServiceBusTrigger`.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-130">Generally, you would include this attribute with another trigger type, such as an `HttpTrigger` or `ServiceBusTrigger`.</span></span> <span data-ttu-id="b6f3e-131">Depois que a função de origem foi disparada, o cliente de orquestração pode ser usado para iniciar uma função de orquestrador.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-131">Once the source function has been triggered, the orchestration client can be used to start an orchestrator function.</span></span>

```csharp
[FunctionName("KickOff")]
public static async Task<HttpResponseMessage> Run(
    [HttpTrigger(AuthorizationLevel.Function, "POST")]HttpRequestMessage req,
    [OrchestrationClient ] DurableOrchestrationClient<orchestrationClient>)
{
    OrderRequestData data = await req.Content.ReadAsAsync<OrderRequestData>();

    string instanceId = await orchestrationClient.StartNewAsync("PlaceOrder", data);

    return orchestrationClient.CreateCheckStatusResponse(req, instanceId);
}
```

### <a name="the-orchestrator-function"></a><span data-ttu-id="b6f3e-132">A função de orquestrador</span><span class="sxs-lookup"><span data-stu-id="b6f3e-132">The orchestrator function</span></span>

<span data-ttu-id="b6f3e-133">Anotar uma função com o OrchestrationTriggerAttribute nas marcas do Azure Functions que funcionam como uma função de orquestrador.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-133">Annotating a function with the OrchestrationTriggerAttribute in Azure Functions marks that function as an orchestrator function.</span></span> <span data-ttu-id="b6f3e-134">Ele é responsável por gerenciar as várias atividades que compõem seu fluxo de trabalho com monitoração de estado.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-134">It's responsible for managing the various activities that make up your stateful workflow.</span></span>

<span data-ttu-id="b6f3e-135">Funções de orquestrador não consegue fazer uso de associações que não seja o OrchestrationTriggerAttribute.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-135">Orchestrator functions are unable to make use of bindings other than the OrchestrationTriggerAttribute.</span></span> <span data-ttu-id="b6f3e-136">Esse atributo só pode ser usado com um tipo de parâmetro DurableOrchestrationContext.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-136">This attribute can only be used with a parameter type of DurableOrchestrationContext.</span></span> <span data-ttu-id="b6f3e-137">Não há outras entradas podem ser usadas, pois não há suporte para desserialização de entradas na assinatura de função.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-137">No other inputs can be used since deserialization of inputs in the function signature isn't supported.</span></span> <span data-ttu-id="b6f3e-138">Para obter as entradas fornecidas pelo cliente de orquestração, o GetInput\<T\> método deve ser usado.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-138">To get inputs provided by the orchestration client, the GetInput\<T\> method must be used.</span></span>

<span data-ttu-id="b6f3e-139">Além disso, os tipos de retorno das funções de orquestração devem ser vazio, Task ou um valor de serializável JSON.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-139">Also, the return types of orchestration functions must be either void, Task, or a JSON serializable value.</span></span>

> <span data-ttu-id="b6f3e-140">*Código de tratamento de erro foram deixado de fora para fins de brevidade*</span><span class="sxs-lookup"><span data-stu-id="b6f3e-140">*Error handling code has been left out for brevity*</span></span>

```csharp
[FunctionName("PlaceOrder")]
public static async Task<string> PlaceOrder([OrchestrationTrigger] DurableOrchestrationContext context)
{
    OrderRequestData orderData = context.GetInput<OrderRequestData>();

    await context.CallActivityAsync<bool>("CheckAndReserveInventory", orderData);
    await context.CallActivityAsync<string>("ProcessPayment", orderData);

    string trackingNumber = await context.CallActivityAsync<string>("ScheduleShipping", orderData);
    await context.CallActivityAsync<string>("EmailCustomer", trackingNumber);

    return trackingNumber;
}
```

<span data-ttu-id="b6f3e-141">Várias instâncias de uma orquestração podem ser iniciado e em execução ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-141">Multiple instances of an orchestration can be started and running at the same time.</span></span> <span data-ttu-id="b6f3e-142">Chamar o `StartNewAsync` método no `DurableOrchestrationClient` inicia uma nova instância da orquestração.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-142">Calling the `StartNewAsync` method on the `DurableOrchestrationClient` launches a new instance of the orchestration.</span></span> <span data-ttu-id="b6f3e-143">O método retorna um `Task<string>` que é concluída quando a orquestração foi iniciada.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-143">The method returns a `Task<string>` that completes when the orchestration has started.</span></span> <span data-ttu-id="b6f3e-144">Uma exceção do tipo `TimeoutException` obtém gerada se a orquestração ainda não começou dentro de 30 segundos.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-144">An exception of type `TimeoutException` gets thrown if the orchestration hasn't started within 30 seconds.</span></span>

<span data-ttu-id="b6f3e-145">Concluídas `Task<string>` de `StartNewAsync` deve conter a ID exclusiva da instância de orquestração.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-145">The completed `Task<string>` from `StartNewAsync` should contain the unique ID of the orchestration instance.</span></span> <span data-ttu-id="b6f3e-146">A ID da instância pode ser usada para invocar operações nessa orquestração específica.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-146">This instance ID can be used to invoke operations on that specific orchestration.</span></span> <span data-ttu-id="b6f3e-147">A orquestração pode ser consultada para obter o status ou enviada notificações de eventos.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-147">The orchestration can be queried for the status or sent event notifications.</span></span>

### <a name="the-activity-functions"></a><span data-ttu-id="b6f3e-148">As funções de atividade</span><span class="sxs-lookup"><span data-stu-id="b6f3e-148">The activity functions</span></span>

<span data-ttu-id="b6f3e-149">Funções de atividade são as operações discretas que são compostas juntas em uma função de orquestração para criar o fluxo de trabalho.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-149">Activity functions are the discrete operations that get composed together within an orchestration function to create the workflow.</span></span> <span data-ttu-id="b6f3e-150">Aqui é onde a maior parte do trabalho real podem ocorrer.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-150">Here is where most of actual work would take place.</span></span> <span data-ttu-id="b6f3e-151">Eles representam a lógica de negócios, processos em execução e as peças do quebra-cabeça para uma solução mais ampla de comprimento.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-151">They represent the business logic, long running processes, and the puzzle pieces to a larger solution.</span></span>

<span data-ttu-id="b6f3e-152">O `ActivityTriggerAttribute` é usado para anotar um parâmetro de função do tipo `DurableActivityContext`.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-152">The `ActivityTriggerAttribute` is used to annotate a function parameter of type `DurableActivityContext`.</span></span> <span data-ttu-id="b6f3e-153">Usando a anotação informa o tempo de execução que a função destina-se a ser usado como uma função de atividade.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-153">Using the annotation informs the runtime that the function is intended to be used as an activity function.</span></span> <span data-ttu-id="b6f3e-154">Valores de entrada para funções de atividade são recuperados usando o `GetInput<T>` método da `DurableActivityContext` parâmetro.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-154">Input values to activity functions are retrieved using the `GetInput<T>` method of the `DurableActivityContext` parameter.</span></span>

<span data-ttu-id="b6f3e-155">Semelhante às funções de orquestração, os tipos de retorno de funções de atividade devem ser vazio, Task ou um valor de serializável JSON.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-155">Similar to orchestration functions, the return types of activity functions must be either void, Task, or a JSON serializable value.</span></span>

<span data-ttu-id="b6f3e-156">Todas as exceções sem tratamento que obterem geradas dentro de funções de atividade obterá enviadas para a função de orquestrador de chamada e apresentadas como um `TaskFailedException`.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-156">Any unhandled exceptions that get thrown within activity functions will get sent up to the calling orchestrator function and presented as a `TaskFailedException`.</span></span> <span data-ttu-id="b6f3e-157">Neste ponto, o erro pode ser detectado e conectado o orchestrator e a atividade pode ser repetida.</span><span class="sxs-lookup"><span data-stu-id="b6f3e-157">At this point, the error can be caught and logged in the orchestrator, and the activity can be retried.</span></span>

```csharp
[FunctionName("CheckAndReserveInventory")]
public static bool CheckAndReserveInventory([ActivityTrigger] DurableActivityContext context)
{
    OrderRequestData orderData = context.GetInput<OrderRequestData>();

    // Connect to inventory system and try to reserve items
    return true;
}
```

## <a name="recommended-resources"></a><span data-ttu-id="b6f3e-158">Recursos recomendados</span><span class="sxs-lookup"><span data-stu-id="b6f3e-158">Recommended resources</span></span>

* [<span data-ttu-id="b6f3e-159">Funções duráveis</span><span class="sxs-lookup"><span data-stu-id="b6f3e-159">Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-overview)
* [<span data-ttu-id="b6f3e-160">Associações para funções duráveis</span><span class="sxs-lookup"><span data-stu-id="b6f3e-160">Bindings for Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-bindings)
* [<span data-ttu-id="b6f3e-161">Gerenciar instâncias nas funções duráveis</span><span class="sxs-lookup"><span data-stu-id="b6f3e-161">Manage instances in Durable Functions</span></span>](https://docs.microsoft.com/azure/azure-functions/durable-functions-instance-management)

>[!div class="step-by-step"]
><span data-ttu-id="b6f3e-162">[Anterior](event-grid.md)
>[Próximo](orchestration-patterns.md)</span><span class="sxs-lookup"><span data-stu-id="b6f3e-162">[Previous](event-grid.md)
[Next](orchestration-patterns.md)</span></span>
