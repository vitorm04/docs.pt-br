---
title: TPL e programação assíncrona .NET tradicional
ms.date: 03/30/2017
ms.technology: dotnet-standard
dev_langs:
- csharp
- vb
helpviewer_keywords:
- tasks, with other asynchronous models
ms.assetid: e7b31170-a156-433f-9f26-b1fc7cd1776f
ms.openlocfilehash: 498bde82d05259bdc561d08819d024090b0417f0
ms.sourcegitcommit: 279fb6e8d515df51676528a7424a1df2f0917116
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/27/2020
ms.locfileid: "92688022"
---
# <a name="tpl-and-traditional-net-asynchronous-programming"></a><span data-ttu-id="b514f-102">TPL e programação assíncrona .NET tradicional</span><span class="sxs-lookup"><span data-stu-id="b514f-102">TPL and traditional .NET asynchronous programming</span></span>

<span data-ttu-id="b514f-103">O .NET fornece os dois padrões padrão a seguir para executar operações assíncronas vinculadas a e/s e de computação:</span><span class="sxs-lookup"><span data-stu-id="b514f-103">.NET provides the following two standard patterns for performing I/O-bound and compute-bound asynchronous operations:</span></span>  
  
- <span data-ttu-id="b514f-104">O APM (modelo de programação assíncrona), no qual as operações assíncronas são representadas por um par de métodos Begin/End.</span><span class="sxs-lookup"><span data-stu-id="b514f-104">Asynchronous Programming Model (APM), in which asynchronous operations are represented by a pair of begin/end methods.</span></span> <span data-ttu-id="b514f-105">Por exemplo: <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> e <xref:System.IO.Stream.EndRead%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="b514f-105">For example: <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> and <xref:System.IO.Stream.EndRead%2A?displayProperty=nameWithType>.</span></span>  
  
- <span data-ttu-id="b514f-106">O padrão assíncrono baseado em evento (EAP), no qual as operações assíncronas são representadas por um par de método/evento que são nomeados `<OperationName>Async` e `<OperationName>Completed` .</span><span class="sxs-lookup"><span data-stu-id="b514f-106">Event-based asynchronous pattern (EAP), in which asynchronous operations are represented by a method/event pair that are named `<OperationName>Async` and `<OperationName>Completed`.</span></span> <span data-ttu-id="b514f-107">Por exemplo: <xref:System.Net.WebClient.DownloadStringAsync%2A?displayProperty=nameWithType> e <xref:System.Net.WebClient.DownloadStringCompleted?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="b514f-107">For example: <xref:System.Net.WebClient.DownloadStringAsync%2A?displayProperty=nameWithType> and <xref:System.Net.WebClient.DownloadStringCompleted?displayProperty=nameWithType>.</span></span>
  
<span data-ttu-id="b514f-108">A Biblioteca paralela de tarefas (TPL) pode ser usada de várias maneiras, junto com qualquer um dos padrões assíncronos.</span><span class="sxs-lookup"><span data-stu-id="b514f-108">The Task Parallel Library (TPL) can be used in various ways in conjunction with either of the asynchronous patterns.</span></span> <span data-ttu-id="b514f-109">Você pode expor operações de APM e EAP como `Task` objetos para consumidores de biblioteca ou pode expor os padrões de APM, mas usar `Task` objetos para implementá-los internamente.</span><span class="sxs-lookup"><span data-stu-id="b514f-109">You can expose both APM and EAP operations as `Task` objects to library consumers, or you can expose the APM patterns but use `Task` objects to implement them internally.</span></span> <span data-ttu-id="b514f-110">Em ambos os cenários, usando `Task` objetos, você pode simplificar o código e aproveitar as seguintes funcionalidades úteis:</span><span class="sxs-lookup"><span data-stu-id="b514f-110">In both scenarios, by using `Task` objects, you can simplify the code and take advantage of the following useful functionality:</span></span>  
  
- <span data-ttu-id="b514f-111">Registrar chamadas de retorno, sob a forma de continuação de tarefas, a qualquer momento após a tarefa ter começado.</span><span class="sxs-lookup"><span data-stu-id="b514f-111">Register callbacks, in the form of task continuations, at any time after the task has started.</span></span>  
  
- <span data-ttu-id="b514f-112">Coordenar várias operações executadas em resposta a um `Begin_` método usando os <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> métodos e <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> , ou os <xref:System.Threading.Tasks.Task.WaitAll%2A> métodos e <xref:System.Threading.Tasks.Task.WaitAny%2A> .</span><span class="sxs-lookup"><span data-stu-id="b514f-112">Coordinate multiple operations that execute in response to a `Begin_` method by using the <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> and <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> methods, or the <xref:System.Threading.Tasks.Task.WaitAll%2A> and <xref:System.Threading.Tasks.Task.WaitAny%2A> methods.</span></span>  
  
- <span data-ttu-id="b514f-113">Encapsular operações assíncronas associadas a e/s no mesmo `Task` objeto.</span><span class="sxs-lookup"><span data-stu-id="b514f-113">Encapsulate asynchronous I/O-bound and compute-bound operations in the same `Task` object.</span></span>  
  
- <span data-ttu-id="b514f-114">Monitore o status do `Task` objeto.</span><span class="sxs-lookup"><span data-stu-id="b514f-114">Monitor the status of the `Task` object.</span></span>  
  
- <span data-ttu-id="b514f-115">Marshaling do status de uma operação para um `Task` objeto usando <xref:System.Threading.Tasks.TaskCompletionSource%601> .</span><span class="sxs-lookup"><span data-stu-id="b514f-115">Marshal the status of an operation to a `Task` object by using <xref:System.Threading.Tasks.TaskCompletionSource%601>.</span></span>  
  
## <a name="wrap-apm-operations-in-a-task"></a><span data-ttu-id="b514f-116">Encapsular operações de APM em uma tarefa</span><span class="sxs-lookup"><span data-stu-id="b514f-116">Wrap APM operations in a Task</span></span>  

 <span data-ttu-id="b514f-117">As <xref:System.Threading.Tasks.TaskFactory?displayProperty=nameWithType> classes e <xref:System.Threading.Tasks.TaskFactory%601?displayProperty=nameWithType> fornecem várias sobrecargas dos <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A?displayProperty=nameWithType> <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%2A?displayProperty=nameWithType> métodos e que permitem encapsular um par de método Begin/End de APM em uma <xref:System.Threading.Tasks.Task> instância ou <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="b514f-117">Both the <xref:System.Threading.Tasks.TaskFactory?displayProperty=nameWithType> and <xref:System.Threading.Tasks.TaskFactory%601?displayProperty=nameWithType> classes provide several overloads of the <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A?displayProperty=nameWithType> and <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%2A?displayProperty=nameWithType> methods that let you encapsulate an APM begin/end method pair in one <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> instance.</span></span> <span data-ttu-id="b514f-118">As várias sobrecargas acomodam qualquer par de método Begin/End que tenha de zero a três parâmetros de entrada.</span><span class="sxs-lookup"><span data-stu-id="b514f-118">The various overloads accommodate any begin/end method pair that have from zero to three input parameters.</span></span>  
  
 <span data-ttu-id="b514f-119">Para pares que têm `End` métodos que retornam um valor (a `Function` em Visual Basic), use os métodos em <xref:System.Threading.Tasks.TaskFactory%601> que criar um <xref:System.Threading.Tasks.Task%601> .</span><span class="sxs-lookup"><span data-stu-id="b514f-119">For pairs that have `End` methods that return a value (a `Function` in Visual Basic), use the methods in <xref:System.Threading.Tasks.TaskFactory%601> that create a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="b514f-120">Para `End` métodos que retornam void (a `Sub` em Visual Basic), use os métodos em <xref:System.Threading.Tasks.TaskFactory> que criar um <xref:System.Threading.Tasks.Task> .</span><span class="sxs-lookup"><span data-stu-id="b514f-120">For `End` methods that return void (a `Sub` in Visual Basic), use the methods in <xref:System.Threading.Tasks.TaskFactory> that create a <xref:System.Threading.Tasks.Task>.</span></span>  
  
 <span data-ttu-id="b514f-121">Para os poucos casos em que o método `Begin` possui mais de três parâmetros ou contém parâmetros `ref` ou `out`, são fornecidas sobrecargas `FromAsync` adicionais que encapsulam apenas o método `End`.</span><span class="sxs-lookup"><span data-stu-id="b514f-121">For those few cases in which the `Begin` method has more than three parameters or contains `ref` or `out` parameters, additional `FromAsync` overloads that encapsulate only the `End` method are provided.</span></span>  
  
 <span data-ttu-id="b514f-122">O exemplo a seguir mostra a assinatura para a sobrecarga `FromAsync` que corresponde aos métodos <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> e <xref:System.IO.FileStream.EndRead%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="b514f-122">The following example shows the signature for the `FromAsync` overload that matches the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> and <xref:System.IO.FileStream.EndRead%2A?displayProperty=nameWithType> methods.</span></span>
  
 [!code-csharp[FromAsync#01](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#01)]
 [!code-vb[FromAsync#01](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#01)]  
  
<span data-ttu-id="b514f-123">Essa sobrecarga leva três parâmetros de entrada, da seguinte forma.</span><span class="sxs-lookup"><span data-stu-id="b514f-123">This overload takes three input parameters, as follows.</span></span> <span data-ttu-id="b514f-124">O primeiro parâmetro é um delegado <xref:System.Func%606> que corresponde à assinatura do método <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="b514f-124">The first parameter is a <xref:System.Func%606> delegate that matches the signature of the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="b514f-125">O segundo parâmetro é um delegado <xref:System.Func%602> que leva um <xref:System.IAsyncResult> e retorna um `TResult`.</span><span class="sxs-lookup"><span data-stu-id="b514f-125">The second parameter is a <xref:System.Func%602> delegate that takes an <xref:System.IAsyncResult> and returns a `TResult`.</span></span> <span data-ttu-id="b514f-126">Como <xref:System.IO.FileStream.EndRead%2A> retorna um inteiro, o compilador infere o tipo de `TResult` como <xref:System.Int32> e o tipo da tarefa como <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="b514f-126">Because <xref:System.IO.FileStream.EndRead%2A> returns an integer, the compiler infers the type of `TResult` as <xref:System.Int32> and the type of the task as <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="b514f-127">Os últimos quatro parâmetros são idênticos aos do método <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType>:</span><span class="sxs-lookup"><span data-stu-id="b514f-127">The last four parameters are identical to those in the <xref:System.IO.FileStream.BeginRead%2A?displayProperty=nameWithType> method:</span></span>  
  
- <span data-ttu-id="b514f-128">O buffer no qual armazenar os dados do arquivo.</span><span class="sxs-lookup"><span data-stu-id="b514f-128">The buffer in which to store the file data.</span></span>  
  
- <span data-ttu-id="b514f-129">O deslocamento no buffer no qual começar a gravar dados.</span><span class="sxs-lookup"><span data-stu-id="b514f-129">The offset in the buffer at which to begin writing data.</span></span>  
  
- <span data-ttu-id="b514f-130">O volume máximo de dados a serem lidos a partir do arquivo.</span><span class="sxs-lookup"><span data-stu-id="b514f-130">The maximum amount of data to read from the file.</span></span>  
  
- <span data-ttu-id="b514f-131">Um objeto opcional que armazena dados de estado definidos pelo usuário para passar para o retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="b514f-131">An optional object that stores user-defined state data to pass to the callback.</span></span>  
  
### <a name="use-continuewith-for-the-callback-functionality"></a><span data-ttu-id="b514f-132">Usar ContinueWith para a funcionalidade de retorno de chamada</span><span class="sxs-lookup"><span data-stu-id="b514f-132">Use ContinueWith for the callback functionality</span></span>

 <span data-ttu-id="b514f-133">Se você precisar acessar os dados no arquivo, em vez de apenas o número de bytes, o método <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A> não é suficiente.</span><span class="sxs-lookup"><span data-stu-id="b514f-133">If you require access to the data in the file, as opposed to just the number of bytes, the <xref:System.Threading.Tasks.TaskFactory.FromAsync%2A> method is not sufficient.</span></span> <span data-ttu-id="b514f-134">Em vez disso, use <xref:System.Threading.Tasks.Task>, cuja propriedade `Result` contém os dados do arquivo.</span><span class="sxs-lookup"><span data-stu-id="b514f-134">Instead, use <xref:System.Threading.Tasks.Task>, whose `Result` property contains the file data.</span></span> <span data-ttu-id="b514f-135">Você pode fazer isso adicionando uma continuação à tarefa original.</span><span class="sxs-lookup"><span data-stu-id="b514f-135">You can do this by adding a continuation to the original task.</span></span> <span data-ttu-id="b514f-136">A continuação executa o trabalho que normalmente seria executado pelo delegado <xref:System.AsyncCallback>.</span><span class="sxs-lookup"><span data-stu-id="b514f-136">The continuation performs the work that would typically be performed by the <xref:System.AsyncCallback> delegate.</span></span> <span data-ttu-id="b514f-137">É invocada quando o antecedente é concluído e o buffer de dados foi preenchido.</span><span class="sxs-lookup"><span data-stu-id="b514f-137">It is invoked when the antecedent completes, and the data buffer has been filled.</span></span> <span data-ttu-id="b514f-138">(O objeto <xref:System.IO.FileStream> deve ser fechado antes de retornar).</span><span class="sxs-lookup"><span data-stu-id="b514f-138">(The <xref:System.IO.FileStream> object should be closed before returning.)</span></span>  
  
 <span data-ttu-id="b514f-139">O exemplo a seguir mostra como retornar um <xref:System.Threading.Tasks.Task> que encapsula o `BeginRead` / `EndRead` par da <xref:System.IO.FileStream> classe.</span><span class="sxs-lookup"><span data-stu-id="b514f-139">The following example shows how to return a <xref:System.Threading.Tasks.Task> that encapsulates the `BeginRead`/`EndRead` pair of the <xref:System.IO.FileStream> class.</span></span>  
  
 [!code-csharp[FromAsync#03](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#03)]
 [!code-vb[FromAsync#03](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#03)]  
  
 <span data-ttu-id="b514f-140">O método pode ser chamado, da seguinte maneira.</span><span class="sxs-lookup"><span data-stu-id="b514f-140">The method can then be called, as follows.</span></span>  
  
 [!code-csharp[FromAsync#04](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#04)]
 [!code-vb[FromAsync#04](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#04)]  
  
### <a name="provide-custom-state-data"></a><span data-ttu-id="b514f-141">Fornecer dados de estado personalizados</span><span class="sxs-lookup"><span data-stu-id="b514f-141">Provide custom state data</span></span>

 <span data-ttu-id="b514f-142">Em operações <xref:System.IAsyncResult> típicas, se o seu delegado <xref:System.AsyncCallback> precisar de alguns dados de estado personalizados, você deve passá-lo através do último parâmetro no método `Begin`, para que os dados possam ser empacotados no objeto <xref:System.IAsyncResult>, que é eventualmente passado para o método de retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="b514f-142">In typical <xref:System.IAsyncResult> operations, if your <xref:System.AsyncCallback> delegate requires some custom state data, you have to pass it in through the last parameter in the `Begin` method, so that the data can be packaged into the <xref:System.IAsyncResult> object that is eventually passed to the callback method.</span></span> <span data-ttu-id="b514f-143">Normalmente, isso não é necessário quando os métodos `FromAsync` são usados.</span><span class="sxs-lookup"><span data-stu-id="b514f-143">This is typically not required when the `FromAsync` methods are used.</span></span> <span data-ttu-id="b514f-144">Se os dados personalizados forem conhecidos pela continuação, ele poderão ser capturados diretamente no delegado de continuação.</span><span class="sxs-lookup"><span data-stu-id="b514f-144">If the custom data is known to the continuation, then it can be captured directly in the continuation delegate.</span></span> <span data-ttu-id="b514f-145">O exemplo a seguir se assemelha ao exemplo anterior, mas em vez de examinar a propriedade `Result` do antecedente, a continuação examina os dados de estado personalizados que são diretamente acessíveis ao delegado de usuário da continuação.</span><span class="sxs-lookup"><span data-stu-id="b514f-145">The following example resembles the previous example, but instead of examining the `Result` property of the antecedent, the continuation examines the custom state data that is directly accessible to the user delegate of the continuation.</span></span>  
  
 [!code-csharp[FromAsync#05](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#05)]
 [!code-vb[FromAsync#05](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#05)]  
  
### <a name="synchronize-multiple-fromasync-tasks"></a><span data-ttu-id="b514f-146">Sincronizar várias tarefas do FromAsync</span><span class="sxs-lookup"><span data-stu-id="b514f-146">Synchronize multiple FromAsync tasks</span></span>

 <span data-ttu-id="b514f-147">Os métodos estáticos <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> e <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> proporcionam maior flexibilidade quando usados em conjunto com os métodos `FromAsync`.</span><span class="sxs-lookup"><span data-stu-id="b514f-147">The static <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAll%2A> and <xref:System.Threading.Tasks.TaskFactory.ContinueWhenAny%2A> methods provide added flexibility when used in conjunction with the `FromAsync` methods.</span></span> <span data-ttu-id="b514f-148">O exemplo a seguir mostra como iniciar várias operações de E/S assíncronas e aguarda que todas elas sejam concluídas antes de executar a continuação.</span><span class="sxs-lookup"><span data-stu-id="b514f-148">The following example shows how to initiate multiple asynchronous I/O operations, and then wait for all of them to complete before you execute the continuation.</span></span>  
  
 [!code-csharp[FromAsync#06](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#06)]
 [!code-vb[FromAsync#06](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#06)]  
  
### <a name="fromasync-tasks-for-only-the-end-method"></a><span data-ttu-id="b514f-149">Tarefas FromAsync apenas para o método End</span><span class="sxs-lookup"><span data-stu-id="b514f-149">FromAsync tasks for only the End method</span></span>

 <span data-ttu-id="b514f-150">Para esses poucos casos em que o `Begin` método requer mais de três parâmetros de entrada ou `ref` tem `out` parâmetros ou, você pode usar as `FromAsync` sobrecargas, por exemplo, <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%28System.IAsyncResult%2CSystem.Func%7BSystem.IAsyncResult%2C%600%7D%29?displayProperty=nameWithType> , que representam apenas o `End` método.</span><span class="sxs-lookup"><span data-stu-id="b514f-150">For those few cases in which the `Begin` method requires more than three input parameters or has `ref` or `out` parameters, you can use the `FromAsync` overloads, for example, <xref:System.Threading.Tasks.TaskFactory%601.FromAsync%28System.IAsyncResult%2CSystem.Func%7BSystem.IAsyncResult%2C%600%7D%29?displayProperty=nameWithType>, that represent only the `End` method.</span></span> <span data-ttu-id="b514f-151">Esses métodos também podem ser usados em qualquer cenário no qual você passa um <xref:System.IAsyncResult> e deseja encapsulá-lo em uma tarefa.</span><span class="sxs-lookup"><span data-stu-id="b514f-151">These methods can also be used in any scenario in which you're passed an <xref:System.IAsyncResult> and want to encapsulate it in a Task.</span></span>  
  
 [!code-csharp[FromAsync#07](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#07)]
 [!code-vb[FromAsync#07](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#07)]  
  
### <a name="start-and-cancel-fromasync-tasks"></a><span data-ttu-id="b514f-152">Iniciar e cancelar tarefas de FromAsync</span><span class="sxs-lookup"><span data-stu-id="b514f-152">Start and cancel FromAsync tasks</span></span>

 <span data-ttu-id="b514f-153">A tarefa retornada por um `FromAsync` método tem um status de `WaitingForActivation` e será iniciada pelo sistema em algum momento depois que a tarefa for criada.</span><span class="sxs-lookup"><span data-stu-id="b514f-153">The task returned by a `FromAsync` method has a status of `WaitingForActivation` and will be started by the system at some point after the task is created.</span></span> <span data-ttu-id="b514f-154">Se você tentar chamar Start em tal tarefa, uma exceção será gerada.</span><span class="sxs-lookup"><span data-stu-id="b514f-154">If you attempt to call Start on such a task, an exception will be raised.</span></span>  
  
 <span data-ttu-id="b514f-155">Não é possível cancelar uma `FromAsync` tarefa, pois as APIs do .NET subjacentes atualmente não dão suporte ao cancelamento em andamento de e/s de arquivo ou de rede.</span><span class="sxs-lookup"><span data-stu-id="b514f-155">You cannot cancel a `FromAsync` task, because the underlying .NET APIs currently do not support in-progress cancellation of file or network I/O.</span></span> <span data-ttu-id="b514f-156">Você pode adicionar funcionalidades de cancelamento a um método que encapsula uma chamada `FromAsync`, mas você só pode responder ao cancelamento antes que `FromAsync` seja chamado ou depois de concluído (por exemplo, em uma tarefa de continuação).</span><span class="sxs-lookup"><span data-stu-id="b514f-156">You can add cancellation functionality to a method that encapsulates a `FromAsync` call, but you can only respond to the cancellation before `FromAsync` is called or after it completed (for example, in a continuation task).</span></span>  
  
 <span data-ttu-id="b514f-157">Algumas classes que dão suporte ao EAP, por exemplo, <xref:System.Net.WebClient>, dão suporte ao cancelamento e você pode integrar essa funcionalidade de cancelamento nativa usando tokens de cancelamento.</span><span class="sxs-lookup"><span data-stu-id="b514f-157">Some classes that support EAP, for example, <xref:System.Net.WebClient>, do support cancellation, and you can integrate that native cancellation functionality by using cancellation tokens.</span></span>  
  
## <a name="expose-complex-eap-operations-as-tasks"></a><span data-ttu-id="b514f-158">Expor operações EAP complexas como tarefas</span><span class="sxs-lookup"><span data-stu-id="b514f-158">Expose complex EAP operations As tasks</span></span>

 <span data-ttu-id="b514f-159">O TPL não fornece nenhum método especificamente projetado para encapsular uma operação assíncrona baseada em eventos da mesma maneira que a família de métodos `FromAsync` envolve o padrão <xref:System.IAsyncResult>.</span><span class="sxs-lookup"><span data-stu-id="b514f-159">The TPL does not provide any methods that are specifically designed to encapsulate an event-based asynchronous operation in the same way that the `FromAsync` family of methods wrap the <xref:System.IAsyncResult> pattern.</span></span> <span data-ttu-id="b514f-160">No entanto, o TPL fornece a classe <xref:System.Threading.Tasks.TaskCompletionSource%601?displayProperty=nameWithType>, que pode ser usada para representar qualquer conjunto arbitrário de operações como <xref:System.Threading.Tasks.Task%601>.</span><span class="sxs-lookup"><span data-stu-id="b514f-160">However, the TPL does provide the <xref:System.Threading.Tasks.TaskCompletionSource%601?displayProperty=nameWithType> class, which can be used to represent any arbitrary set of operations as a <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="b514f-161">As operações podem ser síncronas ou assíncronas, e podem estar ligadas à E/S ou computação, ou ambos.</span><span class="sxs-lookup"><span data-stu-id="b514f-161">The operations may be synchronous or asynchronous, and may be I/O bound or compute-bound, or both.</span></span>  
  
 <span data-ttu-id="b514f-162">O exemplo a seguir mostra como usar um <xref:System.Threading.Tasks.TaskCompletionSource%601> para expor um conjunto de operações <xref:System.Net.WebClient> assíncronas ao código do cliente como um <xref:System.Threading.Tasks.Task%601> básico.</span><span class="sxs-lookup"><span data-stu-id="b514f-162">The following example shows how to use a <xref:System.Threading.Tasks.TaskCompletionSource%601> to expose a set of asynchronous <xref:System.Net.WebClient> operations to client code as a basic <xref:System.Threading.Tasks.Task%601>.</span></span> <span data-ttu-id="b514f-163">O método permite que você insira uma série de URLs da Web e um termo ou nome para pesquisar e, em seguida, retorna o número de vezes que o termo de pesquisa ocorre em cada site.</span><span class="sxs-lookup"><span data-stu-id="b514f-163">The method lets you enter an array of Web URLs, and a term or name to search for, and then returns the number of times the search term occurs on each site.</span></span>  
  
 [!code-csharp[FromAsync#10](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/snippet10.cs#10)]
 [!code-vb[FromAsync#10](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/snippet10.vb#10)]  
  
 <span data-ttu-id="b514f-164">Para um exemplo mais completo, que inclui a manipulação de exceções adicionais e mostra como chamar o método do código do cliente, confira [Como encapsular padrões de EAP em uma tarefa](how-to-wrap-eap-patterns-in-a-task.md).</span><span class="sxs-lookup"><span data-stu-id="b514f-164">For a more complete example, which includes additional exception handling and shows how to call the method from client code, see [How to: Wrap EAP Patterns in a Task](how-to-wrap-eap-patterns-in-a-task.md).</span></span>  
  
 <span data-ttu-id="b514f-165">Lembre-se de que qualquer tarefa criada por um <xref:System.Threading.Tasks.TaskCompletionSource%601> será iniciada por ela `TaskCompletionSource` e, portanto, o código do usuário não deve chamar o `Start` método nessa tarefa.</span><span class="sxs-lookup"><span data-stu-id="b514f-165">Remember that any task that's created by a <xref:System.Threading.Tasks.TaskCompletionSource%601> will be started by that `TaskCompletionSource` and, therefore, user code should not call the `Start` method on that task.</span></span>  
  
## <a name="implement-the-apm-pattern-by-using-tasks"></a><span data-ttu-id="b514f-166">Implementar o padrão APM usando tarefas</span><span class="sxs-lookup"><span data-stu-id="b514f-166">Implement the APM pattern by using tasks</span></span>

 <span data-ttu-id="b514f-167">Em alguns cenários, pode ser desejável expor diretamente o <xref:System.IAsyncResult> padrão usando pares de método Begin/End em uma API.</span><span class="sxs-lookup"><span data-stu-id="b514f-167">In some scenarios, it may be desirable to directly expose the <xref:System.IAsyncResult> pattern by using begin/end method pairs in an API.</span></span> <span data-ttu-id="b514f-168">Por exemplo, talvez você queira manter a consistência com APIs existentes, ou você pode ter ferramentas automatizadas que exigem esse padrão.</span><span class="sxs-lookup"><span data-stu-id="b514f-168">For example, you may want to maintain consistency with existing APIs, or you may have automated tools that require this pattern.</span></span> <span data-ttu-id="b514f-169">Nesses casos, você pode usar `Task` objetos para simplificar como o padrão APM é implementado internamente.</span><span class="sxs-lookup"><span data-stu-id="b514f-169">In such cases, you can use `Task` objects to simplify how the APM pattern is implemented internally.</span></span>  
  
 <span data-ttu-id="b514f-170">O exemplo a seguir mostra como usar tarefas para implementar um par de métodos de início/término do APM para um método associado de computação de longa execução.</span><span class="sxs-lookup"><span data-stu-id="b514f-170">The following example shows how to use tasks to implement an APM begin/end method pair for a long-running compute-bound method.</span></span>  
  
 [!code-csharp[FromAsync#09](../../../samples/snippets/csharp/VS_Snippets_Misc/fromasync/cs/fromasync.cs#09)]
 [!code-vb[FromAsync#09](../../../samples/snippets/visualbasic/VS_Snippets_Misc/fromasync/vb/module1.vb#09)]  
  
## <a name="use-the-streamextensions-sample-code"></a><span data-ttu-id="b514f-171">Usar o código de exemplo StreamExtensions</span><span class="sxs-lookup"><span data-stu-id="b514f-171">Use the StreamExtensions sample code</span></span>

 <span data-ttu-id="b514f-172">O arquivo *StreamExtensions.cs* , no repositório de [extras .net Standard extensões paralelas](/samples/dotnet/samples/parallel-programming-extensions-extras-cs/) , contém várias implementações de referência que usam `Task` objetos para e/s de rede e de arquivo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="b514f-172">The *StreamExtensions.cs* file, in the [.NET Standard parallel extensions extras](/samples/dotnet/samples/parallel-programming-extensions-extras-cs/) repository, contains several reference implementations that use `Task` objects for asynchronous file and network I/O.</span></span>
  
## <a name="see-also"></a><span data-ttu-id="b514f-173">Veja também</span><span class="sxs-lookup"><span data-stu-id="b514f-173">See also</span></span>

- [<span data-ttu-id="b514f-174">Biblioteca de tarefas paralelas (TPL)</span><span class="sxs-lookup"><span data-stu-id="b514f-174">Task Parallel Library (TPL)</span></span>](task-parallel-library-tpl.md)
