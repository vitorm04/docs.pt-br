---
title: Diretrizes para coleções
ms.date: 03/30/2017
ms.technology: dotnet-standard
ms.assetid: 297b8f1d-b11f-4dc6-960a-8e990817304e
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: 3571ebb2fdd2bcdfd8be1f0087d096e01f18790a
ms.sourcegitcommit: 6eac9a01ff5d70c6d18460324c016a3612c5e268
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/16/2018
ms.locfileid: "45674085"
---
# <a name="guidelines-for-collections"></a><span data-ttu-id="d9d95-102">Diretrizes para coleções</span><span class="sxs-lookup"><span data-stu-id="d9d95-102">Guidelines for Collections</span></span>
<span data-ttu-id="d9d95-103">Qualquer tipo projetado especificamente para manipular um grupo de objetos que têm algumas características comuns pode ser considerado uma coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-103">Any type designed specifically to manipulate a group of objects having some common characteristic can be considered a collection.</span></span> <span data-ttu-id="d9d95-104">Quase sempre é apropriado para esses tipos implementar <xref:System.Collections.IEnumerable> ou <xref:System.Collections.Generic.IEnumerable%601>, portanto, nesta seção consideramos apenas tipos que implementam uma ou ambas dessas interfaces para ser coleções.</span><span class="sxs-lookup"><span data-stu-id="d9d95-104">It is almost always appropriate for such types to implement <xref:System.Collections.IEnumerable> or <xref:System.Collections.Generic.IEnumerable%601>, so in this section we only consider types implementing one or both of those interfaces to be collections.</span></span>  
  
 <span data-ttu-id="d9d95-105">**X DO NOT** usar coleções sem rigidez de tipos em APIs públicas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-105">**X DO NOT** use weakly typed collections in public APIs.</span></span>  
  
 <span data-ttu-id="d9d95-106">O tipo de todos os valores de retorno e parâmetros que representam itens de coleta deve ser do tipo de item exato, não qualquer um dos seus tipos base (isso se aplica apenas a membros públicos da coleção).</span><span class="sxs-lookup"><span data-stu-id="d9d95-106">The type of all return values and parameters representing collection items should be the exact item type, not any of its base types (this applies only to public members of the collection).</span></span>  
  
 <span data-ttu-id="d9d95-107">**X DO NOT** usar <xref:System.Collections.ArrayList> ou <xref:System.Collections.Generic.List%601> em APIs públicas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-107">**X DO NOT** use <xref:System.Collections.ArrayList> or <xref:System.Collections.Generic.List%601> in public APIs.</span></span>  
  
 <span data-ttu-id="d9d95-108">Esses tipos são estruturas de dados projetadas para ser usado na implementação interna, não em APIs públicas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-108">These types are data structures designed to be used in internal implementation, not in public APIs.</span></span> <span data-ttu-id="d9d95-109">`List<T>` é otimizado para desempenho e energia às custas de cleanness das APIs e flexibilidade.</span><span class="sxs-lookup"><span data-stu-id="d9d95-109">`List<T>` is optimized for performance and power at the cost of cleanness of the APIs and flexibility.</span></span> <span data-ttu-id="d9d95-110">Por exemplo, se você retornar `List<T>`, você não será capaz de receber notificações quando o código do cliente modifica a coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-110">For example, if you return `List<T>`, you will not ever be able to receive notifications when client code modifies the collection.</span></span> <span data-ttu-id="d9d95-111">Além disso, `List<T>` expõe o número de membros, como <xref:System.Collections.Generic.List%601.BinarySearch%2A>, que não são úteis ou aplicável em muitos cenários.</span><span class="sxs-lookup"><span data-stu-id="d9d95-111">Also, `List<T>` exposes many members, such as <xref:System.Collections.Generic.List%601.BinarySearch%2A>, that are not useful or applicable in many scenarios.</span></span> <span data-ttu-id="d9d95-112">As seções a seguir descrevem os tipos (abstrações) desenvolvidos especificamente para uso em APIs públicas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-112">The following two sections describe types (abstractions) designed specifically for use in public APIs.</span></span>  
  
 <span data-ttu-id="d9d95-113">**X DO NOT** usar `Hashtable` ou `Dictionary<TKey,TValue>` em APIs públicas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-113">**X DO NOT** use `Hashtable` or `Dictionary<TKey,TValue>` in public APIs.</span></span>  
  
 <span data-ttu-id="d9d95-114">Esses tipos são estruturas de dados projetadas para ser usado na implementação interna.</span><span class="sxs-lookup"><span data-stu-id="d9d95-114">These types are data structures designed to be used in internal implementation.</span></span> <span data-ttu-id="d9d95-115">Use as APIs públicas <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, ou um tipo personalizado implementando uma ou ambas as interfaces.</span><span class="sxs-lookup"><span data-stu-id="d9d95-115">Public APIs should use <xref:System.Collections.IDictionary>, `IDictionary <TKey, TValue>`, or a custom type implementing one or both of the interfaces.</span></span>  
  
 <span data-ttu-id="d9d95-116">**X DO NOT** usar <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, ou qualquer outro tipo que implementa uma dessas interfaces, exceto como o tipo de retorno de um `GetEnumerator` método.</span><span class="sxs-lookup"><span data-stu-id="d9d95-116">**X DO NOT** use <xref:System.Collections.Generic.IEnumerator%601>, <xref:System.Collections.IEnumerator>, or any other type that implements either of these interfaces, except as the return type of a `GetEnumerator` method.</span></span>  
  
 <span data-ttu-id="d9d95-117">Tipos de retorno diferente de enumeradores métodos `GetEnumerator` não pode ser usado com o `foreach` instrução.</span><span class="sxs-lookup"><span data-stu-id="d9d95-117">Types returning enumerators from methods other than `GetEnumerator` cannot be used with the `foreach` statement.</span></span>  
  
 <span data-ttu-id="d9d95-118">**X DO NOT** implementar `IEnumerator<T>` e `IEnumerable<T>` do mesmo tipo.</span><span class="sxs-lookup"><span data-stu-id="d9d95-118">**X DO NOT** implement both `IEnumerator<T>` and `IEnumerable<T>` on the same type.</span></span> <span data-ttu-id="d9d95-119">O mesmo se aplica às interfaces não genéricas `IEnumerator` e `IEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-119">The same applies to the nongeneric interfaces `IEnumerator` and `IEnumerable`.</span></span>  
  
## <a name="collection-parameters"></a><span data-ttu-id="d9d95-120">Parâmetros de coleta</span><span class="sxs-lookup"><span data-stu-id="d9d95-120">Collection Parameters</span></span>  
 <span data-ttu-id="d9d95-121">**✓ DO** usar o possível tipo especializado de menos como um tipo de parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d9d95-121">**✓ DO** use the least-specialized type possible as a parameter type.</span></span> <span data-ttu-id="d9d95-122">A maioria dos membros levando coleções como usam parâmetros de `IEnumerable<T>` interface.</span><span class="sxs-lookup"><span data-stu-id="d9d95-122">Most members taking collections as parameters use the `IEnumerable<T>` interface.</span></span>  
  
 <span data-ttu-id="d9d95-123">**X AVOID** usando <xref:System.Collections.Generic.ICollection%601> ou <xref:System.Collections.ICollection> como um parâmetro apenas para acessar o `Count` propriedade.</span><span class="sxs-lookup"><span data-stu-id="d9d95-123">**X AVOID** using <xref:System.Collections.Generic.ICollection%601> or <xref:System.Collections.ICollection> as a parameter just to access the `Count` property.</span></span>  
  
 <span data-ttu-id="d9d95-124">Em vez disso, considere o uso de `IEnumerable<T>` ou `IEnumerable` e dinamicamente, verificando se o objeto implementa `ICollection<T>` ou `ICollection`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-124">Instead, consider using `IEnumerable<T>` or `IEnumerable` and dynamically checking whether the object implements `ICollection<T>` or `ICollection`.</span></span>  
  
## <a name="collection-properties-and-return-values"></a><span data-ttu-id="d9d95-125">Coleção de propriedades e valores de retorno</span><span class="sxs-lookup"><span data-stu-id="d9d95-125">Collection Properties and Return Values</span></span>  
 <span data-ttu-id="d9d95-126">**X DO NOT** fornecem propriedades de coleção configurável.</span><span class="sxs-lookup"><span data-stu-id="d9d95-126">**X DO NOT** provide settable collection properties.</span></span>  
  
 <span data-ttu-id="d9d95-127">Os usuários podem substituir o conteúdo da coleção de limpar a coleção primeiro e, em seguida, adicionando o novo conteúdo.</span><span class="sxs-lookup"><span data-stu-id="d9d95-127">Users can replace the contents of the collection by clearing the collection first and then adding the new contents.</span></span> <span data-ttu-id="d9d95-128">Se substituir toda a coleção é um cenário comum, recomenda-se de fornecer o `AddRange` método na coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-128">If replacing the whole collection is a common scenario, consider providing the `AddRange` method on the collection.</span></span>  
  
 <span data-ttu-id="d9d95-129">**✓ DO** usar `Collection<T>` ou uma subclasse de `Collection<T>` para propriedades ou retornam valores que representam coleções de leitura/gravação.</span><span class="sxs-lookup"><span data-stu-id="d9d95-129">**✓ DO** use `Collection<T>` or a subclass of `Collection<T>` for properties or return values representing read/write collections.</span></span>  
  
 <span data-ttu-id="d9d95-130">Se `Collection<T>` não atender a alguns requisitos (por exemplo, a coleção não deve implementar <xref:System.Collections.IList>), usar uma coleção personalizada implementando `IEnumerable<T>`, `ICollection<T>`, ou <xref:System.Collections.Generic.IList%601>.</span><span class="sxs-lookup"><span data-stu-id="d9d95-130">If `Collection<T>` does not meet some requirement (e.g., the collection must not implement <xref:System.Collections.IList>), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or <xref:System.Collections.Generic.IList%601>.</span></span>  
  
 <span data-ttu-id="d9d95-131">**✓ DO** usar <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, uma subclasse de `ReadOnlyCollection<T>`, ou em casos raros `IEnumerable<T>` para propriedades ou retorno de valores que representam coleções somente leitura.</span><span class="sxs-lookup"><span data-stu-id="d9d95-131">**✓ DO** use <xref:System.Collections.ObjectModel.ReadOnlyCollection%601>, a subclass of `ReadOnlyCollection<T>`, or in rare cases `IEnumerable<T>` for properties or return values representing read-only collections.</span></span>  
  
 <span data-ttu-id="d9d95-132">Em geral, prefira `ReadOnlyCollection<T>`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-132">In general, prefer `ReadOnlyCollection<T>`.</span></span> <span data-ttu-id="d9d95-133">Se ele não atender a algum requisito (por exemplo, a coleção não deve implementar `IList`), usar uma coleção personalizada implementando `IEnumerable<T>`, `ICollection<T>`, ou `IList<T>`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-133">If it does not meet some requirement (e.g., the collection must not implement `IList`), use a custom collection by implementing `IEnumerable<T>`, `ICollection<T>`, or `IList<T>`.</span></span> <span data-ttu-id="d9d95-134">Se você implementar uma coleção somente leitura personalizada, implemente `ICollection<T>.IsReadOnly` para retornar `true`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-134">If you do implement a custom read-only collection, implement `ICollection<T>.IsReadOnly` to return `true`.</span></span>  
  
 <span data-ttu-id="d9d95-135">Em casos onde você está-se de que o único cenário em que você nunca deseja dar suporte a iteração de somente avanço, você pode simplesmente usar `IEnumerable<T>`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-135">In cases where you are sure that the only scenario you will ever want to support is forward-only iteration, you can simply use `IEnumerable<T>`.</span></span>  
  
 <span data-ttu-id="d9d95-136">**✓ CONSIDER** usando as subclasses de coleções genéricas de base em vez de usar as coleções diretamente.</span><span class="sxs-lookup"><span data-stu-id="d9d95-136">**✓ CONSIDER** using subclasses of generic base collections instead of using the collections directly.</span></span>  
  
 <span data-ttu-id="d9d95-137">Isso permite que um nome melhor e para a adição de membros de auxiliar que não estão presentes nos tipos de coleção base.</span><span class="sxs-lookup"><span data-stu-id="d9d95-137">This allows for a better name and for adding helper members that are not present on the base collection types.</span></span> <span data-ttu-id="d9d95-138">Isso é especialmente aplicável para APIs de alto nível.</span><span class="sxs-lookup"><span data-stu-id="d9d95-138">This is especially applicable to high-level APIs.</span></span>  
  
 <span data-ttu-id="d9d95-139">**✓ CONSIDER** retornando uma subclasse de `Collection<T>` ou `ReadOnlyCollection<T>` de propriedades e métodos muito mais usados.</span><span class="sxs-lookup"><span data-stu-id="d9d95-139">**✓ CONSIDER** returning a subclass of `Collection<T>` or `ReadOnlyCollection<T>` from very commonly used methods and properties.</span></span>  
  
 <span data-ttu-id="d9d95-140">Isso tornará possível para que você possa adicionar métodos auxiliares ou alterar a implementação da coleção no futuro.</span><span class="sxs-lookup"><span data-stu-id="d9d95-140">This will make it possible for you to add helper methods or change the collection implementation in the future.</span></span>  
  
 <span data-ttu-id="d9d95-141">**✓ CONSIDER** usando uma coleção com chave se os itens armazenados na coleção têm chaves exclusivas (nomes, IDs, etc.).</span><span class="sxs-lookup"><span data-stu-id="d9d95-141">**✓ CONSIDER** using a keyed collection if the items stored in the collection have unique keys (names, IDs, etc.).</span></span> <span data-ttu-id="d9d95-142">Coleções de chave são coleções que podem ser indexadas por um número inteiro e uma chave e geralmente são implementadas herdando de `KeyedCollection<TKey,TItem>`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-142">Keyed collections are collections that can be indexed by both an integer and a key and are usually implemented by inheriting from `KeyedCollection<TKey,TItem>`.</span></span>  
  
 <span data-ttu-id="d9d95-143">Coleções de chave geralmente tem capacidades maiores de memória e não devem ser usadas se a sobrecarga de memória supera os benefícios de ter as chaves.</span><span class="sxs-lookup"><span data-stu-id="d9d95-143">Keyed collections usually have larger memory footprints and should not be used if the memory overhead outweighs the benefits of having the keys.</span></span>  
  
 <span data-ttu-id="d9d95-144">**X DO NOT** retornar valores nulos de propriedades de coleção ou de métodos de retorno de coleções.</span><span class="sxs-lookup"><span data-stu-id="d9d95-144">**X DO NOT** return null values from collection properties or from methods returning collections.</span></span> <span data-ttu-id="d9d95-145">Em vez disso, retorne uma coleção vazia ou uma matriz vazia.</span><span class="sxs-lookup"><span data-stu-id="d9d95-145">Return an empty collection or an empty array instead.</span></span>  
  
 <span data-ttu-id="d9d95-146">A regra geral é que as coleções (item 0) de null e vazias ou matrizes devem ser tratados da mesma.</span><span class="sxs-lookup"><span data-stu-id="d9d95-146">The general rule is that null and empty (0 item) collections or arrays should be treated the same.</span></span>  
  
### <a name="snapshots-versus-live-collections"></a><span data-ttu-id="d9d95-147">Instantâneos Versus coleções ao vivo</span><span class="sxs-lookup"><span data-stu-id="d9d95-147">Snapshots Versus Live Collections</span></span>  
 <span data-ttu-id="d9d95-148">Coleções de instantâneo são chamadas de coleções que representa um estado em algum ponto no tempo.</span><span class="sxs-lookup"><span data-stu-id="d9d95-148">Collections representing a state at some point in time are called snapshot collections.</span></span> <span data-ttu-id="d9d95-149">Por exemplo, uma coleção que contém linhas retornadas de uma consulta de banco de dados seria um instantâneo.</span><span class="sxs-lookup"><span data-stu-id="d9d95-149">For example, a collection containing rows returned from a database query would be a snapshot.</span></span> <span data-ttu-id="d9d95-150">Coleções em tempo real são chamadas de coleções que sempre representam o estado atual.</span><span class="sxs-lookup"><span data-stu-id="d9d95-150">Collections that always represent the current state are called live collections.</span></span> <span data-ttu-id="d9d95-151">Por exemplo, uma coleção de `ComboBox` itens é uma coleção em tempo real.</span><span class="sxs-lookup"><span data-stu-id="d9d95-151">For example, a collection of `ComboBox` items is a live collection.</span></span>  
  
 <span data-ttu-id="d9d95-152">**X DO NOT** retornam coleções de instantâneo de propriedades.</span><span class="sxs-lookup"><span data-stu-id="d9d95-152">**X DO NOT** return snapshot collections from properties.</span></span> <span data-ttu-id="d9d95-153">As propriedades devem retornar coleções em tempo real.</span><span class="sxs-lookup"><span data-stu-id="d9d95-153">Properties should return live collections.</span></span>  
  
 <span data-ttu-id="d9d95-154">Getters de propriedade devem ser operações muito leves.</span><span class="sxs-lookup"><span data-stu-id="d9d95-154">Property getters should be very lightweight operations.</span></span> <span data-ttu-id="d9d95-155">Retornar um instantâneo requer a criação de uma cópia de uma coleção interna em uma operação de (n).</span><span class="sxs-lookup"><span data-stu-id="d9d95-155">Returning a snapshot requires creating a copy of an internal collection in an O(n) operation.</span></span>  
  
 <span data-ttu-id="d9d95-156">**✓ DO** usar uma coleção de instantâneo ou de um live `IEnumerable<T>` (ou seu subtipo) para representar as coleções são voláteis (ou seja, que pode alterar sem modificar explicitamente a coleção).</span><span class="sxs-lookup"><span data-stu-id="d9d95-156">**✓ DO** use either a snapshot collection or a live `IEnumerable<T>` (or its subtype) to represent collections that are volatile (i.e., that can change without explicitly modifying the collection).</span></span>  
  
 <span data-ttu-id="d9d95-157">Em geral, todas as coleções que representa um recurso compartilhado (por exemplo, arquivos em um diretório) são voláteis.</span><span class="sxs-lookup"><span data-stu-id="d9d95-157">In general, all collections representing a shared resource (e.g., files in a directory) are volatile.</span></span> <span data-ttu-id="d9d95-158">Essas coleções são muito difíceis ou impossíveis de implementar como coleções em tempo real, a menos que a implementação é simplesmente um enumerador de somente avanço.</span><span class="sxs-lookup"><span data-stu-id="d9d95-158">Such collections are very difficult or impossible to implement as live collections unless the implementation is simply a forward-only enumerator.</span></span>  
  
## <a name="choosing-between-arrays-and-collections"></a><span data-ttu-id="d9d95-159">Escolhendo entre coleções e matrizes</span><span class="sxs-lookup"><span data-stu-id="d9d95-159">Choosing Between Arrays and Collections</span></span>  
 <span data-ttu-id="d9d95-160">**✓ DO** prefira a matrizes de coleções.</span><span class="sxs-lookup"><span data-stu-id="d9d95-160">**✓ DO** prefer collections over arrays.</span></span>  
  
 <span data-ttu-id="d9d95-161">Coleções fornecem mais controle sobre o conteúdo, podem evoluir ao longo do tempo e são mais utilizáveis.</span><span class="sxs-lookup"><span data-stu-id="d9d95-161">Collections provide more control over contents, can evolve over time, and are more usable.</span></span> <span data-ttu-id="d9d95-162">Além disso, usar matrizes para cenários somente leitura não é recomendado porque o custo de clonagem da matriz é proibitivo.</span><span class="sxs-lookup"><span data-stu-id="d9d95-162">In addition, using arrays for read-only scenarios is discouraged because the cost of cloning the array is prohibitive.</span></span> <span data-ttu-id="d9d95-163">Estudos de usabilidade mostraram que alguns desenvolvedores se sentir mais confortáveis usando APIs baseadas em coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-163">Usability studies have shown that some developers feel more comfortable using collection-based APIs.</span></span>  
  
 <span data-ttu-id="d9d95-164">No entanto, se você estiver desenvolvendo APIs de baixo nível, talvez seja melhor usar matrizes para cenários de leitura / gravação.</span><span class="sxs-lookup"><span data-stu-id="d9d95-164">However, if you are developing low-level APIs, it might be better to use arrays for read-write scenarios.</span></span> <span data-ttu-id="d9d95-165">As matrizes têm um menor volume de memória, que ajuda a reduzir o conjunto de trabalho, e o acesso a elementos em uma matriz é mais rápido porque é otimizado pelo tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="d9d95-165">Arrays have a smaller memory footprint, which helps reduce the working set, and access to elements in an array is faster because it is optimized by the runtime.</span></span>  
  
 <span data-ttu-id="d9d95-166">**✓ CONSIDER** usando matrizes nas APIs de baixo nível para minimizar o consumo de memória e maximizar o desempenho.</span><span class="sxs-lookup"><span data-stu-id="d9d95-166">**✓ CONSIDER** using arrays in low-level APIs to minimize memory consumption and maximize performance.</span></span>  
  
 <span data-ttu-id="d9d95-167">**✓ DO** usar matrizes de bytes em vez de coleções de bytes.</span><span class="sxs-lookup"><span data-stu-id="d9d95-167">**✓ DO** use byte arrays instead of collections of bytes.</span></span>  
  
 <span data-ttu-id="d9d95-168">**X DO NOT** usar matrizes de propriedades, se a propriedade teria que retornar uma nova matriz (por exemplo, uma cópia de uma matriz interna) sempre que a propriedade getter é chamado.</span><span class="sxs-lookup"><span data-stu-id="d9d95-168">**X DO NOT** use arrays for properties if the property would have to return a new array (e.g., a copy of an internal array) every time the property getter is called.</span></span>  
  
## <a name="implementing-custom-collections"></a><span data-ttu-id="d9d95-169">Implementando coleções personalizadas</span><span class="sxs-lookup"><span data-stu-id="d9d95-169">Implementing Custom Collections</span></span>  
 <span data-ttu-id="d9d95-170">**✓ CONSIDER** herdando `Collection<T>`, `ReadOnlyCollection<T>`, ou `KeyedCollection<TKey,TItem>` durante a criação de novas coleções.</span><span class="sxs-lookup"><span data-stu-id="d9d95-170">**✓ CONSIDER** inheriting from `Collection<T>`, `ReadOnlyCollection<T>`, or `KeyedCollection<TKey,TItem>` when designing new collections.</span></span>  
  
 <span data-ttu-id="d9d95-171">**✓ DO** implementar `IEnumerable<T>` durante a criação de novas coleções.</span><span class="sxs-lookup"><span data-stu-id="d9d95-171">**✓ DO** implement `IEnumerable<T>` when designing new collections.</span></span> <span data-ttu-id="d9d95-172">Considere a implementação `ICollection<T>` ou até mesmo `IList<T>` onde faça sentido.</span><span class="sxs-lookup"><span data-stu-id="d9d95-172">Consider implementing `ICollection<T>` or even `IList<T>` where it makes sense.</span></span>  
  
 <span data-ttu-id="d9d95-173">Ao implementar tal coleção personalizada, seguem o padrão de API estabelecido pela `Collection<T>` e `ReadOnlyCollection<T>` mais próximo possível.</span><span class="sxs-lookup"><span data-stu-id="d9d95-173">When implementing such custom collection, follow the API pattern established by `Collection<T>` and `ReadOnlyCollection<T>` as closely as possible.</span></span> <span data-ttu-id="d9d95-174">Ou seja, implementar os mesmos membros explicitamente, nomear os parâmetros, como essas duas coleções de nome-los e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="d9d95-174">That is, implement the same members explicitly, name the parameters like these two collections name them, and so on.</span></span>  
  
 <span data-ttu-id="d9d95-175">**✓ CONSIDER** implementando interfaces de coleção não genérica (`IList` e `ICollection`) se a coleção será geralmente passada para APIs colocar essas interfaces como entrada.</span><span class="sxs-lookup"><span data-stu-id="d9d95-175">**✓ CONSIDER** implementing nongeneric collection interfaces (`IList` and `ICollection`) if the collection will often be passed to APIs taking these interfaces as input.</span></span>  
  
 <span data-ttu-id="d9d95-176">**X AVOID** implementando interfaces de coleção em tipos com APIs complexas não relacionados ao conceito de uma coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-176">**X AVOID** implementing collection interfaces on types with complex APIs unrelated to the concept of a collection.</span></span>  
  
 <span data-ttu-id="d9d95-177">**X DO NOT** herdam coleções não genéricas de base como `CollectionBase`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-177">**X DO NOT** inherit from nongeneric base collections such as `CollectionBase`.</span></span> <span data-ttu-id="d9d95-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, e `KeyedCollection<TKey,TItem>` em vez disso.</span><span class="sxs-lookup"><span data-stu-id="d9d95-178">Use `Collection<T>`, `ReadOnlyCollection<T>`, and `KeyedCollection<TKey,TItem>` instead.</span></span>  
  
### <a name="naming-custom-collections"></a><span data-ttu-id="d9d95-179">Coleções personalizadas de nomenclatura</span><span class="sxs-lookup"><span data-stu-id="d9d95-179">Naming Custom Collections</span></span>  
 <span data-ttu-id="d9d95-180">Coleções (tipos que implementam `IEnumerable`) são criados basicamente por dois motivos: (1) para criar uma nova estrutura de dados com operações específicas de estrutura e muitas vezes diferentes características de desempenho que estruturas de dados existentes (por exemplo, <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>) e (2) para criar uma coleção especializada para manter um conjunto específico de itens (por exemplo, <xref:System.Collections.Specialized.StringCollection>).</span><span class="sxs-lookup"><span data-stu-id="d9d95-180">Collections (types that implement `IEnumerable`) are created mainly for two reasons: (1) to create a new data structure with structure-specific operations and often different performance characteristics than existing data structures (e.g.,  <xref:System.Collections.Generic.List%601>, <xref:System.Collections.Generic.LinkedList%601>, <xref:System.Collections.Generic.Stack%601>), and (2) to create a specialized collection for holding a specific set of items (e.g.,  <xref:System.Collections.Specialized.StringCollection>).</span></span> <span data-ttu-id="d9d95-181">Estruturas de dados geralmente são usadas na implementação interna de aplicativos e bibliotecas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-181">Data structures are most often used in the internal implementation of applications and libraries.</span></span> <span data-ttu-id="d9d95-182">Coleções especializadas principalmente devem ser expostos nas APIs (como tipos de propriedade e de parâmetro).</span><span class="sxs-lookup"><span data-stu-id="d9d95-182">Specialized collections are mainly to be exposed in APIs (as property and parameter types).</span></span>  
  
 <span data-ttu-id="d9d95-183">**✓ DO** utilize o sufixo "Dicionário" em nomes de abstrações implementando `IDictionary` ou `IDictionary<TKey,TValue>`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-183">**✓ DO** use the "Dictionary" suffix in names of abstractions implementing `IDictionary` or `IDictionary<TKey,TValue>`.</span></span>  
  
 <span data-ttu-id="d9d95-184">**✓ DO** utilize o sufixo "Coleção" em nomes de tipos que implementam `IEnumerable` (ou qualquer um de seus descendentes) e que representa uma lista de itens.</span><span class="sxs-lookup"><span data-stu-id="d9d95-184">**✓ DO** use the "Collection" suffix in names of types implementing `IEnumerable` (or any of its descendants) and representing a list of items.</span></span>  
  
 <span data-ttu-id="d9d95-185">**✓ DO** usar o nome de estrutura de dados apropriados para estruturas de dados personalizadas.</span><span class="sxs-lookup"><span data-stu-id="d9d95-185">**✓ DO** use the appropriate data structure name for custom data structures.</span></span>  
  
 <span data-ttu-id="d9d95-186">**X AVOID** usando qualquer sufixo implicando implementação específica, como "LinkedList" ou "Tabela de hash", em nomes de abstrações de coleção.</span><span class="sxs-lookup"><span data-stu-id="d9d95-186">**X AVOID** using any suffixes implying particular implementation, such as "LinkedList" or "Hashtable," in names of collection abstractions.</span></span>  
  
 <span data-ttu-id="d9d95-187">**✓ CONSIDER** prefixando nomes de coleção com o nome do tipo de item.</span><span class="sxs-lookup"><span data-stu-id="d9d95-187">**✓ CONSIDER** prefixing collection names with the name of the item type.</span></span> <span data-ttu-id="d9d95-188">Por exemplo, uma coleção de itens do tipo de armazenamento `Address` (implementando `IEnumerable<Address>`) deve ser nomeado `AddressCollection`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-188">For example, a collection storing items of type `Address` (implementing `IEnumerable<Address>`) should be named `AddressCollection`.</span></span> <span data-ttu-id="d9d95-189">Se o tipo de item é uma interface, o "I" prefixo de tipo do item pode ser omitido.</span><span class="sxs-lookup"><span data-stu-id="d9d95-189">If the item type is an interface, the "I" prefix of the item type can be omitted.</span></span> <span data-ttu-id="d9d95-190">Portanto, uma coleção de <xref:System.IDisposable> itens podem ser chamados `DisposableCollection`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-190">Thus, a collection of <xref:System.IDisposable> items can be called `DisposableCollection`.</span></span>  
  
 <span data-ttu-id="d9d95-191">**✓ CONSIDER** usando o prefixo "ReadOnly" em nomes de coleções somente leitura se uma coleção gravável correspondente pode ser adicionada ou já existe no framework.</span><span class="sxs-lookup"><span data-stu-id="d9d95-191">**✓ CONSIDER** using the "ReadOnly" prefix in names of read-only collections if a corresponding writeable collection might be added or already exists in the framework.</span></span>  
  
 <span data-ttu-id="d9d95-192">Por exemplo, uma coleção somente leitura de cadeias de caracteres deve ser chamada `ReadOnlyStringCollection`.</span><span class="sxs-lookup"><span data-stu-id="d9d95-192">For example, a read-only collection of strings should be called `ReadOnlyStringCollection`.</span></span>  
  
 <span data-ttu-id="d9d95-193">*Portions © 2005, 2009 Microsoft Corporation. Todos os direitos reservados.*</span><span class="sxs-lookup"><span data-stu-id="d9d95-193">*Portions © 2005, 2009 Microsoft Corporation. All rights reserved.*</span></span>  
  
 <span data-ttu-id="d9d95-194">*Reimpresso com permissão da Pearson Education, Inc. das [Diretrizes de Design do Framework: convenções, linguagens e padrões para bibliotecas do .NET reutilizável, 2ª edição](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) por Krzysztof Cwalina e Brad Abrams, publicado em 22 de outubro de 2008 por Addison-Wesley Professional como parte da série de desenvolvimento do Microsoft Windows.*</span><span class="sxs-lookup"><span data-stu-id="d9d95-194">*Reprinted by permission of Pearson Education, Inc. from [Framework Design Guidelines: Conventions, Idioms, and Patterns for Reusable .NET Libraries, 2nd Edition](https://www.informit.com/store/framework-design-guidelines-conventions-idioms-and-9780321545619) by Krzysztof Cwalina and Brad Abrams, published Oct 22, 2008 by Addison-Wesley Professional as part of the Microsoft Windows Development Series.*</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d9d95-195">Consulte também</span><span class="sxs-lookup"><span data-stu-id="d9d95-195">See also</span></span>

- [<span data-ttu-id="d9d95-196">Diretrizes de design do Framework</span><span class="sxs-lookup"><span data-stu-id="d9d95-196">Framework Design Guidelines</span></span>](../../../docs/standard/design-guidelines/index.md)  
- [<span data-ttu-id="d9d95-197">Diretrizes de uso</span><span class="sxs-lookup"><span data-stu-id="d9d95-197">Usage Guidelines</span></span>](../../../docs/standard/design-guidelines/usage-guidelines.md)
