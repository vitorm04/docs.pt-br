---
title: Implementando um modelo de domínio de microsserviço com o .NET Core
description: Arquitetura de microsserviços do .NET para aplicativos .NET em contêineres | Obtenha os detalhes de implementação de um modelo de domínio orientado a DDD.
author: CESARDELATORRE
ms.author: wiwagn
ms.date: 10/08/2018
ms.openlocfilehash: 884a558827e0e016e27315cee1ea9ed3e0d03dc4
ms.sourcegitcommit: d9a0071d0fd490ae006c816f78a563b9946e269a
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/25/2019
ms.locfileid: "55065902"
---
# <a name="implement-a-microservice-domain-model-with-net-core"></a><span data-ttu-id="3db9a-103">Implementar um modelo de domínio de microsserviço com o .NET Core</span><span class="sxs-lookup"><span data-stu-id="3db9a-103">Implement a microservice domain model with .NET Core</span></span> 

<span data-ttu-id="3db9a-104">Na seção anterior, foram explicados os princípios de design fundamentais e os padrões para criar um modelo de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-104">In the previous section, the fundamental design principles and patterns for designing a domain model were explained.</span></span> <span data-ttu-id="3db9a-105">Agora é hora de explorar possíveis maneiras de implementar o modelo de domínio usando o .NET Core (código C\# simples) e EF Core.</span><span class="sxs-lookup"><span data-stu-id="3db9a-105">Now it is time to explore possible ways to implement the domain model by using .NET Core (plain C\# code) and EF Core.</span></span> <span data-ttu-id="3db9a-106">Observe que seu modelo de domínio será composto apenas pelo seu código.</span><span class="sxs-lookup"><span data-stu-id="3db9a-106">Note that your domain model will be composed simply of your code.</span></span> <span data-ttu-id="3db9a-107">Ele terá apenas os requisitos do modelo EF Core, mas não reais dependências do EF.</span><span class="sxs-lookup"><span data-stu-id="3db9a-107">It will have just the EF Core model requirements, but not real dependencies on EF.</span></span> <span data-ttu-id="3db9a-108">Você não deve ter dependências rígidas nem referências ao EF Core ou qualquer outro ORM em seu modelo de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-108">You should not have hard dependencies or references to EF Core or any other ORM in your domain model.</span></span>

## <a name="domain-model-structure-in-a-custom-net-standard-library"></a><span data-ttu-id="3db9a-109">Estrutura do modelo de domínio em uma biblioteca .NET Standard personalizada</span><span class="sxs-lookup"><span data-stu-id="3db9a-109">Domain model structure in a custom .NET Standard Library</span></span>

<span data-ttu-id="3db9a-110">A organização de pastas usada para o aplicativo de referência eShopOnContainers demonstra o modelo DDD para o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="3db9a-110">The folder organization used for the eShopOnContainers reference application demonstrates the DDD model for the application.</span></span> <span data-ttu-id="3db9a-111">Você pode considerar que uma organização de pastas diferente comunica mais claramente as escolhas de design feitas para o seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="3db9a-111">You might find that a different folder organization more clearly communicates the design choices made for your application.</span></span> <span data-ttu-id="3db9a-112">Como é possível ver na Figura 7-10, no modelo de domínio de ordenação, há duas agregações: a agregação de ordem e a agregação de comprador.</span><span class="sxs-lookup"><span data-stu-id="3db9a-112">As you can see in Figure 7-10, in the ordering domain model there are two aggregates, the order aggregate and the buyer aggregate.</span></span> <span data-ttu-id="3db9a-113">Cada agregação é um grupo de entidades de domínio e objetos de valor, embora você possa ter uma agregação composta por uma única entidade de domínio (a raiz de agregação ou entidade raiz) também.</span><span class="sxs-lookup"><span data-stu-id="3db9a-113">Each aggregate is a group of domain entities and value objects, although you could have an aggregate composed of a single domain entity (the aggregate root or root entity) as well.</span></span>

![<span data-ttu-id="3db9a-114">A exibição do Gerenciador de Soluções do projeto Ordering.Domain, mostrando a pasta AggregatesModel que contém as pastas BuyerAggregate e OrderAggregate, cada uma contendo suas classes de entidade, arquivos-objeto de valor e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="3db9a-114">The Solution Explorer view for the Ordering.Domain project, showing the AggregatesModel folder containing the BuyerAggregate and OrderAggregate folders, each one containing it's entity classes, value object files and so on.</span></span> ](./media/image11.png)

<span data-ttu-id="3db9a-115">**Figura 7-10**.</span><span class="sxs-lookup"><span data-stu-id="3db9a-115">**Figure 7-10**.</span></span> <span data-ttu-id="3db9a-116">Estrutura de modelo de domínio para o microsserviço de ordenação em eShopOnContainers</span><span class="sxs-lookup"><span data-stu-id="3db9a-116">Domain model structure for the ordering microservice in eShopOnContainers</span></span>

<span data-ttu-id="3db9a-117">Além disso, a camada de modelo de domínio inclui os contratos de repositório (interfaces) que são os requisitos de infraestrutura do seu modelo de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-117">Additionally, the domain model layer includes the repository contracts (interfaces) that are the infrastructure requirements of your domain model.</span></span> <span data-ttu-id="3db9a-118">Em outras palavras, essas interfaces expressam quais repositórios e os métodos que a camada de infraestrutura deve implementar.</span><span class="sxs-lookup"><span data-stu-id="3db9a-118">In other words, these interfaces express what repositories and the methods the infrastructure layer must implement.</span></span> <span data-ttu-id="3db9a-119">É importante que a implementação dos repositórios seja colocada fora da camada de modelo de domínio, na biblioteca de camada de infraestrutura, para que a camada de modelo de domínio não seja "contaminada" pela API ou por classes de tecnologias de infraestrutura, como o Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="3db9a-119">It is critical that the implementation of the repositories be placed outside of the domain model layer, in the infrastructure layer library, so the domain model layer is not “contaminated” by API or classes from infrastructure technologies, like Entity Framework.</span></span>

<span data-ttu-id="3db9a-120">Você também pode ver uma pasta [SeedWork](https://martinfowler.com/bliki/Seedwork.html) que contém as classes base personalizadas que você pode usar como base para suas entidades de domínio e objetos de valor, portanto, você não tem código redundante em cada classe de objeto de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-120">You can also see a [SeedWork](https://martinfowler.com/bliki/Seedwork.html) folder that contains custom base classes that you can use as a base for your domain entities and value objects, so you do not have redundant code in each domain’s object class.</span></span>

## <a name="structure-aggregates-in-a-custom-net-standard-library"></a><span data-ttu-id="3db9a-121">Estruturar agregações em uma biblioteca .NET Standard personalizada</span><span class="sxs-lookup"><span data-stu-id="3db9a-121">Structure aggregates in a custom .NET Standard library</span></span>

<span data-ttu-id="3db9a-122">Uma agregação refere-se a um cluster de objetos de domínio agrupados de acordo com a consistência transacional.</span><span class="sxs-lookup"><span data-stu-id="3db9a-122">An aggregate refers to a cluster of domain objects grouped together to match transactional consistency.</span></span> <span data-ttu-id="3db9a-123">Esses objetos podem ser instâncias de entidades (uma das quais é a raiz de agregação ou uma entidade raiz) além de qualquer objeto de valor adicional.</span><span class="sxs-lookup"><span data-stu-id="3db9a-123">Those objects could be instances of entities (one of which is the aggregate root or root entity) plus any additional value objects.</span></span>

<span data-ttu-id="3db9a-124">Consistência transacional significa que uma agregação está garantidamente consistente e atualizada ao final de uma ação de negócios.</span><span class="sxs-lookup"><span data-stu-id="3db9a-124">Transactional consistency means that an aggregate is guaranteed to be consistent and up to date at the end of a business action.</span></span> <span data-ttu-id="3db9a-125">Por exemplo, a agregação de ordem do modelo de domínio de microsserviços de pedidos eShopOnContainers é composta conforme mostra a Figura 7-11.</span><span class="sxs-lookup"><span data-stu-id="3db9a-125">For example, the order aggregate from the eShopOnContainers ordering microservice domain model is composed as shown in Figure 7-11.</span></span>

![Uma exibição detalhada da pasta OrderAggregate: Address.cs é um objeto de valor, IOrderRepository é uma interface de repositório, Order.cs é uma raiz agregada, OrderItem.cs é uma entidade filho e OrderStatus.cs é uma classe de enumeração.](./media/image12.png)

<span data-ttu-id="3db9a-127">**Figura 7-11**.</span><span class="sxs-lookup"><span data-stu-id="3db9a-127">**Figure 7-11**.</span></span> <span data-ttu-id="3db9a-128">A agregação de ordem na solução do Visual Studio</span><span class="sxs-lookup"><span data-stu-id="3db9a-128">The order aggregate in Visual Studio solution</span></span>

<span data-ttu-id="3db9a-129">Se você abrir qualquer um dos arquivos em uma pasta de agregação, verá como ele está marcado como classe base personalizada ou interface, como objeto de entidade ou valor, conforme implementado na pasta [SeedWork](https://github.com/dotnet-architecture/eShopOnContainers/tree/master/src/Services/Ordering/Ordering.Domain/SeedWork).</span><span class="sxs-lookup"><span data-stu-id="3db9a-129">If you open any of the files in an aggregate folder, you can see how it is marked as either a custom base class or interface, like entity or value object, as implemented in the [SeedWork](https://github.com/dotnet-architecture/eShopOnContainers/tree/master/src/Services/Ordering/Ordering.Domain/SeedWork) folder.</span></span>

## <a name="implement-domain-entities-as-poco-classes"></a><span data-ttu-id="3db9a-130">Implementar entidades de domínio como classes POCO</span><span class="sxs-lookup"><span data-stu-id="3db9a-130">Implement domain entities as POCO classes</span></span>

<span data-ttu-id="3db9a-131">Você implementa um modelo de domínio no .NET criando classes POCO que implementam suas entidades de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-131">You implement a domain model in .NET by creating POCO classes that implement your domain entities.</span></span> <span data-ttu-id="3db9a-132">No exemplo a seguir, a classe Ordem é definida como uma entidade e como uma raiz de agregação.</span><span class="sxs-lookup"><span data-stu-id="3db9a-132">In the following example, the Order class is defined as an entity and also as an aggregate root.</span></span> <span data-ttu-id="3db9a-133">Porque a classe Ordem deriva da classe base da Entidade, ela pode reutilizar o código comum relacionado a entidades.</span><span class="sxs-lookup"><span data-stu-id="3db9a-133">Because the Order class derives from the Entity base class, it can reuse common code related to entities.</span></span> <span data-ttu-id="3db9a-134">Tenha em mente que essas interfaces e classes base são definidas por você no projeto de modelo de domínio, portanto, é o seu código, não o código de infraestrutura de um ORM, como EF.</span><span class="sxs-lookup"><span data-stu-id="3db9a-134">Bear in mind that these base classes and interfaces are defined by you in the domain model project, so it is your code, not infrastructure code from an ORM like EF.</span></span>

```csharp
// COMPATIBLE WITH ENTITY FRAMEWORK CORE 2.0
// Entity is a custom base class with the ID
public class Order : Entity, IAggregateRoot
{
    private DateTime _orderDate;
    public Address Address { get; private set; }
    private int? _buyerId;

    public OrderStatus OrderStatus { get; private set; }
    private int _orderStatusId;

    private string _description;
    private int? _paymentMethodId;

    private readonly List<OrderItem> _orderItems;
    public IReadOnlyCollection<OrderItem> OrderItems => _orderItems;
  
    public Order(string userId, Address address, int cardTypeId, string cardNumber, string cardSecurityNumber,
            string cardHolderName, DateTime cardExpiration, int? buyerId = null, int? paymentMethodId = null)
    {
        _orderItems = new List<OrderItem>();
        _buyerId = buyerId;
        _paymentMethodId = paymentMethodId;
        _orderStatusId = OrderStatus.Submitted.Id;
        _orderDate = DateTime.UtcNow;
        Address = address;

        // ...Additional code ...
    }

    public void AddOrderItem(int productId, string productName, 
                            decimal unitPrice, decimal discount, 
                            string pictureUrl, int units = 1)
    {
        //...
        // Domain rules/logic for adding the OrderItem to the order
        // ...

        var orderItem = new OrderItem(productId, productName, unitPrice, discount, pictureUrl, units);
        
        _orderItems.Add(orderItem);
  
    }
    // ...
    // Additional methods with domain rules/logic related to the Order aggregate
    // ...
}
```

<span data-ttu-id="3db9a-135">É importante observar que essa é uma entidade de domínio implementada como uma classe POCO.</span><span class="sxs-lookup"><span data-stu-id="3db9a-135">It is important to note that this is a domain entity implemented as a POCO class.</span></span> <span data-ttu-id="3db9a-136">Ela não tem nenhuma dependência direta do Entity Framework Core nem qualquer outra estrutura de infraestrutura.</span><span class="sxs-lookup"><span data-stu-id="3db9a-136">It does not have any direct dependency on Entity Framework Core or any other infrastructure framework.</span></span> <span data-ttu-id="3db9a-137">Essa implementação é como deve ser em DDD, apenas código C\# implementando um modelo de domínio.</span><span class="sxs-lookup"><span data-stu-id="3db9a-137">This implementation is as it should be in DDD, just C\# code implementing a domain model.</span></span>

<span data-ttu-id="3db9a-138">Além disso, a classe é decorada com uma interface denominada IAggregateRoot.</span><span class="sxs-lookup"><span data-stu-id="3db9a-138">In addition, the class is decorated with an interface named IAggregateRoot.</span></span> <span data-ttu-id="3db9a-139">Essa interface é uma interface vazia, às vezes chamada de uma *interface de marcador*, que é usada apenas para indicar que essa classe de entidade também é uma raiz de agregação.</span><span class="sxs-lookup"><span data-stu-id="3db9a-139">That interface is an empty interface, sometimes called a *marker interface*, that is used just to indicate that this entity class is also an aggregate root.</span></span>

<span data-ttu-id="3db9a-140">Uma interface de marcador às vezes é considerada como um antipadrão; no entanto, também é uma maneira simples de marcar uma classe, especialmente quando essa interface pode estar em evolução.</span><span class="sxs-lookup"><span data-stu-id="3db9a-140">A marker interface is sometimes considered as an anti-pattern; however, it is also a clean way to mark a class, especially when that interface might be evolving.</span></span> <span data-ttu-id="3db9a-141">Um atributo pode ser outra escolha para o marcador, mas é mais rápido ver a classe base (Entity) ao lado da interface IAggregate, em vez de colocar um marcador de atributo Aggregate acima da classe.</span><span class="sxs-lookup"><span data-stu-id="3db9a-141">An attribute could be the other choice for the marker, but it is quicker to see the base class (Entity) next to the IAggregate interface instead of putting an Aggregate attribute marker above the class.</span></span> <span data-ttu-id="3db9a-142">É uma questão de preferências, de qualquer forma.</span><span class="sxs-lookup"><span data-stu-id="3db9a-142">It is a matter of preferences, in any case.</span></span>

<span data-ttu-id="3db9a-143">Ter um meio de raiz de agregação significa que a maioria do código relacionado à consistência e a regras de negócio das entidades da agregação deve ser implementada como métodos na classe raiz agregada de Ordem (por exemplo, AddOrderItem ao adicionar um objeto OrderItem à agregação).</span><span class="sxs-lookup"><span data-stu-id="3db9a-143">Having an aggregate root means that most of the code related to consistency and business rules of the aggregate’s entities should be implemented as methods in the Order aggregate root class (for example, AddOrderItem when adding an OrderItem object to the aggregate).</span></span> <span data-ttu-id="3db9a-144">Você não deve criar nem atualizar objetos OrderItems de modo independente ou direto; a classe AggregateRoot deve manter o controle e a consistência de qualquer operação de atualização com relação às suas entidades filho.</span><span class="sxs-lookup"><span data-stu-id="3db9a-144">You should not create or update OrderItems objects independently or directly; the AggregateRoot class must keep control and consistency of any update operation against its child entities.</span></span>

## <a name="encapsulate-data-in-the-domain-entities"></a><span data-ttu-id="3db9a-145">Encapsular dados nas Entidades de Domínio</span><span class="sxs-lookup"><span data-stu-id="3db9a-145">Encapsulate data in the Domain Entities</span></span>

<span data-ttu-id="3db9a-146">Um problema comum em modelos de entidade é que eles expõem propriedades de navegação da coleção como tipos de lista publicamente acessíveis.</span><span class="sxs-lookup"><span data-stu-id="3db9a-146">A common problem in entity models is that they expose collection navigation properties as publicly accessible list types.</span></span> <span data-ttu-id="3db9a-147">Isso permite que qualquer desenvolvedor do colaborador manipule o conteúdo desses tipos de coleção, o que pode ignorar importantes regras de negócio relacionadas à coleção, possivelmente deixando o objeto em um estado inválido.</span><span class="sxs-lookup"><span data-stu-id="3db9a-147">This allows any collaborator developer to manipulate the contents of these collection types, which may bypass important business rules related to the collection, possibly leaving the object in an invalid state.</span></span> <span data-ttu-id="3db9a-148">A solução para isso é expor o acesso somente leitura a coleções relacionadas e fornecer explicitamente métodos que definem maneiras como os clientes podem manipulá-los.</span><span class="sxs-lookup"><span data-stu-id="3db9a-148">The solution to this is to expose read-only access to related collections and explicitly provide methods that define ways in which clients can manipulate them.</span></span>

<span data-ttu-id="3db9a-149">No código anterior, observe que muitos atributos são somente leitura ou privados e só são atualizáveis pelos métodos de classe, assim, qualquer atualização considera invariáveis de domínio empresarial da conta e lógica especificada dentro do método de classe.</span><span class="sxs-lookup"><span data-stu-id="3db9a-149">In the previous code, note that many attributes are read-only or private and are only updatable by the class methods, so any update considers business domain invariants and logic specified within the class methods.</span></span>

<span data-ttu-id="3db9a-150">Por exemplo, após os padrões DDD, você ***não* deve fazer o seguinte** de nenhum método de manipulador de comando ou classe de camada de aplicativo (na verdade, deve ser impossível você fazer isso):</span><span class="sxs-lookup"><span data-stu-id="3db9a-150">For example, following DDD patterns, **you should *not* do the following** from any command handler method or application layer class (actually, it should be impossible for you to do so):</span></span>

```csharp
// WRONG ACCORDING TO DDD PATTERNS – CODE AT THE APPLICATION LAYER OR
// COMMAND HANDLERS
// Code in command handler methods or Web API controllers
//... (WRONG) Some code with business logic out of the domain classes ...
OrderItem myNewOrderItem = new OrderItem(orderId, productId, productName,
    pictureUrl, unitPrice, discount, units);

//... (WRONG) Accessing the OrderItems colletion directly from the application layer // or command handlers
myOrder.OrderItems.Add(myNewOrderItem);
//...
```

<span data-ttu-id="3db9a-151">Neste caso, o método Add é meramente uma operação para adicionar dados, com acesso direto à coleção OrderItems.</span><span class="sxs-lookup"><span data-stu-id="3db9a-151">In this case, the Add method is purely an operation to add data, with direct access to the OrderItems collection.</span></span> <span data-ttu-id="3db9a-152">Portanto, a maioria da lógica, das regras ou das validações de domínio relacionada àquela operação com as entidades filho será espalhada pela camada de aplicativo (manipuladores de comandos e controladores de API da Web).</span><span class="sxs-lookup"><span data-stu-id="3db9a-152">Therefore, most of the domain logic, rules, or validations related to that operation with the child entities will be spread across the application layer (command handlers and Web API controllers).</span></span>

<span data-ttu-id="3db9a-153">Se você usar uma solução alternativa para a raiz de agregação, a raiz de agregação não assegurará suas invariáveis, sua validade nem sua consistência.</span><span class="sxs-lookup"><span data-stu-id="3db9a-153">If you go around the aggregate root, the aggregate root cannot guarantee its invariants, its validity, or its consistency.</span></span> <span data-ttu-id="3db9a-154">Por fim, você terá código espaguete ou código de script transacional.</span><span class="sxs-lookup"><span data-stu-id="3db9a-154">Eventually you will have spaghetti code or transactional script code.</span></span>

<span data-ttu-id="3db9a-155">Para seguir padrões DDD, as entidades não devem ter setters públicos em nenhuma propriedade de entidade.</span><span class="sxs-lookup"><span data-stu-id="3db9a-155">To follow DDD patterns, entities must not have public setters in any entity property.</span></span> <span data-ttu-id="3db9a-156">Alterações em uma entidade devem ser controladas pelos métodos explícitos com linguagem ubíqua explícita sobre a alteração que estão sendo executadas na entidade.</span><span class="sxs-lookup"><span data-stu-id="3db9a-156">Changes in an entity should be driven by explicit methods with explicit ubiquitous language about the change they are performing in the entity.</span></span>

<span data-ttu-id="3db9a-157">Além disso, coleções na entidade (como os itens do pedido) devem ser propriedades somente leitura (o método AsReadOnly explicado posteriormente).</span><span class="sxs-lookup"><span data-stu-id="3db9a-157">Furthermore, collections within the entity (like the order items) should be read-only properties (the AsReadOnly method explained later).</span></span> <span data-ttu-id="3db9a-158">Você deve ser capaz de atualizá-lo somente de dentro de métodos da classe raiz agregada ou de métodos de entidade filho.</span><span class="sxs-lookup"><span data-stu-id="3db9a-158">You should be able to update it only from within the aggregate root class methods or the child entity methods.</span></span>

<span data-ttu-id="3db9a-159">Como você pode ver no código para a raiz de agregação de ordem, todos os setters devem ser privados ou, pelo menos, somente leitura externamente, de modo que qualquer operação contra os dados da entidade ou entidades seu filho deve ser executada por meio dos métodos na classe de entidade.</span><span class="sxs-lookup"><span data-stu-id="3db9a-159">As you can see in the code for the Order aggregate root, all setters should be private or at least read-only externally, so that any operation against the entity’s data or its child entities has to be performed through methods in the entity class.</span></span> <span data-ttu-id="3db9a-160">Isso mantém a consistência de maneira controlada e orientada a objeto, em vez de implementar o código de script transacional.</span><span class="sxs-lookup"><span data-stu-id="3db9a-160">This maintains consistency in a controlled and object-oriented way instead of implementing transactional script code.</span></span>

<span data-ttu-id="3db9a-161">O snippet de código a seguir mostra a maneira adequada de codificar a tarefa de adicionar um objeto OrderItem para a agregação de ordem de código.</span><span class="sxs-lookup"><span data-stu-id="3db9a-161">The following code snippet shows the proper way to code the task of adding an OrderItem object to the Order aggregate.</span></span>

```csharp
// RIGHT ACCORDING TO DDD--CODE AT THE APPLICATION LAYER OR COMMAND HANDLERS
// The code in command handlers or WebAPI controllers, related only to application stuff
// There is NO code here related to OrderItem object’s business logic
myOrder.AddOrderItem(productId, productName, pictureUrl, unitPrice, discount, units);

// The code related to OrderItem params validations or domain rules should
// be WITHIN the AddOrderItem method.

//...
```

<span data-ttu-id="3db9a-162">Neste snippet, a maioria das validações ou da lógica relacionada à criação de um objeto OrderItem estará sob o controle da raiz de agregação de Ordem (no método AddOrderItem), especialmente validações e lógica relacionadas a outros elementos no agregado.</span><span class="sxs-lookup"><span data-stu-id="3db9a-162">In this snippet, most of the validations or logic related to the creation of an OrderItem object will be under the control of the Order aggregate root—in the AddOrderItem method—especially validations and logic related to other elements in the aggregate.</span></span> <span data-ttu-id="3db9a-163">Por exemplo, você pode obter o mesmo item de produto como resultado de várias chamadas para AddOrderItem.</span><span class="sxs-lookup"><span data-stu-id="3db9a-163">For instance, you might get the same product item as the result of multiple calls to AddOrderItem.</span></span> <span data-ttu-id="3db9a-164">Nesse método, você pode examinar os itens de produto e consolidar os mesmos itens de produto em um único objeto OrderItem com várias unidades.</span><span class="sxs-lookup"><span data-stu-id="3db9a-164">In that method, you could examine the product items and consolidate the same product items into a single OrderItem object with several units.</span></span> <span data-ttu-id="3db9a-165">Além disso, se houver valores de desconto diferentes, mas a ID do produto (product ID) for a mesma, você provavelmente aplicará o desconto maior.</span><span class="sxs-lookup"><span data-stu-id="3db9a-165">Additionally, if there are different discount amounts but the product ID is the same, you would likely apply the higher discount.</span></span> <span data-ttu-id="3db9a-166">Esse princípio se aplica a qualquer outra lógica do domínio para o objeto OrderItem.</span><span class="sxs-lookup"><span data-stu-id="3db9a-166">This principle applies to any other domain logic for the OrderItem object.</span></span>

<span data-ttu-id="3db9a-167">Além disso, a nova operação OrderItem(params) também será controlada e executada pelo método AddOrderItem da raiz de agregação de Ordem.</span><span class="sxs-lookup"><span data-stu-id="3db9a-167">In addition, the new OrderItem(params) operation will also be controlled and performed by the AddOrderItem method from the Order aggregate root.</span></span> <span data-ttu-id="3db9a-168">Portanto, a maioria da lógica ou das validações relacionadas àquela operação (especialmente tudo o que afeta a consistência entre outras entidades filho) estará em um único lugar na raiz da agregação.</span><span class="sxs-lookup"><span data-stu-id="3db9a-168">Therefore, most of the logic or validations related to that operation (especially anything that impacts the consistency between other child entities) will be in a single place within the aggregate root.</span></span> <span data-ttu-id="3db9a-169">Esta é a principal finalidade do padrão de raiz de agregação.</span><span class="sxs-lookup"><span data-stu-id="3db9a-169">That is the ultimate purpose of the aggregate root pattern.</span></span>

<span data-ttu-id="3db9a-170">Quando você usar o Entity Framework Core 1.1 ou posterior, uma entidade DDD pode ser melhor expressada porque ela permite [mapear para os campos](https://docs.microsoft.com/ef/core/modeling/backing-field) além das propriedades.</span><span class="sxs-lookup"><span data-stu-id="3db9a-170">When you use Entity Framework Core 1.1 or later, a DDD entity can be better expressed because it allows [mapping to fields](https://docs.microsoft.com/ef/core/modeling/backing-field) in addition to properties.</span></span> <span data-ttu-id="3db9a-171">Isso é útil ao proteger as coleções de entidades filho ou objetos de valor.</span><span class="sxs-lookup"><span data-stu-id="3db9a-171">This is useful when protecting collections of child entities or value objects.</span></span> <span data-ttu-id="3db9a-172">Com esse aprimoramento, você pode usar os campos privados, em vez das propriedades, e pode implementar qualquer atualização à coleção de campo nos métodos públicos e fornecer acesso somente leitura por meio do método AsReadOnly.</span><span class="sxs-lookup"><span data-stu-id="3db9a-172">With this enhancement, you can use simple private fields instead of properties and you can implement any update to the field collection in public methods and provide read-only access through the AsReadOnly method.</span></span>

<span data-ttu-id="3db9a-173">No DDD, você deseja atualizar a entidade somente por meio de métodos na entidade (ou no construtor) para controlar qualquer invariável e a consistência dos dados, portanto, as propriedades são definidas somente com um acessador get.</span><span class="sxs-lookup"><span data-stu-id="3db9a-173">In DDD you want to update the entity only through methods in the entity (or the constructor) in order to control any invariant and the consistency of the data, so properties are defined only with a get accessor.</span></span> <span data-ttu-id="3db9a-174">As propriedades têm o respaldo de campos privados.</span><span class="sxs-lookup"><span data-stu-id="3db9a-174">The properties are backed by private fields.</span></span> <span data-ttu-id="3db9a-175">Membros privados só pode ser acessados de dentro da classe.</span><span class="sxs-lookup"><span data-stu-id="3db9a-175">Private members can only be accessed from within the class.</span></span> <span data-ttu-id="3db9a-176">No entanto, há uma exceção: o EF Core também precisa definir esses campos (para que ele possa retornar o objeto com os valores adequados).</span><span class="sxs-lookup"><span data-stu-id="3db9a-176">However, there one exception: EF Core needs to set these fields as well (so it can return the object with the proper values).</span></span>

### <a name="map-properties-with-only-get-accessors-to-the-fields-in-the-database-table"></a><span data-ttu-id="3db9a-177">Mapear propriedades com apenas acessadores get para os campos na tabela de banco de dados</span><span class="sxs-lookup"><span data-stu-id="3db9a-177">Map properties with only get accessors to the fields in the database table</span></span>

<span data-ttu-id="3db9a-178">Mapeamento de propriedades para colunas de tabela do banco de dados não é uma responsabilidade do domínio, mas parte da camada de infraestrutura e persistência.</span><span class="sxs-lookup"><span data-stu-id="3db9a-178">Mapping properties to database table columns is not a domain responsibility but part of the infrastructure and persistence layer.</span></span> <span data-ttu-id="3db9a-179">Mencionamos isso aqui apenas para que você esteja ciente das novas funcionalidades no EF Core 1.1 ou posterior relacionadas a como você pode modelar entidades.</span><span class="sxs-lookup"><span data-stu-id="3db9a-179">We mention this here just so you are aware of the new capabilities in EF Core 1.1 or later related to how you can model entities.</span></span> <span data-ttu-id="3db9a-180">Detalhes adicionais sobre este tópico são explicados na seção de infraestrutura e persistência.</span><span class="sxs-lookup"><span data-stu-id="3db9a-180">Additional details on this topic are explained in the infrastructure and persistence section.</span></span>

<span data-ttu-id="3db9a-181">Quando você usa o EF Core 1.0 ou posterior, no DbContext, precisa mapear as propriedades definidas somente com getters para os campos reais na tabela de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="3db9a-181">When you use EF Core 1.0 or later, within the DbContext you need to map the properties that are defined only with getters to the actual fields in the database table.</span></span> <span data-ttu-id="3db9a-182">Isso é feito com o método HasField da classe PropertyBuilder.</span><span class="sxs-lookup"><span data-stu-id="3db9a-182">This is done with the HasField method of the PropertyBuilder class.</span></span>

### <a name="map-fields-without-properties"></a><span data-ttu-id="3db9a-183">Mapear campos sem propriedades</span><span class="sxs-lookup"><span data-stu-id="3db9a-183">Map fields without properties</span></span>

<span data-ttu-id="3db9a-184">Com o recurso no EF Core 1.1 ou posterior para mapear colunas para campos, também é possível não usar propriedades.</span><span class="sxs-lookup"><span data-stu-id="3db9a-184">With the feature in EF Core 1.1 or later to map columns to fields, it is also possible to not use properties.</span></span> <span data-ttu-id="3db9a-185">Em vez disso, você pode apenas mapear as colunas de uma tabela para campos.</span><span class="sxs-lookup"><span data-stu-id="3db9a-185">Instead, you can just map columns from a table to fields.</span></span> <span data-ttu-id="3db9a-186">Um caso de uso comum para isso são campos privados para um estado interno que não precisam ser acessados de fora da entidade.</span><span class="sxs-lookup"><span data-stu-id="3db9a-186">A common use case for this is private fields for an internal state that does not need to be accessed from outside the entity.</span></span>

<span data-ttu-id="3db9a-187">Por exemplo, no exemplo de código anterior, OrderAggregate, há vários campos privados, como o campo `_paymentMethodId`, que não têm nenhuma propriedade relacionada para um setter ou um getter.</span><span class="sxs-lookup"><span data-stu-id="3db9a-187">For example, in the preceding OrderAggregate code example, there are several private fields, like the  `_paymentMethodId` field, that have no related property for either a setter or getter.</span></span> <span data-ttu-id="3db9a-188">Esse campo também poderia ser calculado dentro da lógica de negócios da ordem e usado em métodos da ordem, mas precisa ser mantido no banco de dados também.</span><span class="sxs-lookup"><span data-stu-id="3db9a-188">That field could also be calculated within the order’s business logic and used from the order’s methods, but it needs to be persisted in the database as well.</span></span> <span data-ttu-id="3db9a-189">Assim, no EF Core (desde a v1.1), existe uma maneira de mapear um campo sem uma propriedade relacionada a uma coluna no banco de dados.</span><span class="sxs-lookup"><span data-stu-id="3db9a-189">So in EF Core (since v1.1) there is a way to map a field without a related property to a column in the database.</span></span> <span data-ttu-id="3db9a-190">Isso também é explicado na seção [Camada de infraestrutura](ddd-oriented-microservice.md#the-infrastructure-layer) deste guia.</span><span class="sxs-lookup"><span data-stu-id="3db9a-190">This is also explained in the [Infrastructure layer](ddd-oriented-microservice.md#the-infrastructure-layer) section of this guide.</span></span>

### <a name="additional-resources"></a><span data-ttu-id="3db9a-191">Recursos adicionais</span><span class="sxs-lookup"><span data-stu-id="3db9a-191">Additional resources</span></span>

- <span data-ttu-id="3db9a-192">**Vaughn Vernon. Modelagem de agregações com DDD e Entity Framework.**</span><span class="sxs-lookup"><span data-stu-id="3db9a-192">**Vaughn Vernon. Modeling Aggregates with DDD and Entity Framework.**</span></span> <span data-ttu-id="3db9a-193">Observe que este *não* é um Entity Framework Core.</span><span class="sxs-lookup"><span data-stu-id="3db9a-193">Note that this is *not* Entity Framework Core.</span></span> \
  <https://kalele.io/blog-posts/modeling-aggregates-with-ddd-and-entity-framework/>

- <span data-ttu-id="3db9a-194">**Julie Lerman. Coding for Domain-Driven Design: Tips for Data-Focused Devs** \ (Codificando para o design controlado por domínio: dicas para desenvolvedores com foco em dados)</span><span class="sxs-lookup"><span data-stu-id="3db9a-194">**Julie Lerman. Coding for Domain-Driven Design: Tips for Data-Focused Devs** \\</span></span>
  [*https://msdn.microsoft.com/magazine/dn342868.aspx*](https://msdn.microsoft.com/magazine/dn342868.aspx)

- <span data-ttu-id="3db9a-195">**Udi Dahan. Como criar modelos de domínio totalmente encapsulados** \\</span><span class="sxs-lookup"><span data-stu-id="3db9a-195">**Udi Dahan. How to create fully encapsulated Domain Models** \\</span></span>
  [*http://udidahan.com/2008/02/29/how-to-create-fully-encapsulated-domain-models/*](http://udidahan.com/2008/02/29/how-to-create-fully-encapsulated-domain-models/)

>[!div class="step-by-step"]
><span data-ttu-id="3db9a-196">[Anterior](microservice-domain-model.md)
>[Próximo](seedwork-domain-model-base-classes-interfaces.md)</span><span class="sxs-lookup"><span data-stu-id="3db9a-196">[Previous](microservice-domain-model.md)
[Next](seedwork-domain-model-base-classes-interfaces.md)</span></span>
