---
title: Escrever um código C# seguro e eficiente
description: Aprimoramentos recentes na linguagem C# permitem escrever código seguro verificável que o desempenho associou anteriormente ao código não seguro.
ms.date: 10/23/2018
ms.custom: mvc
ms.openlocfilehash: 73ad7a84d2ad47f0e0242825d250247ffb39928e
ms.sourcegitcommit: 34593b4d0be779699d38a9949d6aec11561657ec
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/11/2019
ms.locfileid: "66832939"
---
# <a name="write-safe-and-efficient-c-code"></a><span data-ttu-id="96d95-103">Escrever um código C# seguro e eficiente</span><span class="sxs-lookup"><span data-stu-id="96d95-103">Write safe and efficient C# code</span></span>

<span data-ttu-id="96d95-104">Novos recursos em C# permitem escrever código seguro verificável com melhor desempenho.</span><span class="sxs-lookup"><span data-stu-id="96d95-104">New features in C# enable you to write verifiable safe code with better performance.</span></span> <span data-ttu-id="96d95-105">Se você aplicar essas técnicas com cuidado, menos cenários exigirão código não seguro.</span><span class="sxs-lookup"><span data-stu-id="96d95-105">If you carefully apply these techniques, fewer scenarios require unsafe code.</span></span> <span data-ttu-id="96d95-106">Esses recursos tornam fácil usar referências a tipos de valor como argumentos e retornos de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-106">These features make it easier to use references to value types as method arguments and method returns.</span></span> <span data-ttu-id="96d95-107">Quando realizadas com segurança, essas técnicas minimizam a cópia de tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-107">When done safely, these techniques minimize copying value types.</span></span> <span data-ttu-id="96d95-108">Usando tipos de valor, é possível minimizar a quantidade de alocações e passagens de coleta de lixo.</span><span class="sxs-lookup"><span data-stu-id="96d95-108">By using value types, you can minimize the number of allocations and garbage collection passes.</span></span>

<span data-ttu-id="96d95-109">Grande parte do código de exemplo neste artigo usa recursos adicionados no C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="96d95-109">Much of the sample code in this article uses features added in C# 7.2.</span></span> <span data-ttu-id="96d95-110">Para usar esses recursos, é necessário configurar seu projeto para usar o C# 7.2 ou posterior.</span><span class="sxs-lookup"><span data-stu-id="96d95-110">To use those features, you must configure your project to use C# 7.2 or later.</span></span> <span data-ttu-id="96d95-111">Para saber mais sobre como definir a versão da linguagem, confira [configure the language version](language-reference/configure-language-version.md) (configurar a versão da linguagem).</span><span class="sxs-lookup"><span data-stu-id="96d95-111">For more information on setting the language version, see [configure the language version](language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="96d95-112">Este artigo se concentra em técnicas para o gerenciamento eficiente de recursos.</span><span class="sxs-lookup"><span data-stu-id="96d95-112">This article focuses on techniques for efficient resource management.</span></span> <span data-ttu-id="96d95-113">Uma vantagem de usar tipos de valor é que eles geralmente evitam alocações de heap.</span><span class="sxs-lookup"><span data-stu-id="96d95-113">One advantage to using value types is that they often avoid heap allocations.</span></span> <span data-ttu-id="96d95-114">A desvantagem é que eles são copiados por valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-114">The disadvantage is that they're copied by value.</span></span> <span data-ttu-id="96d95-115">Essa compensação dificulta a otimização de algoritmos que operam em grandes quantidades de dados.</span><span class="sxs-lookup"><span data-stu-id="96d95-115">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="96d95-116">Os novos recursos de linguagem no C# 7.2 oferecem mecanismos que habilitam o código eficiente seguro que usa referências para tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-116">New language features in C# 7.2 provide mechanisms that enable safe efficient code using references to value types.</span></span> <span data-ttu-id="96d95-117">Use esses recursos criteriosamente para minimizar tanto as alocações quanto as operações de cópia.</span><span class="sxs-lookup"><span data-stu-id="96d95-117">Use these features wisely to minimize both allocations and copy operations.</span></span> <span data-ttu-id="96d95-118">Este artigo explora esses novos recursos.</span><span class="sxs-lookup"><span data-stu-id="96d95-118">This article explores those new features.</span></span>

<span data-ttu-id="96d95-119">Este artigo se concentra nas seguintes técnicas de gerenciamento de recursos:</span><span class="sxs-lookup"><span data-stu-id="96d95-119">This article focuses on the following resource management techniques:</span></span>

- <span data-ttu-id="96d95-120">declarar uma [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) para expressar que um tipo é **imutável** e permite que o compilador salve cópias ao usar os parâmetros [`in`](language-reference/keywords/in-parameter-modifier.md).</span><span class="sxs-lookup"><span data-stu-id="96d95-120">Declare a [`readonly struct`](language-reference/keywords/readonly.md#readonly-struct-example) to express that a type is **immutable** and enables the compiler to save copies when using [`in`](language-reference/keywords/in-parameter-modifier.md) parameters.</span></span>
- <span data-ttu-id="96d95-121">usar um retorno [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) quando o valor retornado é `struct` maior que <xref:System.IntPtr.Size?displayProperty=nameWithType> e o tempo de vida de armazenamento é maior que o método que retorna o valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-121">Use a [`ref readonly`](language-reference/keywords/ref.md#reference-return-values) return when the return value is a `struct` larger than <xref:System.IntPtr.Size?displayProperty=nameWithType> and the storage lifetime is greater than the method returning the value.</span></span>
- <span data-ttu-id="96d95-122">Quando o tamanho de um `readonly struct` é maior que <xref:System.IntPtr.Size?displayProperty=nameWithType>, você deve passá-lo como um parâmetro `in` por motivos de desempenho.</span><span class="sxs-lookup"><span data-stu-id="96d95-122">When the size of a `readonly struct` is bigger than <xref:System.IntPtr.Size?displayProperty=nameWithType>, you should pass it as an `in` parameter for performance reasons.</span></span>
- <span data-ttu-id="96d95-123">Nunca passe um `struct` como um parâmetro `in`, a menos que ele seja declarado com o modificador `readonly`, porque ele pode afetar negativamente o desempenho e pode resultar em um comportamento obscuro.</span><span class="sxs-lookup"><span data-stu-id="96d95-123">Never pass a `struct` as an `in` parameter unless it's declared with the `readonly` modifier because it may negatively affect performance and could lead to an obscure behavior.</span></span>
- <span data-ttu-id="96d95-124">Use um [`ref struct`](language-reference/keywords/ref.md#ref-struct-types) ou um `readonly ref struct` como <xref:System.Span%601> ou <xref:System.ReadOnlySpan%601> para trabalhar com a memória como uma sequência de bytes.</span><span class="sxs-lookup"><span data-stu-id="96d95-124">Use a [`ref struct`](language-reference/keywords/ref.md#ref-struct-types), or a `readonly ref struct` such as <xref:System.Span%601> or <xref:System.ReadOnlySpan%601> to work with memory as a sequence of bytes.</span></span>

<span data-ttu-id="96d95-125">Essas técnicas forçam você a equilibrar duas metras concorrentes em relação às **referências** e aos **valores**.</span><span class="sxs-lookup"><span data-stu-id="96d95-125">These techniques force you to balance two competing goals with regard to **references** and **values**.</span></span> <span data-ttu-id="96d95-126">As variáveis que são [tipos de referência](programming-guide/types/index.md#reference-types) mantêm uma referência ao local na memória.</span><span class="sxs-lookup"><span data-stu-id="96d95-126">Variables that are [reference types](programming-guide/types/index.md#reference-types) hold a reference to the location in memory.</span></span> <span data-ttu-id="96d95-127">As variáveis que são [tipos de valor](programming-guide/types/index.md#value-types) contêm diretamente seu valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-127">Variables that are [value types](programming-guide/types/index.md#value-types) directly contain their value.</span></span> <span data-ttu-id="96d95-128">Essas diferenças realçam as principais diferenças que são importantes para gerenciar recursos de memória.</span><span class="sxs-lookup"><span data-stu-id="96d95-128">These differences highlight the key differences that are important for managing memory resources.</span></span> <span data-ttu-id="96d95-129">Normalmente, os **tipos de valor** são copiados quando passados para um método ou retornados de um método.</span><span class="sxs-lookup"><span data-stu-id="96d95-129">**Value types** are typically copied when passed to a method or returned from a method.</span></span> <span data-ttu-id="96d95-130">Esse comportamento inclui copiar o valor do `this` ao chamar membros de um tipo de valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-130">This behavior includes copying the value of `this` when calling members of a value type.</span></span> <span data-ttu-id="96d95-131">O custo da cópia está relacionado ao tamanho do tipo.</span><span class="sxs-lookup"><span data-stu-id="96d95-131">The cost of the copy is related to the size of the type.</span></span> <span data-ttu-id="96d95-132">Os **tipos de referência** são alocados no heap gerenciado.</span><span class="sxs-lookup"><span data-stu-id="96d95-132">**Reference types** are allocated on the managed heap.</span></span> <span data-ttu-id="96d95-133">Cada novo objeto requer uma nova alocação e, subsequentemente, deve ser recuperado.</span><span class="sxs-lookup"><span data-stu-id="96d95-133">Each new object requires a new allocation, and subsequently must be reclaimed.</span></span> <span data-ttu-id="96d95-134">Ambas as operações levam tempo.</span><span class="sxs-lookup"><span data-stu-id="96d95-134">Both these operations take time.</span></span> <span data-ttu-id="96d95-135">A referência é copiada quando um tipo de referência é passado como um argumento para um método ou retornado de um método.</span><span class="sxs-lookup"><span data-stu-id="96d95-135">The reference is copied when a reference type is passed as an argument to a method or returned from a method.</span></span>

<span data-ttu-id="96d95-136">Este artigo usa o seguinte exemplo de conceito da estrutura de ponto 3D para explicar estas recomendações:</span><span class="sxs-lookup"><span data-stu-id="96d95-136">This article uses the following example concept of the 3D-point structure to explain these recommendations:</span></span>

```csharp
public struct Point3D
{
    public double X;
    public double Y;
    public double Z;
}
```

<span data-ttu-id="96d95-137">diferentes exemplos usam diferentes implementações deste conceito.</span><span class="sxs-lookup"><span data-stu-id="96d95-137">Different examples use different implementations of this concept.</span></span>

## <a name="declare-readonly-structs-for-immutable-value-types"></a><span data-ttu-id="96d95-138">Declarar structs somente leitura para tipos de valor imutáveis</span><span class="sxs-lookup"><span data-stu-id="96d95-138">Declare readonly structs for immutable value types</span></span>

<span data-ttu-id="96d95-139">Declarar um `struct` usando o modificador `readonly` informa ao compilador que sua intenção é criar um tipo imutável.</span><span class="sxs-lookup"><span data-stu-id="96d95-139">Declaring a `struct` using the `readonly` modifier informs the compiler that your intent is to create an immutable type.</span></span> <span data-ttu-id="96d95-140">O compilador impõe essa decisão de design com as seguintes regras:</span><span class="sxs-lookup"><span data-stu-id="96d95-140">The compiler enforces that design decision with the following rules:</span></span>

- <span data-ttu-id="96d95-141">Todos os membros do campo devem ser `readonly`</span><span class="sxs-lookup"><span data-stu-id="96d95-141">All field members must be `readonly`</span></span>
- <span data-ttu-id="96d95-142">Todas as propriedades devem ser somente leitura, incluindo propriedades autoimplementadas.</span><span class="sxs-lookup"><span data-stu-id="96d95-142">All properties must be read-only, including auto-implemented properties.</span></span>

<span data-ttu-id="96d95-143">Essas duas regras são suficientes para garantir que nenhum membro de um `readonly struct` modifica o estado desse struct.</span><span class="sxs-lookup"><span data-stu-id="96d95-143">These two rules are sufficient to ensure that no member of a `readonly struct` modifies the state of that struct.</span></span> <span data-ttu-id="96d95-144">O `struct` é imutável.</span><span class="sxs-lookup"><span data-stu-id="96d95-144">The `struct` is immutable.</span></span> <span data-ttu-id="96d95-145">A estrutura `Point3D` pode ser definida como um struct imutável, conforme mostrado no exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="96d95-145">The `Point3D` structure could be defined as an immutable struct as shown in the following example:</span></span>

```csharp
readonly public struct ReadonlyPoint3D
{
    public ReadonlyPoint3D(double x, double y, double z)
    {
        this.X = x;
        this.Y = y;
        this.Z = z;
    }

    public double X { get; }
    public double Y { get; }
    public double Z { get; }
}
```

<span data-ttu-id="96d95-146">Siga esta recomendação sempre que sua intenção de design for criar um tipo de valor imutável.</span><span class="sxs-lookup"><span data-stu-id="96d95-146">Follow this recommendation whenever your design intent is to create an immutable value type.</span></span> <span data-ttu-id="96d95-147">Quaisquer melhorias no desempenho são um benefício adicional.</span><span class="sxs-lookup"><span data-stu-id="96d95-147">Any performance improvements are an added benefit.</span></span> <span data-ttu-id="96d95-148">O `readonly struct` expressa claramente a intenção do design.</span><span class="sxs-lookup"><span data-stu-id="96d95-148">The `readonly struct` clearly expresses your design intent.</span></span>

## <a name="use-ref-readonly-return-statements-for-large-structures-when-possible"></a><span data-ttu-id="96d95-149">Use instruções `ref readonly return` para grandes estruturas, quando possível</span><span class="sxs-lookup"><span data-stu-id="96d95-149">Use `ref readonly return` statements for large structures when possible</span></span>

<span data-ttu-id="96d95-150">É possível retornar valores por referência quando o valor que está sendo retornado não é local para o método de retorno.</span><span class="sxs-lookup"><span data-stu-id="96d95-150">You can return values by reference when the value being returned isn't local to the returning method.</span></span> <span data-ttu-id="96d95-151">Retornar por referência significa que somente a referência é copiada, não a estrutura.</span><span class="sxs-lookup"><span data-stu-id="96d95-151">Returning by reference means that only the reference is copied, not the structure.</span></span> <span data-ttu-id="96d95-152">No exemplo a seguir, a propriedade `Origin` não pode usar um retorno `ref`, porque o valor que está sendo retornado é uma variável local:</span><span class="sxs-lookup"><span data-stu-id="96d95-152">In the following example, the `Origin` property can't use a `ref` return because the value being returned is a local variable:</span></span>

```csharp
public Point3D Origin => new Point3D(0,0,0);
```

<span data-ttu-id="96d95-153">No entanto, a seguinte definição de propriedade pode ser retornada por referência, porque o valor retornado é um membro estático:</span><span class="sxs-lookup"><span data-stu-id="96d95-153">However, the following property definition can be returned by reference because the returned value is a static member:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    // Dangerous! returning a mutable reference to internal storage
    public ref Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="96d95-154">Você não deseja chamadores que modificam a origem, então deve retornar o valor por `readonly ref`:</span><span class="sxs-lookup"><span data-stu-id="96d95-154">You don't want callers modifying the origin, so you should return the value by `readonly ref`:</span></span>

```csharp
public struct Point3D
{
    private static Point3D origin = new Point3D(0,0,0);

    public static ref readonly Point3D Origin => ref origin;

    // other members removed for space
}
```

<span data-ttu-id="96d95-155">Retornar `ref readonly` permite que você salvar a cópia de estruturas maiores e preserve a imutabilidade de seus membros de dados internos.</span><span class="sxs-lookup"><span data-stu-id="96d95-155">Returning `ref readonly` enables you to save copying larger structures and preserve the immutability of your internal data members.</span></span>

<span data-ttu-id="96d95-156">No site de chamada, os chamadores fazem a opção de usar a propriedade `Origin` como um `readonly ref` ou como um valor:</span><span class="sxs-lookup"><span data-stu-id="96d95-156">At the call site, callers make the choice to use the `Origin` property as a `readonly ref` or as a value:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="96d95-157">A primeira atribuição no código anterior faz uma cópia da constante `Origin` e atribui essa cópia.</span><span class="sxs-lookup"><span data-stu-id="96d95-157">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="96d95-158">A segunda atribui uma referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-158">The second assigns a reference.</span></span> <span data-ttu-id="96d95-159">Observe que o modificador `readonly` deve ser parte da declaração da variável.</span><span class="sxs-lookup"><span data-stu-id="96d95-159">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="96d95-160">A referência à qual ele se relaciona não pode ser modificada.</span><span class="sxs-lookup"><span data-stu-id="96d95-160">The reference to which it refers can't be modified.</span></span> <span data-ttu-id="96d95-161">As tentativas de modificá-la resultam em um erro em tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="96d95-161">Attempts to do so result in a compile-time error.</span></span>

<span data-ttu-id="96d95-162">O modificador `readonly` é necessário na declaração de `originReference`.</span><span class="sxs-lookup"><span data-stu-id="96d95-162">The `readonly` modifier is required on the declaration of `originReference`.</span></span>

<span data-ttu-id="96d95-163">O compilador impõe que o autor da chamada não pode modificar a referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-163">The compiler enforces that the caller can't modify the reference.</span></span> <span data-ttu-id="96d95-164">As tentativas de atribuir o valor diretamente geram um erro em tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="96d95-164">Attempts to assign the value directly generate a compile-time error.</span></span> <span data-ttu-id="96d95-165">No entanto, o compilador não pode saber se algum método de membro modifica o estado do struct.</span><span class="sxs-lookup"><span data-stu-id="96d95-165">However, the compiler can't know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="96d95-166">Para garantir que o objeto não será modificado, o compilador cria uma cópia e chama as referências de membro usando essa cópia.</span><span class="sxs-lookup"><span data-stu-id="96d95-166">To ensure that the object isn't modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="96d95-167">Todas as modificações são para essa cópia de defesa.</span><span class="sxs-lookup"><span data-stu-id="96d95-167">Any modifications are to that defensive copy.</span></span>

## <a name="apply-the-in-modifier-to-readonly-struct-parameters-larger-than-systemintptrsize"></a><span data-ttu-id="96d95-168">Aplique o modificador `in` a parâmetros `readonly struct` maiores que `System.IntPtr.Size`</span><span class="sxs-lookup"><span data-stu-id="96d95-168">Apply the `in` modifier to `readonly struct` parameters larger than `System.IntPtr.Size`</span></span>

<span data-ttu-id="96d95-169">A palavra-chave `in` complementa as palavras-chave `ref` e `out` existentes para passar argumentos por referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-169">The `in` keyword complements the existing `ref` and `out` keywords to pass arguments by reference.</span></span> <span data-ttu-id="96d95-170">A palavra-chave `in` especifica a passagem do argumento por referência, mas o método chamado não modifica o valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-170">The `in` keyword specifies passing the argument by reference, but the called method doesn't modify the value.</span></span>

<span data-ttu-id="96d95-171">Essa adição fornece um vocabulário completo para expressar sua intenção de design.</span><span class="sxs-lookup"><span data-stu-id="96d95-171">This addition provides a full vocabulary to express your design intent.</span></span>
<span data-ttu-id="96d95-172">Os tipos de valor são copiados no momento em que são passados para um método chamado quando você não especifica nenhum dos modificadores a seguir na assinatura do método.</span><span class="sxs-lookup"><span data-stu-id="96d95-172">Value types are copied when passed to a called method when you don't specify any of the following modifiers in the method signature.</span></span> <span data-ttu-id="96d95-173">Cada um desses modificadores especifica que uma variável é passada por referência, evitando a cópia.</span><span class="sxs-lookup"><span data-stu-id="96d95-173">Each of these modifiers specifies that a variable is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="96d95-174">Cada modificador expressa uma intenção diferente:</span><span class="sxs-lookup"><span data-stu-id="96d95-174">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="96d95-175">`out`: esse método define o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="96d95-175">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="96d95-176">`ref`: esse método pode definir o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="96d95-176">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="96d95-177">`in`: esse método não modifica o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="96d95-177">`in`: This method doesn't modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="96d95-178">Adicione o modificador `in` para passar um argumento por referência e declare que sua intenção de design é passar argumentos por referência para evitar cópias desnecessárias.</span><span class="sxs-lookup"><span data-stu-id="96d95-178">Add the `in` modifier to pass an argument by reference and declare your design intent to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="96d95-179">Você não pretende modificar o objeto usado como esse argumento.</span><span class="sxs-lookup"><span data-stu-id="96d95-179">You don't intend to modify the object used as that argument.</span></span>

<span data-ttu-id="96d95-180">Essa prática geralmente melhora o desempenho para tipos de valor somente leitura que são maiores que <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="96d95-180">This practice often improves performance for readonly value types that are larger than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span> <span data-ttu-id="96d95-181">Para tipos simples (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` e `bool`, e tipos `enum`), eventuais ganhos no desempenho são mínimos.</span><span class="sxs-lookup"><span data-stu-id="96d95-181">For simple types (`sbyte`, `byte`, `short`, `ushort`, `int`, `uint`, `long`, `ulong`, `char`, `float`, `double`, `decimal` and `bool`, and `enum` types), any potential performance gains are minimal.</span></span> <span data-ttu-id="96d95-182">Na verdade, o desempenho pode ser degradado usando a passagem por referência para tipos menores que <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="96d95-182">In fact, performance may degrade by using pass-by-reference for types smaller than <xref:System.IntPtr.Size?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="96d95-183">O código a seguir mostra um exemplo de um método que calcula a distância entre dois pontos no espaço 3D.</span><span class="sxs-lookup"><span data-stu-id="96d95-183">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="96d95-184">Os argumentos são duas estruturas que contêm três duplas.</span><span class="sxs-lookup"><span data-stu-id="96d95-184">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="96d95-185">Uma dupla tem 8 bytes. Então, cada argumento tem 24 bytes.</span><span class="sxs-lookup"><span data-stu-id="96d95-185">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="96d95-186">Ao especificar o modificador `in`, você passa uma referência de 4 ou 8 bytes para esses argumentos, dependendo da arquitetura do computador.</span><span class="sxs-lookup"><span data-stu-id="96d95-186">By specifying the `in` modifier, you pass a 4 byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="96d95-187">A diferença no tamanho é pequena, mas ela aumenta rapidamente quando o aplicativo chama esse método em um loop estreito, usando muitos valores diferentes.</span><span class="sxs-lookup"><span data-stu-id="96d95-187">The difference in size is small, but it adds up when your application calls this method in a tight loop using many different values.</span></span>

<span data-ttu-id="96d95-188">O modificador `in` complementa `out` e `ref` de outras formas também.</span><span class="sxs-lookup"><span data-stu-id="96d95-188">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="96d95-189">Não é possível criar sobrecargas de um método que diferem somente na presença de `in`, `out` ou `ref`.</span><span class="sxs-lookup"><span data-stu-id="96d95-189">You can't create overloads of a method that differ only in the presence of `in`, `out`, or `ref`.</span></span> <span data-ttu-id="96d95-190">Essas novas regras apresentam o mesmo comportamento que sempre foi definido para os parâmetros `out` e `ref`.</span><span class="sxs-lookup"><span data-stu-id="96d95-190">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span> <span data-ttu-id="96d95-191">Como os modificadores `out` e `ref`, os tipos de valor não estão demarcados, porque o modificador `in` é aplicado.</span><span class="sxs-lookup"><span data-stu-id="96d95-191">Like the `out` and `ref` modifiers, value types aren't boxed because the `in` modifier is applied.</span></span>

<span data-ttu-id="96d95-192">O modificador `in` pode ser aplicado a qualquer membro que usa os seguintes parâmetros: métodos, delegados, lambdas, funções locais, indexadores e operadores.</span><span class="sxs-lookup"><span data-stu-id="96d95-192">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="96d95-193">Outro recurso dos parâmetros `in` é que você pode usar valores literais ou constantes para o argumento para um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-193">Another feature of `in` parameters is that you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="96d95-194">Além disso, ao contrário de um parâmetro `ref` ou `out`, você não precisa aplicar o modificador `in` no local da chamada.</span><span class="sxs-lookup"><span data-stu-id="96d95-194">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="96d95-195">O código a seguir mostra dois exemplos para chamar o método `CalculateDistance`.</span><span class="sxs-lookup"><span data-stu-id="96d95-195">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="96d95-196">O primeiro usa duas variáveis locais transmitidas por referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-196">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="96d95-197">O segundo inclui uma variável temporária criada como parte da chamada de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-197">The second includes a temporary variable created as part of the method call.</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="96d95-198">Há várias maneiras pelas quais um compilador impõe a natureza somente leitura de um argumento `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-198">There are several ways in which the compiler enforces the read-only nature of an `in` argument.</span></span>  <span data-ttu-id="96d95-199">Em primeiro lugar, o método chamado não pode ser atribuído diretamente a um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-199">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="96d95-200">Não é possível atribuí-lo diretamente a nenhum campo de um parâmetro `in` quando esse valor é um tipo `struct`.</span><span class="sxs-lookup"><span data-stu-id="96d95-200">It can't directly assign to any field of an `in` parameter when that value is a `struct` type.</span></span> <span data-ttu-id="96d95-201">Além disso, não é possível passar um parâmetro `in` para nenhum método usando o modificador `ref` ou `out`.</span><span class="sxs-lookup"><span data-stu-id="96d95-201">In addition, you can't pass an `in` parameter to any method using the `ref` or `out` modifier.</span></span>
<span data-ttu-id="96d95-202">Essas regras se aplicam a qualquer campo de um parâmetro `in`, considerando que o campo seja um tipo `struct` e o parâmetro também seja um tipo `struct`.</span><span class="sxs-lookup"><span data-stu-id="96d95-202">These rules apply to any field of an `in` parameter, provided the field is a `struct` type and the parameter is also a `struct` type.</span></span> <span data-ttu-id="96d95-203">Na verdade, essas regras são aplicadas a várias camadas de acesso de membro, considerando que os tipos, em todos os níveis de acesso de membro, sejam `structs`.</span><span class="sxs-lookup"><span data-stu-id="96d95-203">In fact, these rules apply for multiple layers of member access provided the types at all levels of member access are `structs`.</span></span>
<span data-ttu-id="96d95-204">O compilador impõe que os tipos `struct` passados como argumentos `in` e seus membros `struct` sejam variáveis somente leitura quando usados como argumentos para outros métodos.</span><span class="sxs-lookup"><span data-stu-id="96d95-204">The compiler enforces that `struct` types passed as  `in` arguments and their `struct` members are read-only variables when used as arguments to other methods.</span></span>

<span data-ttu-id="96d95-205">O uso de parâmetros `in` pode evitar os possíveis custos de desempenho com a realização de cópias.</span><span class="sxs-lookup"><span data-stu-id="96d95-205">The use of `in` parameters can avoid the potential performance costs of making copies.</span></span> <span data-ttu-id="96d95-206">Isso não altera a semântica de nenhuma chamada de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-206">It doesn't change the semantics of any method call.</span></span> <span data-ttu-id="96d95-207">Portanto, não é necessário especificar o modificador `in` no site de chamada.</span><span class="sxs-lookup"><span data-stu-id="96d95-207">Therefore, you don't need to specify the `in` modifier at the call site.</span></span> <span data-ttu-id="96d95-208">A omissão do modificador `in` no site de chamada informa ao compilador que ele tem permissão para fazer uma cópia do argumento pelos seguintes motivos:</span><span class="sxs-lookup"><span data-stu-id="96d95-208">Omitting the `in` modifier at the call site informs the compiler that it's allowed to make a copy of the argument for the following reasons:</span></span>

- <span data-ttu-id="96d95-209">Existe uma conversão implícita, mas não uma conversão de identidade do tipo de argumento no tipo de parâmetro.</span><span class="sxs-lookup"><span data-stu-id="96d95-209">There exists an implicit conversion but not an identity conversion from the argument type to the parameter type.</span></span>
- <span data-ttu-id="96d95-210">O argumento é uma expressão, mas não tem uma variável de armazenamento conhecida.</span><span class="sxs-lookup"><span data-stu-id="96d95-210">The argument is an expression but doesn't have a known storage variable.</span></span>
- <span data-ttu-id="96d95-211">Há uma sobrecarga que é distinguível pela presença ou ausência de `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-211">An overload exists that differs by the presence or absence of `in`.</span></span> <span data-ttu-id="96d95-212">Nesse caso, a sobrecarga pelo valor é uma correspondência melhor.</span><span class="sxs-lookup"><span data-stu-id="96d95-212">In that case, the by value overload is a better match.</span></span>

<span data-ttu-id="96d95-213">Essas regras são úteis conforme você atualiza o código existente para usar argumentos de referência somente leitura.</span><span class="sxs-lookup"><span data-stu-id="96d95-213">These rules are useful as you update existing code to use read-only reference arguments.</span></span> <span data-ttu-id="96d95-214">Dentro do método chamado, você pode chamar qualquer método de instância que use parâmetros por valor.</span><span class="sxs-lookup"><span data-stu-id="96d95-214">Inside the called method, you can call any instance method that uses by value parameters.</span></span> <span data-ttu-id="96d95-215">Nessas instâncias, uma cópia do parâmetro `in` é criada.</span><span class="sxs-lookup"><span data-stu-id="96d95-215">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="96d95-216">Uma vez que o compilador pode criar uma variável temporária para qualquer parâmetro `in`, você também pode especificar valores padrão para qualquer parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-216">Because the compiler may create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="96d95-217">O código a seguir especifica a origem (ponto 0,0) como o valor padrão para o segundo ponto:</span><span class="sxs-lookup"><span data-stu-id="96d95-217">The following code specifies the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="96d95-218">Para forçar o compilador a passar argumentos somente leitura por referência, especifique o modificador `in` nos argumentos no site de chamada, conforme mostrado no código a seguir:</span><span class="sxs-lookup"><span data-stu-id="96d95-218">To force the compiler to pass read-only arguments by reference, specify the `in` modifier on the arguments at the call site, as shown in the following code:</span></span>

[!code-csharp[UseInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ExplicitInArgument "Specifying an In argument")]

<span data-ttu-id="96d95-219">Esse comportamento facilita a adoção de parâmetros `in` ao longo do tempo nas grandes bases de código em que os ganhos de desempenho são possíveis.</span><span class="sxs-lookup"><span data-stu-id="96d95-219">This behavior makes it easier to adopt `in` parameters over time in large codebases where performance gains are possible.</span></span> <span data-ttu-id="96d95-220">Primeiro você adiciona o modificador `in` às assinaturas de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-220">You add the `in` modifier to method signatures first.</span></span> <span data-ttu-id="96d95-221">Em seguida, você adiciona o modificador `in` em sites de chamada e cria tipos `readonly struct` para que o compilador evite criar cópias de defesa de parâmetros `in` em mais locais.</span><span class="sxs-lookup"><span data-stu-id="96d95-221">Then, you can add the `in` modifier at call sites and create `readonly struct` types to enable the compiler to avoid creating defensive copies of `in` parameters in more locations.</span></span>

<span data-ttu-id="96d95-222">A designação do parâmetro `in` também pode ser usada com tipos de referência ou valores numéricos.</span><span class="sxs-lookup"><span data-stu-id="96d95-222">The `in` parameter designation can also be used with reference types or numeric values.</span></span> <span data-ttu-id="96d95-223">No entanto, os benefícios em ambos os casos serão mínimos, se houver.</span><span class="sxs-lookup"><span data-stu-id="96d95-223">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="never-use-mutable-structs-as-in-in-argument"></a><span data-ttu-id="96d95-224">Nunca use structs mutáveis como no argumento `in`</span><span class="sxs-lookup"><span data-stu-id="96d95-224">Never use mutable structs as in `in` argument</span></span>

<span data-ttu-id="96d95-225">As técnicas descritas acima explicam como evitar cópias retornando referências e passando valores por referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-225">The techniques described above explain how to avoid copies by returning references and passing values by reference.</span></span> <span data-ttu-id="96d95-226">Essas técnicas funcionam melhor quando os tipos de argumento são declarados como tipos `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="96d95-226">These techniques work best when the argument types are declared as `readonly struct` types.</span></span> <span data-ttu-id="96d95-227">Caso contrário, o compilador deve criar **cópias de defesa** em muitas situações, para impor a qualidade de somente leitura de quaisquer argumentos.</span><span class="sxs-lookup"><span data-stu-id="96d95-227">Otherwise, the compiler must create **defensive copies** in many situations to enforce the readonly-ness of any arguments.</span></span> <span data-ttu-id="96d95-228">Considere o exemplo a seguir que calcula a distância de um ponto 3D da origem:</span><span class="sxs-lookup"><span data-stu-id="96d95-228">Consider the following example that calculates the distance of a 3D point from the origin:</span></span>

[!code-csharp[InArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#InArgument "Specifying an in argument")]

<span data-ttu-id="96d95-229">A estrutura `Point3D` *não* é um struct somente leitura.</span><span class="sxs-lookup"><span data-stu-id="96d95-229">The `Point3D` structure is *not* a readonly struct.</span></span> <span data-ttu-id="96d95-230">Há seis chamadas de acesso de propriedade diferentes no corpo deste método.</span><span class="sxs-lookup"><span data-stu-id="96d95-230">There are six different property access calls in the body of this method.</span></span> <span data-ttu-id="96d95-231">No primeiro exame, você pode ter achado que esses acessos estavam seguros.</span><span class="sxs-lookup"><span data-stu-id="96d95-231">On first examination, you may have thought these accesses were safe.</span></span> <span data-ttu-id="96d95-232">No fim das contas, um acessador `get` não deve modificar o estado do objeto.</span><span class="sxs-lookup"><span data-stu-id="96d95-232">After all, a `get` accessor shouldn't modify the state of the object.</span></span> <span data-ttu-id="96d95-233">Mas não há nenhuma regra de linguagem que impõe isso.</span><span class="sxs-lookup"><span data-stu-id="96d95-233">But there's no language rule that enforces that.</span></span> <span data-ttu-id="96d95-234">É apenas uma convenção comum.</span><span class="sxs-lookup"><span data-stu-id="96d95-234">It's only a common convention.</span></span> <span data-ttu-id="96d95-235">Qualquer tipo pode implementar um acessador `get` que modificou o estado interno.</span><span class="sxs-lookup"><span data-stu-id="96d95-235">Any type could implement a `get` accessor that modified the internal state.</span></span> <span data-ttu-id="96d95-236">Sem alguma garantia de linguagem, o compilador deve criar uma cópia temporária do argumento antes de chamar qualquer membro.</span><span class="sxs-lookup"><span data-stu-id="96d95-236">Without some language guarantee, the compiler must create a temporary copy of the argument before calling any member.</span></span> <span data-ttu-id="96d95-237">O armazenamento temporário é criado na pilha, os valores do argumento são copiados para o armazenamento temporário e o valor é copiado para a pilha para cada acesso de membro como o argumento `this`.</span><span class="sxs-lookup"><span data-stu-id="96d95-237">The temporary storage is created on the stack, the values of the argument are copied to the temporary storage, and the value is copied to the stack for each member access as the `this` argument.</span></span> <span data-ttu-id="96d95-238">Em muitas situações, essas cópias prejudicam tanto o desempenho que a passagem por valor é mais rápida do que a passagem por referência somente leitura quando o tipo de argumento não é um `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="96d95-238">In many situations, these copies harm performance enough that pass-by-value is faster than pass-by-readonly-reference when the argument type isn't a `readonly struct`.</span></span>

<span data-ttu-id="96d95-239">Em vez disso, se o cálculo da distância usa um struct imutável, `ReadonlyPoint3D`, os objetos temporários não são necessários:</span><span class="sxs-lookup"><span data-stu-id="96d95-239">Instead, if the distance calculation uses the immutable struct, `ReadonlyPoint3D`, temporary objects are not needed:</span></span>

[!code-csharp[readonlyInArgument](../../samples/csharp/safe-efficient-code/ref-readonly-struct/Program.cs#ReadOnlyInArgument "Specifying a readonly in argument")]

<span data-ttu-id="96d95-240">O compilador gera um código mais eficiente quando você chama os membros de um `readonly struct`: A referência `this`, em vez de uma cópia do destinatário, sempre é um parâmetro `in` passado por referência para o método do membro.</span><span class="sxs-lookup"><span data-stu-id="96d95-240">The compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="96d95-241">Essa otimização economiza cópias quando você usa um `readonly struct` como um argumento `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-241">This optimization saves copying when you use a `readonly struct` as an `in` argument.</span></span>

<span data-ttu-id="96d95-242">Você não deve passar um tipo de valor anulável como um argumento `in`.</span><span class="sxs-lookup"><span data-stu-id="96d95-242">You should not pass a nullable value type as an `in` argument.</span></span> <span data-ttu-id="96d95-243">O tipo <xref:System.Nullable%601> não é declarado como um struct somente leitura.</span><span class="sxs-lookup"><span data-stu-id="96d95-243">The <xref:System.Nullable%601> type is not declared as a read-only struct.</span></span> <span data-ttu-id="96d95-244">Isso significa que o compilador deve gerar cópias defensivas para qualquer argumento de tipo de valor anulável passado para um método usando o modificador `in` na declaração de parâmetro.</span><span class="sxs-lookup"><span data-stu-id="96d95-244">That means the compiler must generate defensive copies for any nullable value type argument passed to a method using the `in` modifier on the parameter declaration.</span></span>

<span data-ttu-id="96d95-245">É possível ver um programa de exemplo que demonstra as diferenças de desempenho que usam [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) em nosso [repositório de exemplos](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) no GitHub.</span><span class="sxs-lookup"><span data-stu-id="96d95-245">You can see an example program that demonstrates the performance differences using [Benchmark.net](https://www.nuget.org/packages/BenchmarkDotNet/) in our [samples repository](https://github.com/dotnet/samples/tree/master/csharp/safe-efficient-code/benchmark) on GitHub.</span></span> <span data-ttu-id="96d95-246">Ele compara a passagem de um struct mutável por valor e por referência com a passagem de um struct imutável por valor e por referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-246">It compares passing a mutable struct by value and by reference with passing an immutable struct by value and by reference.</span></span> <span data-ttu-id="96d95-247">O uso do struct imutável e da passagem por referência é mais rápido.</span><span class="sxs-lookup"><span data-stu-id="96d95-247">The use of the immutable struct and pass by reference is fastest.</span></span>

## <a name="use-ref-struct-types-to-work-with-blocks-or-memory-on-a-single-stack-frame"></a><span data-ttu-id="96d95-248">Use tipos `ref struct` para trabalhar com blocos ou memória em um único registro de ativação</span><span class="sxs-lookup"><span data-stu-id="96d95-248">Use `ref struct` types to work with blocks or memory on a single stack frame</span></span>

<span data-ttu-id="96d95-249">Um recurso de linguagem relacionado é a capacidade de declarar um tipo de valor que deve ser restrito para um único registro de ativação.</span><span class="sxs-lookup"><span data-stu-id="96d95-249">A related language feature is the ability to declare a value type that must be constrained to a single stack frame.</span></span> <span data-ttu-id="96d95-250">Essa restrição permite que o compilador faça várias otimizações.</span><span class="sxs-lookup"><span data-stu-id="96d95-250">This restriction enables the compiler to make several optimizations.</span></span> <span data-ttu-id="96d95-251">A principal motivação para esse recurso foi <xref:System.Span%601> e as estruturas relacionadas.</span><span class="sxs-lookup"><span data-stu-id="96d95-251">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="96d95-252">Você obterá melhorias de desempenho com esses aprimoramentos usando APIs do .NET novas e atualizadas que fazem uso do tipo <xref:System.Span%601>.</span><span class="sxs-lookup"><span data-stu-id="96d95-252">You'll achieve performance improvements from these enhancements by using new and updated .NET APIs that make use of the <xref:System.Span%601> type.</span></span>

<span data-ttu-id="96d95-253">Você pode ter requisitos semelhantes funcionando com a memória criada usando [`stackalloc`](language-reference/operators/stackalloc.md) ou ao usar a memória das APIs de interoperabilidade.</span><span class="sxs-lookup"><span data-stu-id="96d95-253">You may have similar requirements working with memory created using [`stackalloc`](language-reference/operators/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="96d95-254">Você pode definir seus próprios tipos `ref struct` para essas necessidades.</span><span class="sxs-lookup"><span data-stu-id="96d95-254">You can define your own `ref struct` types for those needs.</span></span>

## <a name="readonly-ref-struct-type"></a><span data-ttu-id="96d95-255">Tipo `readonly ref struct`</span><span class="sxs-lookup"><span data-stu-id="96d95-255">`readonly ref struct` type</span></span>

<span data-ttu-id="96d95-256">A declaração de um struct como `readonly ref` combina os benefícios e as restrições das declarações `ref struct` e `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="96d95-256">Declaring a struct as `readonly ref` combines the benefits and restrictions of `ref struct` and `readonly struct` declarations.</span></span> <span data-ttu-id="96d95-257">A memória usada pelo alcance somente leitura está restrita a um único registro de ativação, e a memória usada pelo alcance de somente leitura não pode ser modificada.</span><span class="sxs-lookup"><span data-stu-id="96d95-257">The memory used by the readonly span is restricted to a single stack frame, and the memory used by the readonly span can't be modified.</span></span>

## <a name="conclusions"></a><span data-ttu-id="96d95-258">Conclusões</span><span class="sxs-lookup"><span data-stu-id="96d95-258">Conclusions</span></span>

<span data-ttu-id="96d95-259">Usar tipos de valor minimiza o número de operações de alocação:</span><span class="sxs-lookup"><span data-stu-id="96d95-259">Using value types minimizes the number of allocation operations:</span></span>

- <span data-ttu-id="96d95-260">o armazenamento para tipos de valor é alocado em pilhas para variáveis locais e argumentos de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-260">Storage for value types is stack allocated for local variables and method arguments.</span></span>
- <span data-ttu-id="96d95-261">o armazenamento para tipos de valor que são membros de outros objetos é alocado como parte desse objeto, não como uma alocação separada.</span><span class="sxs-lookup"><span data-stu-id="96d95-261">Storage for value types that are members of other objects is allocated as part of that object, not as a separate allocation.</span></span>
- <span data-ttu-id="96d95-262">o armazenamento para valores retornados de tipo de valor é alocado em pilhas.</span><span class="sxs-lookup"><span data-stu-id="96d95-262">Storage for value type return values is stack allocated.</span></span>

<span data-ttu-id="96d95-263">Compare isso com tipos de referência nestas mesmas situações:</span><span class="sxs-lookup"><span data-stu-id="96d95-263">Contrast that with reference types in those same situations:</span></span>

- <span data-ttu-id="96d95-264">o armazenamento para tipos de referência é alocado em heap para variáveis locais e argumentos de método.</span><span class="sxs-lookup"><span data-stu-id="96d95-264">Storage for reference types are heap allocated for local variables and method arguments.</span></span> <span data-ttu-id="96d95-265">A referência é armazenada na pilha.</span><span class="sxs-lookup"><span data-stu-id="96d95-265">The reference is stored on the stack.</span></span>
- <span data-ttu-id="96d95-266">O armazenamento para tipos de referência que são membros de outros objetos são alocados separadamente no heap.</span><span class="sxs-lookup"><span data-stu-id="96d95-266">Storage for reference types that are members of other objects are separately allocated on the heap.</span></span> <span data-ttu-id="96d95-267">O objeto recipiente armazena a referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-267">The containing object stores the reference.</span></span>
- <span data-ttu-id="96d95-268">O armazenamento para valores retornados de tipo de referência é alocado em heap.</span><span class="sxs-lookup"><span data-stu-id="96d95-268">Storage for reference type return values is heap allocated.</span></span> <span data-ttu-id="96d95-269">A referência a esse armazenamento é armazenada na pilha.</span><span class="sxs-lookup"><span data-stu-id="96d95-269">The reference to that storage is stored on the stack.</span></span>

<span data-ttu-id="96d95-270">Minimizar alocações implica compensações.</span><span class="sxs-lookup"><span data-stu-id="96d95-270">Minimizing allocations comes with tradeoffs.</span></span> <span data-ttu-id="96d95-271">Você copia mais memória quando o tamanho do `struct` é maior que o tamanho de uma referência.</span><span class="sxs-lookup"><span data-stu-id="96d95-271">You copy more memory when the size of the `struct` is larger than the size of a reference.</span></span> <span data-ttu-id="96d95-272">Normalmente, uma referência é 64 ou 32 bits e depende da CPU do computador de destino.</span><span class="sxs-lookup"><span data-stu-id="96d95-272">A reference is typically 64 bits or 32 bits, and depends on the target machine CPU.</span></span>

<span data-ttu-id="96d95-273">Essas compensações geralmente têm o mínimo de impacto no desempenho.</span><span class="sxs-lookup"><span data-stu-id="96d95-273">These tradeoffs generally have minimal performance impact.</span></span> <span data-ttu-id="96d95-274">No entanto, para estruturas grandes ou coleções maiores, o impacto no desempenho aumenta.</span><span class="sxs-lookup"><span data-stu-id="96d95-274">However, for large structs or larger collections, the performance impact increases.</span></span> <span data-ttu-id="96d95-275">O impacto pode ser grande em loops estreitos e em afunilamentos para programas.</span><span class="sxs-lookup"><span data-stu-id="96d95-275">The impact can be large in tight loops and hot paths for programs.</span></span>

<span data-ttu-id="96d95-276">Esses aprimoramentos na linguagem C# são criados para algoritmos de desempenho críticos, nos quais as alocações de memória são um importante fator para alcançar o desempenho necessário.</span><span class="sxs-lookup"><span data-stu-id="96d95-276">These enhancements to the C# language are designed for performance critical algorithms where minimizing memory allocations is a major factor in achieving the necessary performance.</span></span> <span data-ttu-id="96d95-277">Você pode achar que geralmente não usa esses recursos no código que grava.</span><span class="sxs-lookup"><span data-stu-id="96d95-277">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="96d95-278">No entanto, esses aprimoramentos foram adotados por meio do .NET.</span><span class="sxs-lookup"><span data-stu-id="96d95-278">However, these enhancements have been adopted throughout .NET.</span></span> <span data-ttu-id="96d95-279">À medida que cada vez mais APIs utilizam esses recursos, você verá o desempenho dos seus aplicativos melhorar.</span><span class="sxs-lookup"><span data-stu-id="96d95-279">As more and more APIs make use of these features, you'll see the performance of your applications improve.</span></span>

## <a name="see-also"></a><span data-ttu-id="96d95-280">Consulte também</span><span class="sxs-lookup"><span data-stu-id="96d95-280">See also</span></span>

- [<span data-ttu-id="96d95-281">ref keyword</span><span class="sxs-lookup"><span data-stu-id="96d95-281">ref keyword</span></span>](language-reference/keywords/ref.md)
- [<span data-ttu-id="96d95-282">Retornos de ref e locais de ref</span><span class="sxs-lookup"><span data-stu-id="96d95-282">Ref returns and ref locals</span></span>](programming-guide/classes-and-structs/ref-returns.md)
