---
title: Manipulação de exceções – Guia de Programação em C#
ms.custom: seodec18
ms.date: 07/20/2015
helpviewer_keywords:
- exception handling [C#], about exception handling
- exceptions [C#], handling
ms.assetid: b4e4ecf2-b907-4e58-891f-2563762258e9
ms.openlocfilehash: 5013738e74aaa260ab6f5bcd4d73904cfbdcc3c8
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/19/2019
ms.locfileid: "69590269"
---
# <a name="exception-handling-c-programming-guide"></a><span data-ttu-id="04549-102">Manipulação de exceções (Guia de Programação em C#)</span><span class="sxs-lookup"><span data-stu-id="04549-102">Exception Handling (C# Programming Guide)</span></span>
<span data-ttu-id="04549-103">Um bloco [try](../../language-reference/keywords/try-catch.md) é usado por programadores de C# para particionar o código que pode ser afetado por uma exceção.</span><span class="sxs-lookup"><span data-stu-id="04549-103">A [try](../../language-reference/keywords/try-catch.md) block is used by C# programmers to partition code that might be affected by an exception.</span></span> <span data-ttu-id="04549-104">Os blocos [catch](../../language-reference/keywords/try-catch.md) associados são usados para tratar qualquer exceção resultante.</span><span class="sxs-lookup"><span data-stu-id="04549-104">Associated [catch](../../language-reference/keywords/try-catch.md) blocks are used to handle any resulting exceptions.</span></span> <span data-ttu-id="04549-105">Um bloco [finally](../../language-reference/keywords/try-finally.md) contém código que será executado independentemente de uma exceção ser ou não ser lançada no bloco `try`, como a liberação de recursos que estão alocados no bloco `try`.</span><span class="sxs-lookup"><span data-stu-id="04549-105">A [finally](../../language-reference/keywords/try-finally.md) block contains code that is run regardless of whether or not an exception is thrown in the `try` block, such as releasing resources that are allocated in the `try` block.</span></span> <span data-ttu-id="04549-106">Um bloco `try` exige um ou mais blocos `catch` associados ou um bloco `finally` ou ambos.</span><span class="sxs-lookup"><span data-stu-id="04549-106">A `try` block requires one or more associated `catch` blocks, or a `finally` block, or both.</span></span>  
  
 <span data-ttu-id="04549-107">Os exemplos a seguir mostram uma instrução `try-catch`, uma instrução `try-finally` e um instrução `try-catch-finally`.</span><span class="sxs-lookup"><span data-stu-id="04549-107">The following examples show a `try-catch` statement, a `try-finally` statement, and a `try-catch-finally` statement.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#6](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#6)]  
  
 [!code-csharp[csProgGuideExceptions#7](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#7)]  
  
 [!code-csharp[csProgGuideExceptions#8](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#8)]  
  
 <span data-ttu-id="04549-108">Um bloco `try` sem um bloco `catch` ou `finally` causa um erro do compilador.</span><span class="sxs-lookup"><span data-stu-id="04549-108">A `try` block without a `catch` or `finally` block causes a compiler error.</span></span>  
  
## <a name="catch-blocks"></a><span data-ttu-id="04549-109">Blocos catch</span><span class="sxs-lookup"><span data-stu-id="04549-109">Catch Blocks</span></span>  
 <span data-ttu-id="04549-110">Um bloco `catch` pode especificar o tipo de exceção a ser capturado.</span><span class="sxs-lookup"><span data-stu-id="04549-110">A `catch` block can specify the type of exception to catch.</span></span> <span data-ttu-id="04549-111">A especificação de tipo é chamada de *filtro de exceção*.</span><span class="sxs-lookup"><span data-stu-id="04549-111">The type specification is called an *exception filter*.</span></span> <span data-ttu-id="04549-112">O tipo de exceção deve ser derivado de <xref:System.Exception>.</span><span class="sxs-lookup"><span data-stu-id="04549-112">The exception type should be derived from <xref:System.Exception>.</span></span> <span data-ttu-id="04549-113">Em geral, não especifique <xref:System.Exception> como o filtro de exceção, a menos que você saiba como tratar todas as exceções que podem ser lançadas no bloco `try` ou incluiu uma instrução [throw](../../language-reference/keywords/throw.md) no final do seu bloco `catch`.</span><span class="sxs-lookup"><span data-stu-id="04549-113">In general, do not specify <xref:System.Exception> as the exception filter unless either you know how to handle all exceptions that might be thrown in the `try` block, or you have included a [throw](../../language-reference/keywords/throw.md) statement at the end of your `catch` block.</span></span>  
  
 <span data-ttu-id="04549-114">Vários blocos `catch` com filtros de exceção diferentes podem ser encadeados.</span><span class="sxs-lookup"><span data-stu-id="04549-114">Multiple `catch` blocks with different exception filters can be chained together.</span></span> <span data-ttu-id="04549-115">Os blocos `catch` são avaliados de cima para baixo no seu código, mas somente um bloco `catch` será executado para cada exceção que é lançada.</span><span class="sxs-lookup"><span data-stu-id="04549-115">The `catch` blocks are evaluated from top to bottom in your code, but only one `catch` block is executed for each exception that is thrown.</span></span> <span data-ttu-id="04549-116">O primeiro bloco `catch` que especifica o tipo exato ou uma classe base da exceção lançada será executado.</span><span class="sxs-lookup"><span data-stu-id="04549-116">The first `catch` block that specifies the exact type or a base class of the thrown exception is executed.</span></span> <span data-ttu-id="04549-117">Se nenhum bloco `catch` especificar um filtro de exceção correspondente, um bloco `catch` que não tem um filtro será selecionado, caso haja algum na instrução.</span><span class="sxs-lookup"><span data-stu-id="04549-117">If no `catch` block specifies a matching exception filter, a `catch` block that does not have a filter is selected, if one is present in the statement.</span></span> <span data-ttu-id="04549-118">É importante posicionar os blocos `catch` com os tipos de exceção mais específicos (ou seja, os mais derivados) em primeiro.</span><span class="sxs-lookup"><span data-stu-id="04549-118">It is important to position `catch` blocks with the most specific (that is, the most derived) exception types first.</span></span>  
  
 <span data-ttu-id="04549-119">Você deve capturar exceções quando as seguintes condições forem verdadeiras:</span><span class="sxs-lookup"><span data-stu-id="04549-119">You should catch exceptions when the following conditions are true:</span></span>  
  
- <span data-ttu-id="04549-120">Você tem uma boa compreensão de porque a exceção seria lançada e pode implementar uma recuperação específica, como solicitar que o usuário insira um novo nome de arquivo, quando você capturar um objeto <xref:System.IO.FileNotFoundException>.</span><span class="sxs-lookup"><span data-stu-id="04549-120">You have a good understanding of why the exception might be thrown, and you can implement a specific recovery, such as prompting the user to enter a new file name when you catch a <xref:System.IO.FileNotFoundException> object.</span></span>  
  
- <span data-ttu-id="04549-121">Você pode criar e lançar uma exceção nova e mais específica.</span><span class="sxs-lookup"><span data-stu-id="04549-121">You can create and throw a new, more specific exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#9](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#9)]  
  
- <span data-ttu-id="04549-122">Você deseja tratar parcialmente uma exceção antes de passá-la para tratamento adicional.</span><span class="sxs-lookup"><span data-stu-id="04549-122">You want to partially handle an exception before passing it on for additional handling.</span></span> <span data-ttu-id="04549-123">No exemplo a seguir, um bloco `catch` é usado para adicionar uma entrada a um log de erros antes de lançar novamente a exceção.</span><span class="sxs-lookup"><span data-stu-id="04549-123">In the following example, a `catch` block is used to add an entry to an error log before re-throwing the exception.</span></span>  
  
     [!code-csharp[csProgGuideExceptions#10](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#10)]  
  
## <a name="finally-blocks"></a><span data-ttu-id="04549-124">Blocos Finally</span><span class="sxs-lookup"><span data-stu-id="04549-124">Finally Blocks</span></span>  
 <span data-ttu-id="04549-125">Um bloco `finally` permite que você limpe as ações que são realizadas em um bloco `try`.</span><span class="sxs-lookup"><span data-stu-id="04549-125">A `finally` block enables you to clean up actions that are performed in a `try` block.</span></span> <span data-ttu-id="04549-126">Se estiver presente, o bloco `finally` será executado por último, depois do bloco `try` e de qualquer bloco `catch` de correspondência.</span><span class="sxs-lookup"><span data-stu-id="04549-126">If present, the `finally` block executes last, after the `try` block and any matched `catch` block.</span></span> <span data-ttu-id="04549-127">Um bloco `finally` é sempre executado, independentemente de uma exceção ser lançada ou de um bloco `catch` correspondente ao tipo de exceção ser encontrado.</span><span class="sxs-lookup"><span data-stu-id="04549-127">A `finally` block always runs, regardless of whether an exception is thrown or a `catch` block matching the exception type is found.</span></span>  
  
 <span data-ttu-id="04549-128">O bloco `finally` pode ser usado para liberar recursos, como fluxos de arquivos, conexões de banco de dados e identificadores de gráficos, sem esperar que o coletor de lixo no tempo de execução finalize os objetos.</span><span class="sxs-lookup"><span data-stu-id="04549-128">The `finally` block can be used to release resources such as file streams, database connections, and graphics handles without waiting for the garbage collector in the runtime to finalize the objects.</span></span> <span data-ttu-id="04549-129">Consulte a [Instrução using](../../language-reference/keywords/using-statement.md) para obter mais informações.</span><span class="sxs-lookup"><span data-stu-id="04549-129">See [using Statement](../../language-reference/keywords/using-statement.md) for more information.</span></span>  
  
 <span data-ttu-id="04549-130">No exemplo a seguir, o bloco `finally` é usado para fechar um arquivo está aberto no bloco `try`.</span><span class="sxs-lookup"><span data-stu-id="04549-130">In the following example, the `finally` block is used to close a file that is opened in the `try` block.</span></span> <span data-ttu-id="04549-131">Observe que o estado do identificador de arquivo é selecionado antes do arquivo ser fechado.</span><span class="sxs-lookup"><span data-stu-id="04549-131">Notice that the state of the file handle is checked before the file is closed.</span></span> <span data-ttu-id="04549-132">Se o bloco `try` não puder abrir o arquivo, o identificador de arquivo ainda terá o valor `null` e o bloco `finally` não tentará fechá-lo.</span><span class="sxs-lookup"><span data-stu-id="04549-132">If the `try` block cannot open the file, the file handle still has the value `null` and the `finally` block does not try to close it.</span></span> <span data-ttu-id="04549-133">De outra forma, se o arquivo for aberto com êxito no bloco `try`, o bloco `finally` fechará o arquivo aberto.</span><span class="sxs-lookup"><span data-stu-id="04549-133">Alternatively, if the file is opened successfully in the `try` block, the `finally` block closes the open file.</span></span>  
  
 [!code-csharp[csProgGuideExceptions#11](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideExceptions/CS/Exceptions.cs#11)]  
  
## <a name="c-language-specification"></a><span data-ttu-id="04549-134">Especificação da Linguagem C#</span><span class="sxs-lookup"><span data-stu-id="04549-134">C# Language Specification</span></span>  

<span data-ttu-id="04549-135">Para obter mais informações, veja [Exceções](~/_csharplang/spec/exceptions.md) e [A declaração try](~/_csharplang/spec/statements.md#the-try-statement) na [Especificação da Linguagem C#](../../language-reference/language-specification/index.md).</span><span class="sxs-lookup"><span data-stu-id="04549-135">For more information, see [Exceptions](~/_csharplang/spec/exceptions.md) and [The try statement](~/_csharplang/spec/statements.md#the-try-statement) in the [C# Language Specification](../../language-reference/language-specification/index.md).</span></span> <span data-ttu-id="04549-136">A especificação da linguagem é a fonte definitiva para a sintaxe e o uso de C#.</span><span class="sxs-lookup"><span data-stu-id="04549-136">The language specification is the definitive source for C# syntax and usage.</span></span>
  
## <a name="see-also"></a><span data-ttu-id="04549-137">Consulte também</span><span class="sxs-lookup"><span data-stu-id="04549-137">See also</span></span>

- [<span data-ttu-id="04549-138">Referência de C#</span><span class="sxs-lookup"><span data-stu-id="04549-138">C# Reference</span></span>](../../language-reference/index.md)
- [<span data-ttu-id="04549-139">Guia de Programação em C#</span><span class="sxs-lookup"><span data-stu-id="04549-139">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="04549-140">Exceções e manipulação de exceções</span><span class="sxs-lookup"><span data-stu-id="04549-140">Exceptions and Exception Handling</span></span>](./index.md)
- [<span data-ttu-id="04549-141">try-catch</span><span class="sxs-lookup"><span data-stu-id="04549-141">try-catch</span></span>](../../language-reference/keywords/try-catch.md)
- [<span data-ttu-id="04549-142">try-finally</span><span class="sxs-lookup"><span data-stu-id="04549-142">try-finally</span></span>](../../language-reference/keywords/try-finally.md)
- [<span data-ttu-id="04549-143">try-catch-finally</span><span class="sxs-lookup"><span data-stu-id="04549-143">try-catch-finally</span></span>](../../language-reference/keywords/try-catch-finally.md)
- [<span data-ttu-id="04549-144">Instrução using</span><span class="sxs-lookup"><span data-stu-id="04549-144">using Statement</span></span>](../../language-reference/keywords/using-statement.md)
