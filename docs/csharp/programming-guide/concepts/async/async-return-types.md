---
title: Tipos de retorno assíncronos (C#)
ms.date: 04/14/2020
ms.assetid: ddb2539c-c898-48c1-ad92-245e4a996df8
ms.openlocfilehash: c2584f1e285a7ab76eb43f9a211a8d2a51c2c55e
ms.sourcegitcommit: c76c8b2c39ed2f0eee422b61a2ab4c05ca7771fa
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/21/2020
ms.locfileid: "83761870"
---
# <a name="async-return-types-c"></a><span data-ttu-id="698dd-102">Tipos de retorno assíncronos (C#)</span><span class="sxs-lookup"><span data-stu-id="698dd-102">Async Return Types (C#)</span></span>

<span data-ttu-id="698dd-103">Métodos assíncronos podem conter os seguintes tipos de retorno:</span><span class="sxs-lookup"><span data-stu-id="698dd-103">Async methods can have the following return types:</span></span>

- <span data-ttu-id="698dd-104"><xref:System.Threading.Tasks.Task%601>, para um método assíncrono que retorna um valor.</span><span class="sxs-lookup"><span data-stu-id="698dd-104"><xref:System.Threading.Tasks.Task%601>, for an async method that returns a value.</span></span>
- <span data-ttu-id="698dd-105"><xref:System.Threading.Tasks.Task>, para um método assíncrono que executa uma operação, mas não retorna nenhum valor.</span><span class="sxs-lookup"><span data-stu-id="698dd-105"><xref:System.Threading.Tasks.Task>, for an async method that performs an operation but returns no value.</span></span>
- <span data-ttu-id="698dd-106">`void`, para um manipulador de eventos.</span><span class="sxs-lookup"><span data-stu-id="698dd-106">`void`, for an event handler.</span></span>
- <span data-ttu-id="698dd-107">Começando com o C# 7.0, qualquer tipo que tenha um método acessível `GetAwaiter`.</span><span class="sxs-lookup"><span data-stu-id="698dd-107">Starting with C# 7.0, any type that has an accessible `GetAwaiter` method.</span></span> <span data-ttu-id="698dd-108">O objeto retornado pelo método `GetAwaiter` deve implementar a interface <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="698dd-108">The object returned by the `GetAwaiter` method must implement the <xref:System.Runtime.CompilerServices.ICriticalNotifyCompletion?displayProperty=nameWithType> interface.</span></span>
- <span data-ttu-id="698dd-109">Começando com o C# 8,0, <xref:System.Collections.Generic.IAsyncEnumerable%601> , para um método assíncrono que retorna um *fluxo assíncrono*.</span><span class="sxs-lookup"><span data-stu-id="698dd-109">Starting with C# 8.0, <xref:System.Collections.Generic.IAsyncEnumerable%601>, for an async method that returns an *async stream*.</span></span>

<span data-ttu-id="698dd-110">Para obter mais informações sobre os métodos assíncronos, consulte [Programação assíncrona com async e await (C#)](./index.md).</span><span class="sxs-lookup"><span data-stu-id="698dd-110">For more information about async methods, see [Asynchronous Programming with async and await (C#)](./index.md).</span></span>  
  
## <a name="tasktresult-return-type"></a><span data-ttu-id="698dd-111"> Tipo de retorno Task\<TResult\></span><span class="sxs-lookup"><span data-stu-id="698dd-111">Task\<TResult\> Return Type</span></span>  
<span data-ttu-id="698dd-112">O <xref:System.Threading.Tasks.Task%601> tipo de retorno é usado para um método assíncrono que contém uma instrução [Return](../../../language-reference/keywords/return.md) (C#) na qual o operando é `TResult` .</span><span class="sxs-lookup"><span data-stu-id="698dd-112">The <xref:System.Threading.Tasks.Task%601> return type is used for an async method that contains a [return](../../../language-reference/keywords/return.md) (C#) statement in which the operand is `TResult`.</span></span>  
  
<span data-ttu-id="698dd-113">No exemplo a seguir, o método assíncrono `GetLeisureHours` contém uma instrução `return` que retorna um número inteiro.</span><span class="sxs-lookup"><span data-stu-id="698dd-113">In the following example, the `GetLeisureHours` async method contains a `return` statement that returns an integer.</span></span> <span data-ttu-id="698dd-114">Portanto, a declaração do método deve especificar um tipo de retorno de `Task<int>`.</span><span class="sxs-lookup"><span data-stu-id="698dd-114">Therefore, the method declaration must specify a return type of `Task<int>`.</span></span>  <span data-ttu-id="698dd-115">O método assíncrono <xref:System.Threading.Tasks.Task.FromResult%2A> é um espaço reservado para uma operação que retorna uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="698dd-115">The <xref:System.Threading.Tasks.Task.FromResult%2A> async method is a placeholder for an operation that returns a string.</span></span>
  
:::code language="csharp" source="./snippets/async-return-types/async-returns1.cs" id="SnippetFirstExample":::

<span data-ttu-id="698dd-116">Quando `GetLeisureHours` é chamado de dentro de uma expressão await no método `ShowTodaysInfo`, a expressão await recupera o valor inteiro (o valor de `leisureHours`) que está armazenado na tarefa que é retornada pelo método `GetLeisureHours`.</span><span class="sxs-lookup"><span data-stu-id="698dd-116">When `GetLeisureHours` is called from within an await expression in the `ShowTodaysInfo` method, the await expression retrieves the integer value (the value of `leisureHours`) that's stored in the task returned by the `GetLeisureHours` method.</span></span> <span data-ttu-id="698dd-117">Para obter mais informações sobre expressões await, consulte [await](../../../language-reference/operators/await.md).</span><span class="sxs-lookup"><span data-stu-id="698dd-117">For more information about await expressions, see [await](../../../language-reference/operators/await.md).</span></span>  
  
<span data-ttu-id="698dd-118">Você pode entender melhor como o `await` recupera o resultado de um `Task<T>` separando a chamada para `GetLeisureHours` do aplicativo do `await` , como mostra o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="698dd-118">You can better understand how `await` retrieves the result from a `Task<T>` by separating the call to `GetLeisureHours` from the application of `await`, as the following code shows.</span></span> <span data-ttu-id="698dd-119">Uma chamada ao método `GetLeisureHours` que não é aguardada imediatamente, retorna um `Task<int>`, como você esperaria da declaração do método.</span><span class="sxs-lookup"><span data-stu-id="698dd-119">A call to method `GetLeisureHours` that isn't immediately awaited returns a `Task<int>`, as you would expect from the declaration of the method.</span></span> <span data-ttu-id="698dd-120">A tarefa é atribuída à variável `integerTask` no exemplo.</span><span class="sxs-lookup"><span data-stu-id="698dd-120">The task is assigned to the `integerTask` variable in the example.</span></span> <span data-ttu-id="698dd-121">Já que `integerTask` é um <xref:System.Threading.Tasks.Task%601>, ele contém uma propriedade <xref:System.Threading.Tasks.Task%601.Result> do tipo `TResult`.</span><span class="sxs-lookup"><span data-stu-id="698dd-121">Because `integerTask` is a <xref:System.Threading.Tasks.Task%601>, it contains a <xref:System.Threading.Tasks.Task%601.Result> property of type `TResult`.</span></span> <span data-ttu-id="698dd-122">Nesse caso, `TResult` representa um tipo inteiro.</span><span class="sxs-lookup"><span data-stu-id="698dd-122">In this case, `TResult` represents an integer type.</span></span> <span data-ttu-id="698dd-123">Quando `await` é aplicado à `integerTask`, a expressão await é avaliada como o conteúdo da propriedade <xref:System.Threading.Tasks.Task%601.Result%2A> de `integerTask`.</span><span class="sxs-lookup"><span data-stu-id="698dd-123">When `await` is applied to `integerTask`, the await expression evaluates to the contents of the <xref:System.Threading.Tasks.Task%601.Result%2A> property of `integerTask`.</span></span> <span data-ttu-id="698dd-124">O valor é atribuído à variável `ret`.</span><span class="sxs-lookup"><span data-stu-id="698dd-124">The value is assigned to the `ret` variable.</span></span>  
  
> [!IMPORTANT]
> <span data-ttu-id="698dd-125">A propriedade <xref:System.Threading.Tasks.Task%601.Result%2A> é uma propriedade de bloqueio.</span><span class="sxs-lookup"><span data-stu-id="698dd-125">The <xref:System.Threading.Tasks.Task%601.Result%2A> property is a blocking property.</span></span> <span data-ttu-id="698dd-126">Se você tentar acessá-la antes que sua tarefa seja concluída, o thread que está ativo no momento será bloqueado até que a tarefa seja concluída e o valor esteja disponível.</span><span class="sxs-lookup"><span data-stu-id="698dd-126">If you try to access it before its task is finished, the thread that's currently active is blocked until the task completes and the value is available.</span></span> <span data-ttu-id="698dd-127">Na maioria dos casos, você deve acessar o valor usando `await` em vez de acessar a propriedade diretamente.</span><span class="sxs-lookup"><span data-stu-id="698dd-127">In most cases, you should access the value by using `await` instead of accessing the property directly.</span></span> <br/> <span data-ttu-id="698dd-128">O exemplo anterior recuperou o valor da propriedade <xref:System.Threading.Tasks.Task%601.Result%2A> para bloquear o thread principal, de modo que o método `ShowTodaysInfo` pudesse concluir a execução antes do encerramento do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="698dd-128">The previous example retrieved the value of the <xref:System.Threading.Tasks.Task%601.Result%2A> property to block the main thread so that the `ShowTodaysInfo` method could finish execution before the application ended.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns1a.cs" id="SnippetSecondVersion":::

## <a name="task-return-type"></a><span data-ttu-id="698dd-129"> Tipo de retorno Task</span><span class="sxs-lookup"><span data-stu-id="698dd-129">Task Return Type</span></span>  
<span data-ttu-id="698dd-130">Os métodos assíncronos que não contêm uma instrução `return` ou que contêm uma instrução `return` que não retorna um operando, normalmente têm um tipo de retorno de <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="698dd-130">Async methods that don't contain a `return` statement or that contain a `return` statement that doesn't return an operand usually have a return type of <xref:System.Threading.Tasks.Task>.</span></span> <span data-ttu-id="698dd-131">Esses métodos retornam `void` se eles são executados de forma síncrona.</span><span class="sxs-lookup"><span data-stu-id="698dd-131">Such methods return `void` if they run synchronously.</span></span> <span data-ttu-id="698dd-132">Se você usar um tipo de retorno <xref:System.Threading.Tasks.Task> para um método assíncrono, um método de chamada poderá usar um operador `await` para suspender a conclusão do chamador até que o método assíncrono chamado seja concluído.</span><span class="sxs-lookup"><span data-stu-id="698dd-132">If you use a <xref:System.Threading.Tasks.Task> return type for an async method, a calling method can use an `await` operator to suspend the caller's completion until the called async method has finished.</span></span>  
  
<span data-ttu-id="698dd-133">No exemplo a seguir, o método assíncrono `WaitAndApologize` não contém uma instrução `return`, então o método retorna um objeto <xref:System.Threading.Tasks.Task>.</span><span class="sxs-lookup"><span data-stu-id="698dd-133">In the following example, the `WaitAndApologize` async method doesn't contain a `return` statement, so the method returns a <xref:System.Threading.Tasks.Task> object.</span></span> <span data-ttu-id="698dd-134">Retornando uma `Task` habilitação `WaitAndApologize` a ser aguardada.</span><span class="sxs-lookup"><span data-stu-id="698dd-134">Returning a `Task` enables `WaitAndApologize` to be awaited.</span></span> <span data-ttu-id="698dd-135">O <xref:System.Threading.Tasks.Task> tipo não inclui uma `Result` propriedade porque não tem nenhum valor de retorno.</span><span class="sxs-lookup"><span data-stu-id="698dd-135">The <xref:System.Threading.Tasks.Task> type doesn't include a `Result` property because it has no return value.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns2.cs" id="SnippetTaskReturn":::

<span data-ttu-id="698dd-136">O `WaitAndApologize` é aguardado, usando uma instrução await, em vez de uma expressão await, semelhante à instrução de chamada a um método síncrono de retorno void.</span><span class="sxs-lookup"><span data-stu-id="698dd-136">`WaitAndApologize` is awaited by using an await statement instead of an await expression, similar to the calling statement for a synchronous void-returning method.</span></span> <span data-ttu-id="698dd-137">A aplicação de um operador await, nesse caso, não produz um valor.</span><span class="sxs-lookup"><span data-stu-id="698dd-137">The application of an await operator in this case doesn't produce a value.</span></span>  
  
<span data-ttu-id="698dd-138">Como no exemplo <xref:System.Threading.Tasks.Task%601> anterior, você pode separar a chamada ao `WaitAndApologize`, da aplicação de um operador await, como mostrado no código a seguir.</span><span class="sxs-lookup"><span data-stu-id="698dd-138">As in the previous <xref:System.Threading.Tasks.Task%601> example, you can separate the call to `WaitAndApologize` from the application of an await operator, as the following code shows.</span></span> <span data-ttu-id="698dd-139">No entanto, lembre-se que uma `Task` não tem uma propriedade `Result` e que nenhum valor será produzido quando um operador await for aplicado a uma `Task`.</span><span class="sxs-lookup"><span data-stu-id="698dd-139">However, remember that a `Task` doesn't have a `Result` property, and that no value is produced when an await operator is applied to a `Task`.</span></span>  
  
<span data-ttu-id="698dd-140">O código a seguir separa a chamada ao método `WaitAndApologize` da espera pela tarefa que o método retorna.</span><span class="sxs-lookup"><span data-stu-id="698dd-140">The following code separates calling the `WaitAndApologize` method from awaiting the task that the method returns.</span></span>  

:::code language="csharp" source="./snippets/async-return-types/async-returns2a.cs" id="SnippetAwaitTask":::

## <a name="void-return-type"></a><span data-ttu-id="698dd-141">Tipo de retorno void</span><span class="sxs-lookup"><span data-stu-id="698dd-141">Void return type</span></span>

<span data-ttu-id="698dd-142">O tipo de retorno `void` é usado em manipuladores de eventos assíncronos, que exigem um tipo de retorno `void`.</span><span class="sxs-lookup"><span data-stu-id="698dd-142">You use the `void` return type in asynchronous event handlers, which require a `void` return type.</span></span> <span data-ttu-id="698dd-143">Para métodos diferentes de manipuladores de eventos que não retornam um valor, você deve retornar um <xref:System.Threading.Tasks.Task>, porque não é possível esperar por um método assíncrono que retorna `void`.</span><span class="sxs-lookup"><span data-stu-id="698dd-143">For methods other than event handlers that don't return a value, you should return a <xref:System.Threading.Tasks.Task> instead, because an async method that returns `void` can't be awaited.</span></span> <span data-ttu-id="698dd-144">Qualquer chamador desse método deve continuar a conclusão sem esperar que o método assíncrono chamado seja concluído.</span><span class="sxs-lookup"><span data-stu-id="698dd-144">Any caller of such a method must continue to completion without waiting for the called async method to finish.</span></span> <span data-ttu-id="698dd-145">O chamador deve ser independente de quaisquer valores ou exceções geradas pelo método Async.</span><span class="sxs-lookup"><span data-stu-id="698dd-145">The caller must be independent of any values or exceptions that the async method generates.</span></span>  
  
<span data-ttu-id="698dd-146">O chamador de um método assíncrono de retorno nulo não pode capturar exceções lançadas do método, e essas exceções não tratadas provavelmente causarão a falha do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="698dd-146">The caller of a void-returning async method can't catch exceptions thrown from the method, and such unhandled exceptions are likely to cause your application to fail.</span></span> <span data-ttu-id="698dd-147">Se um método que retorna um <xref:System.Threading.Tasks.Task> ou <xref:System.Threading.Tasks.Task%601> gera uma exceção, a exceção é armazenada na tarefa retornada.</span><span class="sxs-lookup"><span data-stu-id="698dd-147">If a method that returns a <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> throws an exception, the exception is stored in the returned task.</span></span> <span data-ttu-id="698dd-148">A exceção é relançada quando a tarefa é esperada.</span><span class="sxs-lookup"><span data-stu-id="698dd-148">The exception is rethrown when the task is awaited.</span></span> <span data-ttu-id="698dd-149">Portanto, verifique se qualquer método assíncrono que pode produzir uma exceção tem um tipo de retorno de <xref:System.Threading.Tasks.Task> ou <xref:System.Threading.Tasks.Task%601> e que as chamadas ao método são aguardadas.</span><span class="sxs-lookup"><span data-stu-id="698dd-149">Therefore, make sure that any async method that can produce an exception has a return type of <xref:System.Threading.Tasks.Task> or <xref:System.Threading.Tasks.Task%601> and that calls to the method are awaited.</span></span>  
  
<span data-ttu-id="698dd-150">Para obter mais informações sobre como capturar exceções em métodos assíncronos, consulte a seção [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) do artigo [try-catch](../../../language-reference/keywords/try-catch.md) .</span><span class="sxs-lookup"><span data-stu-id="698dd-150">For more information about how to catch exceptions in async methods, see the [Exceptions in Async Methods](../../../language-reference/keywords/try-catch.md#exceptions-in-async-methods) section of the [try-catch](../../../language-reference/keywords/try-catch.md) article.</span></span>  
  
<span data-ttu-id="698dd-151">O exemplo a seguir mostra o comportamento de um manipulador de eventos assíncrono.</span><span class="sxs-lookup"><span data-stu-id="698dd-151">The following example shows the behavior of an async event handler.</span></span> <span data-ttu-id="698dd-152">No código de exemplo, um manipulador de eventos assíncrono deve permitir que o thread principal saiba quando ele for concluído.</span><span class="sxs-lookup"><span data-stu-id="698dd-152">In the example code, an async event handler must let the main thread know when it finishes.</span></span> <span data-ttu-id="698dd-153">Em seguida, o thread principal pode aguardar um manipulador de eventos assíncronos ser concluído antes de sair do programa.</span><span class="sxs-lookup"><span data-stu-id="698dd-153">Then the main thread can wait for an async event handler to complete before exiting the program.</span></span>

:::code language="csharp" source="./snippets/async-return-types/async-returns3.cs":::

## <a name="generalized-async-return-types-and-valuetasktresult"></a><span data-ttu-id="698dd-154">Tipos de retorno assíncronos generalizados e ValueTask\<TResult\></span><span class="sxs-lookup"><span data-stu-id="698dd-154">Generalized async return types and ValueTask\<TResult\></span></span>

<span data-ttu-id="698dd-155">Começando com o C# 7.0, um método assíncrono pode retornar qualquer tipo que tenha um método `GetAwaiter` acessível.</span><span class="sxs-lookup"><span data-stu-id="698dd-155">Starting with C# 7.0, an async method can return any type that has an accessible `GetAwaiter` method.</span></span>

<span data-ttu-id="698dd-156">Já que <xref:System.Threading.Tasks.Task> e <xref:System.Threading.Tasks.Task%601> são tipos de referência, a alocação de memória em caminhos críticos para o desempenho, especialmente quando alocações ocorrerem em loops estreitos, podem afetar o desempenho.</span><span class="sxs-lookup"><span data-stu-id="698dd-156">Because <xref:System.Threading.Tasks.Task> and <xref:System.Threading.Tasks.Task%601> are reference types, memory allocation in performance-critical paths, particularly when allocations occur in tight loops, can adversely affect performance.</span></span> <span data-ttu-id="698dd-157">Suporte para tipos de retorno generalizados significa que você pode retornar um tipo de valor leve em vez de um tipo de referência para evitar as alocações de memória adicionais.</span><span class="sxs-lookup"><span data-stu-id="698dd-157">Support for generalized return types means that you can return a lightweight value type instead of a reference type to avoid additional memory allocations.</span></span>

<span data-ttu-id="698dd-158">O .NET fornece a estrutura <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> como uma implementação leve de um valor de retorno de tarefa generalizado.</span><span class="sxs-lookup"><span data-stu-id="698dd-158">.NET provides the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> structure as a lightweight implementation of a generalized task-returning value.</span></span> <span data-ttu-id="698dd-159">Para usar o tipo <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType>, você deve adicionar o pacote NuGet `System.Threading.Tasks.Extensions` ao seu projeto.</span><span class="sxs-lookup"><span data-stu-id="698dd-159">To use the <xref:System.Threading.Tasks.ValueTask%601?displayProperty=nameWithType> type, you must add the `System.Threading.Tasks.Extensions` NuGet package to your project.</span></span> <span data-ttu-id="698dd-160">O exemplo a seguir usa a estrutura <xref:System.Threading.Tasks.ValueTask%601> para recuperar o valor de dois lançamentos de dados.</span><span class="sxs-lookup"><span data-stu-id="698dd-160">The following example uses the <xref:System.Threading.Tasks.ValueTask%601> structure to retrieve the value of two dice rolls.</span></span>
  
:::code language="csharp" source="./snippets/async-return-types/async-valuetask.cs":::

## <a name="async-streams-with-iasyncenumerablet"></a><span data-ttu-id="698dd-161">Fluxos assíncronos com IAsyncEnumerable \< T\></span><span class="sxs-lookup"><span data-stu-id="698dd-161">Async streams with IAsyncEnumerable\<T\></span></span>

<span data-ttu-id="698dd-162">A partir do C# 8,0, um método assíncrono pode retornar um *fluxo assíncrono*, representado por <xref:System.Collections.Generic.IAsyncEnumerable%601> .</span><span class="sxs-lookup"><span data-stu-id="698dd-162">Starting with C# 8.0, an async method may return an *async stream*, represented by <xref:System.Collections.Generic.IAsyncEnumerable%601>.</span></span> <span data-ttu-id="698dd-163">Um fluxo assíncrono fornece uma maneira de enumerar itens lidos de um fluxo quando elementos são gerados em partes com chamadas assíncronas repetidas.</span><span class="sxs-lookup"><span data-stu-id="698dd-163">An async stream provides a way to enumerate items read from a stream when elements are generated in chunks with repeated asynchronous calls.</span></span> <span data-ttu-id="698dd-164">O exemplo a seguir mostra um método assíncrono que gera um fluxo assíncrono:</span><span class="sxs-lookup"><span data-stu-id="698dd-164">The following example shows an async method that generates an async stream:</span></span>

:::code language="csharp" source="./snippets/async-return-types/AsyncStreams.cs" id="SnippetGenerateAsyncStream":::

<span data-ttu-id="698dd-165">O exemplo anterior lê linhas de uma cadeia de caracteres de forma assíncrona.</span><span class="sxs-lookup"><span data-stu-id="698dd-165">The preceding example reads lines from a string asynchronously.</span></span> <span data-ttu-id="698dd-166">Depois que cada linha é lida, o código enumera cada palavra na cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="698dd-166">Once each line is read, the code enumerates each word in the string.</span></span> <span data-ttu-id="698dd-167">Os chamadores enumerariam cada palavra usando a `await foreach` instrução.</span><span class="sxs-lookup"><span data-stu-id="698dd-167">Callers would enumerate each word using the `await foreach` statement.</span></span> <span data-ttu-id="698dd-168">O método aguarda quando precisa ler de forma assíncrona a próxima linha da cadeia de caracteres de origem.</span><span class="sxs-lookup"><span data-stu-id="698dd-168">The method awaits when it needs to asynchronously read the next line from the source string.</span></span>

## <a name="see-also"></a><span data-ttu-id="698dd-169">Veja também</span><span class="sxs-lookup"><span data-stu-id="698dd-169">See also</span></span>

- <xref:System.Threading.Tasks.Task.FromResult%2A>
- [<span data-ttu-id="698dd-170">Passo a passo: acessando a Web usando async e await (C#)</span><span class="sxs-lookup"><span data-stu-id="698dd-170">Walkthrough: Accessing the Web by Using async and await (C#)</span></span>](./walkthrough-accessing-the-web-by-using-async-and-await.md)
- [<span data-ttu-id="698dd-171">Fluxo de controle em programas assíncronos (C#)</span><span class="sxs-lookup"><span data-stu-id="698dd-171">Control Flow in Async Programs (C#)</span></span>](./control-flow-in-async-programs.md)
- [<span data-ttu-id="698dd-172">async</span><span class="sxs-lookup"><span data-stu-id="698dd-172">async</span></span>](../../../language-reference/keywords/async.md)
- [<span data-ttu-id="698dd-173">expressões</span><span class="sxs-lookup"><span data-stu-id="698dd-173">await</span></span>](../../../language-reference/operators/await.md)
