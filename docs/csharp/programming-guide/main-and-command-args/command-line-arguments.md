---
title: Argumentos de linha de comando – Guia de Programação em C#
description: Saiba mais sobre argumentos de linha de comando. Veja um exemplo que usa argumentos de linha de comando em um aplicativo de console.
ms.date: 07/20/2015
helpviewer_keywords:
- command-line arguments [C#]
ms.assetid: 0e597e0d-ea7a-41ba-a38a-0198122f3c26
ms.openlocfilehash: 35ff0425d3f09cf4ad116cf688b943cef3ef02e3
ms.sourcegitcommit: 552b4b60c094559db9d8178fa74f5bafaece0caf
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/29/2020
ms.locfileid: "87381912"
---
# <a name="command-line-arguments-c-programming-guide"></a><span data-ttu-id="15b40-104">Argumentos de linha de comando (Guia de Programação em C#)</span><span class="sxs-lookup"><span data-stu-id="15b40-104">Command-Line Arguments (C# Programming Guide)</span></span>

<span data-ttu-id="15b40-105">Você pode enviar argumentos para o método `Main` definindo o método de uma das seguintes maneiras:</span><span class="sxs-lookup"><span data-stu-id="15b40-105">You can send arguments to the `Main` method by defining the method in one of the following ways:</span></span>

[!code-csharp[csProgGuideMain#2](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#2)]  

[!code-csharp[csProgGuideMain#3](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#3)]

> [!NOTE]
> <span data-ttu-id="15b40-106">Para habilitar argumentos de linha de comando no `Main` método em um aplicativo Windows Forms, você deve modificar manualmente a assinatura do `Main` em *Program.cs*.</span><span class="sxs-lookup"><span data-stu-id="15b40-106">To enable command-line arguments in the `Main` method in a Windows Forms application, you must manually modify the signature of `Main` in *program.cs*.</span></span> <span data-ttu-id="15b40-107">O código gerado pelo Designer de Formulários do Windows cria um `Main` sem um parâmetro de entrada.</span><span class="sxs-lookup"><span data-stu-id="15b40-107">The code generated by the Windows Forms designer creates a `Main` without an input parameter.</span></span> <span data-ttu-id="15b40-108">Você também pode usar <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> ou <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> para acessar os argumentos de linha de comando de qualquer ponto em um console ou um aplicativo do Windows.</span><span class="sxs-lookup"><span data-stu-id="15b40-108">You can also use <xref:System.Environment.CommandLine%2A?displayProperty=nameWithType> or <xref:System.Environment.GetCommandLineArgs%2A?displayProperty=nameWithType> to access the command-line arguments from any point in a console or Windows application.</span></span>

<span data-ttu-id="15b40-109">O parâmetro do método `Main` é uma matriz <xref:System.String> que representa os argumentos de linha de comando.</span><span class="sxs-lookup"><span data-stu-id="15b40-109">The parameter of the `Main` method is a <xref:System.String> array that represents the command-line arguments.</span></span> <span data-ttu-id="15b40-110">Geralmente você determina se os argumentos existem testando a propriedade `Length`, por exemplo:</span><span class="sxs-lookup"><span data-stu-id="15b40-110">Usually you determine whether arguments exist by testing the `Length` property, for example:</span></span>

[!code-csharp[csProgGuideMain#4](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class3.cs#4)]

> [!TIP]
> <span data-ttu-id="15b40-111">A `args` matriz não pode ser nula.</span><span class="sxs-lookup"><span data-stu-id="15b40-111">The `args` array cannot be null.</span></span> <span data-ttu-id="15b40-112">Portanto, é seguro acessar a `Length` Propriedade sem verificação nula.</span><span class="sxs-lookup"><span data-stu-id="15b40-112">So, it's safe to access the `Length` property without null checking.</span></span>

<span data-ttu-id="15b40-113">Você também pode converter os argumentos de cadeia de caracteres em tipos numéricos, usando a classe <xref:System.Convert> ou o método `Parse`.</span><span class="sxs-lookup"><span data-stu-id="15b40-113">You can also convert the string arguments to numeric types by using the <xref:System.Convert> class or the `Parse` method.</span></span> <span data-ttu-id="15b40-114">Por exemplo, a instrução a seguir converte o `string` em um número `long` usando o método <xref:System.Int64.Parse%2A>:</span><span class="sxs-lookup"><span data-stu-id="15b40-114">For example, the following statement converts the `string` to a `long` number by using the <xref:System.Int64.Parse%2A> method:</span></span>

```csharp
long num = Int64.Parse(args[0]);
```

<span data-ttu-id="15b40-115">Também é possível usar o tipo `long` de C#, que funciona como alias de `Int64`:</span><span class="sxs-lookup"><span data-stu-id="15b40-115">It is also possible to use the C# type `long`, which aliases `Int64`:</span></span>

```csharp
long num = long.Parse(args[0]);
```

<span data-ttu-id="15b40-116">Você também pode usar o método da classe `Convert`, o `ToInt64`, para fazer a mesma coisa:</span><span class="sxs-lookup"><span data-stu-id="15b40-116">You can also use the `Convert` class method `ToInt64` to do the same thing:</span></span>

```csharp
long num = Convert.ToInt64(s);
```

<span data-ttu-id="15b40-117">Para obter mais informações, consulte <xref:System.Int64.Parse%2A> e <xref:System.Convert>.</span><span class="sxs-lookup"><span data-stu-id="15b40-117">For more information, see <xref:System.Int64.Parse%2A> and <xref:System.Convert>.</span></span>

## <a name="example"></a><span data-ttu-id="15b40-118">Exemplo</span><span class="sxs-lookup"><span data-stu-id="15b40-118">Example</span></span>

<span data-ttu-id="15b40-119">O exemplo a seguir mostra como usar argumentos de linha de comando em um aplicativo de console.</span><span class="sxs-lookup"><span data-stu-id="15b40-119">The following example shows how to use command-line arguments in a console application.</span></span> <span data-ttu-id="15b40-120">O aplicativo recebe um argumento em tempo de execução, converte o argumento em um número inteiro e calcula o fatorial do número.</span><span class="sxs-lookup"><span data-stu-id="15b40-120">The application takes one argument at run time, converts the argument to an integer, and calculates the factorial of the number.</span></span> <span data-ttu-id="15b40-121">Se nenhum argumento for fornecido, o aplicativo emitirá uma mensagem que explica o uso correto do programa.</span><span class="sxs-lookup"><span data-stu-id="15b40-121">If no arguments are supplied, the application issues a message that explains the correct usage of the program.</span></span>

<span data-ttu-id="15b40-122">Para compilar e executar o aplicativo em um prompt de comando, siga estas etapas:</span><span class="sxs-lookup"><span data-stu-id="15b40-122">To compile and run the application from a command prompt, follow these steps:</span></span>

1. <span data-ttu-id="15b40-123">Cole o código a seguir em qualquer editor de texto e, em seguida, salve o arquivo como um arquivo de texto com o nome *factorial.cs*.</span><span class="sxs-lookup"><span data-stu-id="15b40-123">Paste the following code into any text editor, and then save the file as  a text file with the name *Factorial.cs*.</span></span>

     [!code-csharp[csProgGuideMain#16](~/samples/snippets/csharp/VS_Snippets_VBCSharp/csProgGuideMain/CS/Class1.cs#16)]

2. <span data-ttu-id="15b40-124">Na tela **Inicial** ou no menu **Iniciar**, abra uma janela de **Prompt de Comando do Desenvolvedor** do Visual Studio e, em seguida, navegue até a pasta que contém o arquivo que você acabou de criar.</span><span class="sxs-lookup"><span data-stu-id="15b40-124">From the **Start** screen or **Start** menu, open a Visual Studio **Developer Command Prompt** window, and then navigate to the folder that contains the file that you just created.</span></span>

3. <span data-ttu-id="15b40-125">Digite o seguinte comando para compilar o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="15b40-125">Enter the following command to compile the application.</span></span>
  
     `csc Factorial.cs`  
  
     <span data-ttu-id="15b40-126">Se seu aplicativo não tiver erros de compilação, um arquivo executável chamado *Factorial.exe* será criado.</span><span class="sxs-lookup"><span data-stu-id="15b40-126">If your application has no compilation errors, an executable file that's named *Factorial.exe* is created.</span></span>
  
4. <span data-ttu-id="15b40-127">Digite o seguinte comando para calcular o fatorial de 3:</span><span class="sxs-lookup"><span data-stu-id="15b40-127">Enter the following command to calculate the factorial of 3:</span></span>
  
     `Factorial 3`  
  
5. <span data-ttu-id="15b40-128">O comando produz esta saída: `The factorial of 3 is 6.`</span><span class="sxs-lookup"><span data-stu-id="15b40-128">The command produces this output: `The factorial of 3 is 6.`</span></span>

> [!NOTE]
> <span data-ttu-id="15b40-129">Ao executar um aplicativo no Visual Studio, você pode especificar argumentos de linha de comando na [Página de depuração, Designer de Projeto](/visualstudio/ide/reference/debug-page-project-designer).</span><span class="sxs-lookup"><span data-stu-id="15b40-129">When running an application in Visual Studio, you can specify command-line arguments in the [Debug Page, Project Designer](/visualstudio/ide/reference/debug-page-project-designer).</span></span>

## <a name="see-also"></a><span data-ttu-id="15b40-130">Veja também</span><span class="sxs-lookup"><span data-stu-id="15b40-130">See also</span></span>

- <xref:System.Environment?displayProperty=nameWithType>
- [<span data-ttu-id="15b40-131">Guia de programação C#</span><span class="sxs-lookup"><span data-stu-id="15b40-131">C# Programming Guide</span></span>](../index.md)
- [<span data-ttu-id="15b40-132">Argumentos Main () e de linha de comando</span><span class="sxs-lookup"><span data-stu-id="15b40-132">Main() and Command-Line Arguments</span></span>](index.md)
- [<span data-ttu-id="15b40-133">Como exibir argumentos de linha de comando</span><span class="sxs-lookup"><span data-stu-id="15b40-133">How to display command line arguments</span></span>](how-to-display-command-line-arguments.md)
- [<span data-ttu-id="15b40-134">Valores de retorno de Main()</span><span class="sxs-lookup"><span data-stu-id="15b40-134">Main() Return Values</span></span>](main-return-values.md)
- [<span data-ttu-id="15b40-135">Classes</span><span class="sxs-lookup"><span data-stu-id="15b40-135">Classes</span></span>](../classes-and-structs/classes.md)
