---
description: Aviso do compilador (nível 1) CS1956
title: Aviso do compilador (nível 1) CS1956
ms.date: 07/20/2015
f1_keywords:
- CS1956
helpviewer_keywords:
- CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
ms.openlocfilehash: a2bb1d2d30627e3511562b4d489f03f1dbd2051f
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91203391"
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="d668b-103">Aviso do compilador (nível 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="d668b-103">Compiler Warning (level 1) CS1956</span></span>

<span data-ttu-id="d668b-104">O 'name' do membro implementa o membro de interface 'name' no tipo 'type'.</span><span class="sxs-lookup"><span data-stu-id="d668b-104">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="d668b-105">Há várias correspondências para o membro de interface em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="d668b-105">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="d668b-106">O método chamado dependerá da implementação.</span><span class="sxs-lookup"><span data-stu-id="d668b-106">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="d668b-107">Esse aviso pode ser gerado quando dois métodos de interface são diferenciados apenas pela marcação de um parâmetro específico com `ref` ou `out`.</span><span class="sxs-lookup"><span data-stu-id="d668b-107">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="d668b-108">É recomendável alterar o código para evitar esse aviso, pois não se tem uma garantia de qual método será chamado em runtime.</span><span class="sxs-lookup"><span data-stu-id="d668b-108">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="d668b-109">Embora o C# diferencie `out` e `ref`, o CLR os vê da mesma forma.</span><span class="sxs-lookup"><span data-stu-id="d668b-109">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="d668b-110">Ao decidir qual método implementa a interface, o CLR escolherá apenas um.</span><span class="sxs-lookup"><span data-stu-id="d668b-110">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="d668b-111">Evitar esse aviso</span><span class="sxs-lookup"><span data-stu-id="d668b-111">To avoid this warning</span></span>  
  
1. <span data-ttu-id="d668b-112">Ofereça ao compilador uma maneira de diferenciar os métodos.</span><span class="sxs-lookup"><span data-stu-id="d668b-112">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="d668b-113">Por exemplo, é possível dar-lhes nomes diferentes ou fornecer um parâmetro adicional a um deles.</span><span class="sxs-lookup"><span data-stu-id="d668b-113">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="d668b-114">Exemplo</span><span class="sxs-lookup"><span data-stu-id="d668b-114">Example</span></span>  

 <span data-ttu-id="d668b-115">O código a seguir gera o erro CS1956, pois os dois métodos `Test` em `Base` diferem somente pelo modificador `ref`/`out` no primeiro parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d668b-115">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
