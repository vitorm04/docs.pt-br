---
title: "Aviso do compilador (nível 1) CS1956"
ms.date: 07/20/2015
ms.prod: .net
ms.technology: devlang-csharp
ms.topic: error-reference
f1_keywords: CS1956
helpviewer_keywords: CS1956
ms.assetid: 841f8b2b-692b-49ae-a587-a40797cf9797
caps.latest.revision: "9"
author: BillWagner
ms.author: wiwagn
ms.openlocfilehash: e60fc721f58100a0c845b27bc302d78800acec23
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2017
---
# <a name="compiler-warning-level-1-cs1956"></a><span data-ttu-id="ba3f0-102">Aviso do compilador (nível 1) CS1956</span><span class="sxs-lookup"><span data-stu-id="ba3f0-102">Compiler Warning (level 1) CS1956</span></span>
<span data-ttu-id="ba3f0-103">O 'name' do membro implementa o membro de interface 'name' no tipo 'type'.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-103">Member 'name' implements interface member 'name' in type 'type'.</span></span> <span data-ttu-id="ba3f0-104">Há várias correspondências para o membro de interface em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-104">There are multiple matches for the interface member at run-time.</span></span> <span data-ttu-id="ba3f0-105">O método chamado dependerá da implementação.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-105">It is implementation dependent which method will be called.</span></span>  
  
 <span data-ttu-id="ba3f0-106">Esse aviso pode ser gerado quando dois métodos de interface são diferenciados apenas pela marcação de um parâmetro específico com `ref` ou `out`.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-106">This warning can be generated when two interface methods are differentiated only by whether a particular parameter is marked with `ref` or with `out`.</span></span> <span data-ttu-id="ba3f0-107">É recomendável alterar o código para evitar esse aviso, pois não se tem uma garantia de qual método será chamado em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-107">It is best to change your code to avoid this warning because it is not obvious or guaranteed which method is called at runtime.</span></span>  
  
 <span data-ttu-id="ba3f0-108">Embora o C# diferencie `out` e `ref`, o CLR os vê da mesma forma.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-108">Although C# distinguishes between `out` and `ref`, the CLR sees them as the same.</span></span> <span data-ttu-id="ba3f0-109">Ao decidir qual método implementa a interface, o CLR escolherá apenas um.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-109">When deciding which method implements the interface, the CLR just picks one.</span></span>  
  
### <a name="to-avoid-this-warning"></a><span data-ttu-id="ba3f0-110">Evitar esse aviso</span><span class="sxs-lookup"><span data-stu-id="ba3f0-110">To avoid this warning</span></span>  
  
1.  <span data-ttu-id="ba3f0-111">Ofereça ao compilador uma maneira de diferenciar os métodos.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-111">Give the compiler some way to differentiate the methods.</span></span> <span data-ttu-id="ba3f0-112">Por exemplo, é possível dar-lhes nomes diferentes ou fornecer um parâmetro adicional a um deles.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-112">For example, you can give them different names or provide an additional parameter on one of them.</span></span>  
  
## <a name="example"></a><span data-ttu-id="ba3f0-113">Exemplo</span><span class="sxs-lookup"><span data-stu-id="ba3f0-113">Example</span></span>  
 <span data-ttu-id="ba3f0-114">O código a seguir gera o erro CS1956, pois os dois métodos `Test` em `Base` diferem somente pelo modificador `ref`/`out` no primeiro parâmetro.</span><span class="sxs-lookup"><span data-stu-id="ba3f0-114">The following code generates CS1956 because the two `Test` methods in `Base` differ only by the `ref`/`out` modifier on the first parameter.</span></span>  
  
```csharp  
// cs1956.cs  
class Base<T, S>  
{  
    // This is the method that should be called.  
    public virtual int Test(out T x) // CS1956  
    {  
        x = default(T);  
        return 0;  
    }  
  
    // This is the "last" method and is the one that ends up being called  
    public virtual int Test(ref S x)  
    {  
        return 1;  
    }  
}  
  
interface IFace  
{  
    int Test(out int x);  
}  
  
class Derived : Base<int, int>, IFace  
{  
    static int Main()  
    {  
        IFace x = new Derived();  
        int y;  
        return x.Test(out y);  
    }  
}  
```
