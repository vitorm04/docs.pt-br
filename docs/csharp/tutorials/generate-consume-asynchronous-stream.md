---
title: Gerar e consumir fluxos assíncronos
description: Este tutorial avançado ilustra os cenários em que gerar e consumir fluxos assíncronos oferecem uma forma mais natural de trabalhar com sequências de dados que podem ser geradas de forma assíncrona.
ms.date: 02/10/2019
ms.custom: mvc
ms.openlocfilehash: 0fa7c778ca9ce0f0124fcc520dd4de65f2f92ea8
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/09/2019
ms.locfileid: "59308543"
---
# <a name="tutorial-generate-and-consume-async-streams-using-c-80-and-net-core-30"></a><span data-ttu-id="127be-103">Tutorial: Gerar e consumir fluxos assíncronos usando o C# 8.0 e .NET Core 3.0</span><span class="sxs-lookup"><span data-stu-id="127be-103">Tutorial: Generate and consume async streams using C# 8.0 and .NET Core 3.0</span></span>

<span data-ttu-id="127be-104">O C#8.0 apresenta **fluxos assíncronos**, que modelam uma fonte de dados de streaming quando os elementos no fluxo de dados podem ser recuperados ou gerados de forma assíncrona.</span><span class="sxs-lookup"><span data-stu-id="127be-104">C# 8.0 introduces **async streams**, which model a streaming source of data when the elements in the data stream may be retrieved or generated asynchronously.</span></span> <span data-ttu-id="127be-105">Os fluxos assíncronos contam com novas interfaces introduzidas no .NET Standard 2.1 e implementadas no .NET Core 3.0 para fornecer um modelo de programação natural para fontes de dados de streaming assíncrono.</span><span class="sxs-lookup"><span data-stu-id="127be-105">Async streams rely on new interfaces introduced in .NET Standard 2.1 and implemented in .NET Core 3.0 to provide a natural programming model for asynchronous streaming data sources.</span></span>

<span data-ttu-id="127be-106">Neste tutorial, você aprenderá a:</span><span class="sxs-lookup"><span data-stu-id="127be-106">In this tutorial, you'll learn how to:</span></span>

> [!div class="checklist"]
> * <span data-ttu-id="127be-107">Criar uma fonte de dados que gera uma sequência de elementos de dados de forma assíncrona.</span><span class="sxs-lookup"><span data-stu-id="127be-107">Create a data source that generates a sequence of data elements asynchronously.</span></span>
> * <span data-ttu-id="127be-108">Consumir essa fonte de dados de forma assíncrona.</span><span class="sxs-lookup"><span data-stu-id="127be-108">Consume that data source asynchronously.</span></span>
> * <span data-ttu-id="127be-109">Reconhecer quando a nova interface e a fonte de dados forem preferenciais para sequências anteriores de dados síncronos.</span><span class="sxs-lookup"><span data-stu-id="127be-109">Recognize when the new interface and data source are preferred to earlier synchronous data sequences.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="127be-110">Pré-requisitos</span><span class="sxs-lookup"><span data-stu-id="127be-110">Prerequisites</span></span>

<span data-ttu-id="127be-111">Você precisará configurar seu computador para executar o .NET Core, incluindo o compilador beta do C# 8.0.</span><span class="sxs-lookup"><span data-stu-id="127be-111">You’ll need to set up your machine to run .NET Core, including the C# 8.0 beta compiler.</span></span> <span data-ttu-id="127be-112">O compilador beta do C# 8 está disponível com o [Visual Studio 2019](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=inline+link&utm_content=download+vs2019) ou o mais recente [SDK .NET Core 3.0 versão prévia](https://dotnet.microsoft.com/download/dotnet-core/3.0).</span><span class="sxs-lookup"><span data-stu-id="127be-112">The C# 8 beta compiler is available starting with [Visual Studio 2019](https://visualstudio.microsoft.com/downloads/?utm_medium=microsoft&utm_source=docs.microsoft.com&utm_campaign=inline+link&utm_content=download+vs2019), or the latest [.NET Core 3.0 preview SDK](https://dotnet.microsoft.com/download/dotnet-core/3.0).</span></span> <span data-ttu-id="127be-113">Os fluxos assíncronos estão disponíveis a partir do .NET Core 3.0 versão prévia 1.</span><span class="sxs-lookup"><span data-stu-id="127be-113">Async streams are first available in .NET Core 3.0 preview 1.</span></span>

<span data-ttu-id="127be-114">Você precisará criar um [token de acesso do GitHub](https://help.github.com/articles/creating-a-personal-access-token-for-the-command-line/#creating-a-token) para poder acessar o ponto de extremidade GitHub GraphQL.</span><span class="sxs-lookup"><span data-stu-id="127be-114">You'll need to create a [GitHub access token](https://help.github.com/articles/creating-a-personal-access-token-for-the-command-line/#creating-a-token) so that you can access the GitHub GraphQL endpoint.</span></span> <span data-ttu-id="127be-115">Selecione as seguintes permissões para o Token de acesso do GitHub:</span><span class="sxs-lookup"><span data-stu-id="127be-115">Select the following permissions for your GitHub Access Token:</span></span>

- <span data-ttu-id="127be-116">repo:status</span><span class="sxs-lookup"><span data-stu-id="127be-116">repo:status</span></span>
- <span data-ttu-id="127be-117">public_repo</span><span class="sxs-lookup"><span data-stu-id="127be-117">public_repo</span></span>

<span data-ttu-id="127be-118">Salve o token de acesso em um local seguro, de modo que possa usá-lo para acessar o ponto de extremidade da API do GitHub.</span><span class="sxs-lookup"><span data-stu-id="127be-118">Save the access token in a safe place so you can use it to gain access to the GitHub API endpoint.</span></span>

> [!WARNING]
> <span data-ttu-id="127be-119">Mantenha seu token de acesso pessoal protegido.</span><span class="sxs-lookup"><span data-stu-id="127be-119">Keep your personal access token secure.</span></span> <span data-ttu-id="127be-120">Qualquer software com seu token de acesso pessoal pode fazer chamadas da API do GitHub usando seus direitos de acesso.</span><span class="sxs-lookup"><span data-stu-id="127be-120">Any software with your personal access token could make GitHub API calls using your access rights.</span></span>

<span data-ttu-id="127be-121">Este tutorial pressupõe que você esteja familiarizado com o C# e .NET, incluindo o Visual Studio ou a CLI do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="127be-121">This tutorial assumes you're familiar with C# and .NET, including either Visual Studio or the .NET Core CLI.</span></span>

## <a name="run-the-starter-application"></a><span data-ttu-id="127be-122">Executar o aplicativo inicial</span><span class="sxs-lookup"><span data-stu-id="127be-122">Run the starter application</span></span>

<span data-ttu-id="127be-123">Você pode obter o código para o aplicativo inicial usado neste tutorial em nosso repositório [dotnet/samples](https://github.com/dotnet/samples) na pasta [csharp/tutoriais/AsyncStreams](https://github.com/dotnet/samples/tree/master/csharp/tutorials/AsyncStreams/start).</span><span class="sxs-lookup"><span data-stu-id="127be-123">You can get the code for the starter application used in this tutorial from our [dotnet/samples](https://github.com/dotnet/samples) repository in the [csharp/tutorials/AsyncStreams](https://github.com/dotnet/samples/tree/master/csharp/tutorials/AsyncStreams/start) folder.</span></span>

<span data-ttu-id="127be-124">O aplicativo inicial é um aplicativo de console que usa a interface [GitHub GraphQL](https://developer.github.com/v4/) para recuperar os problemas recentes gravados no repositório [dotnet/docs](https://github.com/dotnet/docs).</span><span class="sxs-lookup"><span data-stu-id="127be-124">The starter application is a console application that uses the [GitHub GraphQL](https://developer.github.com/v4/) interface to retrieve recent issues written in the [dotnet/docs](https://github.com/dotnet/docs) repository.</span></span> <span data-ttu-id="127be-125">Comece observando o código a seguir para o método `Main` do aplicativo inicial:</span><span class="sxs-lookup"><span data-stu-id="127be-125">Start by looking at the following code for the starter app `Main` method:</span></span>

[!code-csharp[StarterAppMain](~/samples/csharp/tutorials/AsyncStreams/start/IssuePRreport/IssuePRreport/Program.cs#StarterAppMain)]

<span data-ttu-id="127be-126">Você pode definir uma variável de ambiente `GitHubKey` para o token de acesso pessoal ou pode substituir o último argumento na chamada para `GenEnvVariable` com seu token de acesso pessoal.</span><span class="sxs-lookup"><span data-stu-id="127be-126">You can either set a `GitHubKey` environment variable to your personal access token, or you can replace the last argument in the call to `GenEnvVariable` with your personal access token.</span></span> <span data-ttu-id="127be-127">Não coloque o código de acesso no código-fonte se for salvar a fonte com outras ou coloque-a em um repositório de origem compartilhado.</span><span class="sxs-lookup"><span data-stu-id="127be-127">Don't put your access code in source code if you'll be saving the source with others, or putting it in a shared source repository.</span></span>

<span data-ttu-id="127be-128">Após criar o cliente do GitHub, o código em `Main` criará um objeto de relatório de andamento e um token de cancelamento.</span><span class="sxs-lookup"><span data-stu-id="127be-128">After creating the GitHub client, the code in `Main` creates a progress reporting object and a cancellation token.</span></span> <span data-ttu-id="127be-129">Depois que esses objetos forem criados, `Main` chamará `runPagedQueryAsync` para recuperar os 250 problemas mais recente criados.</span><span class="sxs-lookup"><span data-stu-id="127be-129">Once those objects are created, `Main` calls `runPagedQueryAsync` to retrieve the most recent 250 created issues.</span></span> <span data-ttu-id="127be-130">Depois que a tarefa for concluída, os resultados serão exibidos.</span><span class="sxs-lookup"><span data-stu-id="127be-130">After that task has finished, the results are displayed.</span></span>

<span data-ttu-id="127be-131">Ao executar o aplicativo inicial, você poderá realizar algumas observações importantes sobre como esse aplicativo é executado.</span><span class="sxs-lookup"><span data-stu-id="127be-131">When you run the starter application, you can make some important observations about how this application runs.</span></span>  <span data-ttu-id="127be-132">Você verá o progresso informado para cada página retornada do GitHub.</span><span class="sxs-lookup"><span data-stu-id="127be-132">You'll see progress reported for each page returned from GitHub.</span></span> <span data-ttu-id="127be-133">É possível observar uma pausa perceptível antes do GitHub retornar cada nova página de problemas.</span><span class="sxs-lookup"><span data-stu-id="127be-133">You can observe a noticeable pause before GitHub returns each new page of issues.</span></span> <span data-ttu-id="127be-134">Por fim, os problemas só serão exibidos depois que todas as 10 páginas forem recuperadas do GitHub.</span><span class="sxs-lookup"><span data-stu-id="127be-134">Finally, the issues are displayed only after all 10 pages have been retrieved from GitHub.</span></span>

## <a name="examine-the-implementation"></a><span data-ttu-id="127be-135">Examinar a implementação</span><span class="sxs-lookup"><span data-stu-id="127be-135">Examine the implementation</span></span>

<span data-ttu-id="127be-136">A implementação revela por que você observou o comportamento discutido na seção anterior.</span><span class="sxs-lookup"><span data-stu-id="127be-136">The implementation reveals why you observed the behavior discussed in the previous section.</span></span> <span data-ttu-id="127be-137">Examine o código para `runPagedQueryAsync`:</span><span class="sxs-lookup"><span data-stu-id="127be-137">Examine the code for `runPagedQueryAsync`:</span></span>

[!code-csharp[RunPagedQueryStarter](~/samples/csharp/tutorials/AsyncStreams/start/IssuePRreport/IssuePRreport/Program.cs#RunPagedQuery)]

<span data-ttu-id="127be-138">Vamos nos concentrar no algoritmo de paginação e na estrutura assíncrona do código anterior.</span><span class="sxs-lookup"><span data-stu-id="127be-138">Let's concentrate on the paging algorithm and async structure of the preceding code.</span></span> <span data-ttu-id="127be-139">(Confira na [documentação do GitHub GraphQL](https://developer.github.com/v4/guides/) mais detalhes sobre a API do GitHub GraphQL). O método `runPagedQueryAsync` enumera os problemas do mais recente ao mais antigo.</span><span class="sxs-lookup"><span data-stu-id="127be-139">(You can consult the [GitHub GraphQL documentation](https://developer.github.com/v4/guides/) for details on the GitHub GraphQL API.) The `runPagedQueryAsync` method enumerates the issues from most recent to oldest.</span></span> <span data-ttu-id="127be-140">Ele solicita 25 problemas por página e examina a estrutura `pageInfo` da resposta para continuar com a página anterior.</span><span class="sxs-lookup"><span data-stu-id="127be-140">It requests 25 issues per page and examines the `pageInfo` structure of the response to continue with the previous page.</span></span> <span data-ttu-id="127be-141">Isso segue o suporte de paginação padrão do GraphQL para respostas com várias páginas.</span><span class="sxs-lookup"><span data-stu-id="127be-141">That follows GraphQL's standard paging support for multi-page responses.</span></span> <span data-ttu-id="127be-142">A resposta inclui um objeto `pageInfo` que inclui um valor `hasPreviousPages` e um valor `startCursor` usados para solicitar a página anterior.</span><span class="sxs-lookup"><span data-stu-id="127be-142">The response includes a `pageInfo` object that includes a `hasPreviousPages` value and a `startCursor` value used to request the previous page.</span></span> <span data-ttu-id="127be-143">Os problemas estão na matriz `nodes`.</span><span class="sxs-lookup"><span data-stu-id="127be-143">The issues are in the `nodes` array.</span></span> <span data-ttu-id="127be-144">O método `runPagedQueryAsync` anexa esses nós em uma matriz que contém todos os resultados de todas as páginas.</span><span class="sxs-lookup"><span data-stu-id="127be-144">The `runPagedQueryAsync` method appends these nodes to an array that contains all the results from all pages.</span></span>

<span data-ttu-id="127be-145">Após a recuperação e a restauração de uma página de resultados, `runPagedQueryAsync` informa o andamento e verifica o cancelamento.</span><span class="sxs-lookup"><span data-stu-id="127be-145">After retrieving and restoring a page of results, `runPagedQueryAsync` reports progress and checks for cancellation.</span></span> <span data-ttu-id="127be-146">Se o cancelamento tiver sido solicitado, `runPagedQueryAsync` gerará um <xref:System.OperationCanceledException>.</span><span class="sxs-lookup"><span data-stu-id="127be-146">If cancellation has been requested, `runPagedQueryAsync` throws an <xref:System.OperationCanceledException>.</span></span>

<span data-ttu-id="127be-147">Há vários elementos nesse código que podem ser melhorados.</span><span class="sxs-lookup"><span data-stu-id="127be-147">There are several elements in this code that can be improved.</span></span> <span data-ttu-id="127be-148">Acima de tudo, `runPagedQueryAsync` deve alocar armazenamento para todos os problemas retornados.</span><span class="sxs-lookup"><span data-stu-id="127be-148">Most importantly, `runPagedQueryAsync` must allocate storage for all the issues returned.</span></span> <span data-ttu-id="127be-149">Este exemplo é interrompido em 250 problemas porque recuperar todos os problemas exigiria muito mais memória para armazenar todos os problemas recuperados.</span><span class="sxs-lookup"><span data-stu-id="127be-149">This sample stops at 250 issues because retrieving all open issues would require much more memory to store all the retrieved issues.</span></span> <span data-ttu-id="127be-150">Além disso, os protocolos que dão suporte ao progresso e ao cancelamento tornam o algoritmo mais difícil de entender em sua primeira leitura.</span><span class="sxs-lookup"><span data-stu-id="127be-150">In addition, the protocols for supporting progress and supporting cancellation make the algorithm harder to understand on its first reading.</span></span> <span data-ttu-id="127be-151">Você deve procurar a classe progresso para localizar onde o progresso é informado.</span><span class="sxs-lookup"><span data-stu-id="127be-151">You must look for the progress class to find where progress is reported.</span></span> <span data-ttu-id="127be-152">Você também tem que rastrear as comunicações por meio de <xref:System.Threading.CancellationTokenSource> e seu associado <xref:System.Threading.CancellationToken> para entender onde o cancelamento foi solicitado e onde ele foi concedido.</span><span class="sxs-lookup"><span data-stu-id="127be-152">You also have to trace the communications through the <xref:System.Threading.CancellationTokenSource> and its associated <xref:System.Threading.CancellationToken> to understand where cancellation is requested and where it's granted.</span></span>

## <a name="async-streams-provide-a-better-way"></a><span data-ttu-id="127be-153">Os fluxos assíncronos fornecem uma melhor maneira</span><span class="sxs-lookup"><span data-stu-id="127be-153">Async streams provide a better way</span></span>

<span data-ttu-id="127be-154">Os fluxos assíncronos e o suporte de linguagem associado lidam com todas essas preocupações.</span><span class="sxs-lookup"><span data-stu-id="127be-154">Async streams and the associated language support address all those concerns.</span></span> <span data-ttu-id="127be-155">O código que gera a sequência agora pode usar `yield return` para retornar os elementos em um método que foi declarado com o modificador `async`.</span><span class="sxs-lookup"><span data-stu-id="127be-155">The code that generates the sequence can now use `yield return` to return elements in a method that was declared with the `async` modifier.</span></span> <span data-ttu-id="127be-156">É possível consumir um fluxo assíncrono usando um loop `await foreach` da mesma forma que é possível consumir qualquer sequência usando um loop `foreach`.</span><span class="sxs-lookup"><span data-stu-id="127be-156">You can consume an async stream using an `await foreach` loop just as you consume any sequence using a `foreach` loop.</span></span>

<span data-ttu-id="127be-157">Esses novos recursos de linguagem dependem das três novas interfaces adicionadas ao .NET Standard 2.1 e implementadas no .NET Core 3.0:</span><span class="sxs-lookup"><span data-stu-id="127be-157">These new language features depend on three new interfaces added to .NET Standard 2.1 and implemented in .NET Core 3.0:</span></span>

```csharp
namespace System.Collections.Generic
{
    public interface IAsyncEnumerable<out T>
    {
        IAsyncEnumerator<T> GetAsyncEnumerator(CancellationToken cancellationToken = default);
    }

    public interface IAsyncEnumerator<out T> : IAsyncDisposable
    {
        T Current { get; }

        ValueTask<bool> MoveNextAsync();
    }
}

namespace System
{
    public interface IAsyncDisposable
    {
        ValueTask DisposeAsync();
    }
}
```

<span data-ttu-id="127be-158">Essas três interfaces devem ser familiares à maioria dos desenvolvedores C#.</span><span class="sxs-lookup"><span data-stu-id="127be-158">These three interfaces should be familiar to most C# developers.</span></span> <span data-ttu-id="127be-159">Elas se comportam de maneira semelhante às suas contrapartes síncronas:</span><span class="sxs-lookup"><span data-stu-id="127be-159">They behave in a manner similar to their synchronous counterparts:</span></span>

- <xref:System.Collections.Generic.IEnumerable%601?displayProperty=nameWithType>
- <xref:System.Collections.Generic.IEnumerator%601?displayProperty=nameWithType>
- <xref:System.IDisposable?displayProperty=nameWithType>

<span data-ttu-id="127be-160">Um tipo que pode não ser familiar é <xref:System.Threading.Tasks.ValueTask?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="127be-160">One type that may be unfamiliar is <xref:System.Threading.Tasks.ValueTask?displayProperty=nameWithType>.</span></span> <span data-ttu-id="127be-161">A estrutura `ValueTask` fornece uma API semelhante para a classe <xref:System.Threading.Tasks.Task?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="127be-161">The `ValueTask` struct provides a similar API to the <xref:System.Threading.Tasks.Task?displayProperty=nameWithType> class.</span></span> `ValueTask` <span data-ttu-id="127be-162">é usado nas interfaces por motivos de desempenho.</span><span class="sxs-lookup"><span data-stu-id="127be-162">is used in these interfaces for performance reasons.</span></span>

## <a name="convert-to-async-streams"></a><span data-ttu-id="127be-163">Converter para fluxos assíncronos</span><span class="sxs-lookup"><span data-stu-id="127be-163">Convert to async streams</span></span>

<span data-ttu-id="127be-164">Em seguida, converta o método `runPagedQueryAsync` para gerar um fluxo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="127be-164">Next, convert the `runPagedQueryAsync` method to generate an async stream.</span></span> <span data-ttu-id="127be-165">Primeiro, altere a assinatura de `runPagedQueryAsync` para retornar um `IAsyncEnumerable<JToken>` e remova os objetos de progresso e o token de cancelamento da lista de parâmetros, conforme mostrado no código a seguir:</span><span class="sxs-lookup"><span data-stu-id="127be-165">First, change the signature of `runPagedQueryAsync` to return an `IAsyncEnumerable<JToken>`, and remove the cancellation token and progress objects from the parameter list as shown in the following code:</span></span>

[!code-csharp[FinishedSignature](~/samples/csharp/tutorials/AsyncStreams/finished/IssuePRreport/IssuePRreport/Program.cs#UpdateSignature)]

<span data-ttu-id="127be-166">O código inicial processa cada página à medida que a página é recuperada, como mostrado no código a seguir:</span><span class="sxs-lookup"><span data-stu-id="127be-166">The starter code processes each page as the page is retrieved, as shown in the following code:</span></span>

[!code-csharp[StarterPaging](~/samples/csharp/tutorials/AsyncStreams/start/IssuePRreport/IssuePRreport/Program.cs#ProcessPage)]

<span data-ttu-id="127be-167">Substitua essas três linhas pelo seguinte código:</span><span class="sxs-lookup"><span data-stu-id="127be-167">Replace those three lines with the following code:</span></span>

[!code-csharp[FinishedPaging](~/samples/csharp/tutorials/AsyncStreams/finished/IssuePRreport/IssuePRreport/Program.cs#YieldReturnPage)]

<span data-ttu-id="127be-168">Você também pode remover a declaração de `finalResults` anteriormente nesse método e a instrução `return` que segue o loop que você modificou.</span><span class="sxs-lookup"><span data-stu-id="127be-168">You can also remove the declaration of `finalResults` earlier in this method and the `return` statement that follows the loop you modified.</span></span>

<span data-ttu-id="127be-169">Você terminou as alterações para gerar um fluxo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="127be-169">You've finished the changes to generate an async stream.</span></span> <span data-ttu-id="127be-170">O método concluído deve se parecer com o código a seguir:</span><span class="sxs-lookup"><span data-stu-id="127be-170">The finished method should resemble the code below:</span></span>

[!code-csharp[FinishedGenerate](~/samples/csharp/tutorials/AsyncStreams/finished/IssuePRreport/IssuePRreport/Program.cs#GenerateAsyncStream)]

<span data-ttu-id="127be-171">Em seguida, você pode alterar o código que consome a coleção para consumir o fluxo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="127be-171">Next, you change the code that consumes the collection to consume the async stream.</span></span> <span data-ttu-id="127be-172">Localize o seguinte código em `Main` que processa a coleção de problemas:</span><span class="sxs-lookup"><span data-stu-id="127be-172">Find the following code in `Main` that processes the collection of issues:</span></span>

[!code-csharp[EnumerateOldStyle](~/samples/csharp/tutorials/AsyncStreams/start/IssuePRreport/IssuePRreport/Program.cs#EnumerateOldStyle)]

<span data-ttu-id="127be-173">Substitua o código pelo seguinte loop `await foreach`:</span><span class="sxs-lookup"><span data-stu-id="127be-173">Replace that code with the following `await foreach` loop:</span></span>

[!code-csharp[FinishedEnumerateAsyncStream](~/samples/csharp/tutorials/AsyncStreams/finished/IssuePRreport/IssuePRreport/Program.cs#EnumerateAsyncStream)]

<span data-ttu-id="127be-174">Você pode obter o código do tutorial concluído no repositório [dotnet/samples](https://github.com/dotnet/samples) na pasta [csharp/tutoriais/AsyncStreams](https://github.com/dotnet/samples/tree/master/csharp/tutorials/AsyncStreams/finished).</span><span class="sxs-lookup"><span data-stu-id="127be-174">You can get the code for the finished tutorial from the [dotnet/samples](https://github.com/dotnet/samples) repository in the [csharp/tutorials/AsyncStreams](https://github.com/dotnet/samples/tree/master/csharp/tutorials/AsyncStreams/finished) folder.</span></span>

## <a name="run-the-finished-application"></a><span data-ttu-id="127be-175">Executar o aplicativo finalizado</span><span class="sxs-lookup"><span data-stu-id="127be-175">Run the finished application</span></span>

<span data-ttu-id="127be-176">Execute o aplicativo novamente.</span><span class="sxs-lookup"><span data-stu-id="127be-176">Run the application again.</span></span> <span data-ttu-id="127be-177">Compare esse comportamento com o comportamento do aplicativo inicial.</span><span class="sxs-lookup"><span data-stu-id="127be-177">Contrast its behavior with the behavior of the starter application.</span></span> <span data-ttu-id="127be-178">A primeira página de resultados é enumerada assim que fica disponível.</span><span class="sxs-lookup"><span data-stu-id="127be-178">The first page of results is enumerated as soon as it's available.</span></span> <span data-ttu-id="127be-179">Há uma pausa observável à medida que cada nova página é solicitada e recuperada, e os resultados da próxima página são rapidamente enumerados.</span><span class="sxs-lookup"><span data-stu-id="127be-179">There's an observable pause as each new page is requested and retrieved, then the next page's results are quickly enumerated.</span></span> <span data-ttu-id="127be-180">O bloco `try` / `catch` não é necessário para lidar com o cancelamento: o chamador pode interromper a enumeração da coleção.</span><span class="sxs-lookup"><span data-stu-id="127be-180">The `try` / `catch` block isn't needed to handle cancellation: the caller can stop enumerating the collection.</span></span> <span data-ttu-id="127be-181">O progresso é claramente informado, pois o fluxo assíncrono gera resultados à medida que cada página é baixada.</span><span class="sxs-lookup"><span data-stu-id="127be-181">Progress is clearly reported because the async stream generates results as each page is downloaded.</span></span>

<span data-ttu-id="127be-182">Você pode ver as melhorias no uso da memória examinando o código.</span><span class="sxs-lookup"><span data-stu-id="127be-182">You can see improvements in memory use by examining the code.</span></span> <span data-ttu-id="127be-183">Não é mais necessário alocar uma coleção para armazenar todos os resultados antes de serem enumerados.</span><span class="sxs-lookup"><span data-stu-id="127be-183">You no longer need to allocate a collection to store all the results before they're enumerated.</span></span> <span data-ttu-id="127be-184">O chamador pode determinar como consumir os resultados e se uma coleção de armazenamento é necessária.</span><span class="sxs-lookup"><span data-stu-id="127be-184">The caller can determine how to consume the results and if a storage collection is needed.</span></span>

<span data-ttu-id="127be-185">Execute o aplicativos inicial e o acabado, e observe você mesmo as diferenças entre as implementações.</span><span class="sxs-lookup"><span data-stu-id="127be-185">Run both the starter and finished applications and you can observe the differences between the implementations for yourself.</span></span> <span data-ttu-id="127be-186">Depois de terminar, você pode excluir o token de acesso de GitHub criado ao iniciar este tutorial.</span><span class="sxs-lookup"><span data-stu-id="127be-186">You can delete the GitHub access token you created when you started this tutorial after you've finished.</span></span> <span data-ttu-id="127be-187">Se um invasor obtiver acesso a esse token, ele poderá acessar as APIs do GitHub usando suas credenciais.</span><span class="sxs-lookup"><span data-stu-id="127be-187">If an attacker gained access to that token, they could access GitHub APIs using your credentials.</span></span>
