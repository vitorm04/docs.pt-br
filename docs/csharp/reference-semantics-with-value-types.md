---
title: "Semântica de referência com tipos de valores"
description: "Compreenda os recursos de linguagem que minimizam, com segurança, a cópia de estruturas"
author: billwagner
ms.author: wiwagn
ms.date: 11/10/2017
ms.topic: article
ms.prod: .net
ms.technology: devlang-csharp
ms.devlang: csharp
ms.custom: mvc
ms.openlocfilehash: 0c6e44a3e1a1458f4211b66b6d1ef5b4b30cd7c1
ms.sourcegitcommit: 5177d6ae2e9baf026f07ee0631556700a5a193f7
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/28/2017
---
# <a name="reference-semantics-with-value-types"></a><span data-ttu-id="fd1f3-103">Semântica de referência com tipos de valores</span><span class="sxs-lookup"><span data-stu-id="fd1f3-103">Reference semantics with value types</span></span>

<span data-ttu-id="fd1f3-104">Uma vantagem de usar os tipos de valor é que eles normalmente evitam alocações de heap.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-104">An advantage to using value types is that they often avoid heap allocations.</span></span>
<span data-ttu-id="fd1f3-105">A desvantagem correspondente é que eles são copiados por valor.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-105">The corresponding disadvantage is that they are copied by value.</span></span> <span data-ttu-id="fd1f3-106">Essa compensação dificulta a otimização de algoritmos que operam em grandes quantidades de dados.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-106">This tradeoff makes it harder to optimize algorithms that operate on large amounts of data.</span></span> <span data-ttu-id="fd1f3-107">Os novos recursos de linguagem em C# 7.2 fornecem mecanismos que habilitam a semântica de transmissão por referência com tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-107">New language features in C# 7.2 provide mechanisms that enable pass-by-reference semantics with value types.</span></span> <span data-ttu-id="fd1f3-108">Se usar esses recursos criteriosamente, você poderá minimizar tanto as alocações quanto as operações de cópia.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-108">If you use these features wisely you can minimize both allocations and copy operations.</span></span> <span data-ttu-id="fd1f3-109">Este artigo explora esses novos recursos.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-109">This article explores those new features.</span></span>

<span data-ttu-id="fd1f3-110">Grande parte do código de exemplo neste artigo demonstra os recursos adicionados em C# 7.2.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-110">Much of the sample code in this article demonstrates features added in C# 7.2.</span></span> <span data-ttu-id="fd1f3-111">Para usar esses recursos, você precisa configurar seu projeto para usar o C# 7.2 ou posterior em seu projeto.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-111">In order to use those features, you have to configure your project to use C# 7.2 or later in your project.</span></span> <span data-ttu-id="fd1f3-112">Você pode usar o Visual Studio para selecioná-lo.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-112">You can use Visual Studio to select it.</span></span> <span data-ttu-id="fd1f3-113">Para cada projeto, selecione **Projeto** no menu e, em seguida, **Propriedades**.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-113">For each project, select **Project** from the menu, then **Properties**.</span></span> <span data-ttu-id="fd1f3-114">Selecione a guia **Compilar** e clique em **Avançado**.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-114">Select the **Build** tab and click **Advanced**.</span></span> <span data-ttu-id="fd1f3-115">A partir daí, você pode configurar a versão da linguagem.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-115">From there, you can configure the language version.</span></span> <span data-ttu-id="fd1f3-116">Escolha “7.2” ou “mais recente”.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-116">Choose either "7.2", or "latest".</span></span>  <span data-ttu-id="fd1f3-117">Ou você pode editar o arquivo *csproj* e adicionar o seguinte nó:</span><span class="sxs-lookup"><span data-stu-id="fd1f3-117">Or you can edit the *csproj* file and add the following node:</span></span>

```XML
  <PropertyGroup>
    <LangVersion>7.2</LangVersion>
  </PropertyGroup>
```

<span data-ttu-id="fd1f3-118">Você pode usar “7.2” ou “mais recente” para o valor.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-118">You can use either "7.2" or "latest" for the value.</span></span>

## <a name="specifying-in-parameters"></a><span data-ttu-id="fd1f3-119">Especificando os parâmetros `in`</span><span class="sxs-lookup"><span data-stu-id="fd1f3-119">Specifying `in` parameters</span></span>

<span data-ttu-id="fd1f3-120">A linguagem C# 7.2 adiciona a palavra-chave `in` para complementar as palavras-chave `ref` e `out` existentes quando você grava um método que transmite os argumentos por referência.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-120">C# 7.2 adds the `in` keyword to complement the existing `ref` and `out` keywords when you write a method that passes arguments by reference.</span></span> <span data-ttu-id="fd1f3-121">A palavra-chave `in` especifica que você está transmitindo o parâmetro por referência e o método chamado não modifica o valor transmitido para ele.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-121">The `in` keyword specifies that you are passing the parameter by reference and the called method does not modify the value passed to it.</span></span> 

<span data-ttu-id="fd1f3-122">Essa adição fornece um vocabulário completo para expressar sua intenção de design.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-122">This addition provides a full vocabulary to express your design intent.</span></span> <span data-ttu-id="fd1f3-123">Os tipos de valor são copiados no momento em que são transmitidos para um método chamado quando você não especifica nenhum dos modificadores a seguir.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-123">Value types are copied when passed to a called method when you do not specify any of the following modifiers.</span></span> <span data-ttu-id="fd1f3-124">Cada um desses modificadores especifica que um tipo de valor é transmitido por referência, evitando a cópia.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-124">Each of these modifiers specify that a value type is passed by reference, avoiding the copy.</span></span> <span data-ttu-id="fd1f3-125">Cada modificador expressa uma intenção diferente:</span><span class="sxs-lookup"><span data-stu-id="fd1f3-125">Each modifier expresses a different intent:</span></span>

- <span data-ttu-id="fd1f3-126">`out`: esse método define o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-126">`out`: This method sets the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="fd1f3-127">`ref`: esse método pode definir o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-127">`ref`: This method may set the value of the argument used as this parameter.</span></span>
- <span data-ttu-id="fd1f3-128">`in`: esse método não modifica o valor do argumento usado como este parâmetro.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-128">`in`: This method does not modify the value of the argument used as this parameter.</span></span>

<span data-ttu-id="fd1f3-129">Ao adicionar o modificador `in` para transmitir um argumento por referência, você declara que sua intenção de design é transmitir argumentos por referência para evitar cópias desnecessárias.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-129">When you add the `in` modifier to pass an argument by reference, you declare your design intent is to pass arguments by reference to avoid unnecessary copying.</span></span> <span data-ttu-id="fd1f3-130">Você não pretende modificar o objeto utilizado como aquele argumento.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-130">You do not intend to modify the object used as that argument.</span></span> <span data-ttu-id="fd1f3-131">O código a seguir mostra um exemplo de um método que calcula a distância entre dois pontos no espaço 3D.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-131">The following code shows an example of a method that calculates the distance between two points in 3D space.</span></span> 

[!code-csharp[InArgument](../../samples/csharp/reference-semantics/Program.cs#InArgument "Specifying an In argument")]

<span data-ttu-id="fd1f3-132">Os argumentos são duas estruturas que contêm três duplas.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-132">The arguments are two structures that each contain three doubles.</span></span> <span data-ttu-id="fd1f3-133">Uma dupla tem 8 bytes. Então, cada argumento tem 24 bytes.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-133">A double is 8 bytes, so each argument is 24 bytes.</span></span> <span data-ttu-id="fd1f3-134">Ao especificar o modificador `in`, você transmite a referência de 4 ou 8 bytes para esses argumentos, dependendo da arquitetura do computador.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-134">By specifying the `in` modifier, you pass 4-byte or 8-byte reference to those arguments, depending on the architecture of the machine.</span></span> <span data-ttu-id="fd1f3-135">A diferença no tamanho é pequena, mas ela pode aumentar rapidamente quando o aplicativo chama esse método em um loop estreito, usando muitos valores diferentes.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-135">The difference in size is small, but it can quickly add up when your application calls this method in a tight loop using many different values.</span></span>
 
<span data-ttu-id="fd1f3-136">O modificador `in` complementa `out` e `ref` de outras formas também.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-136">The `in` modifier complements `out` and `ref` in other ways as well.</span></span> <span data-ttu-id="fd1f3-137">Não é possível criar sobrecargas de um método que difere somente na presença de `in`, `out` ou `ref`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-137">You cannot create overloads of a method that differ only in the presence of `in`, `out` or `ref`.</span></span> <span data-ttu-id="fd1f3-138">Essas novas regras apresentam o mesmo comportamento que sempre foi definido para os parâmetros `out` e `ref`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-138">These new rules extend the same behavior that had always been defined for `out` and `ref` parameters.</span></span>

<span data-ttu-id="fd1f3-139">O modificador `in` pode ser aplicado a qualquer membro que usa os seguintes parâmetros: métodos, delegados, lambdas, funções locais, indexadores e operadores.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-139">The `in` modifier may be applied to any member that takes parameters: methods, delegates, lambdas, local functions, indexers, operators.</span></span>

<span data-ttu-id="fd1f3-140">Ao contrário dos argumentos `ref` e `out`, você pode usar valores literais ou constantes para o argumento em um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-140">Unlike `ref` and `out` arguments, you may use literal values or constants for the argument to an `in` parameter.</span></span> <span data-ttu-id="fd1f3-141">Além disso, ao contrário de um parâmetro `ref` ou `out`, você não precisa aplicar o modificador `in` no local da chamada.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-141">Also, unlike a `ref` or `out` parameter, you don't need to apply the `in` modifier at the call site.</span></span> <span data-ttu-id="fd1f3-142">O código a seguir mostra dois exemplos para chamar o método `CalculateDistance`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-142">The following code shows you two examples of calling the `CalculateDistance` method.</span></span> <span data-ttu-id="fd1f3-143">O primeiro usa duas variáveis locais transmitidas por referência.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-143">The first uses two local variables passed by reference.</span></span> <span data-ttu-id="fd1f3-144">O segundo inclui uma variável temporária criada como parte da chamada de método.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-144">The second includes a temporary variable created as part of the method call.</span></span> 

[!code-csharp[UseInArgument](../../samples/csharp/reference-semantics/Program.cs#UseInArgument "Specifying an In argument")]

<span data-ttu-id="fd1f3-145">Há várias maneiras pelas quais o compilador garante que a natureza somente leitura de um argumento `in` será aplicada.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-145">There are several ways in which the compiler ensures that the read-only nature of an `in` argument is enforced.</span></span>  <span data-ttu-id="fd1f3-146">Em primeiro lugar, o método chamado não pode ser atribuído diretamente a um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-146">First of all, the called method can't directly assign to an `in` parameter.</span></span> <span data-ttu-id="fd1f3-147">Não é possível atribuí-lo diretamente a nenhum campo de um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-147">It can't directly assign to any field of an `in` parameter.</span></span> <span data-ttu-id="fd1f3-148">Além disso, você não pode transmitir um parâmetro `in` para nenhum método que exige o modificador `ref` ou `out`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-148">In addition, you cannot pass an `in` parameter to any method demanding the `ref` or `out` modifier.</span></span>
<span data-ttu-id="fd1f3-149">O compilador impõe que o argumento `in` seja uma variável somente leitura.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-149">The compiler enforces that the `in` argument is a readonly variable.</span></span> <span data-ttu-id="fd1f3-150">Você pode chamar qualquer método de instância que usa a semântica de passagens por valor.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-150">You can call any instance method that uses pass-by-value semantics.</span></span> <span data-ttu-id="fd1f3-151">Nessas instâncias, uma cópia do parâmetro `in` é criada.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-151">In those instances, a copy of the `in` parameter is created.</span></span> <span data-ttu-id="fd1f3-152">Uma vez que o compilador pode criar uma variável temporária para qualquer parâmetro `in`, você também pode especificar valores padrão para qualquer parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-152">Because the compiler can create a temporary variable for any `in` parameter, you can also specify default values for any `in` parameter.</span></span> <span data-ttu-id="fd1f3-153">O código a seguir o utiliza para especificar a origem (ponto 0,0) como o valor padrão para o segundo ponto:</span><span class="sxs-lookup"><span data-stu-id="fd1f3-153">The follow code uses that to specify the origin (point 0,0) as the default value for the second point:</span></span>

[!code-csharp[InArgumentDefault](../../samples/csharp/reference-semantics/Program.cs#InArgumentDefault "Specifying defaults for an in parameter")]

<span data-ttu-id="fd1f3-154">O parâmetro de designação `in` também pode ser usado com tipos de referência ou compilado em valores numéricos.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-154">The `in` parameter designation can also be used with reference types or built in numeric values.</span></span> <span data-ttu-id="fd1f3-155">No entanto, os benefícios em ambos os casos serão mínimos, se houver.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-155">However, the benefits in both cases are minimal, if any.</span></span>

## <a name="ref-readonly-returns"></a><span data-ttu-id="fd1f3-156">Retornos de `ref readonly`</span><span class="sxs-lookup"><span data-stu-id="fd1f3-156">`ref readonly` returns</span></span>

<span data-ttu-id="fd1f3-157">Talvez você queira retornar um tipo de valor por referência, mas impedir o autor da chamada de modificar esse valor.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-157">You may also want to return a value type by reference, but disallow the caller from modifying that value.</span></span> <span data-ttu-id="fd1f3-158">Use o modificador `ref readonly` para expressar essa intenção de design.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-158">Use the `ref readonly` modifier to express that design intent.</span></span> <span data-ttu-id="fd1f3-159">Ela notifica os leitores que você está retornando uma referência aos dados existentes, mas não está permitindo a modificação.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-159">It notifies readers that you are returning a reference to existing data, but not allowing modification.</span></span> 

<span data-ttu-id="fd1f3-160">O compilador impõe que o autor da chamada não pode modificar a referência.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-160">The compiler enforces that the caller cannot modify the reference.</span></span> <span data-ttu-id="fd1f3-161">As tentativas de atribuir diretamente ao valor geram um erro de tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-161">Attempts to assign to the value directly generate a compile-time error.</span></span> <span data-ttu-id="fd1f3-162">No entanto, o compilador não pode saber se algum método de membro modifica o estado da estrutura.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-162">However, the compiler cannot know if any member method modifies the state of the struct.</span></span>
<span data-ttu-id="fd1f3-163">Para garantir que o objeto não será modificado, o compilador cria uma cópia e chama as referências de membro usando essa cópia.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-163">To ensure that the object is not modified, the compiler creates a copy and calls member references using that copy.</span></span> <span data-ttu-id="fd1f3-164">Todas as modificações são para essa cópia de defesa.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-164">Any modifications are to that defensive copy.</span></span> 

<span data-ttu-id="fd1f3-165">É provável que a biblioteca que utiliza o `Point3D` normalmente usaria a origem em todo o código.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-165">It's likely that the library using `Point3D` would often use the origin throughout the code.</span></span> <span data-ttu-id="fd1f3-166">Cada instância cria um novo objeto na pilha.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-166">Every instance creates a new object on the stack.</span></span> <span data-ttu-id="fd1f3-167">Pode ser vantajoso criar uma constante e retorná-la por referência.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-167">It may be advantageous to create a constant and return it by reference.</span></span> <span data-ttu-id="fd1f3-168">Mas, se retornar uma referência ao armazenamento interno, talvez você queira impor que o autor da chamada não poderá modificar o armazenamento referenciado.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-168">But, if you return a reference to internal storage, you may want to enforce that the caller cannot modify the referenced storage.</span></span> <span data-ttu-id="fd1f3-169">O código a seguir define uma propriedade somente leitura que retorna um `readonly ref` para um `Point3D` que especifica a origem.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-169">The following code defines a read-only property that returns a `readonly ref` to a `Point3D` that specifies the origin.</span></span>

[!code-csharp[OriginReference](../../samples/csharp/reference-semantics/Point3D.cs#OriginReference "Creating a readonly Origin reference")]

<span data-ttu-id="fd1f3-170">É fácil criar uma cópia de um retorno da referência somente leitura: basta atribui-la a uma variável não declarada com o modificador `ref readonly`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-170">Creating a copy of a ref readonly return is easy: Just assign it to a variable not declared with the `ref readonly` modifier.</span></span> <span data-ttu-id="fd1f3-171">O compilador gera um código para copiar o objeto como parte da atribuição.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-171">The compiler generates code to copy the object as part of the assignment.</span></span> 

<span data-ttu-id="fd1f3-172">Ao atribuir uma variável a um `ref readonly return`, você pode especificar uma variável `ref readonly` ou uma cópia por valor da referência somente leitura:</span><span class="sxs-lookup"><span data-stu-id="fd1f3-172">When you assign a variable to a `ref readonly return`, you can specify either a `ref readonly` variable, or a by-value copy of the readonly reference:</span></span>

[!code-csharp[AssignRefReadonly](../../samples/csharp/reference-semantics/Program.cs#AssignRefReadonly "Assigning a ref readonly")]

<span data-ttu-id="fd1f3-173">A primeira atribuição no código anterior faz uma cópia da constante `Origin` e atribui essa cópia.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-173">The first assignment in the preceding code makes a copy of the `Origin` constant and assigns that copy.</span></span> <span data-ttu-id="fd1f3-174">A segunda atribui uma referência.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-174">The second assigns a reference.</span></span> <span data-ttu-id="fd1f3-175">Observe que o modificador `readonly` deve ser parte da declaração da variável.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-175">Notice that the `readonly` modifier must be part of the declaration of the variable.</span></span> <span data-ttu-id="fd1f3-176">A referência à qual ele se relaciona não pode ser modificada.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-176">The reference to which it refers cannot be modified.</span></span> <span data-ttu-id="fd1f3-177">As tentativas de modificá-la resultam em um erro em tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-177">Attempts to do so result in a compile-time error.</span></span>

## <a name="readonly-struct-type"></a><span data-ttu-id="fd1f3-178">Tipo `readonly struct`</span><span class="sxs-lookup"><span data-stu-id="fd1f3-178">`readonly struct` type</span></span>

<span data-ttu-id="fd1f3-179">A aplicação do `ref readonly` para usos de tráfego intenso de um struct pode ser suficiente.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-179">Applying `ref readonly` to high-traffic uses of a struct may be sufficient.</span></span>
<span data-ttu-id="fd1f3-180">Em outros momentos, talvez você queira criar um struct imutável.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-180">Other times, you may want to create an immutable struct.</span></span> <span data-ttu-id="fd1f3-181">Então, você sempre poderá transmitir pela referência somente leitura.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-181">Then you can always pass by readonly reference.</span></span> <span data-ttu-id="fd1f3-182">Essa prática remove a cópia de defesa que é feita quando você acessa os métodos de um struct usado como um parâmetro `in`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-182">That practice removes the defensive copies that take place when you access methods of a struct used as an `in` parameter.</span></span>

<span data-ttu-id="fd1f3-183">Você pode fazer isso criando um tipo `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-183">You can do that by creating a `readonly struct` type.</span></span> <span data-ttu-id="fd1f3-184">Você pode adicionar o modificador `readonly` em uma declaração de struct.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-184">You can add the `readonly` modifier to a struct declaration.</span></span> <span data-ttu-id="fd1f3-185">O compilador impõe que todos os membros de instância do struct são `readonly`; o `struct` deve ser imutável.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-185">The compiler enforces that all instance members of the struct are `readonly`; the `struct` must be immutable.</span></span>

<span data-ttu-id="fd1f3-186">Há outras otimizações para um `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-186">There are other optimizations for a `readonly struct`.</span></span> <span data-ttu-id="fd1f3-187">Você pode usar o modificador `in` em todos os locais em que um `readonly struct` é um argumento.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-187">You can use the `in` modifier at every location where a `readonly struct` is an argument.</span></span> <span data-ttu-id="fd1f3-188">Além disso, você pode retornar um `readonly struct` como um `ref return` quando estiver devolvendo um objeto cujo tempo de vida se estende para além do escopo do método que retorna o objeto.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-188">In addition, you can return a `readonly struct` as a `ref return` when you are returning an object whose lifetime extends beyond the scope of the method returning the object.</span></span>

<span data-ttu-id="fd1f3-189">Por fim, o compilador gera um código mais eficiente quando você chama membros de um `readonly struct`: a referência `this`, em vez de uma cópia do destinatário, é sempre um parâmetro `in` transmitido por referência para o método de membro.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-189">Finally, the compiler generates more efficient code when you call members of a `readonly struct`: The `this` reference, instead of a copy of the receiver, is always an `in` parameter passed by reference to the member method.</span></span> <span data-ttu-id="fd1f3-190">Essa otimização economiza mais cópias quando você usa um `readonly struct`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-190">This optimization saves more copying when you use a `readonly struct`.</span></span> <span data-ttu-id="fd1f3-191">O `Point3D` é um ótimo candidato para essa alteração.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-191">The `Point3D` is a great candidate for this change.</span></span> <span data-ttu-id="fd1f3-192">O código a seguir mostra uma estrutura `ReadonlyPoint3D` atualizada:</span><span class="sxs-lookup"><span data-stu-id="fd1f3-192">The following code shows an updated `ReadonlyPoint3D` structure:</span></span>

[!code-csharp[ReadonlyOnlyPoint3D](../../samples/csharp/reference-semantics/Point3D.cs#ReadonlyOnlyPoint3D "Defining an immutable structure")]

## <a name="ref-struct-type"></a><span data-ttu-id="fd1f3-193">Tipo `ref struct`</span><span class="sxs-lookup"><span data-stu-id="fd1f3-193">`ref struct` type</span></span>

<span data-ttu-id="fd1f3-194">Outro recurso de linguagem relacionado é a capacidade de declarar um tipo de valor que deve ser alocado por pilha.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-194">Another related language feature is the ability to declare a value type that must be stack allocated.</span></span> <span data-ttu-id="fd1f3-195">Em outras palavras, esses tipos nunca podem ser criados no heap como um membro de outra classe.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-195">In other words, these types can never be created on the heap as a member of another class.</span></span> <span data-ttu-id="fd1f3-196">A principal motivação para esse recurso foi <xref:System.Span%601> e as estruturas relacionadas.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-196">The primary motivation for this feature was <xref:System.Span%601> and related structures.</span></span> <span data-ttu-id="fd1f3-197">O <xref:System.Span%601> pode conter um ponteiro gerenciado como um de seus membros, sendo o outro o comprimento da extensão.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-197"><xref:System.Span%601> may contain a managed pointer as one of its members, the other being the length of the span.</span></span> <span data-ttu-id="fd1f3-198">Na verdade, ele é implementado de modo um pouco diferente porque o C# não é compatível com a memória gerenciada fora de um contexto sem segurança.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-198">It's actually implemented a bit differently because C# doesn't support pointers to managed memory outside of an unsafe context.</span></span> <span data-ttu-id="fd1f3-199">Qualquer gravação que altere o ponteiro e o comprimento não é atômica.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-199">Any write that changes the pointer and the length is not atomic.</span></span> <span data-ttu-id="fd1f3-200">Isso significa que um <xref:System.Span%601> estaria sujeito a erros fora do intervalo ou a outras violações de segurança do tipo se não fosse restrito a um único registro de ativação.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-200">That means a <xref:System.Span%601> would be subject to out of range errors or other type safety violations were it not constrained to a single stack frame.</span></span> <span data-ttu-id="fd1f3-201">Além disso, colocar um ponteiro gerenciado no heap de GC normalmente ocasiona uma falha no tempo de JIT.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-201">In addition, putting a managed pointer on the GC heap typically crashes at JIT time.</span></span>

<span data-ttu-id="fd1f3-202">Você pode ter requisitos semelhantes funcionando com a memória criada usando [`stackalloc`](language-reference/keywords/stackalloc.md) ou ao usar a memória das APIs de interoperabilidade.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-202">You may have similar requirements working with memory created using [`stackalloc`](language-reference/keywords/stackalloc.md) or when using memory from interop APIs.</span></span> <span data-ttu-id="fd1f3-203">Você pode definir seus próprios tipos `ref struct` para essas necessidades.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-203">You can define your own `ref struct` types for those needs.</span></span> <span data-ttu-id="fd1f3-204">Neste artigo, você verá exemplos que usam `Span<T>` para manter a simplicidade.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-204">In this article, you see examples using `Span<T>` for simplicity.</span></span>

<span data-ttu-id="fd1f3-205">A declaração `ref struct` afirma que um struct desse tipo deve estar na pilha.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-205">The `ref struct` declaration declares that a struct of this type must be on the stack.</span></span> <span data-ttu-id="fd1f3-206">As regras da linguagem garantem o uso seguro desses tipos.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-206">The language rules ensure the safe use of these types.</span></span> <span data-ttu-id="fd1f3-207">Outros tipos declarados como `ref struct` incluem <xref:System.ReadOnlySpan%601>.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-207">Other types declared as `ref struct` include <xref:System.ReadOnlySpan%601>.</span></span> 

<span data-ttu-id="fd1f3-208">A meta de manter um tipo `ref struct` como uma variável alocada na pilha apresenta várias regras que o compilador aplica para todos os tipos `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-208">The goal of keeping a `ref struct` type as a stack-allocated variable introduces several rules that the compiler enforces for all `ref struct` types.</span></span>

- <span data-ttu-id="fd1f3-209">Você não pode encaixotar um `ref struct`.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-209">You can't box a `ref struct`.</span></span> <span data-ttu-id="fd1f3-210">Você não pode atribuir um tipo `ref struct` a uma variável do tipo `object`, `dynamic` ou de qualquer tipo de interface.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-210">You cannot assign a `ref struct` type to a variable of type `object`, `dynamic`, or any interface type.</span></span>
- <span data-ttu-id="fd1f3-211">Você não pode declarar um `ref struct` como um membro de uma classe ou de um struct normal.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-211">You can't declare a `ref struct` as a member of a class or a normal struct.</span></span>
- <span data-ttu-id="fd1f3-212">Você não pode declarar variáveis locais que são do tipo `ref struct` em métodos assíncronos.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-212">You cannot declare local variables that are `ref struct` types in async methods.</span></span> <span data-ttu-id="fd1f3-213">Você pode declará-las em métodos síncronos que retornam `Task`, `Task<T>` ou tipos semelhantes à tarefa.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-213">You can declare them in synchronous methods that return `Task`, `Task<T>` or Task-like types.</span></span>
- <span data-ttu-id="fd1f3-214">Você não pode declarar as variáveis locais `ref struct` em iteradores.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-214">You cannot declare `ref struct` local variables in iterators.</span></span>
- <span data-ttu-id="fd1f3-215">Você não pode capturar as variáveis `ref struct` em expressões lambda ou em funções locais.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-215">You cannot capture `ref struct` variables in lambda expressions or local functions.</span></span>

<span data-ttu-id="fd1f3-216">Essas restrições garantem que você não use acidentalmente um `ref struct` de maneira que possa promovê-lo para o heap gerenciado.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-216">These restrictions ensure that you do not accidentally use a `ref struct` in a manner that could promote it to the managed heap.</span></span>

## <a name="conclusions"></a><span data-ttu-id="fd1f3-217">Conclusões</span><span class="sxs-lookup"><span data-stu-id="fd1f3-217">Conclusions</span></span>

<span data-ttu-id="fd1f3-218">Esses aprimoramentos para a linguagem C# são projetados para algoritmos de desempenho importantes, nos quais as alocações de memória podem ser fundamentais para alcançar o desempenho necessário.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-218">These enhancements to the C# language are designed for performance critical algorithms where memory allocations can be critical to achieving the necessary performance.</span></span> <span data-ttu-id="fd1f3-219">Você pode achar que geralmente não usa esses recursos no código que grava.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-219">You may find that you don't often use these features in the code you write.</span></span> <span data-ttu-id="fd1f3-220">No entanto, esses aprimoramentos foram adotados em vários locais no .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-220">However, these enhancements have been adopted in many locations in the .NET Framework.</span></span> <span data-ttu-id="fd1f3-221">À medida que mais e mais APIs utilizam esses recursos, você verá o desempenho de seus próprios aplicativos melhorar.</span><span class="sxs-lookup"><span data-stu-id="fd1f3-221">As more and more APIs make use of these features, you'll see the performance of your own applications improve.</span></span>
