---
title: Como lançar com segurança usando a correspondência de padrões e o é e como operadores
description: Aprenda a usar técnicas de correspondência de padrões para converter variáveis com segurança em um tipo diferente. É possível usar a correspondência de padrões, assim como os operadores is e as para converter tipos com segurança.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 60e69a8ef55484e3b04f1674c35a1c5dadfa3b7c
ms.sourcegitcommit: 43cbde34970f5f38f30c43cd63b9c7e2e83717ae
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/11/2020
ms.locfileid: "81121005"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-and-the-is-and-as-operators"></a><span data-ttu-id="ef18a-104">Como lançar com segurança usando a correspondência de padrões e o é e como operadores</span><span class="sxs-lookup"><span data-stu-id="ef18a-104">How to safely cast by using pattern matching and the is and as operators</span></span>

<span data-ttu-id="ef18a-105">Como os objetos são polimórficos, é possível que uma variável de tipo de classe base tenha um [tipo](../programming-guide/types/index.md) derivado.</span><span class="sxs-lookup"><span data-stu-id="ef18a-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="ef18a-106">Para acessar os métodos de instância do tipo derivado, é necessário [converter](../programming-guide/types/casting-and-type-conversions.md) o valor de volta no tipo derivado.</span><span class="sxs-lookup"><span data-stu-id="ef18a-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="ef18a-107">No entanto, uma conversão cria o risco de lançar um <xref:System.InvalidCastException>.</span><span class="sxs-lookup"><span data-stu-id="ef18a-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="ef18a-108">O C# fornece instruções de [correspondência de padrões](../pattern-matching.md) que executarão uma conversão condicionalmente somente quando ela tiver êxito.</span><span class="sxs-lookup"><span data-stu-id="ef18a-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="ef18a-109">O C# também oferece os operadores [is](../language-reference/operators/type-testing-and-cast.md#is-operator) e [as](../language-reference/operators/type-testing-and-cast.md#as-operator) para testar se um valor é de um determinado tipo.</span><span class="sxs-lookup"><span data-stu-id="ef18a-109">C# also provides the [is](../language-reference/operators/type-testing-and-cast.md#is-operator) and [as](../language-reference/operators/type-testing-and-cast.md#as-operator) operators to test if a value is of a certain type.</span></span>

<span data-ttu-id="ef18a-110">O exemplo a seguir mostra `is` como usar a declaração de correspondência de padrões:</span><span class="sxs-lookup"><span data-stu-id="ef18a-110">The following example shows how to use the pattern matching `is` statement:</span></span>

[!code-csharp[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="ef18a-111">O exemplo anterior demonstra alguns recursos da sintaxe de correspondência de padrões.</span><span class="sxs-lookup"><span data-stu-id="ef18a-111">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="ef18a-112">A `if (a is Mammal m)` declaração combina o teste com uma atribuição de inicialização.</span><span class="sxs-lookup"><span data-stu-id="ef18a-112">The `if (a is Mammal m)` statement combines the test with an initialization assignment.</span></span> <span data-ttu-id="ef18a-113">A atribuição ocorre apenas quando o teste é bem-sucedido.</span><span class="sxs-lookup"><span data-stu-id="ef18a-113">The assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="ef18a-114">A variável `m` está somente no escopo na instrução `if` inserida em ela foi atribuída.</span><span class="sxs-lookup"><span data-stu-id="ef18a-114">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="ef18a-115">Não é possível acessar `m` posteriormente no mesmo método.</span><span class="sxs-lookup"><span data-stu-id="ef18a-115">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="ef18a-116">O exemplo anterior também mostra [ `as` ](../language-reference/operators/type-testing-and-cast.md#as-operator) como usar o operador para converter um objeto em um tipo especificado.</span><span class="sxs-lookup"><span data-stu-id="ef18a-116">The preceding example also shows how to use the [`as` operator](../language-reference/operators/type-testing-and-cast.md#as-operator) to convert an object to a specified type.</span></span>

<span data-ttu-id="ef18a-117">Você também pode usar a mesma sintaxe para testar se um [tipo de valor anulado](../language-reference/builtin-types/nullable-value-types.md) tiver um valor, como mostrado no exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="ef18a-117">You can also use the same syntax for testing if a [nullable value type](../language-reference/builtin-types/nullable-value-types.md) has a value, as shown in the following example:</span></span>

[!code-csharp[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="ef18a-118">O exemplo anterior demonstra outros recursos da correspondência de padrões a serem usados com conversões.</span><span class="sxs-lookup"><span data-stu-id="ef18a-118">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="ef18a-119">É possível testar se uma variável tem padrão nulo verificando especificamente o valor `null`.</span><span class="sxs-lookup"><span data-stu-id="ef18a-119">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="ef18a-120">Quando o valor de runtime da variável é `null`, uma instrução `is` que verifica um tipo retorna sempre `false`.</span><span class="sxs-lookup"><span data-stu-id="ef18a-120">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="ef18a-121">A instrução `is` da correspondência de padrões não permite um tipo de valor anulável, como `int?` ou `Nullable<int>`, mas é possível testar qualquer outro tipo de valor.</span><span class="sxs-lookup"><span data-stu-id="ef18a-121">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span> <span data-ttu-id="ef18a-122">Os `is` padrões do exemplo anterior não se limitam aos tipos de valor anulados.</span><span class="sxs-lookup"><span data-stu-id="ef18a-122">The `is` patterns from the preceding example are not limited to the nullable value types.</span></span> <span data-ttu-id="ef18a-123">Você também pode usar esses padrões para testar se uma variável `null`de um tipo de referência tem um valor ou é .</span><span class="sxs-lookup"><span data-stu-id="ef18a-123">You also can use those patterns to test if a variable of a reference type has a value or it's `null`.</span></span>

<span data-ttu-id="ef18a-124">A amostra anterior também mostra como você `switch` usa o padrão de tipo em uma declaração onde a variável pode ser um dos muitos tipos diferentes.</span><span class="sxs-lookup"><span data-stu-id="ef18a-124">The preceding sample also shows how you use the type pattern in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="ef18a-125">Se você quiser testar se uma variável é um determinado tipo, mas não `is` atribuí-la a uma nova variável, você pode usar os operadores e `as` operadores para tipos de referência e tipos de valor anulados.</span><span class="sxs-lookup"><span data-stu-id="ef18a-125">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable value types.</span></span> <span data-ttu-id="ef18a-126">O seguinte código mostra como usar as instruções `is` e `as` que faziam parte da linguagem C# antes que a correspondência de padrões fosse introduzida para testar se uma variável é de um determinado tipo:</span><span class="sxs-lookup"><span data-stu-id="ef18a-126">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="ef18a-127">Como você pode ver na comparação desse código com o de correspondência de padrões, a sintaxe de correspondência de padrões oferece recursos mais robustos combinando o teste e a atribuição em uma única instrução.</span><span class="sxs-lookup"><span data-stu-id="ef18a-127">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="ef18a-128">Use a sintaxe de correspondência de padrões sempre que possível.</span><span class="sxs-lookup"><span data-stu-id="ef18a-128">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="ef18a-129">Você pode experimentar essas amostras olhando para o código em nosso [repositório GitHub](https://github.com/dotnet/docs/tree/master/samples/snippets/csharp/how-to/safelycast).</span><span class="sxs-lookup"><span data-stu-id="ef18a-129">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/docs/tree/master/samples/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="ef18a-130">Ou então, você pode baixar os exemplos [como um arquivo zip](../../../samples/snippets/csharp/how-to/safelycast.zip).</span><span class="sxs-lookup"><span data-stu-id="ef18a-130">Or you can download the samples [as a zip file](../../../samples/snippets/csharp/how-to/safelycast.zip).</span></span>
