---
title: Como converter com segurança usando a correspondência de padrões e os operadores is e as
description: Aprenda a usar técnicas de correspondência de padrões para converter variáveis com segurança em um tipo diferente. É possível usar a correspondência de padrões, assim como os operadores is e as para converter tipos com segurança.
ms.date: 09/05/2018
helpviewer_keywords:
- cast operators [C#], as and is operators
- as operator [C#]
- is operator [C#]
ms.openlocfilehash: 88289099864293b3b19da62155d58ba4797948bd
ms.sourcegitcommit: c7f3e2e9d6ead6cc3acd0d66b10a251d0c66e59d
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/08/2018
ms.locfileid: "44216166"
---
# <a name="how-to-safely-cast-by-using-pattern-matching-is-and-as-operators"></a><span data-ttu-id="297b9-104">Como converter com segurança usando a correspondência de padrões e os operadores is e as</span><span class="sxs-lookup"><span data-stu-id="297b9-104">How to: safely cast by using pattern matching is and as operators</span></span>

<span data-ttu-id="297b9-105">Como os objetos são polimórficos, é possível que uma variável de tipo de classe base tenha um [tipo](../programming-guide/types/index.md) derivado.</span><span class="sxs-lookup"><span data-stu-id="297b9-105">Because objects are polymorphic, it's possible for a variable of a base class type to hold a derived [type](../programming-guide/types/index.md).</span></span> <span data-ttu-id="297b9-106">Para acessar os métodos de instância do tipo derivado, é necessário [converter](../programming-guide/types/casting-and-type-conversions.md) o valor de volta no tipo derivado.</span><span class="sxs-lookup"><span data-stu-id="297b9-106">To access the derived type's instance members, it's necessary to [cast](../programming-guide/types/casting-and-type-conversions.md) the value back to the derived type.</span></span> <span data-ttu-id="297b9-107">No entanto, uma conversão cria o risco de lançar um <xref:System.InvalidCastException>.</span><span class="sxs-lookup"><span data-stu-id="297b9-107">However, a cast creates the risk of throwing an <xref:System.InvalidCastException>.</span></span> <span data-ttu-id="297b9-108">O C# fornece instruções de [correspondência de padrões](../pattern-matching.md) que executarão uma conversão condicionalmente somente quando ela tiver êxito.</span><span class="sxs-lookup"><span data-stu-id="297b9-108">C# provides [pattern matching](../pattern-matching.md) statements that perform a cast conditionally only when it will succeed.</span></span> <span data-ttu-id="297b9-109">O C# também oferece os operadores [is](../language-reference/keywords/is.md) e [as](../language-reference/keywords/as.md) para testar se um valor é de um determinado tipo.</span><span class="sxs-lookup"><span data-stu-id="297b9-109">C# also provides the [is](../language-reference/keywords/is.md) and [as](../language-reference/keywords/as.md) operators to test if a value is of a certain type.</span></span>

[!INCLUDE[interactive-note](~/includes/csharp-interactive-note.md)]

<span data-ttu-id="297b9-110">O código a seguir demonstra a instrução `is` da correspondência de padrões.</span><span class="sxs-lookup"><span data-stu-id="297b9-110">The following code demonstrates the pattern matching `is` statement.</span></span> <span data-ttu-id="297b9-111">Ele contém métodos que testam um argumento de método para determinar se ele é um argumento de um possível conjunto de tipos derivados:</span><span class="sxs-lookup"><span data-stu-id="297b9-111">It contains methods that test a method argument to determine if it is one of a possible set of derived types:</span></span>

[!code-csharp-interactive[Pattern matching is statement](../../../samples/snippets/csharp/how-to/safelycast/patternmatching/Program.cs#PatternMatchingIs)]

<span data-ttu-id="297b9-112">O exemplo anterior demonstra alguns recursos da sintaxe de correspondência de padrões.</span><span class="sxs-lookup"><span data-stu-id="297b9-112">The preceding sample demonstrates a few features of pattern matching syntax.</span></span> <span data-ttu-id="297b9-113">As instruções `if (a is Mammal m)` e `if (o is Mammal m)` combinam o teste com uma atribuição de inicialização.</span><span class="sxs-lookup"><span data-stu-id="297b9-113">The `if (a is Mammal m)` and `if (o is Mammal m)` statements combine the test with an initialization assignment.</span></span> <span data-ttu-id="297b9-114">A atribuição ocorre apenas quando o teste é bem-sucedido.</span><span class="sxs-lookup"><span data-stu-id="297b9-114">TThe assignment occurs only when the test succeeds.</span></span> <span data-ttu-id="297b9-115">A variável `m` está somente no escopo na instrução `if` inserida em ela foi atribuída.</span><span class="sxs-lookup"><span data-stu-id="297b9-115">The variable `m` is only in scope in the embedded `if` statement where it has been assigned.</span></span> <span data-ttu-id="297b9-116">Não é possível acessar `m` posteriormente no mesmo método.</span><span class="sxs-lookup"><span data-stu-id="297b9-116">You cannot access `m` later in the same method.</span></span> <span data-ttu-id="297b9-117">Experimente isso na janela interativa.</span><span class="sxs-lookup"><span data-stu-id="297b9-117">Try it in the interactive window.</span></span>

<span data-ttu-id="297b9-118">Também é possível usar a mesma sintaxe para testar se um [tipo que permite valor nulo](../programming-guide/nullable-types/index.md) tem um valor, conforme mostrado no código de exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="297b9-118">You can also use the same syntax for testing if a [nullable type](../programming-guide/nullable-types/index.md) has a value, as shown in the following sample code:</span></span>

[!code-csharp-interactive[Pattern matching with nullable types](../../../samples/snippets/csharp/how-to/safelycast/nullablepatternmatching/Program.cs#PatternMatchingNullable)]

<span data-ttu-id="297b9-119">O exemplo anterior demonstra outros recursos da correspondência de padrões a serem usados com conversões.</span><span class="sxs-lookup"><span data-stu-id="297b9-119">The preceding sample demonstrates other features of pattern matching to use with conversions.</span></span> <span data-ttu-id="297b9-120">É possível testar se uma variável tem padrão nulo verificando especificamente o valor `null`.</span><span class="sxs-lookup"><span data-stu-id="297b9-120">You can test a variable for the null pattern by checking specifically for the `null` value.</span></span> <span data-ttu-id="297b9-121">Quando o valor de tempo de execução da variável é `null`, uma instrução `is` que verifica um tipo retorna sempre `false`.</span><span class="sxs-lookup"><span data-stu-id="297b9-121">When the runtime value of the variable is `null`, an `is` statement checking for a type always returns `false`.</span></span> <span data-ttu-id="297b9-122">A instrução `is` da correspondência de padrões não permite um tipo de valor anulável, como `int?` ou `Nullable<int>`, mas é possível testar qualquer outro tipo de valor.</span><span class="sxs-lookup"><span data-stu-id="297b9-122">The pattern matching `is` statement doesn't allow a nullable value type, such as `int?` or `Nullable<int>`, but you can test for any other value type.</span></span>

<span data-ttu-id="297b9-123">O exemplo anterior também mostra como você usa a expressão `is` de correspondência de padrões em uma instrução `switch` em que a variável pode ser um dos muitos tipos diferentes.</span><span class="sxs-lookup"><span data-stu-id="297b9-123">The preceding sample also shows how you use the pattern matching `is` expression in a `switch` statement where the variable may be one of many different types.</span></span>

<span data-ttu-id="297b9-124">Se desejar testar se uma variável é um determinado tipo, mas não a atribuir à nova variável, será possível usar os operadores `is` e `as` para tipos de referência e tipos que permitem valor nulo.</span><span class="sxs-lookup"><span data-stu-id="297b9-124">If you want to test if a variable is a given type, but not assign it to a new variable, you can use the `is` and `as` operators for reference types and nullable types.</span></span> <span data-ttu-id="297b9-125">O seguinte código mostra como usar as instruções `is` e `as` que faziam parte da linguagem C# antes que a correspondência de padrões fosse introduzida para testar se uma variável é de um determinado tipo:</span><span class="sxs-lookup"><span data-stu-id="297b9-125">The following code shows how to use the `is` and `as` statements that were part of the C# language before pattern matching was introduced to test if a variable is of a given type:</span></span>

[!code-csharp-interactive[testing variable types with the is and as statements](../../../samples/snippets/csharp/how-to/safelycast/asandis/Program.cs#IsAndAs)]

<span data-ttu-id="297b9-126">Como você pode ver na comparação desse código com o de correspondência de padrões, a sintaxe de correspondência de padrões oferece recursos mais robustos combinando o teste e a atribuição em uma única instrução.</span><span class="sxs-lookup"><span data-stu-id="297b9-126">As you can see by comparing this code with the pattern matching code, the pattern matching syntax provides more robust features by combining the test and the assignment in a single statement.</span></span> <span data-ttu-id="297b9-127">Use a sintaxe de correspondência de padrões sempre que possível.</span><span class="sxs-lookup"><span data-stu-id="297b9-127">Use the pattern matching syntax whenever possible.</span></span>

<span data-ttu-id="297b9-128">Você pode experimentar estes exemplos examinando o código em nosso [repositório GitHub](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span><span class="sxs-lookup"><span data-stu-id="297b9-128">You can try these samples by looking at the code in our [GitHub repository](https://github.com/dotnet/samples/tree/master/snippets/csharp/how-to/safelycast).</span></span> <span data-ttu-id="297b9-129">Ou então, você pode baixar os exemplos [como um arquivo zip](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span><span class="sxs-lookup"><span data-stu-id="297b9-129">Or you can download the samples [as a zip file](https://github.com/dotnet/samples/raw/master/snippets/csharp/how-to/safelycast.zip).</span></span>
