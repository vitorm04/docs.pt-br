---
description: Aviso do compilador (nível 2) CS0279
title: Aviso do compilador (nível 2) CS0279
ms.date: 07/20/2015
f1_keywords:
- CS0279
helpviewer_keywords:
- CS0279
ms.assetid: 5e5faa8f-3d5b-4999-aa62-ff7f131a3e04
ms.openlocfilehash: d9109d0b8cd95728ef2ebcf27d068300a22519b0
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89143253"
---
# <a name="compiler-warning-level-2-cs0279"></a><span data-ttu-id="fbf9d-103">Aviso do compilador (nível 2) CS0279</span><span class="sxs-lookup"><span data-stu-id="fbf9d-103">Compiler Warning (level 2) CS0279</span></span>
<span data-ttu-id="fbf9d-104">o ' nome do tipo ' não implementa o padrão ' nome do padrão '.</span><span class="sxs-lookup"><span data-stu-id="fbf9d-104">'type name' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="fbf9d-105">o ' nome do método ' é estático ou não é público.</span><span class="sxs-lookup"><span data-stu-id="fbf9d-105">'method name' is either static or not public.</span></span>  
  
 <span data-ttu-id="fbf9d-106">Há várias instruções em C# que dependem de padrões definidos, como `foreach` e `using` .</span><span class="sxs-lookup"><span data-stu-id="fbf9d-106">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="fbf9d-107">Por exemplo, `foreach` depende da classe de coleção que implementa o padrão enumerável.</span><span class="sxs-lookup"><span data-stu-id="fbf9d-107">For example, `foreach` relies on the collection class implementing the enumerable pattern.</span></span> <span data-ttu-id="fbf9d-108">Esse erro ocorre quando o compilador não consegue fazer a correspondência devido a um método que está sendo declarado `static` ou não `public` .</span><span class="sxs-lookup"><span data-stu-id="fbf9d-108">This error occurs when the compiler is unable to make the match due to a method being declared `static` or not `public`.</span></span> <span data-ttu-id="fbf9d-109">Os métodos em padrões são necessários para serem instâncias de classes e para serem públicos.</span><span class="sxs-lookup"><span data-stu-id="fbf9d-109">Methods in patterns are required to be instances of classes, and to be public.</span></span>  
  
## <a name="example"></a><span data-ttu-id="fbf9d-110">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fbf9d-110">Example</span></span>  
 <span data-ttu-id="fbf9d-111">O exemplo a seguir gera CS0279:</span><span class="sxs-lookup"><span data-stu-id="fbf9d-111">The following example generates CS0279:</span></span>  
  
```csharp  
// CS0279.cs  
  
using System;  
using System.Collections;  
  
public class myTest : IEnumerable  
{  
    IEnumerator IEnumerable.GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    internal IEnumerator GetEnumerator()  
    {  
        yield return 0;  
    }  
  
    public static void Main()  
    {  
        foreach (int i in new myTest()) {}  // CS0279  
    }  
}  
```
