---
description: Aviso do compilador (nível 2) CS0280
title: Aviso do compilador (nível 2) CS0280
ms.date: 07/20/2015
f1_keywords:
- CS0280
helpviewer_keywords:
- CS0280
ms.assetid: 9b453478-92aa-4fd2-9b87-780fd138603a
ms.openlocfilehash: 1ee2857514a7c653d40ca47d1544daff1820b99a
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89140640"
---
# <a name="compiler-warning-level-2-cs0280"></a><span data-ttu-id="a883b-103">Aviso do compilador (nível 2) CS0280</span><span class="sxs-lookup"><span data-stu-id="a883b-103">Compiler Warning (level 2) CS0280</span></span>
<span data-ttu-id="a883b-104">' type ' não implementa o padrão ' Pattern Name '.</span><span class="sxs-lookup"><span data-stu-id="a883b-104">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="a883b-105">' Method Name ' tem a assinatura incorreta.</span><span class="sxs-lookup"><span data-stu-id="a883b-105">'method name' has the wrong signature.</span></span>  
  
 <span data-ttu-id="a883b-106">Duas instruções em C#, **foreach** e **using**, dependem de padrões predefinidos, "Collection" e "Resource", respectivamente.</span><span class="sxs-lookup"><span data-stu-id="a883b-106">Two statements in C#, **foreach** and **using**, rely on predefined patterns, "collection" and "resource" respectively.</span></span> <span data-ttu-id="a883b-107">Esse aviso ocorre quando o compilador não pode corresponder a uma dessas instruções ao seu padrão devido à assinatura incorreta de um método.</span><span class="sxs-lookup"><span data-stu-id="a883b-107">This warning occurs when the compiler cannot match one of these statements to its pattern due to a method's incorrect signature.</span></span> <span data-ttu-id="a883b-108">Por exemplo, o padrão de "coleção" requer que haja um método chamado <xref:System.Collections.IEnumerator.MoveNext%2A> que não usa nenhum parâmetro e retorna um `boolean` .</span><span class="sxs-lookup"><span data-stu-id="a883b-108">For example, the "collection" pattern requires that there be a method called <xref:System.Collections.IEnumerator.MoveNext%2A> which takes no parameters and returns a `boolean`.</span></span> <span data-ttu-id="a883b-109">Seu código pode conter um <xref:System.Collections.IEnumerator.MoveNext%2A> método que tem um parâmetro ou, talvez, retorna um objeto.</span><span class="sxs-lookup"><span data-stu-id="a883b-109">Your code might contain a <xref:System.Collections.IEnumerator.MoveNext%2A> method that has a parameter or perhaps returns an object.</span></span>  
  
 <span data-ttu-id="a883b-110">O padrão de "recurso" e `using` fornece outro exemplo.</span><span class="sxs-lookup"><span data-stu-id="a883b-110">The "resource" pattern and `using` provide another example.</span></span> <span data-ttu-id="a883b-111">O padrão "Resource" requer o <xref:System.IDisposable.Dispose%2A> método; se você definir uma propriedade com o mesmo nome, receberá esse aviso.</span><span class="sxs-lookup"><span data-stu-id="a883b-111">The "resource" pattern requires the <xref:System.IDisposable.Dispose%2A> method; if you define a property with the same name, you will get this warning.</span></span>  
  
 <span data-ttu-id="a883b-112">Para resolver esse aviso, verifique se as assinaturas de método no seu tipo correspondem às assinaturas dos métodos correspondentes no padrão e certifique-se de que você não tem propriedades com o mesmo nome que um método exigido pelo padrão.</span><span class="sxs-lookup"><span data-stu-id="a883b-112">To resolve this warning, ensure that the method signatures in your type match the signatures of the corresponding methods in the pattern, and ensure that you have no properties with the same name as a method required by the pattern.</span></span>  
  
## <a name="example"></a><span data-ttu-id="a883b-113">Exemplo</span><span class="sxs-lookup"><span data-stu-id="a883b-113">Example</span></span>  
 <span data-ttu-id="a883b-114">O exemplo a seguir gera CS0280.</span><span class="sxs-lookup"><span data-stu-id="a883b-114">The following sample generates CS0280.</span></span>  
  
```csharp  
// CS0280.cs  
using System;  
using System.Collections;  
  
public class ValidBase: IEnumerable  
{  
   IEnumerator IEnumerable.GetEnumerator()  
   {  
      yield return 0;  
   }  
  
   internal IEnumerator GetEnumerator()  
   {  
      yield return 0;  
   }  
}  
  
class Derived : ValidBase  
{  
   // field, not method  
   new public int GetEnumerator;  
}  
  
public class Test  
{  
   public static void Main()  
   {  
      foreach (int i in new Derived()) {}   // CS0280  
   }  
}  
```
