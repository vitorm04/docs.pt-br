---
description: Aviso do compilador (nível 2) CS0728
title: Aviso do compilador (nível 2) CS0728
ms.date: 07/20/2015
f1_keywords:
- CS0728
helpviewer_keywords:
- CS0728
ms.assetid: ad6d860d-bac4-48f3-9eab-1efd2b6de6c0
ms.openlocfilehash: 515ff469d580766eb54ff369e77fba941b4ae5cc
ms.sourcegitcommit: 5b475c1855b32cf78d2d1bbb4295e4c236f39464
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/24/2020
ms.locfileid: "91195942"
---
# <a name="compiler-warning-level-2-cs0728"></a><span data-ttu-id="5989c-103">Aviso do compilador (nível 2) CS0728</span><span class="sxs-lookup"><span data-stu-id="5989c-103">Compiler Warning (level 2) CS0728</span></span>

<span data-ttu-id="5989c-104">Atribuição possivelmente incorreta a ' variável ' local, que é o argumento para uma instrução using ou Lock.</span><span class="sxs-lookup"><span data-stu-id="5989c-104">Possibly incorrect assignment to local 'variable' which is the argument to a using or lock statement.</span></span>  <span data-ttu-id="5989c-105">A chamada Dispose ou o desbloqueio ocorrerá no valor original do local.</span><span class="sxs-lookup"><span data-stu-id="5989c-105">The Dispose call or unlocking will happen on the original value of the local.</span></span>  
  
 <span data-ttu-id="5989c-106">Há vários cenários em que `using` ou `lock` blocos resultarão em um vazamento temporário de recursos.</span><span class="sxs-lookup"><span data-stu-id="5989c-106">There are several scenarios where `using` or `lock` blocks will result in a temporary leak of resources.</span></span> <span data-ttu-id="5989c-107">Aqui está um exemplo:</span><span class="sxs-lookup"><span data-stu-id="5989c-107">Here is one example:</span></span>  
  
 `thisType f = null;`  
  
 `using (f)`  
  
 `{`  
  
 `f = new thisType();`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="5989c-108">Nesse caso, o valor original, como NULL, da variável `thisType` será Descartado quando o `using` bloco terminar a execução, mas o `thisType` objeto criado dentro do bloco não será, embora, eventualmente, obtenha o lixo coletado.</span><span class="sxs-lookup"><span data-stu-id="5989c-108">In this case, the original value, such as null, of the variable `thisType` will be disposed of when the `using` block finishes executing, but the `thisType` object created inside the block will not be, although it will eventually get garbage collected.</span></span>  
  
 <span data-ttu-id="5989c-109">Para resolver esse erro, use o seguinte formato:</span><span class="sxs-lookup"><span data-stu-id="5989c-109">To resolve this error, use the following form:</span></span>  
  
 `using (thisType f = new thisType())`  
  
 `{`  
  
 `...`  
  
 `}`  
  
 <span data-ttu-id="5989c-110">Nesse caso, o objeto recém alocado `thisType` será descartado de.</span><span class="sxs-lookup"><span data-stu-id="5989c-110">In this case, the newly allocated `thisType` object will be disposed of.</span></span>  
  
## <a name="example"></a><span data-ttu-id="5989c-111">Exemplo</span><span class="sxs-lookup"><span data-stu-id="5989c-111">Example</span></span>  

 <span data-ttu-id="5989c-112">O código a seguir irá gerar o aviso CS0728.</span><span class="sxs-lookup"><span data-stu-id="5989c-112">The following code will generate warning CS0728.</span></span>  
  
```csharp
// CS0728.cs  
  
using System;  
public class ValidBase : IDisposable  
{  
    public void Dispose() {  }  
}  
  
public class Logger  
{  
    public static void dummy()  
    {  
        ValidBase vb = null;  
        using (vb)
        {  
            vb = null;  // CS0728  
        }  
        vb = null;  
    }  
    public static void Main() { }  
}  
```
