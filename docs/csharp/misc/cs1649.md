---
title: Erro do compilador CS1649
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: da00bfb56571bc331b8e31bf65dfe9090d258bc4
ms.sourcegitcommit: c93fd5139f9efcf6db514e3474301738a6d1d649
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/28/2018
ms.locfileid: "56797050"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="4d2a5-102">Erro do compilador CS1649</span><span class="sxs-lookup"><span data-stu-id="4d2a5-102">Compiler Error CS1649</span></span>
<span data-ttu-id="4d2a5-103">Os membros somente leitura campo 'identifier' não podem ser passados como ref ou out (exceto em um construtor)</span><span class="sxs-lookup"><span data-stu-id="4d2a5-103">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="4d2a5-104">Esse erro ocorre se você passar uma variável para uma função que é um membro de um `readonly` campo como um `ref` ou `out` argumento.</span><span class="sxs-lookup"><span data-stu-id="4d2a5-104">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="4d2a5-105">Uma vez que `ref` e `out` parâmetros podem ser modificados pela função, isso não é permitido.</span><span class="sxs-lookup"><span data-stu-id="4d2a5-105">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="4d2a5-106">Para resolver esse erro, remova os `readonly` palavra-chave no campo, ou simplesmente não passe os membros do `readonly` campo para a função.</span><span class="sxs-lookup"><span data-stu-id="4d2a5-106">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="4d2a5-107">Por exemplo, você pode tentar criar uma variável temporária que pode ser modificada e passando temporárias como um `ref` argumento, conforme mostrado no exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="4d2a5-107">For example, you might try creating a temporary variable which can be modified and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="4d2a5-108">Exemplo</span><span class="sxs-lookup"><span data-stu-id="4d2a5-108">Example</span></span>  
 <span data-ttu-id="4d2a5-109">O exemplo a seguir gera CS1649:</span><span class="sxs-lookup"><span data-stu-id="4d2a5-109">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();   
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
