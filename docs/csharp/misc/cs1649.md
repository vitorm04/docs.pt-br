---
description: Erro do Compilador CS1649
title: Erro do Compilador CS1649
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: 5ae60aa8dea44f84bc1f18d25240a32329b8143c
ms.sourcegitcommit: d579fb5e4b46745fd0f1f8874c94c6469ce58604
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/30/2020
ms.locfileid: "89118761"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="907b4-103">Erro do Compilador CS1649</span><span class="sxs-lookup"><span data-stu-id="907b4-103">Compiler Error CS1649</span></span>
<span data-ttu-id="907b4-104">Os membros do campo somente leitura ' identifier ' não podem ser passados como ref ou out (exceto em um construtor)</span><span class="sxs-lookup"><span data-stu-id="907b4-104">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="907b4-105">Esse erro ocorrerá se você passar uma variável para uma função que seja membro de um `readonly` campo como um `ref` `out` argumento ou.</span><span class="sxs-lookup"><span data-stu-id="907b4-105">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="907b4-106">Como `ref` `out` os parâmetros e podem ser modificados pela função, isso não é permitido.</span><span class="sxs-lookup"><span data-stu-id="907b4-106">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="907b4-107">Para resolver esse erro, remova a `readonly` palavra-chave no campo ou não passe os membros do `readonly` campo para a função.</span><span class="sxs-lookup"><span data-stu-id="907b4-107">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="907b4-108">Por exemplo, você pode tentar criar uma variável temporária que pode ser modificada e passando o temporário como um `ref` argumento, conforme mostrado no exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="907b4-108">For example, you might try creating a temporary variable which can be modified and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="907b4-109">Exemplo</span><span class="sxs-lookup"><span data-stu-id="907b4-109">Example</span></span>  
 <span data-ttu-id="907b4-110">O exemplo a seguir gera CS1649:</span><span class="sxs-lookup"><span data-stu-id="907b4-110">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
