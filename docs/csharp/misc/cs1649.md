---
title: Erro do Compilador CS1649
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: ec4ca26890fa9a96f8e1bc220650da73d1ac7ded
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79171145"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="78f82-102">Erro do Compilador CS1649</span><span class="sxs-lookup"><span data-stu-id="78f82-102">Compiler Error CS1649</span></span>
<span data-ttu-id="78f82-103">Os membros do 'identificador' do campo readonly não podem ser aprovados como árbitros ou eliminados (exceto em um construtor)</span><span class="sxs-lookup"><span data-stu-id="78f82-103">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="78f82-104">Esse erro ocorre se você passar uma variável para `readonly` uma `ref` função `out` que é um membro de um campo como um ou argumento.</span><span class="sxs-lookup"><span data-stu-id="78f82-104">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="78f82-105">Uma `ref` `out` vez que e parâmetros podem ser modificados pela função, isso não é permitido.</span><span class="sxs-lookup"><span data-stu-id="78f82-105">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="78f82-106">Para resolver este erro, remova a `readonly` palavra-chave no campo `readonly` ou não passe os membros do campo para a função.</span><span class="sxs-lookup"><span data-stu-id="78f82-106">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="78f82-107">Por exemplo, você pode tentar criar uma variável temporária que `ref` pode ser modificada e passar o temporário como argumento, como mostrado no exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="78f82-107">For example, you might try creating a temporary variable which can be modified and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="78f82-108">Exemplo</span><span class="sxs-lookup"><span data-stu-id="78f82-108">Example</span></span>  
 <span data-ttu-id="78f82-109">A seguinte amostra gera CS1649:</span><span class="sxs-lookup"><span data-stu-id="78f82-109">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
