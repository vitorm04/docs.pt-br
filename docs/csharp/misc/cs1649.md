---
title: CS1649 de erro do compilador
ms.date: 07/20/2015
f1_keywords:
- CS1649
helpviewer_keywords:
- CS1649
ms.assetid: 6355c7f2-157c-441d-8925-500062988636
ms.openlocfilehash: 3d8952c99781194f33ee316d5fdd18584b11d07c
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/04/2018
ms.locfileid: "33308913"
---
# <a name="compiler-error-cs1649"></a><span data-ttu-id="84d6e-102">CS1649 de erro do compilador</span><span class="sxs-lookup"><span data-stu-id="84d6e-102">Compiler Error CS1649</span></span>
<span data-ttu-id="84d6e-103">Membros de somente leitura 'Identificador de campo' não podem ser passados como ref ou out (exceto em um construtor)</span><span class="sxs-lookup"><span data-stu-id="84d6e-103">Members of readonly field 'identifier' cannot be passed ref or out (except in a constructor)</span></span>  
  
 <span data-ttu-id="84d6e-104">Esse erro ocorre se você passar uma variável para uma função que é um membro de um `readonly` campo como um `ref` ou `out` argumento.</span><span class="sxs-lookup"><span data-stu-id="84d6e-104">This error occurs if you pass a variable to a function that is a member of a `readonly` field as a `ref` or `out` argument.</span></span> <span data-ttu-id="84d6e-105">Como `ref` e `out` parâmetros podem ser modificados pela função, isso não é permitido.</span><span class="sxs-lookup"><span data-stu-id="84d6e-105">Since `ref` and `out` parameters may be modified by the function, this is not allowed.</span></span> <span data-ttu-id="84d6e-106">Para resolver esse erro, remova o `readonly` palavra-chave no campo, ou não passam os membros do `readonly` campo para a função.</span><span class="sxs-lookup"><span data-stu-id="84d6e-106">To resolve this error, remove the `readonly` keyword on the field, or do not pass the members of the `readonly` field to the function.</span></span> <span data-ttu-id="84d6e-107">Por exemplo, você pode tentar criar um temporário variável que pode ser modificado e passando temporário como um `ref` argumento, conforme mostrado no exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="84d6e-107">For example, you might try creating a temporary variable which can be modifed and passing the temporary as a `ref` argument, as shown in the following example.</span></span>  
  
## <a name="example"></a><span data-ttu-id="84d6e-108">Exemplo</span><span class="sxs-lookup"><span data-stu-id="84d6e-108">Example</span></span>  
 <span data-ttu-id="84d6e-109">O exemplo a seguir gera CS1649:</span><span class="sxs-lookup"><span data-stu-id="84d6e-109">The following sample generates CS1649:</span></span>  
  
```csharp  
// CS1649.cs  
public struct Inner  
    {  
        public int i;  
    }  
  
class Outer  
{  
    public readonly Inner inner = new Inner();  
}  
  
class D  
{  
    static void f(ref int iref)  
    {  
    }  
  
    static void Main()  
    {  
        Outer outer = new Outer();   
        f(ref outer.inner.i);  // CS1649  
        // Try this code instead:  
        // int tmp = outer.inner.i;  
        // f(ref tmp);  
    }  
}  
```
