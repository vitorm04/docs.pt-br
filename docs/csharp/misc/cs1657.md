---
title: Erro do compilador CS1657
ms.date: 07/20/2015
f1_keywords:
- CS1657
helpviewer_keywords:
- CS1657
ms.assetid: 6f0aeebe-5c90-4d5b-981c-1795d2e8fbb9
ms.openlocfilehash: bc1b324603089865a057d60a9fac751b4acb44e1
ms.sourcegitcommit: 986f836f72ef10876878bd6217174e41464c145a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/19/2019
ms.locfileid: "69610841"
---
# <a name="compiler-error-cs1657"></a><span data-ttu-id="5e8dd-102">Erro do compilador CS1657</span><span class="sxs-lookup"><span data-stu-id="5e8dd-102">Compiler Error CS1657</span></span>
<span data-ttu-id="5e8dd-103">Não é possível passar ' Parameter ' como um argumento ref ou out porque ' Reason ' '</span><span class="sxs-lookup"><span data-stu-id="5e8dd-103">Cannot pass 'parameter' as a ref or out argument because 'reason''</span></span>  
  
 <span data-ttu-id="5e8dd-104">Esse erro ocorre quando uma variável é passada como um argumento [ref](../language-reference/keywords/ref.md) ou [out](../language-reference/keywords/out-parameter-modifier.md) em um contexto no qual essa variável é ReadOnly.</span><span class="sxs-lookup"><span data-stu-id="5e8dd-104">This error occurs when a variable is passed as a [ref](../language-reference/keywords/ref.md) or [out](../language-reference/keywords/out-parameter-modifier.md) argument in a context in which that variable is readonly.</span></span> <span data-ttu-id="5e8dd-105">Contextos ReadOnly incluem variáveis de iteração [foreach](../language-reference/keywords/foreach-in.md) , [usando](../language-reference/keywords/using-statement.md) variáveis `fixed` e variáveis.</span><span class="sxs-lookup"><span data-stu-id="5e8dd-105">Readonly contexts include [foreach](../language-reference/keywords/foreach-in.md) iteration variables, [using](../language-reference/keywords/using-statement.md) variables, and `fixed` variables.</span></span> <span data-ttu-id="5e8dd-106">Para resolver `foreach`esse erro, não chame funções que usam a variável, `using` ou `fixed` como um `ref` parâmetro ou `out` em `using` blocos, `foreach` instruções e `fixed` instruções.</span><span class="sxs-lookup"><span data-stu-id="5e8dd-106">To resolve this error, do not call functions that take the `foreach`, `using` or `fixed` variable as a `ref` or `out` parameter in `using` blocks, `foreach` statements, and `fixed` statements.</span></span>  
  
## <a name="example"></a><span data-ttu-id="5e8dd-107">Exemplo</span><span class="sxs-lookup"><span data-stu-id="5e8dd-107">Example</span></span>  
 <span data-ttu-id="5e8dd-108">O exemplo a seguir gera CS1657:</span><span class="sxs-lookup"><span data-stu-id="5e8dd-108">The following sample generates CS1657:</span></span>  
  
```csharp  
// CS1657.cs  
using System;  
class C : IDisposable  
{  
    public int i;  
    public void Dispose() {}  
}  
  
class CMain  
{  
    static void f(ref C c)  
    {  
    }  
    static void Main()  
    {  
        using (C c = new C())  
        {  
            f(ref c);  // CS1657  
        }  
    }  
}  
```  
  
## <a name="example"></a><span data-ttu-id="5e8dd-109">Exemplo</span><span class="sxs-lookup"><span data-stu-id="5e8dd-109">Example</span></span>  
 <span data-ttu-id="5e8dd-110">O código a seguir ilustra o mesmo problema em `fixed` uma instrução:</span><span class="sxs-lookup"><span data-stu-id="5e8dd-110">The following code illustrates the same problem in a `fixed` statement:</span></span>  
  
```csharp  
// CS1657b.cs  
// compile with: /unsafe  
unsafe class C  
{  
    static void F(ref int* p)  
    {  
    }  
  
    static void Main()  
    {  
        int[] a = new int[5];  
        fixed(int* p = a) F(ref p); // CS1657  
    }  
}  
```
