---
title: Compilador aviso (nível 2) CS0278
ms.date: 07/20/2015
f1_keywords:
- CS0278
helpviewer_keywords:
- CS0278
ms.assetid: 5418cbbe-bcec-4379-a6f6-410987beb96a
ms.openlocfilehash: 48f8228520d02d7f2000d3d0b508b1b48c38c37e
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61658078"
---
# <a name="compiler-warning-level-2-cs0278"></a><span data-ttu-id="457ec-102">Compilador aviso (nível 2) CS0278</span><span class="sxs-lookup"><span data-stu-id="457ec-102">Compiler Warning (level 2) CS0278</span></span>
<span data-ttu-id="457ec-103">'type' não implementa o padrão de nome do padrão.</span><span class="sxs-lookup"><span data-stu-id="457ec-103">'type' does not implement the 'pattern name' pattern.</span></span> <span data-ttu-id="457ec-104">'nome do método' é ambíguo com o nome do método.</span><span class="sxs-lookup"><span data-stu-id="457ec-104">'method name' is ambiguous with 'method name'.</span></span>  
  
<span data-ttu-id="457ec-105">Há várias instruções em C# que dependem de padrões definidos, como `foreach` e `using`.</span><span class="sxs-lookup"><span data-stu-id="457ec-105">There are several statements in C# that rely on defined patterns, such as `foreach` and `using`.</span></span> <span data-ttu-id="457ec-106">Por exemplo, o [ `foreach` instrução](../language-reference/keywords/foreach-in.md) depende da classe de coleção Implementando o padrão de "enumerável".</span><span class="sxs-lookup"><span data-stu-id="457ec-106">For example, the [`foreach` statement](../language-reference/keywords/foreach-in.md) relies on the collection class implementing the "enumerable" pattern.</span></span>
  
<span data-ttu-id="457ec-107">CS0278 pode ocorrer se o compilador não puder fazer a correspondência devido a ambiguidades.</span><span class="sxs-lookup"><span data-stu-id="457ec-107">CS0278 can occur if the compiler is unable to make the match due to ambiguities.</span></span> <span data-ttu-id="457ec-108">Por exemplo, o padrão "enumerável" requer que haja um método chamado `MoveNext`, e seu código pode conter dois métodos chamados `MoveNext`.</span><span class="sxs-lookup"><span data-stu-id="457ec-108">For example, the "enumerable" pattern requires that there be a method called `MoveNext`, and your code might contain two methods called `MoveNext`.</span></span> <span data-ttu-id="457ec-109">O compilador tentará encontrar uma interface para usar, mas é recomendável que você determine e resolva a causa da ambiguidade.</span><span class="sxs-lookup"><span data-stu-id="457ec-109">The compiler will attempt to find an interface to use, but it is recommended that you determine and resolve the cause of the ambiguity.</span></span>  
  
## <a name="example"></a><span data-ttu-id="457ec-110">Exemplo</span><span class="sxs-lookup"><span data-stu-id="457ec-110">Example</span></span>  
 <span data-ttu-id="457ec-111">O exemplo a seguir gera CS0278.</span><span class="sxs-lookup"><span data-stu-id="457ec-111">The following sample generates CS0278.</span></span>  
  
```csharp  
// CS0278.cs  
using System.Collections.Generic;  
public class myTest   
{  
   public static void TestForeach<W>(W w)   
      where W: IEnumerable<int>, IEnumerable<string>  
   {  
      foreach (int i in w) {}   // CS0278  
   }  
}  
```
