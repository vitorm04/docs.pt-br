---
title: Erro do compilador CS1937
ms.date: 07/20/2015
f1_keywords:
- CS1937
helpviewer_keywords:
- CS1937
ms.assetid: f13e8dc9-8c20-477e-8b74-7192848e08a2
ms.openlocfilehash: ede51099c5c574f932f1a55d02dc21e959f1e86d
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/09/2019
ms.locfileid: "59327276"
---
# <a name="compiler-error-cs1937"></a><span data-ttu-id="78051-102">Erro do compilador CS1937</span><span class="sxs-lookup"><span data-stu-id="78051-102">Compiler Error CS1937</span></span>
<span data-ttu-id="78051-103">O nome 'name' não está no escopo à esquerda de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="78051-103">The name 'name' is not in scope on the left side of 'equals'.</span></span> <span data-ttu-id="78051-104">Considere trocar as expressões em ambos os lados de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="78051-104">Consider swapping the expressions on either side of 'equals'.</span></span>  
  
 <span data-ttu-id="78051-105">O `equals` palavra-chave é um operador especial que é usado em um `join` cláusula para determinar a igualdade entre duas expressões.</span><span class="sxs-lookup"><span data-stu-id="78051-105">The `equals` keyword is a special operator that is used in a `join` clause to determine equality between two expressions.</span></span> <span data-ttu-id="78051-106">A variável de intervalo para a sequência de origem do lado esquerdo está no escopo no lado esquerdo de equals e a variável de intervalo para a fonte do lado direito é somente no escopo no lado esquerdo de equals.</span><span class="sxs-lookup"><span data-stu-id="78051-106">The range variable for the left side source sequence is in scope on the left side of equals, and the range variable for the right side source is only in scope on the left side of equals.</span></span> <span data-ttu-id="78051-107">Você pode verificar isso por meio da experimentação com o IntelliSense no exemplo de código a seguir.</span><span class="sxs-lookup"><span data-stu-id="78051-107">You can verify this by experimenting with IntelliSense in the following code example.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="78051-108">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="78051-108">To correct this error</span></span>  
  
1. <span data-ttu-id="78051-109">Troque a posição das variáveis de intervalo de dois, como mostra a linha comentada no exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="78051-109">Swap the position of the two range variables as shown in the commented line in the following example:</span></span>  
  
## <a name="example"></a><span data-ttu-id="78051-110">Exemplo</span><span class="sxs-lookup"><span data-stu-id="78051-110">Example</span></span>  
 <span data-ttu-id="78051-111">O exemplo a seguir gera CS1937.</span><span class="sxs-lookup"><span data-stu-id="78051-111">The following example generates CS1937.</span></span>  
  
```csharp  
// cs1937.cs  
using System.Linq;  
class Test  
{  
    static void Main()  
    {  
        int[] sourceA = { 1, 2, 3, 4, 5 };  
        int[] sourceB = { 3, 4, 5, 6, 7 };  
  
        var query = from a in sourceA  
                    join b in sourceB on b equals a // CS1937  
                    // Try the following line instead.  
                    //join b in sourceB on a equals b  
                    select new { a, b };  
    }  
}  
```  
  
 <span data-ttu-id="78051-112">O lado esquerdo é geralmente chamado de lado "externo" e à direita é geralmente chamada de lado "interno".</span><span class="sxs-lookup"><span data-stu-id="78051-112">The left side is generally called the "outer" side and the right is generally called the "inner" side.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="78051-113">Consulte também</span><span class="sxs-lookup"><span data-stu-id="78051-113">See also</span></span>

- [<span data-ttu-id="78051-114">Cláusula join</span><span class="sxs-lookup"><span data-stu-id="78051-114">join clause</span></span>](../../csharp/language-reference/keywords/join-clause.md)
