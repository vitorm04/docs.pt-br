---
title: Erro do compilador CS1938
ms.date: 07/20/2015
f1_keywords:
- CS1938
helpviewer_keywords:
- CS1938
ms.assetid: fc8de996-f7a1-46e8-b07b-aea520b391b9
ms.openlocfilehash: f39cd5e67ee2e75f55abd59cce5d39b1721fb8ea
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61684569"
---
# <a name="compiler-error-cs1938"></a><span data-ttu-id="c0759-102">Erro do compilador CS1938</span><span class="sxs-lookup"><span data-stu-id="c0759-102">Compiler Error CS1938</span></span>
<span data-ttu-id="c0759-103">O nome 'name' não está no escopo à direita de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="c0759-103">The name 'name' is not in scope on the right side of 'equals'.</span></span> <span data-ttu-id="c0759-104">Considere trocar as expressões em ambos os lados de 'equals'.</span><span class="sxs-lookup"><span data-stu-id="c0759-104">Consider swapping the expressions on either side of 'equals'.</span></span>  
  
 <span data-ttu-id="c0759-105">O `equals` palavra-chave é um operador especial que é usado em um `join` cláusula para determinar a igualdade entre duas expressões.</span><span class="sxs-lookup"><span data-stu-id="c0759-105">The `equals` keyword is a special operator that is used in a `join` clause to determine equality between two expressions.</span></span> <span data-ttu-id="c0759-106">A variável de intervalo para a sequência de origem do lado esquerdo está no escopo no lado esquerdo de equals e a variável de intervalo para a fonte do lado direito é somente no escopo no lado esquerdo de equals.</span><span class="sxs-lookup"><span data-stu-id="c0759-106">The range variable for the left side source sequence is in scope on the left side of equals, and the range variable for the right side source is only in scope on the left side of equals.</span></span> <span data-ttu-id="c0759-107">Você pode verificar isso por meio da experimentação com o IntelliSense no exemplo de código a seguir.</span><span class="sxs-lookup"><span data-stu-id="c0759-107">You can verify this by experimenting with IntelliSense in the following code example.</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c0759-108">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="c0759-108">To correct this error</span></span>  
  
1. <span data-ttu-id="c0759-109">Troque a posição das variáveis de intervalo de dois, como mostra a linha comentada no exemplo a seguir:</span><span class="sxs-lookup"><span data-stu-id="c0759-109">Swap the position of the two range variables as shown in the commented line in the following example:</span></span>  
  
## <a name="example"></a><span data-ttu-id="c0759-110">Exemplo</span><span class="sxs-lookup"><span data-stu-id="c0759-110">Example</span></span>  
 <span data-ttu-id="c0759-111">O código a seguir gera CS1938:</span><span class="sxs-lookup"><span data-stu-id="c0759-111">The following code generates CS1938:</span></span>  
  
```csharp  
// cs1938.cs  
using System.Linq;  
class Test  
{  
    static void Main()  
    {  
        int[] sourceA = { 1, 2, 3, 4, 5 };  
        int[] sourceB = { 3, 4, 5, 6, 7 };  
  
        var query = from a in sourceA  
                    join b in sourceB on b equals a // CS1938  
                    // Try the following line instead.  
                    // join b in sourceB on a equals b  
                    select new { a, b };  
    }  
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="c0759-112">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c0759-112">See also</span></span>

- [<span data-ttu-id="c0759-113">Cláusula join</span><span class="sxs-lookup"><span data-stu-id="c0759-113">join clause</span></span>](../../csharp/language-reference/keywords/join-clause.md)
