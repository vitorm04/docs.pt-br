---
title: Operador If (Visual Basic)
ms.date: 07/20/2015
f1_keywords:
- vb.IfOperator
- IfOperator
helpviewer_keywords:
- ternary operators [Visual Basic]
- conditional execution
- If expressions [Visual Basic]
- conditional operator [Visual Basic]
- If Operator [Visual Basic]
ms.assetid: dd56c9df-7cd4-442c-9ba6-20c70ee44c8f
ms.openlocfilehash: 9ab01755d75c91ce87acf83e7f406b26c466aef6
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61663499"
---
# <a name="if-operator-visual-basic"></a><span data-ttu-id="d717f-102">Operador If (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="d717f-102">If Operator (Visual Basic)</span></span>
<span data-ttu-id="d717f-103">Usos de curto-circuito avaliação para retornar condicionalmente um dos dois valores.</span><span class="sxs-lookup"><span data-stu-id="d717f-103">Uses short-circuit evaluation to conditionally return one of two values.</span></span> <span data-ttu-id="d717f-104">O `If` operador pode ser chamado com três argumentos ou com dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="d717f-104">The `If` operator can be called with three arguments or with two arguments.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="d717f-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="d717f-105">Syntax</span></span>  
  
```  
If( [argument1,] argument2, argument3 )  
```  
  
## <a name="if-operator-called-with-three-arguments"></a><span data-ttu-id="d717f-106">Se o operador de chamada com três argumentos</span><span class="sxs-lookup"><span data-stu-id="d717f-106">If Operator Called with Three Arguments</span></span>  
 <span data-ttu-id="d717f-107">Quando `If` é chamado usando três argumentos, o primeiro argumento deve ser avaliada como um valor que pode ser convertido como um `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="d717f-107">When `If` is called by using three arguments, the first argument must evaluate to a value that can be cast as a `Boolean`.</span></span> <span data-ttu-id="d717f-108">Que `Boolean` valor determinará qual dos outros dois argumentos é avaliado e retornado.</span><span class="sxs-lookup"><span data-stu-id="d717f-108">That `Boolean` value will determine which of the other two arguments is evaluated and returned.</span></span> <span data-ttu-id="d717f-109">A lista a seguir se aplica somente quando o `If` operador é chamado usando três argumentos.</span><span class="sxs-lookup"><span data-stu-id="d717f-109">The following list applies only when the `If` operator is called by using three arguments.</span></span>  
  
## <a name="parts"></a><span data-ttu-id="d717f-110">Partes</span><span class="sxs-lookup"><span data-stu-id="d717f-110">Parts</span></span>  
  
|<span data-ttu-id="d717f-111">Termo</span><span class="sxs-lookup"><span data-stu-id="d717f-111">Term</span></span>|<span data-ttu-id="d717f-112">Definição</span><span class="sxs-lookup"><span data-stu-id="d717f-112">Definition</span></span>|  
|---|---|  
|`argument1`|<span data-ttu-id="d717f-113">Necessário.</span><span class="sxs-lookup"><span data-stu-id="d717f-113">Required.</span></span> <span data-ttu-id="d717f-114">`Boolean`.</span><span class="sxs-lookup"><span data-stu-id="d717f-114">`Boolean`.</span></span> <span data-ttu-id="d717f-115">Determina qual dos outros argumentos para avaliar e retornar.</span><span class="sxs-lookup"><span data-stu-id="d717f-115">Determines which of the other arguments to evaluate and return.</span></span>|  
|`argument2`|<span data-ttu-id="d717f-116">Necessário.</span><span class="sxs-lookup"><span data-stu-id="d717f-116">Required.</span></span> <span data-ttu-id="d717f-117">`Object`.</span><span class="sxs-lookup"><span data-stu-id="d717f-117">`Object`.</span></span> <span data-ttu-id="d717f-118">Avaliado e retornado se `argument1` for avaliada como `True`.</span><span class="sxs-lookup"><span data-stu-id="d717f-118">Evaluated and returned if `argument1` evaluates to `True`.</span></span>|  
|`argument3`|<span data-ttu-id="d717f-119">Necessário.</span><span class="sxs-lookup"><span data-stu-id="d717f-119">Required.</span></span> <span data-ttu-id="d717f-120">`Object`.</span><span class="sxs-lookup"><span data-stu-id="d717f-120">`Object`.</span></span> <span data-ttu-id="d717f-121">Avaliado e retornado se `argument1` for avaliada como `False` ou se `argument1` é uma [Nullable](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md) `Boolean` variável que é avaliada como [nada](../../../visual-basic/language-reference/nothing.md).</span><span class="sxs-lookup"><span data-stu-id="d717f-121">Evaluated and returned if `argument1` evaluates to `False` or if `argument1` is a [Nullable](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md)`Boolean` variable that evaluates to [Nothing](../../../visual-basic/language-reference/nothing.md).</span></span>|  
  
 <span data-ttu-id="d717f-122">Uma `If` operador que é chamado com três argumentos funciona como um `IIf` função, exceto pelo fato de que ele usa avaliação curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="d717f-122">An `If` operator that is called with three arguments works like an `IIf` function except that it uses short-circuit evaluation.</span></span> <span data-ttu-id="d717f-123">Uma `IIf` função sempre avalia todas as três de seus argumentos, enquanto um `If` operador que tem três argumentos avalia apenas dois deles.</span><span class="sxs-lookup"><span data-stu-id="d717f-123">An `IIf` function always evaluates all three of its arguments, whereas an `If` operator that has three arguments evaluates only two of them.</span></span> <span data-ttu-id="d717f-124">A primeira `If` argumento é avaliado e o resultado é convertido como um `Boolean` valor, `True` ou `False`.</span><span class="sxs-lookup"><span data-stu-id="d717f-124">The first `If` argument is evaluated and the result is cast as a `Boolean` value, `True` or `False`.</span></span> <span data-ttu-id="d717f-125">Se o valor for `True`, `argument2` é avaliada e seu valor é retornado, mas `argument3` não será avaliado.</span><span class="sxs-lookup"><span data-stu-id="d717f-125">If the value is `True`, `argument2` is evaluated and its value is returned, but `argument3` is not evaluated.</span></span> <span data-ttu-id="d717f-126">Se o valor da `Boolean` expressão é `False`, `argument3` é avaliada e seu valor é retornado, mas `argument2` não será avaliado.</span><span class="sxs-lookup"><span data-stu-id="d717f-126">If the value of the `Boolean` expression is `False`, `argument3` is evaluated and its value is returned, but `argument2` is not evaluated.</span></span> <span data-ttu-id="d717f-127">Os exemplos a seguir ilustram o uso do `If` quando três argumentos são usados:</span><span class="sxs-lookup"><span data-stu-id="d717f-127">The following examples illustrate the use of `If` when three arguments are used:</span></span>  
  
 [!code-vb[VbVbalrOperators#100](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class4.vb#100)]  
  
 <span data-ttu-id="d717f-128">O exemplo a seguir ilustra o valor de avaliação de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="d717f-128">The following example illustrates the value of short-circuit evaluation.</span></span> <span data-ttu-id="d717f-129">O exemplo mostra duas tentativas de dividir variável `number` pela variável `divisor` exceto quando `divisor` é zero.</span><span class="sxs-lookup"><span data-stu-id="d717f-129">The example shows two attempts to divide variable `number` by variable `divisor` except when `divisor` is zero.</span></span> <span data-ttu-id="d717f-130">Nesse caso, um 0 deve ser retornado, e deve ser feita nenhuma tentativa de executar a divisão porque resultaria um erro de tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="d717f-130">In that case, a 0 should be returned, and no attempt should be made to perform the division because a run-time error would result.</span></span> <span data-ttu-id="d717f-131">Porque o `If` avaliação de curto-circuito do uso de expressões, ela avalia o segundo ou terceiro argumento, dependendo do valor do primeiro argumento.</span><span class="sxs-lookup"><span data-stu-id="d717f-131">Because the `If` expression uses short-circuit evaluation, it evaluates either the second or the third argument, depending on the value of the first argument.</span></span> <span data-ttu-id="d717f-132">Se o primeiro argumento for true, o divisor não é zero e é seguro avaliar o segundo argumento e executar a divisão.</span><span class="sxs-lookup"><span data-stu-id="d717f-132">If the first argument is true, the divisor is not zero and it is safe to evaluate the second argument and perform the division.</span></span> <span data-ttu-id="d717f-133">Se o primeiro argumento for false, somente o terceiro argumento é avaliado e 0 é retornado.</span><span class="sxs-lookup"><span data-stu-id="d717f-133">If the first argument is false, only the third argument is evaluated and a 0 is returned.</span></span> <span data-ttu-id="d717f-134">Portanto, quando o divisor for 0, nenhuma tentativa é feita para executar a divisão e nenhum resultado de erro.</span><span class="sxs-lookup"><span data-stu-id="d717f-134">Therefore, when the divisor is 0, no attempt is made to perform the division and no error results.</span></span> <span data-ttu-id="d717f-135">No entanto, porque `IIf` não usa avaliação curto-circuito, o segundo argumento é avaliado, mesmo quando o primeiro argumento for false.</span><span class="sxs-lookup"><span data-stu-id="d717f-135">However, because `IIf` does not use short-circuit evaluation, the second argument is evaluated even when the first argument is false.</span></span> <span data-ttu-id="d717f-136">Isso causa um erro de divisão por zero tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="d717f-136">This causes a run-time divide-by-zero error.</span></span>  
  
 [!code-vb[VbVbalrOperators#101](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class4.vb#101)]  
  
## <a name="if-operator-called-with-two-arguments"></a><span data-ttu-id="d717f-137">Se o operador de chamada com dois argumentos</span><span class="sxs-lookup"><span data-stu-id="d717f-137">If Operator Called with Two Arguments</span></span>  
 <span data-ttu-id="d717f-138">O primeiro argumento para `If` pode ser omitido.</span><span class="sxs-lookup"><span data-stu-id="d717f-138">The first argument to `If` can be omitted.</span></span> <span data-ttu-id="d717f-139">Isso permite que o operador a ser chamado usando apenas dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="d717f-139">This enables the operator to be called by using only two arguments.</span></span> <span data-ttu-id="d717f-140">A lista a seguir se aplica somente quando o `If` operador é chamado com dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="d717f-140">The following list applies only when the `If` operator is called with two arguments.</span></span>  
  
## <a name="parts"></a><span data-ttu-id="d717f-141">Partes</span><span class="sxs-lookup"><span data-stu-id="d717f-141">Parts</span></span>  
  
|<span data-ttu-id="d717f-142">Termo</span><span class="sxs-lookup"><span data-stu-id="d717f-142">Term</span></span>|<span data-ttu-id="d717f-143">Definição</span><span class="sxs-lookup"><span data-stu-id="d717f-143">Definition</span></span>|  
|---|---|  
|`argument2`|<span data-ttu-id="d717f-144">Necessário.</span><span class="sxs-lookup"><span data-stu-id="d717f-144">Required.</span></span> <span data-ttu-id="d717f-145">`Object`.</span><span class="sxs-lookup"><span data-stu-id="d717f-145">`Object`.</span></span> <span data-ttu-id="d717f-146">Deve ser uma referência ou um tipo anulável.</span><span class="sxs-lookup"><span data-stu-id="d717f-146">Must be a reference or nullable type.</span></span> <span data-ttu-id="d717f-147">Avaliado e retornado quando ele for avaliado como algo diferente de `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="d717f-147">Evaluated and returned when it evaluates to anything other than `Nothing`.</span></span>|  
|`argument3`|<span data-ttu-id="d717f-148">Necessário.</span><span class="sxs-lookup"><span data-stu-id="d717f-148">Required.</span></span> <span data-ttu-id="d717f-149">`Object`.</span><span class="sxs-lookup"><span data-stu-id="d717f-149">`Object`.</span></span> <span data-ttu-id="d717f-150">Avaliado e retornado se `argument2` for avaliada como `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="d717f-150">Evaluated and returned if `argument2` evaluates to `Nothing`.</span></span>|  
  
 <span data-ttu-id="d717f-151">Quando o `Boolean` argumento for omitido, o primeiro argumento deve ser uma referência ou um tipo anulável.</span><span class="sxs-lookup"><span data-stu-id="d717f-151">When the `Boolean` argument is omitted, the first argument must be a reference or nullable type.</span></span> <span data-ttu-id="d717f-152">Se o primeiro argumento é avaliado como `Nothing`, o valor do segundo argumento é retornado.</span><span class="sxs-lookup"><span data-stu-id="d717f-152">If the first argument evaluates to `Nothing`, the value of the second argument is returned.</span></span> <span data-ttu-id="d717f-153">Em todos os outros casos, o valor do primeiro argumento é retornado.</span><span class="sxs-lookup"><span data-stu-id="d717f-153">In all other cases, the value of the first argument is returned.</span></span> <span data-ttu-id="d717f-154">O exemplo a seguir ilustra como funciona essa avaliação.</span><span class="sxs-lookup"><span data-stu-id="d717f-154">The following example illustrates how this evaluation works.</span></span>  
  
 [!code-vb[VbVbalrOperators#102](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class4.vb#102)]  
  
## <a name="see-also"></a><span data-ttu-id="d717f-155">Consulte também</span><span class="sxs-lookup"><span data-stu-id="d717f-155">See also</span></span>

- <xref:Microsoft.VisualBasic.Interaction.IIf%2A>
- [<span data-ttu-id="d717f-156">Tipos de Valor Anulável</span><span class="sxs-lookup"><span data-stu-id="d717f-156">Nullable Value Types</span></span>](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md)
- [<span data-ttu-id="d717f-157">Nothing</span><span class="sxs-lookup"><span data-stu-id="d717f-157">Nothing</span></span>](../../../visual-basic/language-reference/nothing.md)
