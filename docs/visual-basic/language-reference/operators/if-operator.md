---
title: Operador If (Visual Basic)
ms.date: 07/20/2015
f1_keywords:
- vb.IfOperator
- IfOperator
helpviewer_keywords:
- ternary operators [Visual Basic]
- conditional execution
- If expressions [Visual Basic]
- conditional operator [Visual Basic]
- If Operator [Visual Basic]
ms.assetid: dd56c9df-7cd4-442c-9ba6-20c70ee44c8f
ms.openlocfilehash: 192309a7ca728feb300e867bf2340e669e9da16c
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/04/2018
ms.locfileid: "33603958"
---
# <a name="if-operator-visual-basic"></a><span data-ttu-id="7d0c4-102">Operador If (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="7d0c4-102">If Operator (Visual Basic)</span></span>
<span data-ttu-id="7d0c4-103">Usos de curto-circuito avaliação para retornar um de dois valores condicionalmente.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-103">Uses short-circuit evaluation to conditionally return one of two values.</span></span> <span data-ttu-id="7d0c4-104">O `If` operador pode ser chamado com três argumentos ou com dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-104">The `If` operator can be called with three arguments or with two arguments.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="7d0c4-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="7d0c4-105">Syntax</span></span>  
  
```  
If( [argument1,] argument2, argument3 )  
```  
  
## <a name="if-operator-called-with-three-arguments"></a><span data-ttu-id="7d0c4-106">Se o operador chamado com três argumentos</span><span class="sxs-lookup"><span data-stu-id="7d0c4-106">If Operator Called with Three Arguments</span></span>  
 <span data-ttu-id="7d0c4-107">Quando `If` é chamado usando três argumentos, o primeiro argumento deve ser avaliada como um valor que pode ser convertido como um `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-107">When `If` is called by using three arguments, the first argument must evaluate to a value that can be cast as a `Boolean`.</span></span> <span data-ttu-id="7d0c4-108">Que `Boolean` valor determinará qual dos outros dois argumentos é avaliado e retornado.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-108">That `Boolean` value will determine which of the other two arguments is evaluated and returned.</span></span> <span data-ttu-id="7d0c4-109">A lista a seguir se aplica somente quando o `If` operador é chamado por meio de três argumentos.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-109">The following list applies only when the `If` operator is called by using three arguments.</span></span>  
  
## <a name="parts"></a><span data-ttu-id="7d0c4-110">Partes</span><span class="sxs-lookup"><span data-stu-id="7d0c4-110">Parts</span></span>  
  
|<span data-ttu-id="7d0c4-111">Termo</span><span class="sxs-lookup"><span data-stu-id="7d0c4-111">Term</span></span>|<span data-ttu-id="7d0c4-112">Definição</span><span class="sxs-lookup"><span data-stu-id="7d0c4-112">Definition</span></span>|  
|---|---|  
|`argument1`|<span data-ttu-id="7d0c4-113">Necessário.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-113">Required.</span></span> <span data-ttu-id="7d0c4-114">`Boolean`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-114">`Boolean`.</span></span> <span data-ttu-id="7d0c4-115">Determina qual dos outros argumentos para avaliar e retornar.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-115">Determines which of the other arguments to evaluate and return.</span></span>|  
|`argument2`|<span data-ttu-id="7d0c4-116">Necessário.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-116">Required.</span></span> <span data-ttu-id="7d0c4-117">`Object`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-117">`Object`.</span></span> <span data-ttu-id="7d0c4-118">Avaliado e retornado se `argument1` é avaliada como `True`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-118">Evaluated and returned if `argument1` evaluates to `True`.</span></span>|  
|`argument3`|<span data-ttu-id="7d0c4-119">Necessário.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-119">Required.</span></span> <span data-ttu-id="7d0c4-120">`Object`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-120">`Object`.</span></span> <span data-ttu-id="7d0c4-121">Avaliado e retornado se `argument1` é avaliada como `False` ou se `argument1` é um [Nullable](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md) `Boolean` variável que é avaliada como [nada](../../../visual-basic/language-reference/nothing.md).</span><span class="sxs-lookup"><span data-stu-id="7d0c4-121">Evaluated and returned if `argument1` evaluates to `False` or if `argument1` is a [Nullable](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md)`Boolean` variable that evaluates to [Nothing](../../../visual-basic/language-reference/nothing.md).</span></span>|  
  
 <span data-ttu-id="7d0c4-122">Um `If` operador que é chamado com três argumentos funciona como um `IIf` função exceto que ele usa curto-circuito avaliação.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-122">An `If` operator that is called with three arguments works like an `IIf` function except that it uses short-circuit evaluation.</span></span> <span data-ttu-id="7d0c4-123">Um `IIf` função sempre avalia todas as três argumentos, enquanto um `If` operador que tem três argumentos é avaliada apenas duas delas.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-123">An `IIf` function always evaluates all three of its arguments, whereas an `If` operator that has three arguments evaluates only two of them.</span></span> <span data-ttu-id="7d0c4-124">A primeira `If` argumento é avaliado e o resultado será convertido como uma `Boolean` valor, `True` ou `False`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-124">The first `If` argument is evaluated and the result is cast as a `Boolean` value, `True` or `False`.</span></span> <span data-ttu-id="7d0c4-125">Se o valor for `True`, `argument2` é avaliada e seu valor é retornado, mas `argument3` não será avaliada.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-125">If the value is `True`, `argument2` is evaluated and its value is returned, but `argument3` is not evaluated.</span></span> <span data-ttu-id="7d0c4-126">Se o valor da `Boolean` expressão é `False`, `argument3` é avaliada e seu valor é retornado, mas `argument2` não será avaliada.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-126">If the value of the `Boolean` expression is `False`, `argument3` is evaluated and its value is returned, but `argument2` is not evaluated.</span></span> <span data-ttu-id="7d0c4-127">Os exemplos a seguir ilustram o uso do `If` quando são usados os três argumentos:</span><span class="sxs-lookup"><span data-stu-id="7d0c4-127">The following examples illustrate the use of `If` when three arguments are used:</span></span>  
  
 [!code-vb[VbVbalrOperators#100](../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/if-operator_1.vb)]  
  
 <span data-ttu-id="7d0c4-128">O exemplo a seguir ilustra o valor de avaliação de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-128">The following example illustrates the value of short-circuit evaluation.</span></span> <span data-ttu-id="7d0c4-129">O exemplo mostra duas tentativas de dividir variável `number` pela variável `divisor` , exceto quando `divisor` é zero.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-129">The example shows two attempts to divide variable `number` by variable `divisor` except when `divisor` is zero.</span></span> <span data-ttu-id="7d0c4-130">Nesse caso, 0 deve ser retornado, e deve ser feita nenhuma tentativa de executar a divisão porque resultaria um erro de tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-130">In that case, a 0 should be returned, and no attempt should be made to perform the division because a run-time error would result.</span></span> <span data-ttu-id="7d0c4-131">Porque o `If` avaliação de curto-circuito do uso de expressões, ela avalia o segundo ou o terceiro argumento, dependendo do valor do primeiro argumento.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-131">Because the `If` expression uses short-circuit evaluation, it evaluates either the second or the third argument, depending on the value of the first argument.</span></span> <span data-ttu-id="7d0c4-132">Se o primeiro argumento for true, o divisor não for zero e é seguro avaliar o segundo argumento e executar a divisão.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-132">If the first argument is true, the divisor is not zero and it is safe to evaluate the second argument and perform the division.</span></span> <span data-ttu-id="7d0c4-133">Se o primeiro argumento for false, apenas o terceiro argumento é avaliado e 0 é retornado.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-133">If the first argument is false, only the third argument is evaluated and a 0 is returned.</span></span> <span data-ttu-id="7d0c4-134">Portanto, quando o divisor for 0, nenhuma tentativa de executar a divisão e nenhum resultado de erro.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-134">Therefore, when the divisor is 0, no attempt is made to perform the division and no error results.</span></span> <span data-ttu-id="7d0c4-135">No entanto, como `IIf` não usa avaliação de curto-circuito, o segundo argumento é avaliado, mesmo quando o primeiro argumento é false.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-135">However, because `IIf` does not use short-circuit evaluation, the second argument is evaluated even when the first argument is false.</span></span> <span data-ttu-id="7d0c4-136">Isso causa um erro de divisão por zero tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-136">This causes a run-time divide-by-zero error.</span></span>  
  
 [!code-vb[VbVbalrOperators#101](../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/if-operator_2.vb)]  
  
## <a name="if-operator-called-with-two-arguments"></a><span data-ttu-id="7d0c4-137">Se o operador chamado com dois argumentos</span><span class="sxs-lookup"><span data-stu-id="7d0c4-137">If Operator Called with Two Arguments</span></span>  
 <span data-ttu-id="7d0c4-138">O primeiro argumento para `If` pode ser omitido.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-138">The first argument to `If` can be omitted.</span></span> <span data-ttu-id="7d0c4-139">Isso permite que o operador a ser chamado usando apenas dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-139">This enables the operator to be called by using only two arguments.</span></span> <span data-ttu-id="7d0c4-140">A lista a seguir se aplica somente quando o `If` operador é chamado com dois argumentos.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-140">The following list applies only when the `If` operator is called with two arguments.</span></span>  
  
## <a name="parts"></a><span data-ttu-id="7d0c4-141">Partes</span><span class="sxs-lookup"><span data-stu-id="7d0c4-141">Parts</span></span>  
  
|<span data-ttu-id="7d0c4-142">Termo</span><span class="sxs-lookup"><span data-stu-id="7d0c4-142">Term</span></span>|<span data-ttu-id="7d0c4-143">Definição</span><span class="sxs-lookup"><span data-stu-id="7d0c4-143">Definition</span></span>|  
|---|---|  
|`argument2`|<span data-ttu-id="7d0c4-144">Necessário.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-144">Required.</span></span> <span data-ttu-id="7d0c4-145">`Object`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-145">`Object`.</span></span> <span data-ttu-id="7d0c4-146">Deve ser uma referência ou um tipo anulável.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-146">Must be a reference or nullable type.</span></span> <span data-ttu-id="7d0c4-147">Avaliado e retornado quando ele for avaliado como algo diferente de `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-147">Evaluated and returned when it evaluates to anything other than `Nothing`.</span></span>|  
|`argument3`|<span data-ttu-id="7d0c4-148">Necessário.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-148">Required.</span></span> <span data-ttu-id="7d0c4-149">`Object`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-149">`Object`.</span></span> <span data-ttu-id="7d0c4-150">Avaliado e retornado se `argument2` é avaliada como `Nothing`.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-150">Evaluated and returned if `argument2` evaluates to `Nothing`.</span></span>|  
  
 <span data-ttu-id="7d0c4-151">Quando o `Boolean` argumento for omitido, o primeiro argumento deve ser uma referência ou um tipo anulável.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-151">When the `Boolean` argument is omitted, the first argument must be a reference or nullable type.</span></span> <span data-ttu-id="7d0c4-152">Se o primeiro argumento for avaliado como `Nothing`, o valor do segundo argumento é retornado.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-152">If the first argument evaluates to `Nothing`, the value of the second argument is returned.</span></span> <span data-ttu-id="7d0c4-153">Em todos os outros casos, o valor do primeiro argumento será retornado.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-153">In all other cases, the value of the first argument is returned.</span></span> <span data-ttu-id="7d0c4-154">O exemplo a seguir ilustra como esta avaliação funciona.</span><span class="sxs-lookup"><span data-stu-id="7d0c4-154">The following example illustrates how this evaluation works.</span></span>  
  
 [!code-vb[VbVbalrOperators#102](../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/if-operator_3.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="7d0c4-155">Consulte também</span><span class="sxs-lookup"><span data-stu-id="7d0c4-155">See Also</span></span>  
 <xref:Microsoft.VisualBasic.Interaction.IIf%2A>  
 [<span data-ttu-id="7d0c4-156">Tipos de Valor Anulável</span><span class="sxs-lookup"><span data-stu-id="7d0c4-156">Nullable Value Types</span></span>](../../../visual-basic/programming-guide/language-features/data-types/nullable-value-types.md)  
 [<span data-ttu-id="7d0c4-157">Nothing</span><span class="sxs-lookup"><span data-stu-id="7d0c4-157">Nothing</span></span>](../../../visual-basic/language-reference/nothing.md)
