---
title: Instrução Select...Case (Visual Basic)
ms.date: 07/20/2015
f1_keywords:
- vb.Select
- vb.Case
helpviewer_keywords:
- Select statement [Visual Basic]
- Case statement [Visual Basic]
- Select...Case statements
- conditional statements [Visual Basic], Select Case
- control flow [Visual Basic], branching
- Else keyword [Visual Basic], in Select...Case statements
- execution [Visual Basic], conditional
- To keyword [Visual Basic], in Select...Case statements
- Select Case statement [Visual Basic], Select...Case
- Select statement [Visual Basic], Select...Case
- Is operator [Visual Basic], in Select...Case statements
- branching [Visual Basic], conditional
- Case Else statement [Visual Basic], Select...Case
- End keyword [Visual Basic], Select Case statements
- Case statement [Visual Basic], Select...Case
ms.assetid: 68877b65-5419-4bf0-a465-20cd0e4c7d44
ms.openlocfilehash: 627318677270ba4ffa8ee430febea7ddf83bd245
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69957657"
---
# <a name="selectcase-statement-visual-basic"></a><span data-ttu-id="c97df-102">Instrução Select...Case (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="c97df-102">Select...Case Statement (Visual Basic)</span></span>
<span data-ttu-id="c97df-103">Executa um dos vários grupos de instruções, dependendo do valor de uma expressão.</span><span class="sxs-lookup"><span data-stu-id="c97df-103">Runs one of several groups of statements, depending on the value of an expression.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="c97df-104">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="c97df-104">Syntax</span></span>  
  
```  
Select [ Case ] testexpression  
    [ Case expressionlist  
        [ statements ] ]  
    [ Case Else  
        [ elsestatements ] ]  
End Select  
```  
  
## <a name="parts"></a><span data-ttu-id="c97df-105">Partes</span><span class="sxs-lookup"><span data-stu-id="c97df-105">Parts</span></span>  
  
|<span data-ttu-id="c97df-106">Termo</span><span class="sxs-lookup"><span data-stu-id="c97df-106">Term</span></span>|<span data-ttu-id="c97df-107">Definição</span><span class="sxs-lookup"><span data-stu-id="c97df-107">Definition</span></span>|  
|---|---|  
|`testexpression`|<span data-ttu-id="c97df-108">Necessário.</span><span class="sxs-lookup"><span data-stu-id="c97df-108">Required.</span></span> <span data-ttu-id="c97df-109">Expressão.</span><span class="sxs-lookup"><span data-stu-id="c97df-109">Expression.</span></span> <span data-ttu-id="c97df-110">Deve ser avaliado como um dos tipos de dados elementares`Boolean`( `Byte` `Char`, `Decimal` `Integer` `Date` `Double` ,,`Long`,,,,,,,, `Short` `Object` `SByte` `Single` ,`String`, ,`ULong`e )`UShort`. `UInteger`</span><span class="sxs-lookup"><span data-stu-id="c97df-110">Must evaluate to one of the elementary data types (`Boolean`, `Byte`, `Char`, `Date`, `Double`, `Decimal`, `Integer`, `Long`, `Object`, `SByte`, `Short`, `Single`, `String`, `UInteger`, `ULong`, and `UShort`).</span></span>|  
|`expressionlist`|<span data-ttu-id="c97df-111">Necessário em uma `Case` instrução.</span><span class="sxs-lookup"><span data-stu-id="c97df-111">Required in a `Case` statement.</span></span> <span data-ttu-id="c97df-112">Lista de cláusulas de expressão que representam valores `testexpression`de correspondência para.</span><span class="sxs-lookup"><span data-stu-id="c97df-112">List of expression clauses representing match values for `testexpression`.</span></span> <span data-ttu-id="c97df-113">Várias cláusulas de expressão são separadas por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="c97df-113">Multiple expression clauses are separated by commas.</span></span> <span data-ttu-id="c97df-114">Cada cláusula pode ter um dos seguintes formatos:</span><span class="sxs-lookup"><span data-stu-id="c97df-114">Each clause can take one of the following forms:</span></span><br /><br /> <span data-ttu-id="c97df-115">-   *expression1* `To` *expression2*</span><span class="sxs-lookup"><span data-stu-id="c97df-115">-   *expression1* `To` *expression2*</span></span><br /><span data-ttu-id="c97df-116">-[ `Is` ] *expressão* ComparisonOperator</span><span class="sxs-lookup"><span data-stu-id="c97df-116">-   [ `Is` ] *comparisonoperator* *expression*</span></span><br /><span data-ttu-id="c97df-117">-   *expressão*</span><span class="sxs-lookup"><span data-stu-id="c97df-117">-   *expression*</span></span><br /><br /> <span data-ttu-id="c97df-118">Use a `To` palavra-chave para especificar os limites de um intervalo de valores `testexpression`de correspondência para.</span><span class="sxs-lookup"><span data-stu-id="c97df-118">Use the `To` keyword to specify the boundaries of a range of match values for `testexpression`.</span></span> <span data-ttu-id="c97df-119">O valor de `expression1` deve ser menor ou igual ao valor de. `expression2`</span><span class="sxs-lookup"><span data-stu-id="c97df-119">The value of `expression1` must be less than or equal to the value of `expression2`.</span></span><br /><br /> <span data-ttu-id="c97df-120">Use a `Is` palavra-chave com um operador`=`de `<>`comparação ( `<=`, `>` `<`,, `>=`, ou) para especificar uma restrição nos valores de `testexpression`correspondência para.</span><span class="sxs-lookup"><span data-stu-id="c97df-120">Use the `Is` keyword with a comparison operator (`=`, `<>`, `<`, `<=`, `>`, or `>=`) to specify a restriction on the match values for `testexpression`.</span></span> <span data-ttu-id="c97df-121">Se a `Is` palavra-chave não for fornecida, ela será inserida automaticamente antes de *ComparisonOperator*.</span><span class="sxs-lookup"><span data-stu-id="c97df-121">If the `Is` keyword is not supplied, it is automatically inserted before *comparisonoperator*.</span></span><br /><br /> <span data-ttu-id="c97df-122">O formulário que especifica `expression` apenas é tratado como um caso especial `Is` do formulário em que *ComparisonOperator* é o sinal de`=`igual ().</span><span class="sxs-lookup"><span data-stu-id="c97df-122">The form specifying only `expression` is treated as a special case of the `Is` form where *comparisonoperator* is the equal sign (`=`).</span></span> <span data-ttu-id="c97df-123">Este formulário é avaliado como `testexpression`.  =  `expression`</span><span class="sxs-lookup"><span data-stu-id="c97df-123">This form is evaluated as `testexpression` = `expression`.</span></span><br /><br /> <span data-ttu-id="c97df-124">As expressões em `expressionlist` podem ser de qualquer tipo de dados, desde que sejam conversíveis implicitamente para o `testexpression` tipo e o `comparisonoperator` apropriado seja válido para os dois tipos com os quais ele está sendo usado.</span><span class="sxs-lookup"><span data-stu-id="c97df-124">The expressions in `expressionlist` can be of any data type, provided they are implicitly convertible to the type of `testexpression` and the appropriate `comparisonoperator` is valid for the two types it is being used with.</span></span>|  
|`statements`|<span data-ttu-id="c97df-125">Opcional.</span><span class="sxs-lookup"><span data-stu-id="c97df-125">Optional.</span></span> <span data-ttu-id="c97df-126">Uma ou mais instruções que `Case` se seguem são `testexpression` executadas se o corresponde `expressionlist`a qualquer cláusula no.</span><span class="sxs-lookup"><span data-stu-id="c97df-126">One or more statements following `Case` that run if `testexpression` matches any clause in `expressionlist`.</span></span>|  
|`elsestatements`|<span data-ttu-id="c97df-127">Opcional.</span><span class="sxs-lookup"><span data-stu-id="c97df-127">Optional.</span></span> <span data-ttu-id="c97df-128">Uma ou mais instruções que `Case Else` se seguem são `testexpression` executadas se não correspondem `Case` a `expressionlist` nenhuma cláusula no de qualquer uma das instruções.</span><span class="sxs-lookup"><span data-stu-id="c97df-128">One or more statements following `Case Else` that run if `testexpression` does not match any clause in the `expressionlist` of any of the `Case` statements.</span></span>|  
|`End Select`|<span data-ttu-id="c97df-129">Encerra a definição do `Select`... `Case` construção.</span><span class="sxs-lookup"><span data-stu-id="c97df-129">Terminates the definition of the `Select`...`Case` construction.</span></span>|  
  
## <a name="remarks"></a><span data-ttu-id="c97df-130">Comentários</span><span class="sxs-lookup"><span data-stu-id="c97df-130">Remarks</span></span>  
 <span data-ttu-id="c97df-131">Se `testexpression` corresponder a `Case` qualquer `Case` `Case Else` `End Select` cláusula, as instruções após `Case` essa instrução são executadas até a próxima instrução, ou. `expressionlist`</span><span class="sxs-lookup"><span data-stu-id="c97df-131">If `testexpression` matches any `Case` `expressionlist` clause, the statements following that `Case` statement run up to the next `Case`, `Case Else`, or `End Select` statement.</span></span> <span data-ttu-id="c97df-132">Em seguida, o controle passa para `End Select`a instrução a seguir.</span><span class="sxs-lookup"><span data-stu-id="c97df-132">Control then passes to the statement following `End Select`.</span></span> <span data-ttu-id="c97df-133">Se `testexpression` corresponder a `expressionlist` uma cláusula em mais de `Case` uma cláusula, somente as instruções após a primeira correspondência são executadas.</span><span class="sxs-lookup"><span data-stu-id="c97df-133">If `testexpression` matches an `expressionlist` clause in more than one `Case` clause, only the statements following the first match run.</span></span>  
  
 <span data-ttu-id="c97df-134">A `Case Else` instrução é usada para introduzir o `elsestatements` a ser executado se nenhuma correspondência for encontrada entre `testexpression` o e `expressionlist` uma cláusula em qualquer uma das `Case` outras instruções.</span><span class="sxs-lookup"><span data-stu-id="c97df-134">The `Case Else` statement is used to introduce the `elsestatements` to run if no match is found between the `testexpression` and an `expressionlist` clause in any of the other `Case` statements.</span></span> <span data-ttu-id="c97df-135">Embora não seja necessário, é uma boa ideia ter uma `Case Else` instrução em sua `Select Case` construção `testexpression` para lidar com valores imprevistos.</span><span class="sxs-lookup"><span data-stu-id="c97df-135">Although not required, it is a good idea to have a `Case Else` statement in your `Select Case` construction to handle unforeseen `testexpression` values.</span></span> <span data-ttu-id="c97df-136">Se nenhuma `Case` `expressionlist` cláusula `End Select`corresponder e não houver instrução, o controle passará para a instrução a seguir. `Case Else` `testexpression`</span><span class="sxs-lookup"><span data-stu-id="c97df-136">If no `Case` `expressionlist` clause matches `testexpression` and there is no `Case Else` statement, control passes to the statement following `End Select`.</span></span>  
  
 <span data-ttu-id="c97df-137">Você pode usar várias expressões ou intervalos em cada `Case` cláusula.</span><span class="sxs-lookup"><span data-stu-id="c97df-137">You can use multiple expressions or ranges in each `Case` clause.</span></span> <span data-ttu-id="c97df-138">Por exemplo, a linha a seguir é válida.</span><span class="sxs-lookup"><span data-stu-id="c97df-138">For example, the following line is valid.</span></span>  
  
 `Case 1 To 4, 7 To 9, 11, 13, Is > maxNumber`  
  
> [!NOTE]
> <span data-ttu-id="c97df-139">A `Is` palavra-chave usada `Case` nas `Case Else` instruções e não é a mesma que o [operador is](../../../visual-basic/language-reference/operators/is-operator.md), que é usado para comparação de referência de objeto.</span><span class="sxs-lookup"><span data-stu-id="c97df-139">The `Is` keyword used in the `Case` and `Case Else` statements is not the same as the [Is Operator](../../../visual-basic/language-reference/operators/is-operator.md), which is used for object reference comparison.</span></span>  
  
 <span data-ttu-id="c97df-140">Você pode especificar intervalos e várias expressões para cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="c97df-140">You can specify ranges and multiple expressions for character strings.</span></span> <span data-ttu-id="c97df-141">No exemplo a seguir, `Case` corresponde a qualquer cadeia de caracteres que seja exatamente igual a "maçãs", tem um valor entre "porcas" e "sopa" em ordem alfabética ou contém exatamente o mesmo valor que o valor `testItem`atual de.</span><span class="sxs-lookup"><span data-stu-id="c97df-141">In the following example, `Case` matches any string that is exactly equal to "apples", has a value between "nuts" and "soup" in alphabetical order, or contains the exact same value as the current value of `testItem`.</span></span>  
  
 `Case "apples", "nuts" To "soup", testItem`  
  
 <span data-ttu-id="c97df-142">A configuração de `Option Compare` pode afetar comparações de cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="c97df-142">The setting of `Option Compare` can affect string comparisons.</span></span> <span data-ttu-id="c97df-143">Em `Option Compare Text`, as cadeias de caracteres "maçãs" e "maçãs" são comparadas como iguais, mas em `Option Compare Binary`, elas não.</span><span class="sxs-lookup"><span data-stu-id="c97df-143">Under `Option Compare Text`, the strings "Apples" and "apples" compare as equal, but under `Option Compare Binary`, they do not.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="c97df-144">Uma `Case` instrução com várias cláusulas pode exibir o comportamento conhecido como *curto-circuito*.</span><span class="sxs-lookup"><span data-stu-id="c97df-144">A `Case` statement with multiple clauses can exhibit behavior known as *short-circuiting*.</span></span> <span data-ttu-id="c97df-145">Visual Basic avalia as cláusulas da esquerda para a direita e, se uma delas produz uma correspondência `testexpression`com, as cláusulas restantes não são avaliadas.</span><span class="sxs-lookup"><span data-stu-id="c97df-145">Visual Basic evaluates the clauses from left to right, and if one produces a match with `testexpression`, the remaining clauses are not evaluated.</span></span> <span data-ttu-id="c97df-146">O curto circuito pode melhorar o desempenho, mas pode produzir resultados inesperados se você espera que todas as `expressionlist` expressões sejam avaliadas.</span><span class="sxs-lookup"><span data-stu-id="c97df-146">Short-circuiting can improve performance, but it can produce unexpected results if you are expecting every expression in `expressionlist` to be evaluated.</span></span> <span data-ttu-id="c97df-147">Para obter mais informações sobre o curto-circuito, consulte [expressões booleanas](../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="c97df-147">For more information on short-circuiting, see [Boolean Expressions](../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md).</span></span>  
  
 <span data-ttu-id="c97df-148">Se o código dentro de `Case` um `Case Else` bloco de instrução ou não precisar executar nenhuma das instruções no bloco, ele poderá sair do bloco usando a `Exit Select` instrução.</span><span class="sxs-lookup"><span data-stu-id="c97df-148">If the code within a `Case` or `Case Else` statement block does not need to run any more of the statements in the block, it can exit the block by using the `Exit Select` statement.</span></span> <span data-ttu-id="c97df-149">Isso transfere o controle imediatamente para a `End Select`instrução a seguir.</span><span class="sxs-lookup"><span data-stu-id="c97df-149">This transfers control immediately to the statement following `End Select`.</span></span>  
  
 <span data-ttu-id="c97df-150">`Select Case`as construções podem ser aninhadas.</span><span class="sxs-lookup"><span data-stu-id="c97df-150">`Select Case` constructions can be nested.</span></span> <span data-ttu-id="c97df-151">Cada construção `Select Case` aninhada deve ter uma `End Select` instrução correspondente e deve estar completamente contida em um `Case` bloco `Case Else` único ou de instrução da `Select Case` construção externa dentro da qual está aninhada.</span><span class="sxs-lookup"><span data-stu-id="c97df-151">Each nested `Select Case` construction must have a matching `End Select` statement and must be completely contained within a single `Case` or `Case Else` statement block of the outer `Select Case` construction within which it is nested.</span></span>  
  
## <a name="example"></a><span data-ttu-id="c97df-152">Exemplo</span><span class="sxs-lookup"><span data-stu-id="c97df-152">Example</span></span>  
 <span data-ttu-id="c97df-153">O exemplo a seguir usa `Select Case` uma construção para gravar uma linha correspondente ao valor da variável. `number`</span><span class="sxs-lookup"><span data-stu-id="c97df-153">The following example uses a `Select Case` construction to write a line corresponding to the value of the variable `number`.</span></span> <span data-ttu-id="c97df-154">A segunda `Case` instrução contém o valor que corresponde ao valor atual de `number`, portanto, a instrução que grava "entre 6 e 8, inclusivo" é executada.</span><span class="sxs-lookup"><span data-stu-id="c97df-154">The second `Case` statement contains the value that matches the current value of `number`, so the statement that writes "Between 6 and 8, inclusive" runs.</span></span>  
  
 [!code-vb[VbVbalrStatements#54](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#54)]  
  
## <a name="see-also"></a><span data-ttu-id="c97df-155">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c97df-155">See also</span></span>

- <xref:Microsoft.VisualBasic.Interaction.Choose%2A>
- [<span data-ttu-id="c97df-156">Instrução End</span><span class="sxs-lookup"><span data-stu-id="c97df-156">End Statement</span></span>](../../../visual-basic/language-reference/statements/end-statement.md)
- [<span data-ttu-id="c97df-157">Instrução If...Then...Else</span><span class="sxs-lookup"><span data-stu-id="c97df-157">If...Then...Else Statement</span></span>](../../../visual-basic/language-reference/statements/if-then-else-statement.md)
- [<span data-ttu-id="c97df-158">Instrução Option Compare</span><span class="sxs-lookup"><span data-stu-id="c97df-158">Option Compare Statement</span></span>](../../../visual-basic/language-reference/statements/option-compare-statement.md)
- [<span data-ttu-id="c97df-159">Instrução Exit</span><span class="sxs-lookup"><span data-stu-id="c97df-159">Exit Statement</span></span>](../../../visual-basic/language-reference/statements/exit-statement.md)
