---
title: Instrução Select...Case (Visual Basic)
ms.date: 07/20/2015
f1_keywords:
- vb.Select
- vb.Case
helpviewer_keywords:
- Select statement [Visual Basic]
- Case statement [Visual Basic]
- Select...Case statements
- conditional statements [Visual Basic], Select Case
- control flow [Visual Basic], branching
- Else keyword [Visual Basic], in Select...Case statements
- execution [Visual Basic], conditional
- To keyword [Visual Basic], in Select...Case statements
- Select Case statement [Visual Basic], Select...Case
- Select statement [Visual Basic], Select...Case
- Is operator [Visual Basic], in Select...Case statements
- branching [Visual Basic], conditional
- Case Else statement [Visual Basic], Select...Case
- End keyword [Visual Basic], Select Case statements
- Case statement [Visual Basic], Select...Case
ms.assetid: 68877b65-5419-4bf0-a465-20cd0e4c7d44
ms.openlocfilehash: d035118febc5ea9d1ea8e5cc0145cb030626b030
ms.sourcegitcommit: 1f12db2d852d05bed8c53845f0b5a57a762979c8
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2019
ms.locfileid: "72583244"
---
# <a name="selectcase-statement-visual-basic"></a><span data-ttu-id="add8b-102">Instrução Select...Case (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="add8b-102">Select...Case Statement (Visual Basic)</span></span>
<span data-ttu-id="add8b-103">Executa um dos vários grupos de instruções, dependendo do valor de uma expressão.</span><span class="sxs-lookup"><span data-stu-id="add8b-103">Runs one of several groups of statements, depending on the value of an expression.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="add8b-104">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="add8b-104">Syntax</span></span>  
  
```vb  
Select [ Case ] testexpression  
    [ Case expressionlist  
        [ statements ] ]  
    [ Case Else  
        [ elsestatements ] ]  
End Select  
```  
  
## <a name="parts"></a><span data-ttu-id="add8b-105">Partes</span><span class="sxs-lookup"><span data-stu-id="add8b-105">Parts</span></span>  
  
|<span data-ttu-id="add8b-106">Termo</span><span class="sxs-lookup"><span data-stu-id="add8b-106">Term</span></span>|<span data-ttu-id="add8b-107">Definição</span><span class="sxs-lookup"><span data-stu-id="add8b-107">Definition</span></span>|  
|---|---|  
|`testexpression`|<span data-ttu-id="add8b-108">Necessário.</span><span class="sxs-lookup"><span data-stu-id="add8b-108">Required.</span></span> <span data-ttu-id="add8b-109">Expressão.</span><span class="sxs-lookup"><span data-stu-id="add8b-109">Expression.</span></span> <span data-ttu-id="add8b-110">É necessário avaliar um dos tipos de dados elementares (`Boolean`, `Byte`, `Char`, `Date`, `Double`, `Decimal`, `Integer`, `Long`, `Object`, `SByte`, 0, 1, 2 , 3, 4 e 5).</span><span class="sxs-lookup"><span data-stu-id="add8b-110">Must evaluate to one of the elementary data types (`Boolean`, `Byte`, `Char`, `Date`, `Double`, `Decimal`, `Integer`, `Long`, `Object`, `SByte`, `Short`, `Single`, `String`, `UInteger`, `ULong`, and `UShort`).</span></span>|  
|`expressionlist`|<span data-ttu-id="add8b-111">Necessário em uma instrução `Case`.</span><span class="sxs-lookup"><span data-stu-id="add8b-111">Required in a `Case` statement.</span></span> <span data-ttu-id="add8b-112">A lista de cláusulas de expressão que representa valores de correspondência para `testexpression`.</span><span class="sxs-lookup"><span data-stu-id="add8b-112">List of expression clauses representing match values for `testexpression`.</span></span> <span data-ttu-id="add8b-113">Várias cláusulas de expressão são separadas por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="add8b-113">Multiple expression clauses are separated by commas.</span></span> <span data-ttu-id="add8b-114">Cada cláusula pode ter um dos seguintes formatos:</span><span class="sxs-lookup"><span data-stu-id="add8b-114">Each clause can take one of the following forms:</span></span><br /><br /> <span data-ttu-id="add8b-115">-   *expression1* `To` *expression2*</span><span class="sxs-lookup"><span data-stu-id="add8b-115">-   *expression1* `To` *expression2*</span></span><br /><span data-ttu-id="add8b-116">-[`Is`] *expressão* ComparisonOperator</span><span class="sxs-lookup"><span data-stu-id="add8b-116">-   [ `Is` ] *comparisonoperator* *expression*</span></span><br /><span data-ttu-id="add8b-117">*expressão* de -   </span><span class="sxs-lookup"><span data-stu-id="add8b-117">-   *expression*</span></span><br /><br /> <span data-ttu-id="add8b-118">Use a palavra-chave `To` para especificar os limites de um intervalo de valores de correspondência para `testexpression`.</span><span class="sxs-lookup"><span data-stu-id="add8b-118">Use the `To` keyword to specify the boundaries of a range of match values for `testexpression`.</span></span> <span data-ttu-id="add8b-119">O valor de `expression1` deve ser menor ou igual ao valor de `expression2`.</span><span class="sxs-lookup"><span data-stu-id="add8b-119">The value of `expression1` must be less than or equal to the value of `expression2`.</span></span><br /><br /> <span data-ttu-id="add8b-120">Use a palavra-chave `Is` com um operador de comparação (`=`, `<>`, `<`, `<=`, `>` ou `>=`) para especificar uma restrição nos valores de correspondência para `testexpression`.</span><span class="sxs-lookup"><span data-stu-id="add8b-120">Use the `Is` keyword with a comparison operator (`=`, `<>`, `<`, `<=`, `>`, or `>=`) to specify a restriction on the match values for `testexpression`.</span></span> <span data-ttu-id="add8b-121">Se a palavra-chave `Is` não for fornecida, ela será inserida automaticamente antes de *ComparisonOperator*.</span><span class="sxs-lookup"><span data-stu-id="add8b-121">If the `Is` keyword is not supplied, it is automatically inserted before *comparisonoperator*.</span></span><br /><br /> <span data-ttu-id="add8b-122">O formulário que especifica apenas `expression` é tratado como um caso especial do formulário `Is` em que *ComparisonOperator* é o sinal de igual (`=`).</span><span class="sxs-lookup"><span data-stu-id="add8b-122">The form specifying only `expression` is treated as a special case of the `Is` form where *comparisonoperator* is the equal sign (`=`).</span></span> <span data-ttu-id="add8b-123">Esse formulário é avaliado como `testexpression`  =  `expression`.</span><span class="sxs-lookup"><span data-stu-id="add8b-123">This form is evaluated as `testexpression` = `expression`.</span></span><br /><br /> <span data-ttu-id="add8b-124">As expressões em `expressionlist` podem ser de qualquer tipo de dados, desde que sejam conversíveis implicitamente no tipo de `testexpression` e o `comparisonoperator` apropriado seja válido para os dois tipos com os quais ele está sendo usado.</span><span class="sxs-lookup"><span data-stu-id="add8b-124">The expressions in `expressionlist` can be of any data type, provided they are implicitly convertible to the type of `testexpression` and the appropriate `comparisonoperator` is valid for the two types it is being used with.</span></span>|  
|`statements`|<span data-ttu-id="add8b-125">Opcional.</span><span class="sxs-lookup"><span data-stu-id="add8b-125">Optional.</span></span> <span data-ttu-id="add8b-126">Uma ou mais instruções após `Case` executadas se `testexpression` corresponde a qualquer cláusula em `expressionlist`.</span><span class="sxs-lookup"><span data-stu-id="add8b-126">One or more statements following `Case` that run if `testexpression` matches any clause in `expressionlist`.</span></span>|  
|`elsestatements`|<span data-ttu-id="add8b-127">Opcional.</span><span class="sxs-lookup"><span data-stu-id="add8b-127">Optional.</span></span> <span data-ttu-id="add8b-128">Uma ou mais instruções a seguir `Case Else` executadas se `testexpression` não corresponder a nenhuma cláusula na `expressionlist` de qualquer uma das instruções de `Case`.</span><span class="sxs-lookup"><span data-stu-id="add8b-128">One or more statements following `Case Else` that run if `testexpression` does not match any clause in the `expressionlist` of any of the `Case` statements.</span></span>|  
|`End Select`|<span data-ttu-id="add8b-129">Encerra a definição do `Select`... `Case` construção.</span><span class="sxs-lookup"><span data-stu-id="add8b-129">Terminates the definition of the `Select`...`Case` construction.</span></span>|  
  
## <a name="remarks"></a><span data-ttu-id="add8b-130">Comentários</span><span class="sxs-lookup"><span data-stu-id="add8b-130">Remarks</span></span>  
 <span data-ttu-id="add8b-131">Se `testexpression` corresponder a qualquer `Case` `expressionlist` cláusula, as instruções após a instrução `Case` são executadas até a próxima instrução `Case`, `Case Else` ou `End Select`.</span><span class="sxs-lookup"><span data-stu-id="add8b-131">If `testexpression` matches any `Case` `expressionlist` clause, the statements following that `Case` statement run up to the next `Case`, `Case Else`, or `End Select` statement.</span></span> <span data-ttu-id="add8b-132">Em seguida, o controle passa para a instrução a seguir `End Select`.</span><span class="sxs-lookup"><span data-stu-id="add8b-132">Control then passes to the statement following `End Select`.</span></span> <span data-ttu-id="add8b-133">Se `testexpression` corresponder a uma cláusula `expressionlist` em mais de uma cláusula `Case`, somente as instruções após a primeira correspondência são executadas.</span><span class="sxs-lookup"><span data-stu-id="add8b-133">If `testexpression` matches an `expressionlist` clause in more than one `Case` clause, only the statements following the first match run.</span></span>  
  
 <span data-ttu-id="add8b-134">A instrução `Case Else` é usada para apresentar a `elsestatements` a ser executada se nenhuma correspondência for encontrada entre as `testexpression` e uma cláusula `expressionlist` em qualquer uma das outras instruções de `Case`.</span><span class="sxs-lookup"><span data-stu-id="add8b-134">The `Case Else` statement is used to introduce the `elsestatements` to run if no match is found between the `testexpression` and an `expressionlist` clause in any of the other `Case` statements.</span></span> <span data-ttu-id="add8b-135">Embora não seja necessário, é uma boa ideia ter uma instrução de `Case Else` em sua construção de `Select Case` para lidar com valores de `testexpression` imprevisto.</span><span class="sxs-lookup"><span data-stu-id="add8b-135">Although not required, it is a good idea to have a `Case Else` statement in your `Select Case` construction to handle unforeseen `testexpression` values.</span></span> <span data-ttu-id="add8b-136">Se nenhuma cláusula `Case` `expressionlist` corresponder a `testexpression` e não houver nenhuma instrução `Case Else`, o controle passará para a instrução a seguir `End Select`.</span><span class="sxs-lookup"><span data-stu-id="add8b-136">If no `Case` `expressionlist` clause matches `testexpression` and there is no `Case Else` statement, control passes to the statement following `End Select`.</span></span>  
  
 <span data-ttu-id="add8b-137">Você pode usar várias expressões ou intervalos em cada cláusula de `Case`.</span><span class="sxs-lookup"><span data-stu-id="add8b-137">You can use multiple expressions or ranges in each `Case` clause.</span></span> <span data-ttu-id="add8b-138">Por exemplo, a linha a seguir é válida.</span><span class="sxs-lookup"><span data-stu-id="add8b-138">For example, the following line is valid.</span></span>  
  
 `Case 1 To 4, 7 To 9, 11, 13, Is > maxNumber`  
  
> [!NOTE]
> <span data-ttu-id="add8b-139">A palavra-chave `Is` usada nas instruções `Case` e `Case Else` não é a mesma que o [operador is](../../../visual-basic/language-reference/operators/is-operator.md), que é usado para comparação de referência de objeto.</span><span class="sxs-lookup"><span data-stu-id="add8b-139">The `Is` keyword used in the `Case` and `Case Else` statements is not the same as the [Is Operator](../../../visual-basic/language-reference/operators/is-operator.md), which is used for object reference comparison.</span></span>  
  
 <span data-ttu-id="add8b-140">Você pode especificar intervalos e várias expressões para cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="add8b-140">You can specify ranges and multiple expressions for character strings.</span></span> <span data-ttu-id="add8b-141">No exemplo a seguir, `Case` corresponde a qualquer cadeia de caracteres que seja exatamente igual a "maçãs", tem um valor entre "nozes" e "sopa" em ordem alfabética ou contém exatamente o mesmo valor que o valor atual de `testItem`.</span><span class="sxs-lookup"><span data-stu-id="add8b-141">In the following example, `Case` matches any string that is exactly equal to "apples", has a value between "nuts" and "soup" in alphabetical order, or contains the exact same value as the current value of `testItem`.</span></span>  
  
 `Case "apples", "nuts" To "soup", testItem`  
  
 <span data-ttu-id="add8b-142">A configuração de `Option Compare` pode afetar comparações de cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="add8b-142">The setting of `Option Compare` can affect string comparisons.</span></span> <span data-ttu-id="add8b-143">Em `Option Compare Text`, as cadeias de caracteres "maçãs" e "maçãs" são comparadas como iguais, mas em `Option Compare Binary`, elas não.</span><span class="sxs-lookup"><span data-stu-id="add8b-143">Under `Option Compare Text`, the strings "Apples" and "apples" compare as equal, but under `Option Compare Binary`, they do not.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="add8b-144">Uma instrução `Case` com várias cláusulas pode exibir o comportamento conhecido como *curto-circuito*.</span><span class="sxs-lookup"><span data-stu-id="add8b-144">A `Case` statement with multiple clauses can exhibit behavior known as *short-circuiting*.</span></span> <span data-ttu-id="add8b-145">Visual Basic avalia as cláusulas da esquerda para a direita e, se uma delas produz uma correspondência com `testexpression`, as cláusulas restantes não são avaliadas.</span><span class="sxs-lookup"><span data-stu-id="add8b-145">Visual Basic evaluates the clauses from left to right, and if one produces a match with `testexpression`, the remaining clauses are not evaluated.</span></span> <span data-ttu-id="add8b-146">O curto-circuito pode melhorar o desempenho, mas pode produzir resultados inesperados se você espera que todas as expressões em `expressionlist` sejam avaliadas.</span><span class="sxs-lookup"><span data-stu-id="add8b-146">Short-circuiting can improve performance, but it can produce unexpected results if you are expecting every expression in `expressionlist` to be evaluated.</span></span> <span data-ttu-id="add8b-147">Para obter mais informações sobre o curto-circuito, consulte [expressões booleanas](../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="add8b-147">For more information on short-circuiting, see [Boolean Expressions](../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md).</span></span>  
  
 <span data-ttu-id="add8b-148">Se o código dentro de um bloco de instrução `Case` ou `Case Else` não precisar executar nenhuma das instruções no bloco, ele poderá sair do bloco usando a instrução `Exit Select`.</span><span class="sxs-lookup"><span data-stu-id="add8b-148">If the code within a `Case` or `Case Else` statement block does not need to run any more of the statements in the block, it can exit the block by using the `Exit Select` statement.</span></span> <span data-ttu-id="add8b-149">Isso transfere o controle imediatamente para a instrução a seguir `End Select`.</span><span class="sxs-lookup"><span data-stu-id="add8b-149">This transfers control immediately to the statement following `End Select`.</span></span>  
  
 <span data-ttu-id="add8b-150">`Select Case` construções podem ser aninhadas.</span><span class="sxs-lookup"><span data-stu-id="add8b-150">`Select Case` constructions can be nested.</span></span> <span data-ttu-id="add8b-151">Cada construção de `Select Case` aninhada deve ter uma instrução `End Select` correspondente e deve estar completamente contida em um único `Case` ou bloco de instrução `Case Else` da construção de `Select Case` externa na qual está aninhado.</span><span class="sxs-lookup"><span data-stu-id="add8b-151">Each nested `Select Case` construction must have a matching `End Select` statement and must be completely contained within a single `Case` or `Case Else` statement block of the outer `Select Case` construction within which it is nested.</span></span>  
  
## <a name="example"></a><span data-ttu-id="add8b-152">Exemplo</span><span class="sxs-lookup"><span data-stu-id="add8b-152">Example</span></span>  
 <span data-ttu-id="add8b-153">O exemplo a seguir usa uma construção `Select Case` para gravar uma linha correspondente ao valor da variável `number`.</span><span class="sxs-lookup"><span data-stu-id="add8b-153">The following example uses a `Select Case` construction to write a line corresponding to the value of the variable `number`.</span></span> <span data-ttu-id="add8b-154">A segunda instrução `Case` contém o valor que corresponde ao valor atual de `number`, portanto, a instrução que grava "entre 6 e 8, inclusivo" é executada.</span><span class="sxs-lookup"><span data-stu-id="add8b-154">The second `Case` statement contains the value that matches the current value of `number`, so the statement that writes "Between 6 and 8, inclusive" runs.</span></span>  
  
 [!code-vb[VbVbalrStatements#54](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#54)]  
  
## <a name="see-also"></a><span data-ttu-id="add8b-155">Consulte também</span><span class="sxs-lookup"><span data-stu-id="add8b-155">See also</span></span>

- <xref:Microsoft.VisualBasic.Interaction.Choose%2A>
- [<span data-ttu-id="add8b-156">Instrução End</span><span class="sxs-lookup"><span data-stu-id="add8b-156">End Statement</span></span>](../../../visual-basic/language-reference/statements/end-statement.md)
- [<span data-ttu-id="add8b-157">Instrução If...Then...Else</span><span class="sxs-lookup"><span data-stu-id="add8b-157">If...Then...Else Statement</span></span>](../../../visual-basic/language-reference/statements/if-then-else-statement.md)
- [<span data-ttu-id="add8b-158">Instrução Option Compare</span><span class="sxs-lookup"><span data-stu-id="add8b-158">Option Compare Statement</span></span>](../../../visual-basic/language-reference/statements/option-compare-statement.md)
- [<span data-ttu-id="add8b-159">Instrução Exit</span><span class="sxs-lookup"><span data-stu-id="add8b-159">Exit Statement</span></span>](../../../visual-basic/language-reference/statements/exit-statement.md)
