---
title: Instrução Try...Catch...Finally (Visual Basic)
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: ''
ms.suite: ''
ms.technology:
- devlang-visual-basic
ms.topic: article
f1_keywords:
- vb.Try...Catch...Finally
- vb.when
- vb.Finally
- vb.Catch
- vb.Try
helpviewer_keywords:
- Try...Catch...Finally statements
- Try statement [Visual Basic]
- try-catch exception handling, Try...Catch...Finally statements
- error handling, while running code
- Try statement [Visual Basic], Try...Catch...Finally
- Finally keyword [Visual Basic], Try...Catch...Finally
- Catch statement [Visual Basic]
- When keyword [Visual Basic]
- Visual Basic code, handling errors while running
- structured exception handling, Try...Catch...Finally statements
ms.assetid: d6488026-ccb3-42b8-a810-0d97b9d6472b
caps.latest.revision: 69
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 701475d41d24ad89e6c83796f7cc5cd4b7802a32
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/26/2018
---
# <a name="trycatchfinally-statement-visual-basic"></a><span data-ttu-id="fa85a-102">Instrução Try...Catch...Finally (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="fa85a-102">Try...Catch...Finally Statement (Visual Basic)</span></span>
<span data-ttu-id="fa85a-103">Fornece uma maneira de lidar com alguns ou todos os erros possíveis que podem ocorrer em um determinado bloco de código, ainda assim, executar código.</span><span class="sxs-lookup"><span data-stu-id="fa85a-103">Provides a way to handle some or all possible errors that may occur in a given block of code, while still running code.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="fa85a-104">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="fa85a-104">Syntax</span></span>  
  
```  
Try  
    [ tryStatements ]  
    [ Exit Try ]  
[ Catch [ exception [ As type ] ] [ When expression ]  
    [ catchStatements ]  
    [ Exit Try ] ]  
[ Catch ... ]  
[ Finally  
    [ finallyStatements ] ]  
End Try  
```  
  
## <a name="parts"></a><span data-ttu-id="fa85a-105">Partes</span><span class="sxs-lookup"><span data-stu-id="fa85a-105">Parts</span></span>  
  
|<span data-ttu-id="fa85a-106">Termo</span><span class="sxs-lookup"><span data-stu-id="fa85a-106">Term</span></span>|<span data-ttu-id="fa85a-107">Definição</span><span class="sxs-lookup"><span data-stu-id="fa85a-107">Definition</span></span>|  
|---|---|  
|`tryStatements`|<span data-ttu-id="fa85a-108">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-108">Optional.</span></span> <span data-ttu-id="fa85a-109">Instrução (ões) onde pode ocorrer um erro.</span><span class="sxs-lookup"><span data-stu-id="fa85a-109">Statement(s) where an error can occur.</span></span> <span data-ttu-id="fa85a-110">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="fa85a-110">Can be a compound statement.</span></span>|  
|`Catch`|<span data-ttu-id="fa85a-111">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-111">Optional.</span></span> <span data-ttu-id="fa85a-112">Vários `Catch` blocos permitidos.</span><span class="sxs-lookup"><span data-stu-id="fa85a-112">Multiple `Catch` blocks permitted.</span></span> <span data-ttu-id="fa85a-113">Se ocorrer uma exceção ao processar o `Try` bloquear, cada `Catch` instrução é examinada em ordem textual para determinar se ele trata a exceção com `exception` que representa a exceção foi gerada.</span><span class="sxs-lookup"><span data-stu-id="fa85a-113">If an exception occurs when processing the `Try` block, each `Catch` statement is examined in textual order to determine whether it handles the exception, with `exception` representing the exception that has been thrown.</span></span>|  
|`exception`|<span data-ttu-id="fa85a-114">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-114">Optional.</span></span> <span data-ttu-id="fa85a-115">Qualquer nome de variável.</span><span class="sxs-lookup"><span data-stu-id="fa85a-115">Any variable name.</span></span> <span data-ttu-id="fa85a-116">O valor inicial de `exception` é o valor do erro gerado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-116">The initial value of `exception` is the value of the thrown error.</span></span> <span data-ttu-id="fa85a-117">Usado com `Catch` especificar o erro detectado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-117">Used with `Catch` to specify the error caught.</span></span> <span data-ttu-id="fa85a-118">Se omitido, o `Catch` instrução captura qualquer exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-118">If omitted, the `Catch` statement catches any exception.</span></span>|  
|`type`|<span data-ttu-id="fa85a-119">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-119">Optional.</span></span> <span data-ttu-id="fa85a-120">Especifica o tipo de filtro de classe.</span><span class="sxs-lookup"><span data-stu-id="fa85a-120">Specifies the type of class filter.</span></span> <span data-ttu-id="fa85a-121">Se o valor de `exception` é do tipo especificado pelo `type` ou de um tipo derivado, o identificador se torna associado ao objeto de exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-121">If the value of `exception` is of the type specified by `type` or of a derived type, the identifier becomes bound to the exception object.</span></span>|  
|`When`|<span data-ttu-id="fa85a-122">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-122">Optional.</span></span> <span data-ttu-id="fa85a-123">Um `Catch` instrução com uma `When` cláusula captura exceções somente quando `expression` é avaliada como `True`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-123">A `Catch` statement with a `When` clause catches exceptions only when `expression` evaluates to `True`.</span></span> <span data-ttu-id="fa85a-124">Um `When` cláusula é aplicada somente depois de verificar se o tipo da exceção, e `expression` podem se referir ao identificador que representa a exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-124">A `When` clause is applied only after checking the type of the exception, and `expression` may refer to the identifier representing the exception.</span></span>|  
|`expression`|<span data-ttu-id="fa85a-125">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-125">Optional.</span></span> <span data-ttu-id="fa85a-126">Deve ser implicitamente conversível em `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-126">Must be implicitly convertible to `Boolean`.</span></span> <span data-ttu-id="fa85a-127">Qualquer expressão que descreve um filtro genérico.</span><span class="sxs-lookup"><span data-stu-id="fa85a-127">Any expression that describes a generic filter.</span></span> <span data-ttu-id="fa85a-128">Normalmente usado para filtrar pelo número do erro.</span><span class="sxs-lookup"><span data-stu-id="fa85a-128">Typically used to filter by error number.</span></span> <span data-ttu-id="fa85a-129">Usado com `When` palavra-chave para especificar as circunstâncias em que o erro é capturado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-129">Used with `When` keyword to specify circumstances under which the error is caught.</span></span>|  
|`catchStatements`|<span data-ttu-id="fa85a-130">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-130">Optional.</span></span> <span data-ttu-id="fa85a-131">Instruções para manipular erros que ocorrem em associado `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-131">Statement(s) to handle errors that occur in the associated `Try` block.</span></span> <span data-ttu-id="fa85a-132">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="fa85a-132">Can be a compound statement.</span></span>|  
|`Exit Try`|<span data-ttu-id="fa85a-133">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-133">Optional.</span></span> <span data-ttu-id="fa85a-134">Palavra-chave que quebras do `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="fa85a-134">Keyword that breaks out of the `Try...Catch...Finally` structure.</span></span> <span data-ttu-id="fa85a-135">A execução continua com o código que segue imediatamente o `End Try` instrução.</span><span class="sxs-lookup"><span data-stu-id="fa85a-135">Execution resumes with the code immediately following the `End Try` statement.</span></span> <span data-ttu-id="fa85a-136">O `Finally` instrução ainda será executada.</span><span class="sxs-lookup"><span data-stu-id="fa85a-136">The `Finally` statement will still be executed.</span></span> <span data-ttu-id="fa85a-137">Não é permitido em `Finally` blocos.</span><span class="sxs-lookup"><span data-stu-id="fa85a-137">Not allowed in `Finally` blocks.</span></span>|  
|`Finally`|<span data-ttu-id="fa85a-138">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-138">Optional.</span></span> <span data-ttu-id="fa85a-139">Um `Finally` bloco sempre é executado quando a execução deixar qualquer parte do `Try...Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="fa85a-139">A `Finally` block is always executed when execution leaves any part of the `Try...Catch` statement.</span></span>|  
|`finallyStatements`|<span data-ttu-id="fa85a-140">Opcional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-140">Optional.</span></span> <span data-ttu-id="fa85a-141">Instruções que são executadas depois que todos os outros processamentos de erros.</span><span class="sxs-lookup"><span data-stu-id="fa85a-141">Statement(s) that are executed after all other error processing has occurred.</span></span>|  
|`End Try`|<span data-ttu-id="fa85a-142">Encerra o `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="fa85a-142">Terminates the `Try...Catch...Finally` structure.</span></span>|  
  
## <a name="remarks"></a><span data-ttu-id="fa85a-143">Comentários</span><span class="sxs-lookup"><span data-stu-id="fa85a-143">Remarks</span></span>  
 <span data-ttu-id="fa85a-144">Se você espera que uma exceção específica pode ocorrer durante uma determinada seção de código, coloque o código em um `Try` bloquear e usar um `Catch` bloco para manter o controle e lidar com a exceção se ele ocorre.</span><span class="sxs-lookup"><span data-stu-id="fa85a-144">If you expect that a particular exception might occur during a particular section of code, put the code in a `Try` block and use a `Catch` block to retain control and handle the exception if it occurs.</span></span>  
  
 <span data-ttu-id="fa85a-145">Um `Try…Catch` consiste em uma `Try` bloco seguido por um ou mais `Catch` cláusulas, que especificam os manipuladores para diversas exceções.</span><span class="sxs-lookup"><span data-stu-id="fa85a-145">A `Try…Catch` statement consists of a `Try` block followed by one or more `Catch` clauses, which specify handlers for various exceptions.</span></span> <span data-ttu-id="fa85a-146">Quando uma exceção será lançada uma `Try` bloco, Visual Basic procura o `Catch` instrução que manipula a exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-146">When an exception is thrown in a `Try` block, Visual Basic looks for the `Catch` statement that handles the exception.</span></span> <span data-ttu-id="fa85a-147">Se uma correspondência `Catch` instrução não for encontrada, Visual Basic examina o método que chamou o método atual, e assim por diante para cima a pilha de chamadas.</span><span class="sxs-lookup"><span data-stu-id="fa85a-147">If a matching `Catch` statement is not found, Visual Basic examines the method that called the current method, and so on up the call stack.</span></span> <span data-ttu-id="fa85a-148">Se nenhum `Catch` bloco for encontrado, o Visual Basic exibe uma mensagem de exceção sem tratamento ao usuário e interrompe a execução do programa.</span><span class="sxs-lookup"><span data-stu-id="fa85a-148">If no `Catch` block is found, Visual Basic displays an unhandled exception message to the user and stops execution of the program.</span></span>  
  
 <span data-ttu-id="fa85a-149">Você pode usar mais de um `Catch` instrução em um `Try…Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="fa85a-149">You can use more than one `Catch` statement in a `Try…Catch` statement.</span></span> <span data-ttu-id="fa85a-150">Se você fizer isso, a ordem dos `Catch` cláusulas é significativo porque eles são examinados em ordem.</span><span class="sxs-lookup"><span data-stu-id="fa85a-150">If you do this, the order of the `Catch` clauses is significant because they are examined in order.</span></span> <span data-ttu-id="fa85a-151">Capture as exceções mais específicas antes das menos específicas.</span><span class="sxs-lookup"><span data-stu-id="fa85a-151">Catch the more specific exceptions before the less specific ones.</span></span>  
  
 <span data-ttu-id="fa85a-152">O seguinte `Catch` condições de instrução são menos específico e irá capturar todos exceções que derivam de <xref:System.Exception> classe.</span><span class="sxs-lookup"><span data-stu-id="fa85a-152">The following `Catch` statement conditions are the least specific, and will catch all exceptions that derive from the <xref:System.Exception> class.</span></span> <span data-ttu-id="fa85a-153">Você normalmente deve usar uma dessas variações como a última `Catch` bloco no `Try...Catch...Finally` estrutura, depois de capturar todas as exceções específicas que você espera.</span><span class="sxs-lookup"><span data-stu-id="fa85a-153">You should ordinarily use one of these variations as the last `Catch` block in the `Try...Catch...Finally` structure, after catching all the specific exceptions you expect.</span></span> <span data-ttu-id="fa85a-154">Fluxo de controle nunca pode alcançar um `Catch` bloco que segue uma dessas variações.</span><span class="sxs-lookup"><span data-stu-id="fa85a-154">Control flow can never reach a `Catch` block that follows either of these variations.</span></span>  
  
-   <span data-ttu-id="fa85a-155">O `type` é `Exception`, por exemplo: `Catch ex As Exception`</span><span class="sxs-lookup"><span data-stu-id="fa85a-155">The `type` is `Exception`, for example: `Catch ex As Exception`</span></span>  
  
-   <span data-ttu-id="fa85a-156">A instrução não tiver nenhuma `exception` variável, por exemplo: `Catch`</span><span class="sxs-lookup"><span data-stu-id="fa85a-156">The statement has no `exception` variable, for example: `Catch`</span></span>  
  
 <span data-ttu-id="fa85a-157">Quando um `Try…Catch…Finally` instrução está aninhada em outro `Try` bloco, Visual Basic primeiro examina cada `Catch` instrução em mais interno `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-157">When a `Try…Catch…Finally` statement is nested in another `Try` block, Visual Basic first examines each `Catch` statement in the innermost `Try` block.</span></span> <span data-ttu-id="fa85a-158">Se nenhuma correspondência `Catch` instrução for encontrada, a pesquisa passa para o `Catch` instruções de externa `Try…Catch…Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-158">If no matching `Catch` statement is found, the search proceeds to the `Catch` statements of the outer `Try…Catch…Finally` block.</span></span>  
  
 <span data-ttu-id="fa85a-159">Variáveis locais de um `Try` bloco não estão disponíveis em um `Catch` bloquear porque eles são os blocos separados.</span><span class="sxs-lookup"><span data-stu-id="fa85a-159">Local variables from a `Try` block are not available in a `Catch` block because they are separate blocks.</span></span> <span data-ttu-id="fa85a-160">Se você quiser usar uma variável em mais de um bloco, declare a variável de fora de `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="fa85a-160">If you want to use a variable in more than one block, declare the variable outside the `Try...Catch...Finally` structure.</span></span>  
  
> [!TIP]
>  <span data-ttu-id="fa85a-161">O `Try…Catch…Finally` instrução está disponível como um trecho de código IntelliSense.</span><span class="sxs-lookup"><span data-stu-id="fa85a-161">The `Try…Catch…Finally` statement is available as an IntelliSense code snippet.</span></span> <span data-ttu-id="fa85a-162">No Gerenciador de trechos de código, expanda **padrões de código - se, para cada um, Try Catch, propriedade, etc**e, em seguida, **tratamento de erros (exceções)**.</span><span class="sxs-lookup"><span data-stu-id="fa85a-162">In the Code Snippets Manager, expand **Code Patterns - If, For Each, Try Catch, Property, etc**, and then **Error Handling (Exceptions)**.</span></span> <span data-ttu-id="fa85a-163">Para obter mais informações, consulte [Trechos de Código](/visualstudio/ide/code-snippets).</span><span class="sxs-lookup"><span data-stu-id="fa85a-163">For more information, see [Code Snippets](/visualstudio/ide/code-snippets).</span></span>  
  
## <a name="finally-block"></a><span data-ttu-id="fa85a-164">Bloco Finally</span><span class="sxs-lookup"><span data-stu-id="fa85a-164">Finally Block</span></span>  
 <span data-ttu-id="fa85a-165">Se você tiver uma ou mais instruções que devem ser executadas antes de sair do `Try` estrutura, use um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-165">If you have one or more statements that must run before you exit the `Try` structure, use a `Finally` block.</span></span> <span data-ttu-id="fa85a-166">O controle passará para o `Finally` bloquear logo antes do `Try…Catch` estrutura.</span><span class="sxs-lookup"><span data-stu-id="fa85a-166">Control passes to the `Finally` block just before it passes out of the `Try…Catch` structure.</span></span> <span data-ttu-id="fa85a-167">Isso é verdadeiro mesmo se uma exceção ocorre em qualquer lugar dentro do `Try` estrutura.</span><span class="sxs-lookup"><span data-stu-id="fa85a-167">This is true even if an exception occurs anywhere inside the `Try` structure.</span></span>  
  
 <span data-ttu-id="fa85a-168">Um `Finally` bloco é útil para executar qualquer código que deve executar mesmo se houver uma exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-168">A `Finally` block is useful for running any code that must execute even if there is an exception.</span></span> <span data-ttu-id="fa85a-169">O controle é passado para o `Finally` bloco, independentemente de como o `Try...Catch` bloquear sai.</span><span class="sxs-lookup"><span data-stu-id="fa85a-169">Control is passed to the `Finally` block regardless of how the `Try...Catch` block exits.</span></span>  
  
 <span data-ttu-id="fa85a-170">O código em um `Finally` bloco é executado, mesmo se seu código encontra uma `Return` instrução em um `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-170">The code in a `Finally` block runs even if your code encounters a `Return` statement in a `Try` or `Catch` block.</span></span> <span data-ttu-id="fa85a-171">Controle não passa de uma `Try` ou `Catch` bloquear correspondente `Finally` bloquear nos seguintes casos:</span><span class="sxs-lookup"><span data-stu-id="fa85a-171">Control does not pass from a `Try` or `Catch` block to the corresponding `Finally` block in the following cases:</span></span>  
  
-   <span data-ttu-id="fa85a-172">Um [instrução End](../../../visual-basic/language-reference/statements/end-statement.md) é encontrado na `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-172">An [End Statement](../../../visual-basic/language-reference/statements/end-statement.md) is encountered in the `Try` or `Catch` block.</span></span>  
  
-   <span data-ttu-id="fa85a-173">Um <xref:System.StackOverflowException> é gerada no `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-173">A <xref:System.StackOverflowException> is thrown in the `Try` or `Catch` block.</span></span>  
  
 <span data-ttu-id="fa85a-174">Não é válido para transferir explicitamente a execução em um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-174">It is not valid to explicitly transfer execution into a `Finally` block.</span></span> <span data-ttu-id="fa85a-175">Transferir a execução de um `Finally` bloco não for válido, exceto por meio de uma exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-175">Transferring execution out of a `Finally` block is not valid, except through an exception.</span></span>  
  
 <span data-ttu-id="fa85a-176">Se um `Try` instrução não contém pelo menos um `Catch` bloco, ele deve conter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-176">If a `Try` statement does not contain at least one `Catch` block, it must contain a `Finally` block.</span></span>  
  
> [!TIP]
>  <span data-ttu-id="fa85a-177">Se você não precisa capturar exceções específicas, o `Using` instrução se comporta como uma `Try…Finally` bloco e garante o descarte dos recursos, independentemente de como você sai do bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-177">If you do not have to catch specific exceptions, the `Using` statement behaves like a `Try…Finally` block, and guarantees disposal of the resources, regardless of how you exit the block.</span></span> <span data-ttu-id="fa85a-178">Isso é verdadeiro mesmo com uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="fa85a-178">This is true even with an unhandled exception.</span></span> <span data-ttu-id="fa85a-179">Para obter mais informações, consulte [Instrução using](../../../visual-basic/language-reference/statements/using-statement.md).</span><span class="sxs-lookup"><span data-stu-id="fa85a-179">For more information, see [Using Statement](../../../visual-basic/language-reference/statements/using-statement.md).</span></span>  
  
## <a name="exception-argument"></a><span data-ttu-id="fa85a-180">Argumento de exceção</span><span class="sxs-lookup"><span data-stu-id="fa85a-180">Exception Argument</span></span>  
 <span data-ttu-id="fa85a-181">O `Catch` bloco `exception` argumento é uma instância do <xref:System.Exception> classe ou uma classe que deriva de `Exception` classe.</span><span class="sxs-lookup"><span data-stu-id="fa85a-181">The `Catch` block `exception` argument is an instance of the <xref:System.Exception> class or a class that derives from the `Exception` class.</span></span> <span data-ttu-id="fa85a-182">O `Exception` instância da classe corresponde ao erro que ocorreu no `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-182">The `Exception` class instance corresponds to the error that occurred in the `Try` block.</span></span>  
  
 <span data-ttu-id="fa85a-183">As propriedades do `Exception` ajuda a identificar a causa e o local de uma exceção do objeto.</span><span class="sxs-lookup"><span data-stu-id="fa85a-183">The properties of the `Exception` object help to identify the cause and location of an exception.</span></span> <span data-ttu-id="fa85a-184">Por exemplo, o <xref:System.Exception.StackTrace%2A> propriedade lista os métodos chamados que conduziram à exceção, ajudando você a identificar onde o erro ocorreu no código.</span><span class="sxs-lookup"><span data-stu-id="fa85a-184">For example, the <xref:System.Exception.StackTrace%2A> property lists the called methods that led to the exception, helping you find where the error occurred in the code.</span></span> <span data-ttu-id="fa85a-185"><xref:System.Exception.Message%2A> Retorna uma mensagem que descreve a exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-185"><xref:System.Exception.Message%2A> returns a message that describes the exception.</span></span> <span data-ttu-id="fa85a-186"><xref:System.Exception.HelpLink%2A> Retorna um link para um arquivo de ajuda associado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-186"><xref:System.Exception.HelpLink%2A> returns a link to an associated Help file.</span></span> <span data-ttu-id="fa85a-187"><xref:System.Exception.InnerException%2A> Retorna o `Exception` objeto que causou a exceção atual ou retorna `Nothing` se não houver nenhum original `Exception`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-187"><xref:System.Exception.InnerException%2A> returns the `Exception` object that caused the current exception, or it returns `Nothing` if there is no original `Exception`.</span></span>  
  
## <a name="considerations-when-using-a-trycatch-statement"></a><span data-ttu-id="fa85a-188">Considerações ao usar um bloco Try... Instrução catch</span><span class="sxs-lookup"><span data-stu-id="fa85a-188">Considerations When Using a Try…Catch Statement</span></span>  
 <span data-ttu-id="fa85a-189">Use um `Try…Catch` instrução somente para indicar a ocorrência de eventos de programa incomuns ou inesperados.</span><span class="sxs-lookup"><span data-stu-id="fa85a-189">Use a `Try…Catch` statement only to signal the occurrence of unusual or unanticipated program events.</span></span> <span data-ttu-id="fa85a-190">Razões para isso incluem o seguinte:</span><span class="sxs-lookup"><span data-stu-id="fa85a-190">Reasons for this include the following:</span></span>  
  
-   <span data-ttu-id="fa85a-191">Capturando exceções em tempo de execução cria uma sobrecarga adicional e provavelmente será mais lento do que a pré-verificação de para evitar exceções.</span><span class="sxs-lookup"><span data-stu-id="fa85a-191">Catching exceptions at runtime creates additional overhead, and is likely to be slower than pre-checking to avoid exceptions.</span></span>  
  
-   <span data-ttu-id="fa85a-192">Se um `Catch` bloco não é manipulado corretamente, a exceção não pode ser relatada corretamente para os usuários.</span><span class="sxs-lookup"><span data-stu-id="fa85a-192">If a `Catch` block is not handled correctly, the exception might not be reported correctly to users.</span></span>  
  
-   <span data-ttu-id="fa85a-193">Tratamento de exceção faz com que um programa mais complexas.</span><span class="sxs-lookup"><span data-stu-id="fa85a-193">Exception handling makes a program more complex.</span></span>  
  
 <span data-ttu-id="fa85a-194">Não é necessário sempre um `Try…Catch` instrução para verificar se há uma condição que é provável de ocorrer.</span><span class="sxs-lookup"><span data-stu-id="fa85a-194">You do not always need a `Try…Catch` statement to check for a condition that is likely to occur.</span></span> <span data-ttu-id="fa85a-195">O exemplo a seguir verifica se um arquivo existe antes de tentar abri-lo.</span><span class="sxs-lookup"><span data-stu-id="fa85a-195">The following example checks whether a file exists before trying to open it.</span></span> <span data-ttu-id="fa85a-196">Isso reduz a necessidade de capturar uma exceção gerada pelo <xref:System.IO.File.OpenText%2A> método.</span><span class="sxs-lookup"><span data-stu-id="fa85a-196">This reduces the need for catching an exception thrown by the <xref:System.IO.File.OpenText%2A> method.</span></span>  
  
 [!code-vb[VbVbalrStatements#94](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_1.vb)]  
  
 <span data-ttu-id="fa85a-197">Certifique-se de que o código em `Catch` blocos corretamente podem relatar exceções aos usuários, seja por meio de mensagens apropriadas ou de log de thread-safe.</span><span class="sxs-lookup"><span data-stu-id="fa85a-197">Ensure that code in `Catch` blocks can properly report exceptions to users, whether through thread-safe logging or appropriate messages.</span></span> <span data-ttu-id="fa85a-198">Caso contrário, as exceções podem permanecer desconhecidas.</span><span class="sxs-lookup"><span data-stu-id="fa85a-198">Otherwise, exceptions might remain unknown.</span></span>  
  
## <a name="async-methods"></a><span data-ttu-id="fa85a-199">Métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="fa85a-199">Async Methods</span></span>  
 <span data-ttu-id="fa85a-200">Se você marcar um método com o [Async](../../../visual-basic/language-reference/modifiers/async.md) modificador, você pode usar o [Await](../../../visual-basic/language-reference/operators/await-operator.md) operador no método.</span><span class="sxs-lookup"><span data-stu-id="fa85a-200">If you mark a method with the [Async](../../../visual-basic/language-reference/modifiers/async.md) modifier, you can use the [Await](../../../visual-basic/language-reference/operators/await-operator.md) operator in the method.</span></span> <span data-ttu-id="fa85a-201">Uma instrução com o `Await` operador suspende a execução do método até que a tarefa em espera é concluída.</span><span class="sxs-lookup"><span data-stu-id="fa85a-201">A statement with the `Await` operator suspends execution of the method until the awaited task completes.</span></span> <span data-ttu-id="fa85a-202">A tarefa representa um trabalho em andamento.</span><span class="sxs-lookup"><span data-stu-id="fa85a-202">The task represents ongoing work.</span></span> <span data-ttu-id="fa85a-203">Quando a tarefa que está associada com o `Await` operador termina, retoma a execução no mesmo método.</span><span class="sxs-lookup"><span data-stu-id="fa85a-203">When the task that's associated with the `Await` operator finishes, execution resumes in the same method.</span></span> <span data-ttu-id="fa85a-204">Para obter mais informações, consulte [fluxo de controle em programas assíncronos](../../../visual-basic/programming-guide/concepts/async/control-flow-in-async-programs.md).</span><span class="sxs-lookup"><span data-stu-id="fa85a-204">For more information, see [Control Flow in Async Programs](../../../visual-basic/programming-guide/concepts/async/control-flow-in-async-programs.md).</span></span>  
  
 <span data-ttu-id="fa85a-205">Uma tarefa retornada por um método assíncrono pode terminar em um estado de falha, indicando que ela foi concluída devido a uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="fa85a-205">A task returned by an Async method may end in a faulted state, indicating that it completed due to an unhandled exception.</span></span> <span data-ttu-id="fa85a-206">Uma tarefa também pode terminar em um estado cancelado, o que resulta em um `OperationCanceledException` que está sendo lançada fora a expressão await.</span><span class="sxs-lookup"><span data-stu-id="fa85a-206">A task may also end in a canceled state, which results in an `OperationCanceledException` being thrown out of the await expression.</span></span> <span data-ttu-id="fa85a-207">Para capturar qualquer tipo de exceção, coloque o `Await` expressão associada com a tarefa em um `Try` bloquear e capturar a exceção no `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-207">To catch either type of exception, place the `Await` expression that's associated with the task in a `Try` block, and catch the exception in the `Catch` block.</span></span> <span data-ttu-id="fa85a-208">Um exemplo é fornecido neste tópico.</span><span class="sxs-lookup"><span data-stu-id="fa85a-208">An example is provided later in this topic.</span></span>  
  
 <span data-ttu-id="fa85a-209">Uma tarefa pode ser em um estado de falha porque várias exceções foram responsáveis por seu com falha.</span><span class="sxs-lookup"><span data-stu-id="fa85a-209">A task can be in a faulted state because multiple exceptions were responsible for its faulting.</span></span> <span data-ttu-id="fa85a-210">Por exemplo, a tarefa pode ser o resultado de uma chamada para <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="fa85a-210">For example, the task might be the result of a call to <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="fa85a-211">Quando você espera uma tarefa, a exceção capturada é apenas uma das exceções, e você não pode prever qual exceção será capturada.</span><span class="sxs-lookup"><span data-stu-id="fa85a-211">When you await such a task, the caught exception is only one of the exceptions, and you can't predict which exception will be caught.</span></span> <span data-ttu-id="fa85a-212">Um exemplo é fornecido neste tópico.</span><span class="sxs-lookup"><span data-stu-id="fa85a-212">An example is provided later in this topic.</span></span>  
  
 <span data-ttu-id="fa85a-213">Um `Await` expressão não pode estar dentro de um `Catch` bloco ou `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-213">An `Await` expression can't be inside a `Catch` block or `Finally` block.</span></span>  
  
## <a name="iterators"></a><span data-ttu-id="fa85a-214">Iterators</span><span class="sxs-lookup"><span data-stu-id="fa85a-214">Iterators</span></span>  
 <span data-ttu-id="fa85a-215">Uma função iterator ou `Get` acessador executa uma iteração personalizada em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-215">An iterator function or `Get` accessor performs a custom iteration over a collection.</span></span> <span data-ttu-id="fa85a-216">Um iterador usa um [Yield](../../../visual-basic/language-reference/statements/yield-statement.md) instrução para retornar cada elemento de uma coleção em uma hora.</span><span class="sxs-lookup"><span data-stu-id="fa85a-216">An iterator uses a [Yield](../../../visual-basic/language-reference/statements/yield-statement.md) statement to return each element of the collection one at a time.</span></span> <span data-ttu-id="fa85a-217">Chamar uma função iterator usando um [para cada um... Próxima instrução](../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="fa85a-217">You call an iterator function by using a [For Each...Next Statement](../../../visual-basic/language-reference/statements/for-each-next-statement.md).</span></span>  
  
 <span data-ttu-id="fa85a-218">Um `Yield` instrução pode estar dentro de um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-218">A `Yield` statement can be inside a `Try` block.</span></span> <span data-ttu-id="fa85a-219">Um `Try` bloco que contém um `Yield` instrução pode ter `Catch` bloqueia e pode ter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-219">A `Try` block that contains a `Yield` statement can have `Catch` blocks, and can have a `Finally` block.</span></span> <span data-ttu-id="fa85a-220">Consulte a seção "Tente blocos no Visual Basic" [iteradores](../../programming-guide/concepts/iterators.md) para obter um exemplo.</span><span class="sxs-lookup"><span data-stu-id="fa85a-220">See the "Try Blocks in Visual Basic" section of [Iterators](../../programming-guide/concepts/iterators.md) for an example.</span></span>  
  
 <span data-ttu-id="fa85a-221">Um `Yield` instrução não pode estar dentro de um `Catch` blocos ou um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-221">A `Yield` statement cannot be inside a `Catch` block or a `Finally` block.</span></span>  
  
 <span data-ttu-id="fa85a-222">Se o `For Each` corpo (fora da função de iterador) gera uma exceção, uma `Catch` bloco na função iterador não é executado, mas um `Finally` bloco na função de iterador é executado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-222">If the `For Each` body (outside of the iterator function) throws an exception, a `Catch` block in the iterator function is not executed, but a `Finally` block in the iterator function is executed.</span></span> <span data-ttu-id="fa85a-223">Um `Catch` blocos dentro de uma função iterator captura somente exceções que ocorrem dentro da função de iterador.</span><span class="sxs-lookup"><span data-stu-id="fa85a-223">A `Catch` block inside an iterator function catches only exceptions that occur inside the iterator function.</span></span>  
  
## <a name="partial-trust-situations"></a><span data-ttu-id="fa85a-224">Situações de confiança parcial</span><span class="sxs-lookup"><span data-stu-id="fa85a-224">Partial-Trust Situations</span></span>  
 <span data-ttu-id="fa85a-225">Em situações de confiança parcial, como um aplicativo hospedado em um compartilhamento de rede, `Try...Catch...Finally` não capturará exceções de segurança que ocorrem antes do método que contém a chamada é invocado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-225">In partial-trust situations, such as an application hosted on a network share, `Try...Catch...Finally` does not catch security exceptions that occur before the method that contains the call is invoked.</span></span> <span data-ttu-id="fa85a-226">O exemplo a seguir, quando você coloca em um compartilhamento de servidor e execute a partir daí, gera o erro "System.Security.SecurityException: falha de solicitação."</span><span class="sxs-lookup"><span data-stu-id="fa85a-226">The following example, when you put it on a server share and run from there, produces the error "System.Security.SecurityException: Request Failed."</span></span> <span data-ttu-id="fa85a-227">Para obter mais informações sobre exceções de segurança, consulte o <xref:System.Security.SecurityException> classe.</span><span class="sxs-lookup"><span data-stu-id="fa85a-227">For more information about security exceptions, see the <xref:System.Security.SecurityException> class.</span></span>  
  
 [!code-vb[VbVbalrStatements#85](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_2.vb)]  
  
 <span data-ttu-id="fa85a-228">Em tal situação confiança parcial, você precisa colocar o `Process.Start` instrução em uma função `Sub`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-228">In such a partial-trust situation, you have to put the `Process.Start` statement in a separate `Sub`.</span></span> <span data-ttu-id="fa85a-229">A chamada inicial para o `Sub` falhará.</span><span class="sxs-lookup"><span data-stu-id="fa85a-229">The initial call to the `Sub` will fail.</span></span> <span data-ttu-id="fa85a-230">Isso permite que `Try...Catch` para capturar antes de `Sub` que contém `Process.Start` é iniciado e a exceção de segurança é produzido.</span><span class="sxs-lookup"><span data-stu-id="fa85a-230">This enables `Try...Catch` to catch it before the `Sub` that contains `Process.Start` is started and the security exception produced.</span></span>  
  
## <a name="example"></a><span data-ttu-id="fa85a-231">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-231">Example</span></span>  
 <span data-ttu-id="fa85a-232">O exemplo a seguir ilustra a estrutura do `Try...Catch...Finally` instrução.</span><span class="sxs-lookup"><span data-stu-id="fa85a-232">The following example illustrates the structure of the `Try...Catch...Finally` statement.</span></span>  
  
 [!code-vb[VbVbalrStatements#86](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_3.vb)]  
  
## <a name="example"></a><span data-ttu-id="fa85a-233">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-233">Example</span></span>  
 <span data-ttu-id="fa85a-234">No exemplo a seguir, o `CreateException` método lança um `NullReferenceException`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-234">In the following example, the `CreateException` method throws a `NullReferenceException`.</span></span> <span data-ttu-id="fa85a-235">O código que gera a exceção não está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-235">The code that generates the exception is not in a `Try` block.</span></span> <span data-ttu-id="fa85a-236">Portanto, o `CreateException` método não lida com a exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-236">Therefore, the `CreateException` method does not handle the exception.</span></span> <span data-ttu-id="fa85a-237">O `RunSample` método lidar com a exceção porque a chamada para o `CreateException` método está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-237">The `RunSample` method does handle the exception because the call to the `CreateException` method is in a `Try` block.</span></span>  
  
 <span data-ttu-id="fa85a-238">O exemplo inclui `Catch` instruções para vários tipos de exceções, ordenado do mais específico para o mais geral.</span><span class="sxs-lookup"><span data-stu-id="fa85a-238">The example includes `Catch` statements for several types of exceptions, ordered from the most specific to the most general.</span></span>  
  
 [!code-vb[VbVbalrStatements#91](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_4.vb)]  
  
## <a name="example"></a><span data-ttu-id="fa85a-239">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-239">Example</span></span>  
 <span data-ttu-id="fa85a-240">O exemplo a seguir mostra como usar um `Catch When` instrução para filtrar em uma expressão condicional.</span><span class="sxs-lookup"><span data-stu-id="fa85a-240">The following example shows how to use a `Catch When` statement to filter on a conditional expression.</span></span> <span data-ttu-id="fa85a-241">Se a expressão condicional for avaliada como `True`, o código de `Catch` bloco é executado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-241">If the conditional expression evaluates to `True`, the code in the `Catch` block runs.</span></span>  
  
 [!code-vb[VbVbalrStatements#92](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_5.vb)]  
  
## <a name="example"></a><span data-ttu-id="fa85a-242">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-242">Example</span></span>  
 <span data-ttu-id="fa85a-243">O exemplo a seguir tem um `Try…Catch` instrução que está contida em uma `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-243">The following example has a `Try…Catch` statement that is contained in a `Try` block.</span></span> <span data-ttu-id="fa85a-244">Interna `Catch` bloco lançar uma exceção que tem seu `InnerException` propriedade definida para a exceção original.</span><span class="sxs-lookup"><span data-stu-id="fa85a-244">The inner `Catch` block throws an exception that has its `InnerException` property set to the original exception.</span></span> <span data-ttu-id="fa85a-245">Externa `Catch` bloco relata a própria exceção e a exceção interna.</span><span class="sxs-lookup"><span data-stu-id="fa85a-245">The outer `Catch` block reports its own exception and the inner exception.</span></span>  
  
 [!code-vb[VbVbalrStatements#93](../../../visual-basic/language-reference/error-messages/codesnippet/VisualBasic/try-catch-finally-statement_6.vb)]  
  
## <a name="example"></a><span data-ttu-id="fa85a-246">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-246">Example</span></span>  
 <span data-ttu-id="fa85a-247">O exemplo a seguir ilustra o tratamento de exceção para métodos assíncronos.</span><span class="sxs-lookup"><span data-stu-id="fa85a-247">The following example illustrates exception handling for async methods.</span></span> <span data-ttu-id="fa85a-248">Para capturar uma exceção se aplica a uma tarefa assíncrona, o `Await` expressão está em um `Try` detectado no bloco do chamador e a exceção a `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="fa85a-248">To catch an exception that applies to an async task, the `Await` expression is in a `Try` block of the caller, and the exception is caught in the `Catch` block.</span></span>  
  
 <span data-ttu-id="fa85a-249">Remova a marca de comentário da linha `Throw New Exception` no exemplo para demonstrar o tratamento de exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-249">Uncomment the `Throw New Exception` line in the example to demonstrate exception handling.</span></span> <span data-ttu-id="fa85a-250">A exceção é detectada no `Catch` bloquear, a tarefa `IsFaulted` está definida como `True`e a tarefa `Exception.InnerException` propriedade é definida para a exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-250">The exception is caught in the `Catch` block, the task's `IsFaulted` property is set to `True`, and the task's `Exception.InnerException` property is set to the exception.</span></span>  
  
 <span data-ttu-id="fa85a-251">Remova a marca de comentário da linha `Throw New OperationCancelledException` para demonstrar o que acontece quando você cancela um processo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="fa85a-251">Uncomment the `Throw New OperationCancelledException` line to demonstrate what happens when you cancel an asynchronous process.</span></span> <span data-ttu-id="fa85a-252">A exceção é detectada no `Catch` bloco e a tarefa `IsCanceled` está definida como `True`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-252">The exception is caught in the `Catch` block, and the task's `IsCanceled` property is set to `True`.</span></span> <span data-ttu-id="fa85a-253">No entanto, em algumas condições que não se aplicam a este exemplo `IsFaulted` é definido como `True` e `IsCanceled` é definido como `False`.</span><span class="sxs-lookup"><span data-stu-id="fa85a-253">However, under some conditions that don't apply to this example, `IsFaulted` is set to `True` and `IsCanceled` is set to `False`.</span></span>  
  
 [!code-vb[csAsyncExceptions#1](../../../csharp/language-reference/keywords/codesnippet/VisualBasic/try-catch-finally-statement_7.vb)]  
  
## <a name="example"></a><span data-ttu-id="fa85a-254">Exemplo</span><span class="sxs-lookup"><span data-stu-id="fa85a-254">Example</span></span>  
 <span data-ttu-id="fa85a-255">O exemplo a seguir ilustra a manipulação de exceção em que várias tarefas podem resultar em várias exceções.</span><span class="sxs-lookup"><span data-stu-id="fa85a-255">The following example illustrates exception handling where multiple tasks can result in multiple exceptions.</span></span> <span data-ttu-id="fa85a-256">O `Try` bloco tem o `Await` expressão para a tarefa que <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> retornado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-256">The `Try` block has the `Await` expression for the task that <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> returned.</span></span> <span data-ttu-id="fa85a-257">A tarefa é concluída quando os três tarefas para o qual <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> é aplicado forem concluídas.</span><span class="sxs-lookup"><span data-stu-id="fa85a-257">The task is complete when the three tasks to which <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> is applied are complete.</span></span>  
  
 <span data-ttu-id="fa85a-258">Cada uma das três tarefas causa uma exceção.</span><span class="sxs-lookup"><span data-stu-id="fa85a-258">Each of the three tasks causes an exception.</span></span> <span data-ttu-id="fa85a-259">O `Catch` bloco itera por meio de exceções, que são encontradas no `Exception.InnerExceptions` propriedade da tarefa que `Task.WhenAll` retornado.</span><span class="sxs-lookup"><span data-stu-id="fa85a-259">The `Catch` block iterates through the exceptions, which are found in the `Exception.InnerExceptions` property of the task that `Task.WhenAll` returned.</span></span>  
  
 [!code-vb[csAsyncExceptions#3](../../../csharp/language-reference/keywords/codesnippet/VisualBasic/try-catch-finally-statement_8.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="fa85a-260">Consulte também</span><span class="sxs-lookup"><span data-stu-id="fa85a-260">See Also</span></span>  
 <xref:Microsoft.VisualBasic.Information.Err%2A>  
 <xref:System.Exception>  
 [<span data-ttu-id="fa85a-261">Instrução Exit</span><span class="sxs-lookup"><span data-stu-id="fa85a-261">Exit Statement</span></span>](../../../visual-basic/language-reference/statements/exit-statement.md)  
 [<span data-ttu-id="fa85a-262">Instrução On Error</span><span class="sxs-lookup"><span data-stu-id="fa85a-262">On Error Statement</span></span>](../../../visual-basic/language-reference/statements/on-error-statement.md)  
 [<span data-ttu-id="fa85a-263">Melhores práticas para usar trechos de código</span><span class="sxs-lookup"><span data-stu-id="fa85a-263">Best Practices for Using Code Snippets</span></span>](/visualstudio/ide/best-practices-for-using-code-snippets)  
 [<span data-ttu-id="fa85a-264">Tratamento de Exceção</span><span class="sxs-lookup"><span data-stu-id="fa85a-264">Exception Handling</span></span>](../../../standard/parallel-programming/exception-handling-task-parallel-library.md)  
 [<span data-ttu-id="fa85a-265">Instrução Throw</span><span class="sxs-lookup"><span data-stu-id="fa85a-265">Throw Statement</span></span>](../../../visual-basic/language-reference/statements/throw-statement.md)
