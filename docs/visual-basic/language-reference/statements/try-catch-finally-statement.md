---
title: Try... Catch... Instrução Finally – Visual Basic
description: Saiba como usar tratamento de exceções com o Visual Basic instruções Try/Catch/Finally.
ms.date: 12/07/2018
f1_keywords:
- vb.Try...Catch...Finally
- vb.when
- vb.Finally
- vb.Catch
- vb.Try
helpviewer_keywords:
- Try...Catch...Finally statements
- Try statement [Visual Basic]
- try-catch exception handling, Try...Catch...Finally statements
- error handling, while running code
- Try statement [Visual Basic], Try...Catch...Finally
- Finally keyword [Visual Basic], Try...Catch...Finally
- Catch statement [Visual Basic]
- When keyword [Visual Basic]
- Visual Basic code, handling errors while running
- structured exception handling, Try...Catch...Finally statements
ms.assetid: d6488026-ccb3-42b8-a810-0d97b9d6472b
ms.custom: seodec18
ms.openlocfilehash: 2e4fef836b08f536565105dbab76292b2fc4388e
ms.sourcegitcommit: a36cfc9dbbfc04bd88971f96e8a3f8e283c15d42
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/11/2019
ms.locfileid: "54221707"
---
# <a name="trycatchfinally-statement-visual-basic"></a><span data-ttu-id="8ff9c-103">Instrução Try...Catch...Finally (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="8ff9c-103">Try...Catch...Finally Statement (Visual Basic)</span></span>

<span data-ttu-id="8ff9c-104">Fornece uma maneira de lidar com alguns ou todos os erros possíveis que podem ocorrer em um determinado bloco de código, executando ainda o código.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-104">Provides a way to handle some or all possible errors that may occur in a given block of code, while still running code.</span></span>

## <a name="syntax"></a><span data-ttu-id="8ff9c-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="8ff9c-105">Syntax</span></span>

```vb
Try
    [ tryStatements ]
    [ Exit Try ]
[ Catch [ exception [ As type ] ] [ When expression ]
    [ catchStatements ]
    [ Exit Try ] ]
[ Catch ... ]
[ Finally
    [ finallyStatements ] ]
End Try
```

## <a name="parts"></a><span data-ttu-id="8ff9c-106">Partes</span><span class="sxs-lookup"><span data-stu-id="8ff9c-106">Parts</span></span>

|<span data-ttu-id="8ff9c-107">Termo</span><span class="sxs-lookup"><span data-stu-id="8ff9c-107">Term</span></span>|<span data-ttu-id="8ff9c-108">Definição</span><span class="sxs-lookup"><span data-stu-id="8ff9c-108">Definition</span></span>|
|---|---|
|`tryStatements`|<span data-ttu-id="8ff9c-109">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-109">Optional.</span></span> <span data-ttu-id="8ff9c-110">Instruções onde pode ocorrer um erro.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-110">Statement(s) where an error can occur.</span></span> <span data-ttu-id="8ff9c-111">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-111">Can be a compound statement.</span></span>|
|`Catch`|<span data-ttu-id="8ff9c-112">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-112">Optional.</span></span> <span data-ttu-id="8ff9c-113">Vários `Catch` blocos permitidos.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-113">Multiple `Catch` blocks permitted.</span></span> <span data-ttu-id="8ff9c-114">Se ocorrer uma exceção ao processar o `Try` bloquear, cada `Catch` instrução é examinada na ordem textual para determinar se ele trata a exceção, com `exception` que representa a exceção que foi lançada.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-114">If an exception occurs when processing the `Try` block, each `Catch` statement is examined in textual order to determine whether it handles the exception, with `exception` representing the exception that has been thrown.</span></span>|
|`exception`|<span data-ttu-id="8ff9c-115">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-115">Optional.</span></span> <span data-ttu-id="8ff9c-116">Qualquer nome de variável.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-116">Any variable name.</span></span> <span data-ttu-id="8ff9c-117">O valor inicial de `exception` é o valor do erro gerado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-117">The initial value of `exception` is the value of the thrown error.</span></span> <span data-ttu-id="8ff9c-118">Usado com `Catch` especificar o erro detectado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-118">Used with `Catch` to specify the error caught.</span></span> <span data-ttu-id="8ff9c-119">Se omitido, o `Catch` instrução captura qualquer exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-119">If omitted, the `Catch` statement catches any exception.</span></span>|
|`type`|<span data-ttu-id="8ff9c-120">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-120">Optional.</span></span> <span data-ttu-id="8ff9c-121">Especifica o tipo de filtro de classe.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-121">Specifies the type of class filter.</span></span> <span data-ttu-id="8ff9c-122">Se o valor de `exception` é do tipo especificado pelo `type` ou de um tipo derivado, o identificador se torna associado ao objeto de exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-122">If the value of `exception` is of the type specified by `type` or of a derived type, the identifier becomes bound to the exception object.</span></span>|
|`When`|<span data-ttu-id="8ff9c-123">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-123">Optional.</span></span> <span data-ttu-id="8ff9c-124">Um `Catch` instrução com uma `When` cláusula captura exceções somente quando `expression` é avaliada como `True`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-124">A `Catch` statement with a `When` clause catches exceptions only when `expression` evaluates to `True`.</span></span> <span data-ttu-id="8ff9c-125">Um `When` cláusula é aplicada somente depois de verificar o tipo de exceção, e `expression` podem se referir ao identificador que representa a exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-125">A `When` clause is applied only after checking the type of the exception, and `expression` may refer to the identifier representing the exception.</span></span>|
|`expression`|<span data-ttu-id="8ff9c-126">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-126">Optional.</span></span> <span data-ttu-id="8ff9c-127">Deve ser implicitamente conversível para `Boolean`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-127">Must be implicitly convertible to `Boolean`.</span></span> <span data-ttu-id="8ff9c-128">Qualquer expressão que descreve um filtro genérico.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-128">Any expression that describes a generic filter.</span></span> <span data-ttu-id="8ff9c-129">Normalmente usado para filtrar por número do erro.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-129">Typically used to filter by error number.</span></span> <span data-ttu-id="8ff9c-130">Usado com `When` palavra-chave para especificar as circunstâncias sob as quais o erro for detectado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-130">Used with `When` keyword to specify circumstances under which the error is caught.</span></span>|
|`catchStatements`|<span data-ttu-id="8ff9c-131">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-131">Optional.</span></span> <span data-ttu-id="8ff9c-132">Instruções para tratar erros que ocorrem em associado `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-132">Statement(s) to handle errors that occur in the associated `Try` block.</span></span> <span data-ttu-id="8ff9c-133">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-133">Can be a compound statement.</span></span>|
|`Exit Try`|<span data-ttu-id="8ff9c-134">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-134">Optional.</span></span> <span data-ttu-id="8ff9c-135">Palavra-chave que é interrompida do `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-135">Keyword that breaks out of the `Try...Catch...Finally` structure.</span></span> <span data-ttu-id="8ff9c-136">Retoma a execução com o código que segue imediatamente o `End Try` instrução.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-136">Execution resumes with the code immediately following the `End Try` statement.</span></span> <span data-ttu-id="8ff9c-137">O `Finally` instrução ainda será executada.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-137">The `Finally` statement will still be executed.</span></span> <span data-ttu-id="8ff9c-138">Não é permitido em `Finally` blocos.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-138">Not allowed in `Finally` blocks.</span></span>|
|`Finally`|<span data-ttu-id="8ff9c-139">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-139">Optional.</span></span> <span data-ttu-id="8ff9c-140">Um `Finally` bloco sempre será executado quando a execução deixar qualquer parte do `Try...Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-140">A `Finally` block is always executed when execution leaves any part of the `Try...Catch` statement.</span></span>|
|`finallyStatements`|<span data-ttu-id="8ff9c-141">Opcional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-141">Optional.</span></span> <span data-ttu-id="8ff9c-142">Instruções que são executadas após a ocorrência de outro processamento de erro.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-142">Statement(s) that are executed after all other error processing has occurred.</span></span>|
|`End Try`|<span data-ttu-id="8ff9c-143">Encerra o `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-143">Terminates the `Try...Catch...Finally` structure.</span></span>|

## <a name="remarks"></a><span data-ttu-id="8ff9c-144">Comentários</span><span class="sxs-lookup"><span data-stu-id="8ff9c-144">Remarks</span></span>

<span data-ttu-id="8ff9c-145">Se você espera que uma exceção específica pode ocorrer durante uma determinada seção de código, coloque o código em um `Try` bloquear e usar um `Catch` bloco para manter o controle e tratar a exceção se ocorrer.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-145">If you expect that a particular exception might occur during a particular section of code, put the code in a `Try` block and use a `Catch` block to retain control and handle the exception if it occurs.</span></span>

<span data-ttu-id="8ff9c-146">Um `Try…Catch` instrução consiste em um `Try` bloco seguido por um ou mais `Catch` cláusulas, que especificam os manipuladores para várias exceções.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-146">A `Try…Catch` statement consists of a `Try` block followed by one or more `Catch` clauses, which specify handlers for various exceptions.</span></span> <span data-ttu-id="8ff9c-147">Quando uma exceção é gerada um `Try` bloquear, Visual Basic procura o `Catch` instrução que trata a exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-147">When an exception is thrown in a `Try` block, Visual Basic looks for the `Catch` statement that handles the exception.</span></span> <span data-ttu-id="8ff9c-148">Se encontrar uma correspondência `Catch` instrução não for encontrada, o Visual Basic examina o método que chamou o método atual, e assim por diante para cima a pilha de chamadas.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-148">If a matching `Catch` statement is not found, Visual Basic examines the method that called the current method, and so on up the call stack.</span></span> <span data-ttu-id="8ff9c-149">Se nenhum `Catch` bloco for encontrado, o Visual Basic exibe uma mensagem de exceção sem tratamento para o usuário e interrompe a execução do programa.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-149">If no `Catch` block is found, Visual Basic displays an unhandled exception message to the user and stops execution of the program.</span></span>

<span data-ttu-id="8ff9c-150">Você pode usar mais de uma `Catch` instrução em um `Try…Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-150">You can use more than one `Catch` statement in a `Try…Catch` statement.</span></span> <span data-ttu-id="8ff9c-151">Se você fizer isso, a ordem dos `Catch` cláusulas é significativo porque eles são examinados em ordem.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-151">If you do this, the order of the `Catch` clauses is significant because they are examined in order.</span></span> <span data-ttu-id="8ff9c-152">Capture as exceções mais específicas antes das menos específicas.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-152">Catch the more specific exceptions before the less specific ones.</span></span>

<span data-ttu-id="8ff9c-153">O seguinte `Catch` condições de instrução são menos específicas e irá capturar todas as exceções que derivam do <xref:System.Exception> classe.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-153">The following `Catch` statement conditions are the least specific, and will catch all exceptions that derive from the <xref:System.Exception> class.</span></span> <span data-ttu-id="8ff9c-154">Você normalmente deve usar um dessas variações como a última `Catch` bloquear o `Try...Catch...Finally` estrutura, depois de capturar todas as exceções específicas que você espera.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-154">You should ordinarily use one of these variations as the last `Catch` block in the `Try...Catch...Finally` structure, after catching all the specific exceptions you expect.</span></span> <span data-ttu-id="8ff9c-155">Fluxo de controle nunca pode atingir um `Catch` bloco que segue a qualquer uma dessas variações.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-155">Control flow can never reach a `Catch` block that follows either of these variations.</span></span>

- <span data-ttu-id="8ff9c-156">O `type` é `Exception`, por exemplo: `Catch ex As Exception`</span><span class="sxs-lookup"><span data-stu-id="8ff9c-156">The `type` is `Exception`, for example: `Catch ex As Exception`</span></span>

- <span data-ttu-id="8ff9c-157">A instrução não tem nenhum `exception` variável, por exemplo: `Catch`</span><span class="sxs-lookup"><span data-stu-id="8ff9c-157">The statement has no `exception` variable, for example: `Catch`</span></span>

<span data-ttu-id="8ff9c-158">Quando um `Try…Catch…Finally` instrução está aninhada em outro `Try` bloco, Visual Basic primeiro examina cada `Catch` instrução em mais interna `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-158">When a `Try…Catch…Finally` statement is nested in another `Try` block, Visual Basic first examines each `Catch` statement in the innermost `Try` block.</span></span> <span data-ttu-id="8ff9c-159">Se nenhuma correspondência `Catch` instrução for encontrada, a pesquisa continua para a `Catch` instruções de externo `Try…Catch…Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-159">If no matching `Catch` statement is found, the search proceeds to the `Catch` statements of the outer `Try…Catch…Finally` block.</span></span>

<span data-ttu-id="8ff9c-160">Variáveis locais de um `Try` bloco não estão disponíveis em um `Catch` bloquear porque eles são blocos separados.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-160">Local variables from a `Try` block are not available in a `Catch` block because they are separate blocks.</span></span> <span data-ttu-id="8ff9c-161">Se você quiser usar uma variável em mais de um bloco, declarar a variável de fora a `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-161">If you want to use a variable in more than one block, declare the variable outside the `Try...Catch...Finally` structure.</span></span>

> [!TIP]
> <span data-ttu-id="8ff9c-162">O `Try…Catch…Finally` instrução está disponível como um trecho de código do IntelliSense.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-162">The `Try…Catch…Finally` statement is available as an IntelliSense code snippet.</span></span> <span data-ttu-id="8ff9c-163">No Gerenciador de trechos de código, expanda **padrões de código - se, para cada um, Try Catch, propriedade, etc**e então **tratamento de erros (exceções)**.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-163">In the Code Snippets Manager, expand **Code Patterns - If, For Each, Try Catch, Property, etc**, and then **Error Handling (Exceptions)**.</span></span> <span data-ttu-id="8ff9c-164">Para obter mais informações, consulte [Snippets de Código](/visualstudio/ide/code-snippets).</span><span class="sxs-lookup"><span data-stu-id="8ff9c-164">For more information, see [Code Snippets](/visualstudio/ide/code-snippets).</span></span>

## <a name="finally-block"></a><span data-ttu-id="8ff9c-165">Bloco Finally</span><span class="sxs-lookup"><span data-stu-id="8ff9c-165">Finally block</span></span>

<span data-ttu-id="8ff9c-166">Se você tiver uma ou mais instruções que devem ser executados antes de sair do `Try` estrutura, use um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-166">If you have one or more statements that must run before you exit the `Try` structure, use a `Finally` block.</span></span> <span data-ttu-id="8ff9c-167">O controle passa para o `Finally` bloquear logo antes do `Try…Catch` estrutura.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-167">Control passes to the `Finally` block just before it passes out of the `Try…Catch` structure.</span></span> <span data-ttu-id="8ff9c-168">Isso é verdadeiro mesmo se uma exceção ocorre em qualquer lugar dentro do `Try` estrutura.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-168">This is true even if an exception occurs anywhere inside the `Try` structure.</span></span>

<span data-ttu-id="8ff9c-169">Um `Finally` bloco é útil para executar qualquer código que deve executar, mesmo se houver uma exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-169">A `Finally` block is useful for running any code that must execute even if there is an exception.</span></span> <span data-ttu-id="8ff9c-170">O controle é passado para o `Finally` bloco, independentemente de como o `Try...Catch` bloquear sai.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-170">Control is passed to the `Finally` block regardless of how the `Try...Catch` block exits.</span></span>

<span data-ttu-id="8ff9c-171">O código em um `Finally` execução do bloco, mesmo se seu código encontrar uma `Return` instrução em um `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-171">The code in a `Finally` block runs even if your code encounters a `Return` statement in a `Try` or `Catch` block.</span></span> <span data-ttu-id="8ff9c-172">Controle não passa de um `Try` ou `Catch` bloquear para os respectivos `Finally` bloquear nos seguintes casos:</span><span class="sxs-lookup"><span data-stu-id="8ff9c-172">Control does not pass from a `Try` or `Catch` block to the corresponding `Finally` block in the following cases:</span></span>

- <span data-ttu-id="8ff9c-173">Uma [instrução End](end-statement.md) for encontrado na `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-173">An [End Statement](end-statement.md) is encountered in the `Try` or `Catch` block.</span></span>

- <span data-ttu-id="8ff9c-174">Um <xref:System.StackOverflowException> é gerada em de `Try` ou `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-174">A <xref:System.StackOverflowException> is thrown in the `Try` or `Catch` block.</span></span>

<span data-ttu-id="8ff9c-175">Não é válido para transferir explicitamente a execução em um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-175">It is not valid to explicitly transfer execution into a `Finally` block.</span></span> <span data-ttu-id="8ff9c-176">Transferir a execução de um `Finally` bloco não for válido, exceto por meio de uma exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-176">Transferring execution out of a `Finally` block is not valid, except through an exception.</span></span>

<span data-ttu-id="8ff9c-177">Se um `Try` instrução não contém pelo menos um `Catch` bloco, ele deve conter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-177">If a `Try` statement does not contain at least one `Catch` block, it must contain a `Finally` block.</span></span>

> [!TIP]
> <span data-ttu-id="8ff9c-178">Se você não precisa capturar exceções específicas, o `Using` instrução se comporta como um `Try…Finally` bloco e garante o descarte dos recursos, independentemente de como você sai do bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-178">If you do not have to catch specific exceptions, the `Using` statement behaves like a `Try…Finally` block, and guarantees disposal of the resources, regardless of how you exit the block.</span></span> <span data-ttu-id="8ff9c-179">Isso é verdadeiro mesmo com uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-179">This is true even with an unhandled exception.</span></span> <span data-ttu-id="8ff9c-180">Para obter mais informações, consulte [Instrução using](using-statement.md).</span><span class="sxs-lookup"><span data-stu-id="8ff9c-180">For more information, see [Using Statement](using-statement.md).</span></span>

## <a name="exception-argument"></a><span data-ttu-id="8ff9c-181">Argumento de exceção</span><span class="sxs-lookup"><span data-stu-id="8ff9c-181">Exception argument</span></span>

<span data-ttu-id="8ff9c-182">O `Catch` bloco `exception` argumento é uma instância da <xref:System.Exception> classe ou uma classe que deriva de `Exception` classe.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-182">The `Catch` block `exception` argument is an instance of the <xref:System.Exception> class or a class that derives from the `Exception` class.</span></span> <span data-ttu-id="8ff9c-183">O `Exception` instância da classe corresponde ao erro que ocorreu no `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-183">The `Exception` class instance corresponds to the error that occurred in the `Try` block.</span></span>

<span data-ttu-id="8ff9c-184">As propriedades do `Exception` ajuda para identificar a causa e a localização de uma exceção do objeto.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-184">The properties of the `Exception` object help to identify the cause and location of an exception.</span></span> <span data-ttu-id="8ff9c-185">Por exemplo, o <xref:System.Exception.StackTrace%2A> propriedade lista os métodos chamados que conduziram à exceção, ajudando você a localizar onde o erro ocorreu no código.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-185">For example, the <xref:System.Exception.StackTrace%2A> property lists the called methods that led to the exception, helping you find where the error occurred in the code.</span></span> <span data-ttu-id="8ff9c-186"><xref:System.Exception.Message%2A> Retorna uma mensagem que descreve a exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-186"><xref:System.Exception.Message%2A> returns a message that describes the exception.</span></span> <span data-ttu-id="8ff9c-187"><xref:System.Exception.HelpLink%2A> Retorna um link para um arquivo de ajuda associado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-187"><xref:System.Exception.HelpLink%2A> returns a link to an associated Help file.</span></span> <span data-ttu-id="8ff9c-188"><xref:System.Exception.InnerException%2A> Retorna o `Exception` retorna o objeto que causou a exceção atual, ou ele `Nothing` se não houver nenhum original `Exception`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-188"><xref:System.Exception.InnerException%2A> returns the `Exception` object that caused the current exception, or it returns `Nothing` if there is no original `Exception`.</span></span>

## <a name="considerations-when-using-a-trycatch-statement"></a><span data-ttu-id="8ff9c-189">Considerações ao usar um bloco Try... Instrução catch</span><span class="sxs-lookup"><span data-stu-id="8ff9c-189">Considerations when using a Try…Catch statement</span></span>

<span data-ttu-id="8ff9c-190">Use um `Try…Catch` instrução somente para sinalizar a ocorrência de eventos do programa incomuns ou inesperados.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-190">Use a `Try…Catch` statement only to signal the occurrence of unusual or unanticipated program events.</span></span> <span data-ttu-id="8ff9c-191">Motivos para isso incluem o seguinte:</span><span class="sxs-lookup"><span data-stu-id="8ff9c-191">Reasons for this include the following:</span></span>

- <span data-ttu-id="8ff9c-192">Capturando exceções no tempo de execução cria uma sobrecarga adicional e provavelmente será mais lento do que a pré-verificação de para evitar exceções.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-192">Catching exceptions at runtime creates additional overhead, and is likely to be slower than pre-checking to avoid exceptions.</span></span>

- <span data-ttu-id="8ff9c-193">Se um `Catch` bloco não é manipulado corretamente, a exceção não pode ser relatada corretamente para os usuários.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-193">If a `Catch` block is not handled correctly, the exception might not be reported correctly to users.</span></span>

- <span data-ttu-id="8ff9c-194">Tratamento de exceção faz com que um programa mais complexos.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-194">Exception handling makes a program more complex.</span></span>

<span data-ttu-id="8ff9c-195">Você não precisa sempre um `Try…Catch` instrução para verificar se há uma condição que é provável de ocorrer.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-195">You do not always need a `Try…Catch` statement to check for a condition that is likely to occur.</span></span> <span data-ttu-id="8ff9c-196">O exemplo a seguir verifica se um arquivo existe antes de tentar abri-lo.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-196">The following example checks whether a file exists before trying to open it.</span></span> <span data-ttu-id="8ff9c-197">Isso reduz a necessidade de capturar uma exceção lançada pelo <xref:System.IO.File.OpenText%2A> método.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-197">This reduces the need for catching an exception thrown by the <xref:System.IO.File.OpenText%2A> method.</span></span>

[!code-vb[VbVbalrStatements#94](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#94)]

<span data-ttu-id="8ff9c-198">Garantir que o código em `Catch` blocos podem relatar corretamente as exceções para os usuários, seja por meio de registro em log de thread-safe ou mensagens apropriadas.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-198">Ensure that code in `Catch` blocks can properly report exceptions to users, whether through thread-safe logging or appropriate messages.</span></span> <span data-ttu-id="8ff9c-199">Caso contrário, as exceções podem permanecer desconhecidas.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-199">Otherwise, exceptions might remain unknown.</span></span>

## <a name="async-methods"></a><span data-ttu-id="8ff9c-200">Métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="8ff9c-200">Async methods</span></span>

<span data-ttu-id="8ff9c-201">Se você marcar um método com o [Async](../modifiers/async.md) modificador, você pode usar o [Await](../operators/await-operator.md) operador no método.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-201">If you mark a method with the [Async](../modifiers/async.md) modifier, you can use the [Await](../operators/await-operator.md) operator in the method.</span></span> <span data-ttu-id="8ff9c-202">Uma instrução com o `Await` operador suspende a execução do método até que a tarefa aguardada seja concluída.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-202">A statement with the `Await` operator suspends execution of the method until the awaited task completes.</span></span> <span data-ttu-id="8ff9c-203">A tarefa representa um trabalho em andamento.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-203">The task represents ongoing work.</span></span> <span data-ttu-id="8ff9c-204">Quando a tarefa que está associada com o `Await` operador termina, retoma a execução no mesmo método.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-204">When the task that's associated with the `Await` operator finishes, execution resumes in the same method.</span></span> <span data-ttu-id="8ff9c-205">Para obter mais informações, consulte [fluxo de controle em programas assíncronos](../../../visual-basic/programming-guide/concepts/async/control-flow-in-async-programs.md).</span><span class="sxs-lookup"><span data-stu-id="8ff9c-205">For more information, see [Control Flow in Async Programs](../../../visual-basic/programming-guide/concepts/async/control-flow-in-async-programs.md).</span></span>

<span data-ttu-id="8ff9c-206">Uma tarefa retornada por um método assíncrono pode terminar com um estado de falha, indicando que ela foi concluída devido a uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-206">A task returned by an Async method may end in a faulted state, indicating that it completed due to an unhandled exception.</span></span> <span data-ttu-id="8ff9c-207">Uma tarefa também pode terminar com um estado cancelado, o que resulta em um `OperationCanceledException` sendo lançadas fora a expressão await.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-207">A task may also end in a canceled state, which results in an `OperationCanceledException` being thrown out of the await expression.</span></span> <span data-ttu-id="8ff9c-208">Para capturar qualquer tipo de exceção, coloque o `Await` expressão associada com a tarefa em um `Try` bloquear e capturar a exceção no `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-208">To catch either type of exception, place the `Await` expression that's associated with the task in a `Try` block, and catch the exception in the `Catch` block.</span></span> <span data-ttu-id="8ff9c-209">Um exemplo é fornecido neste tópico.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-209">An example is provided later in this topic.</span></span>

<span data-ttu-id="8ff9c-210">Uma tarefa pode ser em um estado de falha, porque várias exceções eram responsáveis por sua falha.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-210">A task can be in a faulted state because multiple exceptions were responsible for its faulting.</span></span> <span data-ttu-id="8ff9c-211">Por exemplo, a tarefa pode ser o resultado de uma chamada para <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-211">For example, the task might be the result of a call to <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="8ff9c-212">Quando você espera uma tarefa, a exceção capturada é apenas uma das exceções, e você não pode prever qual exceção será capturada.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-212">When you await such a task, the caught exception is only one of the exceptions, and you can't predict which exception will be caught.</span></span> <span data-ttu-id="8ff9c-213">Um exemplo é fornecido neste tópico.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-213">An example is provided later in this topic.</span></span>

<span data-ttu-id="8ff9c-214">Uma `Await` expressão não pode ser dentro de uma `Catch` bloco ou `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-214">An `Await` expression can't be inside a `Catch` block or `Finally` block.</span></span>

## <a name="iterators"></a><span data-ttu-id="8ff9c-215">Iterators</span><span class="sxs-lookup"><span data-stu-id="8ff9c-215">Iterators</span></span>

<span data-ttu-id="8ff9c-216">Uma função de iterador ou `Get` acessador realiza uma iteração personalizada em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-216">An iterator function or `Get` accessor performs a custom iteration over a collection.</span></span> <span data-ttu-id="8ff9c-217">Um iterador usa uma [Yield](yield-statement.md) instrução para retornar cada elemento da coleção um por vez.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-217">An iterator uses a [Yield](yield-statement.md) statement to return each element of the collection one at a time.</span></span> <span data-ttu-id="8ff9c-218">Chamar uma função de iterador usando uma [para cada um... Próxima instrução](for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="8ff9c-218">You call an iterator function by using a [For Each...Next Statement](for-each-next-statement.md).</span></span>

<span data-ttu-id="8ff9c-219">Um `Yield` instrução pode ser dentro de um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-219">A `Yield` statement can be inside a `Try` block.</span></span> <span data-ttu-id="8ff9c-220">Um `Try` bloco que contém uma `Yield` instrução pode ter `Catch` bloqueia e pode ter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-220">A `Try` block that contains a `Yield` statement can have `Catch` blocks, and can have a `Finally` block.</span></span> <span data-ttu-id="8ff9c-221">Consulte a seção "Experimente blocos no Visual Basic" [iteradores](../../programming-guide/concepts/iterators.md) para obter um exemplo.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-221">See the "Try Blocks in Visual Basic" section of [Iterators](../../programming-guide/concepts/iterators.md) for an example.</span></span>

<span data-ttu-id="8ff9c-222">Um `Yield` instrução não pode estar dentro de uma `Catch` bloco ou uma `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-222">A `Yield` statement cannot be inside a `Catch` block or a `Finally` block.</span></span>

<span data-ttu-id="8ff9c-223">Se o `For Each` corpo (fora da função de iterador) lança uma exceção, uma `Catch` bloco na função de iterador não é executado, mas um `Finally` bloco na função de iterador será executado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-223">If the `For Each` body (outside of the iterator function) throws an exception, a `Catch` block in the iterator function is not executed, but a `Finally` block in the iterator function is executed.</span></span> <span data-ttu-id="8ff9c-224">Um `Catch` bloco dentro de uma função de iterador captura somente exceções que ocorrem dentro da função de iterador.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-224">A `Catch` block inside an iterator function catches only exceptions that occur inside the iterator function.</span></span>

## <a name="partial-trust-situations"></a><span data-ttu-id="8ff9c-225">Situações de confiança parcial</span><span class="sxs-lookup"><span data-stu-id="8ff9c-225">Partial-trust situations</span></span>

<span data-ttu-id="8ff9c-226">Em situações de confiança parcial, como um aplicativo hospedado em um compartilhamento de rede, `Try...Catch...Finally` não capturará exceções de segurança que ocorrem antes que o método que contém a chamada é invocado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-226">In partial-trust situations, such as an application hosted on a network share, `Try...Catch...Finally` does not catch security exceptions that occur before the method that contains the call is invoked.</span></span> <span data-ttu-id="8ff9c-227">O exemplo a seguir, quando você colocá-lo em um compartilhamento de servidor e executar a partir daí, produz o erro "System.Security.SecurityException: Falha na solicitação."</span><span class="sxs-lookup"><span data-stu-id="8ff9c-227">The following example, when you put it on a server share and run from there, produces the error "System.Security.SecurityException: Request Failed."</span></span> <span data-ttu-id="8ff9c-228">Para obter mais informações sobre exceções de segurança, consulte o <xref:System.Security.SecurityException> classe.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-228">For more information about security exceptions, see the <xref:System.Security.SecurityException> class.</span></span>

[!code-vb[VbVbalrStatements#85](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#85)]

<span data-ttu-id="8ff9c-229">Em tal uma situação de confiança parcial, você precisa colocar o `Process.Start` instrução em um separado `Sub`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-229">In such a partial-trust situation, you have to put the `Process.Start` statement in a separate `Sub`.</span></span> <span data-ttu-id="8ff9c-230">A chamada inicial para o `Sub` falhará.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-230">The initial call to the `Sub` will fail.</span></span> <span data-ttu-id="8ff9c-231">Isso permite `Try...Catch` quer capturá-la antes do `Sub` que contém `Process.Start` é iniciado e a exceção de segurança produzidos.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-231">This enables `Try...Catch` to catch it before the `Sub` that contains `Process.Start` is started and the security exception produced.</span></span>

## <a name="examples"></a><span data-ttu-id="8ff9c-232">Exemplos</span><span class="sxs-lookup"><span data-stu-id="8ff9c-232">Examples</span></span>

### <a name="the-structure-of-trycatchfinally"></a><span data-ttu-id="8ff9c-233">A estrutura de Try... Catch... Por fim</span><span class="sxs-lookup"><span data-stu-id="8ff9c-233">The structure of Try...Catch...Finally</span></span>

<span data-ttu-id="8ff9c-234">O exemplo a seguir ilustra a estrutura do `Try...Catch...Finally` instrução.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-234">The following example illustrates the structure of the `Try...Catch...Finally` statement.</span></span>

[!code-vb[VbVbalrStatements#86](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#86)]  

### <a name="exception-in-a-method-called-from-a-try-block"></a><span data-ttu-id="8ff9c-235">Exceção em um método chamado a partir de um bloco Try</span><span class="sxs-lookup"><span data-stu-id="8ff9c-235">Exception in a method called from a Try block</span></span>

<span data-ttu-id="8ff9c-236">No exemplo a seguir, o `CreateException` método lança um `NullReferenceException`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-236">In the following example, the `CreateException` method throws a `NullReferenceException`.</span></span> <span data-ttu-id="8ff9c-237">O código que gera a exceção não está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-237">The code that generates the exception is not in a `Try` block.</span></span> <span data-ttu-id="8ff9c-238">Portanto, o `CreateException` não lidam com a exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-238">Therefore, the `CreateException` method does not handle the exception.</span></span> <span data-ttu-id="8ff9c-239">O `RunSample` lidam com a exceção porque a chamada para o `CreateException` método está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-239">The `RunSample` method does handle the exception because the call to the `CreateException` method is in a `Try` block.</span></span>

<span data-ttu-id="8ff9c-240">O exemplo inclui `Catch` instruções para vários tipos de exceções, ordenados do mais específico para o mais geral.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-240">The example includes `Catch` statements for several types of exceptions, ordered from the most specific to the most general.</span></span>

[!code-vb[VbVbalrStatements#91](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#91)]

### <a name="the-catch-when-statement"></a><span data-ttu-id="8ff9c-241">A instrução Catch quando</span><span class="sxs-lookup"><span data-stu-id="8ff9c-241">The Catch When statement</span></span>

<span data-ttu-id="8ff9c-242">O exemplo a seguir mostra como usar um `Catch When` instrução para filtrar em uma expressão condicional.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-242">The following example shows how to use a `Catch When` statement to filter on a conditional expression.</span></span> <span data-ttu-id="8ff9c-243">Se a expressão condicional é avaliada como `True`, o código a `Catch` execução do bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-243">If the conditional expression evaluates to `True`, the code in the `Catch` block runs.</span></span>

[!code-vb[VbVbalrStatements#92](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#92)]

### <a name="nested-try-statements"></a><span data-ttu-id="8ff9c-244">Instruções de Try aninhadas</span><span class="sxs-lookup"><span data-stu-id="8ff9c-244">Nested Try statements</span></span>

<span data-ttu-id="8ff9c-245">O exemplo a seguir tem um `Try…Catch` instrução que está contida em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-245">The following example has a `Try…Catch` statement that is contained in a `Try` block.</span></span> <span data-ttu-id="8ff9c-246">Interno `Catch` bloco lançar uma exceção que tem seu `InnerException` propriedade definida para a exceção original.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-246">The inner `Catch` block throws an exception that has its `InnerException` property set to the original exception.</span></span> <span data-ttu-id="8ff9c-247">Externo `Catch` bloco relata sua própria exceção e a exceção interna.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-247">The outer `Catch` block reports its own exception and the inner exception.</span></span>

[!code-vb[VbVbalrStatements#93](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#93)]

### <a name="exception-handling-for-async-methods"></a><span data-ttu-id="8ff9c-248">Tratamento de exceção para métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="8ff9c-248">Exception handling for async methods</span></span>

<span data-ttu-id="8ff9c-249">O exemplo a seguir ilustra o tratamento de exceção para métodos assíncronos.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-249">The following example illustrates exception handling for async methods.</span></span> <span data-ttu-id="8ff9c-250">Para capturar uma exceção que se aplica a uma tarefa assíncrona, o `Await` expressão está em um `Try` bloco do chamador e a exceção é capturado no `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-250">To catch an exception that applies to an async task, the `Await` expression is in a `Try` block of the caller, and the exception is caught in the `Catch` block.</span></span>

<span data-ttu-id="8ff9c-251">Remova a marca de comentário da linha `Throw New Exception` no exemplo para demonstrar o tratamento de exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-251">Uncomment the `Throw New Exception` line in the example to demonstrate exception handling.</span></span> <span data-ttu-id="8ff9c-252">A exceção é capturada na `Catch` bloquear, a tarefa `IsFaulted` estiver definida como `True`e a tarefa `Exception.InnerException` estiver definida como a exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-252">The exception is caught in the `Catch` block, the task's `IsFaulted` property is set to `True`, and the task's `Exception.InnerException` property is set to the exception.</span></span>

<span data-ttu-id="8ff9c-253">Remova a marca de comentário da linha `Throw New OperationCancelledException` para demonstrar o que acontece quando você cancela um processo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-253">Uncomment the `Throw New OperationCancelledException` line to demonstrate what happens when you cancel an asynchronous process.</span></span> <span data-ttu-id="8ff9c-254">A exceção é capturada na `Catch` bloco e a tarefa `IsCanceled` estiver definida como `True`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-254">The exception is caught in the `Catch` block, and the task's `IsCanceled` property is set to `True`.</span></span> <span data-ttu-id="8ff9c-255">No entanto, em algumas condições que não se aplicam a este exemplo, `IsFaulted` é definido como `True` e `IsCanceled` é definido como `False`.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-255">However, under some conditions that don't apply to this example, `IsFaulted` is set to `True` and `IsCanceled` is set to `False`.</span></span>

[!code-vb[csAsyncExceptions#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/csasyncexceptions/vb/class1.vb#1)]

### <a name="handling-multiple-exceptions-in-async-methods"></a><span data-ttu-id="8ff9c-256">Manipulando várias exceções nos métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="8ff9c-256">Handling multiple exceptions in async methods</span></span>

<span data-ttu-id="8ff9c-257">O exemplo a seguir ilustra a manipulação de exceção em que várias tarefas podem resultar em várias exceções.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-257">The following example illustrates exception handling where multiple tasks can result in multiple exceptions.</span></span> <span data-ttu-id="8ff9c-258">O `Try` bloco tem o `Await` expressão para a tarefa que <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> retornado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-258">The `Try` block has the `Await` expression for the task that <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> returned.</span></span> <span data-ttu-id="8ff9c-259">A tarefa é concluída quando as três tarefas para o qual <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> é aplicado estão concluídas.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-259">The task is complete when the three tasks to which <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> is applied are complete.</span></span>

<span data-ttu-id="8ff9c-260">Cada uma das três tarefas causa uma exceção.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-260">Each of the three tasks causes an exception.</span></span> <span data-ttu-id="8ff9c-261">O `Catch` bloco itera por meio de exceções, que se encontram na `Exception.InnerExceptions` propriedade da tarefa que `Task.WhenAll` retornado.</span><span class="sxs-lookup"><span data-stu-id="8ff9c-261">The `Catch` block iterates through the exceptions, which are found in the `Exception.InnerExceptions` property of the task that `Task.WhenAll` returned.</span></span>

[!code-vb[csAsyncExceptions#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/csasyncexceptions/vb/class1.vb#3)]

## <a name="see-also"></a><span data-ttu-id="8ff9c-262">Consulte também</span><span class="sxs-lookup"><span data-stu-id="8ff9c-262">See also</span></span>

- <xref:Microsoft.VisualBasic.Information.Err%2A>
- <xref:System.Exception>
- [<span data-ttu-id="8ff9c-263">Instrução Exit</span><span class="sxs-lookup"><span data-stu-id="8ff9c-263">Exit Statement</span></span>](exit-statement.md)
- [<span data-ttu-id="8ff9c-264">Instrução On Error</span><span class="sxs-lookup"><span data-stu-id="8ff9c-264">On Error Statement</span></span>](on-error-statement.md)
- [<span data-ttu-id="8ff9c-265">Melhores práticas para usar snippets de código</span><span class="sxs-lookup"><span data-stu-id="8ff9c-265">Best Practices for Using Code Snippets</span></span>](/visualstudio/ide/best-practices-for-using-code-snippets)
- [<span data-ttu-id="8ff9c-266">Tratamento de Exceção</span><span class="sxs-lookup"><span data-stu-id="8ff9c-266">Exception Handling</span></span>](../../../standard/parallel-programming/exception-handling-task-parallel-library.md)
- [<span data-ttu-id="8ff9c-267">Instrução Throw</span><span class="sxs-lookup"><span data-stu-id="8ff9c-267">Throw Statement</span></span>](throw-statement.md)