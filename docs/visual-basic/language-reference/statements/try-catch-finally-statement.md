---
title: Tentar... Capturar... Instrução Finally
description: Aprenda a usar a manipulação de exceção com instruções Visual Basic try/catch/finally.
ms.date: 12/07/2018
f1_keywords:
- vb.Try...Catch...Finally
- vb.when
- vb.Finally
- vb.Catch
- vb.Try
helpviewer_keywords:
- Try...Catch...Finally statements
- Try statement [Visual Basic]
- try-catch exception handling, Try...Catch...Finally statements
- error handling, while running code
- Try statement [Visual Basic], Try...Catch...Finally
- Finally keyword [Visual Basic], Try...Catch...Finally
- Catch statement [Visual Basic]
- When keyword [Visual Basic]
- Visual Basic code, handling errors while running
- structured exception handling, Try...Catch...Finally statements
ms.assetid: d6488026-ccb3-42b8-a810-0d97b9d6472b
ms.openlocfilehash: 22f1611786a3da512632b5b547b7ef141c8f65c6
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84391762"
---
# <a name="trycatchfinally-statement-visual-basic"></a><span data-ttu-id="64c9c-103">Instrução Try...Catch...Finally (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="64c9c-103">Try...Catch...Finally Statement (Visual Basic)</span></span>

<span data-ttu-id="64c9c-104">Fornece uma maneira de lidar com alguns ou todos os erros possíveis que podem ocorrer em um determinado bloco de código, enquanto ainda executa o código.</span><span class="sxs-lookup"><span data-stu-id="64c9c-104">Provides a way to handle some or all possible errors that may occur in a given block of code, while still running code.</span></span>

## <a name="syntax"></a><span data-ttu-id="64c9c-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="64c9c-105">Syntax</span></span>

```vb
Try
    [ tryStatements ]
    [ Exit Try ]
[ Catch [ exception [ As type ] ] [ When expression ]
    [ catchStatements ]
    [ Exit Try ] ]
[ Catch ... ]
[ Finally
    [ finallyStatements ] ]
End Try
```

## <a name="parts"></a><span data-ttu-id="64c9c-106">Partes</span><span class="sxs-lookup"><span data-stu-id="64c9c-106">Parts</span></span>

|<span data-ttu-id="64c9c-107">Termo</span><span class="sxs-lookup"><span data-stu-id="64c9c-107">Term</span></span>|<span data-ttu-id="64c9c-108">Definição</span><span class="sxs-lookup"><span data-stu-id="64c9c-108">Definition</span></span>|
|---|---|
|`tryStatements`|<span data-ttu-id="64c9c-109">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-109">Optional.</span></span> <span data-ttu-id="64c9c-110">Instrução (ões) em que um erro pode ocorrer.</span><span class="sxs-lookup"><span data-stu-id="64c9c-110">Statement(s) where an error can occur.</span></span> <span data-ttu-id="64c9c-111">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="64c9c-111">Can be a compound statement.</span></span>|
|`Catch`|<span data-ttu-id="64c9c-112">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-112">Optional.</span></span> <span data-ttu-id="64c9c-113">Vários `Catch` blocos permitidos.</span><span class="sxs-lookup"><span data-stu-id="64c9c-113">Multiple `Catch` blocks permitted.</span></span> <span data-ttu-id="64c9c-114">Se ocorrer uma exceção durante o processamento do `Try` bloco, cada `Catch` instrução será examinada em ordem textual para determinar se ele trata a exceção, com `exception` a representação da exceção gerada.</span><span class="sxs-lookup"><span data-stu-id="64c9c-114">If an exception occurs when processing the `Try` block, each `Catch` statement is examined in textual order to determine whether it handles the exception, with `exception` representing the exception that has been thrown.</span></span>|
|`exception`|<span data-ttu-id="64c9c-115">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-115">Optional.</span></span> <span data-ttu-id="64c9c-116">Qualquer nome de variável.</span><span class="sxs-lookup"><span data-stu-id="64c9c-116">Any variable name.</span></span> <span data-ttu-id="64c9c-117">O valor inicial de `exception` é o valor do erro gerado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-117">The initial value of `exception` is the value of the thrown error.</span></span> <span data-ttu-id="64c9c-118">Usado com `Catch` para especificar o erro capturado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-118">Used with `Catch` to specify the error caught.</span></span> <span data-ttu-id="64c9c-119">Se for omitido, a `Catch` instrução capturará qualquer exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-119">If omitted, the `Catch` statement catches any exception.</span></span>|
|`type`|<span data-ttu-id="64c9c-120">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-120">Optional.</span></span> <span data-ttu-id="64c9c-121">Especifica o tipo de filtro de classe.</span><span class="sxs-lookup"><span data-stu-id="64c9c-121">Specifies the type of class filter.</span></span> <span data-ttu-id="64c9c-122">Se o valor de `exception` for do tipo especificado por `type` ou de um tipo derivado, o identificador se tornará associado ao objeto de exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-122">If the value of `exception` is of the type specified by `type` or of a derived type, the identifier becomes bound to the exception object.</span></span>|
|`When`|<span data-ttu-id="64c9c-123">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-123">Optional.</span></span> <span data-ttu-id="64c9c-124">Uma `Catch` instrução com uma `When` cláusula captura exceções somente quando `expression` é avaliada como `True` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-124">A `Catch` statement with a `When` clause catches exceptions only when `expression` evaluates to `True`.</span></span> <span data-ttu-id="64c9c-125">Uma `When` cláusula é aplicada somente após a verificação do tipo da exceção e `expression` pode se referir ao identificador que representa a exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-125">A `When` clause is applied only after checking the type of the exception, and `expression` may refer to the identifier representing the exception.</span></span>|
|`expression`|<span data-ttu-id="64c9c-126">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-126">Optional.</span></span> <span data-ttu-id="64c9c-127">Deve ser implicitamente conversível em `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-127">Must be implicitly convertible to `Boolean`.</span></span> <span data-ttu-id="64c9c-128">Qualquer expressão que descreva um filtro genérico.</span><span class="sxs-lookup"><span data-stu-id="64c9c-128">Any expression that describes a generic filter.</span></span> <span data-ttu-id="64c9c-129">Normalmente usado para filtrar por número de erro.</span><span class="sxs-lookup"><span data-stu-id="64c9c-129">Typically used to filter by error number.</span></span> <span data-ttu-id="64c9c-130">Usado com `When` a palavra-chave para especificar circunstâncias sob as quais o erro é capturado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-130">Used with `When` keyword to specify circumstances under which the error is caught.</span></span>|
|`catchStatements`|<span data-ttu-id="64c9c-131">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-131">Optional.</span></span> <span data-ttu-id="64c9c-132">Instruções para tratar erros que ocorrem no `Try` bloco associado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-132">Statement(s) to handle errors that occur in the associated `Try` block.</span></span> <span data-ttu-id="64c9c-133">Pode ser uma instrução composta.</span><span class="sxs-lookup"><span data-stu-id="64c9c-133">Can be a compound statement.</span></span>|
|`Exit Try`|<span data-ttu-id="64c9c-134">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-134">Optional.</span></span> <span data-ttu-id="64c9c-135">Palavra-chave que divide a `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="64c9c-135">Keyword that breaks out of the `Try...Catch...Finally` structure.</span></span> <span data-ttu-id="64c9c-136">A execução é retomada com o código imediatamente após a `End Try` instrução.</span><span class="sxs-lookup"><span data-stu-id="64c9c-136">Execution resumes with the code immediately following the `End Try` statement.</span></span> <span data-ttu-id="64c9c-137">A `Finally` instrução ainda será executada.</span><span class="sxs-lookup"><span data-stu-id="64c9c-137">The `Finally` statement will still be executed.</span></span> <span data-ttu-id="64c9c-138">Não permitido em `Finally` blocos.</span><span class="sxs-lookup"><span data-stu-id="64c9c-138">Not allowed in `Finally` blocks.</span></span>|
|`Finally`|<span data-ttu-id="64c9c-139">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-139">Optional.</span></span> <span data-ttu-id="64c9c-140">Um `Finally` bloco é sempre executado quando a execução sai de qualquer parte da `Try...Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="64c9c-140">A `Finally` block is always executed when execution leaves any part of the `Try...Catch` statement.</span></span>|
|`finallyStatements`|<span data-ttu-id="64c9c-141">Opcional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-141">Optional.</span></span> <span data-ttu-id="64c9c-142">As instruções que são executadas após o processamento de todo o outro erro ocorreram.</span><span class="sxs-lookup"><span data-stu-id="64c9c-142">Statement(s) that are executed after all other error processing has occurred.</span></span>|
|`End Try`|<span data-ttu-id="64c9c-143">Encerra a `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="64c9c-143">Terminates the `Try...Catch...Finally` structure.</span></span>|

## <a name="remarks"></a><span data-ttu-id="64c9c-144">Comentários</span><span class="sxs-lookup"><span data-stu-id="64c9c-144">Remarks</span></span>

<span data-ttu-id="64c9c-145">Se você espera que uma determinada exceção ocorra durante uma determinada seção do código, coloque o código em um `Try` bloco e use um `Catch` bloco para manter o controle e manipular a exceção se ela ocorrer.</span><span class="sxs-lookup"><span data-stu-id="64c9c-145">If you expect that a particular exception might occur during a particular section of code, put the code in a `Try` block and use a `Catch` block to retain control and handle the exception if it occurs.</span></span>

<span data-ttu-id="64c9c-146">Uma `Try…Catch` instrução consiste em um `Try` bloco seguido por uma ou mais `Catch` cláusulas, que especificam manipuladores para várias exceções.</span><span class="sxs-lookup"><span data-stu-id="64c9c-146">A `Try…Catch` statement consists of a `Try` block followed by one or more `Catch` clauses, which specify handlers for various exceptions.</span></span> <span data-ttu-id="64c9c-147">Quando uma exceção é lançada em um `Try` bloco, Visual Basic procura a `Catch` instrução que manipula a exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-147">When an exception is thrown in a `Try` block, Visual Basic looks for the `Catch` statement that handles the exception.</span></span> <span data-ttu-id="64c9c-148">Se uma `Catch` instrução correspondente não for encontrada, Visual Basic examinará o método que chamou o método atual e assim por diante na pilha de chamadas.</span><span class="sxs-lookup"><span data-stu-id="64c9c-148">If a matching `Catch` statement is not found, Visual Basic examines the method that called the current method, and so on up the call stack.</span></span> <span data-ttu-id="64c9c-149">Se nenhum `Catch` bloco for encontrado, Visual Basic exibirá uma mensagem de exceção sem tratamento para o usuário e interromperá a execução do programa.</span><span class="sxs-lookup"><span data-stu-id="64c9c-149">If no `Catch` block is found, Visual Basic displays an unhandled exception message to the user and stops execution of the program.</span></span>

<span data-ttu-id="64c9c-150">Você pode usar mais de uma `Catch` instrução em uma `Try…Catch` instrução.</span><span class="sxs-lookup"><span data-stu-id="64c9c-150">You can use more than one `Catch` statement in a `Try…Catch` statement.</span></span> <span data-ttu-id="64c9c-151">Se você fizer isso, a ordem das `Catch` cláusulas será significativa, pois elas serão examinadas em ordem.</span><span class="sxs-lookup"><span data-stu-id="64c9c-151">If you do this, the order of the `Catch` clauses is significant because they are examined in order.</span></span> <span data-ttu-id="64c9c-152">Capture as exceções mais específicas antes das menos específicas.</span><span class="sxs-lookup"><span data-stu-id="64c9c-152">Catch the more specific exceptions before the less specific ones.</span></span>

<span data-ttu-id="64c9c-153">As condições de instrução a seguir `Catch` são as menos específicas e capturarão todas as exceções que derivam da <xref:System.Exception> classe.</span><span class="sxs-lookup"><span data-stu-id="64c9c-153">The following `Catch` statement conditions are the least specific, and will catch all exceptions that derive from the <xref:System.Exception> class.</span></span> <span data-ttu-id="64c9c-154">Você normalmente deve usar uma dessas variações como o último `Catch` bloco na `Try...Catch...Finally` estrutura, depois de capturar todas as exceções específicas que você espera.</span><span class="sxs-lookup"><span data-stu-id="64c9c-154">You should ordinarily use one of these variations as the last `Catch` block in the `Try...Catch...Finally` structure, after catching all the specific exceptions you expect.</span></span> <span data-ttu-id="64c9c-155">O fluxo de controle nunca pode alcançar um `Catch` bloco que siga qualquer uma dessas variações.</span><span class="sxs-lookup"><span data-stu-id="64c9c-155">Control flow can never reach a `Catch` block that follows either of these variations.</span></span>

- <span data-ttu-id="64c9c-156">O `type` é `Exception` , por exemplo:`Catch ex As Exception`</span><span class="sxs-lookup"><span data-stu-id="64c9c-156">The `type` is `Exception`, for example: `Catch ex As Exception`</span></span>

- <span data-ttu-id="64c9c-157">A instrução não tem nenhuma `exception` variável, por exemplo:`Catch`</span><span class="sxs-lookup"><span data-stu-id="64c9c-157">The statement has no `exception` variable, for example: `Catch`</span></span>

<span data-ttu-id="64c9c-158">Quando uma `Try…Catch…Finally` instrução é aninhada em outro `Try` bloco, Visual Basic primeiro examina cada `Catch` instrução no bloco interno `Try` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-158">When a `Try…Catch…Finally` statement is nested in another `Try` block, Visual Basic first examines each `Catch` statement in the innermost `Try` block.</span></span> <span data-ttu-id="64c9c-159">Se nenhuma `Catch` instrução correspondente for encontrada, a pesquisa prosseguirá para as `Catch` instruções do bloco externo `Try…Catch…Finally` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-159">If no matching `Catch` statement is found, the search proceeds to the `Catch` statements of the outer `Try…Catch…Finally` block.</span></span>

<span data-ttu-id="64c9c-160">As variáveis locais de um `Try` bloco não estão disponíveis em um `Catch` bloco porque são blocos separados.</span><span class="sxs-lookup"><span data-stu-id="64c9c-160">Local variables from a `Try` block are not available in a `Catch` block because they are separate blocks.</span></span> <span data-ttu-id="64c9c-161">Se você quiser usar uma variável em mais de um bloco, declare a variável fora da `Try...Catch...Finally` estrutura.</span><span class="sxs-lookup"><span data-stu-id="64c9c-161">If you want to use a variable in more than one block, declare the variable outside the `Try...Catch...Finally` structure.</span></span>

> [!TIP]
> <span data-ttu-id="64c9c-162">A `Try…Catch…Finally` instrução está disponível como um trecho de código IntelliSense.</span><span class="sxs-lookup"><span data-stu-id="64c9c-162">The `Try…Catch…Finally` statement is available as an IntelliSense code snippet.</span></span> <span data-ttu-id="64c9c-163">No Gerenciador de trechos de código, expanda **padrões de código-se, para cada um, Tente catch, propriedade, etc**. e, em seguida, **tratamento de erro (exceções)**.</span><span class="sxs-lookup"><span data-stu-id="64c9c-163">In the Code Snippets Manager, expand **Code Patterns - If, For Each, Try Catch, Property, etc**, and then **Error Handling (Exceptions)**.</span></span> <span data-ttu-id="64c9c-164">Para obter mais informações, consulte [Snippets de Código](/visualstudio/ide/code-snippets).</span><span class="sxs-lookup"><span data-stu-id="64c9c-164">For more information, see [Code Snippets](/visualstudio/ide/code-snippets).</span></span>

## <a name="finally-block"></a><span data-ttu-id="64c9c-165">Bloco finally</span><span class="sxs-lookup"><span data-stu-id="64c9c-165">Finally block</span></span>

<span data-ttu-id="64c9c-166">Se você tiver uma ou mais instruções que devem ser executadas antes de sair da `Try` estrutura, use um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-166">If you have one or more statements that must run before you exit the `Try` structure, use a `Finally` block.</span></span> <span data-ttu-id="64c9c-167">O controle passa para o `Finally` bloco logo antes de passar da `Try…Catch` estrutura.</span><span class="sxs-lookup"><span data-stu-id="64c9c-167">Control passes to the `Finally` block just before it passes out of the `Try…Catch` structure.</span></span> <span data-ttu-id="64c9c-168">Isso é verdadeiro mesmo se ocorrer uma exceção em qualquer lugar dentro da `Try` estrutura.</span><span class="sxs-lookup"><span data-stu-id="64c9c-168">This is true even if an exception occurs anywhere inside the `Try` structure.</span></span>

<span data-ttu-id="64c9c-169">Um `Finally` bloco é útil para executar qualquer código que deve ser executado mesmo se houver uma exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-169">A `Finally` block is useful for running any code that must execute even if there is an exception.</span></span> <span data-ttu-id="64c9c-170">O controle é passado para o `Finally` bloco, independentemente de como o `Try...Catch` bloco é encerrado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-170">Control is passed to the `Finally` block regardless of how the `Try...Catch` block exits.</span></span>

<span data-ttu-id="64c9c-171">O código em um `Finally` bloco é executado mesmo que seu código encontre uma `Return` instrução em `Try` um `Catch` bloco ou.</span><span class="sxs-lookup"><span data-stu-id="64c9c-171">The code in a `Finally` block runs even if your code encounters a `Return` statement in a `Try` or `Catch` block.</span></span> <span data-ttu-id="64c9c-172">O controle não passa de um `Try` `Catch` bloco ou para o `Finally` bloco correspondente nos seguintes casos:</span><span class="sxs-lookup"><span data-stu-id="64c9c-172">Control does not pass from a `Try` or `Catch` block to the corresponding `Finally` block in the following cases:</span></span>

- <span data-ttu-id="64c9c-173">Uma [instrução End](end-statement.md) é encontrada no `Try` bloco ou `Catch` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-173">An [End Statement](end-statement.md) is encountered in the `Try` or `Catch` block.</span></span>

- <span data-ttu-id="64c9c-174">Um <xref:System.StackOverflowException> é gerado no `Try` bloco ou `Catch` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-174">A <xref:System.StackOverflowException> is thrown in the `Try` or `Catch` block.</span></span>

<span data-ttu-id="64c9c-175">Não é válido transferir explicitamente a execução para um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-175">It is not valid to explicitly transfer execution into a `Finally` block.</span></span> <span data-ttu-id="64c9c-176">A transferência da execução para fora de um `Finally` bloco não é válida, exceto por meio de uma exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-176">Transferring execution out of a `Finally` block is not valid, except through an exception.</span></span>

<span data-ttu-id="64c9c-177">Se uma `Try` instrução não contiver pelo menos um `Catch` bloco, ela deverá conter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-177">If a `Try` statement does not contain at least one `Catch` block, it must contain a `Finally` block.</span></span>

> [!TIP]
> <span data-ttu-id="64c9c-178">Se você não precisar capturar exceções específicas, a `Using` instrução se comportará como um `Try…Finally` bloco e garantirá a alienação dos recursos, independentemente de como você sai do bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-178">If you do not have to catch specific exceptions, the `Using` statement behaves like a `Try…Finally` block, and guarantees disposal of the resources, regardless of how you exit the block.</span></span> <span data-ttu-id="64c9c-179">Isso é verdadeiro mesmo com uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="64c9c-179">This is true even with an unhandled exception.</span></span> <span data-ttu-id="64c9c-180">Para obter mais informações, consulte [usando a instrução](using-statement.md).</span><span class="sxs-lookup"><span data-stu-id="64c9c-180">For more information, see [Using Statement](using-statement.md).</span></span>

## <a name="exception-argument"></a><span data-ttu-id="64c9c-181">Argumento de exceção</span><span class="sxs-lookup"><span data-stu-id="64c9c-181">Exception argument</span></span>

<span data-ttu-id="64c9c-182">O `Catch` `exception` argumento Block é uma instância da <xref:System.Exception> classe ou uma classe derivada da `Exception` classe.</span><span class="sxs-lookup"><span data-stu-id="64c9c-182">The `Catch` block `exception` argument is an instance of the <xref:System.Exception> class or a class that derives from the `Exception` class.</span></span> <span data-ttu-id="64c9c-183">A `Exception` instância de classe corresponde ao erro que ocorreu no `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-183">The `Exception` class instance corresponds to the error that occurred in the `Try` block.</span></span>

<span data-ttu-id="64c9c-184">As propriedades do `Exception` objeto ajudam a identificar a causa e o local de uma exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-184">The properties of the `Exception` object help to identify the cause and location of an exception.</span></span> <span data-ttu-id="64c9c-185">Por exemplo, a <xref:System.Exception.StackTrace%2A> propriedade lista os métodos chamados que levaram à exceção, ajudando você a descobrir onde o erro ocorreu no código.</span><span class="sxs-lookup"><span data-stu-id="64c9c-185">For example, the <xref:System.Exception.StackTrace%2A> property lists the called methods that led to the exception, helping you find where the error occurred in the code.</span></span> <span data-ttu-id="64c9c-186"><xref:System.Exception.Message%2A>Retorna uma mensagem que descreve a exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-186"><xref:System.Exception.Message%2A> returns a message that describes the exception.</span></span> <span data-ttu-id="64c9c-187"><xref:System.Exception.HelpLink%2A>Retorna um link para um arquivo de ajuda associado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-187"><xref:System.Exception.HelpLink%2A> returns a link to an associated Help file.</span></span> <span data-ttu-id="64c9c-188"><xref:System.Exception.InnerException%2A>Retorna o `Exception` objeto que causou a exceção atual ou retorna se não `Nothing` houver nenhum original `Exception` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-188"><xref:System.Exception.InnerException%2A> returns the `Exception` object that caused the current exception, or it returns `Nothing` if there is no original `Exception`.</span></span>

## <a name="considerations-when-using-a-trycatch-statement"></a><span data-ttu-id="64c9c-189">Considerações ao usar uma tentativa... Instrução Catch</span><span class="sxs-lookup"><span data-stu-id="64c9c-189">Considerations when using a Try…Catch statement</span></span>

<span data-ttu-id="64c9c-190">Use uma `Try…Catch` instrução somente para sinalizar a ocorrência de eventos de programa incomuns ou inesperados.</span><span class="sxs-lookup"><span data-stu-id="64c9c-190">Use a `Try…Catch` statement only to signal the occurrence of unusual or unanticipated program events.</span></span> <span data-ttu-id="64c9c-191">Os motivos para isso incluem o seguinte:</span><span class="sxs-lookup"><span data-stu-id="64c9c-191">Reasons for this include the following:</span></span>

- <span data-ttu-id="64c9c-192">A captura de exceções em tempo de execução cria sobrecarga adicional e provavelmente será mais lenta do que a verificação prévia para evitar exceções.</span><span class="sxs-lookup"><span data-stu-id="64c9c-192">Catching exceptions at runtime creates additional overhead, and is likely to be slower than pre-checking to avoid exceptions.</span></span>

- <span data-ttu-id="64c9c-193">Se um `Catch` bloco não for manipulado corretamente, a exceção poderá não ser relatada corretamente aos usuários.</span><span class="sxs-lookup"><span data-stu-id="64c9c-193">If a `Catch` block is not handled correctly, the exception might not be reported correctly to users.</span></span>

- <span data-ttu-id="64c9c-194">A manipulação de exceções torna um programa mais complexo.</span><span class="sxs-lookup"><span data-stu-id="64c9c-194">Exception handling makes a program more complex.</span></span>

<span data-ttu-id="64c9c-195">Você nem sempre precisa de uma `Try…Catch` instrução para verificar se há uma condição que provavelmente ocorrerá.</span><span class="sxs-lookup"><span data-stu-id="64c9c-195">You do not always need a `Try…Catch` statement to check for a condition that is likely to occur.</span></span> <span data-ttu-id="64c9c-196">O exemplo a seguir verifica se um arquivo existe antes de tentar abri-lo.</span><span class="sxs-lookup"><span data-stu-id="64c9c-196">The following example checks whether a file exists before trying to open it.</span></span> <span data-ttu-id="64c9c-197">Isso reduz a necessidade de capturar uma exceção gerada pelo <xref:System.IO.File.OpenText%2A> método.</span><span class="sxs-lookup"><span data-stu-id="64c9c-197">This reduces the need for catching an exception thrown by the <xref:System.IO.File.OpenText%2A> method.</span></span>

[!code-vb[VbVbalrStatements#94](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#94)]

<span data-ttu-id="64c9c-198">Certifique-se de que o código em `Catch` blocos possa relatar corretamente exceções aos usuários, seja por meio de log seguro de threads ou mensagens apropriadas.</span><span class="sxs-lookup"><span data-stu-id="64c9c-198">Ensure that code in `Catch` blocks can properly report exceptions to users, whether through thread-safe logging or appropriate messages.</span></span> <span data-ttu-id="64c9c-199">Caso contrário, as exceções podem permanecer desconhecidas.</span><span class="sxs-lookup"><span data-stu-id="64c9c-199">Otherwise, exceptions might remain unknown.</span></span>

## <a name="async-methods"></a><span data-ttu-id="64c9c-200">Métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="64c9c-200">Async methods</span></span>

<span data-ttu-id="64c9c-201">Se você marcar um método com o modificador [Async](../modifiers/async.md) , poderá usar o operador [Await](../operators/await-operator.md) no método.</span><span class="sxs-lookup"><span data-stu-id="64c9c-201">If you mark a method with the [Async](../modifiers/async.md) modifier, you can use the [Await](../operators/await-operator.md) operator in the method.</span></span> <span data-ttu-id="64c9c-202">Uma instrução com o `Await` operador suspende a execução do método até que a tarefa esperada seja concluída.</span><span class="sxs-lookup"><span data-stu-id="64c9c-202">A statement with the `Await` operator suspends execution of the method until the awaited task completes.</span></span> <span data-ttu-id="64c9c-203">A tarefa representa um trabalho em andamento.</span><span class="sxs-lookup"><span data-stu-id="64c9c-203">The task represents ongoing work.</span></span> <span data-ttu-id="64c9c-204">Quando a tarefa associada ao `Await` operador for concluída, a execução será retomada no mesmo método.</span><span class="sxs-lookup"><span data-stu-id="64c9c-204">When the task that's associated with the `Await` operator finishes, execution resumes in the same method.</span></span> <span data-ttu-id="64c9c-205">Para obter mais informações, consulte [fluxo de controle em programas assíncronos](../../programming-guide/concepts/async/control-flow-in-async-programs.md).</span><span class="sxs-lookup"><span data-stu-id="64c9c-205">For more information, see [Control Flow in Async Programs](../../programming-guide/concepts/async/control-flow-in-async-programs.md).</span></span>

<span data-ttu-id="64c9c-206">Uma tarefa retornada por um método assíncrono pode terminar em um estado com falha, indicando que ele foi concluído devido a uma exceção sem tratamento.</span><span class="sxs-lookup"><span data-stu-id="64c9c-206">A task returned by an Async method may end in a faulted state, indicating that it completed due to an unhandled exception.</span></span> <span data-ttu-id="64c9c-207">Uma tarefa também pode terminar em um estado cancelado, o que resulta na `OperationCanceledException` geração da expressão Await.</span><span class="sxs-lookup"><span data-stu-id="64c9c-207">A task may also end in a canceled state, which results in an `OperationCanceledException` being thrown out of the await expression.</span></span> <span data-ttu-id="64c9c-208">Para capturar qualquer tipo de exceção, coloque a `Await` expressão associada à tarefa em um `Try` bloco e Capture a exceção no `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-208">To catch either type of exception, place the `Await` expression that's associated with the task in a `Try` block, and catch the exception in the `Catch` block.</span></span> <span data-ttu-id="64c9c-209">Um exemplo é fornecido posteriormente neste tópico.</span><span class="sxs-lookup"><span data-stu-id="64c9c-209">An example is provided later in this topic.</span></span>

<span data-ttu-id="64c9c-210">Uma tarefa pode estar em um estado com falha porque várias exceções foram responsáveis por sua falha.</span><span class="sxs-lookup"><span data-stu-id="64c9c-210">A task can be in a faulted state because multiple exceptions were responsible for its faulting.</span></span> <span data-ttu-id="64c9c-211">Por exemplo, a tarefa pode ser o resultado de uma chamada para <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="64c9c-211">For example, the task might be the result of a call to <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType>.</span></span> <span data-ttu-id="64c9c-212">Quando você aguarda tal tarefa, a exceção capturada é apenas uma das exceções, e você não pode prever qual exceção será detectada.</span><span class="sxs-lookup"><span data-stu-id="64c9c-212">When you await such a task, the caught exception is only one of the exceptions, and you can't predict which exception will be caught.</span></span> <span data-ttu-id="64c9c-213">Um exemplo é fornecido posteriormente neste tópico.</span><span class="sxs-lookup"><span data-stu-id="64c9c-213">An example is provided later in this topic.</span></span>

<span data-ttu-id="64c9c-214">Uma `Await` expressão não pode estar dentro de um `Catch` bloco ou `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-214">An `Await` expression can't be inside a `Catch` block or `Finally` block.</span></span>

## <a name="iterators"></a><span data-ttu-id="64c9c-215">Iterators</span><span class="sxs-lookup"><span data-stu-id="64c9c-215">Iterators</span></span>

<span data-ttu-id="64c9c-216">Uma função ou `Get` acessador de iterador executa uma iteração personalizada em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-216">An iterator function or `Get` accessor performs a custom iteration over a collection.</span></span> <span data-ttu-id="64c9c-217">Um iterador usa uma instrução [yield](yield-statement.md) para retornar cada elemento da coleção um de cada vez.</span><span class="sxs-lookup"><span data-stu-id="64c9c-217">An iterator uses a [Yield](yield-statement.md) statement to return each element of the collection one at a time.</span></span> <span data-ttu-id="64c9c-218">Você chama uma função de iterador usando um [para cada... Próxima instrução](for-each-next-statement.md).</span><span class="sxs-lookup"><span data-stu-id="64c9c-218">You call an iterator function by using a [For Each...Next Statement](for-each-next-statement.md).</span></span>

<span data-ttu-id="64c9c-219">Uma `Yield` instrução pode estar dentro de um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-219">A `Yield` statement can be inside a `Try` block.</span></span> <span data-ttu-id="64c9c-220">Um `Try` bloco que contém uma `Yield` instrução pode ter `Catch` blocos e pode ter um `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-220">A `Try` block that contains a `Yield` statement can have `Catch` blocks, and can have a `Finally` block.</span></span> <span data-ttu-id="64c9c-221">Consulte a seção "blocos try em Visual Basic" de [iteradores](../../programming-guide/concepts/iterators.md) para obter um exemplo.</span><span class="sxs-lookup"><span data-stu-id="64c9c-221">See the "Try Blocks in Visual Basic" section of [Iterators](../../programming-guide/concepts/iterators.md) for an example.</span></span>

<span data-ttu-id="64c9c-222">Uma `Yield` instrução não pode estar dentro de um `Catch` bloco ou `Finally` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-222">A `Yield` statement cannot be inside a `Catch` block or a `Finally` block.</span></span>

<span data-ttu-id="64c9c-223">Se o `For Each` corpo (fora da função de iterador) gerar uma exceção, um `Catch` bloco na função de iterador não será executado, mas um `Finally` bloco na função de iterador será executado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-223">If the `For Each` body (outside of the iterator function) throws an exception, a `Catch` block in the iterator function is not executed, but a `Finally` block in the iterator function is executed.</span></span> <span data-ttu-id="64c9c-224">Um `Catch` bloco dentro de uma função de iterador captura apenas as exceções que ocorrem dentro da função de iterador.</span><span class="sxs-lookup"><span data-stu-id="64c9c-224">A `Catch` block inside an iterator function catches only exceptions that occur inside the iterator function.</span></span>

## <a name="partial-trust-situations"></a><span data-ttu-id="64c9c-225">Situações de confiança parcial</span><span class="sxs-lookup"><span data-stu-id="64c9c-225">Partial-trust situations</span></span>

<span data-ttu-id="64c9c-226">Em situações de confiança parcial, como um aplicativo hospedado em um compartilhamento de rede, `Try...Catch...Finally` o não captura exceções de segurança que ocorrem antes que o método que contém a chamada seja invocado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-226">In partial-trust situations, such as an application hosted on a network share, `Try...Catch...Finally` does not catch security exceptions that occur before the method that contains the call is invoked.</span></span> <span data-ttu-id="64c9c-227">O exemplo a seguir, quando você o coloca em um compartilhamento de servidor e executado a partir daí, produz o erro "System. Security. SecurityException: falha na solicitação."</span><span class="sxs-lookup"><span data-stu-id="64c9c-227">The following example, when you put it on a server share and run from there, produces the error "System.Security.SecurityException: Request Failed."</span></span> <span data-ttu-id="64c9c-228">Para obter mais informações sobre exceções de segurança, consulte a <xref:System.Security.SecurityException> classe.</span><span class="sxs-lookup"><span data-stu-id="64c9c-228">For more information about security exceptions, see the <xref:System.Security.SecurityException> class.</span></span>

[!code-vb[VbVbalrStatements#85](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#85)]

<span data-ttu-id="64c9c-229">Nessa situação de confiança parcial, você precisa colocar a `Process.Start` instrução em uma separada `Sub` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-229">In such a partial-trust situation, you have to put the `Process.Start` statement in a separate `Sub`.</span></span> <span data-ttu-id="64c9c-230">A chamada inicial para o `Sub` falhará.</span><span class="sxs-lookup"><span data-stu-id="64c9c-230">The initial call to the `Sub` will fail.</span></span> <span data-ttu-id="64c9c-231">Isso permite que o `Try...Catch` o acompanhe antes `Sub` que o que contém `Process.Start` seja iniciado e a exceção de segurança produzida.</span><span class="sxs-lookup"><span data-stu-id="64c9c-231">This enables `Try...Catch` to catch it before the `Sub` that contains `Process.Start` is started and the security exception produced.</span></span>

## <a name="examples"></a><span data-ttu-id="64c9c-232">Exemplos</span><span class="sxs-lookup"><span data-stu-id="64c9c-232">Examples</span></span>

### <a name="the-structure-of-trycatchfinally"></a><span data-ttu-id="64c9c-233">A estrutura de try... Capturar... Disso</span><span class="sxs-lookup"><span data-stu-id="64c9c-233">The structure of Try...Catch...Finally</span></span>

<span data-ttu-id="64c9c-234">O exemplo a seguir ilustra a estrutura da `Try...Catch...Finally` instrução.</span><span class="sxs-lookup"><span data-stu-id="64c9c-234">The following example illustrates the structure of the `Try...Catch...Finally` statement.</span></span>

[!code-vb[VbVbalrStatements#86](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#86)]  

### <a name="exception-in-a-method-called-from-a-try-block"></a><span data-ttu-id="64c9c-235">Exceção em um método chamado a partir de um bloco try</span><span class="sxs-lookup"><span data-stu-id="64c9c-235">Exception in a method called from a Try block</span></span>

<span data-ttu-id="64c9c-236">No exemplo a seguir, o `CreateException` método gera um `NullReferenceException` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-236">In the following example, the `CreateException` method throws a `NullReferenceException`.</span></span> <span data-ttu-id="64c9c-237">O código que gera a exceção não está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-237">The code that generates the exception is not in a `Try` block.</span></span> <span data-ttu-id="64c9c-238">Portanto, o `CreateException` método não trata a exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-238">Therefore, the `CreateException` method does not handle the exception.</span></span> <span data-ttu-id="64c9c-239">O `RunSample` método manipula a exceção porque a chamada para o `CreateException` método está em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-239">The `RunSample` method does handle the exception because the call to the `CreateException` method is in a `Try` block.</span></span>

<span data-ttu-id="64c9c-240">O exemplo inclui `Catch` instruções para vários tipos de exceções, ordenadas da mais específica para a mais geral.</span><span class="sxs-lookup"><span data-stu-id="64c9c-240">The example includes `Catch` statements for several types of exceptions, ordered from the most specific to the most general.</span></span>

[!code-vb[VbVbalrStatements#91](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#91)]

### <a name="the-catch-when-statement"></a><span data-ttu-id="64c9c-241">A instrução Catch When</span><span class="sxs-lookup"><span data-stu-id="64c9c-241">The Catch When statement</span></span>

<span data-ttu-id="64c9c-242">O exemplo a seguir mostra como usar uma `Catch When` instrução para filtrar em uma expressão condicional.</span><span class="sxs-lookup"><span data-stu-id="64c9c-242">The following example shows how to use a `Catch When` statement to filter on a conditional expression.</span></span> <span data-ttu-id="64c9c-243">Se a expressão condicional for avaliada como `True` , o código no `Catch` bloco será executado.</span><span class="sxs-lookup"><span data-stu-id="64c9c-243">If the conditional expression evaluates to `True`, the code in the `Catch` block runs.</span></span>

[!code-vb[VbVbalrStatements#92](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#92)]

### <a name="nested-try-statements"></a><span data-ttu-id="64c9c-244">Instruções Try aninhadas</span><span class="sxs-lookup"><span data-stu-id="64c9c-244">Nested Try statements</span></span>

<span data-ttu-id="64c9c-245">O exemplo a seguir tem uma `Try…Catch` instrução contida em um `Try` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-245">The following example has a `Try…Catch` statement that is contained in a `Try` block.</span></span> <span data-ttu-id="64c9c-246">O `Catch` bloco interno gera uma exceção que tem sua `InnerException` propriedade definida como a exceção original.</span><span class="sxs-lookup"><span data-stu-id="64c9c-246">The inner `Catch` block throws an exception that has its `InnerException` property set to the original exception.</span></span> <span data-ttu-id="64c9c-247">O `Catch` bloco externo relata sua própria exceção e a exceção interna.</span><span class="sxs-lookup"><span data-stu-id="64c9c-247">The outer `Catch` block reports its own exception and the inner exception.</span></span>

[!code-vb[VbVbalrStatements#93](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrStatements/VB/Class1.vb#93)]

### <a name="exception-handling-for-async-methods"></a><span data-ttu-id="64c9c-248">Tratamento de exceção para métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="64c9c-248">Exception handling for async methods</span></span>

<span data-ttu-id="64c9c-249">O exemplo a seguir ilustra o tratamento de exceção para métodos assíncronos.</span><span class="sxs-lookup"><span data-stu-id="64c9c-249">The following example illustrates exception handling for async methods.</span></span> <span data-ttu-id="64c9c-250">Para capturar uma exceção que se aplica a uma tarefa assíncrona, a `Await` expressão está em um `Try` bloco do chamador e a exceção é capturada no `Catch` bloco.</span><span class="sxs-lookup"><span data-stu-id="64c9c-250">To catch an exception that applies to an async task, the `Await` expression is in a `Try` block of the caller, and the exception is caught in the `Catch` block.</span></span>

<span data-ttu-id="64c9c-251">Remova a marca de comentário da linha `Throw New Exception` no exemplo para demonstrar o tratamento de exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-251">Uncomment the `Throw New Exception` line in the example to demonstrate exception handling.</span></span> <span data-ttu-id="64c9c-252">A exceção é capturada no `Catch` bloco, a propriedade da tarefa `IsFaulted` é definida como `True` e a propriedade da tarefa `Exception.InnerException` é definida como a exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-252">The exception is caught in the `Catch` block, the task's `IsFaulted` property is set to `True`, and the task's `Exception.InnerException` property is set to the exception.</span></span>

<span data-ttu-id="64c9c-253">Remova a marca de comentário da linha `Throw New OperationCancelledException` para demonstrar o que acontece quando você cancela um processo assíncrono.</span><span class="sxs-lookup"><span data-stu-id="64c9c-253">Uncomment the `Throw New OperationCancelledException` line to demonstrate what happens when you cancel an asynchronous process.</span></span> <span data-ttu-id="64c9c-254">A exceção é capturada no `Catch` bloco e a propriedade da tarefa `IsCanceled` é definida como `True` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-254">The exception is caught in the `Catch` block, and the task's `IsCanceled` property is set to `True`.</span></span> <span data-ttu-id="64c9c-255">No entanto, em algumas condições que não se aplicam a este exemplo, `IsFaulted` é definido como `True` e `IsCanceled` é definido como `False` .</span><span class="sxs-lookup"><span data-stu-id="64c9c-255">However, under some conditions that don't apply to this example, `IsFaulted` is set to `True` and `IsCanceled` is set to `False`.</span></span>

[!code-vb[csAsyncExceptions#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/csasyncexceptions/vb/class1.vb#1)]

### <a name="handling-multiple-exceptions-in-async-methods"></a><span data-ttu-id="64c9c-256">Manipulando várias exceções em métodos assíncronos</span><span class="sxs-lookup"><span data-stu-id="64c9c-256">Handling multiple exceptions in async methods</span></span>

<span data-ttu-id="64c9c-257">O exemplo a seguir ilustra a manipulação de exceção em que várias tarefas podem resultar em várias exceções.</span><span class="sxs-lookup"><span data-stu-id="64c9c-257">The following example illustrates exception handling where multiple tasks can result in multiple exceptions.</span></span> <span data-ttu-id="64c9c-258">O `Try` bloco tem a `Await` expressão para a tarefa <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> retornada.</span><span class="sxs-lookup"><span data-stu-id="64c9c-258">The `Try` block has the `Await` expression for the task that <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> returned.</span></span> <span data-ttu-id="64c9c-259">A tarefa é concluída quando as três tarefas às quais <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> é aplicada são concluídas.</span><span class="sxs-lookup"><span data-stu-id="64c9c-259">The task is complete when the three tasks to which <xref:System.Threading.Tasks.Task.WhenAll%2A?displayProperty=nameWithType> is applied are complete.</span></span>

<span data-ttu-id="64c9c-260">Cada uma das três tarefas causa uma exceção.</span><span class="sxs-lookup"><span data-stu-id="64c9c-260">Each of the three tasks causes an exception.</span></span> <span data-ttu-id="64c9c-261">O `Catch` bloco itera pelas exceções, que são encontradas na `Exception.InnerExceptions` propriedade da tarefa `Task.WhenAll` retornada.</span><span class="sxs-lookup"><span data-stu-id="64c9c-261">The `Catch` block iterates through the exceptions, which are found in the `Exception.InnerExceptions` property of the task that `Task.WhenAll` returned.</span></span>

[!code-vb[csAsyncExceptions#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/csasyncexceptions/vb/class1.vb#3)]

## <a name="see-also"></a><span data-ttu-id="64c9c-262">Confira também</span><span class="sxs-lookup"><span data-stu-id="64c9c-262">See also</span></span>

- <xref:Microsoft.VisualBasic.Information.Err%2A>
- <xref:System.Exception>
- [<span data-ttu-id="64c9c-263">Instrução Exit</span><span class="sxs-lookup"><span data-stu-id="64c9c-263">Exit Statement</span></span>](exit-statement.md)
- [<span data-ttu-id="64c9c-264">Instrução On Error</span><span class="sxs-lookup"><span data-stu-id="64c9c-264">On Error Statement</span></span>](on-error-statement.md)
- [<span data-ttu-id="64c9c-265">Melhores práticas para usar snippets de código</span><span class="sxs-lookup"><span data-stu-id="64c9c-265">Best Practices for Using Code Snippets</span></span>](/visualstudio/ide/best-practices-for-using-code-snippets)
- [<span data-ttu-id="64c9c-266">Tratamento de exceção</span><span class="sxs-lookup"><span data-stu-id="64c9c-266">Exception Handling</span></span>](../../../standard/parallel-programming/exception-handling-task-parallel-library.md)
- [<span data-ttu-id="64c9c-267">Instrução Throw</span><span class="sxs-lookup"><span data-stu-id="64c9c-267">Throw Statement</span></span>](throw-statement.md)
