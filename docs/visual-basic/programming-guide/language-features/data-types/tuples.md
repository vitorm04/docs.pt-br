---
title: Tuplas
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: e0310f31d7becb1f79bb023a277bd565421b44fb
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74350494"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="bc256-102">Tuplas (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="bc256-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="bc256-103">A partir do Visual Basic 2017, a linguagem de Visual Basic oferece suporte interno para tuplas que facilita a criação de tuplas e o acesso aos elementos de tuplas.</span><span class="sxs-lookup"><span data-stu-id="bc256-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="bc256-104">Uma tupla é uma estrutura de dados leve que tem um número específico e uma sequência de valores.</span><span class="sxs-lookup"><span data-stu-id="bc256-104">A tuple is a lightweight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="bc256-105">Ao instanciar a tupla, você define o número e o tipo de dados de cada valor (ou elemento).</span><span class="sxs-lookup"><span data-stu-id="bc256-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="bc256-106">Por exemplo, um 2 tupla (ou par) tem dois elementos.</span><span class="sxs-lookup"><span data-stu-id="bc256-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="bc256-107">O primeiro pode ser um valor `Boolean`, enquanto o segundo é um `String`.</span><span class="sxs-lookup"><span data-stu-id="bc256-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="bc256-108">Como as tuplas facilitam o armazenamento de vários valores em um único objeto, elas são geralmente usadas como uma maneira leve de retornar vários valores de um método.</span><span class="sxs-lookup"><span data-stu-id="bc256-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="bc256-109">O suporte à tupla requer o tipo de <xref:System.ValueTuple>.</span><span class="sxs-lookup"><span data-stu-id="bc256-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="bc256-110">Se o .NET Framework 4,7 não estiver instalado, você deverá adicionar o pacote NuGet `System.ValueTuple`, que está disponível na galeria do NuGet.</span><span class="sxs-lookup"><span data-stu-id="bc256-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="bc256-111">Sem esse pacote, você pode obter um erro de compilação semelhante a, "o tipo predefinido ' ValueTuple (Of,,,) ' não está definido ou importado."</span><span class="sxs-lookup"><span data-stu-id="bc256-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="bc256-112">Criando uma instância e usando uma tupla</span><span class="sxs-lookup"><span data-stu-id="bc256-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="bc256-113">Você cria uma instância de uma tupla ao colocar seus parênteses de mensagem de valores delimitados por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="bc256-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="bc256-114">Cada um desses valores se torna um campo da tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="bc256-115">Por exemplo, o código a seguir define um triplo (ou três tuplas) com um `Date` como seu primeiro valor, um `String` como o segundo e um `Boolean` como terceiro.</span><span class="sxs-lookup"><span data-stu-id="bc256-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="bc256-116">Por padrão, o nome de cada campo em uma tupla consiste na cadeia de caracteres `Item` junto com a posição baseada em um campo na tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="bc256-117">Para essa 3 tupla, o campo `Date` é `Item1`, o campo `String` é `Item2`e o campo `Boolean` é `Item3`.</span><span class="sxs-lookup"><span data-stu-id="bc256-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="bc256-118">O exemplo a seguir exibe os valores dos campos da tupla instanciados na linha de código anterior</span><span class="sxs-lookup"><span data-stu-id="bc256-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="bc256-119">Os campos de uma tupla de Visual Basic são de leitura/gravação; Depois de criar uma instância de uma tupla, você pode modificar seus valores.</span><span class="sxs-lookup"><span data-stu-id="bc256-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="bc256-120">O exemplo a seguir modifica dois dos três campos da tupla criada no exemplo anterior e exibe o resultado.</span><span class="sxs-lookup"><span data-stu-id="bc256-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="bc256-121">Criando uma instância e usando uma tupla nomeada</span><span class="sxs-lookup"><span data-stu-id="bc256-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="bc256-122">Em vez de usar nomes padrão para os campos de uma tupla, você pode criar uma instância de uma *tupla nomeada* atribuindo seus próprios nomes aos elementos da tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="bc256-123">Os campos da tupla podem ser acessados por seus nomes atribuídos *ou* por seus nomes padrão.</span><span class="sxs-lookup"><span data-stu-id="bc256-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="bc256-124">O exemplo a seguir instancia a mesma 3 tupla como anteriormente, exceto que ela nomeia explicitamente o primeiro campo `EventDate`, a segunda `Name`e a terceira `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="bc256-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="bc256-125">Em seguida, ele exibe os valores de campo, modifica-os e exibe os valores de campo novamente.</span><span class="sxs-lookup"><span data-stu-id="bc256-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="bc256-126">Nomes de elementos de tupla inferidos</span><span class="sxs-lookup"><span data-stu-id="bc256-126">Inferred tuple element names</span></span>

<span data-ttu-id="bc256-127">A partir do Visual Basic 15,3, Visual Basic pode inferir os nomes dos elementos de tupla; Você não precisa atribuí-los explicitamente.</span><span class="sxs-lookup"><span data-stu-id="bc256-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="bc256-128">Nomes de tupla deduzidos são úteis quando você Inicializa uma tupla de um conjunto de variáveis e você deseja que o nome do elemento de tupla seja igual ao nome da variável.</span><span class="sxs-lookup"><span data-stu-id="bc256-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span>

<span data-ttu-id="bc256-129">O exemplo a seguir cria uma tupla `stateInfo` que contém três elementos explicitamente nomeados, `state`, `stateName`e `capital`.</span><span class="sxs-lookup"><span data-stu-id="bc256-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="bc256-130">Observe que, ao nomear os elementos, a instrução de inicialização de tupla simplesmente atribui os elementos nomeados aos valores das variáveis nomeadas de forma idêntica.</span><span class="sxs-lookup"><span data-stu-id="bc256-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]

<span data-ttu-id="bc256-131">Como elementos e variáveis têm o mesmo nome, o compilador de Visual Basic pode inferir os nomes dos campos, como mostra o exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="bc256-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="bc256-132">Para habilitar nomes de elementos de tupla deduzidos, você deve definir a versão do compilador de Visual Basic a ser usada no arquivo de projeto Visual Basic (\*. vbproj):</span><span class="sxs-lookup"><span data-stu-id="bc256-132">To enable inferred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span>

```xml
<PropertyGroup>
  <LangVersion>15.3</LangVersion>
</PropertyGroup>
```

<span data-ttu-id="bc256-133">O número de versão pode ser qualquer versão do compilador de Visual Basic começando com 15,3.</span><span class="sxs-lookup"><span data-stu-id="bc256-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="bc256-134">Em vez de embutir em código uma versão específica do compilador, você também pode especificar "mais recente" como o valor de `LangVersion` para compilar com a versão mais recente do compilador Visual Basic instalado no seu sistema.</span><span class="sxs-lookup"><span data-stu-id="bc256-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="bc256-135">Para obter mais informações, consulte [definindo a versão do idioma do Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="bc256-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="bc256-136">Em alguns casos, o compilador Visual Basic não pode inferir o nome do elemento de tupla do nome candidato, e o campo de tupla só pode ser referenciado usando seu nome padrão, como `Item1`, `Item2`, etc. Eles incluem:</span><span class="sxs-lookup"><span data-stu-id="bc256-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="bc256-137">O nome do candidato é o mesmo que o nome de um membro de tupla, como `Item3`, `Rest`ou `ToString`.</span><span class="sxs-lookup"><span data-stu-id="bc256-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="bc256-138">O nome do candidato está duplicado na tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-138">The candidate name is duplicated in the tuple.</span></span>

<span data-ttu-id="bc256-139">Quando a inferência de nome de campo falha, Visual Basic não gera um erro de compilador, nem é uma exceção lançada em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="bc256-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="bc256-140">Em vez disso, os campos de tupla devem ser referenciados por seus nomes predefinidos, como `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="bc256-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span>

## <a name="tuples-versus-structures"></a><span data-ttu-id="bc256-141">Tuplas versus estruturas</span><span class="sxs-lookup"><span data-stu-id="bc256-141">Tuples versus structures</span></span>

<span data-ttu-id="bc256-142">Uma tupla de Visual Basic é um tipo de valor que é uma instância de um dos tipos genéricos **System. ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="bc256-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="bc256-143">Por exemplo, a tupla de `holiday` definida no exemplo anterior é uma instância da estrutura de <xref:System.ValueTuple%603>.</span><span class="sxs-lookup"><span data-stu-id="bc256-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="bc256-144">Ele foi projetado para ser um contêiner leve de dados.</span><span class="sxs-lookup"><span data-stu-id="bc256-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="bc256-145">Como a tupla visa facilitar a criação de um objeto com vários itens de dados, ele não tem alguns dos recursos que uma estrutura personalizada pode ter.</span><span class="sxs-lookup"><span data-stu-id="bc256-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="bc256-146">Elas incluem:</span><span class="sxs-lookup"><span data-stu-id="bc256-146">These include:</span></span>

- <span data-ttu-id="bc256-147">Membros personalizados.</span><span class="sxs-lookup"><span data-stu-id="bc256-147">Custom members.</span></span> <span data-ttu-id="bc256-148">Você não pode definir suas próprias propriedades, métodos ou eventos para uma tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="bc256-149">Confirmação.</span><span class="sxs-lookup"><span data-stu-id="bc256-149">Validation.</span></span> <span data-ttu-id="bc256-150">Você não pode validar os dados atribuídos a campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="bc256-151">Imutabilidade.</span><span class="sxs-lookup"><span data-stu-id="bc256-151">Immutability.</span></span> <span data-ttu-id="bc256-152">Visual Basic tuplas são mutáveis.</span><span class="sxs-lookup"><span data-stu-id="bc256-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="bc256-153">Por outro lado, uma estrutura personalizada permite que você controle se uma instância é mutável ou imutável.</span><span class="sxs-lookup"><span data-stu-id="bc256-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="bc256-154">Se membros personalizados, a validação de propriedade e de campo ou a imutabilidade forem importantes, você deverá usar a instrução Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) para definir um tipo de valor personalizado.</span><span class="sxs-lookup"><span data-stu-id="bc256-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="bc256-155">Uma tupla Visual Basic herda os membros de seu tipo **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="bc256-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="bc256-156">Além de seus campos, eles incluem os seguintes métodos:</span><span class="sxs-lookup"><span data-stu-id="bc256-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="bc256-157">{1&gt;Membro&lt;1}</span><span class="sxs-lookup"><span data-stu-id="bc256-157">Member</span></span> | <span data-ttu-id="bc256-158">Descrição</span><span class="sxs-lookup"><span data-stu-id="bc256-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="bc256-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="bc256-159">CompareTo</span></span> | <span data-ttu-id="bc256-160">Compara a tupla atual com outra tupla com o mesmo número de elementos.</span><span class="sxs-lookup"><span data-stu-id="bc256-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="bc256-161">Igual a</span><span class="sxs-lookup"><span data-stu-id="bc256-161">Equals</span></span> | <span data-ttu-id="bc256-162">Determina se a tupla atual é igual a outra tupla ou objeto.</span><span class="sxs-lookup"><span data-stu-id="bc256-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="bc256-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="bc256-163">GetHashCode</span></span> | <span data-ttu-id="bc256-164">Calcula o código hash para a instância atual.</span><span class="sxs-lookup"><span data-stu-id="bc256-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="bc256-165">ToString</span><span class="sxs-lookup"><span data-stu-id="bc256-165">ToString</span></span> | <span data-ttu-id="bc256-166">Retorna a representação da cadeia de caracteres dessa tupla, que usa o formulário `(Item1, Item2...)`, onde `Item1` e `Item2` representam os valores dos campos da tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="bc256-167">Além disso, os tipos **ValueTuple** implementam <xref:System.Collections.IStructuralComparable> e <xref:System.Collections.IStructuralEquatable> interfaces, que permitem que você defina os comparadores de clientes.</span><span class="sxs-lookup"><span data-stu-id="bc256-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="bc256-168">Atribuição e tuplas</span><span class="sxs-lookup"><span data-stu-id="bc256-168">Assignment and tuples</span></span>

<span data-ttu-id="bc256-169">Visual Basic dá suporte à atribuição entre os tipos de tupla que têm o mesmo número de campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="bc256-170">Os tipos de campo podem ser convertidos se uma das seguintes opções for verdadeira:</span><span class="sxs-lookup"><span data-stu-id="bc256-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="bc256-171">O campo de origem e destino são do mesmo tipo.</span><span class="sxs-lookup"><span data-stu-id="bc256-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="bc256-172">Uma conversão de alargamento (ou implícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="bc256-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span>

- <span data-ttu-id="bc256-173">`Option Strict` é `On`e uma conversão de restrição (ou explícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="bc256-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="bc256-174">Essa conversão pode gerar uma exceção se o valor de origem estiver fora do intervalo do tipo de destino.</span><span class="sxs-lookup"><span data-stu-id="bc256-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="bc256-175">Outras conversões não são consideradas para atribuições.</span><span class="sxs-lookup"><span data-stu-id="bc256-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="bc256-176">Vamos examinar os tipos de atribuições que são permitidos entre tipos de tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="bc256-177">Considere estas variáveis usadas nos exemplos a seguir:</span><span class="sxs-lookup"><span data-stu-id="bc256-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="bc256-178">As duas primeiras variáveis, `unnamed` e `anonymous`, não têm nomes semânticos fornecidos para os campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="bc256-179">Os nomes de campo são os `Item1` e `Item2`padrão.</span><span class="sxs-lookup"><span data-stu-id="bc256-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="bc256-180">As duas últimas variáveis, `named` e `differentName` têm nomes de campo semânticos.</span><span class="sxs-lookup"><span data-stu-id="bc256-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="bc256-181">Observe que essas duas tuplas têm nomes diferentes para os campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="bc256-182">Todas as quatro tuplas têm o mesmo número de campos (chamados de ' aridade '), e os tipos desses campos são idênticos.</span><span class="sxs-lookup"><span data-stu-id="bc256-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="bc256-183">Portanto, todas essas atribuições funcionam:</span><span class="sxs-lookup"><span data-stu-id="bc256-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="bc256-184">Observe que os nomes das tuplas não são atribuídos.</span><span class="sxs-lookup"><span data-stu-id="bc256-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="bc256-185">Os valores dos campos são atribuídos na ordem dos campos na tupla.</span><span class="sxs-lookup"><span data-stu-id="bc256-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="bc256-186">Por fim, observe que podemos atribuir a tupla de `named` à tupla de `conversion`, embora o primeiro campo de `named` seja um `Integer`e o primeiro campo de `conversion` seja um `Long`.</span><span class="sxs-lookup"><span data-stu-id="bc256-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="bc256-187">Essa atribuição é realizada com sucesso porque a conversão de um `Integer` para um `Long` é uma conversão de ampliação.</span><span class="sxs-lookup"><span data-stu-id="bc256-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="bc256-188">Tuplas com números diferentes de campos não podem ser atribuídas:</span><span class="sxs-lookup"><span data-stu-id="bc256-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="bc256-189">Tuplas como valores retornados do método</span><span class="sxs-lookup"><span data-stu-id="bc256-189">Tuples as method return values</span></span>

<span data-ttu-id="bc256-190">Um método pode retornar apenas um único valor.</span><span class="sxs-lookup"><span data-stu-id="bc256-190">A method can return only a single value.</span></span> <span data-ttu-id="bc256-191">Com frequência, no entanto, você gostaria que uma chamada de método retornasse vários valores.</span><span class="sxs-lookup"><span data-stu-id="bc256-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="bc256-192">Há várias maneiras de contornar essa limitação:</span><span class="sxs-lookup"><span data-stu-id="bc256-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="bc256-193">Você pode criar uma classe personalizada ou estrutura cujas propriedades ou campos representam valores retornados pelo método.</span><span class="sxs-lookup"><span data-stu-id="bc256-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="bc256-194">Portanto, é uma solução pesada; Ele requer que você defina um tipo personalizado cujo único objetivo é recuperar valores de uma chamada de método.</span><span class="sxs-lookup"><span data-stu-id="bc256-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="bc256-195">Você pode retornar um único valor do método e retornar os valores restantes passando-os por referência ao método.</span><span class="sxs-lookup"><span data-stu-id="bc256-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="bc256-196">Isso envolve a sobrecarga de instanciar uma variável e riscos, inadvertidamente, substituir o valor da variável que você passa por referência.</span><span class="sxs-lookup"><span data-stu-id="bc256-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="bc256-197">Você pode usar uma tupla, que fornece uma solução leve para recuperar vários valores de retorno.</span><span class="sxs-lookup"><span data-stu-id="bc256-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="bc256-198">Por exemplo, os métodos **TryParse** no .net retornam um valor `Boolean` que indica se a operação de análise foi bem-sucedida.</span><span class="sxs-lookup"><span data-stu-id="bc256-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="bc256-199">O resultado da operação de análise é retornado em uma variável passada por referência ao método.</span><span class="sxs-lookup"><span data-stu-id="bc256-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="bc256-200">Normalmente, uma chamada para o método de análise, como <xref:System.Int32.TryParse%2A?displayProperty=nameWithType>, é semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="bc256-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="bc256-201">Podemos retornar uma tupla da operação de análise se encapsularmos a chamada para o método <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> em nosso próprio método.</span><span class="sxs-lookup"><span data-stu-id="bc256-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="bc256-202">No exemplo a seguir, `NumericLibrary.ParseInteger` chama o método <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> e retorna uma tupla nomeada com dois elementos.</span><span class="sxs-lookup"><span data-stu-id="bc256-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="bc256-203">Em seguida, você pode chamar o método com um código como o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bc256-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="bc256-204">Visual Basic tuplas e tuplas no .NET Framework</span><span class="sxs-lookup"><span data-stu-id="bc256-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="bc256-205">Uma tupla Visual Basic é uma instância de um dos tipos genéricos **System. ValueTuple** , que foram introduzidos no .NET Framework 4,7.</span><span class="sxs-lookup"><span data-stu-id="bc256-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="bc256-206">O .NET Framework também inclui um conjunto de classes genéricas **System. tupla** .</span><span class="sxs-lookup"><span data-stu-id="bc256-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="bc256-207">No entanto, essas classes diferem das tuplas Visual Basic e dos tipos genéricos **System. ValueTuple** de várias maneiras:</span><span class="sxs-lookup"><span data-stu-id="bc256-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="bc256-208">Os elementos das classes de **tupla** são propriedades nomeadas `Item1`, `Item2`e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="bc256-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="bc256-209">Em Visual Basic tuplas e os tipos **ValueTuple** , os elementos de tupla são campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="bc256-210">Você não pode atribuir nomes significativos aos elementos de uma instância de **tupla** ou de uma instância de **ValueTuple** .</span><span class="sxs-lookup"><span data-stu-id="bc256-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="bc256-211">Visual Basic permite atribuir nomes que comunicam o significado dos campos.</span><span class="sxs-lookup"><span data-stu-id="bc256-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="bc256-212">As propriedades de uma instância de **tupla** são somente leitura; as tuplas são imutáveis.</span><span class="sxs-lookup"><span data-stu-id="bc256-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="bc256-213">Em Visual Basic tuplas e os tipos **ValueTuple** , os campos de tupla são de leitura/gravação; as tuplas são mutáveis.</span><span class="sxs-lookup"><span data-stu-id="bc256-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="bc256-214">Os tipos de **tupla** genérica são tipos de referência.</span><span class="sxs-lookup"><span data-stu-id="bc256-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="bc256-215">Usar esses tipos de **tupla** significa alocar objetos.</span><span class="sxs-lookup"><span data-stu-id="bc256-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="bc256-216">Em caminhos de acesso, isso pode ter um impacto mensurável no desempenho do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="bc256-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="bc256-217">Visual Basic tuplas e os tipos de **ValueTuple** são tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="bc256-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="bc256-218">Os métodos de extensão na classe <xref:System.TupleExtensions> facilitam a conversão entre tuplas de Visual Basic e objetos de **tupla** .net.</span><span class="sxs-lookup"><span data-stu-id="bc256-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="bc256-219">O método **totupla** converte uma tupla de Visual Basic em um objeto de **tupla** do .net e o método **ToValueTuple** converte um objeto de **tupla** do .net em uma tupla de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="bc256-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="bc256-220">O exemplo a seguir cria uma tupla, converte-a em um objeto de **tupla** do .net e a converte de volta para uma tupla de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="bc256-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="bc256-221">Em seguida, o exemplo compara essa tupla com a original para garantir que elas sejam iguais.</span><span class="sxs-lookup"><span data-stu-id="bc256-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="bc256-222">Consulte também</span><span class="sxs-lookup"><span data-stu-id="bc256-222">See also</span></span>

- [<span data-ttu-id="bc256-223">Referência da linguagem Visual Basic</span><span class="sxs-lookup"><span data-stu-id="bc256-223">Visual Basic Language Reference</span></span>](index.md)
