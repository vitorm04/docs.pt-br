---
title: Tuplas no Visual Basic
ms.date: 04/23/2017
helpviewer_keywords:
- tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
ms.openlocfilehash: d76c14aa83fcf47faea41946614e83cfbc8ad57b
ms.sourcegitcommit: c93fd5139f9efcf6db514e3474301738a6d1d649
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/27/2018
ms.locfileid: "50195496"
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="78889-102">Tuplas (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="78889-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="78889-103">Começando com o Visual Basic 2017, a linguagem Visual Basic oferece suporte interno para tuplas tornam criar tuplas e acessar os elementos de tuplas mais fácil.</span><span class="sxs-lookup"><span data-stu-id="78889-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="78889-104">Uma tupla é uma estrutura de dados leve que tem um número específico e a sequência de valores.</span><span class="sxs-lookup"><span data-stu-id="78889-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="78889-105">Quando você cria uma instância de tupla, você define o número e o tipo de dados de cada valor (ou elemento).</span><span class="sxs-lookup"><span data-stu-id="78889-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="78889-106">Por exemplo, uma tupla de 2 (ou um par) tem dois elementos.</span><span class="sxs-lookup"><span data-stu-id="78889-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="78889-107">O primeiro pode ser um `Boolean` de valor, enquanto o segundo é um `String`.</span><span class="sxs-lookup"><span data-stu-id="78889-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="78889-108">Porque as tuplas facilitam armazenar diversos valores em um único objeto, elas geralmente são usadas como uma maneira simples de retornar vários valores de um método.</span><span class="sxs-lookup"><span data-stu-id="78889-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="78889-109">Suporte de tupla requer o <xref:System.ValueTuple> tipo.</span><span class="sxs-lookup"><span data-stu-id="78889-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="78889-110">Se o .NET Framework 4.7 não estiver instalado, você deve adicionar o pacote do NuGet `System.ValueTuple`, que está disponível na Galeria do NuGet.</span><span class="sxs-lookup"><span data-stu-id="78889-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="78889-111">Sem esse pacote, você pode receber um erro de compilação semelhante a "Tipo predefinido 'ValueTuple(Of,,,)' não está definido ou importado."</span><span class="sxs-lookup"><span data-stu-id="78889-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="78889-112">Instanciando e usando uma tupla</span><span class="sxs-lookup"><span data-stu-id="78889-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="78889-113">Você instancia uma tupla, colocando os parênteses de mensagens instantâneas de valores delimitada por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="78889-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="78889-114">Cada um desses valores, em seguida, torna-se um campo da tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="78889-115">Por exemplo, o código a seguir define um triplo (ou uma tupla de 3) com um `Date` como seu primeiro valor, uma `String` como seu segundo e um `Boolean` como sua terceira.</span><span class="sxs-lookup"><span data-stu-id="78889-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="78889-116">Por padrão, o nome de cada campo em uma tupla consiste da cadeia de caracteres `Item` juntamente com base em uma posição na tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="78889-117">Para essa tupla de 3, o `Date` campo é `Item1`, o `String` campo é `Item2`e o `Boolean` campo é `Item3`.</span><span class="sxs-lookup"><span data-stu-id="78889-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="78889-118">O exemplo a seguir exibe os valores dos campos da tupla instanciado na linha de código anterior</span><span class="sxs-lookup"><span data-stu-id="78889-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="78889-119">Os campos de uma tupla de Visual Basic são leitura / gravação; Depois de instanciar uma tupla, você pode modificar seus valores.</span><span class="sxs-lookup"><span data-stu-id="78889-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="78889-120">O exemplo a seguir modifica duas das três campos da tupla criado no exemplo anterior e exibe o resultado.</span><span class="sxs-lookup"><span data-stu-id="78889-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="78889-121">Instanciando e usando uma tupla nomeada</span><span class="sxs-lookup"><span data-stu-id="78889-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="78889-122">Em vez de usar nomes padrão para os campos da tupla, você pode criar uma instância de um *tupla nomeada* atribuindo seus próprios nomes de elementos da tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="78889-123">Campos da tupla, em seguida, podem ser acessados por seus nomes atribuídos *ou* por seus nomes padrão.</span><span class="sxs-lookup"><span data-stu-id="78889-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="78889-124">O exemplo a seguir instancia a tupla de 3 mesma de antes, exceto que ela nomeia explicitamente o primeiro campo `EventDate`, a segunda `Name`e o terceiro `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="78889-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="78889-125">Em seguida, exibe os valores de campo, modifica e exibe os valores de campo novamente.</span><span class="sxs-lookup"><span data-stu-id="78889-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="inferred-tuple-element-names"></a><span data-ttu-id="78889-126">Nomes de elementos de tupla inferidos</span><span class="sxs-lookup"><span data-stu-id="78889-126">Inferred tuple element names</span></span>

<span data-ttu-id="78889-127">Começando com o Visual Basic 15.3, Visual Basic pode inferir os nomes dos elementos de tupla; não é preciso atribuí-los explicitamente.</span><span class="sxs-lookup"><span data-stu-id="78889-127">Starting with Visual Basic 15.3, Visual Basic can infer the names of tuple elements; you do not have to assign them explicitly.</span></span> <span data-ttu-id="78889-128">Nomes de tupla inferidos são úteis quando você inicializar uma tupla a partir de um conjunto de variáveis, e você deseja que o nome do elemento de tupla para ser o mesmo que o nome da variável.</span><span class="sxs-lookup"><span data-stu-id="78889-128">Inferred tuple names are useful when you initialize a tuple from a set of variables, and you want the tuple element name to be the same as the variable name.</span></span> 

<span data-ttu-id="78889-129">O exemplo a seguir cria uma `stateInfo` elementos, de chamada de tupla que contém três explicitamente `state`, `stateName`, e `capital`.</span><span class="sxs-lookup"><span data-stu-id="78889-129">The following example creates a `stateInfo` tuple that contains three explicitly named elements, `state`, `stateName`, and `capital`.</span></span> <span data-ttu-id="78889-130">Observe que, os elementos de nomenclatura, a instrução de inicialização de tupla simplesmente atribui os elementos nomeados os valores das variáveis com nomes idênticos.</span><span class="sxs-lookup"><span data-stu-id="78889-130">Note that, in naming the elements, the tuple initialization statement simply assigns the named elements the values of the identically named variables.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#1)]
 
<span data-ttu-id="78889-131">Como variáveis e os elementos têm o mesmo nome, o compilador do Visual Basic pode inferir os nomes dos campos, como mostra o exemplo a seguir.</span><span class="sxs-lookup"><span data-stu-id="78889-131">Because elements and variables have the same name, the Visual Basic compiler can infer the names of the fields, as the following example shows.</span></span>

[!code-vb[ExplicitlyNamed](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/named-tuples/program.vb#2)]

<span data-ttu-id="78889-132">Para habilitar os nomes de elemento de tupla interred, você deve definir a versão do compilador do Visual Basic para usar em seu projeto do Visual Basic (\*. vbproj) arquivo:</span><span class="sxs-lookup"><span data-stu-id="78889-132">To enable interred tuple element names, you must define the version of the Visual Basic compiler to use in your Visual Basic project (\*.vbproj) file:</span></span> 

```xml 
<PropertyGroup> 
  <LangVersion>15.3</LangVersion> 
</PropertyGroup> 
```

<span data-ttu-id="78889-133">O número de versão pode ser qualquer versão do compilador do Visual Basic a partir do 15.3.</span><span class="sxs-lookup"><span data-stu-id="78889-133">The version number can be any version of the Visual Basic compiler starting with 15.3.</span></span> <span data-ttu-id="78889-134">Em vez de embutir uma versão específica do compilador, você também pode especificar "Mais recente" como o valor do `LangVersion` para compilar com a versão mais recente do compilador do Visual Basic instalado em seu sistema.</span><span class="sxs-lookup"><span data-stu-id="78889-134">Rather than hard-coding a specific compiler version, you can also specify "Latest" as the value of `LangVersion` to compile with the most recent version of the Visual Basic compiler installed on your system.</span></span>

<span data-ttu-id="78889-135">Para obter mais informações, consulte [definindo a versão da linguagem Visual Basic](../../../language-reference/configure-language-version.md).</span><span class="sxs-lookup"><span data-stu-id="78889-135">For more information, see [setting the Visual Basic language version](../../../language-reference/configure-language-version.md).</span></span>

<span data-ttu-id="78889-136">Em alguns casos, o compilador do Visual Basic não é possível inferir o nome do elemento de tupla do nome do candidato, e o campo de tupla só pode ser referenciado usando seu nome padrão, como `Item1`, `Item2`, etc. Elas incluem:</span><span class="sxs-lookup"><span data-stu-id="78889-136">In some cases, the Visual Basic compiler cannot infer the tuple element name from the candidate name, and the tuple field can only be referenced using its default name, such as `Item1`, `Item2`, etc. These include:</span></span>

- <span data-ttu-id="78889-137">O nome do candidato é igual ao nome de um membro da tupla, como `Item3`, `Rest`, ou `ToString`.</span><span class="sxs-lookup"><span data-stu-id="78889-137">The candidate name is the same as the name of a tuple member, such as `Item3`, `Rest`, or `ToString`.</span></span>

- <span data-ttu-id="78889-138">O nome do candidato é duplicado na tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-138">The candidate name is duplicated in the tuple.</span></span>
 
<span data-ttu-id="78889-139">Quando ocorre falha na inferência de nome de campo, o Visual Basic não gera um erro do compilador, nem é uma exceção gerada em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="78889-139">When field name inference fails, Visual Basic does not generate a compiler error, nor is an exception thrown at runtime.</span></span> <span data-ttu-id="78889-140">Em vez disso, os campos de tupla devem ser referenciados por seus nomes predefinidos, como `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="78889-140">Instead, tuple fields must be referenced by their predefined names, such as `Item1` and `Item2`.</span></span> 
  
## <a name="tuples-versus-structures"></a><span data-ttu-id="78889-141">Tuplas versus estruturas</span><span class="sxs-lookup"><span data-stu-id="78889-141">Tuples versus structures</span></span>

<span data-ttu-id="78889-142">Uma tupla de Visual Basic é um tipo de valor que é uma instância de um a um **valuetuple** tipos genéricos.</span><span class="sxs-lookup"><span data-stu-id="78889-142">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="78889-143">Por exemplo, o `holiday` tupla definida no exemplo anterior é uma instância do <xref:System.ValueTuple%603> estrutura.</span><span class="sxs-lookup"><span data-stu-id="78889-143">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="78889-144">Ele é projetado para ser um contêiner leve para dados.</span><span class="sxs-lookup"><span data-stu-id="78889-144">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="78889-145">Uma vez que a tupla tem como objetivo tornar mais fácil de criar um objeto com vários itens de dados, ele não tem alguns dos recursos que uma estrutura personalizada pode ter.</span><span class="sxs-lookup"><span data-stu-id="78889-145">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="78889-146">Elas incluem:</span><span class="sxs-lookup"><span data-stu-id="78889-146">These include:</span></span>

- <span data-ttu-id="78889-147">Membros personalizados.</span><span class="sxs-lookup"><span data-stu-id="78889-147">Custom members.</span></span> <span data-ttu-id="78889-148">Você não pode definir suas próprias propriedades, métodos ou eventos para uma tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-148">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="78889-149">Validação.</span><span class="sxs-lookup"><span data-stu-id="78889-149">Validation.</span></span> <span data-ttu-id="78889-150">Você não pode validar os dados atribuídos a campos.</span><span class="sxs-lookup"><span data-stu-id="78889-150">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="78889-151">Imutabilidade.</span><span class="sxs-lookup"><span data-stu-id="78889-151">Immutability.</span></span> <span data-ttu-id="78889-152">As tuplas de Visual Basic são mutáveis.</span><span class="sxs-lookup"><span data-stu-id="78889-152">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="78889-153">Em contraste, uma estrutura personalizada permite que você controle se uma instância é mutável ou imutável.</span><span class="sxs-lookup"><span data-stu-id="78889-153">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="78889-154">Se membros personalizados, a propriedade e a validação do campo ou a imutabilidade forem importante, você deve usar o Visual Basic [estrutura](../../../language-reference/statements/structure-statement.md) instrução para definir um tipo de valor personalizado.</span><span class="sxs-lookup"><span data-stu-id="78889-154">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="78889-155">Uma tupla de Visual Basic herdam os membros de sua **ValueTuple** tipo.</span><span class="sxs-lookup"><span data-stu-id="78889-155">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="78889-156">Além de seus campos, isso inclui os seguintes métodos:</span><span class="sxs-lookup"><span data-stu-id="78889-156">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="78889-157">Membro</span><span class="sxs-lookup"><span data-stu-id="78889-157">Member</span></span> | <span data-ttu-id="78889-158">Descrição</span><span class="sxs-lookup"><span data-stu-id="78889-158">Description</span></span> |
| ---|---|
| <span data-ttu-id="78889-159">CompareTo</span><span class="sxs-lookup"><span data-stu-id="78889-159">CompareTo</span></span> | <span data-ttu-id="78889-160">Compara a tupla atual para outra tupla com o mesmo número de elementos.</span><span class="sxs-lookup"><span data-stu-id="78889-160">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="78889-161">Igual a</span><span class="sxs-lookup"><span data-stu-id="78889-161">Equals</span></span> | <span data-ttu-id="78889-162">Determina se a tupla atual é igual a outro objeto ou tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-162">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="78889-163">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="78889-163">GetHashCode</span></span> | <span data-ttu-id="78889-164">Calcula o código hash para a instância atual.</span><span class="sxs-lookup"><span data-stu-id="78889-164">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="78889-165">ToString</span><span class="sxs-lookup"><span data-stu-id="78889-165">ToString</span></span> | <span data-ttu-id="78889-166">Retorna a representação de cadeia de caracteres dessa tupla, que utiliza o formato `(Item1, Item2...)`, onde `Item1` e `Item2` representam os valores dos campos da tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-166">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="78889-167">Além disso, o **ValueTuple** tipos implementam <xref:System.Collections.IStructuralComparable> e <xref:System.Collections.IStructuralEquatable> interfaces, que permitem que você defina os comparadores de cliente.</span><span class="sxs-lookup"><span data-stu-id="78889-167">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="78889-168">Atribuição e tuplas</span><span class="sxs-lookup"><span data-stu-id="78889-168">Assignment and tuples</span></span>

<span data-ttu-id="78889-169">Visual Basic dá suporte à atribuição entre tipos de tupla que têm o mesmo número de campos.</span><span class="sxs-lookup"><span data-stu-id="78889-169">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="78889-170">Os tipos de campo podem ser convertidos se uma das seguintes opções for verdadeira:</span><span class="sxs-lookup"><span data-stu-id="78889-170">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="78889-171">O campo de origem e destino são do mesmo tipo.</span><span class="sxs-lookup"><span data-stu-id="78889-171">The source and target field are of the same type.</span></span>

- <span data-ttu-id="78889-172">Uma conversão de ampliação (ou implícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="78889-172">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="78889-173">`Option Strict` é `On`, e uma conversão de estreitamento (ou explícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="78889-173">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="78889-174">Essa conversão pode lançar uma exceção se o valor de origem está fora do intervalo do tipo de destino.</span><span class="sxs-lookup"><span data-stu-id="78889-174">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="78889-175">Outras conversões não são consideradas para atribuições.</span><span class="sxs-lookup"><span data-stu-id="78889-175">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="78889-176">Vamos examinar os tipos de atribuições que são permitidos entre tipos de tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-176">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="78889-177">Considere estas variáveis usadas nos exemplos a seguir:</span><span class="sxs-lookup"><span data-stu-id="78889-177">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="78889-178">As primeiras duas variáveis, `unnamed` e `anonymous`, não têm nomes semânticos fornecidos para os campos.</span><span class="sxs-lookup"><span data-stu-id="78889-178">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="78889-179">Seus nomes de campo são o padrão `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="78889-179">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="78889-180">As duas últimas variáveis, `named` e `differentName` têm nomes de campo de semântica.</span><span class="sxs-lookup"><span data-stu-id="78889-180">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="78889-181">Observe que essas duas tuplas têm nomes diferentes para os campos.</span><span class="sxs-lookup"><span data-stu-id="78889-181">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="78889-182">Todas essas quatro tuplas têm o mesmo número de campos (chamados de 'aridades'), e os tipos desses campos são idênticos.</span><span class="sxs-lookup"><span data-stu-id="78889-182">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="78889-183">Portanto, todas essas atribuições funcionam:</span><span class="sxs-lookup"><span data-stu-id="78889-183">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="78889-184">Observe que os nomes das tuplas não são atribuídos.</span><span class="sxs-lookup"><span data-stu-id="78889-184">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="78889-185">Os valores dos campos são atribuídos na ordem dos campos na tupla.</span><span class="sxs-lookup"><span data-stu-id="78889-185">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="78889-186">Finalmente, observe que podemos atribuir a `named` tupla para o `conversion` tupla, mesmo que o primeiro campo da `named` é uma `Integer`e o primeiro campo da `conversion` é um `Long`.</span><span class="sxs-lookup"><span data-stu-id="78889-186">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="78889-187">Essa atribuição será bem-sucedida porque a conversão de um `Integer` para um `Long` é uma conversão de ampliação.</span><span class="sxs-lookup"><span data-stu-id="78889-187">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="78889-188">As tuplas com diferentes números de campos não são atribuíveis:</span><span class="sxs-lookup"><span data-stu-id="78889-188">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="78889-189">Tuplas como valores retornados do método</span><span class="sxs-lookup"><span data-stu-id="78889-189">Tuples as method return values</span></span>

<span data-ttu-id="78889-190">Um método pode retornar apenas um único valor.</span><span class="sxs-lookup"><span data-stu-id="78889-190">A method can return only a single value.</span></span> <span data-ttu-id="78889-191">Com frequência, no entanto, seria uma chamada de método para retornar vários valores.</span><span class="sxs-lookup"><span data-stu-id="78889-191">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="78889-192">Há várias maneiras de contornar essa limitação:</span><span class="sxs-lookup"><span data-stu-id="78889-192">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="78889-193">Você pode criar uma classe personalizada ou uma estrutura cujas propriedades ou campos representam valores retornados pelo método.</span><span class="sxs-lookup"><span data-stu-id="78889-193">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="78889-194">Portanto, é uma solução pesada; ele requer que você definir um tipo personalizado, cuja única finalidade é recuperar valores de uma chamada de método.</span><span class="sxs-lookup"><span data-stu-id="78889-194">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="78889-195">Você pode retornar um único valor do método e retornar os valores restantes, passando-os por referência para o método.</span><span class="sxs-lookup"><span data-stu-id="78889-195">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="78889-196">Isso envolve a sobrecarga de instanciar uma variável e riscos substitua inadvertidamente o valor da variável que é passada por referência.</span><span class="sxs-lookup"><span data-stu-id="78889-196">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="78889-197">Você pode usar uma tupla que fornece uma solução leve para recuperar vários valores de retorno.</span><span class="sxs-lookup"><span data-stu-id="78889-197">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="78889-198">Por exemplo, o **TryParse** métodos de retorno de .NET uma `Boolean` valor que indica se a operação de análise foi bem-sucedida.</span><span class="sxs-lookup"><span data-stu-id="78889-198">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="78889-199">O resultado da operação de análise é retornado em uma variável passada por referência para o método.</span><span class="sxs-lookup"><span data-stu-id="78889-199">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="78889-200">Normalmente, uma chamada para a um método de análise, como <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> é semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="78889-200">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="78889-201">Podemos pode retornar uma tupla da operação de análise se podemos encapsular a chamada para o <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> método no nosso próprio método.</span><span class="sxs-lookup"><span data-stu-id="78889-201">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="78889-202">No exemplo a seguir `NumericLibrary.ParseInteger` chama o <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> método e retorna uma tupla nomeada com dois elementos.</span><span class="sxs-lookup"><span data-stu-id="78889-202">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="78889-203">Em seguida, você pode chamar o método com um código semelhante ao seguinte:</span><span class="sxs-lookup"><span data-stu-id="78889-203">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="78889-204">Tuplas de Visual Basic e tuplas no .NET Framework</span><span class="sxs-lookup"><span data-stu-id="78889-204">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="78889-205">Uma tupla de Visual Basic é uma instância de um dos **valuetuple** tipos genéricos, que foram introduzidos no .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="78889-205">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="78889-206">O .NET Framework também inclui um conjunto de genérico **System. Tuple** classes.</span><span class="sxs-lookup"><span data-stu-id="78889-206">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="78889-207">Essas classes, no entanto, são diferentes das tuplas de Visual Basic e o **valuetuple** tipos genéricos de várias maneiras:</span><span class="sxs-lookup"><span data-stu-id="78889-207">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="78889-208">Os elementos do **tupla** classes são as propriedades nomeadas `Item1`, `Item2`e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="78889-208">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="78889-209">No Visual Basic tuplas e o **ValueTuple** tipos de elementos de tupla são os campos.</span><span class="sxs-lookup"><span data-stu-id="78889-209">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="78889-210">Você não pode atribuir nomes significativos para os elementos de uma **tupla** instância ou de uma **ValueTuple** instância.</span><span class="sxs-lookup"><span data-stu-id="78889-210">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="78889-211">Visual Basic permite que você atribua nomes de comunicam o significado dos campos.</span><span class="sxs-lookup"><span data-stu-id="78889-211">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="78889-212">As propriedades de um **tupla** instância são somente leitura; as tuplas são imutáveis.</span><span class="sxs-lookup"><span data-stu-id="78889-212">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="78889-213">No Visual Basic tuplas e o **ValueTuple** tipos, campos da tupla são leitura / gravação; as tuplas são mutáveis.</span><span class="sxs-lookup"><span data-stu-id="78889-213">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="78889-214">Genérica **tupla** tipos são tipos de referência.</span><span class="sxs-lookup"><span data-stu-id="78889-214">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="78889-215">Usando essas **tupla** significa alocar objetos de tipos.</span><span class="sxs-lookup"><span data-stu-id="78889-215">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="78889-216">Em caminhos de acesso, isso pode ter um impacto mensurável no desempenho do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="78889-216">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="78889-217">As tuplas do Visual Basic e o **ValueTuple** tipos são tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="78889-217">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="78889-218">Métodos de extensão na <xref:System.TupleExtensions> classe tornam mais fácil converter entre as tuplas do Visual Basic e .NET **tupla** objetos.</span><span class="sxs-lookup"><span data-stu-id="78889-218">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="78889-219">O **ToTuple** método converte uma tupla de Visual Basic para um .NET **tupla** objeto e o **ToValueTuple** método converte um .NET **tupla** objeto a ser uma tupla de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="78889-219">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="78889-220">O exemplo a seguir cria uma tupla, converte-o em um .NET **tupla** objeto e converte-o novamente para uma tupla de Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="78889-220">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="78889-221">O exemplo, em seguida, compara essa tupla pelo original para garantir que eles são iguais.</span><span class="sxs-lookup"><span data-stu-id="78889-221">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="78889-222">Consulte também</span><span class="sxs-lookup"><span data-stu-id="78889-222">See also</span></span>

[<span data-ttu-id="78889-223">Referência da linguagem Visual Basic</span><span class="sxs-lookup"><span data-stu-id="78889-223">Visual Basic Language Reference</span></span>](index.md)  
