---
title: Tuplas no Visual Basic
ms.custom: 
ms.date: 04/23/2017
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords: tuples [Visual Basic]
ms.assetid: 3e66cd1b-3432-4e1d-8c37-5ebacae8f53f
author: rpetrusha
ms.author: ronpet
ms.openlocfilehash: be50b22e9acca9ff8cfbde798d78869ee1c72634
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2017
---
# <a name="tuples-visual-basic"></a><span data-ttu-id="48298-102">Tuplas (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="48298-102">Tuples (Visual Basic)</span></span>

<span data-ttu-id="48298-103">A partir do Visual Basic de 2017, a linguagem Visual Basic oferece suporte interno para tuplas que faz com que criar tuplas e acessar os elementos de tuplas mais fácil.</span><span class="sxs-lookup"><span data-stu-id="48298-103">Starting with Visual Basic 2017, the Visual Basic language offers built-in support for tuples that makes creating tuples and accessing the elements of tuples easier.</span></span> <span data-ttu-id="48298-104">Uma tupla é uma estrutura de dados leve que tem um número específico e a sequência de valores.</span><span class="sxs-lookup"><span data-stu-id="48298-104">A tuple is a light-weight data structure that has a specific number and sequence of values.</span></span> <span data-ttu-id="48298-105">Quando você cria uma instância de coleção de itens, você deve definir o número e o tipo de dados de cada valor (ou elemento).</span><span class="sxs-lookup"><span data-stu-id="48298-105">When you instantiate the tuple, you define the number and the data type of each value (or element).</span></span> <span data-ttu-id="48298-106">Por exemplo, uma tupla de 2 (ou par) tem dois elementos.</span><span class="sxs-lookup"><span data-stu-id="48298-106">For example, a 2-tuple (or pair) has two elements.</span></span> <span data-ttu-id="48298-107">O primeiro pode ser um `Boolean` de valor, enquanto o segundo é um `String`.</span><span class="sxs-lookup"><span data-stu-id="48298-107">The first might be a `Boolean` value, while the second is a `String`.</span></span> <span data-ttu-id="48298-108">Porque tuplas facilitam armazenar vários valores em um único objeto, eles geralmente são usados como uma maneira simples de retornar vários valores de um método.</span><span class="sxs-lookup"><span data-stu-id="48298-108">Because tuples make it easy to store multiple values in a single object, they are often used as a lightweight way to return multiple values from a method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="48298-109">Suporte de tupla requer o <xref:System.ValueTuple> tipo.</span><span class="sxs-lookup"><span data-stu-id="48298-109">Tuple support requires the <xref:System.ValueTuple> type.</span></span> <span data-ttu-id="48298-110">Se o .NET Framework 4.7 não estiver instalado, você deve adicionar o pacote NuGet `System.ValueTuple`, que está disponível na Galeria do NuGet.</span><span class="sxs-lookup"><span data-stu-id="48298-110">If the .NET Framework 4.7 is not installed, you must add the NuGet package `System.ValueTuple`, which is available on the NuGet Gallery.</span></span> <span data-ttu-id="48298-111">Sem esse pacote, você pode receber um erro de compilação semelhante a "Tipo predefinido 'ValueTuple(Of,,,)' não está definido ou importado."</span><span class="sxs-lookup"><span data-stu-id="48298-111">Without this package, you may get a compilation error similar to, "Predefined type 'ValueTuple(Of,,,)' is not defined or imported."</span></span>

## <a name="instantiating-and-using-a-tuple"></a><span data-ttu-id="48298-112">Criando e usando uma tupla</span><span class="sxs-lookup"><span data-stu-id="48298-112">Instantiating and using a tuple</span></span>

<span data-ttu-id="48298-113">Você pode instanciar uma tupla colocando os parênteses de im valores delimitados por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="48298-113">You instantiate a tuple by enclosing its comma-delimited values im parentheses.</span></span> <span data-ttu-id="48298-114">Cada um desses valores, em seguida, torna-se um campo da tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-114">Each of those values then becomes a field of the tuple.</span></span> <span data-ttu-id="48298-115">Por exemplo, o código a seguir define um triplo (ou uma tupla de 3) com um `Date` como o primeiro valor, um `String` como seu segundo e um `Boolean` como seu terceiro.</span><span class="sxs-lookup"><span data-stu-id="48298-115">For example, the following code defines a triple (or 3-tuple) with a `Date` as its first value, a `String` as its second, and a `Boolean` as its third.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#1)]

<span data-ttu-id="48298-116">Por padrão, o nome de cada campo em uma tupla consiste da cadeia de caracteres `Item` juntamente com base em uma posição na tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-116">By default, the name of each field in a tuple consists of the string `Item` along with the field's one-based position in the tuple.</span></span> <span data-ttu-id="48298-117">Para essa tupla de 3, o `Date` campo é `Item1`, o `String` campo é `Item2`e o `Boolean` campo é `Item3`.</span><span class="sxs-lookup"><span data-stu-id="48298-117">For this 3-tuple, the `Date` field is `Item1`, the `String` field is `Item2`, and the `Boolean` field is `Item3`.</span></span> <span data-ttu-id="48298-118">O exemplo a seguir exibe os valores dos campos da tupla instanciado na linha de código anterior</span><span class="sxs-lookup"><span data-stu-id="48298-118">The following example displays the values of fields of the tuple instantiated in the previous line of code</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#2)]

<span data-ttu-id="48298-119">Os campos de uma tupla de Visual Basic são de leitura-gravação; Depois de instanciar uma tupla, você pode modificar seus valores.</span><span class="sxs-lookup"><span data-stu-id="48298-119">The fields of a Visual Basic tuple are read-write; after you've instantiated a tuple, you can modify its values.</span></span> <span data-ttu-id="48298-120">O exemplo a seguir modifica duas os três campos da tupla criado no exemplo anterior e exibe o resultado.</span><span class="sxs-lookup"><span data-stu-id="48298-120">The following example modifies two of the three fields of the tuple created in the previous example and displays the result.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#3)]

## <a name="instantiating-and-using-a-named-tuple"></a><span data-ttu-id="48298-121">Criando e usando uma tupla nomeada</span><span class="sxs-lookup"><span data-stu-id="48298-121">Instantiating and using a named tuple</span></span>

<span data-ttu-id="48298-122">Em vez de usar os nomes padrão para campos de uma tupla, você pode instanciar uma *chamado tupla* atribuindo seus próprios nomes de elementos da tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-122">Rather than using default names for a tuple's fields, you can instantiate a *named tuple* by assigning your own names to the tuple's elements.</span></span> <span data-ttu-id="48298-123">Os campos da tupla possam ser acessados pelos nomes atribuídos *ou* por seus nomes padrão.</span><span class="sxs-lookup"><span data-stu-id="48298-123">The tuple's fields can then be accessed by their assigned names *or* by their default names.</span></span> <span data-ttu-id="48298-124">O exemplo a seguir cria a mesmo tupla de 3 como anteriormente, exceto que ele explicitamente nomeia o primeiro campo `EventDate`, a segunda `Name`e o terceiro `IsHoliday`.</span><span class="sxs-lookup"><span data-stu-id="48298-124">The following example instantiates the same 3-tuple as previously, except that it explicitly names the first field `EventDate`, the second `Name`, and the third `IsHoliday`.</span></span> <span data-ttu-id="48298-125">Em seguida, exibe os valores de campo, modifica e exibe os valores de campo novamente.</span><span class="sxs-lookup"><span data-stu-id="48298-125">It then displays the field values, modifies them, and displays the field values again.</span></span>

[!code-vb[Instantiate](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple1.vb#4)]

## <a name="tuples-versus-structures"></a><span data-ttu-id="48298-126">Tuplas versus estruturas</span><span class="sxs-lookup"><span data-stu-id="48298-126">Tuples versus structures</span></span>

<span data-ttu-id="48298-127">Uma coleção de itens do Visual Basic é um tipo de valor que é uma instância de um a um **System.ValueTuple** tipos genéricos.</span><span class="sxs-lookup"><span data-stu-id="48298-127">A Visual Basic tuple is a value type that is an instance of one of the a **System.ValueTuple** generic types.</span></span> <span data-ttu-id="48298-128">Por exemplo, o `holiday` tupla definida no exemplo anterior é uma ocorrência da <xref:System.ValueTuple%603> estrutura.</span><span class="sxs-lookup"><span data-stu-id="48298-128">For example, the `holiday` tuple defined in the previous example is an instance of the <xref:System.ValueTuple%603> structure.</span></span> <span data-ttu-id="48298-129">Ele é projetado para ser um contêiner leve para dados.</span><span class="sxs-lookup"><span data-stu-id="48298-129">It is designed to be a lightweight container for data.</span></span> <span data-ttu-id="48298-130">Desde que a tupla vise para tornar mais fácil criar um objeto com vários itens de dados, ele não tem alguns dos recursos que pode ter uma estrutura personalizada.</span><span class="sxs-lookup"><span data-stu-id="48298-130">Since the tuple aims to make it easy to create an object with multiple data items, it lacks some of the features that a custom structure might have.</span></span> <span data-ttu-id="48298-131">Elas incluem:</span><span class="sxs-lookup"><span data-stu-id="48298-131">These include:</span></span>

- <span data-ttu-id="48298-132">Membros de cliente.</span><span class="sxs-lookup"><span data-stu-id="48298-132">Customer members.</span></span> <span data-ttu-id="48298-133">Você não pode definir suas próprias propriedades, métodos ou eventos de uma tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-133">You cannot define your own properties, methods, or events for a tuple.</span></span>

- <span data-ttu-id="48298-134">Validação.</span><span class="sxs-lookup"><span data-stu-id="48298-134">Validation.</span></span> <span data-ttu-id="48298-135">Não é possível validar os dados atribuídos aos campos.</span><span class="sxs-lookup"><span data-stu-id="48298-135">You cannot validate the data assigned to fields.</span></span>

- <span data-ttu-id="48298-136">Imutabilidade.</span><span class="sxs-lookup"><span data-stu-id="48298-136">Immutability.</span></span> <span data-ttu-id="48298-137">Visual Basic tuplas serão mutáveis.</span><span class="sxs-lookup"><span data-stu-id="48298-137">Visual Basic tuples are mutable.</span></span> <span data-ttu-id="48298-138">Em contraste, uma estrutura personalizada permite controlar se uma instância é mutável ou imutável.</span><span class="sxs-lookup"><span data-stu-id="48298-138">In contrast, a custom structure allows you to control whether an instance is mutable or immutable.</span></span>

<span data-ttu-id="48298-139">Se membros personalizados, a propriedade e a validação de campo ou a imutabilidade forem importante, você deve usar o Visual Basic [estrutura](../../../language-reference/statements/structure-statement.md) instrução para definir um tipo de valor personalizado.</span><span class="sxs-lookup"><span data-stu-id="48298-139">If custom members, property and field validation, or immutability are important, you should use the Visual Basic [Structure](../../../language-reference/statements/structure-statement.md) statement to define a custom value type.</span></span>

<span data-ttu-id="48298-140">Uma tupla de Visual Basic herdam os membros de sua **ValueTuple** tipo.</span><span class="sxs-lookup"><span data-stu-id="48298-140">A Visual Basic tuple does inherit the members of its **ValueTuple** type.</span></span> <span data-ttu-id="48298-141">Além de seus campos, eles incluem os seguintes métodos:</span><span class="sxs-lookup"><span data-stu-id="48298-141">In addition to its fields, these include the following methods:</span></span>

| <span data-ttu-id="48298-142">Membro</span><span class="sxs-lookup"><span data-stu-id="48298-142">Member</span></span> | <span data-ttu-id="48298-143">Descrição</span><span class="sxs-lookup"><span data-stu-id="48298-143">Description</span></span> |
| ---|---|
| <span data-ttu-id="48298-144">CompareTo</span><span class="sxs-lookup"><span data-stu-id="48298-144">CompareTo</span></span> | <span data-ttu-id="48298-145">Compara a tupla atual para outra tupla com o mesmo número de elementos.</span><span class="sxs-lookup"><span data-stu-id="48298-145">Compares the current tuple to another tuple with the same number of elements.</span></span> |
| <span data-ttu-id="48298-146">Igual a</span><span class="sxs-lookup"><span data-stu-id="48298-146">Equals</span></span> | <span data-ttu-id="48298-147">Determina se a tupla atual é igual a outro objeto ou tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-147">Determines whether the current tuple is equal to another tuple or object.</span></span> |
| <span data-ttu-id="48298-148">GetHashCode</span><span class="sxs-lookup"><span data-stu-id="48298-148">GetHashCode</span></span> | <span data-ttu-id="48298-149">Calcula o código hash para a instância atual.</span><span class="sxs-lookup"><span data-stu-id="48298-149">Calculates the hash code for the current instance.</span></span> |
| <span data-ttu-id="48298-150">ToString</span><span class="sxs-lookup"><span data-stu-id="48298-150">ToString</span></span> | <span data-ttu-id="48298-151">Retorna a representação de cadeia de caracteres dessa tupla, que utiliza o formato `(Item1, Item2...)`, onde `Item1` e `Item2` representam os valores dos campos da tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-151">Returns the string representation of this tuple, which takes the form `(Item1, Item2...)`, where `Item1` and `Item2` represent the values of the tuple's fields.</span></span> |

<span data-ttu-id="48298-152">Além disso, o **ValueTuple** tipos implementam <xref:System.Collections.IStructuralComparable> e <xref:System.Collections.IStructuralEquatable> interfaces, que permitem que você defina comparadores de cliente.</span><span class="sxs-lookup"><span data-stu-id="48298-152">In addition, the **ValueTuple** types implement <xref:System.Collections.IStructuralComparable> and <xref:System.Collections.IStructuralEquatable> interfaces, which allow you to define customer comparers.</span></span>

## <a name="assignment-and-tuples"></a><span data-ttu-id="48298-153">Atribuição e tuplas</span><span class="sxs-lookup"><span data-stu-id="48298-153">Assignment and tuples</span></span>

<span data-ttu-id="48298-154">Visual Basic oferece suporte à atribuição entre tipos de coleção de itens que têm o mesmo número de campos.</span><span class="sxs-lookup"><span data-stu-id="48298-154">Visual Basic supports assignment between tuple types that have the same number of fields.</span></span> <span data-ttu-id="48298-155">Os tipos de campo podem ser convertidos se uma das seguintes opções for verdadeira:</span><span class="sxs-lookup"><span data-stu-id="48298-155">The field types can be converted if one of the following is true:</span></span>

- <span data-ttu-id="48298-156">O campo de origem e destino são do mesmo tipo.</span><span class="sxs-lookup"><span data-stu-id="48298-156">The source and target field are of the same type.</span></span>

- <span data-ttu-id="48298-157">Uma conversão de ampliação (ou implícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="48298-157">A widening (or implicit) conversion of the source type to the target type is defined.</span></span> 

- <span data-ttu-id="48298-158">`Option Strict`é `On`, e uma conversão de estreitamento (ou explícita) do tipo de origem para o tipo de destino é definida.</span><span class="sxs-lookup"><span data-stu-id="48298-158">`Option Strict` is `On`, and a narrowing (or explicit) conversion of the source type to the target type is defined.</span></span> <span data-ttu-id="48298-159">Esta conversão pode lançar uma exceção se o valor de origem está fora do intervalo do tipo de destino.</span><span class="sxs-lookup"><span data-stu-id="48298-159">This conversion can throw an exception if the source value is outside the range of the target type.</span></span>

<span data-ttu-id="48298-160">Outras conversões não são consideradas para atribuições.</span><span class="sxs-lookup"><span data-stu-id="48298-160">Other conversions are not considered for assignments.</span></span> <span data-ttu-id="48298-161">Vamos examinar os tipos de atribuições que são permitidos entre tipos de tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-161">Let's look at the kinds of assignments that are allowed between tuple types.</span></span>

<span data-ttu-id="48298-162">Considere estas variáveis usadas nos exemplos a seguir:</span><span class="sxs-lookup"><span data-stu-id="48298-162">Consider these variables used in the following examples:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#1)]

<span data-ttu-id="48298-163">As primeiras duas variáveis, `unnamed` e `anonymous`, não tem semânticos nomes fornecidos para os campos.</span><span class="sxs-lookup"><span data-stu-id="48298-163">The first two variables, `unnamed` and `anonymous`, do not have semantic names provided for the fields.</span></span> <span data-ttu-id="48298-164">Seus nomes de campo são o padrão `Item1` e `Item2`.</span><span class="sxs-lookup"><span data-stu-id="48298-164">Their field names are the default `Item1` and `Item2`.</span></span> <span data-ttu-id="48298-165">As duas últimas variáveis, `named` e `differentName` têm nomes de campo semântica.</span><span class="sxs-lookup"><span data-stu-id="48298-165">The last two variables, `named` and `differentName` have semantic field names.</span></span> <span data-ttu-id="48298-166">Observe que essas duas tuplas têm nomes diferentes para os campos.</span><span class="sxs-lookup"><span data-stu-id="48298-166">Note that these two tuples have different names for the fields.</span></span>

<span data-ttu-id="48298-167">Todos os quatro essas tuplas têm o mesmo número de campos (conhecido como 'arity') e os tipos desses campos são idênticos.</span><span class="sxs-lookup"><span data-stu-id="48298-167">All four of these tuples have the same number of fields (referred to as 'arity'), and the types of those fields are identical.</span></span> <span data-ttu-id="48298-168">Portanto, todas essas atribuições funcionam:</span><span class="sxs-lookup"><span data-stu-id="48298-168">Therefore, all of these assignments work:</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#2)]

<span data-ttu-id="48298-169">Observe que os nomes das tuplas não são atribuídos.</span><span class="sxs-lookup"><span data-stu-id="48298-169">Notice that the names of the tuples are not assigned.</span></span> <span data-ttu-id="48298-170">Os valores dos campos são atribuídos na ordem dos campos na tupla.</span><span class="sxs-lookup"><span data-stu-id="48298-170">The values of the fields are assigned following the order of the fields in the tuple.</span></span>

<span data-ttu-id="48298-171">Finalmente, observe que podemos atribuir a `named` tupla para o `conversion` tupla, mesmo que o primeiro campo de `named` é um `Integer`e o primeiro campo de `conversion` é um `Long`.</span><span class="sxs-lookup"><span data-stu-id="48298-171">Finally, notice that we can assign the `named` tuple to the `conversion` tuple, even though the first field of `named` is an `Integer`, and the first field of `conversion` is a `Long`.</span></span> <span data-ttu-id="48298-172">Essa atribuição é bem-sucedido porque convertendo um `Integer` para um `Long` é uma conversão de ampliação.</span><span class="sxs-lookup"><span data-stu-id="48298-172">This assignment succeeds because converting an `Integer` to a `Long` is a widening conversion.</span></span>

[!code-vb[Assign](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple3.vb#3)]

<span data-ttu-id="48298-173">Tuplas com diferentes números de campos não são atribuíveis:</span><span class="sxs-lookup"><span data-stu-id="48298-173">Tuples with different numbers of fields are not assignable:</span></span>

```vb
' Does not compile.
' VB30311: Value of type '(Integer, Integer, Integer)' cannot be converted
'          to '(Answer As Integer, Message As String)'
var differentShape = (1, 2, 3)
named = differentShape
```

## <a name="tuples-as-method-return-values"></a><span data-ttu-id="48298-174">Tuplas como valores retornados do método</span><span class="sxs-lookup"><span data-stu-id="48298-174">Tuples as method return values</span></span>

<span data-ttu-id="48298-175">Um método pode retornar apenas um único valor.</span><span class="sxs-lookup"><span data-stu-id="48298-175">A method can return only a single value.</span></span> <span data-ttu-id="48298-176">Com frequência, no entanto, você gostaria que uma chamada de método para retornar vários valores.</span><span class="sxs-lookup"><span data-stu-id="48298-176">Frequently, though, you'd like a method call to return multiple values.</span></span> <span data-ttu-id="48298-177">Há várias maneiras de contornar essa limitação:</span><span class="sxs-lookup"><span data-stu-id="48298-177">There are several ways to work around this limitation:</span></span>

- <span data-ttu-id="48298-178">Você pode criar uma classe personalizada ou estrutura cujas propriedades ou campos representam os valores retornados pelo método.</span><span class="sxs-lookup"><span data-stu-id="48298-178">You can create a custom class or structure whose properties or fields represent values returned by the method.</span></span> <span data-ttu-id="48298-179">Portanto, é uma solução de peso; ele requer que você defina um tipo personalizado cujo único propósito é recuperar valores de uma chamada de método.</span><span class="sxs-lookup"><span data-stu-id="48298-179">Thus is a heavyweight solution; it requires that you define a custom type whose only purpose is to retrieve values from a method call.</span></span>

- <span data-ttu-id="48298-180">Você pode retornar um único valor do método e retornar os valores restantes, passando-os por referência para o método.</span><span class="sxs-lookup"><span data-stu-id="48298-180">You can return a single value from the method, and return the remaining values by passing them by reference to the method.</span></span> <span data-ttu-id="48298-181">Isso envolve a sobrecarga de criar uma instância de uma variável e riscos sobrescrever inadvertidamente o valor da variável é passada por referência.</span><span class="sxs-lookup"><span data-stu-id="48298-181">This involves the overhead of instantiating a variable and risks inadvertently overwriting the value of the variable that you pass by reference.</span></span>

- <span data-ttu-id="48298-182">Você pode usar uma tupla, que fornece uma solução simples para recuperar vários valores de retorno.</span><span class="sxs-lookup"><span data-stu-id="48298-182">You can use a tuple, which provides a lightweight solution to retrieving multiple return values.</span></span>

<span data-ttu-id="48298-183">Por exemplo, o **TryParse** métodos de retorno de .NET um `Boolean` valor que indica se a operação de análise foi bem-sucedida.</span><span class="sxs-lookup"><span data-stu-id="48298-183">For example, the **TryParse** methods in .NET return a `Boolean` value that indicates whether the parsing operation succeeded.</span></span> <span data-ttu-id="48298-184">O resultado da operação de análise é retornado em uma variável passada por referência para o método.</span><span class="sxs-lookup"><span data-stu-id="48298-184">The result of the parsing operation is returned in a variable passed by reference to the method.</span></span> <span data-ttu-id="48298-185">Normalmente, uma chamada para a um método de análise, como <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> parece com o seguinte:</span><span class="sxs-lookup"><span data-stu-id="48298-185">Normally, a call to the a parsing method such as <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> looks like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#1)]

<span data-ttu-id="48298-186">É possível retornar uma tupla da operação de análise se encerrar a chamada para o <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> método no nosso próprio método.</span><span class="sxs-lookup"><span data-stu-id="48298-186">We can return a tuple from the parsing operation if we wrap the call to the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method in our own method.</span></span> <span data-ttu-id="48298-187">No exemplo a seguir, `NumericLibrary.ParseInteger` chama o <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> método e retorna uma tupla nomeada com dois elementos.</span><span class="sxs-lookup"><span data-stu-id="48298-187">In the following example, `NumericLibrary.ParseInteger` calls the <xref:System.Int32.TryParse%2A?displayProperty=nameWithType> method and returns a named tuple with two elements.</span></span> 

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#2)]

<span data-ttu-id="48298-188">Em seguida, você pode chamar o método com o código como o seguinte:</span><span class="sxs-lookup"><span data-stu-id="48298-188">You can then call the method with code like the following:</span></span>

[!code-vb[Return](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple-returns.vb#3)]

## <a name="visual-basic-tuples-and-tuples-in-the-net-framework"></a><span data-ttu-id="48298-189">Tuplas de Visual Basic e tuplas no .NET Framework</span><span class="sxs-lookup"><span data-stu-id="48298-189">Visual Basic tuples and tuples in the .NET Framework</span></span>

<span data-ttu-id="48298-190">Uma coleção de itens do Visual Basic é uma instância de uma a **System.ValueTuple** tipos genéricos, que foram introduzidos no .NET Framework 4.7.</span><span class="sxs-lookup"><span data-stu-id="48298-190">A Visual Basic tuple is an instance of one of the **System.ValueTuple** generic types, which were introduced in the .NET Framework 4.7.</span></span> <span data-ttu-id="48298-191">O .NET Framework também inclui um conjunto de genérica **System.Tuple** classes.</span><span class="sxs-lookup"><span data-stu-id="48298-191">The .NET Framework also includes a set of generic **System.Tuple** classes.</span></span> <span data-ttu-id="48298-192">Essas classes, no entanto, são diferentes de tuplas do Visual Basic e o **System.ValueTuple** tipos genéricos de várias maneiras:</span><span class="sxs-lookup"><span data-stu-id="48298-192">These classes, however, differ from Visual Basic tuples and the **System.ValueTuple** generic types in a number of ways:</span></span>

- <span data-ttu-id="48298-193">Os elementos do **tupla** classes são propriedades chamadas `Item1`, `Item2`e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="48298-193">The elements of the **Tuple** classes are properties named `Item1`, `Item2`, and so on.</span></span> <span data-ttu-id="48298-194">No Visual Basic tuplas e **ValueTuple** tipos de elementos de tupla são campos.</span><span class="sxs-lookup"><span data-stu-id="48298-194">In Visual Basic tuples and the **ValueTuple** types, tuple elements are fields.</span></span>

- <span data-ttu-id="48298-195">Você não pode atribuir nomes significativos para os elementos de um **tupla** instância ou de um **ValueTuple** instância.</span><span class="sxs-lookup"><span data-stu-id="48298-195">You cannot assign meaningful names to the elements of a **Tuple** instance or of a **ValueTuple** instance.</span></span> <span data-ttu-id="48298-196">Visual Basic permite que você atribua nomes que se comunicam o significado dos campos.</span><span class="sxs-lookup"><span data-stu-id="48298-196">Visual Basic allows you to assign names that communicate the meaning of the fields.</span></span>

- <span data-ttu-id="48298-197">As propriedades de um **tupla** instância são somente leitura; as tuplas imutáveis.</span><span class="sxs-lookup"><span data-stu-id="48298-197">The properties of a **Tuple** instance are read-only; the tuples are immutable.</span></span> <span data-ttu-id="48298-198">No Visual Basic tuplas e **ValueTuple** tipos, campos de tupla são leitura / gravação; as tuplas são mutáveis.</span><span class="sxs-lookup"><span data-stu-id="48298-198">In Visual Basic tuples and the **ValueTuple** types, tuple fields are read-write; the tuples are mutable.</span></span>

- <span data-ttu-id="48298-199">Genérica **tupla** tipos são tipos de referência.</span><span class="sxs-lookup"><span data-stu-id="48298-199">The generic **Tuple** types are reference types.</span></span> <span data-ttu-id="48298-200">Usando esses **tupla** tipos significa que a alocação de objetos.</span><span class="sxs-lookup"><span data-stu-id="48298-200">Using these **Tuple** types means allocating objects.</span></span> <span data-ttu-id="48298-201">Em caminhos de acesso, isso pode ter um impacto mensurável no desempenho do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="48298-201">On hot paths, this can have a measurable impact on your application's performance.</span></span> <span data-ttu-id="48298-202">Tuplas de Visual Basic e o **ValueTuple** tipos são tipos de valor.</span><span class="sxs-lookup"><span data-stu-id="48298-202">Visual Basic tuples and the **ValueTuple** types are value types.</span></span>

<span data-ttu-id="48298-203">Métodos de extensão no <xref:System.TupleExtensions> classe facilitam a conversão entre tuplas do Visual Basic e o .NET **tupla** objetos.</span><span class="sxs-lookup"><span data-stu-id="48298-203">Extension methods in the <xref:System.TupleExtensions> class make it easy to convert between Visual Basic tuples and .NET **Tuple** objects.</span></span> <span data-ttu-id="48298-204">O **ToTuple** método converte uma tupla de Visual Basic .NET **tupla** objeto e o **ToValueTuple** método converte .NET **tupla** objeto para uma coleção de itens do Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="48298-204">The **ToTuple** method converts a Visual Basic tuple to a .NET **Tuple** object, and the **ToValueTuple** method converts a .NET **Tuple** object to a Visual Basic tuple.</span></span>

<span data-ttu-id="48298-205">O exemplo a seguir cria uma tupla, converte-o em .NET **tupla** objeto e converte-o novamente para uma coleção de itens do Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="48298-205">The following example creates a tuple, converts it to a .NET **Tuple** object, and converts it back to a Visual Basic tuple.</span></span> <span data-ttu-id="48298-206">O exemplo, em seguida, compara essa tupla com o original para garantir que eles são iguais.</span><span class="sxs-lookup"><span data-stu-id="48298-206">The example then compares this tuple with the original one to ensure that they are equal.</span></span>

[!code-vb[Convert](../../../../../samples/snippets/visualbasic/programming-guide/language-features/data-types/tuple2.vb#1)]

## <a name="see-also"></a><span data-ttu-id="48298-207">Consulte também</span><span class="sxs-lookup"><span data-stu-id="48298-207">See also</span></span>

[<span data-ttu-id="48298-208">Referência da linguagem Visual Basic</span><span class="sxs-lookup"><span data-stu-id="48298-208">Visual Basic Language Reference</span></span>](index.md)  
