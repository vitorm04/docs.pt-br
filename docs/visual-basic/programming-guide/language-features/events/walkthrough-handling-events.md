---
title: Manipulando eventos (Visual Basic)
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: 12267e0a70419298bc581421c4f3a220088d205f
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69956308"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="6ffcb-102">Passo a passo: Manipulando eventos (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="6ffcb-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="6ffcb-103">Este é o segundo de dois tópicos que demonstram como trabalhar com eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="6ffcb-104">O primeiro tópico, [Walkthrough: Declarando e](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)gerando eventos, mostra como declarar e gerar eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="6ffcb-105">Esta seção usa o formulário e a classe desse passo a passos para mostrar como tratar eventos quando eles ocorrem.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="6ffcb-106">O `Widget` exemplo de classe usa instruções tradicionais de manipulação de eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="6ffcb-107">Visual Basic fornece outras técnicas para trabalhar com eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="6ffcb-108">Como um exercício, você pode modificar este exemplo para usar as `AddHandler` instruções `Handles` e.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="6ffcb-109">Para manipular o evento PercentDone da classe Widget</span><span class="sxs-lookup"><span data-stu-id="6ffcb-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="6ffcb-110">Coloque o seguinte código em `Form1`:</span><span class="sxs-lookup"><span data-stu-id="6ffcb-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="6ffcb-111">A `WithEvents` palavra-chave especifica que `mWidget` a variável é usada para lidar com os eventos de um objeto.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="6ffcb-112">Você especifica o tipo de objeto fornecendo o nome da classe da qual o objeto será criado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="6ffcb-113">A variável `mWidget` é declarada `WithEvents` em `Form1` porque as variáveis devem ser de nível de classe.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="6ffcb-114">Isso é verdadeiro independentemente do tipo de classe em que você os coloca.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="6ffcb-115">A variável `mblnCancel` é usada para cancelar o `LongTask` método.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="6ffcb-116">Escrevendo código para manipular um evento</span><span class="sxs-lookup"><span data-stu-id="6ffcb-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="6ffcb-117">Assim que você declarar uma variável usando `WithEvents`, o nome da variável aparecerá na lista suspensa à esquerda do editor de **código**da classe.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="6ffcb-118">Quando você seleciona `mWidget`, os `Widget` eventos da classe são exibidos na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="6ffcb-119">A seleção de um evento exibe o procedimento de evento correspondente, `mWidget` com o prefixo e um sublinhado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="6ffcb-120">Todos os procedimentos de evento associados a `WithEvents` uma variável recebem o nome da variável como um prefixo.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="6ffcb-121">Para identificar um evento</span><span class="sxs-lookup"><span data-stu-id="6ffcb-121">To handle an event</span></span>  
  
1. <span data-ttu-id="6ffcb-122">Selecione `mWidget` na lista suspensa à esquerda no **Editor de códigos**.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="6ffcb-123">Selecione o `PercentDone` evento na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="6ffcb-124">O **Editor de código** abre `mWidget_PercentDone` o procedimento de evento.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="6ffcb-125">O **Editor de código** é útil, mas não obrigatório, para inserir novos manipuladores de eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="6ffcb-126">Neste tutorial, é mais direto copiar apenas os manipuladores de eventos diretamente em seu código.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="6ffcb-127">Adicione o seguinte código ao manipulador de eventos do `mWidget_PercentDone`:</span><span class="sxs-lookup"><span data-stu-id="6ffcb-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="6ffcb-128">Sempre que `PercentDone` o evento é gerado, o procedimento de evento exibe a porcentagem concluída `Label` em um controle.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="6ffcb-129">O `DoEvents` método permite que o rótulo seja redesenhado e também dá ao usuário a oportunidade de clicar no botão **Cancelar** .</span><span class="sxs-lookup"><span data-stu-id="6ffcb-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="6ffcb-130">Adicione o seguinte código para o `Button2_Click` manipulador de eventos:</span><span class="sxs-lookup"><span data-stu-id="6ffcb-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="6ffcb-131">Se o usuário clicar no botão **Cancelar** enquanto `LongTask` estiver em execução, `Button2_Click` o evento será executado assim que a `DoEvents` instrução permitir que o processamento de eventos ocorra.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="6ffcb-132">A variável `mblnCancel` no nível de classe é definida `True`como, e `mWidget_PercentDone` o evento o testa e define o `ByRef Cancel` argumento como `True`.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="6ffcb-133">Conectando uma variável WithEvents a um objeto</span><span class="sxs-lookup"><span data-stu-id="6ffcb-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="6ffcb-134">`Form1`Agora está configurado para manipular os eventos `Widget` de um objeto.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="6ffcb-135">Tudo o que resta é encontrar um `Widget` em algum lugar.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="6ffcb-136">Quando você declara uma variável `WithEvents` em tempo de design, nenhum objeto é associado a ela.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="6ffcb-137">Uma `WithEvents` variável é assim como qualquer outra variável de objeto.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="6ffcb-138">Você precisa criar um objeto e atribuir uma referência a ele com a `WithEvents` variável.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="6ffcb-139">Para criar um objeto e atribuir uma referência a ele</span><span class="sxs-lookup"><span data-stu-id="6ffcb-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="6ffcb-140">Selecione **(eventos Form1)** na lista suspensa à esquerda no **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="6ffcb-141">Selecione o `Load` evento na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="6ffcb-142">O **Editor de código** abre `Form1_Load` o procedimento de evento.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="6ffcb-143">Adicione o seguinte código para o `Form1_Load` procedimento de evento para `Widget`criar:</span><span class="sxs-lookup"><span data-stu-id="6ffcb-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="6ffcb-144">Quando esse código é executado, Visual Basic cria um `Widget` objeto e conecta seus eventos aos procedimentos de evento `mWidget`associados ao.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="6ffcb-145">Desse ponto em diante, sempre que `Widget` o gerar `PercentDone` seu evento, `mWidget_PercentDone` o procedimento de evento será executado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="6ffcb-146">Para chamar o método LongTask</span><span class="sxs-lookup"><span data-stu-id="6ffcb-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="6ffcb-147">Adicione o seguinte código ao manipulador de eventos do `Button1_Click`:</span><span class="sxs-lookup"><span data-stu-id="6ffcb-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="6ffcb-148">Antes que `LongTask` o método seja chamado, o rótulo que exibe a porcentagem concluída deve ser inicializado e o sinalizador de `Boolean` nível de classe para cancelar o método deve ser definido `False`como.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="6ffcb-149">`LongTask`é chamado com uma duração de tarefa de 12,2 segundos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="6ffcb-150">O `PercentDone` evento é gerado uma vez a cada um terço de um segundo.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="6ffcb-151">Cada vez que o evento é gerado, `mWidget_PercentDone` o procedimento de evento é executado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="6ffcb-152">Quando `LongTask` for concluído, `mblnCancel` o será testado para ver `LongTask` se terminou normalmente ou se foi interrompido porque `mblnCancel` foi definido como `True`.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="6ffcb-153">A porcentagem concluída é atualizada apenas no primeiro caso.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="6ffcb-154">Para executar o programa</span><span class="sxs-lookup"><span data-stu-id="6ffcb-154">To run the program</span></span>  
  
1. <span data-ttu-id="6ffcb-155">Pressione F5 para colocar o projeto no modo de execução.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="6ffcb-156">Clique no botão **Iniciar tarefa** .</span><span class="sxs-lookup"><span data-stu-id="6ffcb-156">Click the **Start Task** button.</span></span> <span data-ttu-id="6ffcb-157">Cada vez que `PercentDone` o evento é gerado, o rótulo é atualizado com a porcentagem da tarefa concluída.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="6ffcb-158">Clique no botão **Cancelar** para interromper a tarefa.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="6ffcb-159">Observe que a aparência do botão **Cancelar** não é alterada imediatamente quando você clica nele.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="6ffcb-160">O `Click` evento não pode acontecer até `My.Application.DoEvents` que a instrução permita o processamento de eventos.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="6ffcb-161">O `My.Application.DoEvents` método não processa eventos exatamente da mesma forma que o formulário.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="6ffcb-162">Por exemplo, neste tutorial, você deve clicar no botão **Cancelar** duas vezes.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="6ffcb-163">Para permitir que o formulário manipule os eventos diretamente, você pode usar multithreading.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="6ffcb-164">Para obter mais informações, consulte [Threading gerenciado](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="6ffcb-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="6ffcb-165">Talvez você ache instrutivo a executar o programa com F11 e percorrer o código uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="6ffcb-166">Você pode ver claramente como a execução `LongTask`entra e, em seguida, reinsere `Form1` brevemente cada `PercentDone` vez que o evento é gerado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="6ffcb-167">O que aconteceria se, enquanto a execução estava de volta no `Form1`código de `LongTask` , o método fosse chamado novamente?</span><span class="sxs-lookup"><span data-stu-id="6ffcb-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="6ffcb-168">No pior, pode ocorrer um estouro de pilha `LongTask` se fosse chamado toda vez que o evento foi gerado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="6ffcb-169">Você pode fazer com que `mWidget` a variável manipule eventos para um `Widget` objeto diferente atribuindo uma referência ao novo `Widget` para `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="6ffcb-170">Na verdade, você pode fazer com que o `Button1_Click` código faça isso sempre que clicar no botão.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="6ffcb-171">Para manipular eventos para um widget diferente</span><span class="sxs-lookup"><span data-stu-id="6ffcb-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="6ffcb-172">Adicione a seguinte linha de código ao `Button1_Click` procedimento, imediatamente antes da linha que lê: `mWidget.LongTask(12.2, 0.33)`</span><span class="sxs-lookup"><span data-stu-id="6ffcb-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="6ffcb-173">O código acima cria um novo `Widget` cada vez que o botão é clicado.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="6ffcb-174">Assim que o `LongTask` método é concluído, a referência `Widget` ao é liberada e o `Widget` é destruído.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="6ffcb-175">Uma `WithEvents` variável pode conter apenas uma referência de objeto por vez, portanto, se você atribuir um `Widget` objeto diferente `mWidget`ao, os `Widget` eventos do objeto anterior não serão mais manipulados.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="6ffcb-176">Se `mWidget` é a única variável de objeto que contém uma referência ao `Widget`antigo, o objeto é destruído.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="6ffcb-177">Se você quiser manipular eventos de vários `Widget` objetos, use a `AddHandler` instrução para processar eventos de cada objeto separadamente.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="6ffcb-178">Você pode declarar `WithEvents` quantas variáveis forem necessárias, mas não há suporte `WithEvents` para matrizes de variáveis.</span><span class="sxs-lookup"><span data-stu-id="6ffcb-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6ffcb-179">Consulte também</span><span class="sxs-lookup"><span data-stu-id="6ffcb-179">See also</span></span>

- [<span data-ttu-id="6ffcb-180">Passo a passo: Declarando e gerando eventos</span><span class="sxs-lookup"><span data-stu-id="6ffcb-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="6ffcb-181">Eventos</span><span class="sxs-lookup"><span data-stu-id="6ffcb-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
