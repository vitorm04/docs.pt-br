---
title: Tratando eventos
ms.date: 07/20/2015
helpviewer_keywords:
- event handling [Visual Basic], walkthroughs
- walkthroughs [Visual Basic], event handling
- variables [Visual Basic], WithEvents
- events [Visual Basic], walkthroughs
- WithEvents keyword [Visual Basic], walkthroughs
- event handlers [Visual Basic], walkthroughs
ms.assetid: f145b3fc-5ae0-4509-a2aa-1ff6934706bd
ms.openlocfilehash: cb42f2e41e366cf8765cb9395d1a5641434bab40
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74345077"
---
# <a name="walkthrough-handling-events-visual-basic"></a><span data-ttu-id="0e0dc-102">Instruções passo a passo: tratando eventos (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="0e0dc-102">Walkthrough: Handling Events (Visual Basic)</span></span>
<span data-ttu-id="0e0dc-103">Este é o segundo de dois tópicos que demonstram como trabalhar com eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-103">This is the second of two topics that demonstrate how to work with events.</span></span> <span data-ttu-id="0e0dc-104">O primeiro tópico, [Walkthrough: declarando e gerando eventos](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), mostra como declarar e gerar eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-104">The first topic, [Walkthrough: Declaring and Raising Events](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md), shows how to declare and raise events.</span></span> <span data-ttu-id="0e0dc-105">Esta seção usa o formulário e a classe desse passo a passos para mostrar como tratar eventos quando eles ocorrem.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-105">This section uses the form and class from that walkthrough to show how to handle events when they take place.</span></span>  
  
 <span data-ttu-id="0e0dc-106">O exemplo da classe `Widget` usa instruções tradicionais de manipulação de eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-106">The `Widget` class example uses traditional event-handling statements.</span></span> <span data-ttu-id="0e0dc-107">Visual Basic fornece outras técnicas para trabalhar com eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-107">Visual Basic provides other techniques for working with events.</span></span> <span data-ttu-id="0e0dc-108">Como um exercício, você pode modificar este exemplo para usar as instruções `AddHandler` e `Handles`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-108">As an exercise, you can modify this example to use the `AddHandler` and `Handles` statements.</span></span>  
  
### <a name="to-handle-the-percentdone-event-of-the-widget-class"></a><span data-ttu-id="0e0dc-109">Para manipular o evento PercentDone da classe Widget</span><span class="sxs-lookup"><span data-stu-id="0e0dc-109">To handle the PercentDone event of the Widget class</span></span>  
  
1. <span data-ttu-id="0e0dc-110">Coloque o seguinte código no `Form1`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-110">Place the following code in `Form1`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#4)]  
  
     <span data-ttu-id="0e0dc-111">A palavra-chave `WithEvents` especifica que a variável `mWidget` é usada para lidar com os eventos de um objeto.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-111">The `WithEvents` keyword specifies that the variable `mWidget` is used to handle an object's events.</span></span> <span data-ttu-id="0e0dc-112">Você especifica o tipo de objeto fornecendo o nome da classe da qual o objeto será criado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-112">You specify the kind of object by supplying the name of the class from which the object will be created.</span></span>  
  
     <span data-ttu-id="0e0dc-113">A variável `mWidget` é declarada em `Form1` porque `WithEvents` variáveis devem ser de nível de classe.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-113">The variable `mWidget` is declared in `Form1` because `WithEvents` variables must be class-level.</span></span> <span data-ttu-id="0e0dc-114">Isso é verdadeiro independentemente do tipo de classe em que você os coloca.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-114">This is true regardless of the type of class you place them in.</span></span>  
  
     <span data-ttu-id="0e0dc-115">A variável `mblnCancel` é usada para cancelar o método `LongTask`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-115">The variable `mblnCancel` is used to cancel the `LongTask` method.</span></span>  
  
## <a name="writing-code-to-handle-an-event"></a><span data-ttu-id="0e0dc-116">Escrevendo código para manipular um evento</span><span class="sxs-lookup"><span data-stu-id="0e0dc-116">Writing Code to Handle an Event</span></span>  
 <span data-ttu-id="0e0dc-117">Assim que você declarar uma variável usando `WithEvents`, o nome da variável aparecerá na lista suspensa à esquerda do **Editor de código**da classe.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-117">As soon as you declare a variable using `WithEvents`, the variable name appears in the left drop-down list of the class's **Code Editor**.</span></span> <span data-ttu-id="0e0dc-118">Quando você seleciona `mWidget`, os eventos da classe `Widget` aparecem na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-118">When you select `mWidget`, the `Widget` class's events appear in the right drop-down list.</span></span> <span data-ttu-id="0e0dc-119">A seleção de um evento exibe o procedimento de evento correspondente, com o prefixo `mWidget` e um sublinhado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-119">Selecting an event displays the corresponding event procedure, with the prefix `mWidget` and an underscore.</span></span> <span data-ttu-id="0e0dc-120">Todos os procedimentos de evento associados a uma variável `WithEvents` recebem o nome da variável como um prefixo.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-120">All the event procedures associated with a `WithEvents` variable are given the variable name as a prefix.</span></span>  
  
#### <a name="to-handle-an-event"></a><span data-ttu-id="0e0dc-121">Para identificar um evento</span><span class="sxs-lookup"><span data-stu-id="0e0dc-121">To handle an event</span></span>  
  
1. <span data-ttu-id="0e0dc-122">Selecione `mWidget` na lista suspensa à esquerda no **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-122">Select `mWidget` from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="0e0dc-123">Selecione o evento `PercentDone` na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-123">Select the `PercentDone` event from the right drop-down list.</span></span> <span data-ttu-id="0e0dc-124">O **Editor de código** abre o procedimento de evento `mWidget_PercentDone`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-124">The **Code Editor** opens the `mWidget_PercentDone` event procedure.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="0e0dc-125">O **Editor de código** é útil, mas não obrigatório, para inserir novos manipuladores de eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-125">The **Code Editor** is useful, but not required, for inserting new event handlers.</span></span> <span data-ttu-id="0e0dc-126">Neste tutorial, é mais direto copiar apenas os manipuladores de eventos diretamente em seu código.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-126">In this walkthrough, it is more direct to just copy the event handlers directly into your code.</span></span>  
  
3. <span data-ttu-id="0e0dc-127">Adicione o seguinte código ao manipulador de eventos do `mWidget_PercentDone`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-127">Add the following code to the `mWidget_PercentDone` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#5)]  
  
     <span data-ttu-id="0e0dc-128">Sempre que o evento de `PercentDone` é gerado, o procedimento de evento exibe a porcentagem concluída em um controle de `Label`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-128">Whenever the `PercentDone` event is raised, the event procedure displays the percent complete in a `Label` control.</span></span> <span data-ttu-id="0e0dc-129">O método `DoEvents` permite que o rótulo seja redesenhado e também dá ao usuário a oportunidade de clicar no botão **Cancelar** .</span><span class="sxs-lookup"><span data-stu-id="0e0dc-129">The `DoEvents` method allows the label to repaint, and also gives the user the opportunity to click the **Cancel** button.</span></span>  
  
4. <span data-ttu-id="0e0dc-130">Adicione o seguinte código para o manipulador de eventos `Button2_Click`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-130">Add the following code for the `Button2_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#6)]  
  
 <span data-ttu-id="0e0dc-131">Se o usuário clicar no botão **Cancelar** enquanto `LongTask` estiver em execução, o evento `Button2_Click` será executado assim que a instrução `DoEvents` permitir que o processamento de eventos ocorra.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-131">If the user clicks the **Cancel** button while `LongTask` is running, the `Button2_Click` event is executed as soon as the `DoEvents` statement allows event processing to occur.</span></span> <span data-ttu-id="0e0dc-132">A variável no nível de classe `mblnCancel` é definida como `True`, e o evento `mWidget_PercentDone` então o testa e define o argumento `ByRef Cancel` como `True`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-132">The class-level variable `mblnCancel` is set to `True`, and the `mWidget_PercentDone` event then tests it and sets the `ByRef Cancel` argument to `True`.</span></span>  
  
## <a name="connecting-a-withevents-variable-to-an-object"></a><span data-ttu-id="0e0dc-133">Conectando uma variável WithEvents a um objeto</span><span class="sxs-lookup"><span data-stu-id="0e0dc-133">Connecting a WithEvents Variable to an Object</span></span>  
 <span data-ttu-id="0e0dc-134">`Form1` agora está configurado para manipular os eventos de um objeto de `Widget`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-134">`Form1` is now set up to handle a `Widget` object's events.</span></span> <span data-ttu-id="0e0dc-135">Tudo o que resta é encontrar um `Widget` em algum lugar.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-135">All that remains is to find a `Widget` somewhere.</span></span>  
  
 <span data-ttu-id="0e0dc-136">Quando você declara uma variável `WithEvents` em tempo de design, nenhum objeto é associado a ela.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-136">When you declare a variable `WithEvents` at design time, no object is associated with it.</span></span> <span data-ttu-id="0e0dc-137">Uma variável `WithEvents` é assim como qualquer outra variável de objeto.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-137">A `WithEvents` variable is just like any other object variable.</span></span> <span data-ttu-id="0e0dc-138">Você precisa criar um objeto e atribuir uma referência a ele com a variável `WithEvents`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-138">You have to create an object and assign a reference to it with the `WithEvents` variable.</span></span>  
  
#### <a name="to-create-an-object-and-assign-a-reference-to-it"></a><span data-ttu-id="0e0dc-139">Para criar um objeto e atribuir uma referência a ele</span><span class="sxs-lookup"><span data-stu-id="0e0dc-139">To create an object and assign a reference to it</span></span>  
  
1. <span data-ttu-id="0e0dc-140">Selecione **(eventos Form1)** na lista suspensa à esquerda no **Editor de código**.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-140">Select **(Form1 Events)** from the left drop-down list in the **Code Editor**.</span></span>  
  
2. <span data-ttu-id="0e0dc-141">Selecione o evento `Load` na lista suspensa à direita.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-141">Select the `Load` event from the right drop-down list.</span></span> <span data-ttu-id="0e0dc-142">O **Editor de código** abre o procedimento de evento `Form1_Load`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-142">The **Code Editor** opens the `Form1_Load` event procedure.</span></span>  
  
3. <span data-ttu-id="0e0dc-143">Adicione o seguinte código para o procedimento de evento `Form1_Load` para criar o `Widget`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-143">Add the following code for the `Form1_Load` event procedure to create the `Widget`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#7)]  
  
 <span data-ttu-id="0e0dc-144">Quando esse código é executado, Visual Basic cria um objeto `Widget` e conecta seus eventos aos procedimentos de evento associados ao `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-144">When this code executes, Visual Basic creates a `Widget` object and connects its events to the event procedures associated with `mWidget`.</span></span> <span data-ttu-id="0e0dc-145">Desse ponto em diante, sempre que o `Widget` gera seu evento `PercentDone`, o procedimento de evento `mWidget_PercentDone` é executado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-145">From that point on, whenever the `Widget` raises its `PercentDone` event, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
#### <a name="to-call-the-longtask-method"></a><span data-ttu-id="0e0dc-146">Para chamar o método LongTask</span><span class="sxs-lookup"><span data-stu-id="0e0dc-146">To call the LongTask method</span></span>  
  
- <span data-ttu-id="0e0dc-147">Adicione o seguinte código ao manipulador de eventos do `Button1_Click`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-147">Add the following code to the `Button1_Click` event handler:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#8)]  
  
 <span data-ttu-id="0e0dc-148">Antes que o método `LongTask` seja chamado, o rótulo que exibe a porcentagem concluída deve ser inicializado e o sinalizador de `Boolean` de nível de classe para cancelar o método deve ser definido como `False`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-148">Before the `LongTask` method is called, the label that displays the percent complete must be initialized, and the class-level `Boolean` flag for canceling the method must be set to `False`.</span></span>  
  
 <span data-ttu-id="0e0dc-149">`LongTask` é chamado com uma duração de tarefa de 12,2 segundos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-149">`LongTask` is called with a task duration of 12.2 seconds.</span></span> <span data-ttu-id="0e0dc-150">O evento `PercentDone` é gerado uma vez a cada um terço de um segundo.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-150">The `PercentDone` event is raised once every one-third of a second.</span></span> <span data-ttu-id="0e0dc-151">Cada vez que o evento é gerado, o procedimento de evento `mWidget_PercentDone` é executado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-151">Each time the event is raised, the `mWidget_PercentDone` event procedure is executed.</span></span>  
  
 <span data-ttu-id="0e0dc-152">Quando `LongTask` é feito, `mblnCancel` é testado para ver se `LongTask` foi encerrado normalmente ou se foi interrompido porque `mblnCancel` foi definido como `True`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-152">When `LongTask` is done, `mblnCancel` is tested to see if `LongTask` ended normally, or if it stopped because `mblnCancel` was set to `True`.</span></span> <span data-ttu-id="0e0dc-153">A porcentagem concluída é atualizada apenas no primeiro caso.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-153">The percent complete is updated only in the former case.</span></span>  
  
#### <a name="to-run-the-program"></a><span data-ttu-id="0e0dc-154">Para executar o programa</span><span class="sxs-lookup"><span data-stu-id="0e0dc-154">To run the program</span></span>  
  
1. <span data-ttu-id="0e0dc-155">Pressione F5 para colocar o projeto no modo de execução.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-155">Press F5 to put the project in run mode.</span></span>  
  
2. <span data-ttu-id="0e0dc-156">Clique no botão **Iniciar tarefa** .</span><span class="sxs-lookup"><span data-stu-id="0e0dc-156">Click the **Start Task** button.</span></span> <span data-ttu-id="0e0dc-157">Cada vez que o evento de `PercentDone` é gerado, o rótulo é atualizado com a porcentagem da tarefa concluída.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-157">Each time the `PercentDone` event is raised, the label is updated with the percentage of the task that is complete.</span></span>  
  
3. <span data-ttu-id="0e0dc-158">Clique no botão **Cancelar** para interromper a tarefa.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-158">Click the **Cancel** button to stop the task.</span></span> <span data-ttu-id="0e0dc-159">Observe que a aparência do botão **Cancelar** não é alterada imediatamente quando você clica nele.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-159">Notice that the appearance of the **Cancel** button does not change immediately when you click it.</span></span> <span data-ttu-id="0e0dc-160">O evento `Click` não pode acontecer até que a instrução `My.Application.DoEvents` permita o processamento de eventos.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-160">The `Click` event cannot happen until the `My.Application.DoEvents` statement allows event processing.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="0e0dc-161">O método `My.Application.DoEvents` não processa eventos exatamente da mesma forma que o formulário.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-161">The `My.Application.DoEvents` method does not process events in exactly the same way as the form does.</span></span> <span data-ttu-id="0e0dc-162">Por exemplo, neste tutorial, você deve clicar no botão **Cancelar** duas vezes.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-162">For example, in this walkthrough, you must click the **Cancel** button twice.</span></span> <span data-ttu-id="0e0dc-163">Para permitir que o formulário manipule os eventos diretamente, você pode usar multithreading.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-163">To allow the form to handle the events directly, you can use multithreading.</span></span> <span data-ttu-id="0e0dc-164">Para obter mais informações, consulte [Threading gerenciado](../../../../standard/threading/index.md).</span><span class="sxs-lookup"><span data-stu-id="0e0dc-164">For more information, see [Managed Threading](../../../../standard/threading/index.md).</span></span>
  
 <span data-ttu-id="0e0dc-165">Talvez você ache instrutivo a executar o programa com F11 e percorrer o código uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-165">You may find it instructive to run the program with F11 and step through the code a line at a time.</span></span> <span data-ttu-id="0e0dc-166">Você pode ver claramente como a execução entra `LongTask`e, em seguida, reinserir brevemente `Form1` cada vez que o evento `PercentDone` for gerado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-166">You can clearly see how execution enters `LongTask`, and then briefly re-enters `Form1` each time the `PercentDone` event is raised.</span></span>  
  
 <span data-ttu-id="0e0dc-167">O que aconteceria se, enquanto a execução estava de volta no código de `Form1`, o método `LongTask` foi chamado novamente?</span><span class="sxs-lookup"><span data-stu-id="0e0dc-167">What would happen if, while execution was back in the code of `Form1`, the `LongTask` method were called again?</span></span> <span data-ttu-id="0e0dc-168">No pior, pode ocorrer um estouro de pilha se `LongTask` fosse chamado toda vez que o evento foi gerado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-168">At worst, a stack overflow might occur if `LongTask` were called every time the event was raised.</span></span>  
  
 <span data-ttu-id="0e0dc-169">Você pode fazer com que a variável `mWidget` manipule eventos para um objeto `Widget` diferente atribuindo uma referência à nova `Widget` para `mWidget`.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-169">You can cause the variable `mWidget` to handle events for a different `Widget` object by assigning a reference to the new `Widget` to `mWidget`.</span></span> <span data-ttu-id="0e0dc-170">Na verdade, você pode tornar o código em `Button1_Click` fazer isso sempre que clicar no botão.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-170">In fact, you can make the code in `Button1_Click` do this every time you click the button.</span></span>  
  
#### <a name="to-handle-events-for-a-different-widget"></a><span data-ttu-id="0e0dc-171">Para manipular eventos para um widget diferente</span><span class="sxs-lookup"><span data-stu-id="0e0dc-171">To handle events for a different widget</span></span>  
  
- <span data-ttu-id="0e0dc-172">Adicione a seguinte linha de código ao procedimento `Button1_Click`, imediatamente antes da linha que lê `mWidget.LongTask(12.2, 0.33)`:</span><span class="sxs-lookup"><span data-stu-id="0e0dc-172">Add the following line of code to the `Button1_Click` procedure, immediately preceding the line that reads `mWidget.LongTask(12.2, 0.33)`:</span></span>  
  
     [!code-vb[VbVbcnWalkthroughDeclaringAndRaisingEvents#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbcnWalkthroughDeclaringAndRaisingEvents/VB/Form1.vb#9)]  
  
 <span data-ttu-id="0e0dc-173">O código acima cria um novo `Widget` cada vez que o botão é clicado.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-173">The code above creates a new `Widget` each time the button is clicked.</span></span> <span data-ttu-id="0e0dc-174">Assim que o método de `LongTask` é concluído, a referência ao `Widget` é liberada e o `Widget` é destruído.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-174">As soon as the `LongTask` method completes, the reference to the `Widget` is released, and the `Widget` is destroyed.</span></span>  
  
 <span data-ttu-id="0e0dc-175">Uma variável `WithEvents` pode conter apenas uma referência de objeto por vez, portanto, se você atribuir um objeto de `Widget` diferente para `mWidget`, os eventos do objeto `Widget` anterior não serão mais manipulados.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-175">A `WithEvents` variable can contain only one object reference at a time, so if you assign a different `Widget` object to `mWidget`, the previous `Widget` object's events will no longer be handled.</span></span> <span data-ttu-id="0e0dc-176">Se `mWidget` for a única variável de objeto que contém uma referência ao `Widget`antigo, o objeto será destruído.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-176">If `mWidget` is the only object variable containing a reference to the old `Widget`, the object is destroyed.</span></span> <span data-ttu-id="0e0dc-177">Se você quiser manipular eventos de vários objetos `Widget`, use a instrução `AddHandler` para processar eventos de cada objeto separadamente.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-177">If you want to handle events from several `Widget` objects, use the `AddHandler` statement to process events from each object separately.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="0e0dc-178">Você pode declarar quantas variáveis `WithEvents` forem necessárias, mas não há suporte para matrizes de `WithEvents` variáveis.</span><span class="sxs-lookup"><span data-stu-id="0e0dc-178">You can declare as many `WithEvents` variables as you need, but arrays of `WithEvents` variables are not supported.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="0e0dc-179">Consulte também</span><span class="sxs-lookup"><span data-stu-id="0e0dc-179">See also</span></span>

- [<span data-ttu-id="0e0dc-180">Instruções passo a passo: declarando e acionando eventos</span><span class="sxs-lookup"><span data-stu-id="0e0dc-180">Walkthrough: Declaring and Raising Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/walkthrough-declaring-and-raising-events.md)
- [<span data-ttu-id="0e0dc-181">Eventos</span><span class="sxs-lookup"><span data-stu-id="0e0dc-181">Events</span></span>](../../../../visual-basic/programming-guide/language-features/events/index.md)
