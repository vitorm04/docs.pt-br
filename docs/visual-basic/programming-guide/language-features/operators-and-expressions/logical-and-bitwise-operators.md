---
title: "Operadores lógicos e bit a bit no Visual Basic"
ms.custom: 
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: 
ms.suite: 
ms.technology: devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
caps.latest.revision: "22"
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: ba48f722a11e93f82ae99aa407c3096a964e5ddd
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/21/2017
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="0bed3-102">Operadores lógicos e bit a bit no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="0bed3-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="0bed3-103">Operadores lógicos comparam `Boolean` expressões e retorna um `Boolean` resultados.</span><span class="sxs-lookup"><span data-stu-id="0bed3-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="0bed3-104">O `And`, `Or`, `AndAlso`, `OrElse`, e `Xor` operadores são *binário* porque eles usam dois operandos, enquanto o `Not` operador é *unário* porque usa um operando único.</span><span class="sxs-lookup"><span data-stu-id="0bed3-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="0bed3-105">Alguns desses operadores também podem executar operações de lógicas bit a bit em valores integral.</span><span class="sxs-lookup"><span data-stu-id="0bed3-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="0bed3-106">Operador lógico unário</span><span class="sxs-lookup"><span data-stu-id="0bed3-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="0bed3-107">O [operador Not](../../../../visual-basic/language-reference/operators/not-operator.md) executa lógica *negação* em um `Boolean` expressão.</span><span class="sxs-lookup"><span data-stu-id="0bed3-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="0bed3-108">Retorna a lógica oposta seu operando.</span><span class="sxs-lookup"><span data-stu-id="0bed3-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="0bed3-109">Se a expressão for avaliada como `True`, em seguida, `Not` retorna `False`; se a expressão for avaliada como `False`, em seguida, `Not` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="0bed3-110">O exemplo a seguir ilustra essa situação.</span><span class="sxs-lookup"><span data-stu-id="0bed3-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_1.vb)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="0bed3-111">Operadores lógicos binários</span><span class="sxs-lookup"><span data-stu-id="0bed3-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="0bed3-112">O [operador e](../../../../visual-basic/language-reference/operators/and-operator.md) executa lógica *conjunto* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="0bed3-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="0bed3-113">Se as duas expressões são `True`, em seguida, `And` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="0bed3-114">Se pelo menos uma das expressões for avaliada como `False`, em seguida, `And` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="0bed3-115">O [operador ou](../../../../visual-basic/language-reference/operators/or-operator.md) executa lógica *disjunção* ou *inclusão* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="0bed3-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="0bed3-116">Se qualquer expressão for avaliada como `True`, ou ambos são avaliadas como `True`, em seguida, `Or` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="0bed3-117">Se nenhuma expressão for avaliada como `True`, `Or` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="0bed3-118">O [operador Xor](../../../../visual-basic/language-reference/operators/xor-operator.md) executa lógica *exclusão* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="0bed3-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="0bed3-119">Se exatamente uma expressão for avaliada como `True`, mas não ambos, `Xor` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="0bed3-120">Se as duas expressões são `True` ou ambos são avaliadas como `False`, `Xor` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="0bed3-121">O exemplo a seguir ilustra o `And`, `Or`, e `Xor` operadores.</span><span class="sxs-lookup"><span data-stu-id="0bed3-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_2.vb)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="0bed3-122">Operações de lógicas do curto-circuito</span><span class="sxs-lookup"><span data-stu-id="0bed3-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="0bed3-123">O [operador AndAlso](../../../../visual-basic/language-reference/operators/andalso-operator.md) é muito semelhante do `And` operador, que também executa conjunção lógica em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="0bed3-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="0bed3-124">A principal diferença entre as duas é que `AndAlso` exibe *curto-circuito* comportamento.</span><span class="sxs-lookup"><span data-stu-id="0bed3-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="0bed3-125">Se a primeira expressão em uma `AndAlso` expressão é avaliada como `False`, em seguida, a segunda expressão é avaliada não porque ele não é possível alterar o resultado final, e `AndAlso` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="0bed3-126">Da mesma forma, o [operador OrElse](../../../../visual-basic/language-reference/operators/orelse-operator.md) executa uma disjunção lógica em duas do curto-circuito `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="0bed3-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="0bed3-127">Se a primeira expressão em uma `OrElse` expressão é avaliada como `True`, em seguida, a segunda expressão é avaliada não porque ele não é possível alterar o resultado final, e `OrElse` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="0bed3-128">Vantagens e desvantagens do curto-circuito</span><span class="sxs-lookup"><span data-stu-id="0bed3-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="0bed3-129">Curto-circuito pode melhorar o desempenho, não avaliando uma expressão que não é possível alterar o resultado da operação lógica.</span><span class="sxs-lookup"><span data-stu-id="0bed3-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="0bed3-130">No entanto, se essa expressão executar ações adicionais, curto-circuito ignora essas ações.</span><span class="sxs-lookup"><span data-stu-id="0bed3-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="0bed3-131">Por exemplo, se a expressão inclui uma chamada para um `Function` procedimento, o procedimento não é chamado se a expressão é curto-circuitada e qualquer código adicional contidas a `Function` não é executado.</span><span class="sxs-lookup"><span data-stu-id="0bed3-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="0bed3-132">Portanto, a função possam ser executadas apenas ocasionalmente e não pode ser testada corretamente.</span><span class="sxs-lookup"><span data-stu-id="0bed3-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="0bed3-133">Ou a lógica do programa pode depender do código de `Function`.</span><span class="sxs-lookup"><span data-stu-id="0bed3-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="0bed3-134">O exemplo a seguir ilustra a diferença entre `And`, `Or`e suas contrapartes de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="0bed3-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_3.vb)]  
  
 [!code-vb[VbVbalrOperators#80](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_4.vb)]  
  
 [!code-vb[VbVbalrOperators#79](../../../../visual-basic/language-reference/operators/codesnippet/VisualBasic/logical-and-bitwise-operators_5.vb)]  
  
 <span data-ttu-id="0bed3-135">No exemplo anterior, observe que alguns códigos importante dentro `checkIfValid()` não é executado quando a chamada é curto-circuitada.</span><span class="sxs-lookup"><span data-stu-id="0bed3-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="0bed3-136">A primeira `If` chamadas de instrução `checkIfValid()` , embora `12 > 45` retorna `False`, pois `And` não curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="0bed3-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="0bed3-137">A segunda `If` não chama a instrução `checkIfValid()`, porque quando `12 > 45` retorna `False`, `AndAlso` reduz a segunda expressão.</span><span class="sxs-lookup"><span data-stu-id="0bed3-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="0bed3-138">O terceiro `If` chamadas de instrução `checkIfValid()` , embora `12 < 45` retorna `True`, pois `Or` não curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="0bed3-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="0bed3-139">O quarto `If` não chama a instrução `checkIfValid()`, porque quando `12 < 45` retorna `True`, `OrElse` reduz a segunda expressão.</span><span class="sxs-lookup"><span data-stu-id="0bed3-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="0bed3-140">Operações bit a bit</span><span class="sxs-lookup"><span data-stu-id="0bed3-140">Bitwise Operations</span></span>  
 <span data-ttu-id="0bed3-141">Operações bit a bit avaliam dois valores integrais em formato binário (base 2).</span><span class="sxs-lookup"><span data-stu-id="0bed3-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="0bed3-142">Eles comparam os bits em posições correspondentes e, em seguida, atribuem valores com base na comparação.</span><span class="sxs-lookup"><span data-stu-id="0bed3-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="0bed3-143">O exemplo a seguir ilustra o `And` operador.</span><span class="sxs-lookup"><span data-stu-id="0bed3-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/codesnippet/VisualBasic/logical-and-bitwise-operators_6.vb)]  
  
 <span data-ttu-id="0bed3-144">O exemplo anterior define o valor de `x` como 1.</span><span class="sxs-lookup"><span data-stu-id="0bed3-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="0bed3-145">Isso acontece pelos seguintes motivos:</span><span class="sxs-lookup"><span data-stu-id="0bed3-145">This happens for the following reasons:</span></span>  
  
-   <span data-ttu-id="0bed3-146">Os valores são tratados como binários:</span><span class="sxs-lookup"><span data-stu-id="0bed3-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="0bed3-147">3 em formato binário = 011</span><span class="sxs-lookup"><span data-stu-id="0bed3-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="0bed3-148">5 em formato binário = 101</span><span class="sxs-lookup"><span data-stu-id="0bed3-148">5 in binary form = 101</span></span>  
  
-   <span data-ttu-id="0bed3-149">O `And` operador compara as representações binárias, uma posição binária (bits) por vez.</span><span class="sxs-lookup"><span data-stu-id="0bed3-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="0bed3-150">Se ambos os bits na posição especificada forem 1, 1 é colocado na posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="0bed3-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="0bed3-151">Se qualquer bit for 0, 0 é colocado na posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="0bed3-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="0bed3-152">No exemplo anterior isso funciona da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="0bed3-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="0bed3-153">011 (3 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="0bed3-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="0bed3-154">101 (5 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="0bed3-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="0bed3-155">001 (o resultado, no formato binário)</span><span class="sxs-lookup"><span data-stu-id="0bed3-155">001 (The result, in binary form)</span></span>  
  
-   <span data-ttu-id="0bed3-156">O resultado é tratado como decimal.</span><span class="sxs-lookup"><span data-stu-id="0bed3-156">The result is treated as decimal.</span></span> <span data-ttu-id="0bed3-157">O valor 001 é a representação binária de 1, portanto `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="0bed3-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="0bed3-158">Bit a bit `Or` operação é semelhante, exceto que um 1 será atribuído para o bit de resultado se um ou ambos os bits comparados é 1.</span><span class="sxs-lookup"><span data-stu-id="0bed3-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="0bed3-159">`Xor`atribui um 1 para o bit de resultado se exatamente um dos bits comparados (não ambos) é 1.</span><span class="sxs-lookup"><span data-stu-id="0bed3-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="0bed3-160">`Not`receba um operando único e inverte a todos os bits, incluindo o bit de sinal e atribui o valor para o resultado.</span><span class="sxs-lookup"><span data-stu-id="0bed3-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="0bed3-161">Isso significa que, para assinado números positivos, `Not` sempre retorna um valor negativo e para números negativos, `Not` sempre retorna um positivo ou o valor zero.</span><span class="sxs-lookup"><span data-stu-id="0bed3-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="0bed3-162">O `AndAlso` e `OrElse` operadores não oferecem suporte a operações bit a bit.</span><span class="sxs-lookup"><span data-stu-id="0bed3-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="0bed3-163">Operações bit a bit podem ser executadas em tipos integrais somente.</span><span class="sxs-lookup"><span data-stu-id="0bed3-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="0bed3-164">Valores de ponto flutuante devem ser convertidos para tipos integrais antes de prosseguir com a operação bit a bit.</span><span class="sxs-lookup"><span data-stu-id="0bed3-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="0bed3-165">Consulte também</span><span class="sxs-lookup"><span data-stu-id="0bed3-165">See Also</span></span>  
 [<span data-ttu-id="0bed3-166">Operadores lógicos/bit a bit (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="0bed3-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)  
 [<span data-ttu-id="0bed3-167">Expressões Boolianas</span><span class="sxs-lookup"><span data-stu-id="0bed3-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)  
 [<span data-ttu-id="0bed3-168">Operadores aritméticos no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="0bed3-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)  
 [<span data-ttu-id="0bed3-169">Operadores de comparação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="0bed3-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)  
 [<span data-ttu-id="0bed3-170">Operadores de concatenação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="0bed3-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)  
 [<span data-ttu-id="0bed3-171">Combinação Eficiente de Operadores</span><span class="sxs-lookup"><span data-stu-id="0bed3-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
