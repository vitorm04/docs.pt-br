---
title: Operadores lógicos e bit a bit no Visual Basic
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 23f3758527b787551ad83cbd4e19076b788c9dd8
ms.sourcegitcommit: 2701302a99cafbe0d86d53d540eb0fa7e9b46b36
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/28/2019
ms.locfileid: "64649687"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="2c1a7-102">Operadores lógicos e bit a bit no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2c1a7-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="2c1a7-103">Operadores lógicos comparam `Boolean` expressões e retornam um `Boolean` resultado.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="2c1a7-104">O `And`, `Or`, `AndAlso`, `OrElse`, e `Xor` operadores são *binário* porque elas usam dois operandos, enquanto o `Not` operador é *unário* porque ele usa um operando único.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="2c1a7-105">Alguns desses operadores também podem executar operações de lógicas bit a bit em valores integrais.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="2c1a7-106">Operador lógico unário</span><span class="sxs-lookup"><span data-stu-id="2c1a7-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="2c1a7-107">O [não o operador](../../../../visual-basic/language-reference/operators/not-operator.md) executa a lógica *negação* em um `Boolean` expressão.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-107">The [Not Operator](../../../../visual-basic/language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="2c1a7-108">Ele gera a lógica oposta de seu operando.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="2c1a7-109">Se a expressão for avaliada como `True`, em seguida, `Not` retorna `False`; se a expressão for avaliada como `False`, em seguida, `Not` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="2c1a7-110">O exemplo a seguir ilustra essa situação.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="2c1a7-111">Operadores lógicos binários</span><span class="sxs-lookup"><span data-stu-id="2c1a7-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="2c1a7-112">O [operador And](../../../../visual-basic/language-reference/operators/and-operator.md) executa a lógica *junto* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-112">The [And Operator](../../../../visual-basic/language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="2c1a7-113">Se ambas as expressões são avaliadas como `True`, em seguida, `And` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="2c1a7-114">Se pelo menos uma das expressões for avaliada como `False`, em seguida, `And` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="2c1a7-115">O [ou operador](../../../../visual-basic/language-reference/operators/or-operator.md) executa a lógica *disjunção* ou *inclusão* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-115">The [Or Operator](../../../../visual-basic/language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="2c1a7-116">Se qualquer expressão for avaliada como `True`, ou ambos são avaliadas como `True`, em seguida, `Or` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="2c1a7-117">Se nenhuma expressão for avaliada como `True`, `Or` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="2c1a7-118">O [operador Xor](../../../../visual-basic/language-reference/operators/xor-operator.md) executa a lógica *exclusão* em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-118">The [Xor Operator](../../../../visual-basic/language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="2c1a7-119">Se exatamente uma expressão for avaliada como `True`, mas não ambos `Xor` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="2c1a7-120">Se ambas as expressões são avaliadas como `True` ou ambos são avaliadas como `False`, `Xor` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="2c1a7-121">O exemplo a seguir ilustra a `And`, `Or`, e `Xor` operadores.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="2c1a7-122">Operações lógicas de curto-circuito</span><span class="sxs-lookup"><span data-stu-id="2c1a7-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="2c1a7-123">O [operador AndAlso](../../../../visual-basic/language-reference/operators/andalso-operator.md) é muito semelhante ao `And` operador, em que ele também executa a conjunção lógica em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-123">The [AndAlso Operator](../../../../visual-basic/language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="2c1a7-124">A principal diferença entre os dois é que `AndAlso` apresenta *Short-circuiting* comportamento.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="2c1a7-125">Se a primeira expressão em uma `AndAlso` expressão é avaliada como `False`, em seguida, a segunda expressão é avaliada não porque ele não é possível alterar o resultado final, e `AndAlso` retorna `False`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="2c1a7-126">Da mesma forma, o [operador OrElse](../../../../visual-basic/language-reference/operators/orelse-operator.md) executa a disjunção lógica de circuito em dois `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-126">Similarly, the [OrElse Operator](../../../../visual-basic/language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="2c1a7-127">Se a primeira expressão em uma `OrElse` expressão é avaliada como `True`, em seguida, a segunda expressão é avaliada não porque ele não é possível alterar o resultado final, e `OrElse` retorna `True`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="2c1a7-128">Vantagens e desvantagens de curto-circuito</span><span class="sxs-lookup"><span data-stu-id="2c1a7-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="2c1a7-129">Short-circuiting pode melhorar o desempenho, não avaliar uma expressão que não é possível alterar o resultado da operação lógica.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="2c1a7-130">No entanto, se essa expressão executa ações adicionais, Short-circuiting ignora essas ações.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="2c1a7-131">Por exemplo, se a expressão inclui uma chamada para um `Function` procedimento, o procedimento não é chamado se a expressão é curto-circuitada, e qualquer código adicional contido no `Function` não é executado.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="2c1a7-132">Portanto, a função pode ser executado apenas ocasionalmente e não pode ser testada corretamente.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="2c1a7-133">Ou a lógica do programa pode depender do código a `Function`.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="2c1a7-134">O exemplo a seguir ilustra a diferença entre `And`, `Or`e suas contrapartes de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="2c1a7-135">No exemplo anterior, observe que alguns códigos importantes dentro `checkIfValid()` não é executado quando a chamada é curto-circuitada.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="2c1a7-136">A primeira `If` declaração chama `checkIfValid()` Embora `12 > 45` retorna `False`, pois `And` não curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="2c1a7-137">A segunda `If` instrução não chamará `checkIfValid()`, porque quando `12 > 45` retorna `False`, `AndAlso` causam curto-circuito a segunda expressão.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="2c1a7-138">O terceiro `If` declaração chama `checkIfValid()` Embora `12 < 45` retorna `True`, pois `Or` não curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="2c1a7-139">O quarto `If` instrução não chamará `checkIfValid()`, porque quando `12 < 45` retorna `True`, `OrElse` causam curto-circuito a segunda expressão.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="2c1a7-140">Operações bit a bit</span><span class="sxs-lookup"><span data-stu-id="2c1a7-140">Bitwise Operations</span></span>  
 <span data-ttu-id="2c1a7-141">Operações bit a bit avaliar dois valores inteiros em formato binário (base 2).</span><span class="sxs-lookup"><span data-stu-id="2c1a7-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="2c1a7-142">Eles comparam os bits nas posições correspondentes e, em seguida, atribuir valores com base na comparação.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="2c1a7-143">O exemplo a seguir ilustra o `And` operador.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="2c1a7-144">O exemplo anterior define o valor de `x` como 1.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="2c1a7-145">Isso acontece pelos seguintes motivos:</span><span class="sxs-lookup"><span data-stu-id="2c1a7-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="2c1a7-146">Os valores são tratados como binários:</span><span class="sxs-lookup"><span data-stu-id="2c1a7-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="2c1a7-147">3 em formato binário = 011</span><span class="sxs-lookup"><span data-stu-id="2c1a7-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="2c1a7-148">5 em formato binário = 101</span><span class="sxs-lookup"><span data-stu-id="2c1a7-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="2c1a7-149">O `And` operador compara as representações binárias, uma posição binária (bits) por vez.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="2c1a7-150">Se ambos os bits em uma posição especificada forem 1, 1 é colocado naquela posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="2c1a7-151">Se qualquer bit for 0, 0 é colocado na posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="2c1a7-152">No exemplo anterior isso funciona da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="2c1a7-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="2c1a7-153">011 (3 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="2c1a7-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="2c1a7-154">101 (5 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="2c1a7-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="2c1a7-155">001 (o resultado, no formato binário)</span><span class="sxs-lookup"><span data-stu-id="2c1a7-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="2c1a7-156">O resultado será tratado como decimal.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-156">The result is treated as decimal.</span></span> <span data-ttu-id="2c1a7-157">O valor 001 é a representação binária de 1, portanto, `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="2c1a7-158">O bit a bit `Or` operação é semelhante, exceto que um 1 é atribuído para o bit de resultado, se um ou ambos os bits comparados é 1.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="2c1a7-159">`Xor` atribui um 1 para o bit de resultado se exatamente um dos bits comparados (não ambos) for 1.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="2c1a7-160">`Not` receba um operando único e inverte todos os bits, incluindo o bit de sinal e atribui o valor para o resultado.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="2c1a7-161">Isso significa que, para assinado números positivos e negativos `Not` sempre retorna um valor negativo e para números negativos, `Not` sempre retorna um alarme ou valor zero.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="2c1a7-162">O `AndAlso` e `OrElse` operadores não dão suporte a operações bit a bit.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="2c1a7-163">Operações bit a bit podem ser executadas em tipos integrais somente.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="2c1a7-164">Valores de ponto flutuante devem ser convertidos em tipos integrais antes de prosseguir com a operação bit a bit.</span><span class="sxs-lookup"><span data-stu-id="2c1a7-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="2c1a7-165">Consulte também</span><span class="sxs-lookup"><span data-stu-id="2c1a7-165">See also</span></span>

- [<span data-ttu-id="2c1a7-166">Operadores lógicos/bit a bit (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="2c1a7-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../../visual-basic/language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="2c1a7-167">Expressões Boolianas</span><span class="sxs-lookup"><span data-stu-id="2c1a7-167">Boolean Expressions</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/boolean-expressions.md)
- [<span data-ttu-id="2c1a7-168">Operadores aritméticos no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2c1a7-168">Arithmetic Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/arithmetic-operators.md)
- [<span data-ttu-id="2c1a7-169">Operadores de comparação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2c1a7-169">Comparison Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/comparison-operators.md)
- [<span data-ttu-id="2c1a7-170">Operadores de concatenação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2c1a7-170">Concatenation Operators in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/concatenation-operators.md)
- [<span data-ttu-id="2c1a7-171">Combinação Eficiente de Operadores</span><span class="sxs-lookup"><span data-stu-id="2c1a7-171">Efficient Combination of Operators</span></span>](../../../../visual-basic/programming-guide/language-features/operators-and-expressions/efficient-combination-of-operators.md)
