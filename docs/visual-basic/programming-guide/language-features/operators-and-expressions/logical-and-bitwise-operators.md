---
title: Operadores lógicos e bit a bit
ms.date: 07/20/2015
helpviewer_keywords:
- short-circuiting
- Boolean expressions
- logical operators [Visual Basic], Boolean expressions
- operators [Visual Basic], logical
- AndAlso operator [Visual Basic]
- Not operator [Visual Basic], Boolean expressions
- Xor operator [Visual Basic], Boolean expressions
- And operator [Visual Basic], logical operators
- logical operators [Visual Basic]
- expressions [Visual Basic], Boolean
- Or operator [Visual Basic], logical operators
- Visual Basic code, operators
- short-circuiting [Visual Basic], logical operators
- logical operators [Visual Basic], short-circuiting
- Visual Basic code, expressions
- logical operators [Visual Basic], binary
- OrElse operator [Visual Basic]
- logical operators [Visual Basic], unary
ms.assetid: ca474e13-567d-4b1d-a18b-301433705e57
ms.openlocfilehash: 19d3511363f19319c2bd8ce872b62c1462b1370f
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84403402"
---
# <a name="logical-and-bitwise-operators-in-visual-basic"></a><span data-ttu-id="c90ea-102">Operadores lógicos e bit a bit no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="c90ea-102">Logical and Bitwise Operators in Visual Basic</span></span>
<span data-ttu-id="c90ea-103">Os operadores lógicos comparam `Boolean` expressões e retornam um `Boolean` resultado.</span><span class="sxs-lookup"><span data-stu-id="c90ea-103">Logical operators compare `Boolean` expressions and return a `Boolean` result.</span></span> <span data-ttu-id="c90ea-104">Os `And` operadores,, `Or` `AndAlso` , `OrElse` e `Xor` são *binários* porque usam dois operandos, enquanto o `Not` operador é *unário* porque usa um único operando.</span><span class="sxs-lookup"><span data-stu-id="c90ea-104">The `And`, `Or`, `AndAlso`, `OrElse`, and `Xor` operators are *binary* because they take two operands, while the `Not` operator is *unary* because it takes a single operand.</span></span> <span data-ttu-id="c90ea-105">Alguns desses operadores também podem executar operações lógicas de bit a bit em valores integrais.</span><span class="sxs-lookup"><span data-stu-id="c90ea-105">Some of these operators can also perform bitwise logical operations on integral values.</span></span>  
  
## <a name="unary-logical-operator"></a><span data-ttu-id="c90ea-106">Operador lógico unário</span><span class="sxs-lookup"><span data-stu-id="c90ea-106">Unary Logical Operator</span></span>  
 <span data-ttu-id="c90ea-107">O [operador NOT](../../../language-reference/operators/not-operator.md) executa uma *negação* lógica em uma `Boolean` expressão.</span><span class="sxs-lookup"><span data-stu-id="c90ea-107">The [Not Operator](../../../language-reference/operators/not-operator.md) performs logical *negation* on a `Boolean` expression.</span></span> <span data-ttu-id="c90ea-108">Ele gera o oposto lógico de seu operando.</span><span class="sxs-lookup"><span data-stu-id="c90ea-108">It yields the logical opposite of its operand.</span></span> <span data-ttu-id="c90ea-109">Se a expressão for avaliada como `True` , `Not` retornará `False` ; se a expressão for avaliada como `False` , `Not` retorna `True` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-109">If the expression evaluates to `True`, then `Not` returns `False`; if the expression evaluates to `False`, then `Not` returns `True`.</span></span> <span data-ttu-id="c90ea-110">O exemplo a seguir ilustra isto.</span><span class="sxs-lookup"><span data-stu-id="c90ea-110">The following example illustrates this.</span></span>  
  
 [!code-vb[VbVbalrOperators#77](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#77)]  
  
## <a name="binary-logical-operators"></a><span data-ttu-id="c90ea-111">Operadores lógicos binários</span><span class="sxs-lookup"><span data-stu-id="c90ea-111">Binary Logical Operators</span></span>  
 <span data-ttu-id="c90ea-112">O [operador and](../../../language-reference/operators/and-operator.md) executa uma *conjunção* lógica em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="c90ea-112">The [And Operator](../../../language-reference/operators/and-operator.md) performs logical *conjunction* on two `Boolean` expressions.</span></span> <span data-ttu-id="c90ea-113">Se as duas expressões avaliarem como `True` , `And` retornarão `True` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-113">If both expressions evaluate to `True`, then `And` returns `True`.</span></span> <span data-ttu-id="c90ea-114">Se pelo menos uma das expressões for avaliada como `False` , `And` retorna `False` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-114">If at least one of the expressions evaluates to `False`, then `And` returns `False`.</span></span>  
  
 <span data-ttu-id="c90ea-115">O [operador OR](../../../language-reference/operators/or-operator.md) executa a *disjunção* lógica ou a *inclusão* em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="c90ea-115">The [Or Operator](../../../language-reference/operators/or-operator.md) performs logical *disjunction* or *inclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="c90ea-116">Se qualquer expressão for avaliada como `True` , ou se for avaliada como `True` , `Or` retorna `True` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-116">If either expression evaluates to `True`, or both evaluate to `True`, then `Or` returns `True`.</span></span> <span data-ttu-id="c90ea-117">Se nenhuma expressão for avaliada como `True` , `Or` retorna `False` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-117">If neither expression evaluates to `True`, `Or` returns `False`.</span></span>  
  
 <span data-ttu-id="c90ea-118">O [operador XOR](../../../language-reference/operators/xor-operator.md) executa a *exclusão* lógica em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="c90ea-118">The [Xor Operator](../../../language-reference/operators/xor-operator.md) performs logical *exclusion* on two `Boolean` expressions.</span></span> <span data-ttu-id="c90ea-119">Se exatamente uma expressão for avaliada como `True` , mas não ambos, `Xor` retornará `True` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-119">If exactly one expression evaluates to `True`, but not both, `Xor` returns `True`.</span></span> <span data-ttu-id="c90ea-120">Se ambas as expressões forem avaliadas como `True` ou ambas forem avaliadas como `False` , `Xor` retorna `False` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-120">If both expressions evaluate to `True` or both evaluate to `False`, `Xor` returns `False`.</span></span>  
  
 <span data-ttu-id="c90ea-121">O exemplo a seguir ilustra `And` os `Or` operadores, e `Xor` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-121">The following example illustrates the `And`, `Or`, and `Xor` operators.</span></span>  
  
 [!code-vb[VbVbalrOperators#78](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#78)]  
  
## <a name="short-circuiting-logical-operations"></a><span data-ttu-id="c90ea-122">Operações lógicas de curto-circuito</span><span class="sxs-lookup"><span data-stu-id="c90ea-122">Short-Circuiting Logical Operations</span></span>  
 <span data-ttu-id="c90ea-123">O [Operador AndAlso](../../../language-reference/operators/andalso-operator.md) é muito semelhante ao `And` operador, pois ele também executa uma conjunção lógica em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="c90ea-123">The [AndAlso Operator](../../../language-reference/operators/andalso-operator.md) is very similar to the `And` operator, in that it also performs logical conjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="c90ea-124">A principal diferença entre os dois é que o `AndAlso` exibe o comportamento *de curto-circuito* .</span><span class="sxs-lookup"><span data-stu-id="c90ea-124">The key difference between the two is that `AndAlso` exhibits *short-circuiting* behavior.</span></span> <span data-ttu-id="c90ea-125">Se a primeira expressão em uma `AndAlso` expressão for avaliada como `False` , a segunda expressão não será avaliada porque não pode alterar o resultado final e `AndAlso` retorna `False` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-125">If the first expression in an `AndAlso` expression evaluates to `False`, then the second expression is not evaluated because it cannot alter the final result, and `AndAlso` returns `False`.</span></span>  
  
 <span data-ttu-id="c90ea-126">Da mesma forma, o [Operador OrElse](../../../language-reference/operators/orelse-operator.md) executa uma disjunção lógica de curto-circuito em duas `Boolean` expressões.</span><span class="sxs-lookup"><span data-stu-id="c90ea-126">Similarly, the [OrElse Operator](../../../language-reference/operators/orelse-operator.md) performs short-circuiting logical disjunction on two `Boolean` expressions.</span></span> <span data-ttu-id="c90ea-127">Se a primeira expressão em uma `OrElse` expressão for avaliada como `True` , a segunda expressão não será avaliada porque não pode alterar o resultado final e `OrElse` retorna `True` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-127">If the first expression in an `OrElse` expression evaluates to `True`, then the second expression is not evaluated because it cannot alter the final result, and `OrElse` returns `True`.</span></span>  
  
### <a name="short-circuiting-trade-offs"></a><span data-ttu-id="c90ea-128">Compensações de curto-circuito</span><span class="sxs-lookup"><span data-stu-id="c90ea-128">Short-Circuiting Trade-Offs</span></span>  
 <span data-ttu-id="c90ea-129">O curto-circuito pode melhorar o desempenho não avaliando uma expressão que não possa alterar o resultado da operação lógica.</span><span class="sxs-lookup"><span data-stu-id="c90ea-129">Short-circuiting can improve performance by not evaluating an expression that cannot alter the result of the logical operation.</span></span> <span data-ttu-id="c90ea-130">No entanto, se essa expressão executar ações adicionais, o circuito curto ignorará essas ações.</span><span class="sxs-lookup"><span data-stu-id="c90ea-130">However, if that expression performs additional actions, short-circuiting skips those actions.</span></span> <span data-ttu-id="c90ea-131">Por exemplo, se a expressão incluir uma chamada para um `Function` procedimento, esse procedimento não será chamado se a expressão tiver um circuito curto e qualquer código adicional contido no `Function` não for executado.</span><span class="sxs-lookup"><span data-stu-id="c90ea-131">For example, if the expression includes a call to a `Function` procedure, that procedure is not called if the expression is short-circuited, and any additional code contained in the `Function` does not run.</span></span> <span data-ttu-id="c90ea-132">Portanto, a função pode ser executada apenas ocasionalmente e pode não ser testada corretamente.</span><span class="sxs-lookup"><span data-stu-id="c90ea-132">Therefore, the function might run only occasionally, and might not be tested correctly.</span></span> <span data-ttu-id="c90ea-133">Ou a lógica do programa pode depender do código no `Function` .</span><span class="sxs-lookup"><span data-stu-id="c90ea-133">Or the program logic might depend on the code in the `Function`.</span></span>  
  
 <span data-ttu-id="c90ea-134">O exemplo a seguir ilustra a diferença entre `And` , `Or` e suas contrapartes de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="c90ea-134">The following example illustrates the difference between `And`, `Or`, and their short-circuiting counterparts.</span></span>  
  
 [!code-vb[VbVbalrOperators#81](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#81)]  
  
 [!code-vb[VbVbalrOperators#80](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#80)]  
  
 [!code-vb[VbVbalrOperators#79](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrOperators/VB/Class1.vb#79)]  
  
 <span data-ttu-id="c90ea-135">No exemplo anterior, observe que algum código importante dentro não `checkIfValid()` é executado quando a chamada é de curto-circuito.</span><span class="sxs-lookup"><span data-stu-id="c90ea-135">In the preceding example, note that some important code inside `checkIfValid()` does not run when the call is short-circuited.</span></span> <span data-ttu-id="c90ea-136">A primeira `If` instrução chama `checkIfValid()` , apesar de `12 > 45` Returns `False` , porque não `And` é um circuito curto.</span><span class="sxs-lookup"><span data-stu-id="c90ea-136">The first `If` statement calls `checkIfValid()` even though `12 > 45` returns `False`, because `And` does not short-circuit.</span></span> <span data-ttu-id="c90ea-137">A segunda `If` instrução não chama `checkIfValid()` , porque quando `12 > 45` retorna `False` , a segunda expressão é retornada por `AndAlso` circuitos curtos.</span><span class="sxs-lookup"><span data-stu-id="c90ea-137">The second `If` statement does not call `checkIfValid()`, because when `12 > 45` returns `False`, `AndAlso` short-circuits the second expression.</span></span> <span data-ttu-id="c90ea-138">A terceira `If` instrução chama `checkIfValid()` , embora `12 < 45` retorne `True` , porque o `Or` não é um circuito curto.</span><span class="sxs-lookup"><span data-stu-id="c90ea-138">The third `If` statement calls `checkIfValid()` even though `12 < 45` returns `True`, because `Or` does not short-circuit.</span></span> <span data-ttu-id="c90ea-139">A quarta `If` instrução não chama `checkIfValid()` , porque, quando `12 < 45` retorna `True` , `OrElse` a segunda expressão é retornada por circuitos curtos.</span><span class="sxs-lookup"><span data-stu-id="c90ea-139">The fourth `If` statement does not call `checkIfValid()`, because when `12 < 45` returns `True`, `OrElse` short-circuits the second expression.</span></span>  
  
## <a name="bitwise-operations"></a><span data-ttu-id="c90ea-140">Operações bits</span><span class="sxs-lookup"><span data-stu-id="c90ea-140">Bitwise Operations</span></span>  
 <span data-ttu-id="c90ea-141">As operações bit a bit avaliam dois valores integrais no formulário binário (base 2).</span><span class="sxs-lookup"><span data-stu-id="c90ea-141">Bitwise operations evaluate two integral values in binary (base 2) form.</span></span> <span data-ttu-id="c90ea-142">Eles comparam os bits em posições correspondentes e, em seguida, atribuem valores com base na comparação.</span><span class="sxs-lookup"><span data-stu-id="c90ea-142">They compare the bits at corresponding positions and then assign values based on the comparison.</span></span> <span data-ttu-id="c90ea-143">O exemplo a seguir ilustra o `And` operador.</span><span class="sxs-lookup"><span data-stu-id="c90ea-143">The following example illustrates the `And` operator.</span></span>  
  
 [!code-vb[VbVbalrConcepts#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrConcepts/VB/Class1.vb#2)]  
  
 <span data-ttu-id="c90ea-144">O exemplo anterior define o valor de `x` como 1.</span><span class="sxs-lookup"><span data-stu-id="c90ea-144">The preceding example sets the value of `x` to 1.</span></span> <span data-ttu-id="c90ea-145">Isso ocorre pelos seguintes motivos:</span><span class="sxs-lookup"><span data-stu-id="c90ea-145">This happens for the following reasons:</span></span>  
  
- <span data-ttu-id="c90ea-146">Os valores são tratados como binários:</span><span class="sxs-lookup"><span data-stu-id="c90ea-146">The values are treated as binary:</span></span>  
  
     <span data-ttu-id="c90ea-147">3 em formato binário = 011</span><span class="sxs-lookup"><span data-stu-id="c90ea-147">3 in binary form = 011</span></span>  
  
     <span data-ttu-id="c90ea-148">5 em formato binário = 101</span><span class="sxs-lookup"><span data-stu-id="c90ea-148">5 in binary form = 101</span></span>  
  
- <span data-ttu-id="c90ea-149">O `And` operador compara as representações binárias, uma posição binária (bit) por vez.</span><span class="sxs-lookup"><span data-stu-id="c90ea-149">The `And` operator compares the binary representations, one binary position (bit) at a time.</span></span> <span data-ttu-id="c90ea-150">Se ambos os bits em uma determinada posição forem 1, um 1 será colocado nessa posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="c90ea-150">If both bits at a given position are 1, then a 1 is placed in that position in the result.</span></span> <span data-ttu-id="c90ea-151">Se um dos dois bits for 0, um 0 será colocado nessa posição no resultado.</span><span class="sxs-lookup"><span data-stu-id="c90ea-151">If either bit is 0, then a 0 is placed in that position in the result.</span></span> <span data-ttu-id="c90ea-152">No exemplo anterior, isso funciona da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="c90ea-152">In the preceding example this works out as follows:</span></span>  
  
     <span data-ttu-id="c90ea-153">011 (3 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="c90ea-153">011 (3 in binary form)</span></span>  
  
     <span data-ttu-id="c90ea-154">101 (5 em formato binário)</span><span class="sxs-lookup"><span data-stu-id="c90ea-154">101 (5 in binary form)</span></span>  
  
     <span data-ttu-id="c90ea-155">001 (o resultado, em formato binário)</span><span class="sxs-lookup"><span data-stu-id="c90ea-155">001 (The result, in binary form)</span></span>  
  
- <span data-ttu-id="c90ea-156">O resultado é tratado como Decimal.</span><span class="sxs-lookup"><span data-stu-id="c90ea-156">The result is treated as decimal.</span></span> <span data-ttu-id="c90ea-157">O valor 001 é a representação binária de 1, portanto `x` = 1.</span><span class="sxs-lookup"><span data-stu-id="c90ea-157">The value 001 is the binary representation of 1, so `x` = 1.</span></span>  
  
 <span data-ttu-id="c90ea-158">Essa `Or` operação é semelhante, exceto pelo fato de que um 1 é atribuído ao bit de resultado se um ou ambos os bits comparados forem 1.</span><span class="sxs-lookup"><span data-stu-id="c90ea-158">The bitwise `Or` operation is similar, except that a 1 is assigned to the result bit if either or both of the compared bits is 1.</span></span> <span data-ttu-id="c90ea-159">`Xor`atribui um 1 ao bit de resultado se exatamente um dos bits comparados (não ambos) for 1.</span><span class="sxs-lookup"><span data-stu-id="c90ea-159">`Xor` assigns a 1 to the result bit if exactly one of the compared bits (not both) is 1.</span></span> <span data-ttu-id="c90ea-160">`Not`usa um único operando e inverte todos os bits, incluindo o bit de sinal e atribui esse valor ao resultado.</span><span class="sxs-lookup"><span data-stu-id="c90ea-160">`Not` takes a single operand and inverts all the bits, including the sign bit, and assigns that value to the result.</span></span> <span data-ttu-id="c90ea-161">Isso significa que, para números positivos assinados, `Not` sempre retorna um valor negativo e para números negativos, `Not` sempre retorna um valor positivo ou zero.</span><span class="sxs-lookup"><span data-stu-id="c90ea-161">This means that for signed positive numbers, `Not` always returns a negative value, and for negative numbers, `Not` always returns a positive or zero value.</span></span>  
  
 <span data-ttu-id="c90ea-162">Os `AndAlso` operadores e não `OrElse` dão suporte a operações bit a bit.</span><span class="sxs-lookup"><span data-stu-id="c90ea-162">The `AndAlso` and `OrElse` operators do not support bitwise operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="c90ea-163">As operações de bit a bit podem ser executadas somente em tipos integrais.</span><span class="sxs-lookup"><span data-stu-id="c90ea-163">Bitwise operations can be performed on integral types only.</span></span> <span data-ttu-id="c90ea-164">Os valores de ponto flutuante devem ser convertidos em tipos integrais antes que a operação bit-up possa continuar.</span><span class="sxs-lookup"><span data-stu-id="c90ea-164">Floating-point values must be converted to integral types before bitwise operation can proceed.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c90ea-165">Confira também</span><span class="sxs-lookup"><span data-stu-id="c90ea-165">See also</span></span>

- [<span data-ttu-id="c90ea-166">Operadores lógicos/bit a bit (Visual Basic)</span><span class="sxs-lookup"><span data-stu-id="c90ea-166">Logical/Bitwise Operators (Visual Basic)</span></span>](../../../language-reference/operators/logical-bitwise-operators.md)
- [<span data-ttu-id="c90ea-167">Expressões booleanas</span><span class="sxs-lookup"><span data-stu-id="c90ea-167">Boolean Expressions</span></span>](boolean-expressions.md)
- [<span data-ttu-id="c90ea-168">Operadores aritméticos no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="c90ea-168">Arithmetic Operators in Visual Basic</span></span>](arithmetic-operators.md)
- [<span data-ttu-id="c90ea-169">Operadores de comparação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="c90ea-169">Comparison Operators in Visual Basic</span></span>](comparison-operators.md)
- [<span data-ttu-id="c90ea-170">Operadores de concatenação no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="c90ea-170">Concatenation Operators in Visual Basic</span></span>](concatenation-operators.md)
- [<span data-ttu-id="c90ea-171">Combinação eficiente de operadores</span><span class="sxs-lookup"><span data-stu-id="c90ea-171">Efficient Combination of Operators</span></span>](efficient-combination-of-operators.md)
