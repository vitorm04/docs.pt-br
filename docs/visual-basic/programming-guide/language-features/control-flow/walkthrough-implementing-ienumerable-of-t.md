---
title: Implementando IEnumerable no Visual Basic
ms.custom: ''
ms.date: 07/20/2015
ms.prod: .net
ms.reviewer: ''
ms.suite: ''
ms.technology:
- devlang-visual-basic
ms.topic: article
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
caps.latest.revision: 15
author: dotnet-bot
ms.author: dotnetcontent
ms.openlocfilehash: 4645153f9c830bc96b7ec55367f46f09098eb78d
ms.sourcegitcommit: 86adcc06e35390f13c1e372c36d2e044f1fc31ef
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/26/2018
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="54ee9-102">Instruções passo a passo: implementando IEnumerable(Of T) no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="54ee9-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="54ee9-103">O <xref:System.Collections.Generic.IEnumerable%601> interface é implementada por classes que podem retornar uma sequência de um item de valores de cada vez.</span><span class="sxs-lookup"><span data-stu-id="54ee9-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="54ee9-104">A vantagem de retornando dados de um item por vez é que você não precisa carregar o conjunto completo de dados na memória para trabalhar com ele.</span><span class="sxs-lookup"><span data-stu-id="54ee9-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="54ee9-105">Você só precisa usar memória suficiente para carregar um único item de dados.</span><span class="sxs-lookup"><span data-stu-id="54ee9-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="54ee9-106">As classes que implementam o `IEnumerable(T)` interface pode ser usada com `For Each` loops ou consultas LINQ.</span><span class="sxs-lookup"><span data-stu-id="54ee9-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="54ee9-107">Por exemplo, considere um aplicativo que deve ler um arquivo de texto grande e retorna cada linha do arquivo que corresponde aos critérios de pesquisa específica.</span><span class="sxs-lookup"><span data-stu-id="54ee9-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="54ee9-108">O aplicativo usa uma consulta LINQ para retornar linhas do arquivo que correspondem aos critérios especificados.</span><span class="sxs-lookup"><span data-stu-id="54ee9-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="54ee9-109">Para consultar o conteúdo do arquivo usando uma consulta LINQ, o aplicativo foi possível carregar o conteúdo do arquivo em uma matriz ou uma coleção.</span><span class="sxs-lookup"><span data-stu-id="54ee9-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="54ee9-110">No entanto, carregar o arquivo inteiro em uma matriz ou coleção consumiria muito mais memória do que é necessário.</span><span class="sxs-lookup"><span data-stu-id="54ee9-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="54ee9-111">A consulta LINQ em vez disso, pode consultar o conteúdo do arquivo usando uma classe enumerable, retornando somente os valores que correspondem aos critérios de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="54ee9-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="54ee9-112">Consultas que retornam apenas alguns valores correspondentes consumiria muito menos memória.</span><span class="sxs-lookup"><span data-stu-id="54ee9-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="54ee9-113">Você pode criar uma classe que implementa o <xref:System.Collections.Generic.IEnumerable%601> interface para expor a fonte de dados como dados enumeráveis.</span><span class="sxs-lookup"><span data-stu-id="54ee9-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="54ee9-114">A classe que implementa o `IEnumerable(T)` interface exigirá outra classe que implementa o <xref:System.Collections.Generic.IEnumerator%601> interface para iterar os dados de origem.</span><span class="sxs-lookup"><span data-stu-id="54ee9-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="54ee9-115">Essas duas classes permitem retornar itens de dados em sequência como um tipo específico.</span><span class="sxs-lookup"><span data-stu-id="54ee9-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="54ee9-116">Neste passo a passo, você criará uma classe que implementa o `IEnumerable(Of String)` interface e uma classe que implementa o `IEnumerator(Of String)` interface para ler uma linha de um arquivo texto por vez.</span><span class="sxs-lookup"><span data-stu-id="54ee9-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="54ee9-117">Criando a classe enumerável</span><span class="sxs-lookup"><span data-stu-id="54ee9-117">Creating the Enumerable Class</span></span>  
  
|<span data-ttu-id="54ee9-118">Para criar o projeto de classe enumerável</span><span class="sxs-lookup"><span data-stu-id="54ee9-118">To create the enumerable class project</span></span>|  
|---|  
|<span data-ttu-id="54ee9-119">1.  No Visual Basic, sobre o **arquivo** , aponte para **novo** e, em seguida, clique em **projeto**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-119">1.  In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span><br /><span data-ttu-id="54ee9-120">2.  Na caixa de diálogo **Novo Projeto**, no painel **Tipos de Projetos**, certifique-se de que **Windows** esteja selecionado.</span><span class="sxs-lookup"><span data-stu-id="54ee9-120">2.  In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="54ee9-121">Selecione **Biblioteca de Classes** no painel **Modelos**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="54ee9-122">Na caixa **Nome**, digite `StreamReaderEnumerable` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="54ee9-123">O novo projeto é exibido.</span><span class="sxs-lookup"><span data-stu-id="54ee9-123">The new project is displayed.</span></span><br /><span data-ttu-id="54ee9-124">3.  Em **Solution Explorer**, clique no arquivo Class1. vb e clique em **Renomear**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-124">3.  In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="54ee9-125">Renomeie o arquivo como `StreamReaderEnumerable.vb` e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="54ee9-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="54ee9-126">Renomear o arquivo também renomeará a classe para `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="54ee9-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="54ee9-127">Essa classe implementará a interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="54ee9-127">This class will implement the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="54ee9-128">4.  Clique com botão direito no projeto StreamReaderEnumerable, aponte para **adicionar**e, em seguida, clique em **Novo Item**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-128">4.  Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="54ee9-129">Selecione o **classe** modelo.</span><span class="sxs-lookup"><span data-stu-id="54ee9-129">Select the **Class** template.</span></span> <span data-ttu-id="54ee9-130">No **nome** , digite `StreamReaderEnumerator.vb` e clique em **Okey**.</span><span class="sxs-lookup"><span data-stu-id="54ee9-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>|  
  
 <span data-ttu-id="54ee9-131">A primeira classe neste projeto é a classe enumerável e implementar o `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="54ee9-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="54ee9-132">Essa interface genérica implementa o <xref:System.Collections.IEnumerable> interface e garante que os consumidores dessa classe podem acessar valores digitados como `String`.</span><span class="sxs-lookup"><span data-stu-id="54ee9-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
|<span data-ttu-id="54ee9-133">Para adicionar o código para implementar IEnumerable</span><span class="sxs-lookup"><span data-stu-id="54ee9-133">To add the code to implement IEnumerable</span></span>|  
|---|  
|<span data-ttu-id="54ee9-134">1.  Abra o arquivo StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="54ee9-134">1.  Open the StreamReaderEnumerable.vb file.</span></span><br /><span data-ttu-id="54ee9-135">2.  Na linha após `Public Class StreamReaderEnumerable`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="54ee9-135">2.  On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#1](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_1.vb)]<br />     <span data-ttu-id="54ee9-136">Visual Basic automaticamente preenche a classe com os membros necessários para o `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="54ee9-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span><br /><span data-ttu-id="54ee9-137">3.  Essa classe enumerável lê linhas em uma linha de um arquivo texto por vez.</span><span class="sxs-lookup"><span data-stu-id="54ee9-137">3.  This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="54ee9-138">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como um parâmetro de entrada.</span><span class="sxs-lookup"><span data-stu-id="54ee9-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#2](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_2.vb)]<br /><span data-ttu-id="54ee9-139">4.  Sua implementação do <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> método o `IEnumerable(Of String)` interface retornará uma nova instância do `StreamReaderEnumerator` classe.</span><span class="sxs-lookup"><span data-stu-id="54ee9-139">4.  Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="54ee9-140">A implementação do `GetEnumerator` método o `IEnumerable` interface pode ser feita `Private`, pois você precisa expor somente os membros do `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="54ee9-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="54ee9-141">Substitua o código Visual Basic gerado para o `GetEnumerator` métodos com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="54ee9-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#3](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_3.vb)]|  
  
|<span data-ttu-id="54ee9-142">Para adicionar o código para implementar IEnumerator</span><span class="sxs-lookup"><span data-stu-id="54ee9-142">To add the code to implement IEnumerator</span></span>|  
|---|  
|<span data-ttu-id="54ee9-143">1.  Abra o arquivo StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="54ee9-143">1.  Open the StreamReaderEnumerator.vb file.</span></span><br /><span data-ttu-id="54ee9-144">2.  Na linha após `Public Class StreamReaderEnumerator`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="54ee9-144">2.  On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#4](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_4.vb)]<br />     <span data-ttu-id="54ee9-145">Visual Basic automaticamente preenche a classe com os membros necessários para o `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="54ee9-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span><br /><span data-ttu-id="54ee9-146">3.  A classe de enumerador abre o arquivo de texto e executa o arquivo de e/s ao ler as linhas do arquivo.</span><span class="sxs-lookup"><span data-stu-id="54ee9-146">3.  The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="54ee9-147">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como um parâmetro de entrada e abra o arquivo de texto para leitura.</span><span class="sxs-lookup"><span data-stu-id="54ee9-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#5](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_5.vb)]<br /><span data-ttu-id="54ee9-148">4.  O `Current` propriedades para ambos os `IEnumerator(Of String)` e `IEnumerator` interfaces retornam o item atual do arquivo de texto como um `String`.</span><span class="sxs-lookup"><span data-stu-id="54ee9-148">4.  The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="54ee9-149">A implementação do `Current` propriedade do `IEnumerator` interface pode ser feita `Private`, pois você precisa expor somente os membros do `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="54ee9-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="54ee9-150">Substitua o código Visual Basic gerado para o `Current` propriedades com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="54ee9-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#6](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_6.vb)]<br /><span data-ttu-id="54ee9-151">5.  O `MoveNext` método o `IEnumerator` interface navega para o próximo item no arquivo de texto e atualiza o valor retornado pelo `Current` propriedade.</span><span class="sxs-lookup"><span data-stu-id="54ee9-151">5.  The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="54ee9-152">Se não houver nenhum outro item para ler, o `MoveNext` método retorna `False`; caso contrário, o `MoveNext` método retornará `True`.</span><span class="sxs-lookup"><span data-stu-id="54ee9-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="54ee9-153">Adicione o seguinte código ao método de `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="54ee9-153">Add the following code to the `MoveNext` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#7](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_7.vb)]<br /><span data-ttu-id="54ee9-154">6.  O `Reset` método o `IEnumerator` interface direciona o iterador para apontar para o início do arquivo de texto e limpa o valor do item atual.</span><span class="sxs-lookup"><span data-stu-id="54ee9-154">6.  The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="54ee9-155">Adicione o seguinte código ao método de `Reset` .</span><span class="sxs-lookup"><span data-stu-id="54ee9-155">Add the following code to the `Reset` method.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#8](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_8.vb)]<br /><span data-ttu-id="54ee9-156">7.  O `Dispose` método o `IEnumerator` interface garante que todos os recursos não gerenciados são liberados antes que o iterador é destruído.</span><span class="sxs-lookup"><span data-stu-id="54ee9-156">7.  The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="54ee9-157">O identificador de arquivo que é usado pelo `StreamReader` objeto é um recurso não gerenciado e devem ser fechado antes da instância de iterador é destruída.</span><span class="sxs-lookup"><span data-stu-id="54ee9-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="54ee9-158">Substitua o código Visual Basic gerado para o `Dispose` método com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="54ee9-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span><br />     [!code-vb[VbVbalrIteratorWalkthrough#9](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_9.vb)]|  
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="54ee9-159">Usando o iterador de exemplo</span><span class="sxs-lookup"><span data-stu-id="54ee9-159">Using the Sample Iterator</span></span>  
 <span data-ttu-id="54ee9-160">Você pode usar uma classe enumerável em seu código junto com estruturas de controle que exigem um objeto que implementa `IEnumerable`, como um `For Next` loop ou uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="54ee9-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="54ee9-161">A exemplo a seguir mostra o `StreamReaderEnumerable` em uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="54ee9-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](../../../../visual-basic/programming-guide/language-features/control-flow/codesnippet/VisualBasic/walkthrough-implementing-ienumerable-of-t_10.vb)]  
  
## <a name="see-also"></a><span data-ttu-id="54ee9-162">Consulte também</span><span class="sxs-lookup"><span data-stu-id="54ee9-162">See Also</span></span>  
 [<span data-ttu-id="54ee9-163">Introdução ao LINQ no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="54ee9-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)  
 [<span data-ttu-id="54ee9-164">Fluxo de Controle</span><span class="sxs-lookup"><span data-stu-id="54ee9-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)  
 [<span data-ttu-id="54ee9-165">Estruturas de Loop</span><span class="sxs-lookup"><span data-stu-id="54ee9-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)  
 [<span data-ttu-id="54ee9-166">Instrução For Each...Next</span><span class="sxs-lookup"><span data-stu-id="54ee9-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
