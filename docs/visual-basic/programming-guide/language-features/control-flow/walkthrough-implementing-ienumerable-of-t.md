---
title: Implementando IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: f40fcf7e0724addc478b261dcd36d09e1d8a751a
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/22/2019
ms.locfileid: "74333694"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="030a2-102">Instruções passo a passo: implementando IEnumerable(Of T) no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="030a2-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="030a2-103">A interface <xref:System.Collections.Generic.IEnumerable%601> é implementada por classes que podem retornar uma sequência de valores de um item por vez.</span><span class="sxs-lookup"><span data-stu-id="030a2-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="030a2-104">A vantagem de retornar dados de um item por vez é que você não precisa carregar o conjunto completo de dados na memória para trabalhar com ele.</span><span class="sxs-lookup"><span data-stu-id="030a2-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="030a2-105">Você só precisa usar memória suficiente para carregar um único item dos dados.</span><span class="sxs-lookup"><span data-stu-id="030a2-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="030a2-106">As classes que implementam a interface `IEnumerable(T)` podem ser usadas com loops `For Each` ou consultas LINQ.</span><span class="sxs-lookup"><span data-stu-id="030a2-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="030a2-107">Por exemplo, considere um aplicativo que deve ler um arquivo de texto grande e retornar cada linha do arquivo que corresponde a critérios de pesquisa específicos.</span><span class="sxs-lookup"><span data-stu-id="030a2-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="030a2-108">O aplicativo usa uma consulta LINQ para retornar linhas do arquivo que correspondem aos critérios especificados.</span><span class="sxs-lookup"><span data-stu-id="030a2-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="030a2-109">Para consultar o conteúdo do arquivo usando uma consulta LINQ, o aplicativo pode carregar o conteúdo do arquivo em uma matriz ou em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="030a2-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="030a2-110">No entanto, carregar o arquivo inteiro em uma matriz ou coleção consumiria muito mais memória do que o necessário.</span><span class="sxs-lookup"><span data-stu-id="030a2-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="030a2-111">Em vez disso, a consulta LINQ poderia consultar o conteúdo do arquivo usando uma classe enumerável, retornando apenas os valores que correspondem aos critérios de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="030a2-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="030a2-112">As consultas que retornam apenas alguns valores correspondentes consumirão muito menos memória.</span><span class="sxs-lookup"><span data-stu-id="030a2-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="030a2-113">Você pode criar uma classe que implementa a interface <xref:System.Collections.Generic.IEnumerable%601> para expor dados de origem como dados enumeráveis.</span><span class="sxs-lookup"><span data-stu-id="030a2-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="030a2-114">Sua classe que implementa a interface `IEnumerable(T)` exigirá outra classe que implementa a interface <xref:System.Collections.Generic.IEnumerator%601> para iterar pelos dados de origem.</span><span class="sxs-lookup"><span data-stu-id="030a2-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="030a2-115">Essas duas classes permitem que você retorne itens de dados sequencialmente como um tipo específico.</span><span class="sxs-lookup"><span data-stu-id="030a2-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="030a2-116">Neste tutorial, você criará uma classe que implementa a interface `IEnumerable(Of String)` e uma classe que implementa a interface `IEnumerator(Of String)` para ler um arquivo de texto, uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="030a2-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="030a2-117">Criando a classe Enumerable</span><span class="sxs-lookup"><span data-stu-id="030a2-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="030a2-118">**Criar o projeto de classe enumerável**</span><span class="sxs-lookup"><span data-stu-id="030a2-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="030a2-119">No Visual Basic, no menu **arquivo** , aponte para **novo** e clique em **projeto**.</span><span class="sxs-lookup"><span data-stu-id="030a2-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="030a2-120">Na caixa de diálogo **Novo Projeto**, no painel **Tipos de Projetos**, certifique-se de que **Windows** esteja selecionado.</span><span class="sxs-lookup"><span data-stu-id="030a2-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="030a2-121">Selecione **Biblioteca de Classes** no painel **Modelos**.</span><span class="sxs-lookup"><span data-stu-id="030a2-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="030a2-122">Na caixa **Nome**, digite `StreamReaderEnumerable` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="030a2-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="030a2-123">O novo projeto é exibido.</span><span class="sxs-lookup"><span data-stu-id="030a2-123">The new project is displayed.</span></span>

1. <span data-ttu-id="030a2-124">Em **Gerenciador de soluções**, clique com o botão direito do mouse no arquivo Class1. vb e clique em **renomear**.</span><span class="sxs-lookup"><span data-stu-id="030a2-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="030a2-125">Renomeie o arquivo como `StreamReaderEnumerable.vb` e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="030a2-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="030a2-126">Renomear o arquivo também renomeará a classe para `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="030a2-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="030a2-127">Essa classe implementará a interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="030a2-128">Clique com o botão direito do mouse no projeto StreamReaderEnumerable, aponte para **Adicionar**e clique em **novo item**.</span><span class="sxs-lookup"><span data-stu-id="030a2-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="030a2-129">Selecione o modelo de **classe** .</span><span class="sxs-lookup"><span data-stu-id="030a2-129">Select the **Class** template.</span></span> <span data-ttu-id="030a2-130">Na caixa **nome** , digite `StreamReaderEnumerator.vb` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="030a2-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="030a2-131">A primeira classe neste projeto é a classe enumerável e implementará a interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="030a2-132">Essa interface genérica implementa a interface <xref:System.Collections.IEnumerable> e garante que os consumidores dessa classe possam acessar valores digitados como `String`.</span><span class="sxs-lookup"><span data-stu-id="030a2-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="030a2-133">**Adicione o código para implementar IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="030a2-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="030a2-134">Abra o arquivo StreamReaderEnumerable. vb.</span><span class="sxs-lookup"><span data-stu-id="030a2-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="030a2-135">Na linha após `Public Class StreamReaderEnumerable`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="030a2-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="030a2-136">Visual Basic popula automaticamente a classe com os membros exigidos pela interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="030a2-137">Essa classe enumerável lerá linhas de um arquivo de texto, uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="030a2-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="030a2-138">Adicione o código a seguir à classe para expor um construtor público que usa um caminho de arquivo como um parâmetro de entrada.</span><span class="sxs-lookup"><span data-stu-id="030a2-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="030a2-139">Sua implementação do método <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> da interface de `IEnumerable(Of String)` retornará uma nova instância da classe `StreamReaderEnumerator`.</span><span class="sxs-lookup"><span data-stu-id="030a2-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="030a2-140">A implementação do método de `GetEnumerator` da interface `IEnumerable` pode ser feita `Private`, pois você precisa expor somente os membros da interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="030a2-141">Substitua o código que Visual Basic gerado para os métodos `GetEnumerator` com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="030a2-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="030a2-142">**Adicione o código para implementar IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="030a2-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="030a2-143">Abra o arquivo StreamReaderEnumerator. vb.</span><span class="sxs-lookup"><span data-stu-id="030a2-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="030a2-144">Na linha após `Public Class StreamReaderEnumerator`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="030a2-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="030a2-145">Visual Basic popula automaticamente a classe com os membros exigidos pela interface `IEnumerator(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="030a2-146">A classe Enumerator abre o arquivo de texto e executa a e/s de arquivo para ler as linhas do arquivo.</span><span class="sxs-lookup"><span data-stu-id="030a2-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="030a2-147">Adicione o código a seguir à classe para expor um construtor público que usa um caminho de arquivo como um parâmetro de entrada e abra o arquivo de texto para leitura.</span><span class="sxs-lookup"><span data-stu-id="030a2-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="030a2-148">As propriedades `Current` para as interfaces `IEnumerator(Of String)` e `IEnumerator` retornam o item atual do arquivo de texto como um `String`.</span><span class="sxs-lookup"><span data-stu-id="030a2-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="030a2-149">A implementação da propriedade `Current` da interface `IEnumerator` pode ser feita `Private`, pois você precisa expor somente os membros da interface `IEnumerator(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="030a2-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="030a2-150">Substitua o código que Visual Basic gerado para as propriedades de `Current` com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="030a2-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="030a2-151">O método `MoveNext` da interface `IEnumerator` navega para o próximo item no arquivo de texto e atualiza o valor retornado pela propriedade `Current`.</span><span class="sxs-lookup"><span data-stu-id="030a2-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="030a2-152">Se não houver mais itens a serem lidos, o método `MoveNext` retornará `False`; caso contrário, o método `MoveNext` retornará `True`.</span><span class="sxs-lookup"><span data-stu-id="030a2-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="030a2-153">Adicione o seguinte código ao método de `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="030a2-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="030a2-154">O método `Reset` da interface `IEnumerator` direciona o iterador para apontar para o início do arquivo de texto e limpa o valor do item atual.</span><span class="sxs-lookup"><span data-stu-id="030a2-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="030a2-155">Adicione o seguinte código ao método de `Reset` .</span><span class="sxs-lookup"><span data-stu-id="030a2-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="030a2-156">O método `Dispose` da interface `IEnumerator` garante que todos os recursos não gerenciados sejam liberados antes que o iterador seja destruído.</span><span class="sxs-lookup"><span data-stu-id="030a2-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="030a2-157">O identificador de arquivo usado pelo objeto `StreamReader` é um recurso não gerenciado e deve ser fechado antes que a instância do iterador seja destruída.</span><span class="sxs-lookup"><span data-stu-id="030a2-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="030a2-158">Substitua o código que Visual Basic gerado para o método `Dispose` com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="030a2-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="030a2-159">Usando o iterador de exemplo</span><span class="sxs-lookup"><span data-stu-id="030a2-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="030a2-160">Você pode usar uma classe enumerável em seu código junto com estruturas de controle que exigem um objeto que implementa `IEnumerable`, como um loop `For Next` ou uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="030a2-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="030a2-161">O exemplo a seguir mostra o `StreamReaderEnumerable` em uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="030a2-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="030a2-162">Consulte também</span><span class="sxs-lookup"><span data-stu-id="030a2-162">See also</span></span>

- [<span data-ttu-id="030a2-163">Introdução ao LINQ no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="030a2-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="030a2-164">Fluxo de Controle</span><span class="sxs-lookup"><span data-stu-id="030a2-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="030a2-165">Estruturas de Loop</span><span class="sxs-lookup"><span data-stu-id="030a2-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="030a2-166">Instrução For Each...Next</span><span class="sxs-lookup"><span data-stu-id="030a2-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
