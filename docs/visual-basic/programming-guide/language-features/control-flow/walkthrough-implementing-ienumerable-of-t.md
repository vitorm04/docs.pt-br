---
title: Implementando IEnumerable
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: 4151a680050f234d450d8de5e67a767c54e8df68
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266905"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="2cc03-102">Instruções passo a passo: implementando IEnumerable(Of T) no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2cc03-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="2cc03-103">A <xref:System.Collections.Generic.IEnumerable%601> interface é implementada por classes que podem retornar uma seqüência de valores um item de cada vez.</span><span class="sxs-lookup"><span data-stu-id="2cc03-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="2cc03-104">A vantagem de devolver dados um item de cada vez é que você não precisa carregar o conjunto completo de dados na memória para trabalhar com ele.</span><span class="sxs-lookup"><span data-stu-id="2cc03-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="2cc03-105">Você só precisa usar memória suficiente para carregar um único item dos dados.</span><span class="sxs-lookup"><span data-stu-id="2cc03-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="2cc03-106">As classes `IEnumerable(T)` que implementam `For Each` a interface podem ser usadas com loops ou consultas LINQ.</span><span class="sxs-lookup"><span data-stu-id="2cc03-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="2cc03-107">Por exemplo, considere um aplicativo que deve ler um grande arquivo de texto e retornar cada linha do arquivo que corresponda a critérios de pesquisa específicos.</span><span class="sxs-lookup"><span data-stu-id="2cc03-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="2cc03-108">O aplicativo usa uma consulta LINQ para retornar linhas do arquivo que correspondem aos critérios especificados.</span><span class="sxs-lookup"><span data-stu-id="2cc03-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="2cc03-109">Para consultar o conteúdo do arquivo usando uma consulta LINQ, o aplicativo poderia carregar o conteúdo do arquivo em uma matriz ou uma coleção.</span><span class="sxs-lookup"><span data-stu-id="2cc03-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="2cc03-110">No entanto, carregar todo o arquivo em uma matriz ou coleção consumiria muito mais memória do que o necessário.</span><span class="sxs-lookup"><span data-stu-id="2cc03-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="2cc03-111">A consulta LINQ poderia, em vez disso, consultar o conteúdo do arquivo usando uma classe enumerada, retornando apenas valores que correspondem aos critérios de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="2cc03-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="2cc03-112">Consultas que retornam apenas alguns valores correspondentes consumiriam muito menos memória.</span><span class="sxs-lookup"><span data-stu-id="2cc03-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="2cc03-113">Você pode criar uma classe <xref:System.Collections.Generic.IEnumerable%601> que implemente a interface para expor os dados de origem como dados enumerados.</span><span class="sxs-lookup"><span data-stu-id="2cc03-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="2cc03-114">Sua classe que `IEnumerable(T)` implementa a interface exigirá <xref:System.Collections.Generic.IEnumerator%601> outra classe que implemente a interface para iterar através dos dados de origem.</span><span class="sxs-lookup"><span data-stu-id="2cc03-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="2cc03-115">Essas duas classes permitem que você devolva itens de dados sequencialmente como um tipo específico.</span><span class="sxs-lookup"><span data-stu-id="2cc03-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="2cc03-116">Neste passo a passo, você criará `IEnumerable(Of String)` uma classe que implementa `IEnumerator(Of String)` a interface e uma classe que implementa a interface para ler um arquivo de texto uma linha de cada vez.</span><span class="sxs-lookup"><span data-stu-id="2cc03-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="2cc03-117">Criando a Classe Enumerada</span><span class="sxs-lookup"><span data-stu-id="2cc03-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="2cc03-118">**Crie o projeto de classe enumerado**</span><span class="sxs-lookup"><span data-stu-id="2cc03-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="2cc03-119">No Visual Basic, no menu **Arquivo,** aponte para **Novo** e clique em **Projeto**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="2cc03-120">Na caixa de diálogo **Novo Projeto**, no painel **Tipos de Projetos**, certifique-se de que **Windows** esteja selecionado.</span><span class="sxs-lookup"><span data-stu-id="2cc03-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="2cc03-121">Selecione **Biblioteca de Classes** no painel **Modelos**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="2cc03-122">Na caixa **Nome**, digite `StreamReaderEnumerable` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="2cc03-123">O novo projeto é exibido.</span><span class="sxs-lookup"><span data-stu-id="2cc03-123">The new project is displayed.</span></span>

1. <span data-ttu-id="2cc03-124">No **Solution Explorer,** clique com o botão direito do mouse no arquivo Class1.vb e clique **em Renomear**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="2cc03-125">Renomeie o arquivo como `StreamReaderEnumerable.vb` e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="2cc03-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="2cc03-126">Renomear o arquivo também renomeará a classe para `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="2cc03-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="2cc03-127">Essa classe implementará a interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="2cc03-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="2cc03-128">Clique com o botão direito do mouse no projeto StreamReaderEnumerable, aponte para **Adicionar**e clique em **Novo Item**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="2cc03-129">Selecione o modelo **Classe.**</span><span class="sxs-lookup"><span data-stu-id="2cc03-129">Select the **Class** template.</span></span> <span data-ttu-id="2cc03-130">Na caixa **Nome**, digite `StreamReaderEnumerator.vb` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="2cc03-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="2cc03-131">A primeira classe deste projeto é a classe `IEnumerable(Of String)` enumerada e implementará a interface.</span><span class="sxs-lookup"><span data-stu-id="2cc03-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="2cc03-132">Esta interface genérica <xref:System.Collections.IEnumerable> implementa a interface e garante que os `String`consumidores desta classe possam acessar valores digitados como .</span><span class="sxs-lookup"><span data-stu-id="2cc03-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="2cc03-133">**Adicione o código para implementar IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="2cc03-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="2cc03-134">Abra o arquivo StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="2cc03-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="2cc03-135">Na linha `Public Class StreamReaderEnumerable`após , digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="2cc03-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="2cc03-136">O Visual Basic preenche automaticamente a classe com `IEnumerable(Of String)` os membros que são exigidos pela interface.</span><span class="sxs-lookup"><span data-stu-id="2cc03-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="2cc03-137">Esta classe enumerada lerá linhas de um arquivo de texto uma linha de cada vez.</span><span class="sxs-lookup"><span data-stu-id="2cc03-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="2cc03-138">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como parâmetro de entrada.</span><span class="sxs-lookup"><span data-stu-id="2cc03-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="2cc03-139">Sua implementação <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> do `IEnumerable(Of String)` método da interface retornará `StreamReaderEnumerator` uma nova instância da classe.</span><span class="sxs-lookup"><span data-stu-id="2cc03-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="2cc03-140">A implementação `GetEnumerator` do `IEnumerable` método da `Private`interface pode ser feita, porque `IEnumerable(Of String)` você tem que expor apenas os membros da interface.</span><span class="sxs-lookup"><span data-stu-id="2cc03-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="2cc03-141">Substitua o código que `GetEnumerator` o Visual Basic gerou para os métodos com o seguinte código.</span><span class="sxs-lookup"><span data-stu-id="2cc03-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="2cc03-142">**Adicione o código para implementar o IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="2cc03-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="2cc03-143">Abra o arquivo StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="2cc03-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="2cc03-144">Na linha `Public Class StreamReaderEnumerator`após , digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="2cc03-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="2cc03-145">O Visual Basic preenche automaticamente a classe com `IEnumerator(Of String)` os membros que são exigidos pela interface.</span><span class="sxs-lookup"><span data-stu-id="2cc03-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="2cc03-146">A classe enumerador abre o arquivo de texto e executa a I/O do arquivo para ler as linhas do arquivo.</span><span class="sxs-lookup"><span data-stu-id="2cc03-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="2cc03-147">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como parâmetro de entrada e abre o arquivo de texto para leitura.</span><span class="sxs-lookup"><span data-stu-id="2cc03-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="2cc03-148">As `Current` propriedades para `IEnumerator(Of String)` `IEnumerator` as interfaces e interfaces retornam o `String`item atual do arquivo de texto como um .</span><span class="sxs-lookup"><span data-stu-id="2cc03-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="2cc03-149">A implementação `Current` da `IEnumerator` propriedade da `Private`interface pode ser feita, porque `IEnumerator(Of String)` você tem que expor apenas os membros da interface.</span><span class="sxs-lookup"><span data-stu-id="2cc03-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="2cc03-150">Substitua o código que `Current` o Visual Basic gerou para as propriedades pelo seguinte código.</span><span class="sxs-lookup"><span data-stu-id="2cc03-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="2cc03-151">O `MoveNext` método `IEnumerator` da interface navega até o próximo item no arquivo de texto `Current` e atualiza o valor que é devolvido pela propriedade.</span><span class="sxs-lookup"><span data-stu-id="2cc03-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="2cc03-152">Se não houver mais itens para `MoveNext` ler, `False`o método retorna; caso contrário, `MoveNext` `True`o método retorna .</span><span class="sxs-lookup"><span data-stu-id="2cc03-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="2cc03-153">Adicione o seguinte código ao `MoveNext` método.</span><span class="sxs-lookup"><span data-stu-id="2cc03-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="2cc03-154">O `Reset` método `IEnumerator` da interface direciona o iterador para apontar para o início do arquivo de texto e limpa o valor atual do item.</span><span class="sxs-lookup"><span data-stu-id="2cc03-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="2cc03-155">Adicione o seguinte código ao `Reset` método.</span><span class="sxs-lookup"><span data-stu-id="2cc03-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="2cc03-156">O `Dispose` método `IEnumerator` da interface garante que todos os recursos não gerenciados sejam liberados antes que o iterizador seja destruído.</span><span class="sxs-lookup"><span data-stu-id="2cc03-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="2cc03-157">O cabo de arquivo `StreamReader` usado pelo objeto é um recurso não gerenciado e deve ser fechado antes que a instância do tempo rétero seja destruída.</span><span class="sxs-lookup"><span data-stu-id="2cc03-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="2cc03-158">Substitua o código que `Dispose` o Visual Basic gerou para o método pelo seguinte código.</span><span class="sxs-lookup"><span data-stu-id="2cc03-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)]
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="2cc03-159">Usando o éterizador de amostras</span><span class="sxs-lookup"><span data-stu-id="2cc03-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="2cc03-160">Você pode usar uma classe enumerada em seu código juntamente `IEnumerable`com estruturas `For Next` de controle que requerem um objeto que implementa, como um loop ou uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="2cc03-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="2cc03-161">O exemplo a `StreamReaderEnumerable` seguir mostra a consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="2cc03-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="2cc03-162">Confira também</span><span class="sxs-lookup"><span data-stu-id="2cc03-162">See also</span></span>

- [<span data-ttu-id="2cc03-163">Introdução a LINQ no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="2cc03-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="2cc03-164">Fluxo de Controle</span><span class="sxs-lookup"><span data-stu-id="2cc03-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="2cc03-165">Estruturas de Loop</span><span class="sxs-lookup"><span data-stu-id="2cc03-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="2cc03-166">Instrução For Each...Next</span><span class="sxs-lookup"><span data-stu-id="2cc03-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
