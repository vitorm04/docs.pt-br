---
title: Implementando IEnumerable no Visual Basic
ms.date: 07/31/2018
helpviewer_keywords:
- control flow [Visual Basic]
- enumerable interfaces
- loop structures [Visual Basic], optimizing performance
- control flow [Visual Basic]
ms.assetid: c60d7589-51f2-4463-a2d5-22506bbc1554
ms.openlocfilehash: b13fd85ae01fd0b6f3c963d87a372add930be99d
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/18/2019
ms.locfileid: "59302576"
---
# <a name="walkthrough-implementing-ienumerableof-t-in-visual-basic"></a><span data-ttu-id="df14d-102">Passo a passo: Implementando IEnumerable(Of T) no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="df14d-102">Walkthrough: Implementing IEnumerable(Of T) in Visual Basic</span></span>
<span data-ttu-id="df14d-103">O <xref:System.Collections.Generic.IEnumerable%601> interface é implementada por classes que podem retornar uma sequência de um item de valores de cada vez.</span><span class="sxs-lookup"><span data-stu-id="df14d-103">The <xref:System.Collections.Generic.IEnumerable%601> interface is implemented by classes that can return a sequence of values one item at a time.</span></span> <span data-ttu-id="df14d-104">A vantagem de retornar dados de um item por vez é que você não precisa carregar o conjunto completo de dados na memória para trabalhar com ela.</span><span class="sxs-lookup"><span data-stu-id="df14d-104">The advantage of returning data one item at a time is that you do not have to load the complete set of data into memory to work with it.</span></span> <span data-ttu-id="df14d-105">Você só precisará usar memória suficiente para carregar um único item de dados.</span><span class="sxs-lookup"><span data-stu-id="df14d-105">You only have to use sufficient memory to load a single item from the data.</span></span> <span data-ttu-id="df14d-106">As classes que implementam o `IEnumerable(T)` interface pode ser usada com `For Each` loops ou consultas LINQ.</span><span class="sxs-lookup"><span data-stu-id="df14d-106">Classes that implement the `IEnumerable(T)` interface can be used with `For Each` loops or LINQ queries.</span></span>  
  
 <span data-ttu-id="df14d-107">Por exemplo, considere um aplicativo que deve ler um arquivo de texto grande e retornar todas as linhas do arquivo que corresponde aos critérios de pesquisa específica.</span><span class="sxs-lookup"><span data-stu-id="df14d-107">For example, consider an application that must read a large text file and return each line from the file that matches particular search criteria.</span></span> <span data-ttu-id="df14d-108">O aplicativo usa uma consulta LINQ para retornar linhas do arquivo que correspondem aos critérios especificados.</span><span class="sxs-lookup"><span data-stu-id="df14d-108">The application uses a LINQ query to return lines from the file that match the specified criteria.</span></span> <span data-ttu-id="df14d-109">Para consultar o conteúdo do arquivo, usando uma consulta LINQ, o aplicativo foi possível carregar o conteúdo do arquivo em uma matriz ou uma coleção.</span><span class="sxs-lookup"><span data-stu-id="df14d-109">To query the contents of the file by using a LINQ query, the application could load the contents of the file into an array or a collection.</span></span> <span data-ttu-id="df14d-110">No entanto, carregar o arquivo inteiro em uma matriz ou coleção consumiria muito mais memória do que é necessário.</span><span class="sxs-lookup"><span data-stu-id="df14d-110">However, loading the whole file into an array or collection would consume far more memory than is required.</span></span> <span data-ttu-id="df14d-111">A consulta LINQ em vez disso, pode consultar o conteúdo do arquivo por meio de uma classe enumerable, retornando somente os valores que correspondem aos critérios de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="df14d-111">The LINQ query could instead query the file contents by using an enumerable class, returning only values that match the search criteria.</span></span> <span data-ttu-id="df14d-112">Consultas que retornam apenas alguns valores correspondentes consumiria muito menos memória.</span><span class="sxs-lookup"><span data-stu-id="df14d-112">Queries that return only a few matching values would consume far less memory.</span></span>  
  
 <span data-ttu-id="df14d-113">Você pode criar uma classe que implementa o <xref:System.Collections.Generic.IEnumerable%601> interface para expor dados de origem como dados enumeráveis.</span><span class="sxs-lookup"><span data-stu-id="df14d-113">You can create a class that implements the <xref:System.Collections.Generic.IEnumerable%601> interface to expose source data as enumerable data.</span></span> <span data-ttu-id="df14d-114">Sua classe que implementa o `IEnumerable(T)` interface exigirá outra classe que implementa o <xref:System.Collections.Generic.IEnumerator%601> interface para iterar os dados de origem.</span><span class="sxs-lookup"><span data-stu-id="df14d-114">Your class that implements the `IEnumerable(T)` interface will require another class that implements the <xref:System.Collections.Generic.IEnumerator%601> interface to iterate through the source data.</span></span> <span data-ttu-id="df14d-115">Essas duas classes permitem retornar itens de dados em sequência como um tipo específico.</span><span class="sxs-lookup"><span data-stu-id="df14d-115">These two classes enable you to return items of data sequentially as a specific type.</span></span>  
  
 <span data-ttu-id="df14d-116">Neste passo a passo, você criará uma classe que implementa o `IEnumerable(Of String)` interface e uma classe que implementa o `IEnumerator(Of String)` interface para ler um arquivo de texto uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="df14d-116">In this walkthrough, you will create a class that implements the `IEnumerable(Of String)` interface and a class that implements the `IEnumerator(Of String)` interface to read a text file one line at a time.</span></span>  
  
[!INCLUDE[note_settings_general](~/includes/note-settings-general-md.md)]  
  
## <a name="creating-the-enumerable-class"></a><span data-ttu-id="df14d-117">Criando a classe Enumerable</span><span class="sxs-lookup"><span data-stu-id="df14d-117">Creating the Enumerable Class</span></span>  
  
<span data-ttu-id="df14d-118">**Criar o projeto de classe enumerable**</span><span class="sxs-lookup"><span data-stu-id="df14d-118">**Create the enumerable class project**</span></span>

1. <span data-ttu-id="df14d-119">No Visual Basic, sobre o **arquivo** , aponte para **New** e, em seguida, clique em **projeto**.</span><span class="sxs-lookup"><span data-stu-id="df14d-119">In Visual Basic, on the **File** menu, point to **New** and then click **Project**.</span></span>

1. <span data-ttu-id="df14d-120">Na caixa de diálogo **Novo Projeto**, no painel **Tipos de Projetos**, certifique-se de que **Windows** esteja selecionado.</span><span class="sxs-lookup"><span data-stu-id="df14d-120">In the **New Project** dialog box, in the **Project Types** pane, make sure that **Windows** is selected.</span></span> <span data-ttu-id="df14d-121">Selecione **Biblioteca de Classes** no painel **Modelos**.</span><span class="sxs-lookup"><span data-stu-id="df14d-121">Select **Class Library** in the **Templates** pane.</span></span> <span data-ttu-id="df14d-122">Na caixa **Nome**, digite `StreamReaderEnumerable` e clique em **OK**.</span><span class="sxs-lookup"><span data-stu-id="df14d-122">In the **Name** box, type `StreamReaderEnumerable`, and then click **OK**.</span></span> <span data-ttu-id="df14d-123">O novo projeto é exibido.</span><span class="sxs-lookup"><span data-stu-id="df14d-123">The new project is displayed.</span></span>

1. <span data-ttu-id="df14d-124">Na **Gerenciador de soluções**, o arquivo Class1.vb com o botão direito e clique em **Renomear**.</span><span class="sxs-lookup"><span data-stu-id="df14d-124">In **Solution Explorer**, right-click the Class1.vb file and click **Rename**.</span></span> <span data-ttu-id="df14d-125">Renomeie o arquivo como `StreamReaderEnumerable.vb` e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="df14d-125">Rename the file to `StreamReaderEnumerable.vb` and press ENTER.</span></span> <span data-ttu-id="df14d-126">Renomear o arquivo também renomeará a classe para `StreamReaderEnumerable`.</span><span class="sxs-lookup"><span data-stu-id="df14d-126">Renaming the file will also rename the class to `StreamReaderEnumerable`.</span></span> <span data-ttu-id="df14d-127">Essa classe implementará a interface `IEnumerable(Of String)`.</span><span class="sxs-lookup"><span data-stu-id="df14d-127">This class will implement the `IEnumerable(Of String)` interface.</span></span>

1. <span data-ttu-id="df14d-128">Clique com botão direito no projeto StreamReaderEnumerable, aponte para **Add**e, em seguida, clique em **Novo Item**.</span><span class="sxs-lookup"><span data-stu-id="df14d-128">Right-click the StreamReaderEnumerable project, point to **Add**, and then click **New Item**.</span></span> <span data-ttu-id="df14d-129">Selecione o **classe** modelo.</span><span class="sxs-lookup"><span data-stu-id="df14d-129">Select the **Class** template.</span></span> <span data-ttu-id="df14d-130">No **nome** , digite `StreamReaderEnumerator.vb` e clique em **Okey**.</span><span class="sxs-lookup"><span data-stu-id="df14d-130">In the **Name** box, type `StreamReaderEnumerator.vb` and click **OK**.</span></span>

 <span data-ttu-id="df14d-131">A primeira classe no projeto é a classe enumerable e implementará o `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="df14d-131">The first class in this project is the enumerable class and will implement the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="df14d-132">Essa interface genérica implementa o <xref:System.Collections.IEnumerable> interface e garantias de que os consumidores dessa classe podem acessar valores digitados como `String`.</span><span class="sxs-lookup"><span data-stu-id="df14d-132">This generic interface implements the <xref:System.Collections.IEnumerable> interface and guarantees that consumers of this class can access values typed as `String`.</span></span>  
  
<span data-ttu-id="df14d-133">**Adicione o código para implementar IEnumerable**</span><span class="sxs-lookup"><span data-stu-id="df14d-133">**Add the code to implement IEnumerable**</span></span>

1. <span data-ttu-id="df14d-134">Abra o arquivo StreamReaderEnumerable.vb.</span><span class="sxs-lookup"><span data-stu-id="df14d-134">Open the StreamReaderEnumerable.vb file.</span></span>

2. <span data-ttu-id="df14d-135">Na linha após `Public Class StreamReaderEnumerable`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="df14d-135">On the line after `Public Class StreamReaderEnumerable`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#1](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#1)]

   <span data-ttu-id="df14d-136">Visual Basic preenche automaticamente a classe com os membros que são exigidas pelo `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="df14d-136">Visual Basic automatically populates the class with the members that are required by the `IEnumerable(Of String)` interface.</span></span>
  
3. <span data-ttu-id="df14d-137">Essa classe enumerable lerá linhas de um arquivo de texto uma linha por vez.</span><span class="sxs-lookup"><span data-stu-id="df14d-137">This enumerable class will read lines from a text file one line at a time.</span></span> <span data-ttu-id="df14d-138">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como um parâmetro de entrada.</span><span class="sxs-lookup"><span data-stu-id="df14d-138">Add the following code to the class to expose a public constructor that takes a file path as an input parameter.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#2](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#2)]

4. <span data-ttu-id="df14d-139">Sua implementação do <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> método do `IEnumerable(Of String)` retorna uma nova instância da interface a `StreamReaderEnumerator` classe.</span><span class="sxs-lookup"><span data-stu-id="df14d-139">Your implementation of the <xref:System.Collections.Generic.IEnumerable%601.GetEnumerator%2A> method of the `IEnumerable(Of String)` interface will return a new instance of the `StreamReaderEnumerator` class.</span></span> <span data-ttu-id="df14d-140">A implementação do `GetEnumerator` método da `IEnumerable` interface pode ser feita `Private`, porque você tem para expor somente os membros do `IEnumerable(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="df14d-140">The implementation of the `GetEnumerator` method of the `IEnumerable` interface can be made `Private`, because you have to expose only members of the `IEnumerable(Of String)` interface.</span></span> <span data-ttu-id="df14d-141">Substitua o código de Visual Basic gerado para o `GetEnumerator` métodos com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="df14d-141">Replace the code that Visual Basic generated for the `GetEnumerator` methods with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#3](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#3)]  
  
<span data-ttu-id="df14d-142">**Adicione o código para implementar IEnumerator**</span><span class="sxs-lookup"><span data-stu-id="df14d-142">**Add the code to implement IEnumerator**</span></span>

1. <span data-ttu-id="df14d-143">Abra o arquivo StreamReaderEnumerator.vb.</span><span class="sxs-lookup"><span data-stu-id="df14d-143">Open the StreamReaderEnumerator.vb file.</span></span>

2. <span data-ttu-id="df14d-144">Na linha após `Public Class StreamReaderEnumerator`, digite o seguinte e pressione ENTER.</span><span class="sxs-lookup"><span data-stu-id="df14d-144">On the line after `Public Class StreamReaderEnumerator`, type the following and press ENTER.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#4](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#4)]

   <span data-ttu-id="df14d-145">Visual Basic preenche automaticamente a classe com os membros que são exigidas pelo `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="df14d-145">Visual Basic automatically populates the class with the members that are required by the `IEnumerator(Of String)` interface.</span></span>

3. <span data-ttu-id="df14d-146">A classe de enumerador abre o arquivo de texto e executa o arquivo de e/s ao ler as linhas do arquivo.</span><span class="sxs-lookup"><span data-stu-id="df14d-146">The enumerator class opens the text file and performs the file I/O to read the lines from the file.</span></span> <span data-ttu-id="df14d-147">Adicione o seguinte código à classe para expor um construtor público que toma um caminho de arquivo como um parâmetro de entrada e abra o arquivo de texto para leitura.</span><span class="sxs-lookup"><span data-stu-id="df14d-147">Add the following code to the class to expose a public constructor that takes a file path as an input parameter and open the text file for reading.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#5](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#5)]

4. <span data-ttu-id="df14d-148">O `Current` propriedades para ambos os `IEnumerator(Of String)` e `IEnumerator` interfaces retornam o item atual do arquivo de texto como um `String`.</span><span class="sxs-lookup"><span data-stu-id="df14d-148">The `Current` properties for both the `IEnumerator(Of String)` and `IEnumerator` interfaces return the current item from the text file as a `String`.</span></span> <span data-ttu-id="df14d-149">A implementação do `Current` propriedade do `IEnumerator` interface pode ser feita `Private`, porque você tem para expor somente os membros do `IEnumerator(Of String)` interface.</span><span class="sxs-lookup"><span data-stu-id="df14d-149">The implementation of the `Current` property of the `IEnumerator` interface can be made `Private`, because you have to expose only members of the `IEnumerator(Of String)` interface.</span></span> <span data-ttu-id="df14d-150">Substitua o código de Visual Basic gerado para o `Current` propriedades com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="df14d-150">Replace the code that Visual Basic generated for the `Current` properties with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#6](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#6)]

5. <span data-ttu-id="df14d-151">O `MoveNext` método da `IEnumerator` interface navega para o próximo item no arquivo de texto e atualiza o valor retornado pelo `Current` propriedade.</span><span class="sxs-lookup"><span data-stu-id="df14d-151">The `MoveNext` method of the `IEnumerator` interface navigates to the next item in the text file and updates the value that is returned by the `Current` property.</span></span> <span data-ttu-id="df14d-152">Se não houver nenhum outro item para ler, o `MoveNext` retorn `False`; caso contrário, o `MoveNext` retorno do método `True`.</span><span class="sxs-lookup"><span data-stu-id="df14d-152">If there are no more items to read, the `MoveNext` method returns `False`; otherwise the `MoveNext` method returns `True`.</span></span> <span data-ttu-id="df14d-153">Adicione o seguinte código ao método de `MoveNext` .</span><span class="sxs-lookup"><span data-stu-id="df14d-153">Add the following code to the `MoveNext` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#7](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#7)]

6. <span data-ttu-id="df14d-154">O `Reset` método da `IEnumerator` interface direciona o iterador para apontar para o início do arquivo de texto e limpa o valor do item atual.</span><span class="sxs-lookup"><span data-stu-id="df14d-154">The `Reset` method of the `IEnumerator` interface directs the iterator to point to the start of the text file and clears the current item value.</span></span> <span data-ttu-id="df14d-155">Adicione o seguinte código ao método de `Reset` .</span><span class="sxs-lookup"><span data-stu-id="df14d-155">Add the following code to the `Reset` method.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#8](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#8)]

7. <span data-ttu-id="df14d-156">O `Dispose` método da `IEnumerator` interface garante que todos os recursos não gerenciados são liberados antes que o iterador seja destruído.</span><span class="sxs-lookup"><span data-stu-id="df14d-156">The `Dispose` method of the `IEnumerator` interface guarantees that all unmanaged resources are released before the iterator is destroyed.</span></span> <span data-ttu-id="df14d-157">O identificador de arquivo que é usado pelo `StreamReader` objeto é um recurso não gerenciado e deve ser fechado antes que a instância do iterador seja destruída.</span><span class="sxs-lookup"><span data-stu-id="df14d-157">The file handle that is used by the `StreamReader` object is an unmanaged resource and must be closed before the iterator instance is destroyed.</span></span> <span data-ttu-id="df14d-158">Substitua o código de Visual Basic gerado para o `Dispose` método com o código a seguir.</span><span class="sxs-lookup"><span data-stu-id="df14d-158">Replace the code that Visual Basic generated for the `Dispose` method with the following code.</span></span>

     [!code-vb[VbVbalrIteratorWalkthrough#9](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/StreamReaderIterator.vb#9)] 
  
## <a name="using-the-sample-iterator"></a><span data-ttu-id="df14d-159">Usando o iterador de exemplo</span><span class="sxs-lookup"><span data-stu-id="df14d-159">Using the Sample Iterator</span></span>

 <span data-ttu-id="df14d-160">Você pode usar uma classe enumerável em seu código junto com estruturas de controle que exigem um objeto que implementa `IEnumerable`, como um `For Next` loop ou uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="df14d-160">You can use an enumerable class in your code together with control structures that require an object that implements `IEnumerable`, such as a `For Next` loop or a LINQ query.</span></span> <span data-ttu-id="df14d-161">A exemplo a seguir mostra o `StreamReaderEnumerable` em uma consulta LINQ.</span><span class="sxs-lookup"><span data-stu-id="df14d-161">The following example shows the `StreamReaderEnumerable` in a LINQ query.</span></span>  
  
 [!code-vb[VbVbalrIteratorWalkthrough#10](~/samples/snippets/visualbasic/VS_Snippets_VBCSharp/VbVbalrIteratorWalkthrough/VB/Module1.vb#10)]  
  
## <a name="see-also"></a><span data-ttu-id="df14d-162">Consulte também</span><span class="sxs-lookup"><span data-stu-id="df14d-162">See also</span></span>

- [<span data-ttu-id="df14d-163">Introdução ao LINQ no Visual Basic</span><span class="sxs-lookup"><span data-stu-id="df14d-163">Introduction to LINQ in Visual Basic</span></span>](../../../../visual-basic/programming-guide/language-features/linq/introduction-to-linq.md)
- [<span data-ttu-id="df14d-164">Fluxo de Controle</span><span class="sxs-lookup"><span data-stu-id="df14d-164">Control Flow</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/index.md)
- [<span data-ttu-id="df14d-165">Estruturas de Loop</span><span class="sxs-lookup"><span data-stu-id="df14d-165">Loop Structures</span></span>](../../../../visual-basic/programming-guide/language-features/control-flow/loop-structures.md)
- [<span data-ttu-id="df14d-166">Instrução For Each...Next</span><span class="sxs-lookup"><span data-stu-id="df14d-166">For Each...Next Statement</span></span>](../../../../visual-basic/language-reference/statements/for-each-next-statement.md)
