---
title: O tipo de '<variablename>' é ambíguo porque os limites de loop e a variável step não são expandidos para o mesmo tipo
ms.date: 07/20/2015
f1_keywords:
- vbc30983
- bc30983
helpviewer_keywords:
- BC30983
ms.assetid: 6b97153c-dee3-4429-b92a-2e5a212c864b
ms.openlocfilehash: c126561fb19b0d0af43c22bd3ae9b957448558c4
ms.sourcegitcommit: 43d10ef65f0f1fd6c3b515e363bde11a3fcd8d6d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/04/2020
ms.locfileid: "78266437"
---
# <a name="type-of-variablename-is-ambiguous-because-the-loop-bounds-and-the-step-variable-do-not-widen-to-the-same-type"></a><span data-ttu-id="6db32-102">O tipo\<de 'variávelnome>' é ambíguo porque os limites do loop e a variável de passo não se ampliam para o mesmo tipo</span><span class="sxs-lookup"><span data-stu-id="6db32-102">Type of '\<variablename>' is ambiguous because the loop bounds and the step variable do not widen to the same type</span></span>
<span data-ttu-id="6db32-103">Seu código `For...Next` contém um loop no qual o compilador não pode inferir um tipo de dados para a variável de controle de loop porque as seguintes condições são verdadeiras:</span><span class="sxs-lookup"><span data-stu-id="6db32-103">Your code contains a `For...Next` loop in which the compiler cannot infer a data type for the loop control variable because the following conditions is true:</span></span>  
  
- <span data-ttu-id="6db32-104">O tipo de dados da variável de `As` controle de loop não é especificado com uma cláusula.</span><span class="sxs-lookup"><span data-stu-id="6db32-104">The data type of the loop control variable is not specified with an `As` clause.</span></span>  
  
- <span data-ttu-id="6db32-105">Os limites de loop e a variável de passo contêm pelo menos dois tipos de dados.</span><span class="sxs-lookup"><span data-stu-id="6db32-105">The loop bounds and step variable contain at least two data types.</span></span>  
  
- <span data-ttu-id="6db32-106">Mais de uma possível conversão existe entre os tipos de dados.</span><span class="sxs-lookup"><span data-stu-id="6db32-106">More than one possible conversion exists between the data types.</span></span>  
  
- <span data-ttu-id="6db32-107">Não há melhor tipo entre os candidatos, de modo que a escolha do tipo para a variável de controle de loop é ambígua.</span><span class="sxs-lookup"><span data-stu-id="6db32-107">There is no best type among the candidates, so that the choice of type for the loop control variable is ambiguous.</span></span>  
  
 <span data-ttu-id="6db32-108">Por exemplo, o loop a seguir `Integer` tem um loop `UInteger`bound de tipo e um loop bound do tipo:</span><span class="sxs-lookup"><span data-stu-id="6db32-108">For example, the following loop has one loop bound of type `Integer` and one loop bound of type `UInteger`:</span></span>  
  
```vb  
Dim m As Integer = 1  
Dim n As UInteger = 10  
' Not valid.  
' For i = m To n  
    ' Loop processing.  
' Next  
```  
  
 <span data-ttu-id="6db32-109">Há uma possível `Integer` conversão `UInteger`entre e , `UInteger` `Integer`e uma possível conversão entre e , mas ambos estão estreitando conversões, então nem é a melhor escolha.</span><span class="sxs-lookup"><span data-stu-id="6db32-109">There is a possible conversion between `Integer` and `UInteger`, and a possible conversion between `UInteger` and `Integer`, but both are narrowing conversions so neither is the best choice.</span></span>  
  
 <span data-ttu-id="6db32-110">No exemplo seguinte, uma terceira `Double` variável de tipo é adicionada.</span><span class="sxs-lookup"><span data-stu-id="6db32-110">In the next example, a third variable of type `Double` is added.</span></span> <span data-ttu-id="6db32-111">Ambos `Integer` `UInteger` e têm conversões `Double`padrão de ampliação para, o que torna a conversão para `Double` a melhor escolha.</span><span class="sxs-lookup"><span data-stu-id="6db32-111">Both `Integer` and `UInteger` have standard widening conversions to `Double`, which makes conversion to `Double` the best choice.</span></span> <span data-ttu-id="6db32-112">Inferência de tipo atribui `i` à variável `Double`controle de loop do tipo de dados .</span><span class="sxs-lookup"><span data-stu-id="6db32-112">Type inference assigns to loop control variable `i` the data type `Double`.</span></span>  
  
```vb  
Dim stepVar As Double = 1  
' Valid.  
For i = m To n Step stepVar  
    ' Loop processing.  
Next  
```  
  
 <span data-ttu-id="6db32-113">**ID de erro:** BC30983</span><span class="sxs-lookup"><span data-stu-id="6db32-113">**Error ID:** BC30983</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="6db32-114">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="6db32-114">To correct this error</span></span>  
  
- <span data-ttu-id="6db32-115">Converta explicitamente uma das variáveis para um tipo que as outras tenham uma conversão ampliada, por exemplo:</span><span class="sxs-lookup"><span data-stu-id="6db32-115">Explicitly convert one of the variables to a type that the others have a widening conversion to, for example:</span></span>  
  
    ```vb  
    For i = m To CLng(n)  
    ```  
  
- <span data-ttu-id="6db32-116">Use `As` uma cláusula para especificar o tipo da variável de controle:</span><span class="sxs-lookup"><span data-stu-id="6db32-116">Use an `As` clause to specify the type of the control variable:</span></span>  
  
    ```vb  
    For i As Double = m To n
    ```  
  
## <a name="see-also"></a><span data-ttu-id="6db32-117">Confira também</span><span class="sxs-lookup"><span data-stu-id="6db32-117">See also</span></span>

- [<span data-ttu-id="6db32-118">Instrução For...Next</span><span class="sxs-lookup"><span data-stu-id="6db32-118">For...Next Statement</span></span>](../../visual-basic/language-reference/statements/for-next-statement.md)
- [<span data-ttu-id="6db32-119">Conversões Implícitas e Explícitas</span><span class="sxs-lookup"><span data-stu-id="6db32-119">Implicit and Explicit Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/implicit-and-explicit-conversions.md)
- [<span data-ttu-id="6db32-120">Inferência de Tipo de Variável Local</span><span class="sxs-lookup"><span data-stu-id="6db32-120">Local Type Inference</span></span>](../../visual-basic/programming-guide/language-features/variables/local-type-inference.md)
- [<span data-ttu-id="6db32-121">Instrução Option Infer</span><span class="sxs-lookup"><span data-stu-id="6db32-121">Option Infer Statement</span></span>](../../visual-basic/language-reference/statements/option-infer-statement.md)
- [<span data-ttu-id="6db32-122">Conversões de Widening e Narrowing</span><span class="sxs-lookup"><span data-stu-id="6db32-122">Widening and Narrowing Conversions</span></span>](../../visual-basic/programming-guide/language-features/data-types/widening-and-narrowing-conversions.md)
