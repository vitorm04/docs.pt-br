---
title: "' #ElseIf ', ' #Else ' ou ' #End If ' deve ser precedido por um ' #If ' correspondente"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 88e4de9458ab2596486c6aeb7c0e61455dad73d1
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69935232"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="46ba0-102">' #ElseIf ', ' #Else ' ou ' #End If ' deve ser precedido por um ' #If ' correspondente</span><span class="sxs-lookup"><span data-stu-id="46ba0-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="46ba0-103">`#ElseIf`, `#Else` e`#End If` são diretivas de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="46ba0-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="46ba0-104">O `#ElseIf`, `#Else` `#If` ou nãoéprecedidoporumadiretivacorrespondente`#End If` .</span><span class="sxs-lookup"><span data-stu-id="46ba0-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="46ba0-105">**ID do erro:** BC30013</span><span class="sxs-lookup"><span data-stu-id="46ba0-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="46ba0-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="46ba0-106">To correct this error</span></span>  
  
1. <span data-ttu-id="46ba0-107">Verifique se o pretendido `#If` não está separado da cláusula em questão por um bloco de compilação condicional intermediário ou um colocado `#End If`incorretamente.</span><span class="sxs-lookup"><span data-stu-id="46ba0-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    > <span data-ttu-id="46ba0-108">Apenas um `#Else` é permitido em cada `#If` bloco `#Else` , portanto, duas diretivas sucessivas causam esse erro.</span><span class="sxs-lookup"><span data-stu-id="46ba0-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2. <span data-ttu-id="46ba0-109">Verifique se a entrelinha `#` não está faltando em uma diretiva `#If` anterior.</span><span class="sxs-lookup"><span data-stu-id="46ba0-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3. <span data-ttu-id="46ba0-110">Se todo o resto estiver em ordem, adicione `#If` uma diretiva ao início do bloco de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="46ba0-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="46ba0-111">Consulte também</span><span class="sxs-lookup"><span data-stu-id="46ba0-111">See also</span></span>

- [<span data-ttu-id="46ba0-112">Diretivas #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="46ba0-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
