---
title: "'#ElseIf', '#Else' ou '#End If' deve ser precedido por um '#If' correspondente"
ms.date: 07/20/2015
f1_keywords:
- vbc30013
- bc30013
helpviewer_keywords:
- BC30013
ms.assetid: 8fe2d23c-8b8f-46d8-90f2-7f8857ea43bb
ms.openlocfilehash: 92a481febe4d2fb4045e7fbb50eee81c9656b189
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/15/2019
ms.locfileid: "58031683"
---
# <a name="elseif-else-or-end-if-must-be-preceded-by-a-matching-if"></a><span data-ttu-id="7c806-102">'#ElseIf', '#Else' ou '#End If' deve ser precedido por um '#If' correspondente</span><span class="sxs-lookup"><span data-stu-id="7c806-102">'#ElseIf', '#Else', or '#End If' must be preceded by a matching '#If'</span></span>
<span data-ttu-id="7c806-103">`#ElseIf`, `#Else`, e `#End If` são diretivas de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="7c806-103">`#ElseIf`, `#Else`, and `#End If` are conditional compilation directives.</span></span> <span data-ttu-id="7c806-104">O `#ElseIf`, `#Else`, ou `#End If` não é precedida por uma `#If` diretiva.</span><span class="sxs-lookup"><span data-stu-id="7c806-104">The `#ElseIf`, `#Else`, or `#End If` is not preceded by a corresponding `#If` directive.</span></span>  
  
 <span data-ttu-id="7c806-105">**ID do erro:** BC30013</span><span class="sxs-lookup"><span data-stu-id="7c806-105">**Error ID:** BC30013</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="7c806-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="7c806-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="7c806-107">Verifique se o pretendido `#If` não é separado da cláusula em questão por um bloco de compilação condicional ou incorretamente localizado `#End If`.</span><span class="sxs-lookup"><span data-stu-id="7c806-107">Check that the intended `#If` is not separated from the clause in question by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="7c806-108">Somente um `#Else` é permitido em cada `#If` bloquear, portanto, dois sucessivas `#Else` diretivas causam esse erro.</span><span class="sxs-lookup"><span data-stu-id="7c806-108">Only one `#Else` is permitted in each `#If` block, so two successive `#Else` directives cause this error.</span></span>  
  
2.  <span data-ttu-id="7c806-109">Verifique se o entrelinhamento `#` não está ausente do anterior `#If` diretiva.</span><span class="sxs-lookup"><span data-stu-id="7c806-109">Check that the leading `#` is not missing from an earlier `#If` directive.</span></span>  
  
3.  <span data-ttu-id="7c806-110">Se tudo estiver em ordem, adicione um `#If` diretiva para o início do bloco de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="7c806-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="7c806-111">Consulte também</span><span class="sxs-lookup"><span data-stu-id="7c806-111">See also</span></span>

- [<span data-ttu-id="7c806-112">Diretivas #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="7c806-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
