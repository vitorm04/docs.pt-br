---
title: "'#ElseIf' não é possível seguir #Else como parte de um bloco '#If'"
ms.date: 07/20/2015
f1_keywords:
- bc32030
- vbc32030
helpviewer_keywords:
- BC32030
ms.assetid: 248d6464-3019-4753-8a33-7070bbe5d2a6
ms.openlocfilehash: 3ed968e77d12f3c9a00bfc9be455ad0befc101e2
ms.sourcegitcommit: 5c1abeec15fbddcc7dbaa729fabc1f1f29f12045
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/15/2019
ms.locfileid: "58033918"
---
# <a name="elseif-cannot-follow-else-as-part-of-an-if-block"></a><span data-ttu-id="c701e-102">'#ElseIf' não é possível seguir #Else como parte de um bloco '#If'</span><span class="sxs-lookup"><span data-stu-id="c701e-102">'#ElseIf' cannot follow '#Else' as part of an '#If' block</span></span>
<span data-ttu-id="c701e-103">Uma `#ElseIf` diretiva de compilação condicional segue um `#Else` diretiva.</span><span class="sxs-lookup"><span data-stu-id="c701e-103">An `#ElseIf` conditional compilation directive follows an `#Else` directive.</span></span> <span data-ttu-id="c701e-104">`#Else` deve ser a última diretiva no bloco condicional antes do `#End If` diretiva.</span><span class="sxs-lookup"><span data-stu-id="c701e-104">`#Else` must be the last directive in the conditional block before the `#End If` directive.</span></span>  
  
 <span data-ttu-id="c701e-105">**ID do erro:** BC32030</span><span class="sxs-lookup"><span data-stu-id="c701e-105">**Error ID:** BC32030</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="c701e-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="c701e-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="c701e-107">Verifique se o anterior `#Else` deve ser um `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="c701e-107">Check if the preceding `#Else` should be an `#ElseIf`.</span></span>  
  
2.  <span data-ttu-id="c701e-108">Verifique se precedidos `#If` bloco é encerrado corretamente e que um novo `#If` bloco é iniciado.</span><span class="sxs-lookup"><span data-stu-id="c701e-108">Check that a preceding `#If` block is properly terminated and that a new `#If` block is initiated.</span></span>  
  
3.  <span data-ttu-id="c701e-109">Se tudo estiver correto, movê-la `#ElseIf` bloqueiam de diretiva e sua instrução correspondente para preceder o `#Else` bloco.</span><span class="sxs-lookup"><span data-stu-id="c701e-109">If everything else is correct, move this `#ElseIf` directive and its corresponding statement block to precede the `#Else` block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c701e-110">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c701e-110">See also</span></span>

- [<span data-ttu-id="c701e-111">Diretivas #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="c701e-111">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
