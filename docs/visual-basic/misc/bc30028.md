---
title: "'#Else' deve ser precedido por um '#If' ou '#ElseIf' correspondente"
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: fee4a48a95aeb020a0f7e3c42a29812b9a0848e9
ms.sourcegitcommit: f8c270376ed905f6a8896ce0fe25b4f4b38ff498
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/04/2020
ms.locfileid: "84404077"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="1f15b-102">'#Else' deve ser precedido por um '#If' ou '#ElseIf' correspondente</span><span class="sxs-lookup"><span data-stu-id="1f15b-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>
<span data-ttu-id="1f15b-103">`#Else`é uma diretiva de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="1f15b-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="1f15b-104">Uma `#Else` diretiva não é precedida por uma `#If` diretiva or correspondente `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="1f15b-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="1f15b-105">**ID do erro:** BC30028</span><span class="sxs-lookup"><span data-stu-id="1f15b-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="1f15b-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="1f15b-106">To correct this error</span></span>  
  
1. <span data-ttu-id="1f15b-107">Verifique se um anterior `#If` ou `#ElseIf` não está separado dele `#Else` por um bloco de compilação condicional intermediário ou um colocado incorretamente `#End If` .</span><span class="sxs-lookup"><span data-stu-id="1f15b-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2. <span data-ttu-id="1f15b-108">Verifique se isso `#Else` é precedido por outra `#Else` diretiva.</span><span class="sxs-lookup"><span data-stu-id="1f15b-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="1f15b-109">Se for, remova `#Else` ou altere-o para um `#ElseIf` .</span><span class="sxs-lookup"><span data-stu-id="1f15b-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3. <span data-ttu-id="1f15b-110">Se todo o resto estiver em ordem, adicione uma `#If` diretiva ao início do bloco de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="1f15b-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="1f15b-111">Confira também</span><span class="sxs-lookup"><span data-stu-id="1f15b-111">See also</span></span>

- [<span data-ttu-id="1f15b-112">#If... Diretivas then... #Else</span><span class="sxs-lookup"><span data-stu-id="1f15b-112">#If...Then...#Else Directives</span></span>](../language-reference/directives/if-then-else-directives.md)
