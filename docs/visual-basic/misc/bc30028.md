---
title: "' #Else ' deve ser precedido por um '#If' ou '#ElseIf' correspondente"
ms.date: 07/20/2015
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
ms.openlocfilehash: 7ded9b96c447068b5fd1436481d82bd8827f37ff
ms.sourcegitcommit: 0888d7b24f475c346a3f444de8d83ec1ca7cd234
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/22/2018
ms.locfileid: "53782214"
---
# <a name="else-must-be-preceded-by-a-matching-if-or-elseif"></a><span data-ttu-id="cf1c6-102">' #Else ' deve ser precedido por um '#If' ou '#ElseIf' correspondente</span><span class="sxs-lookup"><span data-stu-id="cf1c6-102">'#Else' must be preceded by a matching '#If' or '#ElseIf'</span></span>
<span data-ttu-id="cf1c6-103">`#Else` é uma diretiva de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="cf1c6-104">Uma `#Else` diretiva não é precedida por uma `#If` ou `#ElseIf` diretiva.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="cf1c6-105">**ID do erro:** BC30028</span><span class="sxs-lookup"><span data-stu-id="cf1c6-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="cf1c6-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="cf1c6-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="cf1c6-107">Verifique se precedidos `#If` ou `#ElseIf` não é separado disso `#Else` por um bloco de compilação condicional ou incorretamente localizado `#End If`.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="cf1c6-108">Verifique se `#Else` é precedida por outro `#Else` diretiva.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="cf1c6-109">Se estiver, remova `#Else` ou altere-o para um `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="cf1c6-110">Se tudo estiver em ordem, adicione um `#If` diretiva para o início do bloco de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="cf1c6-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="cf1c6-111">Consulte também</span><span class="sxs-lookup"><span data-stu-id="cf1c6-111">See Also</span></span>  
 [<span data-ttu-id="cf1c6-112">Diretivas #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="cf1c6-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
