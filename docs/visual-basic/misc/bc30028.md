---
title: '&quot; #Else &quot; deve ser precedido por um &quot;#If&quot; ou &quot;#ElseIf&quot; correspondente | Documentos do Microsoft'
ms.date: 2015-07-20
ms.prod: .net
ms.technology:
- devlang-visual-basic
ms.topic: article
f1_keywords:
- vbc30028
- bc30028
helpviewer_keywords:
- BC30028
ms.assetid: c6ed34de-d6ed-4227-9880-538055aff20a
caps.latest.revision: 12
author: dotnet-bot
ms.author: dotnetcontent
translation.priority.ht:
- de-de
- es-es
- fr-fr
- it-it
- ja-jp
- ko-kr
- ru-ru
- zh-cn
- zh-tw
translation.priority.mt:
- cs-cz
- pl-pl
- pt-br
- tr-tr
ms.translationtype: Machine Translation
ms.sourcegitcommit: 9f5b8ebb69c9206ff90b05e748c64d29d82f7a16
ms.openlocfilehash: 91ce52b69273cba04b126fb9ed7390025770be19
ms.contentlocale: pt-br
ms.lasthandoff: 04/12/2017

---
# <a name="39else39-must-be-preceded-by-a-matching-39if39-or-39elseif39"></a><span data-ttu-id="d9e0c-102">' #Else ' deve ser precedido por um '#If' ou '#ElseIf' correspondente</span><span class="sxs-lookup"><span data-stu-id="d9e0c-102">&#39;#Else&#39; must be preceded by a matching &#39;#If&#39; or &#39;#ElseIf&#39;</span></span>
<span data-ttu-id="d9e0c-103">`#Else`é uma diretiva de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-103">`#Else` is a conditional compilation directive.</span></span> <span data-ttu-id="d9e0c-104">Um `#Else` diretiva não é precedida por uma `#If` ou `#ElseIf` diretiva.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-104">An `#Else` directive is not preceded by a corresponding `#If` or `#ElseIf` directive.</span></span>  
  
 <span data-ttu-id="d9e0c-105">**ID do erro:** BC30028</span><span class="sxs-lookup"><span data-stu-id="d9e0c-105">**Error ID:** BC30028</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="d9e0c-106">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="d9e0c-106">To correct this error</span></span>  
  
1.  <span data-ttu-id="d9e0c-107">Verifique se precedidos `#If` ou `#ElseIf` não é separado disso `#Else` por um bloco de compilação condicional ou incorretamente localizado `#End If`.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-107">Check that a preceding `#If` or `#ElseIf` is not separated from this `#Else` by an intervening conditional compilation block or an incorrectly placed `#End If`.</span></span>  
  
2.  <span data-ttu-id="d9e0c-108">Verifique se `#Else` é precedido por outro `#Else` diretiva.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-108">Check that `#Else` is preceded by another `#Else` directive.</span></span> <span data-ttu-id="d9e0c-109">Se estiver, remova `#Else` ou alterá-lo para um `#ElseIf`.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-109">If it is, either remove `#Else` or change it to an `#ElseIf`.</span></span>  
  
3.  <span data-ttu-id="d9e0c-110">Se tudo estiver em ordem, adicione um `#If` diretiva para o início do bloco de compilação condicional.</span><span class="sxs-lookup"><span data-stu-id="d9e0c-110">If everything else is in order, add an `#If` directive to the beginning of the conditional compilation block.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d9e0c-111">Consulte também</span><span class="sxs-lookup"><span data-stu-id="d9e0c-111">See Also</span></span>  
 [<span data-ttu-id="d9e0c-112">Diretivas #If...Then...#Else</span><span class="sxs-lookup"><span data-stu-id="d9e0c-112">#If...Then...#Else Directives</span></span>](../../visual-basic/language-reference/directives/if-then-else-directives.md)
