---
title: Option Strict On requer que cada parâmetro de expressão lambda seja declarado com uma &#39;como&#39; cláusula se seu tipo não pode ser inferido.
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: f5743338d22775f5782f6a4cb11f713dbd99261c
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/04/2018
ms.locfileid: "33631674"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-39as39-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="cdcfe-102">Option Strict On requer que cada parâmetro de expressão lambda seja declarado com uma &#39;como&#39; cláusula se seu tipo não pode ser inferido.</span><span class="sxs-lookup"><span data-stu-id="cdcfe-102">Option Strict On requires each lambda expression parameter to be declared with an &#39;As&#39; clause if its type cannot be inferred</span></span>
<span data-ttu-id="cdcfe-103">Você declarou um parâmetro em uma expressão lambda sem usar uma `As` cláusula, com `Option Strict` em.</span><span class="sxs-lookup"><span data-stu-id="cdcfe-103">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="cdcfe-104">A declaração anterior é válida se o tipo de `n` pode ser inferido.</span><span class="sxs-lookup"><span data-stu-id="cdcfe-104">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="cdcfe-105">Por exemplo, se você estiver atribuindo a expressão lambda para uma função delegar `Del`:</span><span class="sxs-lookup"><span data-stu-id="cdcfe-105">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="cdcfe-106">Agora o tipo de `n` pode ser inferido de parâmetro `p`:</span><span class="sxs-lookup"><span data-stu-id="cdcfe-106">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="cdcfe-107">**ID do erro:** BC36642</span><span class="sxs-lookup"><span data-stu-id="cdcfe-107">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="cdcfe-108">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="cdcfe-108">To correct this error</span></span>  
  
-   <span data-ttu-id="cdcfe-109">Adicionar um `As` cláusula à declaração de parâmetro:</span><span class="sxs-lookup"><span data-stu-id="cdcfe-109">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="cdcfe-110">Consulte também</span><span class="sxs-lookup"><span data-stu-id="cdcfe-110">See Also</span></span>  
 [<span data-ttu-id="cdcfe-111">Expressões Lambda</span><span class="sxs-lookup"><span data-stu-id="cdcfe-111">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
