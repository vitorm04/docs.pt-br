---
title: Option Strict On requer que cada parâmetro de expressão lambda seja declarado com uma cláusula 'As', se seu tipo não pode ser deduzido
ms.date: 07/20/2015
f1_keywords:
- bc36642
- vbc36642
helpviewer_keywords:
- BC36642
ms.assetid: 2aaa62b8-49c9-4ae8-b0f5-08e3f0b5ad10
ms.openlocfilehash: 20d22c18105a9d9163703702a984bbaa71be0adc
ms.sourcegitcommit: 0888d7b24f475c346a3f444de8d83ec1ca7cd234
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/22/2018
ms.locfileid: "53759360"
---
# <a name="option-strict-on-requires-each-lambda-expression-parameter-to-be-declared-with-an-as-clause-if-its-type-cannot-be-inferred"></a><span data-ttu-id="cf1d0-102">Option Strict On requer que cada parâmetro de expressão lambda seja declarado com uma cláusula 'As', se seu tipo não pode ser deduzido</span><span class="sxs-lookup"><span data-stu-id="cf1d0-102">Option Strict On requires each lambda expression parameter to be declared with an 'As' clause if its type cannot be inferred</span></span>
<span data-ttu-id="cf1d0-103">Você declarou um parâmetro em uma expressão lambda sem usar um `As` cláusula, com `Option Strict` no.</span><span class="sxs-lookup"><span data-stu-id="cf1d0-103">You have declared a parameter in a lambda expression without using an `As` clause, with `Option Strict` on.</span></span>  
  
```  
' Not valid when Option Strict is on.  
' Dim increment1 = Function (n) n + 1  
```  
  
 <span data-ttu-id="cf1d0-104">A declaração anterior é válida se o tipo de `n` pode ser inferido.</span><span class="sxs-lookup"><span data-stu-id="cf1d0-104">The previous declaration is valid if the type of `n` can be inferred.</span></span> <span data-ttu-id="cf1d0-105">Por exemplo, se você está atribuindo a expressão lambda para um delegado de função `Del`:</span><span class="sxs-lookup"><span data-stu-id="cf1d0-105">For example, if you are assigning the previous lambda expression to a function delegate, `Del`:</span></span>  
  
```  
Delegate Function Del(ByVal p As Integer) As Integer  
```  
  
 <span data-ttu-id="cf1d0-106">Agora o tipo de `n` pode ser inferido de parâmetro `p`:</span><span class="sxs-lookup"><span data-stu-id="cf1d0-106">Now the type of `n` can be inferred from parameter `p`:</span></span>  
  
```  
Dim increment2 as Del = Function(n) n + 1  
```  
  
 <span data-ttu-id="cf1d0-107">**ID do erro:** BC36642</span><span class="sxs-lookup"><span data-stu-id="cf1d0-107">**Error ID:** BC36642</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="cf1d0-108">Para corrigir este erro</span><span class="sxs-lookup"><span data-stu-id="cf1d0-108">To correct this error</span></span>  
  
-   <span data-ttu-id="cf1d0-109">Adicionar um `As` cláusula à declaração de parâmetro:</span><span class="sxs-lookup"><span data-stu-id="cf1d0-109">Add an `As` clause to the parameter declaration:</span></span>  
  
    ```  
    Dim increment3 = Function (n As Integer) n + 1  
    ```  
  
## <a name="see-also"></a><span data-ttu-id="cf1d0-110">Consulte também</span><span class="sxs-lookup"><span data-stu-id="cf1d0-110">See Also</span></span>  
 [<span data-ttu-id="cf1d0-111">Expressões Lambda</span><span class="sxs-lookup"><span data-stu-id="cf1d0-111">Lambda Expressions</span></span>](../../visual-basic/programming-guide/language-features/procedures/lambda-expressions.md)
