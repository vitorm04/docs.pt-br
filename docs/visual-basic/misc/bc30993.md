---
title: A propriedade '<propertyname>' não pode ser inicializada em uma expressão de inicializador de objeto porque todas as sobrecargas acessíveis exigem argumentos
ms.date: 07/20/2015
f1_keywords:
- bc30993
- vbc30993
helpviewer_keywords:
- BC30993
ms.assetid: d4476065-2ca2-4c9e-a571-c08917a6387f
ms.openlocfilehash: 4c6cbbfc7218fc4a30a71a0acff7603edd024fbb
ms.sourcegitcommit: da2dd2772fcf32b44eb18b1cbe8affd17b1753c9
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/27/2019
ms.locfileid: "71352434"
---
# <a name="property-propertyname-cannot-be-initialized-in-an-object-initializer-expression-because-all-accessible-overloads-require-arguments"></a><span data-ttu-id="e0d40-102">A propriedade '\<PropertyName > ' não pode ser inicializada em uma expressão de inicializador de objeto porque todas as sobrecargas acessíveis exigem argumentos</span><span class="sxs-lookup"><span data-stu-id="e0d40-102">Property '\<propertyname>' cannot be initialized in an object initializer expression because all accessible overloads require arguments</span></span>
<span data-ttu-id="e0d40-103">Os membros inicializados em uma lista de inicializadores de objeto devem ser campos ou propriedades.</span><span class="sxs-lookup"><span data-stu-id="e0d40-103">The members initialized in an object initializer list must be either fields or properties.</span></span> <span data-ttu-id="e0d40-104">Além disso, as propriedades em uma lista de inicializadores não podem ter parâmetros.</span><span class="sxs-lookup"><span data-stu-id="e0d40-104">Additionally, properties in an initializer list cannot have parameters.</span></span> <span data-ttu-id="e0d40-105">A propriedade que está causando esse erro está sobrecarregada e cada uma de suas versões requer argumentos.</span><span class="sxs-lookup"><span data-stu-id="e0d40-105">The property causing this error is overloaded, and each of its versions requires arguments.</span></span> <span data-ttu-id="e0d40-106">Portanto, a propriedade não pode ser inicializada em uma lista de inicializadores de objeto.</span><span class="sxs-lookup"><span data-stu-id="e0d40-106">Therefore, the property cannot be initialized in an object initializer list.</span></span>  
  
 <span data-ttu-id="e0d40-107">**ID do erro:** BC30993</span><span class="sxs-lookup"><span data-stu-id="e0d40-107">**Error ID:** BC30993</span></span>  
  
## <a name="to-correct-this-error"></a><span data-ttu-id="e0d40-108">Para corrigir esse erro</span><span class="sxs-lookup"><span data-stu-id="e0d40-108">To correct this error</span></span>  
  
- <span data-ttu-id="e0d40-109">Remova a propriedade que requer argumentos da lista de inicializadores.</span><span class="sxs-lookup"><span data-stu-id="e0d40-109">Remove the property that requires arguments from the initializer list.</span></span>  
  
## <a name="example"></a><span data-ttu-id="e0d40-110">{1&gt;Exemplo&lt;1}</span><span class="sxs-lookup"><span data-stu-id="e0d40-110">Example</span></span>  
 <span data-ttu-id="e0d40-111">A classe a seguir contém três definições de propriedade: uma para `TotalItems` e duas para `Item`, que está sobrecarregado.</span><span class="sxs-lookup"><span data-stu-id="e0d40-111">The following class contains three property definitions: one for `TotalItems` and two for `Item`, which is overloaded.</span></span>  
  
```vb  
Class CollectionOfItems  
    Property TotalItems() As Integer  
        Get  
        End Get  
        Set(ByVal value As Integer)  
        End Set  
    End Property  
    Property Item(ByVal Key As String) As Object  
        Get  
        End Get  
        Set(ByVal value As Object)  
        End Set  
    End Property  
    Property Item(ByVal Index As Integer) As Object  
        Get  
        End Get  
        Set(ByVal value As Object)  
        End Set  
    End Property  
End Class  
```  
  
 <span data-ttu-id="e0d40-112">A propriedade `TotalItems` não requer argumentos e pode ser inicializada em uma lista de inicialização de objeto, conforme mostrado na declaração a seguir.</span><span class="sxs-lookup"><span data-stu-id="e0d40-112">The `TotalItems` property requires no arguments and can be initialized in an object initialization list, as shown in the following declaration.</span></span>  
  
```vb  
Dim coinCollection As New CollectionOfItems With { .TotalItems = 0 }  
```  
  
 <span data-ttu-id="e0d40-113">A propriedade `Item` está sobrecarregada e cada sobrecarga requer um argumento.</span><span class="sxs-lookup"><span data-stu-id="e0d40-113">The `Item` property is overloaded, and each overload requires an argument.</span></span> <span data-ttu-id="e0d40-114">Portanto, `Item` não pode aparecer em uma lista de inicializadores de objeto.</span><span class="sxs-lookup"><span data-stu-id="e0d40-114">Therefore, `Item` cannot appear in an object initializer list.</span></span>  
  
```vb  
' The following declaration is not valid.  
' Dim coinCollection As New CollectionOfItems With { .TotalItems = 0, _  
'    .Item = aCoinObject }  
```  
  
 <span data-ttu-id="e0d40-115">Para evitar esse erro, inicialize a propriedade `Item` fora do inicializador de objeto.</span><span class="sxs-lookup"><span data-stu-id="e0d40-115">To avoid this error, initialize the `Item` property outside the object initializer.</span></span>  
  
```vb  
Dim coinCollection As New CollectionOfItems With { .TotalItems = 0 }  
coinCollection.Item(1) = aCoinObject  
```  
  
## <a name="see-also"></a><span data-ttu-id="e0d40-116">Consulte também</span><span class="sxs-lookup"><span data-stu-id="e0d40-116">See also</span></span>

- [<span data-ttu-id="e0d40-117">Inicializadores de objeto: tipos nomeados e anônimos</span><span class="sxs-lookup"><span data-stu-id="e0d40-117">Object Initializers: Named and Anonymous Types</span></span>](../../visual-basic/programming-guide/language-features/objects-and-classes/object-initializers-named-and-anonymous-types.md)
- [<span data-ttu-id="e0d40-118">Como chamar um procedimento de propriedade</span><span class="sxs-lookup"><span data-stu-id="e0d40-118">How to: Call a Property Procedure</span></span>](../../visual-basic/programming-guide/language-features/procedures/how-to-call-a-property-procedure.md)

- [<span data-ttu-id="e0d40-119">Sobrecargas</span><span class="sxs-lookup"><span data-stu-id="e0d40-119">Overloads</span></span>](../../visual-basic/language-reference/modifiers/overloads.md)
- [<span data-ttu-id="e0d40-120">Sobrecarga de Procedimento</span><span class="sxs-lookup"><span data-stu-id="e0d40-120">Procedure Overloading</span></span>](../../visual-basic/programming-guide/language-features/procedures/procedure-overloading.md)
