---
title: Citações de código
description: Saiba mais F# sobre as cotações de código, um recurso de linguagem que permite gerar e trabalhar F# com expressões de código programaticamente.
ms.date: 05/16/2016
ms.openlocfilehash: c6ec0078c685a6452f49edd289b01491dd62e3db
ms.sourcegitcommit: f20dd18dbcf2275513281f5d9ad7ece6a62644b4
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/30/2019
ms.locfileid: "68630423"
---
# <a name="code-quotations"></a><span data-ttu-id="9103f-103">Citações de código</span><span class="sxs-lookup"><span data-stu-id="9103f-103">Code Quotations</span></span>

> [!NOTE]
> <span data-ttu-id="9103f-104">O link de referência da API levará você até o MSDN.</span><span class="sxs-lookup"><span data-stu-id="9103f-104">The API reference link will take you to MSDN.</span></span>  <span data-ttu-id="9103f-105">A referência da API docs.microsoft.com não está completa.</span><span class="sxs-lookup"><span data-stu-id="9103f-105">The docs.microsoft.com API reference is not complete.</span></span>

<span data-ttu-id="9103f-106">Este tópico descreve as *Cotações de código*, um recurso de linguagem que permite gerar e trabalhar com F# expressões de código programaticamente.</span><span class="sxs-lookup"><span data-stu-id="9103f-106">This topic describes *code quotations*, a language feature that enables you to generate and work with F# code expressions programmatically.</span></span> <span data-ttu-id="9103f-107">Esse recurso permite gerar uma árvore de sintaxe abstrata que representa F# o código.</span><span class="sxs-lookup"><span data-stu-id="9103f-107">This feature lets you generate an abstract syntax tree that represents F# code.</span></span> <span data-ttu-id="9103f-108">A árvore de sintaxe abstrata pode ser atravessada e processada de acordo com as necessidades do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="9103f-108">The abstract syntax tree can then be traversed and processed according to the needs of your application.</span></span> <span data-ttu-id="9103f-109">Por exemplo, você pode usar a árvore para gerar F# código ou gerar código em alguma outra linguagem.</span><span class="sxs-lookup"><span data-stu-id="9103f-109">For example, you can use the tree to generate F# code or generate code in some other language.</span></span>

## <a name="quoted-expressions"></a><span data-ttu-id="9103f-110">Expressões entre aspas</span><span class="sxs-lookup"><span data-stu-id="9103f-110">Quoted Expressions</span></span>

<span data-ttu-id="9103f-111">Uma *expressão entre aspas* é F# uma expressão em seu código delimitada de forma que ela não seja compilada como parte do seu programa, mas em vez disso, é compilada em um objeto que F# representa uma expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-111">A *quoted expression* is an F# expression in your code that is delimited in such a way that it is not compiled as part of your program, but instead is compiled into an object that represents an F# expression.</span></span> <span data-ttu-id="9103f-112">Você pode marcar uma expressão entre aspas de uma das duas maneiras: com informações de tipo ou sem informações de tipo.</span><span class="sxs-lookup"><span data-stu-id="9103f-112">You can mark a quoted expression in one of two ways: either with type information or without type information.</span></span> <span data-ttu-id="9103f-113">Se você quiser incluir informações de tipo, use os símbolos `<@` e `@>` para delimitar a expressão entre aspas.</span><span class="sxs-lookup"><span data-stu-id="9103f-113">If you want to include type information, you use the symbols `<@` and `@>` to delimit the quoted expression.</span></span> <span data-ttu-id="9103f-114">Se você não precisar de informações de tipo, use os símbolos `<@@` e `@@>`.</span><span class="sxs-lookup"><span data-stu-id="9103f-114">If you do not need type information, you use the symbols `<@@` and `@@>`.</span></span> <span data-ttu-id="9103f-115">O código a seguir mostra as cotações digitadas e não tipadas.</span><span class="sxs-lookup"><span data-stu-id="9103f-115">The following code shows typed and untyped quotations.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet501.fs)]

<span data-ttu-id="9103f-116">Percorrer uma árvore de expressão grande é mais rápido se você não incluir informações de tipo.</span><span class="sxs-lookup"><span data-stu-id="9103f-116">Traversing a large expression tree is faster if you do not include type information.</span></span> <span data-ttu-id="9103f-117">O tipo resultante de uma expressão citada com os símbolos tipados é `Expr<'T>`, em que o parâmetro de tipo tem o tipo da expressão conforme determinado pelo algoritmo de inferência de tipo do F# compilador.</span><span class="sxs-lookup"><span data-stu-id="9103f-117">The resulting type of an expression quoted with the typed symbols is `Expr<'T>`, where the type parameter has the type of the expression as determined by the F# compiler's type inference algorithm.</span></span> <span data-ttu-id="9103f-118">Quando você usa Cotações de código sem informações de tipo, o tipo da expressão entre aspas é a [expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65)de tipo não genérico.</span><span class="sxs-lookup"><span data-stu-id="9103f-118">When you use code quotations without type information, the type of the quoted expression is the non-generic type [Expr](https://msdn.microsoft.com/library/ed6a2caf-69d4-45c2-ab97-e9b3be9bce65).</span></span> <span data-ttu-id="9103f-119">Você pode chamar a propriedade [RAW](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) na classe tipada `Expr` para obter `Expr` o objeto não tipado.</span><span class="sxs-lookup"><span data-stu-id="9103f-119">You can call the [Raw](https://msdn.microsoft.com/library/47fb94f1-e77f-4c68-aabc-2b0ba40d59c2) property on the typed `Expr` class to obtain the untyped `Expr` object.</span></span>

<span data-ttu-id="9103f-120">Há uma variedade de métodos estáticos que permitem gerar F# objetos de expressão programaticamente na `Expr` classe sem usar expressões entre aspas.</span><span class="sxs-lookup"><span data-stu-id="9103f-120">There are a variety of static methods that allow you to generate F# expression objects programmatically in the `Expr` class without using quoted expressions.</span></span>

<span data-ttu-id="9103f-121">Observe que uma cotação de código deve incluir uma expressão completa.</span><span class="sxs-lookup"><span data-stu-id="9103f-121">Note that a code quotation must include a complete expression.</span></span> <span data-ttu-id="9103f-122">Para uma `let` associação, por exemplo, você precisa da definição do nome associado e de uma expressão adicional que usa a associação.</span><span class="sxs-lookup"><span data-stu-id="9103f-122">For a `let` binding, for example, you need both the definition of the bound name and an additional expression that uses the binding.</span></span> <span data-ttu-id="9103f-123">Na sintaxe detalhada, essa é uma expressão que segue a `in` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="9103f-123">In verbose syntax, this is an expression that follows the `in` keyword.</span></span> <span data-ttu-id="9103f-124">No nível superior de um módulo, essa é apenas a próxima expressão no módulo, mas em uma cotação, ela é explicitamente necessária.</span><span class="sxs-lookup"><span data-stu-id="9103f-124">At the top-level in a module, this is just the next expression in the module, but in a quotation, it is explicitly required.</span></span>

<span data-ttu-id="9103f-125">Portanto, a expressão a seguir não é válida.</span><span class="sxs-lookup"><span data-stu-id="9103f-125">Therefore, the following expression is not valid.</span></span>

```fsharp
// Not valid:
// <@ let f x = x + 1 @>
```

<span data-ttu-id="9103f-126">Mas as expressões a seguir são válidas.</span><span class="sxs-lookup"><span data-stu-id="9103f-126">But the following expressions are valid.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet502.fs)]

<span data-ttu-id="9103f-127">Para avaliar F# as cotações, você deve usar o [ F# avaliador de aspas](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span><span class="sxs-lookup"><span data-stu-id="9103f-127">To evaluate F# quotations, you must use the [F# Quotation Evaluator](https://github.com/fsprojects/FSharp.Quotations.Evaluator).</span></span> <span data-ttu-id="9103f-128">Ele fornece suporte para avaliar e executar F# objetos de expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-128">It provides support for evaluating and executing F# expression objects.</span></span>

## <a name="expr-type"></a><span data-ttu-id="9103f-129">Tipo de expr</span><span class="sxs-lookup"><span data-stu-id="9103f-129">Expr Type</span></span>

<span data-ttu-id="9103f-130">Uma instância do `Expr` tipo representa uma F# expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-130">An instance of the `Expr` type represents an F# expression.</span></span> <span data-ttu-id="9103f-131">Os tipos genérico e não genérico `Expr` são documentados na documentação da F# biblioteca.</span><span class="sxs-lookup"><span data-stu-id="9103f-131">Both the generic and the non-generic `Expr` types are documented in the F# library documentation.</span></span> <span data-ttu-id="9103f-132">Para obter mais informações, consulte a classe [Microsoft. FSharp. Requotas namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) e requotas [. expr](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="9103f-132">For more information, see [Microsoft.FSharp.Quotations Namespace](https://msdn.microsoft.com/visualfsharpdocs/conceptual/microsoft.fsharp.quotations-namespace-%5bfsharp%5d) and [Quotations.Expr Class](https://msdn.microsoft.com/visualfsharpdocs/conceptual/quotations.expr-class-%5bfsharp%5d).</span></span>

## <a name="splicing-operators"></a><span data-ttu-id="9103f-133">Operadores da União</span><span class="sxs-lookup"><span data-stu-id="9103f-133">Splicing Operators</span></span>

<span data-ttu-id="9103f-134">O da União permite combinar Cotações de código literais com expressões que você criou programaticamente ou de outra cotação de código.</span><span class="sxs-lookup"><span data-stu-id="9103f-134">Splicing enables you to combine literal code quotations with expressions that you have created programmatically or from another code quotation.</span></span> <span data-ttu-id="9103f-135">Os `%` operadores `%%` e permitem que você adicione um F# objeto de expressão a uma cotação de código.</span><span class="sxs-lookup"><span data-stu-id="9103f-135">The `%` and `%%` operators enable you to add an F# expression object into a code quotation.</span></span> <span data-ttu-id="9103f-136">Você usa o `%` operador para inserir um objeto de expressão com tipo em uma cotação tipada `%%` ; você usa o operador para inserir um objeto de expressão não tipado em uma cotação não tipada.</span><span class="sxs-lookup"><span data-stu-id="9103f-136">You use the `%` operator to insert a typed expression object into a typed quotation; you use the `%%` operator to insert an untyped expression object into an untyped quotation.</span></span> <span data-ttu-id="9103f-137">Ambos os operadores são operadores de prefixo unários.</span><span class="sxs-lookup"><span data-stu-id="9103f-137">Both operators are unary prefix operators.</span></span> <span data-ttu-id="9103f-138">Portanto, `expr` se for uma expressão não tipada do `Expr`tipo, o código a seguir será válido.</span><span class="sxs-lookup"><span data-stu-id="9103f-138">Thus if `expr` is an untyped expression of type `Expr`, the following code is valid.</span></span>

```fsharp
<@@ 1 + %%expr @@>
```

<span data-ttu-id="9103f-139">E se `expr` for uma citação tipada do `Expr<int>`tipo, o código a seguir será válido.</span><span class="sxs-lookup"><span data-stu-id="9103f-139">And if `expr` is a typed quotation of type `Expr<int>`, the following code is valid.</span></span>

```fsharp
<@ 1 + %expr @>
```

## <a name="example"></a><span data-ttu-id="9103f-140">Exemplo</span><span class="sxs-lookup"><span data-stu-id="9103f-140">Example</span></span>

### <a name="description"></a><span data-ttu-id="9103f-141">Descrição</span><span class="sxs-lookup"><span data-stu-id="9103f-141">Description</span></span>

<span data-ttu-id="9103f-142">O exemplo a seguir ilustra o uso de aspas de código para F# colocar o código em um objeto de expressão e F# , em seguida, imprimir o código que representa a expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-142">The following example illustrates the use of code quotations to put F# code into an expression object and then print the F# code that represents the expression.</span></span> <span data-ttu-id="9103f-143">Uma função `println` é definida que contém uma função `print` recursiva que exibe um F# objeto de expressão (do `Expr`tipo) em um formato amigável.</span><span class="sxs-lookup"><span data-stu-id="9103f-143">A function `println` is defined that contains a recursive function `print` that displays an F# expression object (of type `Expr`) in a friendly format.</span></span> <span data-ttu-id="9103f-144">Há vários padrões ativos nos módulos [Microsoft. FSharp. Requotas. Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) e [Microsoft. FSharp. requotars. DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) que podem ser usados para analisar objetos de expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-144">There are several active patterns in the [Microsoft.FSharp.Quotations.Patterns](https://msdn.microsoft.com/library/093944a9-c752-403a-8983-5fcd5dbf92a4) and [Microsoft.FSharp.Quotations.DerivedPatterns](https://msdn.microsoft.com/library/d2434a6e-ae7b-4f3d-b567-c162938bc9cd) modules that can be used to analyze expression objects.</span></span> <span data-ttu-id="9103f-145">Este exemplo não inclui todos os padrões possíveis que podem aparecer em uma F# expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-145">This example does not include all the possible patterns that might appear in an F# expression.</span></span> <span data-ttu-id="9103f-146">Qualquer padrão não reconhecido dispara uma correspondência ao padrão curinga (`_`) e é renderizado usando o `ToString` método `Expr` , que, no tipo, permite que você saiba o padrão ativo a ser adicionado à expressão de correspondência.</span><span class="sxs-lookup"><span data-stu-id="9103f-146">Any unrecognized pattern triggers a match to the wildcard pattern (`_`) and is rendered by using the `ToString` method, which, on the `Expr` type, lets you know the active pattern to add to your match expression.</span></span>

### <a name="code"></a><span data-ttu-id="9103f-147">Código</span><span class="sxs-lookup"><span data-stu-id="9103f-147">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet601.fs)]

### <a name="output"></a><span data-ttu-id="9103f-148">Saída</span><span class="sxs-lookup"><span data-stu-id="9103f-148">Output</span></span>

```fsharp
fun (x:System.Int32) -> x + 1
a + 1
let f = fun (x:System.Int32) -> x + 10 in f 10
```

## <a name="example"></a><span data-ttu-id="9103f-149">Exemplo</span><span class="sxs-lookup"><span data-stu-id="9103f-149">Example</span></span>

### <a name="description"></a><span data-ttu-id="9103f-150">Descrição</span><span class="sxs-lookup"><span data-stu-id="9103f-150">Description</span></span>

<span data-ttu-id="9103f-151">Você também pode usar os três padrões ativos no [módulo ExprShape](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) para atravessar árvores de expressão com menos padrões ativos.</span><span class="sxs-lookup"><span data-stu-id="9103f-151">You can also use the three active patterns in the [ExprShape module](https://msdn.microsoft.com/library/7685150e-2432-4d39-9338-57292eff18de) to traverse expression trees with fewer active patterns.</span></span> <span data-ttu-id="9103f-152">Esses padrões ativos podem ser úteis quando você deseja percorrer uma árvore, mas não precisa de todas as informações na maioria dos nós.</span><span class="sxs-lookup"><span data-stu-id="9103f-152">These active patterns can be useful when you want to traverse a tree but you do not need all the information in most of the nodes.</span></span> <span data-ttu-id="9103f-153">Quando você usa esses padrões, qualquer F# expressão corresponde a um dos três padrões a seguir `ShapeVar` : se a expressão for uma variável `ShapeLambda` , se a expressão for uma expressão lambda ou `ShapeCombination` se a expressão for outra coisa.</span><span class="sxs-lookup"><span data-stu-id="9103f-153">When you use these patterns, any F# expression matches one of the following three patterns: `ShapeVar` if the expression is a variable, `ShapeLambda` if the expression is a lambda expression, or `ShapeCombination` if the expression is anything else.</span></span> <span data-ttu-id="9103f-154">Se você percorrer uma árvore de expressão usando os padrões ativos como no exemplo de código anterior, precisará usar muitos outros padrões para lidar com todos os tipos F# de expressão possíveis e seu código será mais complexo.</span><span class="sxs-lookup"><span data-stu-id="9103f-154">If you traverse an expression tree by using the active patterns as in the previous code example, you have to use many more patterns to handle all possible F# expression types, and your code will be more complex.</span></span> <span data-ttu-id="9103f-155">Para obter mais informações, consulte [ExprShape.&#124;ShapeVar&#124;ShapeLambda ShapeCombination active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span><span class="sxs-lookup"><span data-stu-id="9103f-155">For more information, see [ExprShape.ShapeVar&#124;ShapeLambda&#124;ShapeCombination Active Pattern](https://msdn.microsoft.com/visualfsharpdocs/conceptual/exprshape.shapevarhshapelambdahshapecombination-active-pattern-%5bfsharp%5d).</span></span>

<span data-ttu-id="9103f-156">O exemplo de código a seguir pode ser usado como base para passagens mais complexas.</span><span class="sxs-lookup"><span data-stu-id="9103f-156">The following code example can be used as a basis for more complex traversals.</span></span> <span data-ttu-id="9103f-157">Nesse código, uma árvore de expressão é criada para uma expressão que envolve uma chamada de função `add`,.</span><span class="sxs-lookup"><span data-stu-id="9103f-157">In this code, an expression tree is created for an expression that involves a function call, `add`.</span></span> <span data-ttu-id="9103f-158">O padrão [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) ativo é usado para detectar qualquer chamada para `add` na árvore de expressão.</span><span class="sxs-lookup"><span data-stu-id="9103f-158">The [SpecificCall](https://msdn.microsoft.com/library/05a77b21-20fe-4b9a-8e07-aa999538198d) active pattern is used to detect any call to `add` in the expression tree.</span></span> <span data-ttu-id="9103f-159">Esse padrão ativo atribui os argumentos da chamada ao `exprList` valor.</span><span class="sxs-lookup"><span data-stu-id="9103f-159">This active pattern assigns the arguments of the call to the `exprList` value.</span></span> <span data-ttu-id="9103f-160">Nesse caso, há apenas dois, portanto, eles são retirados e a função é chamada recursivamente nos argumentos.</span><span class="sxs-lookup"><span data-stu-id="9103f-160">In this case, there are only two, so these are pulled out and the function is called recursively on the arguments.</span></span> <span data-ttu-id="9103f-161">Os resultados são inseridos em uma cotação de código que representa `mul` uma chamada para usando o operador de`%%`União ().</span><span class="sxs-lookup"><span data-stu-id="9103f-161">The results are inserted into a code quotation that represents a call to `mul` by using the splice operator (`%%`).</span></span> <span data-ttu-id="9103f-162">A `println` função do exemplo anterior é usada para exibir os resultados.</span><span class="sxs-lookup"><span data-stu-id="9103f-162">The `println` function from the previous example is used to display the results.</span></span>

<span data-ttu-id="9103f-163">O código em outras ramificações de padrão ativo apenas gera a mesma árvore de expressão, portanto, a única alteração na expressão resultante é a alteração de `add` para `mul`.</span><span class="sxs-lookup"><span data-stu-id="9103f-163">The code in the other active pattern branches just regenerates the same expression tree, so the only change in the resulting expression is the change from `add` to `mul`.</span></span>

### <a name="code"></a><span data-ttu-id="9103f-164">Código</span><span class="sxs-lookup"><span data-stu-id="9103f-164">Code</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-3/snippet701.fs)]

### <a name="output"></a><span data-ttu-id="9103f-165">Saída</span><span class="sxs-lookup"><span data-stu-id="9103f-165">Output</span></span>

```fsharp
1 + Module1.add(2,Module1.add(3,4))
1 + Module1.mul(2,Module1.mul(3,4))
```

## <a name="see-also"></a><span data-ttu-id="9103f-166">Consulte também</span><span class="sxs-lookup"><span data-stu-id="9103f-166">See also</span></span>

- [<span data-ttu-id="9103f-167">Referência da Linguagem F#</span><span class="sxs-lookup"><span data-stu-id="9103f-167">F# Language Reference</span></span>](index.md)
