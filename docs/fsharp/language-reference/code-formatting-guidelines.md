---
title: "Diretrizes de formatação de código (F#)"
description: "Saiba mais diretrizes de formatação de recuo de código para o F # linguagem para legibilidade, a estética, a padronização e a compilação de programação."
keywords: "visual f#, f#, programação funcional"
author: cartermp
ms.author: phcart
ms.date: 05/16/2016
ms.topic: language-reference
ms.prod: .net
ms.technology: devlang-fsharp
ms.devlang: fsharp
ms.assetid: 3f79717c-f84e-448d-9ce4-90e40a644ba1
ms.openlocfilehash: cc56c67f356b99defd8dc28770f87be1f58443c5
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2017
---
# <a name="code-formatting-guidelines"></a><span data-ttu-id="ef9f2-104">Diretrizes de formatação de código</span><span class="sxs-lookup"><span data-stu-id="ef9f2-104">Code Formatting Guidelines</span></span>

<span data-ttu-id="ef9f2-105">Este tópico resume as diretrizes de código de recuo para F #.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-105">This topic summarizes code indentation guidelines for F#.</span></span> <span data-ttu-id="ef9f2-106">Como a linguagem F # é sensível a quebras de linha e recuo, não é apenas uma questão de legibilidade, problema estético ou problema de padronização de codificação para formatar o código corretamente.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-106">Because the F# language is sensitive to line breaks and indentation, it is not just a readability issue, aesthetic issue, or coding standardization issue to format your code correctly.</span></span> <span data-ttu-id="ef9f2-107">Você deve formatar seu código corretamente compilar corretamente.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-107">You must format your code correctly for it to compile correctly.</span></span>


## <a name="general-rules-for-indentation"></a><span data-ttu-id="ef9f2-108">Regras gerais para de recuo</span><span class="sxs-lookup"><span data-stu-id="ef9f2-108">General Rules for Indentation</span></span>
<span data-ttu-id="ef9f2-109">Quando o recuo for necessário, você deve usar espaços, tabulações não.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-109">When indentation is required, you must use spaces, not tabs.</span></span> <span data-ttu-id="ef9f2-110">É necessário pelo menos um espaço.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-110">At least one space is required.</span></span> <span data-ttu-id="ef9f2-111">Sua organização pode criar padrões de codificação para especificar o número de espaços a serem usados para recuo; três ou quatro espaços de recuo em cada nível onde ocorre o recuo é comum.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-111">Your organization can create coding standards to specify the number of spaces to use for indentation; three or four spaces of indentation at each level where indentation occurs is typical.</span></span> <span data-ttu-id="ef9f2-112">Você pode configurar o Visual Studio para corresponder aos padrões de recuo de sua organização, alterando as opções de `Options` caixa de diálogo, que está disponível na `Tools` menu.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-112">You can configure Visual Studio to match your organization's indentation standards by changing the options in the `Options` dialog box, which is available from the `Tools` menu.</span></span> <span data-ttu-id="ef9f2-113">No `Text Editor` nó, expanda `F#` e, em seguida, clique em `Tabs`.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-113">In the `Text Editor` node, expand `F#` and then click `Tabs`.</span></span> <span data-ttu-id="ef9f2-114">Para obter uma descrição das opções disponíveis, consulte [opções, Editor de texto, todos os idiomas, guias](https://msdn.microsoft.com/library/7sffa753.aspx).</span><span class="sxs-lookup"><span data-stu-id="ef9f2-114">For a description of the available options, see [Options, Text Editor, All Languages, Tabs](https://msdn.microsoft.com/library/7sffa753.aspx).</span></span>

<span data-ttu-id="ef9f2-115">Em geral, quando o compilador analisa seu código, ele mantém uma pilha interna que indica o nível de aninhamento atual.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-115">In general, when the compiler parses your code, it maintains an internal stack that indicates the current level of nesting.</span></span> <span data-ttu-id="ef9f2-116">Quando o conteúdo é recuado, um novo nível de aninhamento é criado ou enviada por push para esta pilha interna.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-116">When code is indented, a new level of nesting is created, or pushed onto this internal stack.</span></span> <span data-ttu-id="ef9f2-117">Quando uma construção termina, o nível é exibido.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-117">When a construct ends, the level is popped.</span></span> <span data-ttu-id="ef9f2-118">Recuo é uma maneira para sinalizar o final de um nível e exibir a pilha interna, mas determinados símbolos também fazer com que o nível deve ser exibido, como o `end` palavra-chave, ou uma chave de fechamento ou parênteses.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-118">Indentation is one way to signal the end of a level and pop the internal stack, but certain tokens also cause the level to be popped, such as the `end` keyword, or a closing brace or parenthesis.</span></span>

<span data-ttu-id="ef9f2-119">Definição de função de código em uma construção de várias linhas, como uma definição de tipo `try...with` construção e construções de loop devem ser recuados em relação a linha de abertura da construção.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-119">Code in a multiline construct, such as a type definition, function definition, `try...with` construct, and looping constructs, must be indented relative to the opening line of the construct.</span></span> <span data-ttu-id="ef9f2-120">A primeira linha recuada estabelece uma posição de coluna para código subsequente na construção da mesma.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-120">The first indented line establishes a column position for subsequent code in the same construct.</span></span> <span data-ttu-id="ef9f2-121">O nível de recuo é chamado um *contexto*.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-121">The indentation level is called a *context*.</span></span> <span data-ttu-id="ef9f2-122">A posição da coluna define uma coluna mínimo, conhecida como um *linha fora*, para subsequentes linhas de código que estão no mesmo contexto.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-122">The column position sets a minimum column, referred to as an *offside line*, for subsequent lines of code that are in the same context.</span></span> <span data-ttu-id="ef9f2-123">Quando uma linha de código é encontrada é recuado menor que a posição de coluna estabelecida, o compilador assumirá que o contexto foi concluída e que você está agora codificando do próximo nível, o contexto anterior.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-123">When a line of code is encountered that is indented less than this established column position, the compiler assumes that the context has ended and that you are now coding at the next level up, in the previous context.</span></span> <span data-ttu-id="ef9f2-124">O termo *fora* é usado para descrever a condição em que uma linha de código dispara o final de uma construção porque ele não é recuado distante o suficiente.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-124">The term *offside* is used to describe the condition in which a line of code triggers the end of a construct because it is not indented far enough.</span></span> <span data-ttu-id="ef9f2-125">Em outras palavras, o código para a esquerda de uma linha fora está fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-125">In other words, code to the left of an offside line is offside.</span></span> <span data-ttu-id="ef9f2-126">No código recuado corretamente, você pode tirar proveito da regra de fora para delinear final das construções.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-126">In correctly indented code, you take advantage of the offside rule in order to delineate the end of constructs.</span></span> <span data-ttu-id="ef9f2-127">Se você usar o recuo incorretamente, uma condição fora pode fazer com que o compilador emite um aviso ou pode levar à interpretação incorreta do seu código.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-127">If you use indentation improperly, an offside condition can cause the compiler to issue a warning or can lead to the incorrect interpretation of your code.</span></span>

<span data-ttu-id="ef9f2-128">Linhas fora são determinadas da seguinte maneira.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-128">Offside lines are determined as follows.</span></span>


- <span data-ttu-id="ef9f2-129">Um `=` token associado a um `let` apresenta uma linha fora a coluna do primeiro token após o `=` sinal.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-129">An `=` token associated with a `let` introduces an offside line at the column of the first token after the `=` sign.</span></span>


- <span data-ttu-id="ef9f2-130">Em um `if...then...else` expressão, a posição da coluna do primeiro token após o `then` palavra-chave ou o `else` palavra-chave introduz uma linha fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-130">In an `if...then...else` expression, the column position of the first token after the `then` keyword or the `else` keyword introduces an offside line.</span></span>


- <span data-ttu-id="ef9f2-131">Em um `try...with` expressão, o primeiro token após `try` apresenta uma linha fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-131">In a `try...with` expression, the first token after `try` introduces an offside line.</span></span>


- <span data-ttu-id="ef9f2-132">Em um `match` expressão, o primeiro token após `with` e o primeiro token após cada `->` introduzir linhas fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-132">In a `match` expression, the first token after `with` and the first token after each `->` introduce offside lines.</span></span>


- <span data-ttu-id="ef9f2-133">O primeiro token após `with` em um tipo de extensão apresenta uma linha fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-133">The first token after `with` in a type extension introduces an offside line.</span></span>


- <span data-ttu-id="ef9f2-134">O primeiro token após uma chave de abertura ou parênteses, ou após o `begin` palavra-chave, apresenta uma linha fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-134">The first token after an opening brace or parenthesis, or after the `begin` keyword, introduces an offside line.</span></span>


- <span data-ttu-id="ef9f2-135">O primeiro caractere em palavras-chave `let`, `if`, e `module` introduzir linhas fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-135">The first character in the keywords `let`, `if`, and `module` introduce offside lines.</span></span>


<span data-ttu-id="ef9f2-136">Os exemplos de código a seguir ilustram as regras de recuo.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-136">The following code examples illustrate the indentation rules.</span></span> <span data-ttu-id="ef9f2-137">Aqui, as instruções de impressão dependem de recuo para associá-los com o contexto apropriado.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-137">Here, the print statements rely on indentation to associate them with the appropriate context.</span></span> <span data-ttu-id="ef9f2-138">Toda vez que o recuo deslocado, o contexto é exibido e retorna para o contexto anterior.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-138">Every time the indentation shifts, the context is popped and returns to the previous context.</span></span> <span data-ttu-id="ef9f2-139">Portanto, um espaço é impressa no final de cada iteração; "Concluído"!</span><span class="sxs-lookup"><span data-stu-id="ef9f2-139">Therefore, a space is printed at the end of each iteration; "Done!"</span></span> <span data-ttu-id="ef9f2-140">só é impressa uma vez porque o recuo fora estabelece que não é parte do loop.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-140">is only printed one time because the offside indentation establishes that it is not part of the loop.</span></span> <span data-ttu-id="ef9f2-141">A impressão de cadeia de caracteres "Contexto de nível superior" não faz parte da função.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-141">The printing of the string "Top-level context" is not part of the function.</span></span> <span data-ttu-id="ef9f2-142">Portanto, ele é impresso pela primeira vez, durante a inicialização estática, antes da função é chamada.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-142">Therefore, it is printed first, during the static initialization, before the function is called.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet1.fs)]

<span data-ttu-id="ef9f2-143">A saída é a seguinte.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-143">The output is as follows.</span></span>

```
Top-level context

(Negative number) Zero 1 2 3 Done!
```

<span data-ttu-id="ef9f2-144">Quando você quebrar linhas longas, a continuação da linha deve recuada além da construção de delimitador.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-144">When you break long lines, the continuation of the line must be indented farther than the enclosing construct.</span></span> <span data-ttu-id="ef9f2-145">Por exemplo, argumentos de função devem ser recuados além do primeiro caractere do nome da função, conforme mostrado no código a seguir.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-145">For example, function arguments must be indented farther than the first character of the function name, as shown in the following code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet2.fs)]

<span data-ttu-id="ef9f2-146">Há exceções a essas regras, conforme descrito na próxima seção.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-146">There are exceptions to these rules, as described in the next section.</span></span>


## <a name="indentation-in-modules"></a><span data-ttu-id="ef9f2-147">Recuo em módulos</span><span class="sxs-lookup"><span data-stu-id="ef9f2-147">Indentation in Modules</span></span>
<span data-ttu-id="ef9f2-148">Em um módulo local deverá ser recuado em relação ao módulo, mas o código em um módulo de nível superior não precisa ser recuado.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-148">Code in a local module must be indented relative to the module, but code in a top-level module does not have to be indented.</span></span> <span data-ttu-id="ef9f2-149">Elementos de Namespace não precisam ser recuado.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-149">Namespace elements do not have to be indented.</span></span>

<span data-ttu-id="ef9f2-150">Os exemplos de código a seguir ilustram isso.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-150">The following code examples illustrate this.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet3.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet4.fs)]

<span data-ttu-id="ef9f2-151">Para obter mais informações, consulte [módulos](modules.md).</span><span class="sxs-lookup"><span data-stu-id="ef9f2-151">For more information, see [Modules](modules.md).</span></span>


## <a name="exceptions-to-the-basic-indentation-rules"></a><span data-ttu-id="ef9f2-152">Exceções às regras básicas de recuo</span><span class="sxs-lookup"><span data-stu-id="ef9f2-152">Exceptions to the Basic Indentation Rules</span></span>
<span data-ttu-id="ef9f2-153">A regra geral, conforme descrito na seção anterior, é que as construções de várias linhas devem ser recuado em relação ao recuo da primeira linha da construção e que o fim da construção é determinado pelo quando ocorre a primeira linha de fora.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-153">The general rule, as described in the previous section, is that code in multiline constructs must be indented relative to the indentation of the first line of the construct, and that the end of the construct is determined by when the first offside line occurs.</span></span> <span data-ttu-id="ef9f2-154">Uma exceção à regra sobre quando a fim de contextos é que algumas construções, como o `try...with` expressão, o `if...then...else` expressão e o uso de `and` sintaxe para declarar mutuamente funções recursivas ou tipos, tem várias partes.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-154">An exception to the rule about when contexts end is that some constructs, such as the `try...with` expression, the `if...then...else` expression, and the use of `and` syntax for declaring mutually recursive functions or types, have multiple parts.</span></span> <span data-ttu-id="ef9f2-155">Recuar as últimas partes, como `then` e `else` em um `if...then...else` expressão, no mesmo nível como o token que inicia a expressão, mas em vez de indicar um final para o contexto, ele representa a próxima parte do mesmo contexto.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-155">You indent the later parts, such as `then` and `else` in an `if...then...else` expression, at the same level as the token that starts the expression, but instead of indicating an end to the context, it represents the next part of the same context.</span></span> <span data-ttu-id="ef9f2-156">Portanto, um `if...then...else` expressão pode ser gravada como no exemplo de código a seguir.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-156">Therefore, an `if...then...else` expression can be written as in the following code example.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet5.fs)]

<span data-ttu-id="ef9f2-157">A exceção à regra fora se aplicam somente ao `then` e `else` palavras-chave.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-157">The exception to the offside rule applies only to the `then` and `else` keywords.</span></span> <span data-ttu-id="ef9f2-158">Portanto, embora não seja um erro para recuar o `then` e `else` Além disso, com falha recuar as linhas de código em um `then` bloco produz um aviso.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-158">Therefore, although it is not an error to indent the `then` and `else` further, failing to indent the lines of code in a `then` block produces a warning.</span></span> <span data-ttu-id="ef9f2-159">Isso é ilustrado em linhas de código a seguir.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-159">This is illustrated in the following lines of code.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet6.fs)]
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet7.fs)]

<span data-ttu-id="ef9f2-160">Para o código em um `else` bloco, uma regra especial adicional se aplica.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-160">For code in an `else` block, an additional special rule applies.</span></span> <span data-ttu-id="ef9f2-161">O aviso no exemplo anterior somente ocorre no código no `then` blocos, não no código no `else` bloco.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-161">The warning in the previous example occurs only on the code in the `then` block, not on the code in the `else` block.</span></span> <span data-ttu-id="ef9f2-162">Isso permite que você escreva código que verifica se há várias condições no início de uma função sem forçar o restante do código para a função, que pode estar em um `else` bloco a ser recuado.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-162">This allows you to write code that checks for various conditions at the beginning of a function without forcing the rest of the code for the function, which might be in an `else` block, to be indented.</span></span> <span data-ttu-id="ef9f2-163">Assim, você pode escrever o seguinte sem gerar um aviso.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-163">Thus, you can write the following without producing a warning.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet8.fs)]

<span data-ttu-id="ef9f2-164">Outra exceção à regra de contextos terminam quando uma linha não ficará recuada quanto uma linha anterior é infixo operadores, como `+` e `|>`.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-164">Another exception to the rule that contexts end when a line is not indented as far as a previous line is for infix operators, such as `+` and `|>`.</span></span> <span data-ttu-id="ef9f2-165">Linhas que começam com operadores fixos são permitidas para começar a `(1 + oplength)` colunas antes que a posição normal sem disparar um final para o contexto onde `oplength` é o número de caracteres que compõem o operador.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-165">Lines that start with infix operators are permitted to begin `(1 + oplength)` columns before the normal position without triggering an end to the context, where `oplength` is the number of characters that make up the operator.</span></span> <span data-ttu-id="ef9f2-166">Isso faz com que o primeiro token depois do operador para alinhar com a linha anterior.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-166">This causes the first token after the operator to align with the previous line.</span></span>

<span data-ttu-id="ef9f2-167">Por exemplo, no código a seguir, o `+` símbolo tem permissão para ser recuadas duas colunas menor do que a linha anterior.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-167">For example, in the following code, the `+` symbol is permitted to be indented two columns less than the previous line.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet9.fs)]

<span data-ttu-id="ef9f2-168">Embora o recuo geralmente aumenta à medida que o nível de aninhamento se torna maior, há várias construções em que o compilador permite que você redefina o recuo para uma posição inferior da coluna.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-168">Although indentation usually increases as the level of nesting becomes higher, there are several constructs in which the compiler allows you to reset the indentation to a lower column position.</span></span>

<span data-ttu-id="ef9f2-169">As construções que permitem uma redefinição da posição de coluna são os seguintes:</span><span class="sxs-lookup"><span data-stu-id="ef9f2-169">The constructs that permit a reset of column position are as follows:</span></span>


- <span data-ttu-id="ef9f2-170">Corpos de funções anônimas.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-170">Bodies of anonymous functions.</span></span> <span data-ttu-id="ef9f2-171">No código a seguir, a expressão de impressão é iniciado em uma posição de coluna mais à esquerda que o `fun` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-171">In the following code, the print expression starts at a column position that is farther to the left than the `fun` keyword.</span></span> <span data-ttu-id="ef9f2-172">No entanto, a linha não deve começar em uma coluna à esquerda do início do nível de recuo anterior (ou seja, à esquerda do `L` em `List`).</span><span class="sxs-lookup"><span data-stu-id="ef9f2-172">However, the line must not start at a column to the left of the start of the previous indentation level (that is, to the left of the `L` in `List`).</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet10.fs)]

- <span data-ttu-id="ef9f2-173">Construções entre parênteses ou por `begin` e `end` em uma `then` ou `else` block de um `if...then...else` expressão, fornecido o recuo é não menos do que a posição de coluna a `if` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-173">Constructs enclosed by parentheses or by `begin` and `end` in a `then` or `else` block of an `if...then...else` expression, provided the indentation is no less than the column position of the `if` keyword.</span></span> <span data-ttu-id="ef9f2-174">Essa exceção permite que um estilo de codificação no qual um parêntese de abertura ou `begin` é usado no final de uma linha após `then` ou `else`.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-174">This exception allows for a coding style in which an opening parenthesis or `begin` is used at the end of a line after `then` or `else`.</span></span>


- <span data-ttu-id="ef9f2-175">Corpos de módulos, classes, interfaces e estruturas delimitadas por `begin...end`, `{...}`, `class...end`, ou `interface...end`.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-175">Bodies of modules, classes, interfaces, and structures delimited by `begin...end`, `{...}`, `class...end`, or `interface...end`.</span></span> <span data-ttu-id="ef9f2-176">Isso permite que um estilo no qual a palavra-chave de abertura de uma definição de tipo pode ser na mesma linha como o nome do tipo sem fazer com que todo o corpo a ser recuado além da palavra-chave de abertura.</span><span class="sxs-lookup"><span data-stu-id="ef9f2-176">This allows for a style in which the opening keyword of a type definition can be on the same line as the type name without forcing the whole body to be indented farther than the opening keyword.</span></span>
[!code-fsharp[Main](../../../samples/snippets/fsharp/code-formatting/snippet13.fs)]


## <a name="see-also"></a><span data-ttu-id="ef9f2-177">Consulte também</span><span class="sxs-lookup"><span data-stu-id="ef9f2-177">See Also</span></span>
[<span data-ttu-id="ef9f2-178">Referência da Linguagem F#</span><span class="sxs-lookup"><span data-stu-id="ef9f2-178">F# Language Reference</span></span>](index.md)
