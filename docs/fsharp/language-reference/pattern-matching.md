---
title: Correspondência padrão (F#)
description: 'Saiba como os padrões são usados em F # para comparar dados com estruturas lógicas, decompor dados em partes constituintes ou extrair informações de dados.'
ms.date: 05/16/2016
ms.openlocfilehash: 64f5b2534190552db71a67b30ece41bafed3d16e
ms.sourcegitcommit: 3d5d33f384eeba41b2dff79d096f47ccc8d8f03d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/04/2018
---
# <a name="pattern-matching"></a><span data-ttu-id="40e55-103">Correspondência padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-103">Pattern Matching</span></span>

<span data-ttu-id="40e55-104">Os padrões são regras de transformação de dados de entrada.</span><span class="sxs-lookup"><span data-stu-id="40e55-104">Patterns are rules for transforming input data.</span></span> <span data-ttu-id="40e55-105">Eles são usados em toda a linguagem F # para comparar dados com uma estrutura lógica ou estruturas, decompor dados em partes constituintes ou extrair informações de dados de várias maneiras.</span><span class="sxs-lookup"><span data-stu-id="40e55-105">They are used throughout the F# language to compare data with a logical structure or structures, decompose data into constituent parts, or extract information from data in various ways.</span></span>


## <a name="remarks"></a><span data-ttu-id="40e55-106">Comentários</span><span class="sxs-lookup"><span data-stu-id="40e55-106">Remarks</span></span>
<span data-ttu-id="40e55-107">Padrões são usados em muitas construções de linguagem, como o `match` expressão.</span><span class="sxs-lookup"><span data-stu-id="40e55-107">Patterns are used in many language constructs, such as the `match` expression.</span></span> <span data-ttu-id="40e55-108">Eles são usados quando você estiver processando os argumentos para funções na `let` associações, as expressões lambda e no manipulador de exceção associado a `try...with` expressão.</span><span class="sxs-lookup"><span data-stu-id="40e55-108">They are used when you are processing arguments for functions in `let` bindings, lambda expressions, and in the exception handlers associated with the `try...with` expression.</span></span> <span data-ttu-id="40e55-109">Para obter mais informações, consulte [correspondência expressões](match-expressions.md), [associações let](functions/let-bindings.md), [expressões Lambda: A `fun` palavra-chave](functions/lambda-expressions-the-fun-keyword.md), e [exceções: A `try...with` Expressão](exception-handling/the-try-with-expression.md).</span><span class="sxs-lookup"><span data-stu-id="40e55-109">For more information, see [Match Expressions](match-expressions.md), [let Bindings](functions/let-bindings.md), [Lambda Expressions: The `fun` Keyword](functions/lambda-expressions-the-fun-keyword.md), and [Exceptions: The `try...with` Expression](exception-handling/the-try-with-expression.md).</span></span>

<span data-ttu-id="40e55-110">Por exemplo, o `match` expressão, o *padrão* é o que segue o símbolo de pipe.</span><span class="sxs-lookup"><span data-stu-id="40e55-110">For example, in the `match` expression, the *pattern* is what follows the pipe symbol.</span></span>

```fsharp
match expression with
| pattern [ when condition ] -> result-expression
...
```

<span data-ttu-id="40e55-111">Cada padrão atua como uma regra para transformar a entrada de algum modo.</span><span class="sxs-lookup"><span data-stu-id="40e55-111">Each pattern acts as a rule for transforming input in some way.</span></span> <span data-ttu-id="40e55-112">No `match` expressão, cada padrão é examinado por sua vez, para ver se os dados de entrada serão compatíveis com o padrão.</span><span class="sxs-lookup"><span data-stu-id="40e55-112">In the `match` expression, each pattern is examined in turn to see if the input data is compatible with the pattern.</span></span> <span data-ttu-id="40e55-113">Se uma correspondência for encontrada, a expressão resultante é executada.</span><span class="sxs-lookup"><span data-stu-id="40e55-113">If a match is found, the result expression is executed.</span></span> <span data-ttu-id="40e55-114">Se uma correspondência não for encontrada, a próxima regra padrão será testada.</span><span class="sxs-lookup"><span data-stu-id="40e55-114">If a match is not found, the next pattern rule is tested.</span></span> <span data-ttu-id="40e55-115">Opcional quando *condição* parte é explicada em [correspondência expressões](match-expressions.md).</span><span class="sxs-lookup"><span data-stu-id="40e55-115">The optional when *condition* part is explained in [Match Expressions](match-expressions.md).</span></span>

<span data-ttu-id="40e55-116">Padrões suportados são mostrados na tabela a seguir.</span><span class="sxs-lookup"><span data-stu-id="40e55-116">Supported patterns are shown in the following table.</span></span> <span data-ttu-id="40e55-117">Em tempo de execução, a entrada é testada em relação a cada um dos seguintes padrões na ordem listada na tabela e padrões são aplicadas recursivamente, do primeiro ao último como aparecem no seu código e da esquerda para a direita para os padrões em cada linha.</span><span class="sxs-lookup"><span data-stu-id="40e55-117">At run time, the input is tested against each of the following patterns in the order listed in the table, and patterns are applied recursively, from first to last as they appear in your code, and from left to right for the patterns on each line.</span></span>

|<span data-ttu-id="40e55-118">Nome</span><span class="sxs-lookup"><span data-stu-id="40e55-118">Name</span></span>|<span data-ttu-id="40e55-119">Descrição</span><span class="sxs-lookup"><span data-stu-id="40e55-119">Description</span></span>|<span data-ttu-id="40e55-120">Exemplo</span><span class="sxs-lookup"><span data-stu-id="40e55-120">Example</span></span>|
|----|-----------|-------|
|<span data-ttu-id="40e55-121">Padrão de constante</span><span class="sxs-lookup"><span data-stu-id="40e55-121">Constant pattern</span></span>|<span data-ttu-id="40e55-122">Qualquer numéricos, caractere ou cadeia de caracteres literal, uma constante de enumeração ou um identificador de literal definido</span><span class="sxs-lookup"><span data-stu-id="40e55-122">Any numeric, character, or string literal, an enumeration constant, or a defined literal identifier</span></span>|<span data-ttu-id="40e55-123">`1.0`, `"test"`, `30`, `Color.Red`</span><span class="sxs-lookup"><span data-stu-id="40e55-123">`1.0`, `"test"`, `30`, `Color.Red`</span></span>|
|<span data-ttu-id="40e55-124">Padrão de identificador</span><span class="sxs-lookup"><span data-stu-id="40e55-124">Identifier pattern</span></span>|<span data-ttu-id="40e55-125">Um valor de caso de uma união discriminada, um rótulo de exceção ou uma ocorrência do padrão ativo</span><span class="sxs-lookup"><span data-stu-id="40e55-125">A case value of a discriminated union, an exception label, or an active pattern case</span></span>|`Some(x)`<br /><br />`Failure(msg)`|
|<span data-ttu-id="40e55-126">Padrão de variável</span><span class="sxs-lookup"><span data-stu-id="40e55-126">Variable pattern</span></span>|<span data-ttu-id="40e55-127">*identifier*</span><span class="sxs-lookup"><span data-stu-id="40e55-127">*identifier*</span></span>|`a`|
|<span data-ttu-id="40e55-128">`as` Padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-128">`as` pattern</span></span>|<span data-ttu-id="40e55-129">*padrão de* como *identificador*</span><span class="sxs-lookup"><span data-stu-id="40e55-129">*pattern* as *identifier*</span></span>|`(a, b) as tuple1`|
|<span data-ttu-id="40e55-130">OU padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-130">OR pattern</span></span>|<span data-ttu-id="40e55-131">*pattern1* &#124; *pattern2*</span><span class="sxs-lookup"><span data-stu-id="40e55-131">*pattern1* &#124; *pattern2*</span></span>|<code>([h] &#124; [h; _])</code>|
|<span data-ttu-id="40e55-132">E o padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-132">AND pattern</span></span>|<span data-ttu-id="40e55-133">*pattern1* &amp; *pattern2*</span><span class="sxs-lookup"><span data-stu-id="40e55-133">*pattern1* &amp; *pattern2*</span></span>|`(a, b) & (_, "test")`|
|<span data-ttu-id="40e55-134">Padrão Cons</span><span class="sxs-lookup"><span data-stu-id="40e55-134">Cons pattern</span></span>|<span data-ttu-id="40e55-135">*identificador* :: *identificador da lista*</span><span class="sxs-lookup"><span data-stu-id="40e55-135">*identifier* :: *list-identifier*</span></span>|`h :: t`|
|<span data-ttu-id="40e55-136">Padrão de lista</span><span class="sxs-lookup"><span data-stu-id="40e55-136">List pattern</span></span>|<span data-ttu-id="40e55-137">[ *pattern_1*;...; *pattern_n* ]</span><span class="sxs-lookup"><span data-stu-id="40e55-137">[ *pattern_1*; ... ; *pattern_n* ]</span></span>|`[ a; b; c ]`|
|<span data-ttu-id="40e55-138">Padrão de matriz</span><span class="sxs-lookup"><span data-stu-id="40e55-138">Array pattern</span></span>|<span data-ttu-id="40e55-139">[&#124; *pattern_1*;...; *pattern_n* &#124;]</span><span class="sxs-lookup"><span data-stu-id="40e55-139">[&#124; *pattern_1*; ..; *pattern_n* &#124;]</span></span>|<code>[&#124; a; b; c &#124;]</code>|
|<span data-ttu-id="40e55-140">Padrão entre parênteses</span><span class="sxs-lookup"><span data-stu-id="40e55-140">Parenthesized pattern</span></span>|<span data-ttu-id="40e55-141">( *padrão* )</span><span class="sxs-lookup"><span data-stu-id="40e55-141">( *pattern* )</span></span>|`( a )`|
|<span data-ttu-id="40e55-142">Padrão de tupla</span><span class="sxs-lookup"><span data-stu-id="40e55-142">Tuple pattern</span></span>|<span data-ttu-id="40e55-143">( *pattern_1*,..., *pattern_n* )</span><span class="sxs-lookup"><span data-stu-id="40e55-143">( *pattern_1*, ... , *pattern_n* )</span></span>|`( a, b )`|
|<span data-ttu-id="40e55-144">Padrão de registro</span><span class="sxs-lookup"><span data-stu-id="40e55-144">Record pattern</span></span>|<span data-ttu-id="40e55-145">{ *identifier1* = *pattern_1*;...; *identifier_n* = *pattern_n* }</span><span class="sxs-lookup"><span data-stu-id="40e55-145">{ *identifier1* = *pattern_1*; ... ; *identifier_n* = *pattern_n* }</span></span>|`{ Name = name; }`|
|<span data-ttu-id="40e55-146">Padrão de curinga</span><span class="sxs-lookup"><span data-stu-id="40e55-146">Wildcard pattern</span></span>|<span data-ttu-id="40e55-147">_</span><span class="sxs-lookup"><span data-stu-id="40e55-147">_</span></span>|`_`|
|<span data-ttu-id="40e55-148">Padrão junto com a anotação de tipo</span><span class="sxs-lookup"><span data-stu-id="40e55-148">Pattern together with type annotation</span></span>|<span data-ttu-id="40e55-149">*padrão de* : *tipo*</span><span class="sxs-lookup"><span data-stu-id="40e55-149">*pattern* : *type*</span></span>|`a : int`|
|<span data-ttu-id="40e55-150">Padrão de teste de tipo</span><span class="sxs-lookup"><span data-stu-id="40e55-150">Type test pattern</span></span>|<span data-ttu-id="40e55-151">:?</span><span class="sxs-lookup"><span data-stu-id="40e55-151">:?</span></span> <span data-ttu-id="40e55-152">*tipo* [como *identificador* ]</span><span class="sxs-lookup"><span data-stu-id="40e55-152">*type* [ as *identifier* ]</span></span>|`:? System.DateTime as dt`|
|<span data-ttu-id="40e55-153">Padrão nulo</span><span class="sxs-lookup"><span data-stu-id="40e55-153">Null pattern</span></span>|<span data-ttu-id="40e55-154">null</span><span class="sxs-lookup"><span data-stu-id="40e55-154">null</span></span>|`null`|

## <a name="constant-patterns"></a><span data-ttu-id="40e55-155">Padrões de constantes</span><span class="sxs-lookup"><span data-stu-id="40e55-155">Constant Patterns</span></span>
<span data-ttu-id="40e55-156">Constantes padrões são numéricos, caracteres e literais de cadeia de caracteres, constantes de enumeração (com o nome de tipo de enumeração incluído).</span><span class="sxs-lookup"><span data-stu-id="40e55-156">Constant patterns are numeric, character, and string literals, enumeration constants (with the enumeration type name included).</span></span> <span data-ttu-id="40e55-157">Um `match` expressão que contém apenas os padrões constantes pode ser comparado a uma instrução case em outros idiomas.</span><span class="sxs-lookup"><span data-stu-id="40e55-157">A `match` expression that has only constant patterns can be compared to a case statement in other languages.</span></span> <span data-ttu-id="40e55-158">A entrada é comparada com o valor literal e o padrão corresponde se os valores são iguais.</span><span class="sxs-lookup"><span data-stu-id="40e55-158">The input is compared with the literal value and the pattern matches if the values are equal.</span></span> <span data-ttu-id="40e55-159">O tipo do literal deve ser compatível com o tipo da entrada.</span><span class="sxs-lookup"><span data-stu-id="40e55-159">The type of the literal must be compatible with the type of the input.</span></span>

<span data-ttu-id="40e55-160">O exemplo a seguir demonstra o uso de padrões de literais e também usa um padrão de variável e um padrão de OR.</span><span class="sxs-lookup"><span data-stu-id="40e55-160">The following example demonstrates the use of literal patterns, and also uses a variable pattern and an OR pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4801.fs)]

<span data-ttu-id="40e55-161">Outro exemplo de um padrão de literal é um padrão com base em constantes de enumeração.</span><span class="sxs-lookup"><span data-stu-id="40e55-161">Another example of a literal pattern is a pattern based on enumeration constants.</span></span> <span data-ttu-id="40e55-162">Você deve especificar o nome do tipo de enumeração ao usar constantes de enumeração.</span><span class="sxs-lookup"><span data-stu-id="40e55-162">You must specify the enumeration type name when you use enumeration constants.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4802.fs)]

## <a name="identifier-patterns"></a><span data-ttu-id="40e55-163">Padrões de identificador</span><span class="sxs-lookup"><span data-stu-id="40e55-163">Identifier Patterns</span></span>
<span data-ttu-id="40e55-164">Se o padrão é uma cadeia de caracteres que constitui um identificador válido, o formato do identificador determina como o padrão é correspondido.</span><span class="sxs-lookup"><span data-stu-id="40e55-164">If the pattern is a string of characters that forms a valid identifier, the form of the identifier determines how the pattern is matched.</span></span> <span data-ttu-id="40e55-165">Se o identificador é maior do que um único caractere e começa com um caractere maiusculo, o compilador tenta fazer uma correspondência para o padrão de identificador.</span><span class="sxs-lookup"><span data-stu-id="40e55-165">If the identifier is longer than a single character and starts with an uppercase character, the compiler tries to make a match to the identifier pattern.</span></span> <span data-ttu-id="40e55-166">O identificador para esse padrão pode ser um valor marcado com o atributo Literal, um caso de união discriminado, um identificador de exceção ou um caso de padrão ativo.</span><span class="sxs-lookup"><span data-stu-id="40e55-166">The identifier for this pattern could be a value marked with the Literal attribute, a discriminated union case, an exception identifier, or an active pattern case.</span></span> <span data-ttu-id="40e55-167">Se nenhum identificador correspondente for encontrado, a correspondência falhar e a próxima regra padrão, o padrão de variável é comparada com a entrada.</span><span class="sxs-lookup"><span data-stu-id="40e55-167">If no matching identifier is found, the match fails and the next pattern rule, the variable pattern, is compared to the input.</span></span>

<span data-ttu-id="40e55-168">Padrões de união discriminadas podem ser simples denominado casos ou eles podem ter um valor ou uma tupla que contém vários valores.</span><span class="sxs-lookup"><span data-stu-id="40e55-168">Discriminated union patterns can be simple named cases or they can have a value, or a tuple containing multiple values.</span></span> <span data-ttu-id="40e55-169">Se houver um valor, você deve especificar um identificador para o valor.</span><span class="sxs-lookup"><span data-stu-id="40e55-169">If there is a value, you must specify an identifier for the value.</span></span> <span data-ttu-id="40e55-170">No caso de uma tupla, você deve fornecer um padrão de tupla com um identificador para cada elemento da tupla ou um identificador com um nome de campo para uma ou mais chamado campos union.</span><span class="sxs-lookup"><span data-stu-id="40e55-170">In the case of a tuple, you must supply a tuple pattern with an identifier for each element of the tuple or an identifier with a field name for one or more named union fields.</span></span> <span data-ttu-id="40e55-171">Consulte os exemplos de código nesta seção para obter exemplos.</span><span class="sxs-lookup"><span data-stu-id="40e55-171">See the code examples in this section for examples.</span></span>

<span data-ttu-id="40e55-172">O `option` tipo é uma união discriminada que tem dois casos, `Some` e `None`.</span><span class="sxs-lookup"><span data-stu-id="40e55-172">The `option` type is a discriminated union that has two cases, `Some` and `None`.</span></span> <span data-ttu-id="40e55-173">Um caso (`Some`) tem um valor, mas o outro (`None`) é apenas um caso nomeado.</span><span class="sxs-lookup"><span data-stu-id="40e55-173">One case (`Some`) has a value, but the other (`None`) is just a named case.</span></span> <span data-ttu-id="40e55-174">Portanto, `Some` deve ter uma variável para o valor associado com o `Some` caso, mas `None` deve aparecer sozinho.</span><span class="sxs-lookup"><span data-stu-id="40e55-174">Therefore, `Some` needs to have a variable for the value associated with the `Some` case, but `None` must appear by itself.</span></span> <span data-ttu-id="40e55-175">No código a seguir, a variável `var1` recebe o valor é obtido por meio da correspondência para o `Some` caso.</span><span class="sxs-lookup"><span data-stu-id="40e55-175">In the following code, the variable `var1` is given the value that is obtained by matching to the `Some` case.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4803.fs)]

<span data-ttu-id="40e55-176">No exemplo a seguir, o `PersonName` união discriminada contém uma mistura de cadeias de caracteres e caracteres que representam formas possíveis de nomes.</span><span class="sxs-lookup"><span data-stu-id="40e55-176">In the following example, the `PersonName` discriminated union contains a mixture of strings and characters that represent possible forms of names.</span></span> <span data-ttu-id="40e55-177">Os casos de união discriminada são `FirstOnly`, `LastOnly`, e `FirstLast`.</span><span class="sxs-lookup"><span data-stu-id="40e55-177">The cases of the discriminated union are `FirstOnly`, `LastOnly`, and `FirstLast`.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4804.fs)]

<span data-ttu-id="40e55-178">Para uniões discriminadas que têm campos nomeados, use o sinal de igual (=) para extrair o valor de um campo.</span><span class="sxs-lookup"><span data-stu-id="40e55-178">For discriminated unions that have named fields, you use the equals sign (=) to extract the value of a named field.</span></span> <span data-ttu-id="40e55-179">Por exemplo, considere uma união discriminada com uma declaração semelhante ao seguinte.</span><span class="sxs-lookup"><span data-stu-id="40e55-179">For example, consider a discriminated union with a declaration like the following.</span></span>

```fsharp
type Shape =
    | Rectangle of height : float * width : float
    | Circle of radius : float
```

<span data-ttu-id="40e55-180">Você pode usar os campos nomeados em uma expressão de correspondência de padrão da seguinte maneira.</span><span class="sxs-lookup"><span data-stu-id="40e55-180">You can use the named fields in a pattern matching expression as follows.</span></span>

```fsharp
let matchShape shape =
    match shape with
    | Rectangle(height = h) -> printfn "Rectangle with length %f" h
    | Circle(r) -> printfn "Circle with radius %f" r
```

<span data-ttu-id="40e55-181">O uso do campo nomeado é opcional, portanto, no exemplo anterior, ambos `Circle(r)` e `Circle(radius = r)` têm o mesmo efeito.</span><span class="sxs-lookup"><span data-stu-id="40e55-181">The use of the named field is optional, so in the previous example, both `Circle(r)` and `Circle(radius = r)` have the same effect.</span></span>

<span data-ttu-id="40e55-182">Quando você especificar vários campos, use o ponto e vírgula (;) como separador.</span><span class="sxs-lookup"><span data-stu-id="40e55-182">When you specify multiple fields, use the semicolon (;) as a separator.</span></span>

```
match shape with
| Rectangle(height = h; width = w) -> printfn "Rectangle with height %f and width %f" h w
| _ -> ()
```

<span data-ttu-id="40e55-183">Padrões ativos permitem definir a correspondência de padrão personalizado mais complexas.</span><span class="sxs-lookup"><span data-stu-id="40e55-183">Active patterns enable you to define more complex custom pattern matching.</span></span> <span data-ttu-id="40e55-184">Para obter mais informações sobre padrões ativos, consulte [padrões ativos](active-patterns.md).</span><span class="sxs-lookup"><span data-stu-id="40e55-184">For more information about active patterns, see [Active Patterns](active-patterns.md).</span></span>

<span data-ttu-id="40e55-185">O caso em que o identificador é uma exceção é usado na correspondência de padrão de contexto de manipuladores de exceção.</span><span class="sxs-lookup"><span data-stu-id="40e55-185">The case in which the identifier is an exception is used in pattern matching in the context of exception handlers.</span></span> <span data-ttu-id="40e55-186">Para obter informações sobre correspondência de padrão de manipulação de exceção, consulte [exceções: A `try...with` expressão](exception-handling/the-try-with-expression.md).</span><span class="sxs-lookup"><span data-stu-id="40e55-186">For information about pattern matching in exception handling, see [Exceptions: The `try...with` Expression](exception-handling/the-try-with-expression.md).</span></span>


## <a name="variable-patterns"></a><span data-ttu-id="40e55-187">Padrões de variáveis</span><span class="sxs-lookup"><span data-stu-id="40e55-187">Variable Patterns</span></span>
<span data-ttu-id="40e55-188">O padrão de variável atribui o valor sendo correspondido a um nome de variável, que estará disponível para uso na expressão à direita da execução de `->` símbolo.</span><span class="sxs-lookup"><span data-stu-id="40e55-188">The variable pattern assigns the value being matched to a variable name, which is then available for use in the execution expression to the right of the `->` symbol.</span></span> <span data-ttu-id="40e55-189">Um padrão de variável sozinho corresponde qualquer entrada, mas padrões variáveis geralmente aparecem em outros padrões, portanto Permitindo estruturas mais complexas, como tuplas e matrizes para ser decomposto em variáveis.</span><span class="sxs-lookup"><span data-stu-id="40e55-189">A variable pattern alone matches any input, but variable patterns often appear within other patterns, therefore enabling more complex structures such as tuples and arrays to be decomposed into variables.</span></span>

<span data-ttu-id="40e55-190">O exemplo a seguir demonstra um padrão de variável dentro de um padrão de tupla.</span><span class="sxs-lookup"><span data-stu-id="40e55-190">The following example demonstrates a variable pattern within a tuple pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4805.fs)]

## <a name="as-pattern"></a><span data-ttu-id="40e55-191">como padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-191">as Pattern</span></span>
<span data-ttu-id="40e55-192">O `as` padrão é um padrão que tem um `as` cláusula anexada a ele.</span><span class="sxs-lookup"><span data-stu-id="40e55-192">The `as` pattern is a pattern that has an `as` clause appended to it.</span></span> <span data-ttu-id="40e55-193">O `as` cláusula associa o valor correspondente a um nome que pode ser usado na expressão de execução de um `match` expressão, ou, no caso em que esse padrão é usado em um `let` de associação, o nome é adicionado como uma ligação com o escopo local.</span><span class="sxs-lookup"><span data-stu-id="40e55-193">The `as` clause binds the matched value to a name that can be used in the execution expression of a `match` expression, or, in the case where this pattern is used in a `let` binding, the name is added as a binding to the local scope.</span></span>

<span data-ttu-id="40e55-194">O exemplo a seguir usa um `as` padrão.</span><span class="sxs-lookup"><span data-stu-id="40e55-194">The following example uses an `as` pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4806.fs)]

## <a name="or-pattern"></a><span data-ttu-id="40e55-195">OU padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-195">OR Pattern</span></span>
<span data-ttu-id="40e55-196">O padrão ou é usado quando os dados de entrada podem corresponder a vários padrões, e você deseja executar o mesmo código como resultado.</span><span class="sxs-lookup"><span data-stu-id="40e55-196">The OR pattern is used when input data can match multiple patterns, and you want to execute the same code as a result.</span></span> <span data-ttu-id="40e55-197">Os tipos de ambos os lados do padrão ou devem ser compatíveis.</span><span class="sxs-lookup"><span data-stu-id="40e55-197">The types of both sides of the OR pattern must be compatible.</span></span>

<span data-ttu-id="40e55-198">O exemplo a seguir demonstra o padrão de OR.</span><span class="sxs-lookup"><span data-stu-id="40e55-198">The following example demonstrates the OR pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4807.fs)]

## <a name="and-pattern"></a><span data-ttu-id="40e55-199">E o padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-199">AND Pattern</span></span>
<span data-ttu-id="40e55-200">O padrão e requer que a entrada corresponder dois padrões.</span><span class="sxs-lookup"><span data-stu-id="40e55-200">The AND pattern requires that the input match two patterns.</span></span> <span data-ttu-id="40e55-201">Os tipos de ambos os lados do padrão e devem ser compatíveis.</span><span class="sxs-lookup"><span data-stu-id="40e55-201">The types of both sides of the AND pattern must be compatible.</span></span>

<span data-ttu-id="40e55-202">O exemplo a seguir é semelhante `detectZeroTuple` mostra o [padrão de tupla](https://msdn.microsoft.com/library/#tuple) seção mais adiante neste tópico, mas aqui ambos `var1` e `var2` são obtidos como valores usando o padrão e.</span><span class="sxs-lookup"><span data-stu-id="40e55-202">The following example is like `detectZeroTuple` shown in the [Tuple Pattern](https://msdn.microsoft.com/library/#tuple) section later in this topic, but here both `var1` and `var2` are obtained as values by using the AND pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4808.fs)]

## <a name="cons-pattern"></a><span data-ttu-id="40e55-203">Contras padrão</span><span class="sxs-lookup"><span data-stu-id="40e55-203">Cons Pattern</span></span>
<span data-ttu-id="40e55-204">O padrão de contras é usado para decompor uma lista para o primeiro elemento, o *head*e uma lista que contém os elementos restantes, o *final*.</span><span class="sxs-lookup"><span data-stu-id="40e55-204">The cons pattern is used to decompose a list into the first element, the *head*, and a list that contains the remaining elements, the *tail*.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4809.fs)]

## <a name="list-pattern"></a><span data-ttu-id="40e55-205">Padrão de lista</span><span class="sxs-lookup"><span data-stu-id="40e55-205">List Pattern</span></span>
<span data-ttu-id="40e55-206">O padrão de lista permite que a lista ser decomposto em um número de elementos.</span><span class="sxs-lookup"><span data-stu-id="40e55-206">The list pattern enables lists to be decomposed into a number of elements.</span></span> <span data-ttu-id="40e55-207">O padrão de lista em si pode corresponder a apenas uma lista de um número específico de elementos.</span><span class="sxs-lookup"><span data-stu-id="40e55-207">The list pattern itself can match only lists of a specific number of elements.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4810.fs)]

## <a name="array-pattern"></a><span data-ttu-id="40e55-208">Padrão de matriz</span><span class="sxs-lookup"><span data-stu-id="40e55-208">Array Pattern</span></span>
<span data-ttu-id="40e55-209">O padrão de matriz é semelhante ao padrão de lista e pode ser usado para decompor as matrizes de um período específico.</span><span class="sxs-lookup"><span data-stu-id="40e55-209">The array pattern resembles the list pattern and can be used to decompose arrays of a specific length.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4811.fs)]

## <a name="parenthesized-pattern"></a><span data-ttu-id="40e55-210">Padrão entre parênteses</span><span class="sxs-lookup"><span data-stu-id="40e55-210">Parenthesized Pattern</span></span>
<span data-ttu-id="40e55-211">Parênteses podem ser agrupados em torno de padrões para alcançar a capacidade de associação desejada.</span><span class="sxs-lookup"><span data-stu-id="40e55-211">Parentheses can be grouped around patterns to achieve the desired associativity.</span></span> <span data-ttu-id="40e55-212">No exemplo a seguir, os parênteses são usados para controlar a associação entre um padrão AND e um padrão de contras.</span><span class="sxs-lookup"><span data-stu-id="40e55-212">In the following example, parentheses are used to control associativity between an AND pattern and a cons pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4812.fs)]

## <a name="tuple-pattern"></a><span data-ttu-id="40e55-213">Padrão de tupla</span><span class="sxs-lookup"><span data-stu-id="40e55-213">Tuple Pattern</span></span>
<span data-ttu-id="40e55-214">O padrão de tupla corresponde a entrada na forma de tupla e permite que a tupla a ser decomposto em seus elementos constituintes usando variáveis para cada posição na tupla de correspondência de padrões.</span><span class="sxs-lookup"><span data-stu-id="40e55-214">The tuple pattern matches input in tuple form and enables the tuple to be decomposed into its constituent elements by using pattern matching variables for each position in the tuple.</span></span>

<span data-ttu-id="40e55-215">O exemplo a seguir demonstra o padrão de tupla e também usa padrões literal, padrões de variáveis e o padrão de curinga.</span><span class="sxs-lookup"><span data-stu-id="40e55-215">The following example demonstrates the tuple pattern and also uses literal patterns, variable patterns, and the wildcard pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4813.fs)]

## <a name="record-pattern"></a><span data-ttu-id="40e55-216">Padrão de registro</span><span class="sxs-lookup"><span data-stu-id="40e55-216">Record Pattern</span></span>
<span data-ttu-id="40e55-217">O padrão de registro é usado para decompor registros para extrair os valores dos campos.</span><span class="sxs-lookup"><span data-stu-id="40e55-217">The record pattern is used to decompose records to extract the values of fields.</span></span> <span data-ttu-id="40e55-218">O padrão não precisa fazer referência a todos os campos do registro. todos os campos omitidos apenas não participam de correspondência e não são extraídos.</span><span class="sxs-lookup"><span data-stu-id="40e55-218">The pattern does not have to reference all fields of the record; any omitted fields just do not participate in matching and are not extracted.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4814.fs)]

## <a name="wildcard-pattern"></a><span data-ttu-id="40e55-219">Padrão de curinga</span><span class="sxs-lookup"><span data-stu-id="40e55-219">Wildcard Pattern</span></span>
<span data-ttu-id="40e55-220">O padrão de curinga é representado por um sublinhado (`_`) de caracteres e corresponde qualquer entrada, assim como o padrão de variável, exceto que a entrada é descartada, em vez de atribuído a uma variável.</span><span class="sxs-lookup"><span data-stu-id="40e55-220">The wildcard pattern is represented by the underscore (`_`) character and matches any input, just like the variable pattern, except that the input is discarded instead of assigned to a variable.</span></span> <span data-ttu-id="40e55-221">O padrão de curinga é geralmente usado em outros padrões como um espaço reservado para valores que não são necessários na expressão à direita do `->` símbolo.</span><span class="sxs-lookup"><span data-stu-id="40e55-221">The wildcard pattern is often used within other patterns as a placeholder for values that are not needed in the expression to the right of the `->` symbol.</span></span> <span data-ttu-id="40e55-222">O padrão de curinga é usado frequentemente no final de uma lista de padrões para corresponder a nenhuma entrada não correspondente.</span><span class="sxs-lookup"><span data-stu-id="40e55-222">The wildcard pattern is also frequently used at the end of a list of patterns to match any unmatched input.</span></span> <span data-ttu-id="40e55-223">O padrão de curinga é demonstrado em muitos exemplos de código neste tópico.</span><span class="sxs-lookup"><span data-stu-id="40e55-223">The wildcard pattern is demonstrated in many code examples in this topic.</span></span> <span data-ttu-id="40e55-224">Consulte o código anterior para obter um exemplo.</span><span class="sxs-lookup"><span data-stu-id="40e55-224">See the preceding code for one example.</span></span>


## <a name="patterns-that-have-type-annotations"></a><span data-ttu-id="40e55-225">Padrões com anotações de tipo</span><span class="sxs-lookup"><span data-stu-id="40e55-225">Patterns That Have Type Annotations</span></span>
<span data-ttu-id="40e55-226">Os padrões podem ter anotações de tipo.</span><span class="sxs-lookup"><span data-stu-id="40e55-226">Patterns can have type annotations.</span></span> <span data-ttu-id="40e55-227">Eles se comportam como outras anotações de tipo e guia inferência como outras anotações de tipo.</span><span class="sxs-lookup"><span data-stu-id="40e55-227">These behave like other type annotations and guide inference like other type annotations.</span></span> <span data-ttu-id="40e55-228">São necessários parênteses em torno de anotações de tipo em padrões.</span><span class="sxs-lookup"><span data-stu-id="40e55-228">Parentheses are required around type annotations in patterns.</span></span> <span data-ttu-id="40e55-229">O código a seguir mostra um padrão que tem uma anotação de tipo.</span><span class="sxs-lookup"><span data-stu-id="40e55-229">The following code shows a pattern that has a type annotation.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4815.fs)]

## <a name="type-test-pattern"></a><span data-ttu-id="40e55-230">Padrão de teste de tipo</span><span class="sxs-lookup"><span data-stu-id="40e55-230">Type Test Pattern</span></span>
<span data-ttu-id="40e55-231">O padrão de teste de tipo é usado para corresponder à entrada em relação a um tipo.</span><span class="sxs-lookup"><span data-stu-id="40e55-231">The type test pattern is used to match the input against a type.</span></span> <span data-ttu-id="40e55-232">Se o tipo de entrada é uma correspondência (ou um tipo derivado de) o tipo especificado no padrão, a correspondência for bem-sucedida.</span><span class="sxs-lookup"><span data-stu-id="40e55-232">If the input type is a match to (or a derived type of) the type specified in the pattern, the match succeeds.</span></span>

<span data-ttu-id="40e55-233">O exemplo a seguir demonstra o padrão de teste de tipo.</span><span class="sxs-lookup"><span data-stu-id="40e55-233">The following example demonstrates the type test pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4816.fs)]

## <a name="null-pattern"></a><span data-ttu-id="40e55-234">Padrão nulo</span><span class="sxs-lookup"><span data-stu-id="40e55-234">Null Pattern</span></span>
<span data-ttu-id="40e55-235">O padrão de nulo corresponde o valor nulo que pode aparecer quando você estiver trabalhando com tipos que permitem valor nulo.</span><span class="sxs-lookup"><span data-stu-id="40e55-235">The null pattern matches the null value that can appear when you are working with types that allow a null value.</span></span> <span data-ttu-id="40e55-236">Nulo padrões são usados ao interagir com código do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="40e55-236">Null patterns are frequently used when interoperating with .NET Framework code.</span></span> <span data-ttu-id="40e55-237">Por exemplo, o valor de retorno de uma API .NET pode ser a entrada para um `match` expressão.</span><span class="sxs-lookup"><span data-stu-id="40e55-237">For example, the return value of a .NET API might be the input to a `match` expression.</span></span> <span data-ttu-id="40e55-238">Você pode controlar o fluxo de programa com base em se o valor de retorno é nulo e também em outras características do valor retornado.</span><span class="sxs-lookup"><span data-stu-id="40e55-238">You can control program flow based on whether the return value is null, and also on other characteristics of the returned value.</span></span> <span data-ttu-id="40e55-239">Você pode usar o padrão de null para impedir que valores nulos sejam propagadas para o restante do seu programa.</span><span class="sxs-lookup"><span data-stu-id="40e55-239">You can use the null pattern to prevent null values from propagating to the rest of your program.</span></span>

<span data-ttu-id="40e55-240">O exemplo a seguir usa o padrão de null e o padrão de variável.</span><span class="sxs-lookup"><span data-stu-id="40e55-240">The following example uses the null pattern and the variable pattern.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-2/snippet4817.fs)]

## <a name="see-also"></a><span data-ttu-id="40e55-241">Consulte também</span><span class="sxs-lookup"><span data-stu-id="40e55-241">See Also</span></span>
[<span data-ttu-id="40e55-242">Expressões Match</span><span class="sxs-lookup"><span data-stu-id="40e55-242">Match Expressions</span></span>](match-expressions.md)

[<span data-ttu-id="40e55-243">Padrões Ativos</span><span class="sxs-lookup"><span data-stu-id="40e55-243">Active Patterns</span></span>](active-patterns.md)

[<span data-ttu-id="40e55-244">Referência da Linguagem F#</span><span class="sxs-lookup"><span data-stu-id="40e55-244">F# Language Reference</span></span>](index.md)
