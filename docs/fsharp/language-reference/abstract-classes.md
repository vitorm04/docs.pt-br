---
title: Classes abstratas
description: Saiba mais sobre F# classes abstratas, que deixar alguns ou todos os membros não implementados e representam a funcionalidade comum de um conjunto diversificado de tipos de objeto.
ms.date: 05/16/2016
ms.openlocfilehash: fecd3b2d550c6b8f59fa614f5d00c5f730a4896a
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61772769"
---
# <a name="abstract-classes"></a><span data-ttu-id="776a6-103">Classes abstratas</span><span class="sxs-lookup"><span data-stu-id="776a6-103">Abstract Classes</span></span>

<span data-ttu-id="776a6-104">*Classes abstratas* são classes que deixam alguns ou todos os membros não implementados, para que as implementações podem ser fornecidas por classes derivadas.</span><span class="sxs-lookup"><span data-stu-id="776a6-104">*Abstract classes* are classes that leave some or all members unimplemented, so that implementations can be provided by derived classes.</span></span>

## <a name="syntax"></a><span data-ttu-id="776a6-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="776a6-105">Syntax</span></span>

```fsharp
// Abstract class syntax.
[<AbstractClass>]
type [ accessibility-modifier ] abstract-class-name =
[ inherit base-class-or-interface-name ]
[ abstract-member-declarations-and-member-definitions ]

// Abstract member syntax.
abstract member member-name : type-signature
```

## <a name="remarks"></a><span data-ttu-id="776a6-106">Comentários</span><span class="sxs-lookup"><span data-stu-id="776a6-106">Remarks</span></span>

<span data-ttu-id="776a6-107">Na programação orientada a objeto, uma classe abstrata é usada como uma classe base de uma hierarquia e representa a funcionalidade comum de um conjunto diversificado de tipos de objeto.</span><span class="sxs-lookup"><span data-stu-id="776a6-107">In object-oriented programming, an abstract class is used as a base class of a hierarchy, and represents common functionality of a diverse set of object types.</span></span> <span data-ttu-id="776a6-108">Como o "abstrato" nome implica, classes abstratas geralmente não correspondem diretamente em entidades concretas no domínio do problema.</span><span class="sxs-lookup"><span data-stu-id="776a6-108">As the name "abstract" implies, abstract classes often do not correspond directly onto concrete entities in the problem domain.</span></span> <span data-ttu-id="776a6-109">No entanto, elas representam o que muitas entidades concretas diferentes têm em comum.</span><span class="sxs-lookup"><span data-stu-id="776a6-109">However, they do represent what many different concrete entities have in common.</span></span>

<span data-ttu-id="776a6-110">Classes abstratas devem ter o `AbstractClass` atributo.</span><span class="sxs-lookup"><span data-stu-id="776a6-110">Abstract classes must have the `AbstractClass` attribute.</span></span> <span data-ttu-id="776a6-111">Eles podem ter implementado e não implementados membros.</span><span class="sxs-lookup"><span data-stu-id="776a6-111">They can have implemented and unimplemented members.</span></span> <span data-ttu-id="776a6-112">O uso do termo *abstrata* quando aplicado a uma classe é o mesmo como em outras linguagens .NET; no entanto, o uso do termo *abstrata* quando aplicado a (propriedades e métodos) é um pouco diferente em F# do seu uso em outras linguagens .NET.</span><span class="sxs-lookup"><span data-stu-id="776a6-112">The use of the term *abstract* when applied to a class is the same as in other .NET languages; however, the use of the term *abstract* when applied to methods (and properties) is a little different in F# from its use in other .NET languages.</span></span> <span data-ttu-id="776a6-113">No F#, quando um método é marcado com o `abstract` palavra-chave, isso indica que um membro tem uma entrada, conhecida como um *slot de expedição virtual*, na tabela de funções virtuais para esse tipo interna.</span><span class="sxs-lookup"><span data-stu-id="776a6-113">In F#, when a method is marked with the `abstract` keyword, this indicates that a member has an entry, known as a *virtual dispatch slot*, in the internal table of virtual functions for that type.</span></span> <span data-ttu-id="776a6-114">Em outras palavras, o método é virtual, embora a `virtual` palavra-chave não é usado o F# idioma.</span><span class="sxs-lookup"><span data-stu-id="776a6-114">In other words, the method is virtual, although the `virtual` keyword is not used in the F# language.</span></span> <span data-ttu-id="776a6-115">A palavra-chave `abstract` é usado em métodos virtuais, independentemente se o método é implementado.</span><span class="sxs-lookup"><span data-stu-id="776a6-115">The keyword `abstract` is used on virtual methods regardless of whether the method is implemented.</span></span> <span data-ttu-id="776a6-116">A declaração de um slot de expedição virtual é separada da definição de um método para esse slot de expedição.</span><span class="sxs-lookup"><span data-stu-id="776a6-116">The declaration of a virtual dispatch slot is separate from the definition of a method for that dispatch slot.</span></span> <span data-ttu-id="776a6-117">Portanto, o F# equivalente a uma declaração de método virtual e uma definição em outra linguagem .NET é uma combinação de uma declaração de método abstrato e uma definição separada, com qualquer um de `default` palavra-chave ou o `override` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="776a6-117">Therefore, the F# equivalent of a virtual method declaration and definition in another .NET language is a combination of both an abstract method declaration and a separate definition, with either the `default` keyword or the `override` keyword.</span></span> <span data-ttu-id="776a6-118">Para obter mais informações e exemplos, consulte [métodos](members/methods.md).</span><span class="sxs-lookup"><span data-stu-id="776a6-118">For more information and examples, see [Methods](members/methods.md).</span></span>

<span data-ttu-id="776a6-119">Uma classe é considerada abstrata somente se houver métodos abstratos que são declarados, mas não definidos.</span><span class="sxs-lookup"><span data-stu-id="776a6-119">A class is considered abstract only if there are abstract methods that are declared but not defined.</span></span> <span data-ttu-id="776a6-120">Portanto, as classes que têm os métodos abstratos não são necessariamente abstratas de classes.</span><span class="sxs-lookup"><span data-stu-id="776a6-120">Therefore, classes that have abstract methods are not necessarily abstract classes.</span></span> <span data-ttu-id="776a6-121">A menos que uma classe tem indefinido métodos abstratos, não use o **AbstractClass** atributo.</span><span class="sxs-lookup"><span data-stu-id="776a6-121">Unless a class has undefined abstract methods, do not use the **AbstractClass** attribute.</span></span>

<span data-ttu-id="776a6-122">Na sintaxe anterior, *modificador de acessibilidade* pode ser `public`, `private` ou `internal`.</span><span class="sxs-lookup"><span data-stu-id="776a6-122">In the previous syntax, *accessibility-modifier* can be `public`, `private` or `internal`.</span></span> <span data-ttu-id="776a6-123">Para saber mais, veja [Controle de acesso](access-control.md).</span><span class="sxs-lookup"><span data-stu-id="776a6-123">For more information, see [Access Control](access-control.md).</span></span>

<span data-ttu-id="776a6-124">Assim como acontece com outros tipos, classes abstratas podem ter uma classe base e um ou mais interfaces base.</span><span class="sxs-lookup"><span data-stu-id="776a6-124">As with other types, abstract classes can have a base class and one or more base interfaces.</span></span> <span data-ttu-id="776a6-125">Cada classe base ou interface é exibida em uma linha separada, junto com o `inherit` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="776a6-125">Each base class or interface appears on a separate line together with the `inherit` keyword.</span></span>

<span data-ttu-id="776a6-126">A definição de tipo de uma classe abstrata pode conter membros totalmente definidos, mas ele também pode conter membros abstratos.</span><span class="sxs-lookup"><span data-stu-id="776a6-126">The type definition of an abstract class can contain fully defined members, but it can also contain abstract members.</span></span> <span data-ttu-id="776a6-127">A sintaxe para membros abstratos é exibida separadamente na sintaxe anterior.</span><span class="sxs-lookup"><span data-stu-id="776a6-127">The syntax for abstract members is shown separately in the previous syntax.</span></span> <span data-ttu-id="776a6-128">Nesta sintaxe, o *assinatura de tipo* de um membro é uma lista que contém os tipos de parâmetros na ordem e os tipos de retorno, separados por `->` tokens de e/ou `*` tokens conforme apropriado para via currying e tupled parâmetros.</span><span class="sxs-lookup"><span data-stu-id="776a6-128">In this syntax, the *type signature* of a member is a list that contains the parameter types in order and the return types, separated by `->` tokens and/or `*` tokens as appropriate for curried and tupled parameters.</span></span> <span data-ttu-id="776a6-129">A sintaxe para assinaturas de tipo de membro abstrato é o mesmo que o usado em arquivos de assinatura e se mostrado pelo IntelliSense no Editor de código do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="776a6-129">The syntax for abstract member type signatures is the same as that used in signature files and that shown by IntelliSense in the Visual Studio Code Editor.</span></span>

<span data-ttu-id="776a6-130">O código a seguir ilustra uma classe abstrata Shape, que tem duas classes de derivada não abstrata, círculo e um quadrado.</span><span class="sxs-lookup"><span data-stu-id="776a6-130">The following code illustrates an abstract class Shape, which has two non-abstract derived classes, Square and Circle.</span></span> <span data-ttu-id="776a6-131">O exemplo mostra como usar propriedades, métodos e classes abstratas.</span><span class="sxs-lookup"><span data-stu-id="776a6-131">The example shows how to use abstract classes, methods, and properties.</span></span> <span data-ttu-id="776a6-132">No exemplo, a forma de classe abstrata representa os elementos comuns do círculo de entidades concreta e quadrado.</span><span class="sxs-lookup"><span data-stu-id="776a6-132">In the example, the abstract class Shape represents the common elements of the concrete entities circle and square.</span></span> <span data-ttu-id="776a6-133">Os recursos comuns de todas as formas (em um sistema de coordenadas bidimensional) são abstraídos out em classe Shape: a posição na grade, um ângulo de rotação e as propriedades de área e perímetro.</span><span class="sxs-lookup"><span data-stu-id="776a6-133">The common features of all shapes (in a two-dimensional coordinate system) are abstracted out into the Shape class: the position on the grid, an angle of rotation, and the area and perimeter properties.</span></span> <span data-ttu-id="776a6-134">Elas podem ser substituídas, exceto para a posição, o comportamento dos quais formas individuais não é possível alterar.</span><span class="sxs-lookup"><span data-stu-id="776a6-134">These can be overridden, except for position, the behavior of which individual shapes cannot change.</span></span>

<span data-ttu-id="776a6-135">O método de rotação pode ser substituído, como na classe do círculo, o que é invariável da rotação devido a seu simetria.</span><span class="sxs-lookup"><span data-stu-id="776a6-135">The rotation method can be overridden, as in the Circle class, which is rotation invariant because of its symmetry.</span></span> <span data-ttu-id="776a6-136">Portanto, na classe do círculo, o método de rotação é substituído por um método que não faz nada.</span><span class="sxs-lookup"><span data-stu-id="776a6-136">So in the Circle class, the rotation method is replaced by a method that does nothing.</span></span>

[!code-fsharp[Main](../../../samples/snippets/fsharp/lang-ref-1/snippet2901.fs)]

<span data-ttu-id="776a6-137">**Saída:**</span><span class="sxs-lookup"><span data-stu-id="776a6-137">**Output:**</span></span>

```
Perimeter of square with side length 10.000000 is 40.000000
Circumference of circle with radius 5.000000 is 31.415927
Area of Square: 100.000000
Area of Circle: 78.539816
```

## <a name="see-also"></a><span data-ttu-id="776a6-138">Consulte também</span><span class="sxs-lookup"><span data-stu-id="776a6-138">See also</span></span>

- [<span data-ttu-id="776a6-139">Classes</span><span class="sxs-lookup"><span data-stu-id="776a6-139">Classes</span></span>](classes.md)
- [<span data-ttu-id="776a6-140">Membros</span><span class="sxs-lookup"><span data-stu-id="776a6-140">Members</span></span>](members/index.md)
- [<span data-ttu-id="776a6-141">Métodos</span><span class="sxs-lookup"><span data-stu-id="776a6-141">Methods</span></span>](members/methods.md)
- [<span data-ttu-id="776a6-142">Propriedades</span><span class="sxs-lookup"><span data-stu-id="776a6-142">Properties</span></span>](members/Properties.md)