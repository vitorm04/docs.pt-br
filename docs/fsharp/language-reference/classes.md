---
title: Classes
description: Saiba como F# as classes são tipos que representam objetos que podem ter propriedades, métodos e eventos.
ms.date: 05/16/2016
ms.openlocfilehash: 5c012d028bc1f89e3e9f5969b3461faab9aad3a0
ms.sourcegitcommit: f20dd18dbcf2275513281f5d9ad7ece6a62644b4
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 07/30/2019
ms.locfileid: "68630448"
---
# <a name="classes"></a><span data-ttu-id="d5bf9-103">Classes</span><span class="sxs-lookup"><span data-stu-id="d5bf9-103">Classes</span></span>

<span data-ttu-id="d5bf9-104">*Classes* são tipos que representam objetos que podem ter propriedades, métodos e eventos.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-104">*Classes* are types that represent objects that can have properties, methods, and events.</span></span>

## <a name="syntax"></a><span data-ttu-id="d5bf9-105">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="d5bf9-105">Syntax</span></span>

```fsharp
// Class definition:
type [access-modifier] type-name [type-params] [access-modifier] ( parameter-list ) [ as identifier ] =
[ class ]
[ inherit base-type-name(base-constructor-args) ]
[ let-bindings ]
[ do-bindings ]
member-list
...
[ end ]
// Mutually recursive class definitions:
type [access-modifier] type-name1 ...
and [access-modifier] type-name2 ...
...
```

## <a name="remarks"></a><span data-ttu-id="d5bf9-106">Comentários</span><span class="sxs-lookup"><span data-stu-id="d5bf9-106">Remarks</span></span>

<span data-ttu-id="d5bf9-107">Classes representam a descrição fundamental dos tipos de objeto do .NET; a classe é o conceito de tipo principal que oferece suporte à programação orientada a objeto no F#.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-107">Classes represent the fundamental description of .NET object types; the class is the primary type concept that supports object-oriented programming in F#.</span></span>

<span data-ttu-id="d5bf9-108">Na sintaxe anterior, o `type-name` é qualquer identificador válido.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-108">In the preceding syntax, the `type-name` is any valid identifier.</span></span> <span data-ttu-id="d5bf9-109">O `type-params` descreve os parâmetros de tipo genérico opcionais.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-109">The `type-params` describes optional generic type parameters.</span></span> <span data-ttu-id="d5bf9-110">Ele consiste em nomes de parâmetro de tipo e restrições entre colchetes angulares `>`(`<` e).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-110">It consists of type parameter names and constraints enclosed in angle brackets (`<` and `>`).</span></span> <span data-ttu-id="d5bf9-111">Para obter mais informações, consulte [genéricos](./generics/index.md) e [restrições](./generics/constraints.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-111">For more information, see [Generics](./generics/index.md) and [Constraints](./generics/constraints.md).</span></span> <span data-ttu-id="d5bf9-112">O `parameter-list` descreve os parâmetros do construtor.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-112">The `parameter-list` describes constructor parameters.</span></span> <span data-ttu-id="d5bf9-113">O primeiro modificador de acesso pertence ao tipo; o segundo pertence ao Construtor principal.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-113">The first access modifier pertains to the type; the second pertains to the primary constructor.</span></span> <span data-ttu-id="d5bf9-114">Em ambos os casos, o padrão `public`é.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-114">In both cases, the default is `public`.</span></span>

<span data-ttu-id="d5bf9-115">Você especifica a classe base para uma classe usando a `inherit` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-115">You specify the base class for a class by using the `inherit` keyword.</span></span> <span data-ttu-id="d5bf9-116">Você deve fornecer argumentos, entre parênteses, para o construtor da classe base.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-116">You must supply arguments, in parentheses, for the base class constructor.</span></span>

<span data-ttu-id="d5bf9-117">Você declara campos ou valores de função que são locais para a classe usando `let` associações, e você deve seguir as regras gerais para `let` associações.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-117">You declare fields or function values that are local to the class by using `let` bindings, and you must follow the general rules for `let` bindings.</span></span> <span data-ttu-id="d5bf9-118">A `do-bindings` seção inclui o código a ser executado na construção do objeto.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-118">The `do-bindings` section includes code to be executed upon object construction.</span></span>

<span data-ttu-id="d5bf9-119">O `member-list` consiste em construtores adicionais, declarações de método estático e de instância, declarações de interface, associações abstratas e declarações de eventos e propriedades.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-119">The `member-list` consists of additional constructors, instance and static method declarations, interface declarations, abstract bindings, and property and event declarations.</span></span> <span data-ttu-id="d5bf9-120">Eles são descritos em [Membros](./members/index.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-120">These are described in [Members](./members/index.md).</span></span>

<span data-ttu-id="d5bf9-121">O `identifier` que é usado com a palavra `as` -chave Optional fornece um nome para a variável de instância, ou autoidentifier, que pode ser usado na definição de tipo para se referir à instância do tipo.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-121">The `identifier` that is used with the optional `as` keyword gives a name to the instance variable, or self identifier, which can be used in the type definition to refer to the instance of the type.</span></span> <span data-ttu-id="d5bf9-122">Para obter mais informações, consulte a seção identificadores automáticos mais adiante neste tópico.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-122">For more information, see the section Self Identifiers later in this topic.</span></span>

<span data-ttu-id="d5bf9-123">As palavras-chave `class` e `end` que marcam o início e o fim da definição são opcionais.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-123">The keywords `class` and `end` that mark the start and end of the definition are optional.</span></span>

<span data-ttu-id="d5bf9-124">Os tipos recursivos mutuamente, que são tipos que fazem referência uns aos outros, são Unidos `and` junto com a palavra-chave, assim como as funções recursivas mutuamente.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-124">Mutually recursive types, which are types that reference each other, are joined together with the `and` keyword just as mutually recursive functions are.</span></span> <span data-ttu-id="d5bf9-125">Para obter um exemplo, consulte a seção tipos recursivos mutuamente.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-125">For an example, see the section Mutually Recursive Types.</span></span>

## <a name="constructors"></a><span data-ttu-id="d5bf9-126">Construtores</span><span class="sxs-lookup"><span data-stu-id="d5bf9-126">Constructors</span></span>

<span data-ttu-id="d5bf9-127">O construtor é um código que cria uma instância do tipo de classe.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-127">The constructor is code that creates an instance of the class type.</span></span> <span data-ttu-id="d5bf9-128">Construtores para classes funcionam de maneira um pouco F# diferente em outras linguagens .net.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-128">Constructors for classes work somewhat differently in F# than they do in other .NET languages.</span></span> <span data-ttu-id="d5bf9-129">Em uma F# classe, sempre há um construtor principal cujos argumentos são descritos no `parameter-list` que segue o nome do tipo e cujo `let` corpo consiste nas associações (e `let rec`) no início da declaração de classe e no `do` associações a seguir.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-129">In an F# class, there is always a primary constructor whose arguments are described in the `parameter-list` that follows the type name, and whose body consists of the `let` (and `let rec`) bindings at the start of the class declaration and the `do` bindings that follow.</span></span> <span data-ttu-id="d5bf9-130">Os argumentos do construtor primário estão no escopo durante a declaração de classe.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-130">The arguments of the primary constructor are in scope throughout the class declaration.</span></span>

<span data-ttu-id="d5bf9-131">Você pode adicionar construtores adicionais usando a `new` palavra-chave para adicionar um membro, da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="d5bf9-131">You can add additional constructors by using the `new` keyword to add a member, as follows:</span></span>

<span data-ttu-id="d5bf9-132">`new`(`argument-list`) = `constructor-body`</span><span class="sxs-lookup"><span data-stu-id="d5bf9-132">`new`(`argument-list`) = `constructor-body`</span></span>

<span data-ttu-id="d5bf9-133">O corpo do novo construtor deve invocar o construtor primário que é especificado na parte superior da declaração de classe.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-133">The body of the new constructor must invoke the primary constructor that is specified at the top of the class declaration.</span></span>

<span data-ttu-id="d5bf9-134">O exemplo a seguir ilustra esse conceito.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-134">The following example illustrates this concept.</span></span> <span data-ttu-id="d5bf9-135">No código a seguir, `MyClass` tem dois construtores, um construtor principal que usa dois argumentos e outro construtor que não usa argumentos.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-135">In the following code, `MyClass` has two constructors, a primary constructor that takes two arguments and another constructor that takes no arguments.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-1/snippet2401.fs)]

## <a name="let-and-do-bindings"></a><span data-ttu-id="d5bf9-136">permitir e fazer associações</span><span class="sxs-lookup"><span data-stu-id="d5bf9-136">let and do Bindings</span></span>

<span data-ttu-id="d5bf9-137">As `let` associações `do` e em uma definição de classe formam o corpo do construtor da classe primária e, portanto, são executadas sempre que uma instância de classe é criada.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-137">The `let` and `do` bindings in a class definition form the body of the primary class constructor, and therefore they run whenever a class instance is created.</span></span> <span data-ttu-id="d5bf9-138">Se uma `let` associação for uma função, ela será compilada em um membro.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-138">If a `let` binding is a function, then it is compiled into a member.</span></span> <span data-ttu-id="d5bf9-139">Se a `let` associação for um valor que não é usado em nenhuma função ou membro, ele será compilado em uma variável que é local para o construtor.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-139">If the `let` binding is a value that is not used in any function or member, then it is compiled into a variable that is local to the constructor.</span></span> <span data-ttu-id="d5bf9-140">Caso contrário, ele é compilado em um campo da classe.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-140">Otherwise, it is compiled into a field of the class.</span></span> <span data-ttu-id="d5bf9-141">As `do` expressões a seguir são compiladas no construtor primário e executam o código de inicialização para cada instância.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-141">The `do` expressions that follow are compiled into the primary constructor and execute initialization code for every instance.</span></span> <span data-ttu-id="d5bf9-142">Como quaisquer construtores adicionais sempre chamam o Construtor principal, as `let` associações e `do` associações sempre são executadas, independentemente de qual Construtor é chamado.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-142">Because any additional constructors always call the primary constructor, the `let` bindings and `do` bindings always execute regardless of which constructor is called.</span></span>

<span data-ttu-id="d5bf9-143">Os campos criados por `let` associações podem ser acessados em todos os métodos e propriedades da classe; no entanto, eles não podem ser acessados de métodos estáticos, mesmo que os métodos estáticos tenham uma variável de instância como um parâmetro.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-143">Fields that are created by `let` bindings can be accessed throughout the methods and properties of the class; however, they cannot be accessed from static methods, even if the static methods take an instance variable as a parameter.</span></span> <span data-ttu-id="d5bf9-144">Eles não podem ser acessados usando o autoidentifier, se houver um.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-144">They cannot be accessed by using the self identifier, if one exists.</span></span>

## <a name="self-identifiers"></a><span data-ttu-id="d5bf9-145">Identificadores automáticos</span><span class="sxs-lookup"><span data-stu-id="d5bf9-145">Self Identifiers</span></span>

<span data-ttu-id="d5bf9-146">Um *autoidentifier* é um nome que representa a instância atual.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-146">A *self identifier* is a name that represents the current instance.</span></span> <span data-ttu-id="d5bf9-147">Os autoidentificadores se assemelham à `this` palavra- `Me` chave em C# ou C++ ou em Visual Basic.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-147">Self identifiers resemble the `this` keyword in C# or C++ or `Me` in Visual Basic.</span></span> <span data-ttu-id="d5bf9-148">Você pode definir um autoidentifier de duas maneiras diferentes, dependendo se deseja que o autoidentificador esteja no escopo para a definição de classe inteira ou apenas para um método individual.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-148">You can define a self identifier in two different ways, depending on whether you want the self identifier to be in scope for the whole class definition or just for an individual method.</span></span>

<span data-ttu-id="d5bf9-149">Para definir um autoidentifier para a classe inteira, use a `as` palavra-chave após os parênteses de fechamento da lista de parâmetros do construtor e especifique o nome do identificador.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-149">To define a self identifier for the whole class, use the `as` keyword after the closing parentheses of the constructor parameter list, and specify the identifier name.</span></span>

<span data-ttu-id="d5bf9-150">Para definir um autoidentifier para apenas um método, forneça o autoidentifier na declaração de membro, logo antes do nome do método e de um ponto (.) como um separador.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-150">To define a self identifier for just one method, provide the self identifier in the member declaration, just before the method name and a period (.) as a separator.</span></span>

<span data-ttu-id="d5bf9-151">O exemplo de código a seguir ilustra as duas maneiras de criar um autoidentifier.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-151">The following code example illustrates the two ways to create a self identifier.</span></span> <span data-ttu-id="d5bf9-152">Na primeira linha, a `as` palavra-chave é usada para definir o próprio identificador.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-152">In the first line, the `as` keyword is used to define the self identifier.</span></span> <span data-ttu-id="d5bf9-153">Na quinta linha, o identificador `this` é usado para definir um identificador próprio cujo escopo é restrito ao método. `PrintMessage`</span><span class="sxs-lookup"><span data-stu-id="d5bf9-153">In the fifth line, the identifier `this` is used to define a self identifier whose scope is restricted to the method `PrintMessage`.</span></span>

```fsharp
type MyClass2(dataIn) as self =
    let data = dataIn
    do
        self.PrintMessage()
    member this.PrintMessage() =
        printf "Creating MyClass2 with Data %d" data
```

<span data-ttu-id="d5bf9-154">Ao contrário de outras linguagens .NET, você pode nomear o autoidentificador como desejar; Você não está restrito a nomes como `self`, `Me`ou `this`.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-154">Unlike in other .NET languages, you can name the self identifier however you want; you are not restricted to names such as `self`, `Me`, or `this`.</span></span>

<span data-ttu-id="d5bf9-155">O autoidentifier declarado com a `as` palavra-chave não é inicializado até que as `let` associações sejam executadas.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-155">The self identifier that is declared with the `as` keyword is not initialized until after the `let` bindings are executed.</span></span> <span data-ttu-id="d5bf9-156">Portanto, ele não pode ser usado nas `let` associações.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-156">Therefore, it cannot be used in the `let` bindings.</span></span> <span data-ttu-id="d5bf9-157">Você pode usar o autoidentifier na `do` seção associações.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-157">You can use the self identifier in the `do` bindings section.</span></span>

## <a name="generic-type-parameters"></a><span data-ttu-id="d5bf9-158">Parâmetros de tipo genérico</span><span class="sxs-lookup"><span data-stu-id="d5bf9-158">Generic Type Parameters</span></span>

<span data-ttu-id="d5bf9-159">Parâmetros de tipo genérico são especificados entre colchetes angulares `>`(`<` e), na forma de uma aspa simples seguida por um identificador.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-159">Generic type parameters are specified in angle brackets (`<` and `>`), in the form of a single quotation mark followed by an identifier.</span></span> <span data-ttu-id="d5bf9-160">Vários parâmetros de tipo genérico são separados por vírgulas.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-160">Multiple generic type parameters are separated by commas.</span></span> <span data-ttu-id="d5bf9-161">O parâmetro de tipo genérico está no escopo durante a declaração.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-161">The generic type parameter is in scope throughout the declaration.</span></span> <span data-ttu-id="d5bf9-162">O exemplo de código a seguir mostra como especificar parâmetros de tipo genérico.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-162">The following code example shows how to specify generic type parameters.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-1/snippet2403.fs)]

<span data-ttu-id="d5bf9-163">Os argumentos de tipo são inferidos quando o tipo é usado.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-163">Type arguments are inferred when the type is used.</span></span> <span data-ttu-id="d5bf9-164">No código a seguir, o tipo inferido é uma sequência de tuplas.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-164">In the following code, the inferred type is a sequence of tuples.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-1/snippet24031.fs)]

## <a name="specifying-inheritance"></a><span data-ttu-id="d5bf9-165">Especificando a herança</span><span class="sxs-lookup"><span data-stu-id="d5bf9-165">Specifying Inheritance</span></span>

<span data-ttu-id="d5bf9-166">A `inherit` cláusula identifica a classe base direta, se houver uma.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-166">The `inherit` clause identifies the direct base class, if there is one.</span></span> <span data-ttu-id="d5bf9-167">No F#, apenas uma classe base direta é permitida.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-167">In F#, only one direct base class is allowed.</span></span> <span data-ttu-id="d5bf9-168">As interfaces que uma classe implementa não são consideradas classes base.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-168">Interfaces that a class implements are not considered base classes.</span></span> <span data-ttu-id="d5bf9-169">As interfaces são discutidas no tópico [interfaces](Interfaces.md) .</span><span class="sxs-lookup"><span data-stu-id="d5bf9-169">Interfaces are discussed in the [Interfaces](Interfaces.md) topic.</span></span>

<span data-ttu-id="d5bf9-170">Você pode acessar os métodos e as propriedades da classe base da classe derivada usando a palavra-chave `base` Language como um identificador, seguido por um ponto final (.) e o nome do membro.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-170">You can access the methods and properties of the base class from the derived class by using the language keyword `base` as an identifier, followed by a period (.) and the name of the member.</span></span>

<span data-ttu-id="d5bf9-171">Para obter mais informações, consulte [Herança](inheritance.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-171">For more information, see [Inheritance](inheritance.md).</span></span>

## <a name="members-section"></a><span data-ttu-id="d5bf9-172">Seção Membros</span><span class="sxs-lookup"><span data-stu-id="d5bf9-172">Members Section</span></span>

<span data-ttu-id="d5bf9-173">Você pode definir métodos estáticos ou de instância, propriedades, implementações de interface, membros abstratos, declarações de eventos e construtores adicionais nesta seção.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-173">You can define static or instance methods, properties, interface implementations, abstract members, event declarations, and additional constructors in this section.</span></span> <span data-ttu-id="d5bf9-174">As associações Let e não podem aparecer nesta seção.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-174">Let and do bindings cannot appear in this section.</span></span> <span data-ttu-id="d5bf9-175">Como os membros podem ser adicionados a uma variedade F# de tipos além de classes, eles são discutidos em um tópico separado, [Membros](./members/index.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-175">Because members can be added to a variety of F# types in addition to classes, they are discussed in a separate topic, [Members](./members/index.md).</span></span>

## <a name="mutually-recursive-types"></a><span data-ttu-id="d5bf9-176">Tipos recursivos mutuamente</span><span class="sxs-lookup"><span data-stu-id="d5bf9-176">Mutually Recursive Types</span></span>

<span data-ttu-id="d5bf9-177">Quando você define tipos que fazem referência uns aos outros de forma circular, você cadeia de caracteres de acordo com as `and` definições de tipo usando a palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-177">When you define types that reference each other in a circular way, you string together the type definitions by using the `and` keyword.</span></span> <span data-ttu-id="d5bf9-178">A `and` palavra-chave `type` substitui a palavra-chave em todos, exceto a primeira definição, da seguinte maneira.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-178">The `and` keyword replaces the `type` keyword on all except the first definition, as follows.</span></span>

[!code-fsharp[Main](~/samples/snippets/fsharp/lang-ref-1/snippet2404.fs)]

<span data-ttu-id="d5bf9-179">A saída é uma lista de todos os arquivos no diretório atual.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-179">The output is a list of all the files in the current directory.</span></span>

## <a name="when-to-use-classes-unions-records-and-structures"></a><span data-ttu-id="d5bf9-180">Quando usar classes, uniões, registros e estruturas</span><span class="sxs-lookup"><span data-stu-id="d5bf9-180">When to Use Classes, Unions, Records, and Structures</span></span>

<span data-ttu-id="d5bf9-181">Considerando a variedade de tipos dos quais escolher, você precisa ter uma boa compreensão do que cada tipo foi projetado para selecionar o tipo apropriado para uma situação específica.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-181">Given the variety of types to choose from, you need to have a good understanding of what each type is designed for to select the appropriate type for a particular situation.</span></span> <span data-ttu-id="d5bf9-182">As classes são projetadas para uso em contextos de programação orientada a objeto.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-182">Classes are designed for use in object-oriented programming contexts.</span></span> <span data-ttu-id="d5bf9-183">A programação orientada a objeto é o paradigma dominante usado em aplicativos que são escritos para o .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-183">Object-oriented programming is the dominant paradigm used in applications that are written for the .NET Framework.</span></span> <span data-ttu-id="d5bf9-184">Se o F# seu código tiver que trabalhar junto com o .NET Framework ou outra biblioteca orientada a objeto e, especialmente, se você precisar se estender de um sistema de tipos orientado a objeto, como uma biblioteca de interface do usuário, as classes provavelmente serão apropriadas.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-184">If your F# code has to work closely with the .NET Framework or another object-oriented library, and especially if you have to extend from an object-oriented type system such as a UI library, classes are probably appropriate.</span></span>

<span data-ttu-id="d5bf9-185">Se você não estiver Interoperando com código orientado a objeto, ou se estiver escrevendo código que seja autônomo e, portanto, protegido contra uma interação frequente com o código orientado a objeto, considere usar registros e uniões discriminadas.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-185">If you are not interoperating closely with object-oriented code, or if you are writing code that is self-contained and therefore protected from frequent interaction with object-oriented code, you should consider using records and discriminated unions.</span></span> <span data-ttu-id="d5bf9-186">Uma única união discriminada e bem pensada, junto com o código de correspondência de padrões apropriado, geralmente pode ser usada como uma alternativa mais simples para uma hierarquia de objetos.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-186">A single, well thought–out discriminated union, together with appropriate pattern matching code, can often be used as a simpler alternative to an object hierarchy.</span></span> <span data-ttu-id="d5bf9-187">Para obter mais informações sobre uniões discriminadas, consulte [uniões discriminadas](discriminated-unions.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-187">For more information about discriminated unions, see [Discriminated Unions](discriminated-unions.md).</span></span>

<span data-ttu-id="d5bf9-188">Os registros têm a vantagem de ser mais simples do que as classes, mas os registros não são apropriados quando as demandas de um tipo excedem o que pode ser feito com sua simplicidade.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-188">Records have the advantage of being simpler than classes, but records are not appropriate when the demands of a type exceed what can be accomplished with their simplicity.</span></span> <span data-ttu-id="d5bf9-189">Os registros são basicamente agregações simples de valores, sem construtores separados que podem executar ações personalizadas, sem campos ocultos, e sem implementações de herança ou de interface.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-189">Records are basically simple aggregates of values, without separate constructors that can perform custom actions, without hidden fields, and without inheritance or interface implementations.</span></span> <span data-ttu-id="d5bf9-190">Embora Membros como propriedades e métodos possam ser adicionados aos registros para tornar seu comportamento mais complexo, os campos armazenados em um registro ainda são uma agregação simples de valores.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-190">Although members such as properties and methods can be added to records to make their behavior more complex, the fields stored in a record are still a simple aggregate of values.</span></span> <span data-ttu-id="d5bf9-191">Para obter mais informações sobre registros, consulte [registros](records.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-191">For more information about records, see [Records](records.md).</span></span>

<span data-ttu-id="d5bf9-192">As estruturas também são úteis para pequenas agregações de dados, mas diferem de classes e registros em que são tipos de valor .NET.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-192">Structures are also useful for small aggregates of data, but they differ from classes and records in that they are .NET value types.</span></span> <span data-ttu-id="d5bf9-193">Classes e registros são tipos de referência do .NET.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-193">Classes and records are .NET reference types.</span></span> <span data-ttu-id="d5bf9-194">A semântica dos tipos de valor e dos tipos de referência são diferentes nos tipos de valor que são passados por valor.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-194">The semantics of value types and reference types are different in that value types are passed by value.</span></span> <span data-ttu-id="d5bf9-195">Isso significa que eles são bits copiados para bit quando são passados como um parâmetro ou retornados de uma função.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-195">This means that they are copied bit for bit when they are passed as a parameter or returned from a function.</span></span> <span data-ttu-id="d5bf9-196">Eles também são armazenados na pilha ou, se forem usados como um campo, inserido dentro do objeto pai, em vez de armazenados em seu próprio local separado no heap.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-196">They are also stored on the stack or, if they are used as a field, embedded inside the parent object instead of stored in their own separate location on the heap.</span></span> <span data-ttu-id="d5bf9-197">Portanto, as estruturas são apropriadas para dados acessados com frequência quando a sobrecarga de acessar o heap é um problema.</span><span class="sxs-lookup"><span data-stu-id="d5bf9-197">Therefore, structures are appropriate for frequently accessed data when the overhead of accessing the heap is a problem.</span></span> <span data-ttu-id="d5bf9-198">Para obter mais informações sobre estruturas, consulte [estruturas](structures.md).</span><span class="sxs-lookup"><span data-stu-id="d5bf9-198">For more information about structures, see [Structures](structures.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="d5bf9-199">Consulte também</span><span class="sxs-lookup"><span data-stu-id="d5bf9-199">See also</span></span>

- [<span data-ttu-id="d5bf9-200">Referência da Linguagem F#</span><span class="sxs-lookup"><span data-stu-id="d5bf9-200">F# Language Reference</span></span>](index.md)
- [<span data-ttu-id="d5bf9-201">Membros</span><span class="sxs-lookup"><span data-stu-id="d5bf9-201">Members</span></span>](./members/index.md)
- [<span data-ttu-id="d5bf9-202">Herança</span><span class="sxs-lookup"><span data-stu-id="d5bf9-202">Inheritance</span></span>](inheritance.md)
- [<span data-ttu-id="d5bf9-203">Interfaces</span><span class="sxs-lookup"><span data-stu-id="d5bf9-203">Interfaces</span></span>](interfaces.md)
