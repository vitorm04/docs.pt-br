---
title: Sintaxe PropertyPath (XAML)
ms.date: 03/30/2017
helpviewer_keywords:
- PropertyPath object [WPF]
- XAML [WPF], PropertyPath object
ms.assetid: 0e3cdf07-abe6-460a-a9af-3764b4fd707f
ms.openlocfilehash: 7db435e45ddc55346af5ea5fdbcce611173c774b
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "62053529"
---
# <a name="propertypath-xaml-syntax"></a><span data-ttu-id="6a661-102">Sintaxe PropertyPath (XAML)</span><span class="sxs-lookup"><span data-stu-id="6a661-102">PropertyPath XAML Syntax</span></span>
<span data-ttu-id="6a661-103">O <xref:System.Windows.PropertyPath> objeto dá suporte a uma embutida complexa [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] sintaxe para definir várias propriedades que usam o <xref:System.Windows.PropertyPath> tipo como seu valor.</span><span class="sxs-lookup"><span data-stu-id="6a661-103">The <xref:System.Windows.PropertyPath> object supports a complex inline [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] syntax for setting various properties that take the <xref:System.Windows.PropertyPath> type as their value.</span></span> <span data-ttu-id="6a661-104">Este tópico documenta o <xref:System.Windows.PropertyPath> sintaxe conforme aplicado a sintaxes de associação e animação.</span><span class="sxs-lookup"><span data-stu-id="6a661-104">This topic documents the <xref:System.Windows.PropertyPath> syntax as applied to binding and animation syntaxes.</span></span>  

<a name="where"></a>   
## <a name="where-propertypath-is-used"></a><span data-ttu-id="6a661-105">Onde o PropertyPath é usado</span><span class="sxs-lookup"><span data-stu-id="6a661-105">Where PropertyPath Is Used</span></span>  
 <span data-ttu-id="6a661-106"><xref:System.Windows.PropertyPath> é um objeto comum que é usado em vários [!INCLUDE[TLA#tla_winclient](../../../../includes/tlasharptla-winclient-md.md)] recursos.</span><span class="sxs-lookup"><span data-stu-id="6a661-106"><xref:System.Windows.PropertyPath> is a common object that is used in several [!INCLUDE[TLA#tla_winclient](../../../../includes/tlasharptla-winclient-md.md)] features.</span></span> <span data-ttu-id="6a661-107">Apesar do uso comum <xref:System.Windows.PropertyPath> para transmitir informações de caminho de propriedade, os usos para cada área de recursos onde <xref:System.Windows.PropertyPath> é usado como um tipo variam.</span><span class="sxs-lookup"><span data-stu-id="6a661-107">Despite using the common <xref:System.Windows.PropertyPath> to convey property path information, the usages for each feature area where <xref:System.Windows.PropertyPath> is used as a type vary.</span></span> <span data-ttu-id="6a661-108">Portanto, é mais prático documentar as sintaxes por recurso.</span><span class="sxs-lookup"><span data-stu-id="6a661-108">Therefore, it is more practical to document the syntaxes on a per-feature basis.</span></span>  
  
 <span data-ttu-id="6a661-109">Basicamente, [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] usa <xref:System.Windows.PropertyPath> para descrever caminhos de modelo de objeto para percorrer as propriedades de um objeto de fonte de dados e para descrever o caminho de destino para animações direcionadas.</span><span class="sxs-lookup"><span data-stu-id="6a661-109">Primarily, [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] uses <xref:System.Windows.PropertyPath> to describe object-model paths for traversing the properties of an object data source, and to describe the target path for targeted animations.</span></span>  
  
 <span data-ttu-id="6a661-110">Algumas propriedades de estilo e modelo, como <xref:System.Windows.Setter.Property%2A?displayProperty=nameWithType> recebem um nome de propriedade qualificado que lembra superficialmente um <xref:System.Windows.PropertyPath>.</span><span class="sxs-lookup"><span data-stu-id="6a661-110">Some style and template properties such as <xref:System.Windows.Setter.Property%2A?displayProperty=nameWithType> take a qualified property name that superficially resembles a <xref:System.Windows.PropertyPath>.</span></span> <span data-ttu-id="6a661-111">Mas isso não é uma verdadeira <xref:System.Windows.PropertyPath>; em vez disso, ele é um qualificado *Owner* uso de formato que é habilitado pelo WPF de cadeia de caracteres [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processador em combinação com o conversor de tipo para <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="6a661-111">But this is not a true <xref:System.Windows.PropertyPath>; instead it is a qualified *owner.property* string format usage that is enabled by the WPF [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor in combination with the type converter for <xref:System.Windows.DependencyProperty>.</span></span>  
  
<a name="databinding_s"></a>   
## <a name="propertypath-for-objects-in-data-binding"></a><span data-ttu-id="6a661-112">PropertyPath para objetos em associação de dados</span><span class="sxs-lookup"><span data-stu-id="6a661-112">PropertyPath for Objects in Data Binding</span></span>  
 <span data-ttu-id="6a661-113">A associação de dados é um recurso do [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] por meio do qual é possível se associar ao valor de destino de qualquer propriedade de dependência.</span><span class="sxs-lookup"><span data-stu-id="6a661-113">Data binding is a [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] feature whereby you can bind to the target value of any dependency property.</span></span> <span data-ttu-id="6a661-114">No entanto, a origem dessa associação de dados não precisa ser uma propriedade de dependência; pode ser qualquer tipo de propriedade que seja reconhecido pelo provedor de dados aplicável.</span><span class="sxs-lookup"><span data-stu-id="6a661-114">However, the source of such a data binding need not be a dependency property; it can be any property type that is recognized by the applicable data provider.</span></span> <span data-ttu-id="6a661-115">Caminhos de propriedade são usados principalmente para o <xref:System.Windows.Data.ObjectDataProvider>, que é usado para obter origens da associação de [!INCLUDE[TLA#tla_clr](../../../../includes/tlasharptla-clr-md.md)] objetos e suas propriedades.</span><span class="sxs-lookup"><span data-stu-id="6a661-115">Property paths are particularly used for the <xref:System.Windows.Data.ObjectDataProvider>, which is used for obtaining binding sources from [!INCLUDE[TLA#tla_clr](../../../../includes/tlasharptla-clr-md.md)] objects and their properties.</span></span>  
  
 <span data-ttu-id="6a661-116">Observe que associação de dados para [!INCLUDE[TLA#tla_xml](../../../../includes/tlasharptla-xml-md.md)] não usa <xref:System.Windows.PropertyPath>, porque ele não usa <xref:System.Windows.Data.Binding.Path%2A> no <xref:System.Windows.Data.Binding>.</span><span class="sxs-lookup"><span data-stu-id="6a661-116">Note that data binding to [!INCLUDE[TLA#tla_xml](../../../../includes/tlasharptla-xml-md.md)] does not use <xref:System.Windows.PropertyPath>, because it does not use <xref:System.Windows.Data.Binding.Path%2A> in the <xref:System.Windows.Data.Binding>.</span></span> <span data-ttu-id="6a661-117">Em vez disso, você usa <xref:System.Windows.Data.Binding.XPath%2A> e especifique a sintaxe XPath válida para o [!INCLUDE[TLA#tla_xmldom](../../../../includes/tlasharptla-xmldom-md.md)] dos dados.</span><span class="sxs-lookup"><span data-stu-id="6a661-117">Instead, you use <xref:System.Windows.Data.Binding.XPath%2A> and specify valid XPath syntax into the [!INCLUDE[TLA#tla_xmldom](../../../../includes/tlasharptla-xmldom-md.md)] of the data.</span></span> <span data-ttu-id="6a661-118"><xref:System.Windows.Data.Binding.XPath%2A> também é especificado como uma cadeia de caracteres, mas não está documentado aqui; ver [associar a dados XML usando um XMLDataProvider e consultas XPath](../data/how-to-bind-to-xml-data-using-an-xmldataprovider-and-xpath-queries.md).</span><span class="sxs-lookup"><span data-stu-id="6a661-118"><xref:System.Windows.Data.Binding.XPath%2A> is also specified as a string, but is not documented here; see [Bind to XML Data Using an XMLDataProvider and XPath Queries](../data/how-to-bind-to-xml-data-using-an-xmldataprovider-and-xpath-queries.md).</span></span>  
  
 <span data-ttu-id="6a661-119">A chave para entender os caminhos da propriedade na associação de dados é que você pode direcionar a associação a um valor da propriedade individual ou, em vez disso, pode associar a propriedades de destino que assumem listas ou coleções.</span><span class="sxs-lookup"><span data-stu-id="6a661-119">A key to understanding property paths in data binding is that you can target the binding to an individual property value, or you can instead bind to target properties that take lists or collections.</span></span> <span data-ttu-id="6a661-120">Se você estiver associando coleções, por exemplo, associando um <xref:System.Windows.Controls.ListBox> que vai se expandir dependendo de quantos itens de dados estão na coleção e, em seguida, o caminho da propriedade deve referenciar o objeto de coleção, os itens da coleção individuais não.</span><span class="sxs-lookup"><span data-stu-id="6a661-120">If you are binding collections, for instance binding a <xref:System.Windows.Controls.ListBox> that will expand depending on how many data items are in the collection, then your property path should reference the collection object, not individual collection items.</span></span> <span data-ttu-id="6a661-121">O mecanismo de associação de dados corresponderá a coleção usada como a fonte de dados para o tipo do destino da associação automaticamente, resultando em um comportamento como preencher um <xref:System.Windows.Controls.ListBox> com uma matriz de itens.</span><span class="sxs-lookup"><span data-stu-id="6a661-121">The data binding engine will match the collection used as the data source to the type of the binding target automatically, resulting in behavior such as populating a <xref:System.Windows.Controls.ListBox> with an items array.</span></span>  
  
<a name="singlecurrent"></a>   
### <a name="single-property-on-the-immediate-object-as-data-context"></a><span data-ttu-id="6a661-122">Propriedade única no objeto imediato como contexto de dados</span><span class="sxs-lookup"><span data-stu-id="6a661-122">Single Property on the Immediate Object as Data Context</span></span>  
  
```xml  
<Binding Path="propertyName" .../>  
```  
  
 <span data-ttu-id="6a661-123">*propertyName* deve ser o nome de uma propriedade que é atual <xref:System.Windows.FrameworkElement.DataContext%2A> para um <xref:System.Windows.Data.Binding.Path%2A> uso.</span><span class="sxs-lookup"><span data-stu-id="6a661-123">*propertyName* must resolve to be the name of a property that is in the current <xref:System.Windows.FrameworkElement.DataContext%2A> for a <xref:System.Windows.Data.Binding.Path%2A> usage.</span></span> <span data-ttu-id="6a661-124">Se a associação atualizar a fonte, essa propriedade deverá ser de leitura/gravação e o objeto de origem deverá ser mutável.</span><span class="sxs-lookup"><span data-stu-id="6a661-124">If your binding updates the source, that property must be read/write and the source object must be mutable.</span></span>  
  
<a name="singleindex"></a>   
### <a name="single-indexer-on-the-immediate-object-as-data-context"></a><span data-ttu-id="6a661-125">Indexador único no objeto imediato como contexto de dados</span><span class="sxs-lookup"><span data-stu-id="6a661-125">Single Indexer on the Immediate Object as Data Context</span></span>  
  
```xml  
<Binding Path="[key]" .../>  
```  
  
 <span data-ttu-id="6a661-126">O `key` deve ser o índice tipado para um dicionário ou tabela de hash ou o índice inteiro de uma matriz.</span><span class="sxs-lookup"><span data-stu-id="6a661-126">`key` must be either the typed index to a dictionary or hash table, or the integer index of an array.</span></span> <span data-ttu-id="6a661-127">Além disso, o valor da chave deve ser um tipo diretamente associável à propriedade em que foi aplicado.</span><span class="sxs-lookup"><span data-stu-id="6a661-127">Also, the value of the key must be a type that is directly bindable to the property where it is applied.</span></span> <span data-ttu-id="6a661-128">Por exemplo, uma tabela de hash que contém as chaves de cadeia de caracteres e valores de cadeia de caracteres pode ser usada dessa maneira para associar ao texto para um <xref:System.Windows.Controls.TextBox>.</span><span class="sxs-lookup"><span data-stu-id="6a661-128">For instance, a hash table that contains string keys and string values can be used this way to bind to Text for a <xref:System.Windows.Controls.TextBox>.</span></span> <span data-ttu-id="6a661-129">Ou, se a chave apontar para uma coleção ou subíndice, será possível usar essa sintaxe para se associar a uma propriedade de coleção de destino.</span><span class="sxs-lookup"><span data-stu-id="6a661-129">Or, if the key points to a collection or subindex, you could use this syntax to bind to a target collection property.</span></span> <span data-ttu-id="6a661-130">Caso contrário, será necessário fazer referência a uma propriedade específica, por meio de uma sintaxe como `<Binding Path="[key].propertyName" .../>`.</span><span class="sxs-lookup"><span data-stu-id="6a661-130">Otherwise, you need to reference a specific property, through a syntax such as `<Binding Path="[key].propertyName" .../>`.</span></span>  
  
 <span data-ttu-id="6a661-131">Você pode especificar o tipo do índice, se necessário.</span><span class="sxs-lookup"><span data-stu-id="6a661-131">You can specify the type of the index if necessary.</span></span> <span data-ttu-id="6a661-132">Para obter detalhes sobre esse aspecto do caminho de propriedade indexado, consulte <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="6a661-132">For details on this aspect of an indexed property path, see <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span></span>  
  
<a name="multipleindirect"></a>   
### <a name="multiple-property-indirect-property-targeting"></a><span data-ttu-id="6a661-133">Propriedade múltipla (direcionamento indireto de propriedade)</span><span class="sxs-lookup"><span data-stu-id="6a661-133">Multiple Property (Indirect Property Targeting)</span></span>  
  
```xml  
<Binding Path="propertyName.propertyName2" .../>  
```  
  
 <span data-ttu-id="6a661-134">`propertyName` deve ser o nome de uma propriedade que é o atual <xref:System.Windows.FrameworkElement.DataContext%2A>.</span><span class="sxs-lookup"><span data-stu-id="6a661-134">`propertyName` must resolve to be the name of a property that is the current <xref:System.Windows.FrameworkElement.DataContext%2A>.</span></span> <span data-ttu-id="6a661-135">As propriedades de caminho `propertyName` e `propertyName2` podem ser quaisquer propriedades que existam em uma relação, em que `propertyName2` é uma propriedade que existe no tipo que é o valor de `propertyName`.</span><span class="sxs-lookup"><span data-stu-id="6a661-135">The path properties `propertyName` and `propertyName2` can be any properties that exist in a relationship, where `propertyName2` is a property that exists on the type that is the value of `propertyName`.</span></span>  
  
<a name="singleattached"></a>   
### <a name="single-property-attached-or-otherwise-type-qualified"></a><span data-ttu-id="6a661-136">Propriedade única, anexada ou de tipo qualificado</span><span class="sxs-lookup"><span data-stu-id="6a661-136">Single Property, Attached or Otherwise Type-Qualified</span></span>  
  
```xml  
<object property="(ownerType.propertyName)" .../>  
```  
  
 <span data-ttu-id="6a661-137">Os parênteses indicam que essa propriedade em um <xref:System.Windows.PropertyPath> deve ser construída usando uma qualificação parcial.</span><span class="sxs-lookup"><span data-stu-id="6a661-137">The parentheses indicate that this property in a <xref:System.Windows.PropertyPath> should be constructed using a partial qualification.</span></span> <span data-ttu-id="6a661-138">Ela pode utilizar um namespace de XML para localizar o tipo com um mapeamento apropriado.</span><span class="sxs-lookup"><span data-stu-id="6a661-138">It can use an XML namespace to find the type with an appropriate mapping.</span></span> <span data-ttu-id="6a661-139">O `ownerType` procura tipos aos quais um [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processador tem acesso, por meio de <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarações em cada assembly.</span><span class="sxs-lookup"><span data-stu-id="6a661-139">The `ownerType` searches types that a [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor has access to, through the <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarations in each assembly.</span></span> <span data-ttu-id="6a661-140">A maioria dos aplicativos tem o namespace de XML padrão mapeado para o namespace [!INCLUDE[TLA#tla_wpfxmlnsv1](../../../../includes/tlasharptla-wpfxmlnsv1-md.md)]; portanto, um prefixo geralmente é necessário apenas para tipos personalizados ou tipos fora desse namespace.</span><span class="sxs-lookup"><span data-stu-id="6a661-140">Most applications have the default XML namespace mapped to the [!INCLUDE[TLA#tla_wpfxmlnsv1](../../../../includes/tlasharptla-wpfxmlnsv1-md.md)] namespace, so a prefix is usually only necessary for custom types or types otherwise outside that namespace.</span></span>  <span data-ttu-id="6a661-141">O `propertyName` deve ser o nome de uma propriedade existente no `ownerType`.</span><span class="sxs-lookup"><span data-stu-id="6a661-141">`propertyName` must resolve to be the name of a property existing on the `ownerType`.</span></span> <span data-ttu-id="6a661-142">Em geral, essa sintaxe é usada para um dos casos a seguir:</span><span class="sxs-lookup"><span data-stu-id="6a661-142">This syntax is generally used for one of the following cases:</span></span>  
  
- <span data-ttu-id="6a661-143">O caminho é especificado no [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)], que tem um estilo ou modelo sem um tipo de destino especificado.</span><span class="sxs-lookup"><span data-stu-id="6a661-143">The path is specified in [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] that is in a style or template that does not have a specified Target Type.</span></span> <span data-ttu-id="6a661-144">Um uso qualificado normalmente não é válido para casos diferentes deste, porque, em casos sem estilo e sem modelo, a propriedade existe em uma instância, não em um tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-144">A qualified usage is generally not valid for cases other than this, because in non-style, non-template cases, the property exists on an instance, not a type.</span></span>  
  
- <span data-ttu-id="6a661-145">A propriedade é uma propriedade anexada.</span><span class="sxs-lookup"><span data-stu-id="6a661-145">The property is an attached property.</span></span>  
  
- <span data-ttu-id="6a661-146">A associação é a uma propriedade estática.</span><span class="sxs-lookup"><span data-stu-id="6a661-146">You are binding to a static property.</span></span>  
  
 <span data-ttu-id="6a661-147">Para uso como destino de storyboard, a propriedade especificada como `propertyName` deve ser um <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="6a661-147">For use as storyboard target, the property specified as `propertyName` must be a <xref:System.Windows.DependencyProperty>.</span></span>  
  
<a name="sourcetraversal"></a>   
### <a name="source-traversal-binding-to-hierarchies-of-collections"></a><span data-ttu-id="6a661-148">Passagem de fonte (associação a hierarquias de coleções)</span><span class="sxs-lookup"><span data-stu-id="6a661-148">Source Traversal (Binding to Hierarchies of Collections)</span></span>  
  
```xml  
<object Path="propertyName/propertyNameX" .../>  
```  
  
 <span data-ttu-id="6a661-149">A / nessa sintaxe é usada para navegar dentro de um objeto de fonte de dados hierárquicos; há suporte para várias etapas para a hierarquia com caracteres / sucessivos.</span><span class="sxs-lookup"><span data-stu-id="6a661-149">The / in this syntax is used to navigate within a hierarchical data source object, and multiple steps into the hierarchy with successive / characters are supported.</span></span> <span data-ttu-id="6a661-150">A passagem de fonte explica a posição atual do ponteiro de registro, que é determinada pela sincronização dos dados com a interface do usuário do modo de exibição.</span><span class="sxs-lookup"><span data-stu-id="6a661-150">The source traversal accounts for the current record pointer position, which is determined by synchronizing the data with the UI of its view.</span></span> <span data-ttu-id="6a661-151">Para ver detalhes sobre a associação com objetos de fonte de dados hierárquicos e o conceito do ponteiro de registro atual na associação de dados, consulte [Usar o padrão de detalhes mestre com os dados hierárquicos](../data/how-to-use-the-master-detail-pattern-with-hierarchical-data.md) ou [Visão geral da associação de dados](../data/data-binding-overview.md).</span><span class="sxs-lookup"><span data-stu-id="6a661-151">For details on binding with hierarchical data source objects, and the concept of current record pointer in data binding, see [Use the Master-Detail Pattern with Hierarchical Data](../data/how-to-use-the-master-detail-pattern-with-hierarchical-data.md) or [Data Binding Overview](../data/data-binding-overview.md).</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6a661-152">Superficialmente, essa sintaxe é semelhante a [!INCLUDE[TLA2#tla_xpath](../../../../includes/tla2sharptla-xpath-md.md)].</span><span class="sxs-lookup"><span data-stu-id="6a661-152">Superficially, this syntax resembles [!INCLUDE[TLA2#tla_xpath](../../../../includes/tla2sharptla-xpath-md.md)].</span></span> <span data-ttu-id="6a661-153">Uma verdadeira [!INCLUDE[TLA2#tla_xpath](../../../../includes/tla2sharptla-xpath-md.md)] expressão para associação a um [!INCLUDE[TLA2#tla_xml](../../../../includes/tla2sharptla-xml-md.md)] fonte de dados não é usada como um <xref:System.Windows.Data.Binding.Path%2A> de valor e deverão ser usadas para mutuamente exclusivos <xref:System.Windows.Data.Binding.XPath%2A> propriedade.</span><span class="sxs-lookup"><span data-stu-id="6a661-153">A true [!INCLUDE[TLA2#tla_xpath](../../../../includes/tla2sharptla-xpath-md.md)] expression for binding to an [!INCLUDE[TLA2#tla_xml](../../../../includes/tla2sharptla-xml-md.md)] data source is not used as a <xref:System.Windows.Data.Binding.Path%2A> value and should instead be used for the mutually exclusive <xref:System.Windows.Data.Binding.XPath%2A> property.</span></span>  
  
### <a name="collection-views"></a><span data-ttu-id="6a661-154">Exibições de coleção</span><span class="sxs-lookup"><span data-stu-id="6a661-154">Collection Views</span></span>  
 <span data-ttu-id="6a661-155">Para fazer referência a um modo de exibição de coleção nomeado, o nome do modo de exibição da coleção deve ter o caractere (`#`) como prefixo.</span><span class="sxs-lookup"><span data-stu-id="6a661-155">To reference a named collection view, prefix the collection view name with the hash character (`#`).</span></span>  
  
### <a name="current-record-pointer"></a><span data-ttu-id="6a661-156">Ponteiro de registro atual</span><span class="sxs-lookup"><span data-stu-id="6a661-156">Current Record Pointer</span></span>  
 <span data-ttu-id="6a661-157">Para fazer referência ao ponteiro de registro atual para um modo de exibição de coleção ou um cenário de associação de dados com detalhes mestre, inicie a cadeia de caracteres do caminho com uma barra invertida (`/`).</span><span class="sxs-lookup"><span data-stu-id="6a661-157">To reference the current record pointer for a collection view or master detail data binding scenario, start the path string with a forward slash (`/`).</span></span> <span data-ttu-id="6a661-158">Qualquer caminho após a barra invertida é atravessado por meio do ponteiro de registro atual.</span><span class="sxs-lookup"><span data-stu-id="6a661-158">Any path past the forward slash is traversed starting from the current record pointer.</span></span>  
  
### <a name="multiple-indexers"></a><span data-ttu-id="6a661-159">Vários indexadores</span><span class="sxs-lookup"><span data-stu-id="6a661-159">Multiple Indexers</span></span>  
  
```  
<object Path="[index1,index2...]" .../>  
or  
<object Path="propertyName[index,index2...]" .../>  
```  
  
 <span data-ttu-id="6a661-160">Se determinado objeto dá suporte a vários indexadores, eles podem ser especificados em ordem, de forma semelhante a uma sintaxe de referência de matriz.</span><span class="sxs-lookup"><span data-stu-id="6a661-160">If a given object supports multiple indexers, those indexers can be specified in order, similar to an array referencing syntax.</span></span> <span data-ttu-id="6a661-161">O objeto em questão pode ser o contexto atual ou o valor de uma propriedade que contém um objeto com vários índices.</span><span class="sxs-lookup"><span data-stu-id="6a661-161">The object in question can be either the current context or the value of a property that contains a multiple index object.</span></span>  
  
 <span data-ttu-id="6a661-162">Por padrão, os valores do indexador são tipados usando as características do objeto subjacente.</span><span class="sxs-lookup"><span data-stu-id="6a661-162">By default, the indexer values are typed by using the characteristics of the underlying object.</span></span> <span data-ttu-id="6a661-163">Você pode especificar o tipo do índice, se necessário.</span><span class="sxs-lookup"><span data-stu-id="6a661-163">You can specify the type of the index if necessary.</span></span> <span data-ttu-id="6a661-164">Para obter detalhes sobre a tipagem de indexadores, consulte <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="6a661-164">For details on typing the indexers, see <xref:System.Windows.Data.Binding.Path%2A?displayProperty=nameWithType>.</span></span>  
  
<a name="mixing"></a>   
### <a name="mixing-syntaxes"></a><span data-ttu-id="6a661-165">Misturando sintaxes</span><span class="sxs-lookup"><span data-stu-id="6a661-165">Mixing Syntaxes</span></span>  
 <span data-ttu-id="6a661-166">Cada uma das sintaxes mostradas acima pode ser intercalada.</span><span class="sxs-lookup"><span data-stu-id="6a661-166">Each of the syntaxes shown above can be interspersed.</span></span> <span data-ttu-id="6a661-167">Por exemplo, o seguinte é um exemplo que cria um caminho de propriedade para a cor em um determinado x, y de um `ColorGrid` propriedade que contém uma matriz de grade de pixel de <xref:System.Windows.Media.SolidColorBrush> objetos:</span><span class="sxs-lookup"><span data-stu-id="6a661-167">For instance, the following is an example that creates a property path to the color at a particular x,y of a `ColorGrid` property that contains a pixel grid array of <xref:System.Windows.Media.SolidColorBrush> objects:</span></span>  
  
```xml  
<Rectangle Fill="{Binding ColorGrid[20,30].SolidColorBrushResult}" .../>  
```  
  
### <a name="escapes-for-property-path-strings"></a><span data-ttu-id="6a661-168">Escapes para cadeias de caracteres de caminho da propriedade</span><span class="sxs-lookup"><span data-stu-id="6a661-168">Escapes for Property Path Strings</span></span>  
 <span data-ttu-id="6a661-169">Para determinados objetos de negócios, é possível encontrar um caso em que a cadeia de caracteres de caminho da propriedade requer uma sequência de escape para uma análise correta.</span><span class="sxs-lookup"><span data-stu-id="6a661-169">For certain business objects, you might encounter a case where the property path string requires an escape sequence in order to parse correctly.</span></span> <span data-ttu-id="6a661-170">A necessidade de escape deve ser rara, pois muitos desses caracteres têm problemas semelhantes de interação de nomenclatura em linguagens que costumam ser usadas para definir o objeto de negócios.</span><span class="sxs-lookup"><span data-stu-id="6a661-170">The need to escape should be rare, because many of these characters have similar naming-interaction issues in languages that would typically be used to define the business object.</span></span>  
  
- <span data-ttu-id="6a661-171">Dentro dos indexadores ([]), o caractere de acento circunflexo (^) pula o próximo caractere.</span><span class="sxs-lookup"><span data-stu-id="6a661-171">Inside indexers ([ ]), the caret character (^) escapes the next character.</span></span>  
  
- <span data-ttu-id="6a661-172">É necessário pular (usando entidades XML) alguns caracteres que são especiais para a definição da linguagem XML.</span><span class="sxs-lookup"><span data-stu-id="6a661-172">You must escape (using XML entities) certain characters that are special to the XML language definition.</span></span> <span data-ttu-id="6a661-173">Use `&` para pular o caractere “&”.</span><span class="sxs-lookup"><span data-stu-id="6a661-173">Use `&` to escape the character "&".</span></span> <span data-ttu-id="6a661-174">Use `>` para pular a marca de fim “>”.</span><span class="sxs-lookup"><span data-stu-id="6a661-174">Use `>` to escape the end tag ">".</span></span>  
  
- <span data-ttu-id="6a661-175">É necessário pular (usando a barra invertida `\`) caracteres que são especiais para o comportamento de analisador de XAML do WPF, para o processamento de uma extensão de marcação.</span><span class="sxs-lookup"><span data-stu-id="6a661-175">You must escape (using backslash `\`) characters that are special to the WPF XAML parser behavior for processing a markup extension.</span></span>  
  
    - <span data-ttu-id="6a661-176">A barra invertida (`\`) é o caractere de escape.</span><span class="sxs-lookup"><span data-stu-id="6a661-176">Backslash (`\`) is the escape character itself.</span></span>  
  
    - <span data-ttu-id="6a661-177">O sinal de igual (`=`) separa o nome da propriedade do valor da propriedade.</span><span class="sxs-lookup"><span data-stu-id="6a661-177">The equal sign (`=`) separates property name from property value.</span></span>  
  
    - <span data-ttu-id="6a661-178">A vírgula (`,`) separa as propriedades.</span><span class="sxs-lookup"><span data-stu-id="6a661-178">Comma (`,`) separates properties.</span></span>  
  
    - <span data-ttu-id="6a661-179">A chave direita (`}`) é o final de uma extensão de marcação.</span><span class="sxs-lookup"><span data-stu-id="6a661-179">The right curly brace (`}`) is the end of a markup extension.</span></span>  
  
> [!NOTE]
>  <span data-ttu-id="6a661-180">Tecnicamente, esses escapes também funcionam para um caminho da propriedade de storyboard; contudo, você normalmente atravessa modelos de objeto para objetos existentes do WPF e o escape deve ser desnecessário.</span><span class="sxs-lookup"><span data-stu-id="6a661-180">Technically, these escapes work for a storyboard property path also, but you are usually traversing object models for existing WPF objects, and escaping should be unnecessary.</span></span>  
  
<a name="databinding_sa"></a>   
## <a name="propertypath-for-animation-targets"></a><span data-ttu-id="6a661-181">PropertyPath para destinos de animação</span><span class="sxs-lookup"><span data-stu-id="6a661-181">PropertyPath for Animation Targets</span></span>  
 <span data-ttu-id="6a661-182">A propriedade de destino de uma animação deve ser uma propriedade de dependência que aceita um <xref:System.Windows.Freezable> ou um tipo primitivo.</span><span class="sxs-lookup"><span data-stu-id="6a661-182">The target property of an animation must be a dependency property that takes either a <xref:System.Windows.Freezable> or a primitive type.</span></span> <span data-ttu-id="6a661-183">No entanto, a propriedade direcionada em um tipo e a propriedade animada eventual podem existir em diferentes objetos.</span><span class="sxs-lookup"><span data-stu-id="6a661-183">However, the targeted property on a type and the eventual animated property can exist on different objects.</span></span> <span data-ttu-id="6a661-184">No caso de animações, um caminho da propriedade é usado para definir a conexão entre a propriedade do objeto de destino da animação nomeada e a propriedade de animação de destino pretendida, atravessando relações de propriedade do objeto nos valores da propriedade.</span><span class="sxs-lookup"><span data-stu-id="6a661-184">For animations, a property path is used to define the connection between the named animation target object's property and the intended target animation property, by traversing object-property relationships in the property values.</span></span>  
  
<a name="general"></a>   
### <a name="general-object-property-considerations-for-animations"></a><span data-ttu-id="6a661-185">Considerações sobre a propriedade de objeto geral para animações</span><span class="sxs-lookup"><span data-stu-id="6a661-185">General Object-Property Considerations for Animations</span></span>  
 <span data-ttu-id="6a661-186">Para obter mais informações sobre conceitos de animação em geral, consulte [Visão geral de storyboards](../graphics-multimedia/storyboards-overview.md) e [Visão geral da animação](../graphics-multimedia/animation-overview.md).</span><span class="sxs-lookup"><span data-stu-id="6a661-186">For more information on animation concepts in general, see [Storyboards Overview](../graphics-multimedia/storyboards-overview.md) and [Animation Overview](../graphics-multimedia/animation-overview.md).</span></span>  
  
 <span data-ttu-id="6a661-187">O tipo de valor ou a propriedade sendo animada deve ser um <xref:System.Windows.Freezable> tipo ou um primitivo.</span><span class="sxs-lookup"><span data-stu-id="6a661-187">The value type or the property being animated must be either a <xref:System.Windows.Freezable> type or a primitive.</span></span> <span data-ttu-id="6a661-188">A propriedade que inicia o caminho deve ser resolvido para ser o nome de uma propriedade de dependência que existe em especificado <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-188">The property that starts the path must resolve to be the name of a dependency property that exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>  
  
 <span data-ttu-id="6a661-189">Para dar suporte à clonagem para animar uma <xref:System.Windows.Freezable> que já está congelado, o objeto especificado por <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> deve ser um <xref:System.Windows.FrameworkElement> ou <xref:System.Windows.FrameworkContentElement> classe derivada.</span><span class="sxs-lookup"><span data-stu-id="6a661-189">In order to support cloning for animating a <xref:System.Windows.Freezable> that is already frozen, the object specified by <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> must be a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> derived class.</span></span>  
  
<a name="singlestepanim"></a>   
### <a name="single-property-on-the-target-object"></a><span data-ttu-id="6a661-190">Propriedade única no objeto de destino</span><span class="sxs-lookup"><span data-stu-id="6a661-190">Single Property on the Target Object</span></span>  
  
```xml  
<animation Storyboard.TargetProperty="propertyName" .../>  
```  
  
 <span data-ttu-id="6a661-191">`propertyName` deve ser o nome da propriedade de dependência que existe em especificado <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-191">`propertyName` must resolve to be the name of a dependency property that exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>  
  
<a name="indirectanim"></a>   
### <a name="indirect-property-targeting"></a><span data-ttu-id="6a661-192">Direcionamento indireto de propriedade</span><span class="sxs-lookup"><span data-stu-id="6a661-192">Indirect Property Targeting</span></span>  
  
```xml  
<animation Storyboard.TargetProperty="propertyName.propertyName2" .../>  
```  
  
 <span data-ttu-id="6a661-193">`propertyName` deve ser uma propriedade que seja um <xref:System.Windows.Freezable> tipo de valor ou um primitivo, que existe no especificado <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-193">`propertyName` must be a property that is either a <xref:System.Windows.Freezable> value type or a primitive, which exists on the specified <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> type.</span></span>  
  
 <span data-ttu-id="6a661-194">`propertyName2` deve ser o nome de uma propriedade de dependência que existe no objeto que é o valor de `propertyName`.</span><span class="sxs-lookup"><span data-stu-id="6a661-194">`propertyName2` must be the name of a dependency property that exists on the object that is the value of `propertyName`.</span></span> <span data-ttu-id="6a661-195">Em outras palavras, `propertyName2` deve existir como uma propriedade de dependência no tipo que é o `propertyName` <xref:System.Windows.DependencyProperty.PropertyType%2A>.</span><span class="sxs-lookup"><span data-stu-id="6a661-195">In other words, `propertyName2` must exist as a dependency property on the type that is the `propertyName` <xref:System.Windows.DependencyProperty.PropertyType%2A>.</span></span>  
  
 <span data-ttu-id="6a661-196">O direcionamento indireto de animações é necessário por causa dos estilos e modelos aplicados.</span><span class="sxs-lookup"><span data-stu-id="6a661-196">Indirect targeting of animations is necessary because of applied styles and templates.</span></span> <span data-ttu-id="6a661-197">Para direcionar uma animação, você precisa de uma <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> em um objeto de destino e que o nome é estabelecido por [X:Name](../../xaml-services/x-name-directive.md) ou <xref:System.Windows.FrameworkElement.Name%2A>.</span><span class="sxs-lookup"><span data-stu-id="6a661-197">In order to target an animation, you need a <xref:System.Windows.Media.Animation.Storyboard.TargetName%2A> on a target object, and that name is established by [x:Name](../../xaml-services/x-name-directive.md) or <xref:System.Windows.FrameworkElement.Name%2A>.</span></span> <span data-ttu-id="6a661-198">Embora os elementos de modelo e estilo também possam ter nomes, esses nomes são válidos somente dentro do escopo de nome do estilo e do modelo.</span><span class="sxs-lookup"><span data-stu-id="6a661-198">Although template and style elements also can have names, those names are only valid within the namescope of the style and template.</span></span> <span data-ttu-id="6a661-199">(Se os modelos e estilos compartilhassem os escopos de nome com a marcação do aplicativo, os nomes não poderiam ser exclusivos.</span><span class="sxs-lookup"><span data-stu-id="6a661-199">(If templates and styles did share namescopes with application markup, names couldn't be unique.</span></span> <span data-ttu-id="6a661-200">Os estilos e modelos são literalmente compartilhados entre instâncias e teriam nomes duplicados.) Consequentemente, se as propriedades individuais de um elemento que você gostaria de animar viessem de um estilo ou modelo, seria necessário começar com uma instância de elemento nomeada que não vem de um modelo de estilo e, em seguida, direcionar para a árvore visual de estilo ou modelo a fim de chegar à propriedade que deseja animar.</span><span class="sxs-lookup"><span data-stu-id="6a661-200">The styles and templates are literally shared between instances and would perpetuate duplicate names.) Thus, if the individual properties of an element that you might wish to animate came from a style or template, you need to start with a named element instance that is not from a style template, and then target into the style or template visual tree to arrive at the property you wish to animate.</span></span>  
  
 <span data-ttu-id="6a661-201">Por exemplo, o <xref:System.Windows.Controls.Panel.Background%2A> propriedade de um <xref:System.Windows.Controls.Panel> é uma completa <xref:System.Windows.Media.Brush> (na verdade, um <xref:System.Windows.Media.SolidColorBrush>) que veio de um modelo de tema.</span><span class="sxs-lookup"><span data-stu-id="6a661-201">For instance, the <xref:System.Windows.Controls.Panel.Background%2A> property of a <xref:System.Windows.Controls.Panel> is a complete <xref:System.Windows.Media.Brush> (actually a <xref:System.Windows.Media.SolidColorBrush>) that came from a theme template.</span></span> <span data-ttu-id="6a661-202">Para animar uma <xref:System.Windows.Media.Brush> completamente, seria necessário haver uma BrushAnimation (provavelmente uma para cada <xref:System.Windows.Media.Brush> tipo) e não há nenhum tal tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-202">To animate a <xref:System.Windows.Media.Brush> completely, there would need to be a BrushAnimation (probably one for every <xref:System.Windows.Media.Brush> type) and there is no such type.</span></span> <span data-ttu-id="6a661-203">Para animar um pincel, você precisa animar as propriedades de um determinado <xref:System.Windows.Media.Brush> tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-203">To animate a Brush, you instead animate properties of a particular <xref:System.Windows.Media.Brush> type.</span></span> <span data-ttu-id="6a661-204">Você precisará obter do <xref:System.Windows.Media.SolidColorBrush> para seus <xref:System.Windows.Media.SolidColorBrush.Color%2A> para aplicar um <xref:System.Windows.Media.Animation.ColorAnimation> lá.</span><span class="sxs-lookup"><span data-stu-id="6a661-204">You need to get from <xref:System.Windows.Media.SolidColorBrush> to its <xref:System.Windows.Media.SolidColorBrush.Color%2A> to apply a <xref:System.Windows.Media.Animation.ColorAnimation> there.</span></span> <span data-ttu-id="6a661-205">O caminho da propriedade para esse exemplo seria `Background.Color`.</span><span class="sxs-lookup"><span data-stu-id="6a661-205">The property path for this example would be `Background.Color`.</span></span>  
  
<a name="attachedanim"></a>   
### <a name="attached-properties"></a><span data-ttu-id="6a661-206">Propriedades anexadas</span><span class="sxs-lookup"><span data-stu-id="6a661-206">Attached Properties</span></span>  
  
```xml  
<animation Storyboard.TargetProperty="(ownerType.propertyName)" .../>  
```  
  
 <span data-ttu-id="6a661-207">Os parênteses indicam que essa propriedade em um <xref:System.Windows.PropertyPath> deve ser construída usando uma qualificação parcial.</span><span class="sxs-lookup"><span data-stu-id="6a661-207">The parentheses indicate that this property in a <xref:System.Windows.PropertyPath> should be constructed using a partial qualification.</span></span> <span data-ttu-id="6a661-208">Ela pode usar um namespace de XML para encontrar o tipo.</span><span class="sxs-lookup"><span data-stu-id="6a661-208">It can use an XML namespace to find the type.</span></span> <span data-ttu-id="6a661-209">O `ownerType` procura tipos aos quais um [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processador tem acesso, por meio de <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarações em cada assembly.</span><span class="sxs-lookup"><span data-stu-id="6a661-209">The `ownerType` searches types that a [!INCLUDE[TLA2#tla_xaml](../../../../includes/tla2sharptla-xaml-md.md)] processor has access to, through the <xref:System.Windows.Markup.XmlnsDefinitionAttribute> declarations in each assembly.</span></span> <span data-ttu-id="6a661-210">A maioria dos aplicativos tem o namespace de XML padrão mapeado para o namespace [!INCLUDE[TLA#tla_wpfxmlnsv1](../../../../includes/tlasharptla-wpfxmlnsv1-md.md)]; portanto, um prefixo geralmente é necessário apenas para tipos personalizados ou tipos fora desse namespace.</span><span class="sxs-lookup"><span data-stu-id="6a661-210">Most applications have the default XML namespace mapped to the [!INCLUDE[TLA#tla_wpfxmlnsv1](../../../../includes/tlasharptla-wpfxmlnsv1-md.md)] namespace, so a prefix is usually only necessary for custom types or types otherwise outside that namespace.</span></span> <span data-ttu-id="6a661-211">O `propertyName` deve ser o nome de uma propriedade existente no `ownerType`.</span><span class="sxs-lookup"><span data-stu-id="6a661-211">`propertyName` must resolve to be the name of a property existing on the `ownerType`.</span></span> <span data-ttu-id="6a661-212">A propriedade especificada como `propertyName` deve ser um <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="6a661-212">The property specified as `propertyName` must be a <xref:System.Windows.DependencyProperty>.</span></span> <span data-ttu-id="6a661-213">(Todas as propriedades anexadas do [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] são implementadas como propriedades de dependência; portanto, o problema diz respeito apenas a propriedades anexadas personalizadas.)</span><span class="sxs-lookup"><span data-stu-id="6a661-213">(All [!INCLUDE[TLA2#tla_winclient](../../../../includes/tla2sharptla-winclient-md.md)] attached properties are implemented as dependency properties, so this issue is only of concern for custom attached properties.)</span></span>  
  
<a name="indexanim"></a>   
### <a name="indexers"></a><span data-ttu-id="6a661-214">Indexadores</span><span class="sxs-lookup"><span data-stu-id="6a661-214">Indexers</span></span>  
  
```xml  
<animation Storyboard.TargetProperty="propertyName.propertyName2[index].propertyName3" .../>  
```  
  
 <span data-ttu-id="6a661-215">A maioria das propriedades de dependência ou <xref:System.Windows.Freezable> tipos não dão suporte a um indexador.</span><span class="sxs-lookup"><span data-stu-id="6a661-215">Most dependency properties or <xref:System.Windows.Freezable> types do not support an indexer.</span></span> <span data-ttu-id="6a661-216">Portanto, o único uso para um indexador em um caminho de animação é em uma posição intermediária entre a propriedade que inicia a cadeia no destino nomeado e a propriedade animada eventual.</span><span class="sxs-lookup"><span data-stu-id="6a661-216">Therefore, the only usage for an indexer in an animation path is at an intermediate position between the property that starts the chain on the named target and the eventual animated property.</span></span> <span data-ttu-id="6a661-217">Na sintaxe fornecida, é `propertyName2`.</span><span class="sxs-lookup"><span data-stu-id="6a661-217">In the provided syntax, that is `propertyName2`.</span></span> <span data-ttu-id="6a661-218">Por exemplo, o uso de um indexador pode ser necessário se a propriedade intermediária é uma coleção, como <xref:System.Windows.Media.TransformGroup>, em um caminho de propriedade, como `RenderTransform.Children[1].Angle`.</span><span class="sxs-lookup"><span data-stu-id="6a661-218">For instance, an indexer usage might be necessary if the intermediate property is a collection such as <xref:System.Windows.Media.TransformGroup>, in a property path such as `RenderTransform.Children[1].Angle`.</span></span>  
  
<a name="ppincode"></a>   
## <a name="propertypath-in-code"></a><span data-ttu-id="6a661-219">PropertyPath em código</span><span class="sxs-lookup"><span data-stu-id="6a661-219">PropertyPath in Code</span></span>  
 <span data-ttu-id="6a661-220">Uso em código de <xref:System.Windows.PropertyPath>, incluindo como construir uma <xref:System.Windows.PropertyPath>, está documentado no tópico de referência para <xref:System.Windows.PropertyPath>.</span><span class="sxs-lookup"><span data-stu-id="6a661-220">Code usage for <xref:System.Windows.PropertyPath>, including how to construct a <xref:System.Windows.PropertyPath>, is documented in the reference topic for <xref:System.Windows.PropertyPath>.</span></span>  
  
 <span data-ttu-id="6a661-221">Em geral, <xref:System.Windows.PropertyPath> foi projetado para usar dois construtores diferentes, um para os usos de associação e os usos de animação mais simples e outro para os usos de animação complexos.</span><span class="sxs-lookup"><span data-stu-id="6a661-221">In general, <xref:System.Windows.PropertyPath> is designed to use two different constructors, one for the binding usages and simplest animation usages, and one for the complex animation usages.</span></span> <span data-ttu-id="6a661-222">Use o <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> assinatura para usos de associação, em que o objeto é uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="6a661-222">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> signature for binding usages, where the object is a string.</span></span> <span data-ttu-id="6a661-223">Use o <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> assinatura para trajetórias de animação, em que o objeto é um <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="6a661-223">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.Object%29> signature for one-step animation paths, where the object is a <xref:System.Windows.DependencyProperty>.</span></span> <span data-ttu-id="6a661-224">Use o <xref:System.Windows.PropertyPath.%23ctor%28System.String%2CSystem.Object%5B%5D%29> assinatura para animações complexas.</span><span class="sxs-lookup"><span data-stu-id="6a661-224">Use the <xref:System.Windows.PropertyPath.%23ctor%28System.String%2CSystem.Object%5B%5D%29> signature for complex animations.</span></span> <span data-ttu-id="6a661-225">Esse último construtor usa uma cadeia de caracteres de token para o primeiro parâmetro e uma matriz de objetos que preenchem posições na cadeia de caracteres de token para definir uma relação de caminho da propriedade.</span><span class="sxs-lookup"><span data-stu-id="6a661-225">This latter constructor uses a token string for the first parameter and an array of objects that fill positions in the token string to define a property path relationship.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="6a661-226">Consulte também</span><span class="sxs-lookup"><span data-stu-id="6a661-226">See also</span></span>

- <xref:System.Windows.PropertyPath>
- [<span data-ttu-id="6a661-227">Visão geral da vinculação de dados</span><span class="sxs-lookup"><span data-stu-id="6a661-227">Data Binding Overview</span></span>](../data/data-binding-overview.md)
- [<span data-ttu-id="6a661-228">Visão geral de storyboards</span><span class="sxs-lookup"><span data-stu-id="6a661-228">Storyboards Overview</span></span>](../graphics-multimedia/storyboards-overview.md)
