---
title: Derivando de WebRequest
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords:
- WebRequest class, pluggable protocols
- protocol-specific request handler
- sending data, pluggable protocols
- pluggable protocols, class criteria
- Internet, pluggable protocols
- receiving data, pluggable protocols
- protocols, pluggable
ms.assetid: 9810c177-973e-43d7-823c-14960bd625ea
caps.latest.revision: "9"
author: mcleblanc
ms.author: markl
manager: markl
ms.workload: dotnet
ms.openlocfilehash: 83579c25c154462cb21488acf9fcf84999b9a2d4
ms.sourcegitcommit: 16186c34a957fdd52e5db7294f291f7530ac9d24
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/22/2017
---
# <a name="deriving-from-webrequest"></a><span data-ttu-id="e2a9f-102">Derivando de WebRequest</span><span class="sxs-lookup"><span data-stu-id="e2a9f-102">Deriving from WebRequest</span></span>
<span data-ttu-id="e2a9f-103">A classe <xref:System.Net.WebRequest> é uma classe base abstrata que fornece os métodos e as propriedades básicas para criar um manipulador de solicitação específico ao protocolo que se ajusta ao modelo de protocolo conectável do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-103">The <xref:System.Net.WebRequest> class is an abstract base class that provides the basic methods and properties for creating a protocol-specific request handler that fits the .NET Framework pluggable protocol model.</span></span> <span data-ttu-id="e2a9f-104">Os aplicativos que usam a classe **WebRequest** podem solicitar dados usando qualquer protocolo com suporte sem a necessidade de especificar o protocolo usado.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-104">Applications that use the **WebRequest** class can request data using any supported protocol without needing to specify the protocol used.</span></span>  
  
 <span data-ttu-id="e2a9f-105">Dois critérios devem ser atendidos para que uma classe específica ao protocolo seja usada como um protocolo conectável: a classe deve implementar a interface <xref:System.Net.IWebRequestCreate> e deve se registrar com o método <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-105">Two criteria must be met in order for a protocol-specific class to be used as a pluggable protocol: The class must implement the <xref:System.Net.IWebRequestCreate> interface, and it must register with the <xref:System.Net.WebRequest.RegisterPrefix%2A?displayProperty=nameWithType> method.</span></span> <span data-ttu-id="e2a9f-106">A classe deve substituir todos os métodos abstratos e as propriedades de **WebRequest** para fornecer a interface conectável.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-106">The class must override all the abstract methods and properties of **WebRequest** to provide the pluggable interface.</span></span>  
  
 <span data-ttu-id="e2a9f-107">As instâncias de **WebRequest** são destinadas a um único uso; se desejar fazer outra solicitação, crie uma nova **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-107">**WebRequest** instances are intended for one-time use; if you want to make another request, create a new **WebRequest**.</span></span> <span data-ttu-id="e2a9f-108">**WebRequest** dá suporte à interface <xref:System.Runtime.Serialization.ISerializable> para permitir aos desenvolvedores serializar uma **WebRequest** de modelo e, em seguida, reconstruir o modelo para solicitações adicionais.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-108">**WebRequest** supports the <xref:System.Runtime.Serialization.ISerializable> interface to enable developers to serialize a template **WebRequest** and then reconstruct the template for additional requests.</span></span>  
  
## <a name="iwebrequest-create-method"></a><span data-ttu-id="e2a9f-109">Método IWebRequest Create</span><span class="sxs-lookup"><span data-stu-id="e2a9f-109">IWebRequest Create Method</span></span>  
 <span data-ttu-id="e2a9f-110">O método <xref:System.Net.IWebRequestCreate.Create%2A> é responsável por inicializar uma nova instância da classe específica ao protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-110">The <xref:System.Net.IWebRequestCreate.Create%2A> method is responsible for initializing a new instance of the protocol-specific class.</span></span> <span data-ttu-id="e2a9f-111">Quando uma nova **WebRequest** é criada, o método <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> corresponde ao URI solicitado com os prefixos URI registrados com o método **RegisterPrefix**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-111">When a new **WebRequest** is created, the <xref:System.Net.WebRequest.Create%2A?displayProperty=nameWithType> method matches the requested URI with the URI prefixes registered with the **RegisterPrefix** method.</span></span> <span data-ttu-id="e2a9f-112">O método **Create** do descendente específico ao protocolo correto deve retornar uma instância inicializada do descendente com a capacidade de executar uma transação de solicitação/resposta padrão para o protocolo sem a necessidade de modificar os campos específicos ao protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-112">The **Create** method of the proper protocol-specific descendant must return an initialized instance of the descendant capable of performing a standard request/response transaction for the protocol without needing any protocol-specific fields modified.</span></span>  
  
## <a name="connectiongroupname-property"></a><span data-ttu-id="e2a9f-113">Propriedade ConnectionGroupName</span><span class="sxs-lookup"><span data-stu-id="e2a9f-113">ConnectionGroupName Property</span></span>  
 <span data-ttu-id="e2a9f-114">A propriedade <xref:System.Net.WebRequest.ConnectionGroupName%2A> é usada para nomear um grupo de conexões com um recurso, de modo que várias solicitações possam ser feitas em uma única conexão.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-114">The <xref:System.Net.WebRequest.ConnectionGroupName%2A> property is used to name a group of connections to a resource so that multiple requests can be made over a single connection.</span></span> <span data-ttu-id="e2a9f-115">Para implementar o compartilhamento de conexão, você deve usar um método específico ao protocolo de pooling e atribuição de conexões.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-115">To implement connection-sharing, you must use a protocol-specific method of pooling and assigning connections.</span></span> <span data-ttu-id="e2a9f-116">Por exemplo, a classe <xref:System.Net.ServicePointManager> fornecida implementa o compartilhamento de conexão na classe <xref:System.Net.HttpWebRequest>.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-116">For example, the provided <xref:System.Net.ServicePointManager> class implements connection sharing for the <xref:System.Net.HttpWebRequest> class.</span></span> <span data-ttu-id="e2a9f-117">A classe **ServicePointManager** cria um <xref:System.Net.ServicePoint> que fornece uma conexão com um servidor específico para cada grupo de conexão.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-117">The **ServicePointManager** class creates a <xref:System.Net.ServicePoint> that provides a connection to a specific server for each connection group.</span></span>  
  
## <a name="contentlength-property"></a><span data-ttu-id="e2a9f-118">Propriedade ContentLength</span><span class="sxs-lookup"><span data-stu-id="e2a9f-118">ContentLength Property</span></span>  
 <span data-ttu-id="e2a9f-119">A propriedade <xref:System.Net.WebRequest.ContentLength%2A> especifica o número de bytes de dados que serão enviados para o servidor ao carregar dados.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-119">The <xref:System.Net.WebRequest.ContentLength%2A> property specifies the number of bytes of data that will be sent to the server when uploading data.</span></span>  
  
 <span data-ttu-id="e2a9f-120">Normalmente, a propriedade <xref:System.Net.WebRequest.Method%2A> deve ser definida para indicar que um upload está ocorrendo quando a propriedade **ContentLength** é definida com um valor maior que zero.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-120">Typically the <xref:System.Net.WebRequest.Method%2A> property must be set to indicate that an upload is taking place when the **ContentLength** property is set to a value greater than zero.</span></span>  
  
## <a name="contenttype-property"></a><span data-ttu-id="e2a9f-121">Propriedade ContentType</span><span class="sxs-lookup"><span data-stu-id="e2a9f-121">ContentType Property</span></span>  
 <span data-ttu-id="e2a9f-122">A propriedade <xref:System.Net.WebRequest.ContentType%2A> fornece informações especiais que o protocolo exige que sejam enviadas ao servidor para identificar o tipo de conteúdo que está sendo enviado.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-122">The <xref:System.Net.WebRequest.ContentType%2A> property provides any special information that your protocol requires you to send to the server to identify the type of content that you are sending.</span></span> <span data-ttu-id="e2a9f-123">Normalmente, esse é o tipo de conteúdo MIME dos dados carregados.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-123">Typically this is the MIME content type of any data uploaded.</span></span>  
  
## <a name="credentials-property"></a><span data-ttu-id="e2a9f-124">Propriedade Credentials</span><span class="sxs-lookup"><span data-stu-id="e2a9f-124">Credentials Property</span></span>  
 <span data-ttu-id="e2a9f-125">A propriedade <xref:System.Net.WebRequest.Credentials%2A> contém as informações necessárias para autenticar a solicitação no servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-125">The <xref:System.Net.WebRequest.Credentials%2A> property contains information needed to authenticate the request with the server.</span></span> <span data-ttu-id="e2a9f-126">É necessário implementar os detalhes do processo de autenticação do protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-126">You must implement the details of the authentication process for your protocol.</span></span> <span data-ttu-id="e2a9f-127">A classe <xref:System.Net.AuthenticationManager> é responsável por autenticar as solicitações e fornecer um token de autenticação.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-127">The <xref:System.Net.AuthenticationManager> class is responsible for authenticating requests and providing an authentication token.</span></span> <span data-ttu-id="e2a9f-128">A classe que fornece as credenciais usadas pelo protocolo deve implementar a interface <xref:System.Net.ICredentials>.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-128">The class that provides the credentials used by your protocol must implement the <xref:System.Net.ICredentials> interface.</span></span>  
  
## <a name="headers-property"></a><span data-ttu-id="e2a9f-129">Propriedade Headers</span><span class="sxs-lookup"><span data-stu-id="e2a9f-129">Headers Property</span></span>  
 <span data-ttu-id="e2a9f-130">A propriedade <xref:System.Net.WebRequest.Headers%2A> contém uma coleção arbitrária de pares de nome/valor de metadados associados à solicitação.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-130">The <xref:System.Net.WebRequest.Headers%2A> property contains an arbitrary collection of name/value pairs of metadata associated with the request.</span></span> <span data-ttu-id="e2a9f-131">Todos os metadados necessários para o protocolo que podem ser expressos como um par nome/valor podem ser incluídos na propriedade **Headers**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-131">Any metadata needed by the protocol that can be expressed as a name/value pair can be included in the **Headers** property.</span></span> <span data-ttu-id="e2a9f-132">Normalmente, essas informações devem ser definidas antes de chamar os métodos <xref:System.Net.WebRequest.GetRequestStream%2A> ou <xref:System.Net.WebRequest.GetResponse%2A>; depois que a solicitação for feita, os metadados serão considerados somente leitura.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-132">Typically this information must be set before calling the <xref:System.Net.WebRequest.GetRequestStream%2A> or <xref:System.Net.WebRequest.GetResponse%2A> methods; once the request has been made, the metadata is considered read-only.</span></span>  
  
 <span data-ttu-id="e2a9f-133">Não é necessário usar a propriedade **Headers** para usar os metadados do cabeçalho.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-133">You are not required to use the **Headers** property to use header metadata.</span></span> <span data-ttu-id="e2a9f-134">Os metadados específicos ao protocolo podem ser expostos como propriedades; por exemplo, a propriedade <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> expõe o cabeçalho HTTP **User-Agent**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-134">Protocol-specific metadata can be exposed as properties; for example, the <xref:System.Net.HttpWebRequest.UserAgent%2A?displayProperty=nameWithType> property exposes the **User-Agent** HTTP header.</span></span> <span data-ttu-id="e2a9f-135">Ao expor os metadados do cabeçalho como uma propriedade, você não deve permitir que a mesma propriedade seja definida usando a propriedade **Headers**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-135">When you expose header metadata as a property, you should not allow the same property to be set using the **Headers** property.</span></span>  
  
## <a name="method-property"></a><span data-ttu-id="e2a9f-136">Propriedade Method</span><span class="sxs-lookup"><span data-stu-id="e2a9f-136">Method Property</span></span>  
 <span data-ttu-id="e2a9f-137">A propriedade <xref:System.Net.WebRequest.Method%2A> contém o verbo ou a ação que a solicitação está solicitando que o servidor execute.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-137">The <xref:System.Net.WebRequest.Method%2A> property contains the verb or action that the request is asking the server to perform.</span></span> <span data-ttu-id="e2a9f-138">O padrão para a propriedade **Method** deve permitir uma ação de solicitação/resposta padrão sem a necessidade de definir propriedades específicas ao protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-138">The default for the **Method** property must enable a standard request/response action without requiring any protocol-specific properties to be set.</span></span> <span data-ttu-id="e2a9f-139">Por exemplo, o método <xref:System.Net.HttpWebResponse.Method%2A> usa GET como padrão, que solicita um recurso de um servidor Web e retorna a resposta.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-139">For example, the <xref:System.Net.HttpWebResponse.Method%2A> method defaults to GET, which requests a resource from a Web server and returns the response.</span></span>  
  
 <span data-ttu-id="e2a9f-140">Normalmente, a propriedade **ContentLength** deve ser definida com um valor maior que zero quando a propriedade **Method** é definida com um verbo ou uma ação que indica que um upload está ocorrendo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-140">Typically the **ContentLength** property must be set to a value greater than zero when the **Method** property is set to a verb or action that indicates that an upload is taking place.</span></span>  
  
## <a name="preauthenticate-property"></a><span data-ttu-id="e2a9f-141">Propriedade PreAuthenticate</span><span class="sxs-lookup"><span data-stu-id="e2a9f-141">PreAuthenticate Property</span></span>  
 <span data-ttu-id="e2a9f-142">Os aplicativos definem a propriedade <xref:System.Net.WebRequest.PreAuthenticate%2A> para indicar que as informações de autenticação devem ser enviadas com a solicitação inicial, em vez de aguardar um desafio de autenticação.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-142">Applications set the <xref:System.Net.WebRequest.PreAuthenticate%2A> property to indicate that authentication information should be sent with the initial request rather than waiting for an authentication challenge.</span></span> <span data-ttu-id="e2a9f-143">A propriedade **PreAuthenticate** só será significativa se o protocolo der suporte a credenciais de autenticação enviadas com a solicitação inicial.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-143">The **PreAuthenticate** property is only meaningful if the protocol supports authentication credentials sent with the initial request.</span></span>  
  
## <a name="proxy-property"></a><span data-ttu-id="e2a9f-144">Propriedade Proxy</span><span class="sxs-lookup"><span data-stu-id="e2a9f-144">Proxy Property</span></span>  
 <span data-ttu-id="e2a9f-145">A propriedade <xref:System.Net.WebRequest.Proxy%2A> contém uma interface <xref:System.Net.IWebProxy> que é usada para acessar o recurso solicitado.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-145">The <xref:System.Net.WebRequest.Proxy%2A> property contains an <xref:System.Net.IWebProxy> interface that is used to access the requested resource.</span></span> <span data-ttu-id="e2a9f-146">A propriedade **Proxy** só será significativa se o protocolo der suporte a solicitações com proxy.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-146">The **Proxy** property is meaningful only if your protocol supports proxied requests.</span></span> <span data-ttu-id="e2a9f-147">É necessário definir o proxy padrão se for exigido um para o protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-147">You must set the default proxy if one is required by your protocol.</span></span>  
  
 <span data-ttu-id="e2a9f-148">Em alguns ambientes, como quando protegido por um firewall corporativo, o protocolo pode precisar usar um proxy.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-148">In some environments, such as behind a corporate firewall, your protocol might be required to use a proxy.</span></span> <span data-ttu-id="e2a9f-149">Nesse caso, é necessário implementar a interface **IWebProxy** para criar uma classe proxy que funcionará para o protocolo.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-149">In that case, you must implement the **IWebProxy** interface to create a proxy class that will work for your protocol.</span></span>  
  
## <a name="requesturi-property"></a><span data-ttu-id="e2a9f-150">Propriedade RequestUri</span><span class="sxs-lookup"><span data-stu-id="e2a9f-150">RequestUri Property</span></span>  
 <span data-ttu-id="e2a9f-151">A propriedade <xref:System.Net.WebRequest.RequestUri%2A> contém o URI que foi passado para o método **WebRequest.Create**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-151">The <xref:System.Net.WebRequest.RequestUri%2A> property contains the URI that was passed to the **WebRequest.Create** method.</span></span> <span data-ttu-id="e2a9f-152">Ela é somente leitura e não poderá ser alterada depois que **WebRequest** for criada.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-152">It is read-only and cannot be changed once the **WebRequest** has been created.</span></span> <span data-ttu-id="e2a9f-153">Se o protocolo der suporte ao redirecionamento, a resposta poderá ser recebida de um recurso identificado por outro URI.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-153">If your protocol supports redirection, the response can come from a resource identified by a different URI.</span></span> <span data-ttu-id="e2a9f-154">Se você precisar fornecer acesso ao URI que respondeu, deverá fornecer uma propriedade adicional que contém esse URI.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-154">If you need to provide access to the URI that responded, you must provide an additional property containing that URI.</span></span>  
  
## <a name="timeout-property"></a><span data-ttu-id="e2a9f-155">Propriedade Timeout</span><span class="sxs-lookup"><span data-stu-id="e2a9f-155">Timeout Property</span></span>  
 <span data-ttu-id="e2a9f-156">A propriedade <xref:System.Net.WebRequest.Timeout%2A> contém o tempo, em milissegundos, de espera antes que a solicitação atinja o tempo limite e gere uma exceção.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-156">The <xref:System.Net.WebRequest.Timeout%2A> property contains the length of time, in milliseconds, to wait before the request times out and throws an exception.</span></span> <span data-ttu-id="e2a9f-157">**Timeout** se aplica somente a solicitações síncronas feitas com o método <xref:System.Net.WebRequest.GetResponse%2A>; as solicitações assíncronas devem usar o método <xref:System.Net.WebRequest.Abort%2A> para cancelar uma solicitação pendente.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-157">**Timeout** applies only to synchronous requests made with the <xref:System.Net.WebRequest.GetResponse%2A> method; asynchronous requests must use the <xref:System.Net.WebRequest.Abort%2A> method to cancel a pending request.</span></span>  
  
 <span data-ttu-id="e2a9f-158">A configuração da propriedade **Timeout** só será significativa se a classe específica ao protocolo implementar um processo de tempo limite.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-158">Setting the **Timeout** property is meaningful only if the protocol-specific class implements a time-out process.</span></span>  
  
## <a name="abort-method"></a><span data-ttu-id="e2a9f-159">Método Abort</span><span class="sxs-lookup"><span data-stu-id="e2a9f-159">Abort Method</span></span>  
 <span data-ttu-id="e2a9f-160">O método <xref:System.Net.WebRequest.Abort%2A> cancela uma solicitação assíncrona pendente para um servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-160">The <xref:System.Net.WebRequest.Abort%2A> method cancels a pending asynchronous request to a server.</span></span> <span data-ttu-id="e2a9f-161">Depois que a solicitação for cancelada, uma chamada a **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream** ou **EndGetRequestStream** gerará uma <xref:System.Net.WebException> com a propriedade <xref:System.Net.WebException.Status%2A> definida como <xref:System.Net.WebExceptionStatus>.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-161">After the request has been canceled, calling **GetResponse**, **BeginGetResponse**, **EndGetResponse**, **GetRequestStream**, **BeginGetRequestStream**, or **EndGetRequestStream** will throw a <xref:System.Net.WebException> with the <xref:System.Net.WebException.Status%2A> property set to <xref:System.Net.WebExceptionStatus>.</span></span>  
  
## <a name="begingetrequeststream-and-endgetrequeststream-methods"></a><span data-ttu-id="e2a9f-162">Métodos BeginGetRequestStream e EndGetRequestStream</span><span class="sxs-lookup"><span data-stu-id="e2a9f-162">BeginGetRequestStream and EndGetRequestStream Methods</span></span>  
 <span data-ttu-id="e2a9f-163">O método <xref:System.Net.WebRequest.BeginGetRequestStream%2A> inicia uma solicitação assíncrona para o fluxo que é usado para carregar dados no servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-163">The <xref:System.Net.WebRequest.BeginGetRequestStream%2A> method starts an asynchronous request for the stream that is used to upload data to the server.</span></span> <span data-ttu-id="e2a9f-164">O método <xref:System.Net.WebRequest.EndGetRequestStream%2A> conclui a solicitação assíncrona e retorna o fluxo solicitado.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-164">The <xref:System.Net.WebRequest.EndGetRequestStream%2A> method completes the asynchronous request and returns the requested stream.</span></span> <span data-ttu-id="e2a9f-165">Esses métodos implementam o método **GetRequestStream** usando o padrão assíncrono do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-165">These methods implement the **GetRequestStream** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="begingetresponse-and-endgetresponse-methods"></a><span data-ttu-id="e2a9f-166">Métodos BeginGetResponse e EndGetResponse</span><span class="sxs-lookup"><span data-stu-id="e2a9f-166">BeginGetResponse and EndGetResponse Methods</span></span>  
 <span data-ttu-id="e2a9f-167">O método <xref:System.Net.WebRequest.BeginGetResponse%2A> inicia uma solicitação assíncrona para um servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-167">The <xref:System.Net.WebRequest.BeginGetResponse%2A> method starts an asynchronous request to a server.</span></span> <span data-ttu-id="e2a9f-168">O método <xref:System.Net.WebRequest.EndGetResponse%2A> conclui a solicitação assíncrona e retorna a resposta solicitada.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-168">The <xref:System.Net.WebRequest.EndGetResponse%2A> method completes the asynchronous request and returns the requested response.</span></span> <span data-ttu-id="e2a9f-169">Esses métodos implementam o método **GetResponse** usando o padrão assíncrono do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-169">These methods implement the **GetResponse** method using the standard .NET Framework asynchronous pattern.</span></span>  
  
## <a name="getrequeststream-method"></a><span data-ttu-id="e2a9f-170">Método GetRequestStream</span><span class="sxs-lookup"><span data-stu-id="e2a9f-170">GetRequestStream Method</span></span>  
 <span data-ttu-id="e2a9f-171">O método <xref:System.Net.WebRequest.GetRequestStream%2A> retorna um fluxo que é usado para gravar dados no servidor solicitado.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-171">The <xref:System.Net.WebRequest.GetRequestStream%2A> method returns a stream that is used to write data to the requested server.</span></span> <span data-ttu-id="e2a9f-172">O fluxo retornado deve ser um fluxo somente gravação que não faz buscas; ele se destina como um fluxo de dados unidirecional que é gravado no servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-172">The stream returned should be a write-only stream that does not seek; it is intended as a one-way stream of data that is written to the server.</span></span> <span data-ttu-id="e2a9f-173">O fluxo retorna falso para as propriedades <xref:System.IO.Stream.CanRead%2A> e <xref:System.IO.Stream.CanSeek%2A> e verdadeiro para a propriedade <xref:System.IO.Stream.CanWrite%2A>.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-173">The stream returns false for the <xref:System.IO.Stream.CanRead%2A> and <xref:System.IO.Stream.CanSeek%2A> properties and true for the <xref:System.IO.Stream.CanWrite%2A> property.</span></span>  
  
 <span data-ttu-id="e2a9f-174">O método **GetRequestStream** normalmente abre uma conexão com o servidor e, antes de retornar o fluxo, envia informações de cabeçalho que indicam que os dados estão sendo enviados para o servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-174">The **GetRequestStream** method typically opens a connection to the server and, before returning the stream, sends header information that indicates that data is being sent to the server.</span></span> <span data-ttu-id="e2a9f-175">Como **GetRequestStream** inicia a solicitação, a configuração de propriedades **Header** ou da propriedade **ContentLength** normalmente não é permitida após a chamada a **GetRequestStream**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-175">Because **GetRequestStream** begins the request, setting any **Header** properties or the **ContentLength** property is typically not allowed after calling **GetRequestStream**.</span></span>  
  
## <a name="getresponse-method"></a><span data-ttu-id="e2a9f-176">Método GetResponse</span><span class="sxs-lookup"><span data-stu-id="e2a9f-176">GetResponse Method</span></span>  
 <span data-ttu-id="e2a9f-177">O método <xref:System.Net.WebRequest.GetResponse%2A> retorna um descendente específico ao protocolo da classe <xref:System.Net.WebResponse> que representa a resposta do servidor.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-177">The <xref:System.Net.WebRequest.GetResponse%2A> method returns a protocol-specific descendant of the <xref:System.Net.WebResponse> class that represents the response from the server.</span></span> <span data-ttu-id="e2a9f-178">A menos que a solicitação já tenha sido iniciada pelo método **GetRequestStream**, o método **GetResponse** cria uma conexão com o recurso identificado pelo **RequestUri**, envia informações de cabeçalho que indicam o tipo de solicitação que está sendo feita e, em seguida, recebe a resposta do recurso.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-178">Unless the request has already been initiated by the **GetRequestStream** method, the **GetResponse** method creates a connection to the resource identified by **RequestUri**, sends header information indicating the type of request being made, and then receives the response from the resource.</span></span>  
  
 <span data-ttu-id="e2a9f-179">Depois que o método **GetResponse** for chamado, todas as propriedades deverão ser consideradas somente leitura.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-179">Once the **GetResponse** method is called, all properties should be considered read-only.</span></span> <span data-ttu-id="e2a9f-180">As instâncias de **WebRequest** são destinadas a um único uso; se desejar fazer outra solicitação, crie uma nova **WebRequest**.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-180">**WebRequest** instances are intended for one-time use; if you want to make another request, you should create a new **WebRequest**.</span></span>  
  
 <span data-ttu-id="e2a9f-181">O método **GetResponse** é responsável por criar um descendente apropriado de **WebResponse** para conter a resposta de entrada.</span><span class="sxs-lookup"><span data-stu-id="e2a9f-181">The **GetResponse** method is responsible for creating an appropriate **WebResponse** descendant to contain the incoming response.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="e2a9f-182">Consulte também</span><span class="sxs-lookup"><span data-stu-id="e2a9f-182">See Also</span></span>  
 <xref:System.Net.WebRequest>  
 <xref:System.Net.HttpWebRequest>  
 <xref:System.Net.FileWebRequest>  
 [<span data-ttu-id="e2a9f-183">Programando protocolos conectáveis</span><span class="sxs-lookup"><span data-stu-id="e2a9f-183">Programming Pluggable Protocols</span></span>](../../../docs/framework/network-programming/programming-pluggable-protocols.md)  
 [<span data-ttu-id="e2a9f-184">Derivando de WebResponse</span><span class="sxs-lookup"><span data-stu-id="e2a9f-184">Deriving from WebResponse</span></span>](../../../docs/framework/network-programming/deriving-from-webresponse.md)
