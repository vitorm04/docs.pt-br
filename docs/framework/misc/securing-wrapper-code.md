---
title: Protegendo código de wrapper
ms.date: 03/30/2017
helpviewer_keywords:
- security [.NET Framework], wrapper code
- wrapper code, securing
- secure coding, wrapper code
- code security, wrapper code
ms.assetid: 1df6c516-5bba-48bd-b450-1070e04b7389
author: mairaw
ms.author: mairaw
ms.openlocfilehash: e4d8497d17e1a82791f4dd6ca8f91c9a012db167
ms.sourcegitcommit: 0be8a279af6d8a43e03141e349d3efd5d35f8767
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/18/2019
ms.locfileid: "59132776"
---
# <a name="securing-wrapper-code"></a><span data-ttu-id="a622e-102">Protegendo código de wrapper</span><span class="sxs-lookup"><span data-stu-id="a622e-102">Securing Wrapper Code</span></span>
[!INCLUDE[net_security_note](../../../includes/net-security-note-md.md)]  
  
 <span data-ttu-id="a622e-103">Código de wrapper, especialmente em que o wrapper tem confiança mais alto que o código que utiliza, pode abrir um conjunto exclusivo de vulnerabilidades de segurança.</span><span class="sxs-lookup"><span data-stu-id="a622e-103">Wrapper code, especially where the wrapper has higher trust than code that uses it, can open a unique set of security weaknesses.</span></span> <span data-ttu-id="a622e-104">Qualquer coisa feita em nome de um chamador, em que as permissões do chamador limitadas não estão incluídas na verificação de segurança apropriadas, é um ponto fraco potencial para ser explorada.</span><span class="sxs-lookup"><span data-stu-id="a622e-104">Anything done on behalf of a caller, where the caller's limited permissions are not included in the appropriate security check, is a potential weakness to be exploited.</span></span>  
  
 <span data-ttu-id="a622e-105">Habilite nunca algo através do wrapper que o chamador não foi possível em si.</span><span class="sxs-lookup"><span data-stu-id="a622e-105">Never enable something through the wrapper that the caller could not do itself.</span></span> <span data-ttu-id="a622e-106">Isso é um risco especial quando fazer algo que envolve uma verificação de segurança limitada, em vez de uma demanda de movimentação de pilha completa.</span><span class="sxs-lookup"><span data-stu-id="a622e-106">This is a special danger when doing something that involves a limited security check, as opposed to a full stack walk demand.</span></span> <span data-ttu-id="a622e-107">Quando as verificações de nível único estão envolvidas, interposing o código de wrapper entre o chamador real e o elemento de API em questão pode facilmente fazer com que a verificação de segurança seja bem-sucedida quando ela deve não e, portanto, enfraquecer a segurança.</span><span class="sxs-lookup"><span data-stu-id="a622e-107">When single-level checks are involved, interposing the wrapper code between the real caller and the API element in question can easily cause the security check to succeed when it should not, thereby weakening security.</span></span>  
  
## <a name="delegates"></a><span data-ttu-id="a622e-108">Delegados</span><span class="sxs-lookup"><span data-stu-id="a622e-108">Delegates</span></span>  
 <span data-ttu-id="a622e-109">Segurança de delegado é diferente entre as versões do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="a622e-109">Delegate security differs between versions of the .NET Framework.</span></span>  <span data-ttu-id="a622e-110">Esta seção descreve os comportamentos diferentes de delegado e associados a considerações de segurança.</span><span class="sxs-lookup"><span data-stu-id="a622e-110">This section describes the different delegate behaviors and associated security considerations.</span></span>  
  
### <a name="in-version-10-and-11-of-the-net-framework"></a><span data-ttu-id="a622e-111">Na versão 1.0 e 1.1 do .NET Framework</span><span class="sxs-lookup"><span data-stu-id="a622e-111">In version 1.0 and 1.1 of the .NET Framework</span></span>  
 <span data-ttu-id="a622e-112">Versão 1.0 e 1.1 do .NET Framework execute as seguintes ações de segurança em relação a um criador de delegado e um chamador do delegado.</span><span class="sxs-lookup"><span data-stu-id="a622e-112">Version 1.0 and 1.1 of the .NET Framework perform the following security actions against a delegate creator and a delegate caller.</span></span>  
  
-   <span data-ttu-id="a622e-113">Quando um delegado é criado, as demandas de link de segurança no método de destino de delegado são executadas em conjunto de concessões do criador do delegado.</span><span class="sxs-lookup"><span data-stu-id="a622e-113">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="a622e-114">Falha para satisfazer a ação de segurança resulta em um <xref:System.Security.SecurityException>.</span><span class="sxs-lookup"><span data-stu-id="a622e-114">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
-   <span data-ttu-id="a622e-115">Quando o delegado é invocado, qualquer demandas de segurança existentes no chamador delegado são executadas.</span><span class="sxs-lookup"><span data-stu-id="a622e-115">When the delegate is invoked, any existing security demands on the delegate caller are performed.</span></span>  
  
 <span data-ttu-id="a622e-116">Sempre que seu código recebe um <xref:System.Delegate> do código menos confiável que possa chamá-lo, certifique-se de que você não estiver habilitando o código menos confiável escalonar suas permissões.</span><span class="sxs-lookup"><span data-stu-id="a622e-116">Whenever your code takes a <xref:System.Delegate> from less-trusted code that might call it, make sure that you are not enabling the less-trusted code to escalate its permissions.</span></span> <span data-ttu-id="a622e-117">Se você levar um delegado e usá-lo posteriormente, o código que criou o delegado não está na pilha de chamadas e suas permissões não serão testadas se o código no ou sob o delegado tentar uma operação de protegido.</span><span class="sxs-lookup"><span data-stu-id="a622e-117">If you take a delegate and use it later, the code that created the delegate is not on the call stack and its permissions will not be tested if code in or under the delegate attempts a protected operation.</span></span> <span data-ttu-id="a622e-118">Se seu código e o código chamador tem mais privilégios que o criador, o criador pode orquestrar o caminho da chamada sem fazer parte da pilha de chamadas.</span><span class="sxs-lookup"><span data-stu-id="a622e-118">If your code and the caller code have higher privileges than the creator, the creator can orchestrate the call path without being part of the call stack.</span></span>  
  
### <a name="in-version-20-and-later-versions-of-the-net-framework"></a><span data-ttu-id="a622e-119">Na versão 2.0 e versões posteriores do .NET Framework</span><span class="sxs-lookup"><span data-stu-id="a622e-119">In version 2.0 and later versions of the .NET Framework</span></span>  
 <span data-ttu-id="a622e-120">Diferentemente das versões anteriores, versão 2.0 e versões posteriores do .NET Framework executa a ação de segurança contra o criador de delegado quando o delegado é criado e chamado.</span><span class="sxs-lookup"><span data-stu-id="a622e-120">Unlike previous versions, version 2.0 and later versions of the .NET Framework performs security action against the delegate creator when the delegate is created and called.</span></span>  
  
-   <span data-ttu-id="a622e-121">Quando um delegado é criado, as demandas de link de segurança no método de destino de delegado são executadas em conjunto de concessões do criador do delegado.</span><span class="sxs-lookup"><span data-stu-id="a622e-121">When a delegate is created, security link demands on the delegate target method are performed against the grant set of the delegate creator.</span></span>  <span data-ttu-id="a622e-122">Falha para satisfazer a ação de segurança resulta em um <xref:System.Security.SecurityException>.</span><span class="sxs-lookup"><span data-stu-id="a622e-122">Failure to satisfy the security action results in a <xref:System.Security.SecurityException>.</span></span>  
  
-   <span data-ttu-id="a622e-123">Conjunto de concessões do criador de delegado também é capturado durante a criação de delegado e armazenado com o delegado.</span><span class="sxs-lookup"><span data-stu-id="a622e-123">The delegate creator's grant set is also captured during delegate creation and stored with the delegate.</span></span>  
  
-   <span data-ttu-id="a622e-124">Quando o delegado é invocado, conjunto de concessões capturados de delegado do criador primeiro é avaliado em relação a qualquer demandas no contexto atual se o criador de delegado e o chamador pertencerem a assemblies diferentes.</span><span class="sxs-lookup"><span data-stu-id="a622e-124">When the delegate is invoked, the delegate creator's captured grant set is first evaluated against any demands in the current context if the delegate creator and caller belong to different assemblies.</span></span>  <span data-ttu-id="a622e-125">Em seguida, qualquer demandas de segurança existentes no chamador delegado são executadas.</span><span class="sxs-lookup"><span data-stu-id="a622e-125">Next, any existing security demands on the delegate caller are performed.</span></span>  
  
## <a name="link-demands-and-wrappers"></a><span data-ttu-id="a622e-126">Demandas de link e wrappers</span><span class="sxs-lookup"><span data-stu-id="a622e-126">Link demands and wrappers</span></span>  
 <span data-ttu-id="a622e-127">Um caso especial de proteção com demandas de link foi reforçado na infra-estrutura de segurança, mas ainda é uma fonte de um possível ponto fraco em seu código.</span><span class="sxs-lookup"><span data-stu-id="a622e-127">A special protection case with link demands has been strengthened in the security infrastructure, but it is still a source of possible weakness in your code.</span></span>  
  
 <span data-ttu-id="a622e-128">Se o código totalmente confiável chamar uma propriedade, evento ou método protegido por um [LinkDemand](../../../docs/framework/misc/link-demands.md), a chamada terá êxito se o **LinkDemand** verificação de permissão para que o chamador é atendida.</span><span class="sxs-lookup"><span data-stu-id="a622e-128">If fully trusted code calls a property, event, or method protected by a [LinkDemand](../../../docs/framework/misc/link-demands.md), the call succeeds if the **LinkDemand** permission check for the caller is satisfied.</span></span> <span data-ttu-id="a622e-129">Além disso, se o código totalmente confiável expõe uma classe que usa o nome de uma propriedade e chamadas de seu **Obtenha** acessador usando a reflexão, que é chamada para o **obter** acessador for bem-sucedida, mesmo que o código de usuário faz não tem o direito de acessar essa propriedade.</span><span class="sxs-lookup"><span data-stu-id="a622e-129">Additionally, if the fully trusted code exposes a class that takes the name of a property and calls its **get** accessor using reflection, that call to the **get** accessor succeeds even though the user code does not have the right to access this property.</span></span> <span data-ttu-id="a622e-130">Isso ocorre porque o **LinkDemand** verifica somente o chamador imediato, que é o código totalmente confiável.</span><span class="sxs-lookup"><span data-stu-id="a622e-130">This is because the **LinkDemand** checks only the immediate caller, which is the fully trusted code.</span></span> <span data-ttu-id="a622e-131">Em essência, o código totalmente confiável está fazendo uma chamada de privilegiadas em nome do código do usuário sem certificando-se de que o código de usuário tem o direito de fazer essa chamada.</span><span class="sxs-lookup"><span data-stu-id="a622e-131">In essence, the fully trusted code is making a privileged call on behalf of user code without making sure that the user code has the right to make that call.</span></span>  
  
 <span data-ttu-id="a622e-132">Para evitar tais falhas de segurança, o common language runtime estende a seleção em uma demanda de movimentação de pilha completa em qualquer chamada indireta para um método, construtor, propriedade ou evento protegido por um **LinkDemand**.</span><span class="sxs-lookup"><span data-stu-id="a622e-132">To help prevent such security holes, the common language runtime extends the check into a full stack-walking demand on any indirect call to a method, constructor, property, or event protected by a **LinkDemand**.</span></span> <span data-ttu-id="a622e-133">Essa proteção incorre em alguns custos de desempenho e altera a semântica da verificação de segurança; a demanda de movimentação de pilha completo poderá falhar em que a verificação de um nível mais rápida passaria.</span><span class="sxs-lookup"><span data-stu-id="a622e-133">This protection incurs some performance costs and changes the semantics of the security check; the full stack-walk demand might fail where the faster, one-level check would have passed.</span></span>  
  
## <a name="assembly-loading-wrappers"></a><span data-ttu-id="a622e-134">Wrappers de carregamento de assembly</span><span class="sxs-lookup"><span data-stu-id="a622e-134">Assembly loading wrappers</span></span>  
 <span data-ttu-id="a622e-135">Vários métodos usados para carregar o código gerenciado, incluindo <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>, carregar assemblies com a evidência do chamador.</span><span class="sxs-lookup"><span data-stu-id="a622e-135">Several methods used to load managed code, including <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>, load assemblies with the evidence of the caller.</span></span> <span data-ttu-id="a622e-136">Se você encapsula qualquer um desses métodos, o sistema de segurança pode usar concessão de permissão do seu código, em vez de permissões do chamador para o wrapper, carregar os assemblies.</span><span class="sxs-lookup"><span data-stu-id="a622e-136">If you wrap any of these methods, the security system could use your code's permission grant, instead of the permissions of the caller to your wrapper, to load the assemblies.</span></span> <span data-ttu-id="a622e-137">Você não deve permitir que código menos confiável carregar o código que recebeu permissões mais elevadas que as do chamador para o wrapper.</span><span class="sxs-lookup"><span data-stu-id="a622e-137">You should not allow less-trusted code to load code that is granted higher permissions than those of the caller to your wrapper.</span></span>  
  
 <span data-ttu-id="a622e-138">Qualquer código que tenha confiança significativamente maior do que um chamador potencial (incluindo um chamador de nível de permissões da Internet) ou confiança total pode diminuir o nível de segurança dessa maneira.</span><span class="sxs-lookup"><span data-stu-id="a622e-138">Any code that has full trust or significantly higher trust than a potential caller (including an Internet-permissions-level caller) could weaken security in this way.</span></span> <span data-ttu-id="a622e-139">Se o código tem um método público que pega uma matriz de bytes e a passa **Load**e, portanto, criando um assembly em nome do chamador, ele poderá interromper a segurança.</span><span class="sxs-lookup"><span data-stu-id="a622e-139">If your code has a public method that takes a byte array and passes it to **Assembly.Load**, thereby creating an assembly on the caller's behalf, it might break security.</span></span>  
  
 <span data-ttu-id="a622e-140">Esse problema se aplica aos seguintes elementos de API:</span><span class="sxs-lookup"><span data-stu-id="a622e-140">This issue applies to the following API elements:</span></span>  
  
-   <xref:System.AppDomain.DefineDynamicAssembly%2A?displayProperty=nameWithType>  
  
-   <xref:System.AppDomain.Load%2A?displayProperty=nameWithType>  
  
-   <xref:System.Reflection.Assembly.LoadFrom%2A?displayProperty=nameWithType>  
  
-   <xref:System.Reflection.Assembly.Load%2A?displayProperty=nameWithType>  
  
## <a name="demand-vs-linkdemand"></a><span data-ttu-id="a622e-141">Demanda vs. LinkDemand</span><span class="sxs-lookup"><span data-stu-id="a622e-141">Demand vs. LinkDemand</span></span>  
 <span data-ttu-id="a622e-142">Segurança declarativa oferece dois tipos de verificações de segurança que são semelhantes, mas executam verificações muito diferentes.</span><span class="sxs-lookup"><span data-stu-id="a622e-142">Declarative security offers two kinds of security checks that are similar but perform very different checks.</span></span> <span data-ttu-id="a622e-143">Você deve compreender as duas formas porque a escolha errada pode resultar em perda de segurança ou o desempenho fraca.</span><span class="sxs-lookup"><span data-stu-id="a622e-143">You should understand both forms because the wrong choice can result in weak security or performance loss.</span></span>  
  
 <span data-ttu-id="a622e-144">Segurança declarativa oferece as seguintes verificações de segurança:</span><span class="sxs-lookup"><span data-stu-id="a622e-144">Declarative security offers the following security checks:</span></span>  
  
-   <span data-ttu-id="a622e-145"><xref:System.Security.Permissions.SecurityAction.Demand> Especifica a movimentação de pilha de segurança de acesso de código.</span><span class="sxs-lookup"><span data-stu-id="a622e-145"><xref:System.Security.Permissions.SecurityAction.Demand> specifies the code access security stack walk.</span></span> <span data-ttu-id="a622e-146">Todos os chamadores na pilha devem ter a permissão especificada ou a identidade para passar.</span><span class="sxs-lookup"><span data-stu-id="a622e-146">All callers on the stack must have the specified permission or identity to pass.</span></span> <span data-ttu-id="a622e-147">**Demanda** ocorre em todas as chamadas, porque a pilha talvez contenha chamadores diferentes.</span><span class="sxs-lookup"><span data-stu-id="a622e-147">**Demand** occurs on every call because the stack might contain different callers.</span></span> <span data-ttu-id="a622e-148">Se você chamar um método repetidamente, essa verificação de segurança ocorre cada vez.</span><span class="sxs-lookup"><span data-stu-id="a622e-148">If you call a method repeatedly, this security check occurs each time.</span></span> <span data-ttu-id="a622e-149">**Demanda** é boa proteção contra ataques de atração código não autorizado tentar obter por meio dele será detectado.</span><span class="sxs-lookup"><span data-stu-id="a622e-149">**Demand** is good protection against luring attacks; unauthorized code trying to get through it will be detected.</span></span>  
  
-   <span data-ttu-id="a622e-150">[LinkDemand](../../../docs/framework/misc/link-demands.md) ocorre em tempo de compilação just-in-time (JIT) e verifica somente o chamador imediato.</span><span class="sxs-lookup"><span data-stu-id="a622e-150">[LinkDemand](../../../docs/framework/misc/link-demands.md) happens at just-in-time (JIT) compilation time and checks only the immediate caller.</span></span> <span data-ttu-id="a622e-151">Essa verificação de segurança não verifica chamador o chamador do.</span><span class="sxs-lookup"><span data-stu-id="a622e-151">This security check does not check the caller's caller.</span></span> <span data-ttu-id="a622e-152">Depois que essa verificação passa, não há nenhuma segurança adicional sobrecarga, independentemente de quantas vezes o chamador pode chamar.</span><span class="sxs-lookup"><span data-stu-id="a622e-152">Once this check passes, there is no additional security overhead no matter how many times the caller might call.</span></span> <span data-ttu-id="a622e-153">No entanto, também não há nenhuma proteção contra ataques de atração.</span><span class="sxs-lookup"><span data-stu-id="a622e-153">However, there is also no protection from luring attacks.</span></span> <span data-ttu-id="a622e-154">Com o **LinkDemand**, qualquer código que é aprovado no teste e pode fazer referência a seu código pode potencialmente quebrar a segurança, permitindo que o código mal-intencionado chamar usando o código não autorizado.</span><span class="sxs-lookup"><span data-stu-id="a622e-154">With **LinkDemand**, any code that passes the test and can reference your code can potentially break security by allowing malicious code to call using the authorized code.</span></span> <span data-ttu-id="a622e-155">Portanto, não use **LinkDemand** , a menos que todas as falhas possíveis podem ser completamente evitadas.</span><span class="sxs-lookup"><span data-stu-id="a622e-155">Therefore, do not use **LinkDemand** unless all the possible weaknesses can be thoroughly avoided.</span></span>  
  
    > [!NOTE]
    >  <span data-ttu-id="a622e-156">No [!INCLUDE[net_v40_long](../../../includes/net-v40-long-md.md)], as demandas de link foram substituídas pela <xref:System.Security.SecurityCriticalAttribute> atributo em <xref:System.Security.SecurityRuleSet.Level2> assemblies.</span><span class="sxs-lookup"><span data-stu-id="a622e-156">In the [!INCLUDE[net_v40_long](../../../includes/net-v40-long-md.md)], link demands have been replaced by the <xref:System.Security.SecurityCriticalAttribute> attribute in <xref:System.Security.SecurityRuleSet.Level2> assemblies.</span></span> <span data-ttu-id="a622e-157">O <xref:System.Security.SecurityCriticalAttribute> é equivalente a uma demanda de link para confiança total; no entanto, isso também afeta as regras de herança.</span><span class="sxs-lookup"><span data-stu-id="a622e-157">The <xref:System.Security.SecurityCriticalAttribute> is equivalent to a link demand for full trust; however, it also affects inheritance rules.</span></span> <span data-ttu-id="a622e-158">Para obter mais informações sobre essa alteração, consulte [código transparente de segurança, nível 2](../../../docs/framework/misc/security-transparent-code-level-2.md).</span><span class="sxs-lookup"><span data-stu-id="a622e-158">For more information about this change, see [Security-Transparent Code, Level 2](../../../docs/framework/misc/security-transparent-code-level-2.md).</span></span>  
  
 <span data-ttu-id="a622e-159">As precauções extras necessárias ao utilizar **LinkDemand** deve ser programado individualmente; o sistema de segurança pode ajudar com a imposição.</span><span class="sxs-lookup"><span data-stu-id="a622e-159">The extra precautions required when using **LinkDemand** must be programmed individually; the security system can help with enforcement.</span></span> <span data-ttu-id="a622e-160">Qualquer erro abre uma vulnerabilidade de segurança.</span><span class="sxs-lookup"><span data-stu-id="a622e-160">Any mistake opens a security weakness.</span></span> <span data-ttu-id="a622e-161">Todos os autorizados código que usa seu código deve ser responsável por implementar segurança adicional fazendo o seguinte:</span><span class="sxs-lookup"><span data-stu-id="a622e-161">All authorized code that uses your code must be responsible for implementing additional security by doing the following:</span></span>  
  
-   <span data-ttu-id="a622e-162">Restringindo o acesso do código de chamada da classe ou assembly.</span><span class="sxs-lookup"><span data-stu-id="a622e-162">Restricting the calling code's access to the class or assembly.</span></span>  
  
-   <span data-ttu-id="a622e-163">Colocando a mesma segurança verifica no código de chamada que aparecem no código que está sendo chamado e obligating seus chamadores para fazê-lo.</span><span class="sxs-lookup"><span data-stu-id="a622e-163">Placing the same security checks on the calling code that appear on the code being called and obligating its callers to do so.</span></span> <span data-ttu-id="a622e-164">Por exemplo, se você escrever código que chama um método que é protegido com uma **LinkDemand** para o <xref:System.Security.Permissions.SecurityPermission> com o <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> sinalizador for especificado, o método também deve fazer uma **LinkDemand** (ou **Demanda**, que é mais forte) para essa permissão.</span><span class="sxs-lookup"><span data-stu-id="a622e-164">For example, if you write code that calls a method that is protected with a **LinkDemand** for the <xref:System.Security.Permissions.SecurityPermission> with the <xref:System.Security.Permissions.SecurityPermissionFlag.UnmanagedCode> flag specified, your method should also make a **LinkDemand** (or **Demand**, which is stronger) for this permission.</span></span> <span data-ttu-id="a622e-165">A exceção é se o seu código usa o **LinkDemand**-método protegido de forma limitada que você achar que é seguro, considerando outros mecanismos de proteção de segurança (como demandas) em seu código.</span><span class="sxs-lookup"><span data-stu-id="a622e-165">The exception is if your code uses the **LinkDemand**-protected method in a limited way that you decide is safe, given other security protection mechanisms (such as demands) in your code.</span></span> <span data-ttu-id="a622e-166">Nesse caso excepcional, o chamador assume a responsabilidade em enfraquecer a proteção de segurança no código subjacente.</span><span class="sxs-lookup"><span data-stu-id="a622e-166">In this exceptional case, the caller takes responsibility in weakening the security protection on the underlying code.</span></span>  
  
-   <span data-ttu-id="a622e-167">Garantir que os chamadores do seu código não é possível induzir seu código chama o código protegido em nome deles.</span><span class="sxs-lookup"><span data-stu-id="a622e-167">Ensuring that your code's callers cannot trick your code into calling the protected code on their behalf.</span></span> <span data-ttu-id="a622e-168">Em outras palavras, os chamadores não podem forçar o código não autorizado para passar parâmetros específicos para o código protegido ou para obter resultados dela.</span><span class="sxs-lookup"><span data-stu-id="a622e-168">In other words, callers cannot force the authorized code to pass specific parameters to the protected code, or to get results back from it.</span></span>  
  
### <a name="interfaces-and-link-demands"></a><span data-ttu-id="a622e-169">Interfaces e demandas de Link</span><span class="sxs-lookup"><span data-stu-id="a622e-169">Interfaces and Link Demands</span></span>  
 <span data-ttu-id="a622e-170">Se um método virtual, propriedade ou evento com **LinkDemand** substitui um método de classe base, o método de classe base também deve ter o mesmo **LinkDemand** para o método substituído para serem eficazes.</span><span class="sxs-lookup"><span data-stu-id="a622e-170">If a virtual method, property, or event with **LinkDemand** overrides a base class method, the base class method must also have the same **LinkDemand** for the overridden method in order to be effective.</span></span> <span data-ttu-id="a622e-171">É possível a conversão de volta ao tipo base e chame o método de classe base por código mal-intencionado.</span><span class="sxs-lookup"><span data-stu-id="a622e-171">It is possible for malicious code to cast back to the base type and call the base class method.</span></span> <span data-ttu-id="a622e-172">Também Observe que as demandas de link pode ser adicionado implicitamente a assemblies que não têm o <xref:System.Security.AllowPartiallyTrustedCallersAttribute> atributo de nível de assembly.</span><span class="sxs-lookup"><span data-stu-id="a622e-172">Also note that link demands can be added implicitly to assemblies that do not have the <xref:System.Security.AllowPartiallyTrustedCallersAttribute> assembly-level attribute.</span></span>  
  
 <span data-ttu-id="a622e-173">É uma boa prática para proteger as implementações de método com demandas de link quando métodos de interface também tem demandas de link.</span><span class="sxs-lookup"><span data-stu-id="a622e-173">It is a good practice to protect method implementations with link demands when interface methods also have link demands.</span></span> <span data-ttu-id="a622e-174">Observe o seguinte sobre como usar demandas de link com interfaces:</span><span class="sxs-lookup"><span data-stu-id="a622e-174">Note the following about using link demands with interfaces:</span></span>  
  
-   <span data-ttu-id="a622e-175">Se você colocar uma **LinkDemand** em um método público de uma classe que implementa um método de interface, o **LinkDemand** não serão impostas se, em seguida, convertido para a interface e chamar o método.</span><span class="sxs-lookup"><span data-stu-id="a622e-175">If you place a **LinkDemand** on a public method of a class that implements an interface method, the **LinkDemand** will not be enforced if you then cast to the interface and call the method.</span></span> <span data-ttu-id="a622e-176">Nesse caso, porque você vinculou com base na interface, apenas o **LinkDemand** na interface será respeitada.</span><span class="sxs-lookup"><span data-stu-id="a622e-176">In this case, because you linked against the interface, only the **LinkDemand** on the interface is honored.</span></span>  
  
 <span data-ttu-id="a622e-177">Examine os seguintes itens para problemas de segurança:</span><span class="sxs-lookup"><span data-stu-id="a622e-177">Review the following items for security issues:</span></span>  
  
-   <span data-ttu-id="a622e-178">Demandas de link explícito em métodos de interface.</span><span class="sxs-lookup"><span data-stu-id="a622e-178">Explicit link demands on interface methods.</span></span> <span data-ttu-id="a622e-179">Verifique se essas demandas de link oferecem a proteção esperada.</span><span class="sxs-lookup"><span data-stu-id="a622e-179">Make sure these link demands offer the expected protection.</span></span> <span data-ttu-id="a622e-180">Determine se o código mal-intencionado pode usar uma conversão para contornar as demandas de link, conforme descrito anteriormente.</span><span class="sxs-lookup"><span data-stu-id="a622e-180">Determine whether malicious code can use a cast to get around the link demands as described previously.</span></span>  
  
-   <span data-ttu-id="a622e-181">Métodos virtuais com demandas de link aplicados.</span><span class="sxs-lookup"><span data-stu-id="a622e-181">Virtual methods with link demands applied.</span></span>  
  
-   <span data-ttu-id="a622e-182">Tipos e as interfaces que elas implementam.</span><span class="sxs-lookup"><span data-stu-id="a622e-182">Types and the interfaces they implement.</span></span> <span data-ttu-id="a622e-183">Eles devem usar demandas de link consistentemente.</span><span class="sxs-lookup"><span data-stu-id="a622e-183">These should use link demands consistently.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="a622e-184">Consulte também</span><span class="sxs-lookup"><span data-stu-id="a622e-184">See also</span></span>

- [<span data-ttu-id="a622e-185">Diretrizes de codificação segura</span><span class="sxs-lookup"><span data-stu-id="a622e-185">Secure Coding Guidelines</span></span>](../../../docs/standard/security/secure-coding-guidelines.md)
