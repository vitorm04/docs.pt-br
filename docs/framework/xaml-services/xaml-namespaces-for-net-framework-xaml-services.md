---
title: Namespaces XAML para serviços XAML do .NET Framework
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 11bf5d112c64fb0b942decf7635f02b90a98bdeb
ms.sourcegitcommit: a4f9b754059f0210e29ae0578363a27b9ba84b64
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/05/2019
ms.locfileid: "74837162"
---
# <a name="xaml-namespaces-for-net-framework-xaml-services"></a><span data-ttu-id="80621-102">Namespaces XAML para serviços XAML do .NET Framework</span><span class="sxs-lookup"><span data-stu-id="80621-102">XAML Namespaces for .NET Framework XAML Services</span></span>
<span data-ttu-id="80621-103">Um namespace XAML é um conceito que se expande na definição de um namespace XML.</span><span class="sxs-lookup"><span data-stu-id="80621-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="80621-104">Semelhante a um namespace XML, você pode definir um namespace XAML usando um atributo `xmlns` na marcação.</span><span class="sxs-lookup"><span data-stu-id="80621-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="80621-105">Os namespaces XAML também são representados no fluxo do nó XAML e em outras APIs de serviços XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="80621-106">Este tópico define o conceito de namespace XAML e descreve como os namespaces XAML podem ser definidos e são usados por contextos de esquema XAML e outros aspectos de .NET Framework serviços XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET Framework XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="80621-107">Namespace XML e namespace XAML</span><span class="sxs-lookup"><span data-stu-id="80621-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="80621-108">Um namespace XAML é um namespace XML especializado, assim como o XAML é uma forma especializada de XML e usa o formulário XML básico para sua marcação.</span><span class="sxs-lookup"><span data-stu-id="80621-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="80621-109">Na marcação, você declara um namespace XAML e seu mapeamento por meio de um atributo `xmlns` aplicado a um elemento.</span><span class="sxs-lookup"><span data-stu-id="80621-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="80621-110">A declaração de `xmlns` pode ser feita no mesmo elemento em que o namespace XAML é declarado.</span><span class="sxs-lookup"><span data-stu-id="80621-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="80621-111">Uma declaração de namespace XAML feita a um elemento é válida para esse elemento, todos os atributos desse elemento e todos os filhos desse elemento.</span><span class="sxs-lookup"><span data-stu-id="80621-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="80621-112">Os atributos podem usar um namespace XAML que não seja o mesmo que o elemento que contém o atributo, desde que o próprio nome do atributo referencie o prefixo como parte de seu nome de atributo na marcação.</span><span class="sxs-lookup"><span data-stu-id="80621-112">Attributes can use a XAML namespaces that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="80621-113">A distinção de um namespace XAML versus um namespace XML é que um namespace XML pode ser usado para fazer referência a um esquema ou pode ser usado para simplesmente diferenciar as entidades.</span><span class="sxs-lookup"><span data-stu-id="80621-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema, or might be used to simply differentiate entities.</span></span> <span data-ttu-id="80621-114">Para o XAML, os tipos e membros como usados em XAML, em última análise, devem ser resolvidos para tipos de backup e os conceitos do esquema XML não se aplicam bem a esse recurso.</span><span class="sxs-lookup"><span data-stu-id="80621-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="80621-115">O namespace XAML contém informações que o contexto de esquema XAML deve ter disponível para executar esse mapeamento de tipo.</span><span class="sxs-lookup"><span data-stu-id="80621-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="80621-116">Componentes de namespace XAML</span><span class="sxs-lookup"><span data-stu-id="80621-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="80621-117">A definição do namespace XAML tem dois componentes: um prefixo e um identificador.</span><span class="sxs-lookup"><span data-stu-id="80621-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="80621-118">Cada um desses componentes está presente quando um namespace XAML é declarado em marcação ou definido no sistema de tipos XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-118">Each of these components are present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="80621-119">O prefixo pode ser qualquer cadeia de caracteres como permitido pelos [namespaces W3C na especificação XML 1,0](https://www.w3.org/TR/REC-xml-names/) .</span><span class="sxs-lookup"><span data-stu-id="80621-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/) .</span></span> <span data-ttu-id="80621-120">Por convenção, os prefixos normalmente são cadeias de caracteres muito curtas, porque o prefixo é repetido muitas vezes em um arquivo de marcação típico.</span><span class="sxs-lookup"><span data-stu-id="80621-120">By convention, the prefixes are typically very short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="80621-121">Determinados namespaces XAML que se destinam a serem usados em várias implementações XAML usam prefixos convencionais específicos.</span><span class="sxs-lookup"><span data-stu-id="80621-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="80621-122">Por exemplo, o XAML Language XAML namespace é normalmente mapeado usando o prefixo `x`.</span><span class="sxs-lookup"><span data-stu-id="80621-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="80621-123">Você pode definir um namespace XAML padrão, em que o prefixo não é fornecido na definição, mas é representado como uma cadeia de caracteres vazia, se definido ou consultado API de serviços XAML do by.NET Framework.</span><span class="sxs-lookup"><span data-stu-id="80621-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET Framework XAML Services API.</span></span> <span data-ttu-id="80621-124">Normalmente, o namespace XAML padrão é deliberadamente escolhido para promover uma quantidade maximizada de prefixo, omitindo marcação por uma tecnologia de implementação de XAML e seus cenários e vocabulários.</span><span class="sxs-lookup"><span data-stu-id="80621-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="80621-125">O identificador pode ser qualquer cadeia de caracteres como permitido pelos [namespaces W3C na especificação XML 1,0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="80621-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="80621-126">Por convenção, identificadores para namespaces XML ou namespaces XAML geralmente são fornecidos em formato de URI, normalmente como um URI absoluto qualificado por protocolo.</span><span class="sxs-lookup"><span data-stu-id="80621-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="80621-127">Geralmente, as informações de versão que definem um vocabulário XAML específico são implícitas como parte da cadeia de caracteres do caminho.</span><span class="sxs-lookup"><span data-stu-id="80621-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="80621-128">Os namespaces XAML adicionam uma Convenção de identificador adicional além da Convenção de URI XML.</span><span class="sxs-lookup"><span data-stu-id="80621-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="80621-129">Para namespaces XAML, o identificador comunica as informações necessárias para um contexto de esquema XAML para resolver os tipos especificados como elementos sob esse namespace XAML ou para resolver atributos para membros.</span><span class="sxs-lookup"><span data-stu-id="80621-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="80621-130">Para fins de comunicação de informações para um contexto de esquema XAML, o identificador de um namespace XAML ainda pode estar no formato URI.</span><span class="sxs-lookup"><span data-stu-id="80621-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="80621-131">No entanto, nesse caso, o URI também é declarado como um identificador correspondente em um determinado assembly ou lista de assemblies.</span><span class="sxs-lookup"><span data-stu-id="80621-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="80621-132">Isso é feito em assemblies ao atribuir o assembly com <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span><span class="sxs-lookup"><span data-stu-id="80621-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="80621-133">Esse método de identificar o namespace XAML e dar suporte a um comportamento de resolução de tipo baseado em CLR no assembly atribuído tem suporte do contexto de esquema XAML padrão em .NET Framework serviços XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET Framework XAML Services.</span></span> <span data-ttu-id="80621-134">Em geral, essa convenção pode ser usada para casos em que o contexto do esquema XAML incorpora o CLR ou é baseado no contexto do esquema XAML padrão, que é necessário para ler atributos CLR de assemblies CLR.</span><span class="sxs-lookup"><span data-stu-id="80621-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="80621-135">Os namespaces XAML também podem ser identificados por uma convenção que comunica um namespace CLR e um assembly de definição de tipo.</span><span class="sxs-lookup"><span data-stu-id="80621-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="80621-136">Essa convenção é usada nos casos em que nenhuma atribuição de <xref:System.Windows.Markup.XmlnsDefinitionAttribute> existe nos assemblies que contêm tipos.</span><span class="sxs-lookup"><span data-stu-id="80621-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="80621-137">Essa convenção é potencialmente mais complexa do que a Convenção de URI e também tem o potencial de ambiguidade e duplicação, pois há várias maneiras de fazer referência a um assembly.</span><span class="sxs-lookup"><span data-stu-id="80621-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="80621-138">A forma mais básica de um identificador que usa o namespace CLR e a Convenção de assembly é a seguinte:</span><span class="sxs-lookup"><span data-stu-id="80621-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="80621-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span><span class="sxs-lookup"><span data-stu-id="80621-139">`clr-namespace:` *clrnsName* `; assembly=` *assemblyShortName*</span></span>  
  
 <span data-ttu-id="80621-140">`clr-namespace:` e `; assembly=` são componentes literais da sintaxe.</span><span class="sxs-lookup"><span data-stu-id="80621-140">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="80621-141">*clrnsName* é o nome da cadeia de caracteres que identifica um namespace CLR.</span><span class="sxs-lookup"><span data-stu-id="80621-141">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="80621-142">Esse nome de cadeia de caracteres inclui quaisquer caracteres de ponto internos (.) que fornecem dicas sobre o namespace CLR e sua relação com outros namespaces CLR.</span><span class="sxs-lookup"><span data-stu-id="80621-142">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>  
  
 <span data-ttu-id="80621-143">*AssemblyShortName* é o nome da cadeia de caracteres de um assembly que define os tipos que são úteis em XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-143">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="80621-144">Os tipos a serem acessados por meio do namespace XAML declarado devem ser definidos pelo assembly e para serem especificamente declarados dentro do namespace CLR especificado por *clrnsName*.</span><span class="sxs-lookup"><span data-stu-id="80621-144">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be specifically declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="80621-145">Esse nome de cadeia de caracteres normalmente paraleliza as informações conforme relatadas pelo <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="80621-145">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="80621-146">Uma definição mais completa do namespace CLR e da Convenção de assembly é a seguinte:</span><span class="sxs-lookup"><span data-stu-id="80621-146">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 <span data-ttu-id="80621-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span><span class="sxs-lookup"><span data-stu-id="80621-147">`clr-namespace:` *clrnsName* `; assembly=` *assemblyName*</span></span>  
  
 <span data-ttu-id="80621-148">*AssemblyName* representa qualquer cadeia de caracteres que seja válida como entrada de <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="80621-148">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="80621-149">Essa cadeia de caracteres pode incluir informações de cultura, chave pública ou versão (definições desses conceitos são definidas no tópico de referência para <xref:System.Reflection.Assembly>).</span><span class="sxs-lookup"><span data-stu-id="80621-149">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="80621-150">O formato COFF e a evidência (conforme usado por outras sobrecargas de <xref:System.Reflection.Assembly.Load%2A>) não são relevantes para fins de carregamento de assembly XAML; todas as informações de carga devem ser apresentadas como uma cadeia de caracteres.</span><span class="sxs-lookup"><span data-stu-id="80621-150">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="80621-151">A especificação de uma chave pública para o assembly é uma técnica útil para segurança XAML ou para remover uma possível ambiguidade que pode existir se os assemblies forem carregados por nome simples ou se houver um domínio de aplicativo ou cache.</span><span class="sxs-lookup"><span data-stu-id="80621-151">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="80621-152">Para obter mais informações, consulte [considerações de segurança de XAML](xaml-security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="80621-152">For more information, see [XAML Security Considerations](xaml-security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="80621-153">Declarações de namespace XAML na API de serviços XAML</span><span class="sxs-lookup"><span data-stu-id="80621-153">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="80621-154">Na API de serviços XAML, uma declaração de namespace XAML é representada por um objeto <xref:System.Xaml.NamespaceDeclaration>.</span><span class="sxs-lookup"><span data-stu-id="80621-154">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="80621-155">Se você estiver declarando um namespace XAML no código, chame o Construtor <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29>.</span><span class="sxs-lookup"><span data-stu-id="80621-155">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="80621-156">Os parâmetros `ns` e `prefix` são especificados como cadeias de caracteres, e a entrada a ser fornecida para esses parâmetros corresponde à definição do identificador do namespace XAML e do prefixo do namespace XAML, conforme fornecido anteriormente neste tópico.</span><span class="sxs-lookup"><span data-stu-id="80621-156">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="80621-157">Se você estiver examinando as informações do namespace XAML como parte de um fluxo do nó XAML ou por meio de outro acesso ao sistema de tipos XAML, o <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> relatará o identificador do namespace XAML e <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> relatará o prefixo do namespace XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-157">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="80621-158">Em um fluxo de nó XAML, as informações do namespace XAML podem aparecer como um nó XAML que precede a entidade à qual se aplica.</span><span class="sxs-lookup"><span data-stu-id="80621-158">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="80621-159">Isso inclui casos em que as informações do namespace XAML precede o `StartObject` do elemento raiz XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-159">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="80621-160">Para obter mais informações, consulte [noções básicas sobre estruturas e conceitos de fluxo do nó XAML](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="80621-160">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="80621-161">Para muitos cenários que usam .NET Framework API de serviços XAML, espera-se que pelo menos uma declaração de namespace XAML exista e a declaração deve conter ou se referir a informações exigidas por um contexto de esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-161">For many scenarios that use .NET Framework XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="80621-162">Os namespaces XAML devem especificar assemblies a serem carregados ou auxiliar na resolução de tipos específicos em namespaces e assemblies já carregados ou conhecidos pelo contexto de esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-162">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="80621-163">Para gerar um fluxo de nó XAML, as informações do tipo XAML devem estar disponíveis, por meio do contexto do esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-163">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="80621-164">As informações do tipo XAML não podem ser determinadas sem determinar primeiro o namespace XAML relevante para cada nó a ser criado.</span><span class="sxs-lookup"><span data-stu-id="80621-164">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="80621-165">Neste ponto, nenhuma instância dos tipos foi criada ainda, mas o contexto do esquema XAML pode precisar pesquisar informações do assembly de definição e do tipo de suporte.</span><span class="sxs-lookup"><span data-stu-id="80621-165">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="80621-166">Por exemplo, para processar a marcação `<Party><PartyFavor/></Party>`, o contexto do esquema XAML deve ser capaz de determinar o nome e o tipo do `ContentProperty` de `Party`e, portanto, também deve conhecer as informações do namespace XAML para `Party` e `PartyFavor`.</span><span class="sxs-lookup"><span data-stu-id="80621-166">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="80621-167">No caso do contexto de esquema XAML padrão, a reflexão estática relata grande parte do tipo XAML de informações do sistema que é necessária para gerar nós de tipo XAML no fluxo do nó.</span><span class="sxs-lookup"><span data-stu-id="80621-167">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="80621-168">Para gerar um grafo de objeto de um fluxo de nó XAML, as declarações de namespace XAML devem existir para cada prefixo XAML usado na marcação original e registrados no fluxo do nó XAML.</span><span class="sxs-lookup"><span data-stu-id="80621-168">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="80621-169">Neste ponto, as instâncias estão sendo criadas e verdadeiro comportamento de mapeamento de tipo ocorre.</span><span class="sxs-lookup"><span data-stu-id="80621-169">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="80621-170">Se você precisar pré-popular informações de namespace XAML, nos casos em que o namespace XAML que você pretende que o contexto de esquema XAML use não esteja definido na marcação, uma técnica que você pode usar é declarar declarações de namespace XML no <xref:System.Xml.XmlParserContext> para um <xref:System.Xml.XmlReader>.</span><span class="sxs-lookup"><span data-stu-id="80621-170">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="80621-171">Em seguida, use essa <xref:System.Xml.XmlReader> como entrada para um construtor de leitor XAML ou <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span><span class="sxs-lookup"><span data-stu-id="80621-171">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="80621-172">Duas outras APIs que são relevantes para o tratamento de namespace XAML em .NET Framework serviços XAML são os atributos <xref:System.Windows.Markup.XmlnsDefinitionAttribute> e <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span><span class="sxs-lookup"><span data-stu-id="80621-172">Two other API that are relevant for XAML namespace handling in .NET Framework XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="80621-173">Esses atributos se aplicam a assemblies.</span><span class="sxs-lookup"><span data-stu-id="80621-173">These attributes apply to assemblies.</span></span> <span data-ttu-id="80621-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> é usado por um contexto de esquema XAML para interpretar qualquer declaração de namespace XAML que inclua um URI.</span><span class="sxs-lookup"><span data-stu-id="80621-174"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="80621-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> é usado por ferramentas que emitem XAML para que um namespace XAML específico possa ser serializado com um prefixo previsível.</span><span class="sxs-lookup"><span data-stu-id="80621-175"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="80621-176">Para obter mais informações, consulte [atributos CLR relacionados a XAML para tipos e bibliotecas personalizadas](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="80621-176">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](xaml-related-clr-attributes-for-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="80621-177">Consulte também</span><span class="sxs-lookup"><span data-stu-id="80621-177">See also</span></span>

- [<span data-ttu-id="80621-178">Noções básicas sobre estruturas e conceitos do fluxo de nó XAML</span><span class="sxs-lookup"><span data-stu-id="80621-178">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
