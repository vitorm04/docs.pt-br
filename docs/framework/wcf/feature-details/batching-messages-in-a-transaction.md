---
title: "Mensagens de lote em uma transação"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
helpviewer_keywords: batching messages [WCF]
ms.assetid: 53305392-e82e-4e89-aedc-3efb6ebcd28c
caps.latest.revision: "19"
author: Erikre
ms.author: erikre
manager: erikre
ms.openlocfilehash: 2aa633d2e89612549d1dbe6703e80f4a5e713bf0
ms.sourcegitcommit: 4f3fef493080a43e70e951223894768d36ce430a
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/21/2017
---
# <a name="batching-messages-in-a-transaction"></a><span data-ttu-id="41bcf-102">Mensagens de lote em uma transação</span><span class="sxs-lookup"><span data-stu-id="41bcf-102">Batching Messages in a Transaction</span></span>
<span data-ttu-id="41bcf-103">Na fila de aplicativos usam transações para garantir a correção e a entrega confiável de mensagens.</span><span class="sxs-lookup"><span data-stu-id="41bcf-103">Queued applications use transactions to ensure correctness and reliable delivery of messages.</span></span> <span data-ttu-id="41bcf-104">Transações, no entanto, são operações caras e podem reduzir drasticamente a taxa de transferência de mensagem.</span><span class="sxs-lookup"><span data-stu-id="41bcf-104">Transactions, however, are expensive operations and can dramatically reduce message throughput.</span></span> <span data-ttu-id="41bcf-105">Uma maneira de melhorar a taxa de transferência de mensagem é ter um aplicativo de ler e processar várias mensagens em uma única transação.</span><span class="sxs-lookup"><span data-stu-id="41bcf-105">One way to improve message throughput is to have an application read and process multiple messages within a single transaction.</span></span> <span data-ttu-id="41bcf-106">É a compensação entre desempenho e a recuperação: à medida que aumenta o número de mensagens em um lote, aumenta a quantidade de trabalho de recuperação que necessárias se as transações são revertidas.</span><span class="sxs-lookup"><span data-stu-id="41bcf-106">The trade-off is between performance and recovery: as the number of messages in a batch increases, so does the amount of recovery work that required if transactions are rolled back.</span></span> <span data-ttu-id="41bcf-107">É importante observar a diferença entre mensagens de lote em uma transação e sessões.</span><span class="sxs-lookup"><span data-stu-id="41bcf-107">It is important to note the difference between batching messages in a transaction and sessions.</span></span> <span data-ttu-id="41bcf-108">Um *sessão* é um agrupamento de mensagens relacionadas que são processados por um único aplicativo e confirmado como uma única unidade.</span><span class="sxs-lookup"><span data-stu-id="41bcf-108">A *session* is a grouping of related messages that are processed by a single application and committed as a single unit.</span></span> <span data-ttu-id="41bcf-109">Sessões geralmente são usadas quando um grupo de mensagens relacionadas deve ser processado em conjunto.</span><span class="sxs-lookup"><span data-stu-id="41bcf-109">Sessions are generally used when a group of related messages must be processed together.</span></span> <span data-ttu-id="41bcf-110">Um exemplo disso é um site de compra online.</span><span class="sxs-lookup"><span data-stu-id="41bcf-110">An example of this is an online shopping Web site.</span></span> <span data-ttu-id="41bcf-111">*Lotes* são usados para processar vários, não relacionados a mensagens de forma que a taxa de transferência de mensagem aumenta.</span><span class="sxs-lookup"><span data-stu-id="41bcf-111">*Batches* are used to process multiple, unrelated messages in a way that increases message throughput.</span></span> [!INCLUDE[crabout](../../../../includes/crabout-md.md)]<span data-ttu-id="41bcf-112">sessões, consulte [mensagens na fila de agrupamento em uma sessão](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span><span class="sxs-lookup"><span data-stu-id="41bcf-112"> sessions, see [Grouping Queued Messages in a Session](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span></span> <span data-ttu-id="41bcf-113">Mensagens em um lote também são processadas por um único aplicativo e confirmadas como uma única unidade, mas não pode haver nenhuma relação entre as mensagens no lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-113">Messages in a batch are also processed by a single application and committed as a single unit, but there may be no relationship between the messages in the batch.</span></span> <span data-ttu-id="41bcf-114">Mensagens de lote em uma transação são uma otimização que não seja alterado como o aplicativo é executado.</span><span class="sxs-lookup"><span data-stu-id="41bcf-114">Batching messages in a transaction is an optimization that does not change how the application runs.</span></span>  
  
## <a name="entering-batching-mode"></a><span data-ttu-id="41bcf-115">Inserir o modo de lote</span><span class="sxs-lookup"><span data-stu-id="41bcf-115">Entering Batching Mode</span></span>  
 <span data-ttu-id="41bcf-116">O <xref:System.ServiceModel.Description.TransactedBatchingBehavior> controles de comportamento de ponto de extremidade envio em lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-116">The <xref:System.ServiceModel.Description.TransactedBatchingBehavior> endpoint behavior controls batching.</span></span> <span data-ttu-id="41bcf-117">Adicionar esse comportamento de ponto de extremidade para um ponto de extremidade de serviço informa [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] para mensagens em lote em uma transação.</span><span class="sxs-lookup"><span data-stu-id="41bcf-117">Adding this endpoint behavior to a service endpoint tells [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] to batch messages in a transaction.</span></span> <span data-ttu-id="41bcf-118">Nem todas as mensagens necessitam de uma transação, de forma que apenas as mensagens que requerem uma transação são colocadas em um lote, e apenas as mensagens enviadas com as operações marcadas com `TransactionScopeRequired`  =  `true` e `TransactionAutoComplete`  =  `true` são considerado para um lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-118">Not all messages require a transaction, so only messages that require a transaction are placed in a batch, and only messages sent from operations marked with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true` are considered for a batch.</span></span> <span data-ttu-id="41bcf-119">Se todas as operações no contrato de serviço são marcadas com `TransactionScopeRequired`  =  `false` e `TransactionAutoComplete`  =  `false`, em seguida, o modo de lote nunca é inserido.</span><span class="sxs-lookup"><span data-stu-id="41bcf-119">If all operations on the service contract are marked with `TransactionScopeRequired` = `false` and `TransactionAutoComplete` = `false`, then batching mode is never entered.</span></span>  
  
## <a name="committing-a-transaction"></a><span data-ttu-id="41bcf-120">Confirmar uma transação</span><span class="sxs-lookup"><span data-stu-id="41bcf-120">Committing a Transaction</span></span>  
 <span data-ttu-id="41bcf-121">Uma transação em lote é confirmada com base no seguinte:</span><span class="sxs-lookup"><span data-stu-id="41bcf-121">A batched transaction is committed based on the following:</span></span>  
  
-   <span data-ttu-id="41bcf-122">`MaxBatchSize`.</span><span class="sxs-lookup"><span data-stu-id="41bcf-122">`MaxBatchSize`.</span></span> <span data-ttu-id="41bcf-123">Uma propriedade do <xref:System.ServiceModel.Description.TransactedBatchingBehavior> comportamento.</span><span class="sxs-lookup"><span data-stu-id="41bcf-123">A property of the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> behavior.</span></span> <span data-ttu-id="41bcf-124">Essa propriedade determina o número máximo de mensagens que são colocados em um lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-124">This property determines the maximum number of messages that are placed into a batch.</span></span> <span data-ttu-id="41bcf-125">Quando esse número for atingido, o lote será confirmado.</span><span class="sxs-lookup"><span data-stu-id="41bcf-125">When this number is reached, the batch is committed.</span></span> <span data-ttu-id="41bcf-126">Este é o valor não é um limite estrito, é possível confirmar um lote antes de receber esse número de mensagens.</span><span class="sxs-lookup"><span data-stu-id="41bcf-126">This is value is not a strict limit, it is possible to commit a batch before receiving this number of messages.</span></span>  
  
-   <span data-ttu-id="41bcf-127">`Transaction Timeout`.</span><span class="sxs-lookup"><span data-stu-id="41bcf-127">`Transaction Timeout`.</span></span> <span data-ttu-id="41bcf-128">Depois de decorrido 80 por cento do tempo-limite da transação, o lote será confirmado e é criado um novo lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-128">After 80 percent of the transaction's time-out has elapsed, the batch is committed and a new batch is created.</span></span> <span data-ttu-id="41bcf-129">Isso significa que se 20 por cento ou menos de tempo fornecido para a conclusão de uma transação permanece, o lote será confirmado.</span><span class="sxs-lookup"><span data-stu-id="41bcf-129">This means that if 20 percent or less of the time given for a transaction to complete remains, the batch is committed.</span></span>  
  
-   <span data-ttu-id="41bcf-130">`TransactionScopeRequired`.</span><span class="sxs-lookup"><span data-stu-id="41bcf-130">`TransactionScopeRequired`.</span></span> <span data-ttu-id="41bcf-131">Durante o processamento de um lote de mensagens, se [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] encontrar um que tenha `TransactionScopeRequired`  =  `false`, ele confirma o lote e reabre um novo lote no recebimento da primeira mensagem com `TransactionScopeRequired`  =  `true` e `TransactionAutoComplete` = `true`.</span><span class="sxs-lookup"><span data-stu-id="41bcf-131">When processing a batch of messages, if [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] finds one that has `TransactionScopeRequired` = `false`, it commits the batch and reopens a new batch on receipt of the first message with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true`.</span></span>  
  
-   <span data-ttu-id="41bcf-132">Se não mais houver mensagens na fila, o lote atual for confirmado, mesmo se o `MaxBatchSize` não foi atingido ou 80 por cento do tempo-limite da transação ainda não tiver decorrido.</span><span class="sxs-lookup"><span data-stu-id="41bcf-132">If no more messages exist in the queue, then the current batch is committed, even if the `MaxBatchSize` has not been reached or 80 percent of the transaction's time-out has not elapsed.</span></span>  
  
## <a name="leaving-batching-mode"></a><span data-ttu-id="41bcf-133">Deixar o modo de lote</span><span class="sxs-lookup"><span data-stu-id="41bcf-133">Leaving Batching Mode</span></span>  
 <span data-ttu-id="41bcf-134">Se uma mensagem em um lote faz com que a transação seja anulada, ocorrem as seguintes etapas:</span><span class="sxs-lookup"><span data-stu-id="41bcf-134">If a message in a batch causes the transaction to abort, the following steps occur:</span></span>  
  
1.  <span data-ttu-id="41bcf-135">Todo o lote de mensagens é revertido.</span><span class="sxs-lookup"><span data-stu-id="41bcf-135">The entire batch of messages is rolled back.</span></span>  
  
2.  <span data-ttu-id="41bcf-136">As mensagens são lidas um de cada vez até que o número de mensagens ler excede duas vezes o tamanho máximo de lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-136">Messages are read one at a time until the number of messages read exceeds twice the maximum batch size.</span></span>  
  
3.  <span data-ttu-id="41bcf-137">Modo de lote é inserido novamente.</span><span class="sxs-lookup"><span data-stu-id="41bcf-137">Batch mode is re-entered.</span></span>  
  
## <a name="choosing-the-batch-size"></a><span data-ttu-id="41bcf-138">Escolher o tamanho do lote</span><span class="sxs-lookup"><span data-stu-id="41bcf-138">Choosing the Batch Size</span></span>  
 <span data-ttu-id="41bcf-139">O tamanho do lote depende do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="41bcf-139">The size of a batch is application-dependent.</span></span> <span data-ttu-id="41bcf-140">O método empírico é a melhor maneira de chegar a um tamanho de lote ideal para o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="41bcf-140">The empirical method is the best way to arrive at an optimal batch size for the application.</span></span> <span data-ttu-id="41bcf-141">É importante lembrar-se ao escolher um tamanho de lote para escolher o tamanho de acordo com o modelo de implantação real do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="41bcf-141">It is important to remember when choosing a batch size to choose the size according to your application's actual deployment model.</span></span> <span data-ttu-id="41bcf-142">Por exemplo, ao implantar o aplicativo, se você precisar de um SQL server em um computador remoto e uma transação que abrange a fila e o SQL server, em seguida, o tamanho do lote é melhor determinado executando essa configuração exata.</span><span class="sxs-lookup"><span data-stu-id="41bcf-142">For example, when deploying the application, if you need an SQL server on a remote machine and a transaction that spans the queue and the SQL server, then the batch size is best determined by running this exact configuration.</span></span>  
  
## <a name="concurrency-and-batching"></a><span data-ttu-id="41bcf-143">Simultaneidade e envio em lote</span><span class="sxs-lookup"><span data-stu-id="41bcf-143">Concurrency and Batching</span></span>  
 <span data-ttu-id="41bcf-144">Para aumentar a taxa de transferência, você também pode ter muitos lotes executados simultaneamente.</span><span class="sxs-lookup"><span data-stu-id="41bcf-144">To increase throughput, you can also have many batches run concurrently.</span></span> <span data-ttu-id="41bcf-145">Definindo `ConcurrencyMode.Multiple` em `ServiceBehaviorAttribute`, habilitar o processamento em lotes simultâneos.</span><span class="sxs-lookup"><span data-stu-id="41bcf-145">By setting `ConcurrencyMode.Multiple` in `ServiceBehaviorAttribute`, you enable concurrent batching.</span></span>  
  
 <span data-ttu-id="41bcf-146">*Limitação de serviço* é um comportamento de serviço que é usado para indicar o número máximo de chamadas simultâneas pode ser feito no serviço.</span><span class="sxs-lookup"><span data-stu-id="41bcf-146">*Service throttling* is a service behavior that is used to indicate how many maximum concurrent calls can be made on the service.</span></span> <span data-ttu-id="41bcf-147">Quando usado com o envio em lote, isso é interpretado como muitos lotes simultâneos podem ser executados.</span><span class="sxs-lookup"><span data-stu-id="41bcf-147">When used with batching, this is interpreted as how many concurrent batches can be run.</span></span> <span data-ttu-id="41bcf-148">Se a limitação de serviço não for definida, [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] padrão é o máximo de chamadas simultâneas para 16.</span><span class="sxs-lookup"><span data-stu-id="41bcf-148">If the service throttling is not set, [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)] defaults the maximum concurrent calls to 16.</span></span> <span data-ttu-id="41bcf-149">Portanto, se o comportamento de lote foram adicionado por padrão, um máximo de 16 lotes pode ser ativo ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="41bcf-149">Thus, if batching behavior were added by default, a maximum of 16 batches can be active at the same time.</span></span> <span data-ttu-id="41bcf-150">É aconselhável ajustar a limitação de serviço e envio em lote com base na sua capacidade.</span><span class="sxs-lookup"><span data-stu-id="41bcf-150">It is best to tune the service throttling and batching based on your capacity.</span></span> <span data-ttu-id="41bcf-151">Por exemplo, se a fila tem 100 mensagens e um lote de 20 for desejado, com o máximo de chamadas simultâneas definido como 16 não é útil porque, dependendo da taxa de transferência, 16 transações podem ser ativo, semelhante a não ter o envio em lote ativado.</span><span class="sxs-lookup"><span data-stu-id="41bcf-151">For example, if the queue has 100 messages and a batch of 20 is desired, having the maximum concurrent calls set to 16 is not useful because, depending on throughput, 16 transactions could be active, similar to not having batching turned on.</span></span> <span data-ttu-id="41bcf-152">Portanto, quando ajustar o desempenho, ou não ter o processamento em lotes simultâneos ou tenha lotes simultâneos com o tamanho de limitação de serviço correta.</span><span class="sxs-lookup"><span data-stu-id="41bcf-152">Therefore, when fine-tuning for performance, either do not have concurrent batching or have concurrent batching with the correct service throttle size.</span></span>  
  
## <a name="batching-and-multiple-endpoints"></a><span data-ttu-id="41bcf-153">Envio em lote e vários pontos de extremidade</span><span class="sxs-lookup"><span data-stu-id="41bcf-153">Batching and Multiple Endpoints</span></span>  
 <span data-ttu-id="41bcf-154">Um ponto de extremidade é composto de um endereço e um contrato.</span><span class="sxs-lookup"><span data-stu-id="41bcf-154">An endpoint is composed of an address and a contract.</span></span> <span data-ttu-id="41bcf-155">Pode haver vários pontos de extremidade que compartilham a mesma associação.</span><span class="sxs-lookup"><span data-stu-id="41bcf-155">There may be multiple endpoints that share the same binding.</span></span> <span data-ttu-id="41bcf-156">É possível que dois pontos de extremidade compartilhar a mesma associação e escutar identificador de recurso uniforme (URI) ou o endereço da fila.</span><span class="sxs-lookup"><span data-stu-id="41bcf-156">It is possible for two endpoints to share the same binding and listen Uniform Resource Identifier (URI), or queue address.</span></span> <span data-ttu-id="41bcf-157">Se dois pontos de extremidade estão lendo da mesma fila e transacionado comportamento de lote é adicionado a ambos os pontos de extremidade, um conflito no lote tamanhos especificados podem surgir.</span><span class="sxs-lookup"><span data-stu-id="41bcf-157">If two endpoints are reading from the same queue, and transacted batching behavior is added to both endpoints, a conflict in the batch sizes specified could arise.</span></span> <span data-ttu-id="41bcf-158">Isto pode ser resolvido com a implementação de envio em lote usando o tamanho do lote mínimo especificado entre os dois comportamentos de envio em lote transacionados.</span><span class="sxs-lookup"><span data-stu-id="41bcf-158">This is resolved by implementing batching using the minimal batch size specified between the two transacted batching behaviors.</span></span> <span data-ttu-id="41bcf-159">Nesse cenário, se um dos pontos de extremidade não especificar o lote transacionado, em seguida, os pontos de extremidade não usaria envio em lote.</span><span class="sxs-lookup"><span data-stu-id="41bcf-159">In this scenario, if one of the endpoints does not specify transacted batching, then both endpoints would not use batching.</span></span>  
  
## <a name="example"></a><span data-ttu-id="41bcf-160">Exemplo</span><span class="sxs-lookup"><span data-stu-id="41bcf-160">Example</span></span>  
 <span data-ttu-id="41bcf-161">O exemplo a seguir mostra como especificar o `TransactedBatchingBehavior` em um arquivo de configuração.</span><span class="sxs-lookup"><span data-stu-id="41bcf-161">The following example shows how to specify the `TransactedBatchingBehavior` in a configuration file.</span></span>  
  
```xml  
<behaviors>
  <endpointBehaviors>
    <behavior name="TransactedBatchingBehavior"
              maxBatchSize="100" />
  </endpointBehaviors>
</behaviors>
```  
  
 <span data-ttu-id="41bcf-162">O exemplo a seguir mostra como especificar o <xref:System.ServiceModel.Description.TransactedBatchingBehavior> no código.</span><span class="sxs-lookup"><span data-stu-id="41bcf-162">The following example shows how to specify the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> in code.</span></span>  
  
```csharp
using (ServiceHost serviceHost = new ServiceHost(typeof(OrderProcessorService)))
{
     ServiceEndpoint sep = ServiceHost.AddServiceEndpoint(typeof(IOrderProcessor), new NetMsmqBinding(), "net.msmq://localhost/private/ServiceModelSamplesTransacted");
     sep.Behaviors.Add(new TransactedBatchingBehavior(100));
     
     // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();
  
    // The service can now be accessed.
    Console.WriteLine("The service is ready.");
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.WriteLine();
    Console.ReadLine();
  
    // Close the ServiceHostB to shut down the service.
    serviceHost.Close();
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="41bcf-163">Consulte também</span><span class="sxs-lookup"><span data-stu-id="41bcf-163">See Also</span></span>  
 [<span data-ttu-id="41bcf-164">Visão geral de filas</span><span class="sxs-lookup"><span data-stu-id="41bcf-164">Queues Overview</span></span>](../../../../docs/framework/wcf/feature-details/queues-overview.md)  
 [<span data-ttu-id="41bcf-165">Enfileiramento no WCF</span><span class="sxs-lookup"><span data-stu-id="41bcf-165">Queuing in WCF</span></span>](../../../../docs/framework/wcf/feature-details/queuing-in-wcf.md)
