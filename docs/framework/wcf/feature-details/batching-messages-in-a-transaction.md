---
title: Mensagens de lote em uma transação
ms.date: 03/30/2017
helpviewer_keywords:
- batching messages [WCF]
ms.assetid: 53305392-e82e-4e89-aedc-3efb6ebcd28c
ms.openlocfilehash: be9661525c960ae558d21b05781007b81b8a3f56
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79185447"
---
# <a name="batching-messages-in-a-transaction"></a><span data-ttu-id="f0e2d-102">Mensagens de lote em uma transação</span><span class="sxs-lookup"><span data-stu-id="f0e2d-102">Batching Messages in a Transaction</span></span>
<span data-ttu-id="f0e2d-103">Os aplicativos enfileirados usam transações para garantir a correção e a entrega confiável das mensagens.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-103">Queued applications use transactions to ensure correctness and reliable delivery of messages.</span></span> <span data-ttu-id="f0e2d-104">As transações, no entanto, são operações caras e podem reduzir drasticamente o throughput de mensagens.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-104">Transactions, however, are expensive operations and can dramatically reduce message throughput.</span></span> <span data-ttu-id="f0e2d-105">Uma maneira de melhorar o throughput de mensagens é ter um aplicativo lido e processar várias mensagens dentro de uma única transação.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-105">One way to improve message throughput is to have an application read and process multiple messages within a single transaction.</span></span> <span data-ttu-id="f0e2d-106">A troca é entre desempenho e recuperação: à medida que o número de mensagens em um lote aumenta, também aumenta a quantidade de trabalho de recuperação que é necessário se as transações forem revertidas.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-106">The trade-off is between performance and recovery: as the number of messages in a batch increases, so does the amount of recovery work that required if transactions are rolled back.</span></span> <span data-ttu-id="f0e2d-107">É importante notar a diferença entre o loteamento de mensagens em uma transação e sessões.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-107">It is important to note the difference between batching messages in a transaction and sessions.</span></span> <span data-ttu-id="f0e2d-108">Uma *sessão* é um agrupamento de mensagens relacionadas que são processadas por um único aplicativo e comprometidas como uma única unidade.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-108">A *session* is a grouping of related messages that are processed by a single application and committed as a single unit.</span></span> <span data-ttu-id="f0e2d-109">As sessões são geralmente usadas quando um grupo de mensagens relacionadas deve ser processada em conjunto.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-109">Sessions are generally used when a group of related messages must be processed together.</span></span> <span data-ttu-id="f0e2d-110">Um exemplo disso é um site de compras online.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-110">An example of this is an online shopping Web site.</span></span> <span data-ttu-id="f0e2d-111">Os lotes são *usados* para processar várias mensagens não relacionadas de uma forma que aumenta o throughput de mensagens.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-111">*Batches* are used to process multiple, unrelated messages in a way that increases message throughput.</span></span> <span data-ttu-id="f0e2d-112">Para obter mais informações sobre as sessões, consulte [Agrupamento de Mensagens Enfileiradas em uma Sessão](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span><span class="sxs-lookup"><span data-stu-id="f0e2d-112">For more information about sessions, see [Grouping Queued Messages in a Session](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span></span> <span data-ttu-id="f0e2d-113">As mensagens em um lote também são processadas por um único aplicativo e comprometidas como uma única unidade, mas pode não haver relação entre as mensagens no lote.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-113">Messages in a batch are also processed by a single application and committed as a single unit, but there may be no relationship between the messages in the batch.</span></span> <span data-ttu-id="f0e2d-114">O loteamento de mensagens em uma transação é uma otimização que não altera a forma como o aplicativo é executado.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-114">Batching messages in a transaction is an optimization that does not change how the application runs.</span></span>  
  
## <a name="entering-batching-mode"></a><span data-ttu-id="f0e2d-115">Entrando no modo de loteamento</span><span class="sxs-lookup"><span data-stu-id="f0e2d-115">Entering Batching Mode</span></span>  
 <span data-ttu-id="f0e2d-116">O <xref:System.ServiceModel.Description.TransactedBatchingBehavior> comportamento do ponto final controla o loteamento.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-116">The <xref:System.ServiceModel.Description.TransactedBatchingBehavior> endpoint behavior controls batching.</span></span> <span data-ttu-id="f0e2d-117">Adicionar esse comportamento de ponto final a um ponto final de serviço diz ao Windows Communication Foundation (WCF) para em lote de mensagens em uma transação.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-117">Adding this endpoint behavior to a service endpoint tells Windows Communication Foundation (WCF) to batch messages in a transaction.</span></span> <span data-ttu-id="f0e2d-118">Nem todas as mensagens exigem uma transação, portanto, apenas as mensagens que requerem uma `TransactionScopeRequired`  =  `true` `TransactionAutoComplete`  =  `true` transação são colocadas em um lote, e apenas mensagens enviadas de operações marcadas com e são consideradas para um lote.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-118">Not all messages require a transaction, so only messages that require a transaction are placed in a batch, and only messages sent from operations marked with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true` are considered for a batch.</span></span> <span data-ttu-id="f0e2d-119">Se todas as operações do `TransactionScopeRequired`  =  `false` `TransactionAutoComplete`  =  `false`contrato de serviço forem marcadas com e , então o modo de loteamento nunca será inserido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-119">If all operations on the service contract are marked with `TransactionScopeRequired` = `false` and `TransactionAutoComplete` = `false`, then batching mode is never entered.</span></span>  
  
## <a name="committing-a-transaction"></a><span data-ttu-id="f0e2d-120">Confirmando uma transação</span><span class="sxs-lookup"><span data-stu-id="f0e2d-120">Committing a Transaction</span></span>  
 <span data-ttu-id="f0e2d-121">Uma transação em lote é cometida com base no seguinte:</span><span class="sxs-lookup"><span data-stu-id="f0e2d-121">A batched transaction is committed based on the following:</span></span>  
  
- <span data-ttu-id="f0e2d-122">`MaxBatchSize`.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-122">`MaxBatchSize`.</span></span> <span data-ttu-id="f0e2d-123">Uma propriedade <xref:System.ServiceModel.Description.TransactedBatchingBehavior> do comportamento.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-123">A property of the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> behavior.</span></span> <span data-ttu-id="f0e2d-124">Esta propriedade determina o número máximo de mensagens que são colocadas em um lote.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-124">This property determines the maximum number of messages that are placed into a batch.</span></span> <span data-ttu-id="f0e2d-125">Quando esse número é atingido, o lote é comprometido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-125">When this number is reached, the batch is committed.</span></span> <span data-ttu-id="f0e2d-126">Este é o valor não é um limite rigoroso, é possível cometer um lote antes de receber esse número de mensagens.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-126">This is value is not a strict limit, it is possible to commit a batch before receiving this number of messages.</span></span>  
  
- <span data-ttu-id="f0e2d-127">`Transaction Timeout`.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-127">`Transaction Timeout`.</span></span> <span data-ttu-id="f0e2d-128">Depois que 80% do tempo de saída da transação é decorrido, o lote é comprometido e um novo lote é criado.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-128">After 80 percent of the transaction's time-out has elapsed, the batch is committed and a new batch is created.</span></span> <span data-ttu-id="f0e2d-129">Isso significa que se 20% ou menos do tempo dado para uma transação ser concluída, o lote será comprometido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-129">This means that if 20 percent or less of the time given for a transaction to complete remains, the batch is committed.</span></span>  
  
- <span data-ttu-id="f0e2d-130">`TransactionScopeRequired`.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-130">`TransactionScopeRequired`.</span></span> <span data-ttu-id="f0e2d-131">Ao processar um lote de mensagens, se `TransactionScopeRequired`  =  `false`o WCF encontrar um que tenha, ele compromete o `TransactionScopeRequired`  =  `true` `TransactionAutoComplete`  =  `true`lote e reabre um novo lote no recebimento da primeira mensagem com e .</span><span class="sxs-lookup"><span data-stu-id="f0e2d-131">When processing a batch of messages, if WCF finds one that has `TransactionScopeRequired` = `false`, it commits the batch and reopens a new batch on receipt of the first message with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true`.</span></span>  
  
- <span data-ttu-id="f0e2d-132">Se não houver mais mensagens na fila, o lote atual `MaxBatchSize` será comprometido, mesmo que o lote não tenha sido atingido ou 80% do tempo de intervalo da transação não tenha transcorrido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-132">If no more messages exist in the queue, then the current batch is committed, even if the `MaxBatchSize` has not been reached or 80 percent of the transaction's time-out has not elapsed.</span></span>  
  
## <a name="leaving-batching-mode"></a><span data-ttu-id="f0e2d-133">Deixando o modo de loteamento</span><span class="sxs-lookup"><span data-stu-id="f0e2d-133">Leaving Batching Mode</span></span>  
 <span data-ttu-id="f0e2d-134">Se uma mensagem em um lote fizer com que a transação aborte, ocorrerão as seguintes etapas:</span><span class="sxs-lookup"><span data-stu-id="f0e2d-134">If a message in a batch causes the transaction to abort, the following steps occur:</span></span>  
  
1. <span data-ttu-id="f0e2d-135">Todo o lote de mensagens é revertido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-135">The entire batch of messages is rolled back.</span></span>  
  
2. <span data-ttu-id="f0e2d-136">As mensagens são lidas uma de cada vez até que o número de mensagens lidas exceda o dobro do tamanho máximo do lote.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-136">Messages are read one at a time until the number of messages read exceeds twice the maximum batch size.</span></span>  
  
3. <span data-ttu-id="f0e2d-137">O modo de lote é reinserido.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-137">Batch mode is re-entered.</span></span>  
  
## <a name="choosing-the-batch-size"></a><span data-ttu-id="f0e2d-138">Escolhendo o tamanho do lote</span><span class="sxs-lookup"><span data-stu-id="f0e2d-138">Choosing the Batch Size</span></span>  
 <span data-ttu-id="f0e2d-139">O tamanho de um lote depende do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-139">The size of a batch is application-dependent.</span></span> <span data-ttu-id="f0e2d-140">O método empírico é a melhor maneira de chegar a um tamanho de lote ideal para a aplicação.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-140">The empirical method is the best way to arrive at an optimal batch size for the application.</span></span> <span data-ttu-id="f0e2d-141">É importante lembrar ao escolher um tamanho de lote para escolher o tamanho de acordo com o modelo real de implantação do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-141">It is important to remember when choosing a batch size to choose the size according to your application's actual deployment model.</span></span> <span data-ttu-id="f0e2d-142">Por exemplo, ao implantar o aplicativo, se você precisar de um servidor SQL em uma máquina remota e uma transação que abrange a fila e o servidor SQL, então o tamanho do lote é melhor determinado executando essa configuração exata.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-142">For example, when deploying the application, if you need an SQL server on a remote machine and a transaction that spans the queue and the SQL server, then the batch size is best determined by running this exact configuration.</span></span>  
  
## <a name="concurrency-and-batching"></a><span data-ttu-id="f0e2d-143">Simultâneo e Loteamento</span><span class="sxs-lookup"><span data-stu-id="f0e2d-143">Concurrency and Batching</span></span>  
 <span data-ttu-id="f0e2d-144">Para aumentar o throughput, você também pode ter muitos lotes executados simultaneamente.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-144">To increase throughput, you can also have many batches run concurrently.</span></span> <span data-ttu-id="f0e2d-145">Ao `ConcurrencyMode.Multiple` `ServiceBehaviorAttribute`configurar, você habilita o loteamento simultâneo.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-145">By setting `ConcurrencyMode.Multiple` in `ServiceBehaviorAttribute`, you enable concurrent batching.</span></span>  
  
 <span data-ttu-id="f0e2d-146">*O estrangulamento do* serviço é um comportamento de serviço que é usado para indicar quantas chamadas simultâneas máximas podem ser feitas no serviço.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-146">*Service throttling* is a service behavior that is used to indicate how many maximum concurrent calls can be made on the service.</span></span> <span data-ttu-id="f0e2d-147">Quando usado com loteamento, isso é interpretado como quantos lotes simultâneos podem ser executados.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-147">When used with batching, this is interpreted as how many concurrent batches can be run.</span></span> <span data-ttu-id="f0e2d-148">Se o estrangulamento do serviço não estiver definido, o WCF será padrão das chamadas máximas simultâneas para 16.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-148">If the service throttling is not set, WCF defaults the maximum concurrent calls to 16.</span></span> <span data-ttu-id="f0e2d-149">Assim, se o comportamento de loteamento foi adicionado por padrão, um máximo de 16 lotes podem estar ativos ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-149">Thus, if batching behavior were added by default, a maximum of 16 batches can be active at the same time.</span></span> <span data-ttu-id="f0e2d-150">É melhor ajustar o serviço de estrangulamento e loteamento com base na sua capacidade.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-150">It is best to tune the service throttling and batching based on your capacity.</span></span> <span data-ttu-id="f0e2d-151">Por exemplo, se a fila tiver 100 mensagens e um lote de 20 for desejado, ter as chamadas simultâneas máximas definidas para 16 não é útil porque, dependendo do throughput, 16 transações podem estar ativas, semelhante a não ter o lote ligado.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-151">For example, if the queue has 100 messages and a batch of 20 is desired, having the maximum concurrent calls set to 16 is not useful because, depending on throughput, 16 transactions could be active, similar to not having batching turned on.</span></span> <span data-ttu-id="f0e2d-152">Portanto, ao ajustar o desempenho, não possuem loteamento simultâneo ou possuem loteamento simultâneo com o tamanho correto do acelerador de serviço.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-152">Therefore, when fine-tuning for performance, either do not have concurrent batching or have concurrent batching with the correct service throttle size.</span></span>  
  
## <a name="batching-and-multiple-endpoints"></a><span data-ttu-id="f0e2d-153">Loteamento e vários pontos finais</span><span class="sxs-lookup"><span data-stu-id="f0e2d-153">Batching and Multiple Endpoints</span></span>  
 <span data-ttu-id="f0e2d-154">Um ponto final é composto por um endereço e um contrato.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-154">An endpoint is composed of an address and a contract.</span></span> <span data-ttu-id="f0e2d-155">Pode haver vários pontos finais que compartilham a mesma vinculação.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-155">There may be multiple endpoints that share the same binding.</span></span> <span data-ttu-id="f0e2d-156">É possível que dois pontos finais compartilhem a mesma vinculação e ouçam uri (Uniform Resource Identifier, identificador de recursos uniformes) ou endereço de fila.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-156">It is possible for two endpoints to share the same binding and listen Uniform Resource Identifier (URI), or queue address.</span></span> <span data-ttu-id="f0e2d-157">Se dois pontos finais estiverem sendo lidos na mesma fila e o comportamento de loteamento transacionado for adicionado a ambos os pontos finais, pode surgir um conflito nos tamanhos de lote especificados.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-157">If two endpoints are reading from the same queue, and transacted batching behavior is added to both endpoints, a conflict in the batch sizes specified could arise.</span></span> <span data-ttu-id="f0e2d-158">Isso é resolvido implementando o lote usando o tamanho mínimo do lote especificado entre os dois comportamentos de lote ação transacionada.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-158">This is resolved by implementing batching using the minimal batch size specified between the two transacted batching behaviors.</span></span> <span data-ttu-id="f0e2d-159">Neste cenário, se um dos pontos finais não especificar o lote amento transacionado, ambos os pontos finais não usariam o loteamento.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-159">In this scenario, if one of the endpoints does not specify transacted batching, then both endpoints would not use batching.</span></span>  
  
## <a name="example"></a><span data-ttu-id="f0e2d-160">Exemplo</span><span class="sxs-lookup"><span data-stu-id="f0e2d-160">Example</span></span>  
 <span data-ttu-id="f0e2d-161">O exemplo a seguir `TransactedBatchingBehavior` mostra como especificar o arquivo em uma configuração.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-161">The following example shows how to specify the `TransactedBatchingBehavior` in a configuration file.</span></span>  
  
```xml  
<behaviors>
  <endpointBehaviors>
    <behavior name="TransactedBatchingBehavior"
              maxBatchSize="100" />
  </endpointBehaviors>
</behaviors>
```  
  
 <span data-ttu-id="f0e2d-162">O exemplo a seguir <xref:System.ServiceModel.Description.TransactedBatchingBehavior> mostra como especificar o código em.</span><span class="sxs-lookup"><span data-stu-id="f0e2d-162">The following example shows how to specify the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> in code.</span></span>  
  
```csharp
using (ServiceHost serviceHost = new ServiceHost(typeof(OrderProcessorService)))
{
     ServiceEndpoint sep = ServiceHost.AddServiceEndpoint(typeof(IOrderProcessor), new NetMsmqBinding(), "net.msmq://localhost/private/ServiceModelSamplesTransacted");
     sep.Behaviors.Add(new TransactedBatchingBehavior(100));

     // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();
  
    // The service can now be accessed.
    Console.WriteLine("The service is ready.");
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.WriteLine();
    Console.ReadLine();
  
    // Close the ServiceHostB to shut down the service.
    serviceHost.Close();
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="f0e2d-163">Confira também</span><span class="sxs-lookup"><span data-stu-id="f0e2d-163">See also</span></span>

- [<span data-ttu-id="f0e2d-164">Visão geral de filas</span><span class="sxs-lookup"><span data-stu-id="f0e2d-164">Queues Overview</span></span>](../../../../docs/framework/wcf/feature-details/queues-overview.md)
- [<span data-ttu-id="f0e2d-165">Enfileiramento no WCF</span><span class="sxs-lookup"><span data-stu-id="f0e2d-165">Queuing in WCF</span></span>](../../../../docs/framework/wcf/feature-details/queuing-in-wcf.md)
