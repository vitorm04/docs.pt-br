---
title: Mensagens de lote em uma transação
ms.date: 03/30/2017
helpviewer_keywords:
- batching messages [WCF]
ms.assetid: 53305392-e82e-4e89-aedc-3efb6ebcd28c
ms.openlocfilehash: 2d820087973e689514a0a19a7adc912f49e9d0a2
ms.sourcegitcommit: 558d78d2a68acd4c95ef23231c8b4e4c7bac3902
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/09/2019
ms.locfileid: "59310519"
---
# <a name="batching-messages-in-a-transaction"></a><span data-ttu-id="9a19b-102">Mensagens de lote em uma transação</span><span class="sxs-lookup"><span data-stu-id="9a19b-102">Batching Messages in a Transaction</span></span>
<span data-ttu-id="9a19b-103">Aplicativos em fila usam transações para garantir a correção e entrega confiável de mensagens.</span><span class="sxs-lookup"><span data-stu-id="9a19b-103">Queued applications use transactions to ensure correctness and reliable delivery of messages.</span></span> <span data-ttu-id="9a19b-104">Transações, no entanto, são operações caras e podem reduzir drasticamente a taxa de transferência de mensagem.</span><span class="sxs-lookup"><span data-stu-id="9a19b-104">Transactions, however, are expensive operations and can dramatically reduce message throughput.</span></span> <span data-ttu-id="9a19b-105">Uma maneira para melhorar a taxa de transferência de mensagem é ter um aplicativo de ler e processar várias mensagens em uma única transação.</span><span class="sxs-lookup"><span data-stu-id="9a19b-105">One way to improve message throughput is to have an application read and process multiple messages within a single transaction.</span></span> <span data-ttu-id="9a19b-106">A compensação está entre desempenho e recuperação: conforme aumenta o número de mensagens em um lote, aumenta a quantidade de trabalho de recuperação que necessário se as transações serão revertidas.</span><span class="sxs-lookup"><span data-stu-id="9a19b-106">The trade-off is between performance and recovery: as the number of messages in a batch increases, so does the amount of recovery work that required if transactions are rolled back.</span></span> <span data-ttu-id="9a19b-107">É importante observar a diferença entre o envio em lote de mensagens em uma transação e sessões.</span><span class="sxs-lookup"><span data-stu-id="9a19b-107">It is important to note the difference between batching messages in a transaction and sessions.</span></span> <span data-ttu-id="9a19b-108">Um *sessão* é um agrupamento de mensagens relacionadas que são processados por um único aplicativo e confirmado como uma única unidade.</span><span class="sxs-lookup"><span data-stu-id="9a19b-108">A *session* is a grouping of related messages that are processed by a single application and committed as a single unit.</span></span> <span data-ttu-id="9a19b-109">Sessões geralmente são usadas quando um grupo de mensagens relacionadas deve ser processado em conjunto.</span><span class="sxs-lookup"><span data-stu-id="9a19b-109">Sessions are generally used when a group of related messages must be processed together.</span></span> <span data-ttu-id="9a19b-110">Um exemplo disso é um site de compra online.</span><span class="sxs-lookup"><span data-stu-id="9a19b-110">An example of this is an online shopping Web site.</span></span> <span data-ttu-id="9a19b-111">*Lotes* são usados para processar vários, não relacionados a mensagens de forma que a taxa de transferência de mensagem aumenta.</span><span class="sxs-lookup"><span data-stu-id="9a19b-111">*Batches* are used to process multiple, unrelated messages in a way that increases message throughput.</span></span> <span data-ttu-id="9a19b-112">Para obter mais informações sobre as sessões, consulte [mensagens na fila de agrupamento em uma sessão](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span><span class="sxs-lookup"><span data-stu-id="9a19b-112">For more information about sessions, see [Grouping Queued Messages in a Session](../../../../docs/framework/wcf/feature-details/grouping-queued-messages-in-a-session.md).</span></span> <span data-ttu-id="9a19b-113">Mensagens em um lote também são processadas por um único aplicativo e confirmadas como uma única unidade, mas não pode haver nenhuma relação entre as mensagens no lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-113">Messages in a batch are also processed by a single application and committed as a single unit, but there may be no relationship between the messages in the batch.</span></span> <span data-ttu-id="9a19b-114">Mensagens em lote em uma transação são uma otimização que não são alterados como o aplicativo é executado.</span><span class="sxs-lookup"><span data-stu-id="9a19b-114">Batching messages in a transaction is an optimization that does not change how the application runs.</span></span>  
  
## <a name="entering-batching-mode"></a><span data-ttu-id="9a19b-115">Inserir o modo de lote</span><span class="sxs-lookup"><span data-stu-id="9a19b-115">Entering Batching Mode</span></span>  
 <span data-ttu-id="9a19b-116">O <xref:System.ServiceModel.Description.TransactedBatchingBehavior> controles de comportamento de ponto de extremidade envio em lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-116">The <xref:System.ServiceModel.Description.TransactedBatchingBehavior> endpoint behavior controls batching.</span></span> <span data-ttu-id="9a19b-117">Adicionar esse comportamento de ponto de extremidade para um ponto de extremidade de serviço informa ao Windows Communication Foundation (WCF) para mensagens em lote em uma transação.</span><span class="sxs-lookup"><span data-stu-id="9a19b-117">Adding this endpoint behavior to a service endpoint tells Windows Communication Foundation (WCF) to batch messages in a transaction.</span></span> <span data-ttu-id="9a19b-118">Nem todas as mensagens exigem uma transação, portanto, apenas as mensagens que exigem uma transação são colocadas em um lote, e apenas as mensagens enviadas de operações marcadas com `TransactionScopeRequired`  =  `true` e `TransactionAutoComplete`  =  `true` são consideradas para um lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-118">Not all messages require a transaction, so only messages that require a transaction are placed in a batch, and only messages sent from operations marked with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true` are considered for a batch.</span></span> <span data-ttu-id="9a19b-119">Se todas as operações no contrato de serviço são marcadas com `TransactionScopeRequired`  =  `false` e `TransactionAutoComplete`  =  `false`, em seguida, o modo de lote nunca é inserido.</span><span class="sxs-lookup"><span data-stu-id="9a19b-119">If all operations on the service contract are marked with `TransactionScopeRequired` = `false` and `TransactionAutoComplete` = `false`, then batching mode is never entered.</span></span>  
  
## <a name="committing-a-transaction"></a><span data-ttu-id="9a19b-120">Confirmar uma transação</span><span class="sxs-lookup"><span data-stu-id="9a19b-120">Committing a Transaction</span></span>  
 <span data-ttu-id="9a19b-121">Uma transação em lote é confirmada com base no seguinte:</span><span class="sxs-lookup"><span data-stu-id="9a19b-121">A batched transaction is committed based on the following:</span></span>  
  
-   `MaxBatchSize`<span data-ttu-id="9a19b-122">.</span><span class="sxs-lookup"><span data-stu-id="9a19b-122">.</span></span> <span data-ttu-id="9a19b-123">Uma propriedade do <xref:System.ServiceModel.Description.TransactedBatchingBehavior> comportamento.</span><span class="sxs-lookup"><span data-stu-id="9a19b-123">A property of the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> behavior.</span></span> <span data-ttu-id="9a19b-124">Essa propriedade determina o número máximo de mensagens que são colocados em um lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-124">This property determines the maximum number of messages that are placed into a batch.</span></span> <span data-ttu-id="9a19b-125">Quando esse número for atingido, o lote será confirmado.</span><span class="sxs-lookup"><span data-stu-id="9a19b-125">When this number is reached, the batch is committed.</span></span> <span data-ttu-id="9a19b-126">Esse é o valor não é um limite estrito, é possível confirmar um lote antes de receber esse número de mensagens.</span><span class="sxs-lookup"><span data-stu-id="9a19b-126">This is value is not a strict limit, it is possible to commit a batch before receiving this number of messages.</span></span>  
  
-   `Transaction Timeout`<span data-ttu-id="9a19b-127">.</span><span class="sxs-lookup"><span data-stu-id="9a19b-127">.</span></span> <span data-ttu-id="9a19b-128">Depois de decorrido 80 por cento do tempo de limite da transação, o lote será confirmado e um novo lote é criado.</span><span class="sxs-lookup"><span data-stu-id="9a19b-128">After 80 percent of the transaction's time-out has elapsed, the batch is committed and a new batch is created.</span></span> <span data-ttu-id="9a19b-129">Isso significa que se 20 por cento ou menos do tempo fornecido para a conclusão de uma transação permanece, o lote será confirmado.</span><span class="sxs-lookup"><span data-stu-id="9a19b-129">This means that if 20 percent or less of the time given for a transaction to complete remains, the batch is committed.</span></span>  
  
-   `TransactionScopeRequired`<span data-ttu-id="9a19b-130">.</span><span class="sxs-lookup"><span data-stu-id="9a19b-130">.</span></span> <span data-ttu-id="9a19b-131">Ao processar um lote de mensagens, se WCF encontrar um que tenha `TransactionScopeRequired`  =  `false`, ele confirma o lote e reabre um novo lote após o recebimento da primeira mensagem com `TransactionScopeRequired`  =  `true` e `TransactionAutoComplete`  = `true`.</span><span class="sxs-lookup"><span data-stu-id="9a19b-131">When processing a batch of messages, if WCF finds one that has `TransactionScopeRequired` = `false`, it commits the batch and reopens a new batch on receipt of the first message with `TransactionScopeRequired` = `true` and `TransactionAutoComplete` = `true`.</span></span>  
  
-   <span data-ttu-id="9a19b-132">Se mais nenhuma mensagem existe na fila e, em seguida, o lote atual for confirmado, mesmo se o `MaxBatchSize` não tiver sido atingida ou 80 por cento do tempo de limite da transação ainda não tiver decorrido.</span><span class="sxs-lookup"><span data-stu-id="9a19b-132">If no more messages exist in the queue, then the current batch is committed, even if the `MaxBatchSize` has not been reached or 80 percent of the transaction's time-out has not elapsed.</span></span>  
  
## <a name="leaving-batching-mode"></a><span data-ttu-id="9a19b-133">Sair do modo de lote</span><span class="sxs-lookup"><span data-stu-id="9a19b-133">Leaving Batching Mode</span></span>  
 <span data-ttu-id="9a19b-134">Se uma mensagem em um lote faz com que a transação seja anulada, ocorrem as seguintes etapas:</span><span class="sxs-lookup"><span data-stu-id="9a19b-134">If a message in a batch causes the transaction to abort, the following steps occur:</span></span>  
  
1. <span data-ttu-id="9a19b-135">Todo o lote de mensagens é revertido.</span><span class="sxs-lookup"><span data-stu-id="9a19b-135">The entire batch of messages is rolled back.</span></span>  
  
2. <span data-ttu-id="9a19b-136">As mensagens são lidas uma de cada vez até que o número de mensagens lidas excede duas vezes o tamanho máximo de lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-136">Messages are read one at a time until the number of messages read exceeds twice the maximum batch size.</span></span>  
  
3. <span data-ttu-id="9a19b-137">Modo de lote é inserido novamente.</span><span class="sxs-lookup"><span data-stu-id="9a19b-137">Batch mode is re-entered.</span></span>  
  
## <a name="choosing-the-batch-size"></a><span data-ttu-id="9a19b-138">Escolhendo o tamanho do lote</span><span class="sxs-lookup"><span data-stu-id="9a19b-138">Choosing the Batch Size</span></span>  
 <span data-ttu-id="9a19b-139">O tamanho de um lote é dependente de aplicativo.</span><span class="sxs-lookup"><span data-stu-id="9a19b-139">The size of a batch is application-dependent.</span></span> <span data-ttu-id="9a19b-140">O método empírico é a melhor maneira de chegar a um tamanho de lote ideal para o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="9a19b-140">The empirical method is the best way to arrive at an optimal batch size for the application.</span></span> <span data-ttu-id="9a19b-141">É importante lembrar-se ao escolher um tamanho de lote para escolher o tamanho de acordo com o modelo de implantação real do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="9a19b-141">It is important to remember when choosing a batch size to choose the size according to your application's actual deployment model.</span></span> <span data-ttu-id="9a19b-142">Por exemplo, ao implantar o aplicativo, se você precisar de um SQL server em um computador remoto e uma transação que abrange a fila e o SQL server, em seguida, o tamanho do lote é melhor determinado executando essa configuração exata.</span><span class="sxs-lookup"><span data-stu-id="9a19b-142">For example, when deploying the application, if you need an SQL server on a remote machine and a transaction that spans the queue and the SQL server, then the batch size is best determined by running this exact configuration.</span></span>  
  
## <a name="concurrency-and-batching"></a><span data-ttu-id="9a19b-143">Simultaneidade e o envio em lote</span><span class="sxs-lookup"><span data-stu-id="9a19b-143">Concurrency and Batching</span></span>  
 <span data-ttu-id="9a19b-144">Para aumentar a taxa de transferência, você também pode ter muitos lotes executados simultaneamente.</span><span class="sxs-lookup"><span data-stu-id="9a19b-144">To increase throughput, you can also have many batches run concurrently.</span></span> <span data-ttu-id="9a19b-145">Definindo `ConcurrencyMode.Multiple` em `ServiceBehaviorAttribute`, habilitar o envio em lote simultâneas.</span><span class="sxs-lookup"><span data-stu-id="9a19b-145">By setting `ConcurrencyMode.Multiple` in `ServiceBehaviorAttribute`, you enable concurrent batching.</span></span>  
  
 <span data-ttu-id="9a19b-146">*Limitação de serviço* é um comportamento de serviço que é usado para indicar o número máximo de chamadas simultâneas pode ser feito no serviço.</span><span class="sxs-lookup"><span data-stu-id="9a19b-146">*Service throttling* is a service behavior that is used to indicate how many maximum concurrent calls can be made on the service.</span></span> <span data-ttu-id="9a19b-147">Quando usado com o envio em lote, isso é interpretado como a muitos lotes simultâneos podem ser executados.</span><span class="sxs-lookup"><span data-stu-id="9a19b-147">When used with batching, this is interpreted as how many concurrent batches can be run.</span></span> <span data-ttu-id="9a19b-148">Se a limitação do serviço não for definido, o WCF padrão é o máximo de chamadas simultâneas para 16.</span><span class="sxs-lookup"><span data-stu-id="9a19b-148">If the service throttling is not set, WCF defaults the maximum concurrent calls to 16.</span></span> <span data-ttu-id="9a19b-149">Assim, se o comportamento de lote foram adicionado por padrão, um máximo de 16 lotes pode ser ativo ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="9a19b-149">Thus, if batching behavior were added by default, a maximum of 16 batches can be active at the same time.</span></span> <span data-ttu-id="9a19b-150">É melhor ajustar a limitação do serviço e o envio em lote com base em sua capacidade.</span><span class="sxs-lookup"><span data-stu-id="9a19b-150">It is best to tune the service throttling and batching based on your capacity.</span></span> <span data-ttu-id="9a19b-151">Por exemplo, se a fila tem 100 mensagens e um lote de 20 for desejado, ter o máximo de chamadas simultâneas definidas como 16 não é útil porque, dependendo da taxa de transferência, 16 transações pode ser o Active Directory, semelhante a não ter o envio em lote ativado.</span><span class="sxs-lookup"><span data-stu-id="9a19b-151">For example, if the queue has 100 messages and a batch of 20 is desired, having the maximum concurrent calls set to 16 is not useful because, depending on throughput, 16 transactions could be active, similar to not having batching turned on.</span></span> <span data-ttu-id="9a19b-152">Portanto, quando ajustar o desempenho, ou não ter o envio em lote simultâneas ou tenha simultâneas envio em lote com o tamanho de limitação de serviço correto.</span><span class="sxs-lookup"><span data-stu-id="9a19b-152">Therefore, when fine-tuning for performance, either do not have concurrent batching or have concurrent batching with the correct service throttle size.</span></span>  
  
## <a name="batching-and-multiple-endpoints"></a><span data-ttu-id="9a19b-153">Envio em lote e vários pontos de extremidade</span><span class="sxs-lookup"><span data-stu-id="9a19b-153">Batching and Multiple Endpoints</span></span>  
 <span data-ttu-id="9a19b-154">Um ponto de extremidade é composto de um endereço e um contrato.</span><span class="sxs-lookup"><span data-stu-id="9a19b-154">An endpoint is composed of an address and a contract.</span></span> <span data-ttu-id="9a19b-155">Pode haver vários pontos de extremidade que compartilham a mesma associação.</span><span class="sxs-lookup"><span data-stu-id="9a19b-155">There may be multiple endpoints that share the same binding.</span></span> <span data-ttu-id="9a19b-156">É possível que dois pontos de extremidade compartilharem a mesma ligação e ouvir o identificador de recurso uniforme (URI) ou o endereço da fila.</span><span class="sxs-lookup"><span data-stu-id="9a19b-156">It is possible for two endpoints to share the same binding and listen Uniform Resource Identifier (URI), or queue address.</span></span> <span data-ttu-id="9a19b-157">Se dois pontos de extremidade estão lendo a mesma fila e transacionado comportamento de lote é adicionado a ambos os pontos de extremidade, um conflito no lote tamanhos especificados podem surgir.</span><span class="sxs-lookup"><span data-stu-id="9a19b-157">If two endpoints are reading from the same queue, and transacted batching behavior is added to both endpoints, a conflict in the batch sizes specified could arise.</span></span> <span data-ttu-id="9a19b-158">Isso é resolvido com a implementação de envio em lote usando o tamanho de lote mínimo especificado entre os dois comportamentos de envio em lote transacionados.</span><span class="sxs-lookup"><span data-stu-id="9a19b-158">This is resolved by implementing batching using the minimal batch size specified between the two transacted batching behaviors.</span></span> <span data-ttu-id="9a19b-159">Nesse cenário, se um dos pontos de extremidade não especificar o lote transacionado, em seguida, ambos os pontos de extremidade não usaria envio em lote.</span><span class="sxs-lookup"><span data-stu-id="9a19b-159">In this scenario, if one of the endpoints does not specify transacted batching, then both endpoints would not use batching.</span></span>  
  
## <a name="example"></a><span data-ttu-id="9a19b-160">Exemplo</span><span class="sxs-lookup"><span data-stu-id="9a19b-160">Example</span></span>  
 <span data-ttu-id="9a19b-161">O exemplo a seguir mostra como especificar o `TransactedBatchingBehavior` em um arquivo de configuração.</span><span class="sxs-lookup"><span data-stu-id="9a19b-161">The following example shows how to specify the `TransactedBatchingBehavior` in a configuration file.</span></span>  
  
```xml  
<behaviors>
  <endpointBehaviors>
    <behavior name="TransactedBatchingBehavior"
              maxBatchSize="100" />
  </endpointBehaviors>
</behaviors>
```  
  
 <span data-ttu-id="9a19b-162">O exemplo a seguir mostra como especificar o <xref:System.ServiceModel.Description.TransactedBatchingBehavior> no código.</span><span class="sxs-lookup"><span data-stu-id="9a19b-162">The following example shows how to specify the <xref:System.ServiceModel.Description.TransactedBatchingBehavior> in code.</span></span>  
  
```csharp
using (ServiceHost serviceHost = new ServiceHost(typeof(OrderProcessorService)))
{
     ServiceEndpoint sep = ServiceHost.AddServiceEndpoint(typeof(IOrderProcessor), new NetMsmqBinding(), "net.msmq://localhost/private/ServiceModelSamplesTransacted");
     sep.Behaviors.Add(new TransactedBatchingBehavior(100));
     
     // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();
  
    // The service can now be accessed.
    Console.WriteLine("The service is ready.");
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.WriteLine();
    Console.ReadLine();
  
    // Close the ServiceHostB to shut down the service.
    serviceHost.Close();
}  
```  
  
## <a name="see-also"></a><span data-ttu-id="9a19b-163">Consulte também</span><span class="sxs-lookup"><span data-stu-id="9a19b-163">See also</span></span>

- [<span data-ttu-id="9a19b-164">Visão geral de filas</span><span class="sxs-lookup"><span data-stu-id="9a19b-164">Queues Overview</span></span>](../../../../docs/framework/wcf/feature-details/queues-overview.md)
- [<span data-ttu-id="9a19b-165">Enfileiramento no WCF</span><span class="sxs-lookup"><span data-stu-id="9a19b-165">Queuing in WCF</span></span>](../../../../docs/framework/wcf/feature-details/queuing-in-wcf.md)
