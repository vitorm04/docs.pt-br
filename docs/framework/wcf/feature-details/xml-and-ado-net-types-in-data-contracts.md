---
title: Tipos de XML e ADO.NET em contratos de dados
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
ms.assetid: c2ce8461-3c15-4c41-8c81-1cb78f5b59a6
ms.openlocfilehash: 975d4f4f37bbbd895cab4e87686f15017e90f382
ms.sourcegitcommit: cdb295dd1db589ce5169ac9ff096f01fd0c2da9d
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/09/2020
ms.locfileid: "84600069"
---
# <a name="xml-and-adonet-types-in-data-contracts"></a><span data-ttu-id="030cd-102">Tipos de XML e ADO.NET em contratos de dados</span><span class="sxs-lookup"><span data-stu-id="030cd-102">XML and ADO.NET Types in Data Contracts</span></span>
<span data-ttu-id="030cd-103">O modelo de contrato de dados do Windows Communication Foundation (WCF) dá suporte a determinados tipos que representam XML diretamente.</span><span class="sxs-lookup"><span data-stu-id="030cd-103">The Windows Communication Foundation (WCF) data contract model supports certain types that represent XML directly.</span></span> <span data-ttu-id="030cd-104">Quando esses tipos são serializados para XML, o serializador grava o conteúdo XML desses tipos sem nenhum processamento adicional.</span><span class="sxs-lookup"><span data-stu-id="030cd-104">When these types are serialized to XML, the serializer writes out the XML contents of these types without any further processing.</span></span> <span data-ttu-id="030cd-105">Os tipos com suporte são <xref:System.Xml.XmlElement> , matrizes <xref:System.Xml.XmlNode> (mas não o `XmlNode` próprio tipo), bem como tipos que implementam <xref:System.Xml.Serialization.IXmlSerializable> .</span><span class="sxs-lookup"><span data-stu-id="030cd-105">Supported types are <xref:System.Xml.XmlElement>, arrays of <xref:System.Xml.XmlNode> (but not the `XmlNode` type itself), as well as types that implement <xref:System.Xml.Serialization.IXmlSerializable>.</span></span> <span data-ttu-id="030cd-106">O <xref:System.Data.DataSet> <xref:System.Data.DataTable> tipo e, bem como os conjuntos de dados tipados, são comumente usados na programação de banco de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-106">The <xref:System.Data.DataSet> and <xref:System.Data.DataTable> type, as well as typed datasets, are commonly used in database programming.</span></span> <span data-ttu-id="030cd-107">Esses tipos implementam a `IXmlSerializable` interface e, portanto, são serializáveis no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-107">These types implement the `IXmlSerializable` interface and are therefore serializable in the data contract model.</span></span> <span data-ttu-id="030cd-108">Algumas considerações especiais para esses tipos são listadas no final deste tópico.</span><span class="sxs-lookup"><span data-stu-id="030cd-108">Some special considerations for these types are listed at the end of this topic.</span></span>  
  
## <a name="xml-types"></a><span data-ttu-id="030cd-109">Tipos XML</span><span class="sxs-lookup"><span data-stu-id="030cd-109">XML Types</span></span>  
  
### <a name="xml-element"></a><span data-ttu-id="030cd-110">Elemento XML</span><span class="sxs-lookup"><span data-stu-id="030cd-110">Xml Element</span></span>  
 <span data-ttu-id="030cd-111">O `XmlElement` tipo é serializado usando seu conteúdo XML.</span><span class="sxs-lookup"><span data-stu-id="030cd-111">The `XmlElement` type is serialized using its XML contents.</span></span> <span data-ttu-id="030cd-112">Por exemplo, usando o seguinte tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-112">For example, using the following type.</span></span>  
  
 [!code-csharp[DataContractAttribute#4](../../../../samples/snippets/csharp/VS_Snippets_CFX/datacontractattribute/cs/overview.cs#4)]
 [!code-vb[DataContractAttribute#4](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/datacontractattribute/vb/overview.vb#4)]  
  
 <span data-ttu-id="030cd-113">Isso é serializado para XML da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="030cd-113">This is serialized to XML as follows:</span></span>  
  
```xml  
<MyDataContract xmlns="http://schemas.contoso.com">  
    <myDataMember>  
        <myElement xmlns="" myAttribute="myValue">  
            myContents  
        </myElement>  
    </myDataMember>  
</MyDataContract>  
```  
  
 <span data-ttu-id="030cd-114">Observe que um elemento de membro de dados de wrapper `<myDataMember>` ainda está presente.</span><span class="sxs-lookup"><span data-stu-id="030cd-114">Notice that a wrapper data member element `<myDataMember>` is still present.</span></span> <span data-ttu-id="030cd-115">Não há nenhuma maneira de remover esse elemento no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-115">There is no way of removing this element in the data contract model.</span></span> <span data-ttu-id="030cd-116">Os serializadores que manipulam esse modelo ( <xref:System.Runtime.Serialization.DataContractSerializer> e <xref:System.Runtime.Serialization.NetDataContractSerializer> ) podem emitir atributos especiais para esse elemento wrapper.</span><span class="sxs-lookup"><span data-stu-id="030cd-116">The serializers that handle this model (the <xref:System.Runtime.Serialization.DataContractSerializer> and <xref:System.Runtime.Serialization.NetDataContractSerializer>) may emit special attributes into this wrapper element.</span></span> <span data-ttu-id="030cd-117">Esses atributos incluem o atributo "nil" da instância de esquema XML padrão (permitindo que seja `XmlElement` `null` ) e o atributo "Type" (permitindo que `XmlElement` seja usado polimorficamente).</span><span class="sxs-lookup"><span data-stu-id="030cd-117">These attributes include the standard XML Schema Instance "nil" attribute (allowing the `XmlElement` to be `null`) and the "type" attribute (allowing `XmlElement` to be used polymorphically).</span></span> <span data-ttu-id="030cd-118">Além disso, os seguintes atributos XML são específicos para o WCF: "ID", "ref", "Type" e "assembly".</span><span class="sxs-lookup"><span data-stu-id="030cd-118">Also, the following XML attributes are specific to WCF: "Id", "Ref", "Type" and "Assembly".</span></span> <span data-ttu-id="030cd-119">Esses atributos podem ser emitidos para dar suporte ao uso do `XmlElement` com o modo de preservação de grafo de objeto habilitado ou com o <xref:System.Runtime.Serialization.NetDataContractSerializer> .</span><span class="sxs-lookup"><span data-stu-id="030cd-119">These attributes may be emitted to support using the `XmlElement` with the object graph preservation mode enabled, or with the <xref:System.Runtime.Serialization.NetDataContractSerializer>.</span></span> <span data-ttu-id="030cd-120">(Para obter mais informações sobre o modo de preservação de grafo de objetos, consulte [serialização e desserialização](serialization-and-deserialization.md).)</span><span class="sxs-lookup"><span data-stu-id="030cd-120">(For more information about the object graph preservation mode, see [Serialization and Deserialization](serialization-and-deserialization.md).)</span></span>  
  
 <span data-ttu-id="030cd-121">Matrizes ou coleções de `XmlElement` são permitidas e são tratadas como qualquer outra matriz ou coleção.</span><span class="sxs-lookup"><span data-stu-id="030cd-121">Arrays or collections of `XmlElement` are allowed and are handled as any other array or collection.</span></span> <span data-ttu-id="030cd-122">Ou seja, há um elemento wrapper para a coleção inteira e um elemento wrapper separado (semelhante ao `<myDataMember>` no exemplo anterior) para cada `XmlElement` uma na matriz.</span><span class="sxs-lookup"><span data-stu-id="030cd-122">That is, there is a wrapper element for the entire collection, and a separate wrapper element (similar to `<myDataMember>` in the preceding example) for each `XmlElement` in the array.</span></span>  
  
 <span data-ttu-id="030cd-123">Na desserialização, um `XmlElement` é criado pelo desserializador do XML de entrada.</span><span class="sxs-lookup"><span data-stu-id="030cd-123">On deserialization, an `XmlElement` is created by the deserializer from the incoming XML.</span></span> <span data-ttu-id="030cd-124">Um pai válido <xref:System.Xml.XmlDocument> é fornecido pelo desserializador.</span><span class="sxs-lookup"><span data-stu-id="030cd-124">A valid parent <xref:System.Xml.XmlDocument> is provided by the deserializer.</span></span>  
  
 <span data-ttu-id="030cd-125">Verifique se o fragmento XML que é desserializado para um `XmlElement` define todos os prefixos que ele usa e se não depende de definições de prefixo de elementos ancestrais.</span><span class="sxs-lookup"><span data-stu-id="030cd-125">Make sure that the XML fragment that is deserialized to an `XmlElement` defines all prefixes that it uses and does not rely on any prefix definitions from ancestor elements.</span></span> <span data-ttu-id="030cd-126">Essa é uma preocupação apenas ao usar o `DataContractSerializer` para acessar o XML de uma fonte diferente (não `DataContractSerializer` ).</span><span class="sxs-lookup"><span data-stu-id="030cd-126">This is a concern only when using the `DataContractSerializer` to access XML from a different (non-`DataContractSerializer`) source.</span></span>  
  
 <span data-ttu-id="030cd-127">Quando usado com o `DataContractSerializer` , o `XmlElement` pode ser atribuído a polimorficamente, mas somente a um membro de dados do tipo <xref:System.Object> .</span><span class="sxs-lookup"><span data-stu-id="030cd-127">When used with the `DataContractSerializer`, the `XmlElement` may be assigned polymorphically, but only to a data member of type <xref:System.Object>.</span></span> <span data-ttu-id="030cd-128">Mesmo que ele implemente <xref:System.Collections.IEnumerable> , um `XmlElement` não pode ser usado como um tipo de coleção e não pode ser atribuído a um <xref:System.Collections.IEnumerable> membro de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-128">Even though it implements <xref:System.Collections.IEnumerable>, an `XmlElement` cannot be used as a collection type and cannot be assigned to an <xref:System.Collections.IEnumerable> data member.</span></span> <span data-ttu-id="030cd-129">Assim como acontece com todas as atribuições polimórficas, o `DataContractSerializer` emite o nome do contrato de dados no XML resultante – nesse caso, é "XmlElement" no http://schemas.datacontract.org/2004/07/System.Xml namespace "".</span><span class="sxs-lookup"><span data-stu-id="030cd-129">As with all polymorphic assignments, the `DataContractSerializer` emits the data contract name in the resulting XML – in this case, it is "XmlElement" in the "http://schemas.datacontract.org/2004/07/System.Xml" namespace.</span></span>  
  
 <span data-ttu-id="030cd-130">Com o `NetDataContractSerializer` , há suporte para qualquer atribuição polimórfica válida de `XmlElement` (para `Object` ou `IEnumerable` ).</span><span class="sxs-lookup"><span data-stu-id="030cd-130">With the `NetDataContractSerializer`, any valid polymorphic assignment of `XmlElement` (to `Object` or `IEnumerable`) is supported.</span></span>  
  
 <span data-ttu-id="030cd-131">Não tente usar nenhum dos serializadores com tipos derivados de `XmlElement` , se eles forem atribuídos a polimorficamente ou não.</span><span class="sxs-lookup"><span data-stu-id="030cd-131">Do not attempt to use either of the serializers with types derived from `XmlElement`, whether they are assigned polymorphically or not.</span></span>  
  
### <a name="array-of-xmlnode"></a><span data-ttu-id="030cd-132">Matriz de XmlNode</span><span class="sxs-lookup"><span data-stu-id="030cd-132">Array of XmlNode</span></span>  
 <span data-ttu-id="030cd-133">O uso de matrizes do <xref:System.Xml.XmlNode> é muito semelhante ao uso do `XmlElement` .</span><span class="sxs-lookup"><span data-stu-id="030cd-133">Using arrays of <xref:System.Xml.XmlNode> is very similar to using `XmlElement`.</span></span> <span data-ttu-id="030cd-134">O uso de matrizes `XmlNode` proporciona mais flexibilidade do que o uso do `XmlElement` .</span><span class="sxs-lookup"><span data-stu-id="030cd-134">Using arrays of `XmlNode` gives you more flexibility than using `XmlElement`.</span></span> <span data-ttu-id="030cd-135">Você pode escrever vários elementos dentro do elemento de quebra de membro de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-135">You can write multiple elements inside the data member wrapping element.</span></span> <span data-ttu-id="030cd-136">Você também pode injetar conteúdo que não seja elementos dentro do elemento de quebra de membro de dados, como comentários XML.</span><span class="sxs-lookup"><span data-stu-id="030cd-136">You can also inject content other than elements inside of the data member wrapping element, such as XML comments.</span></span> <span data-ttu-id="030cd-137">Por fim, você pode colocar atributos no elemento membro de dados de encapsulamento.</span><span class="sxs-lookup"><span data-stu-id="030cd-137">Finally, you can put attributes into the wrapping data member element.</span></span> <span data-ttu-id="030cd-138">Tudo isso pode ser obtido preenchendo a matriz de `XmlNode` com classes derivadas específicas de como `XmlNode` <xref:System.Xml.XmlAttribute> , `XmlElement` ou <xref:System.Xml.XmlComment> .</span><span class="sxs-lookup"><span data-stu-id="030cd-138">All this can be achieved by populating the array of `XmlNode` with specific derived classes of `XmlNode` such as <xref:System.Xml.XmlAttribute>, `XmlElement` or <xref:System.Xml.XmlComment>.</span></span> <span data-ttu-id="030cd-139">Por exemplo, usando o seguinte tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-139">For example, using the following type.</span></span>  
  
 [!code-csharp[DataContractAttribute#5](../../../../samples/snippets/csharp/VS_Snippets_CFX/datacontractattribute/cs/overview.cs#5)]
 [!code-vb[DataContractAttribute#5](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/datacontractattribute/vb/overview.vb#5)]  
  
 <span data-ttu-id="030cd-140">Quando serializado, o XML resultante é semelhante ao código a seguir.</span><span class="sxs-lookup"><span data-stu-id="030cd-140">When serialized, the resulting XML is similar to the following code.</span></span>  
  
```xml  
<MyDataContract xmlns="http://schemas.contoso.com">  
  <myDataMember myAttribute="myValue">  
     <!--myComment-->  
     <myElement xmlns="" myAttribute="myValue">  
 myContents  
     </myElement>  
     <myElement xmlns="" myAttribute="myValue">  
       myContents  
     </myElement>  
  </myDataMember>  
</MyDataContract>  
```  
  
 <span data-ttu-id="030cd-141">Observe que o elemento wrapper de membro de dados `<myDataMember>` contém um atributo, um comentário e dois elementos.</span><span class="sxs-lookup"><span data-stu-id="030cd-141">Note that the data member wrapper element `<myDataMember>` contains an attribute, a comment, and two elements.</span></span> <span data-ttu-id="030cd-142">Essas são as quatro `XmlNode` instâncias que foram serializadas.</span><span class="sxs-lookup"><span data-stu-id="030cd-142">These are the four `XmlNode` instances that were serialized.</span></span>  
  
 <span data-ttu-id="030cd-143">Uma matriz do `XmlNode` que resulta em um XML inválido não pode ser serializada.</span><span class="sxs-lookup"><span data-stu-id="030cd-143">An array of `XmlNode` that results in invalid XML cannot be serialized.</span></span> <span data-ttu-id="030cd-144">Por exemplo, uma matriz de duas `XmlNode` instâncias em que a primeira é uma `XmlElement` e a segunda é <xref:System.Xml.XmlAttribute> inválida, porque essa sequência não corresponde a nenhuma instância XML válida (não há nenhum local para anexar o atributo).</span><span class="sxs-lookup"><span data-stu-id="030cd-144">For example, an array of two `XmlNode` instances where the first one is an `XmlElement` and the second one is an <xref:System.Xml.XmlAttribute> is invalid, because this sequence does not correspond to any valid XML instance (there is no place to attach the attribute to).</span></span>  
  
 <span data-ttu-id="030cd-145">Na desserialização de uma matriz do `XmlNode` , os nós são criados e preenchidos com informações do XML de entrada.</span><span class="sxs-lookup"><span data-stu-id="030cd-145">On deserialization of an array of `XmlNode`, nodes are created and populated with information from the incoming XML.</span></span> <span data-ttu-id="030cd-146">Um pai válido <xref:System.Xml.XmlDocument> é fornecido pelo desserializador.</span><span class="sxs-lookup"><span data-stu-id="030cd-146">A valid parent <xref:System.Xml.XmlDocument> is provided by the deserializer.</span></span> <span data-ttu-id="030cd-147">Todos os nós são desserializados, incluindo quaisquer atributos no elemento membro de dados do wrapper, mas excluindo os atributos colocados ali pelos serializadores do WCF (como os atributos usados para indicar a atribuição polimórfica).</span><span class="sxs-lookup"><span data-stu-id="030cd-147">All nodes are deserialized, including any attributes on the wrapper data member element, but excluding the attributes placed there by the WCF serializers (such as the attributes used to indicate polymorphic assignment).</span></span> <span data-ttu-id="030cd-148">A limitação sobre a definição de todos os prefixos de namespace no fragmento XML se aplica à desserialização de matrizes da `XmlNode` mesma forma que a desserialização `XmlElement` .</span><span class="sxs-lookup"><span data-stu-id="030cd-148">The caveat about defining all namespace prefixes in the XML fragment applies to the deserialization of arrays of `XmlNode` just like it does to deserializing `XmlElement`.</span></span>  
  
 <span data-ttu-id="030cd-149">Ao usar os serializadores com preservação de grafo de objetos ativada, a igualdade de objeto é preservada apenas no nível de `XmlNode` matrizes, não em `XmlNode` instâncias individuais.</span><span class="sxs-lookup"><span data-stu-id="030cd-149">When using the serializers with object graph preservation turned on, object equality is only preserved on the level of `XmlNode` arrays, not individual `XmlNode` instances.</span></span>  
  
 <span data-ttu-id="030cd-150">Não tente serializar uma matriz de `XmlNode` onde um ou mais nós está definido como `null` .</span><span class="sxs-lookup"><span data-stu-id="030cd-150">Do not attempt to serialize an array of `XmlNode` where one or more of the nodes is set to `null`.</span></span> <span data-ttu-id="030cd-151">É permitido que todo o membro da matriz seja `null` , mas não para qualquer indivíduo `XmlNode` contido na matriz.</span><span class="sxs-lookup"><span data-stu-id="030cd-151">It is permitted for the entire array member to be `null`, but not for any individual `XmlNode` contained in the array.</span></span> <span data-ttu-id="030cd-152">Se todo o membro da matriz for nulo, o elemento do membro de dados do wrapper conterá um atributo especial que indica que ele é nulo.</span><span class="sxs-lookup"><span data-stu-id="030cd-152">If the entire array member is null, the wrapper data member element contains a special attribute that indicates that it is null.</span></span> <span data-ttu-id="030cd-153">Na desserialização, todo o membro da matriz também se torna nulo.</span><span class="sxs-lookup"><span data-stu-id="030cd-153">On deserialization, the entire array member also becomes null.</span></span>  
  
 <span data-ttu-id="030cd-154">Somente matrizes regulares do `XmlNode` são tratadas especialmente pelo serializador.</span><span class="sxs-lookup"><span data-stu-id="030cd-154">Only regular arrays of `XmlNode` are treated specially by the serializer.</span></span> <span data-ttu-id="030cd-155">Os membros de dados declarados como outros tipos de coleção que contêm `XmlNode` ou membros de dados declarados como matrizes de tipos derivados de `XmlNode` , não são tratados especialmente.</span><span class="sxs-lookup"><span data-stu-id="030cd-155">Data members declared as other collection types that contain `XmlNode`, or data members declared as arrays of types derived from `XmlNode`, are not treated specially.</span></span> <span data-ttu-id="030cd-156">Portanto, eles normalmente não são serializáveis, a menos que também atendam a um dos outros critérios para a serialização.</span><span class="sxs-lookup"><span data-stu-id="030cd-156">Thus, they are normally not serializable unless they also meet one of the other criteria for serializing.</span></span>  
  
 <span data-ttu-id="030cd-157">Matrizes ou coleções de matrizes `XmlNode` são permitidas.</span><span class="sxs-lookup"><span data-stu-id="030cd-157">Arrays or collections of arrays of `XmlNode` are allowed.</span></span> <span data-ttu-id="030cd-158">Há um elemento wrapper para toda a coleção e um elemento wrapper separado (semelhante ao `<myDataMember>` exemplo anterior) para cada matriz de `XmlNode` na matriz ou coleção externa.</span><span class="sxs-lookup"><span data-stu-id="030cd-158">There is a wrapper element for the entire collection, and a separate wrapper element (similar to `<myDataMember>` in the preceding example) for each array of `XmlNode` in the outer array or collection.</span></span>  
  
 <span data-ttu-id="030cd-159">Popular um membro de dados do <xref:System.Array> tipo `Object` de `Array` ou `IEnumerable` com `XmlNode` instâncias não resulta no membro de dados sendo tratado como uma `Array` das `XmlNode` instâncias.</span><span class="sxs-lookup"><span data-stu-id="030cd-159">Populating a data member of type <xref:System.Array> of `Object` or `Array` of `IEnumerable` with `XmlNode` instances does not result in the data member being treated as an `Array` of `XmlNode` instances.</span></span> <span data-ttu-id="030cd-160">Cada membro da matriz é serializado separadamente.</span><span class="sxs-lookup"><span data-stu-id="030cd-160">Each array member is serialized separately.</span></span>  
  
 <span data-ttu-id="030cd-161">Quando usado com o `DataContractSerializer` , as matrizes de `XmlNode` podem ser atribuídas polimorficamente, mas somente a um membro de dados do tipo `Object` .</span><span class="sxs-lookup"><span data-stu-id="030cd-161">When used with the `DataContractSerializer`, arrays of `XmlNode` can be assigned polymorphically, but only to a data member of type `Object`.</span></span> <span data-ttu-id="030cd-162">Mesmo que ele implemente `IEnumerable` , uma matriz de `XmlNode` não pode ser usada como um tipo de coleção e ser atribuída a um `IEnumerable` membro de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-162">Even though it implements `IEnumerable`, an array of `XmlNode` cannot be used as a collection type and be assigned to an `IEnumerable` data member.</span></span> <span data-ttu-id="030cd-163">Assim como acontece com todas as atribuições polimórficas, o `DataContractSerializer` emite o nome do contrato de dados no XML resultante – nesse caso, é "ArrayOfXmlNode" no http://schemas.datacontract.org/2004/07/System.Xml namespace "".</span><span class="sxs-lookup"><span data-stu-id="030cd-163">As with all polymorphic assignments, the `DataContractSerializer` emits the data contract name in the resulting XML – in this case, it is "ArrayOfXmlNode" in the "http://schemas.datacontract.org/2004/07/System.Xml" namespace.</span></span> <span data-ttu-id="030cd-164">Quando usado com o `NetDataContractSerializer` , há suporte para qualquer atribuição válida de uma `XmlNode` matriz.</span><span class="sxs-lookup"><span data-stu-id="030cd-164">When used with the `NetDataContractSerializer`, any valid assignment of an `XmlNode` array is supported.</span></span>  
  
### <a name="schema-considerations"></a><span data-ttu-id="030cd-165">Considerações sobre o esquema</span><span class="sxs-lookup"><span data-stu-id="030cd-165">Schema Considerations</span></span>  
 <span data-ttu-id="030cd-166">Para obter detalhes sobre o mapeamento de esquema de tipos XML, consulte [referência de esquema de contrato de dados](data-contract-schema-reference.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-166">For details about the schema mapping of XML types, see [Data Contract Schema Reference](data-contract-schema-reference.md).</span></span> <span data-ttu-id="030cd-167">Esta seção fornece um resumo dos pontos importantes.</span><span class="sxs-lookup"><span data-stu-id="030cd-167">This section provides a summary of the important points.</span></span>  
  
 <span data-ttu-id="030cd-168">Um membro de dados do tipo `XmlElement` é mapeado para um elemento definido usando o seguinte tipo anônimo.</span><span class="sxs-lookup"><span data-stu-id="030cd-168">A data member of type `XmlElement` is mapped to an element defined using the following anonymous type.</span></span>  
  
```xml  
<xsd:complexType>  
   <xsd:sequence>  
      <xsd:any minOccurs="0" processContents="lax" />  
   </xsd:sequence>  
</xsd:complexType>  
```  
  
 <span data-ttu-id="030cd-169">Um membro de dados do tipo matriz de `XmlNode` é mapeado para um elemento definido usando o seguinte tipo anônimo.</span><span class="sxs-lookup"><span data-stu-id="030cd-169">A data member of type Array of `XmlNode` is mapped to an element defined using the following anonymous type.</span></span>  
  
```xml  
<xsd:complexType mixed="true">  
   <xsd:sequence>  
      <xsd:any minOccurs="0" maxOccurs="unbounded" processContents="lax" />  
   </xsd:sequence>  
   <xsd:anyAttribute/>  
</xsd:complexType>  
```  
  
## <a name="types-implementing-the-ixmlserializable-interface"></a><span data-ttu-id="030cd-170">Tipos que implementam a interface IXmlSerializable</span><span class="sxs-lookup"><span data-stu-id="030cd-170">Types Implementing the IXmlSerializable Interface</span></span>  
 <span data-ttu-id="030cd-171">Os tipos que implementam a `IXmlSerializable` interface têm suporte total do `DataContractSerializer` .</span><span class="sxs-lookup"><span data-stu-id="030cd-171">Types that implement the `IXmlSerializable` interface are fully supported by the `DataContractSerializer`.</span></span> <span data-ttu-id="030cd-172">O <xref:System.Xml.Serialization.XmlSchemaProviderAttribute> atributo sempre deve ser aplicado a esses tipos para controlar seu esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-172">The <xref:System.Xml.Serialization.XmlSchemaProviderAttribute> attribute should always be applied to these types to control their schema.</span></span>  
  
 <span data-ttu-id="030cd-173">Há três variedades de tipos que implementam `IXmlSerializable` : tipos que representam conteúdo arbitrário, tipos que representam um único elemento e tipos herdados <xref:System.Data.DataSet> .</span><span class="sxs-lookup"><span data-stu-id="030cd-173">There are three varieties of types that implement `IXmlSerializable`: types that represent arbitrary content, types that represent a single element, and legacy <xref:System.Data.DataSet> types.</span></span>  
  
- <span data-ttu-id="030cd-174">Os tipos de conteúdo usam um método de provedor de esquema especificado pelo `XmlSchemaProviderAttribute` atributo.</span><span class="sxs-lookup"><span data-stu-id="030cd-174">Content types use a schema provider method specified by the `XmlSchemaProviderAttribute` attribute.</span></span> <span data-ttu-id="030cd-175">O método não retorna `null` , e a <xref:System.Xml.Serialization.XmlSchemaProviderAttribute.IsAny%2A> propriedade no atributo é deixada com seu valor padrão de `false` .</span><span class="sxs-lookup"><span data-stu-id="030cd-175">The method does not return `null`, and the <xref:System.Xml.Serialization.XmlSchemaProviderAttribute.IsAny%2A> property on the attribute is left at its default value of `false`.</span></span> <span data-ttu-id="030cd-176">Esse é o uso mais comum de `IXmlSerializable` tipos.</span><span class="sxs-lookup"><span data-stu-id="030cd-176">This is the most common usage of `IXmlSerializable` types.</span></span>  
  
- <span data-ttu-id="030cd-177">Tipos de elemento são usados quando um `IXmlSerializable` tipo deve controlar seu próprio nome de elemento raiz.</span><span class="sxs-lookup"><span data-stu-id="030cd-177">Element types are used when an `IXmlSerializable` type must control its own root element name.</span></span> <span data-ttu-id="030cd-178">Para marcar um tipo como um tipo de elemento, defina a <xref:System.Xml.Serialization.XmlSchemaProviderAttribute.IsAny%2A> propriedade no <xref:System.Xml.Serialization.XmlSchemaProviderAttribute> atributo como `true` ou retorne NULL do método de provedor de esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-178">To mark a type as an element type, either set the <xref:System.Xml.Serialization.XmlSchemaProviderAttribute.IsAny%2A> property on the <xref:System.Xml.Serialization.XmlSchemaProviderAttribute> attribute to `true` or return null from the schema provider method.</span></span> <span data-ttu-id="030cd-179">Ter um método de provedor de esquema é opcional para tipos de elementos – você pode especificar NULL em vez do nome do método no `XmlSchemaProviderAttribute` .</span><span class="sxs-lookup"><span data-stu-id="030cd-179">Having a schema provider method is optional for element types – you may specify null instead of the method name in the `XmlSchemaProviderAttribute`.</span></span> <span data-ttu-id="030cd-180">No entanto, se `IsAny` for `true` e um método de provedor de esquema for especificado, o método deverá retornar NULL.</span><span class="sxs-lookup"><span data-stu-id="030cd-180">However, if `IsAny` is `true` and a schema provider method is specified, the method must return null.</span></span>  
  
- <span data-ttu-id="030cd-181">Tipos herdados <xref:System.Data.DataSet> são `IXmlSerializable` tipos que não são marcados com o `XmlSchemaProviderAttribute` atributo.</span><span class="sxs-lookup"><span data-stu-id="030cd-181">Legacy <xref:System.Data.DataSet> types are `IXmlSerializable` types that are not marked with the `XmlSchemaProviderAttribute` attribute.</span></span> <span data-ttu-id="030cd-182">Em vez disso, eles dependem do <xref:System.Xml.Serialization.IXmlSerializable.GetSchema%2A> método de geração de esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-182">Instead, they rely on the <xref:System.Xml.Serialization.IXmlSerializable.GetSchema%2A> method for schema generation.</span></span> <span data-ttu-id="030cd-183">Esse padrão é usado para o `DataSet` tipo e seu dataset tipado deriva uma classe em versões anteriores do .NET Framework, mas agora é obsoleto e tem suporte apenas por motivos herdados.</span><span class="sxs-lookup"><span data-stu-id="030cd-183">This pattern is used for the `DataSet` type and its typed dataset derives a class in earlier versions of the .NET Framework, but is now obsolete and is supported only for legacy reasons.</span></span> <span data-ttu-id="030cd-184">Não confie nesse padrão e sempre aplique o `XmlSchemaProviderAttribute` aos seus `IXmlSerializable` tipos.</span><span class="sxs-lookup"><span data-stu-id="030cd-184">Do not rely on this pattern and always apply the `XmlSchemaProviderAttribute` to your `IXmlSerializable` types.</span></span>  
  
### <a name="ixmlserializable-content-types"></a><span data-ttu-id="030cd-185">Tipos de conteúdo IXmlSerializable</span><span class="sxs-lookup"><span data-stu-id="030cd-185">IXmlSerializable Content Types</span></span>  
 <span data-ttu-id="030cd-186">Ao serializar um membro de dados de um tipo que implementa `IXmlSerializable` e é um tipo de conteúdo conforme definido anteriormente, o serializador grava o elemento wrapper para o membro de dados e passa o controle para o <xref:System.Xml.Serialization.IXmlSerializable.WriteXml%2A> método.</span><span class="sxs-lookup"><span data-stu-id="030cd-186">When serializing a data member of a type that implements `IXmlSerializable` and is a content type as defined previously, the serializer writes the wrapper element for the data member and pass control to the <xref:System.Xml.Serialization.IXmlSerializable.WriteXml%2A> method.</span></span> <span data-ttu-id="030cd-187">A <xref:System.Xml.Serialization.IXmlSerializable.WriteXml%2A> implementação pode gravar qualquer XML, incluindo a adição de atributos ao elemento wrapper.</span><span class="sxs-lookup"><span data-stu-id="030cd-187">The <xref:System.Xml.Serialization.IXmlSerializable.WriteXml%2A> implementation can write any XML, including adding attributes to the wrapper element.</span></span> <span data-ttu-id="030cd-188">Após a `WriteXml` conclusão, o serializador fecha o elemento.</span><span class="sxs-lookup"><span data-stu-id="030cd-188">After `WriteXml` is done, the serializer closes the element.</span></span>  
  
 <span data-ttu-id="030cd-189">Ao desserializar um membro de dados de um tipo que implementa `IXmlSerializable` e é um tipo de conteúdo definido anteriormente, o desserializador posiciona o leitor XML no elemento wrapper do membro de dados e passa o controle para o <xref:System.Xml.Serialization.IXmlSerializable.ReadXml%2A> método.</span><span class="sxs-lookup"><span data-stu-id="030cd-189">When deserializing a data member of a type that implements `IXmlSerializable` and is a content type as defined previously, the deserializer positions the XML reader on the wrapper element for the data member and pass control to the <xref:System.Xml.Serialization.IXmlSerializable.ReadXml%2A> method.</span></span> <span data-ttu-id="030cd-190">O método deve ler o elemento inteiro, incluindo as marcas de início e de fim.</span><span class="sxs-lookup"><span data-stu-id="030cd-190">The method must read the entire element, including the start and end tags.</span></span> <span data-ttu-id="030cd-191">Verifique se seu `ReadXml` código manipula o caso em que o elemento está vazio.</span><span class="sxs-lookup"><span data-stu-id="030cd-191">Make sure your `ReadXml` code handles the case where the element is empty.</span></span> <span data-ttu-id="030cd-192">Além disso, sua `ReadXml` implementação não deve depender do elemento wrapper que está sendo nomeado de forma específica.</span><span class="sxs-lookup"><span data-stu-id="030cd-192">Additionally, your `ReadXml` implementation should not rely on the wrapper element being named a particular way.</span></span> <span data-ttu-id="030cd-193">O nome é escolhido pelo serializador pode variar.</span><span class="sxs-lookup"><span data-stu-id="030cd-193">The name is chosen by the serializer can vary.</span></span>  
  
 <span data-ttu-id="030cd-194">É permitido atribuir tipos de `IXmlSerializable` conteúdo polimorficamente, por exemplo, a membros de dados do tipo <xref:System.Object> .</span><span class="sxs-lookup"><span data-stu-id="030cd-194">It is permitted to assign `IXmlSerializable` content types polymorphically, for example, to data members of type <xref:System.Object>.</span></span> <span data-ttu-id="030cd-195">Também é permitido que as instâncias de tipo sejam nulas.</span><span class="sxs-lookup"><span data-stu-id="030cd-195">It is also permitted for the type instances to be null.</span></span> <span data-ttu-id="030cd-196">Por fim, é possível usar `IXmlSerializable` tipos com preservação de grafo de objetos habilitada e com o <xref:System.Runtime.Serialization.NetDataContractSerializer> .</span><span class="sxs-lookup"><span data-stu-id="030cd-196">Finally, it is possible to use `IXmlSerializable` types with object graph preservation enabled and with the <xref:System.Runtime.Serialization.NetDataContractSerializer>.</span></span> <span data-ttu-id="030cd-197">Todos esses recursos exigem que o serializador do WCF anexe determinados atributos ao elemento wrapper ("nil" e "Type" no namespace da instância de esquema XML e "ID", "ref", "Type" e "assembly" em um namespace específico do WCF).</span><span class="sxs-lookup"><span data-stu-id="030cd-197">All these features require the WCF serializer to attach certain attributes into the wrapper element ("nil" and "type" in the XML Schema Instance namespace and "Id", "Ref", "Type" and "Assembly" in a WCF-specific namespace).</span></span>  
  
#### <a name="attributes-to-ignore-when-implementing-readxml"></a><span data-ttu-id="030cd-198">Atributos a serem ignorados ao implementar ReadXml</span><span class="sxs-lookup"><span data-stu-id="030cd-198">Attributes to Ignore when Implementing ReadXml</span></span>  
 <span data-ttu-id="030cd-199">Antes de passar o controle para seu `ReadXml` código, o desserializador examina o elemento XML, detecta esses atributos XML especiais e atua neles.</span><span class="sxs-lookup"><span data-stu-id="030cd-199">Before passing control to your `ReadXml` code, the deserializer examines the XML element, detects these special XML attributes, and acts on them.</span></span> <span data-ttu-id="030cd-200">Por exemplo, se "nil" for `true` , um valor nulo será desserializado e `ReadXml` não será chamado.</span><span class="sxs-lookup"><span data-stu-id="030cd-200">For example, if "nil" is `true`, a null value is deserialized and `ReadXml` is not called.</span></span> <span data-ttu-id="030cd-201">Se polimorfismo for detectado, o conteúdo do elemento será desserializado como se fosse um tipo diferente.</span><span class="sxs-lookup"><span data-stu-id="030cd-201">If polymorphism is detected, the contents of the element are deserialized as if it was a different type.</span></span> <span data-ttu-id="030cd-202">A implementação do tipo atribuído polimorficamente de `ReadXml` é chamada.</span><span class="sxs-lookup"><span data-stu-id="030cd-202">The polymorphically assigned type’s implementation of `ReadXml` is called.</span></span> <span data-ttu-id="030cd-203">Em qualquer caso, uma `ReadXml` implementação deve ignorar esses atributos especiais porque eles são tratados pelo desserializador.</span><span class="sxs-lookup"><span data-stu-id="030cd-203">In any case, a `ReadXml` implementation should ignore these special attributes because they are handled by the deserializer.</span></span>  
  
### <a name="schema-considerations-for-ixmlserializable-content-types"></a><span data-ttu-id="030cd-204">Considerações de esquema para tipos de conteúdo IXmlSerializable</span><span class="sxs-lookup"><span data-stu-id="030cd-204">Schema Considerations for IXmlSerializable Content Types</span></span>  
 <span data-ttu-id="030cd-205">Ao exportar o esquema de um `IXmlSerializable` tipo de conteúdo, o método de provedor de esquema é chamado.</span><span class="sxs-lookup"><span data-stu-id="030cd-205">When exporting schema an `IXmlSerializable` content type, the schema provider method is called.</span></span> <span data-ttu-id="030cd-206">Um <xref:System.Xml.Schema.XmlSchemaSet> é passado para o método de provedor de esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-206">An <xref:System.Xml.Schema.XmlSchemaSet> is passed to the schema provider method.</span></span> <span data-ttu-id="030cd-207">O método pode adicionar qualquer esquema válido ao conjunto de esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-207">The method can add any valid schema to the schema set.</span></span> <span data-ttu-id="030cd-208">O conjunto de esquema contém o esquema que já é conhecido no momento em que a exportação de esquema ocorre.</span><span class="sxs-lookup"><span data-stu-id="030cd-208">The schema set contains the schema that is already known at the time when schema export occurs.</span></span> <span data-ttu-id="030cd-209">Quando o método do provedor de esquema deve adicionar um item ao conjunto de esquema, ele deve determinar se um <xref:System.Xml.Schema.XmlSchema> com o namespace apropriado já existe no conjunto.</span><span class="sxs-lookup"><span data-stu-id="030cd-209">When the schema provider method must add an item to the schema set, it must determine if an <xref:System.Xml.Schema.XmlSchema> with the appropriate namespace already exists in the set.</span></span> <span data-ttu-id="030cd-210">Se tiver, o método de provedor de esquema deverá adicionar o novo item ao existente `XmlSchema` .</span><span class="sxs-lookup"><span data-stu-id="030cd-210">If it does, the schema provider method must add the new item to the existing `XmlSchema`.</span></span> <span data-ttu-id="030cd-211">Caso contrário, ele deve criar uma nova `XmlSchema` instância.</span><span class="sxs-lookup"><span data-stu-id="030cd-211">Otherwise, it must create a new `XmlSchema` instance.</span></span> <span data-ttu-id="030cd-212">Isso é importante se as matrizes de `IXmlSerializable` tipos estiverem sendo usadas.</span><span class="sxs-lookup"><span data-stu-id="030cd-212">This is important if arrays of `IXmlSerializable` types are being used.</span></span> <span data-ttu-id="030cd-213">Por exemplo, se você tiver um `IXmlSerializable` tipo que é exportado como o tipo "A" no namespace "b", é possível que, no momento em que o método do provedor de esquema for chamado, o conjunto de esquema já contenha o esquema para "B" para manter o tipo "ArrayOfA".</span><span class="sxs-lookup"><span data-stu-id="030cd-213">For example, if you have an `IXmlSerializable` type that gets exported as type "A" in namespace "B", it is possible that by the time the schema provider method is called the schema set already contains the schema for "B" to hold the "ArrayOfA" type.</span></span>  
  
 <span data-ttu-id="030cd-214">Além de adicionar tipos ao <xref:System.Xml.Schema.XmlSchemaSet> , o método de provedor de esquema para tipos de conteúdo deve retornar um valor não nulo.</span><span class="sxs-lookup"><span data-stu-id="030cd-214">In addition to adding types to the <xref:System.Xml.Schema.XmlSchemaSet>, the schema provider method for content types must return a non-null value.</span></span> <span data-ttu-id="030cd-215">Ele pode retornar um <xref:System.Xml.XmlQualifiedName> que especifica o nome do tipo de esquema a ser usado para o `IXmlSerializable` tipo fornecido.</span><span class="sxs-lookup"><span data-stu-id="030cd-215">It can return an <xref:System.Xml.XmlQualifiedName> that specifies the name of the schema type to use for the given `IXmlSerializable` type.</span></span> <span data-ttu-id="030cd-216">Esse nome qualificado também serve como o nome do contrato de dados e o namespace para o tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-216">This qualified name also serves as the data contract name and namespace for the type.</span></span> <span data-ttu-id="030cd-217">É permitido retornar um tipo que não existe no conjunto de esquema imediatamente quando o método de provedor de esquema retorna.</span><span class="sxs-lookup"><span data-stu-id="030cd-217">It is permitted to return a type that does not exist in the schema set immediately when the schema provider method returns.</span></span> <span data-ttu-id="030cd-218">No entanto, é esperado que, no momento em que todos os tipos relacionados forem exportados (o <xref:System.Runtime.Serialization.XsdDataContractExporter.Export%2A> método for chamado para todos os tipos relevantes no <xref:System.Runtime.Serialization.XsdDataContractExporter> e a <xref:System.Runtime.Serialization.XsdDataContractExporter.Schemas%2A> propriedade for acessada), o tipo exista no conjunto de esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-218">However, it is expected that by the time all related types are exported (the <xref:System.Runtime.Serialization.XsdDataContractExporter.Export%2A> method is called for all relevant types on the <xref:System.Runtime.Serialization.XsdDataContractExporter> and the <xref:System.Runtime.Serialization.XsdDataContractExporter.Schemas%2A> property is accessed), the type exists in the schema set.</span></span> <span data-ttu-id="030cd-219">O acesso à `Schemas` propriedade antes que todas as `Export` chamadas relevantes tenham sido feitas pode resultar em um <xref:System.Xml.Schema.XmlSchemaException> .</span><span class="sxs-lookup"><span data-stu-id="030cd-219">Accessing the `Schemas` property before all relevant `Export` calls have been made can result in an <xref:System.Xml.Schema.XmlSchemaException>.</span></span> <span data-ttu-id="030cd-220">Para obter mais informações sobre o processo de exportação, consulte [exportando esquemas de classes](exporting-schemas-from-classes.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-220">For more information about the export process, see [Exporting Schemas from Classes](exporting-schemas-from-classes.md).</span></span>  
  
 <span data-ttu-id="030cd-221">O método de provedor de esquema também pode retornar o <xref:System.Xml.Schema.XmlSchemaType> a ser usado.</span><span class="sxs-lookup"><span data-stu-id="030cd-221">The schema provider method can also return the <xref:System.Xml.Schema.XmlSchemaType> to use.</span></span> <span data-ttu-id="030cd-222">O tipo pode ou não ser anônimo.</span><span class="sxs-lookup"><span data-stu-id="030cd-222">The type may or may not be anonymous.</span></span> <span data-ttu-id="030cd-223">Se for anônimo, o esquema para o `IXmlSerializable` tipo será exportado como um tipo anônimo toda vez que o `IXmlSerializable` tipo for usado como um membro de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-223">If it is anonymous, the schema for the `IXmlSerializable` type is exported as an anonymous type every time the `IXmlSerializable` type is used as a data member.</span></span> <span data-ttu-id="030cd-224">O `IXmlSerializable` tipo ainda tem um nome e namespace de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-224">The `IXmlSerializable` type still has a data contract name and namespace.</span></span> <span data-ttu-id="030cd-225">(Isso é determinado conforme descrito em [nomes de contrato de dados](data-contract-names.md) , exceto que o <xref:System.Runtime.Serialization.DataContractAttribute> atributo não pode ser usado para personalizar o nome.) Se não for anônimo, ele deverá ser um dos tipos no `XmlSchemaSet` .</span><span class="sxs-lookup"><span data-stu-id="030cd-225">(This is determined as described in [Data Contract Names](data-contract-names.md) except that the <xref:System.Runtime.Serialization.DataContractAttribute> attribute cannot be used to customize the name.) If it is not anonymous, it must be one of the types in the `XmlSchemaSet`.</span></span> <span data-ttu-id="030cd-226">Esse caso é equivalente a retornar o `XmlQualifiedName` do tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-226">This case is equivalent to returning the `XmlQualifiedName` of the type.</span></span>  
  
 <span data-ttu-id="030cd-227">Além disso, uma declaração de elemento global é exportada para o tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-227">Additionally, a global element declaration is exported for the type.</span></span> <span data-ttu-id="030cd-228">Se o tipo não tiver o <xref:System.Xml.Serialization.XmlRootAttribute> atributo aplicado a ele, o elemento terá o mesmo nome e namespace que o contrato de dados, e sua propriedade "anulável" será verdadeira.</span><span class="sxs-lookup"><span data-stu-id="030cd-228">If the type does not have the <xref:System.Xml.Serialization.XmlRootAttribute> attribute applied to it, the element has the same name and namespace as the data contract, and its "nillable" property is true.</span></span> <span data-ttu-id="030cd-229">A única exceção é o namespace do esquema (" http://www.w3.org/2001/XMLSchema ") – se o contrato de dados do tipo estiver nesse namespace, o elemento global correspondente estará no namespace em branco porque é proibido adicionar novos elementos ao namespace do esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-229">The only exception to this is the schema namespace ("http://www.w3.org/2001/XMLSchema") – if the type’s data contract is in this namespace, the corresponding global element is in the blank namespace because it is forbidden to add new elements to the schema namespace.</span></span> <span data-ttu-id="030cd-230">Se o tipo tiver o `XmlRootAttribute` atributo aplicado, a declaração de elemento global será exportada usando as propriedades a seguir: <xref:System.Xml.Serialization.XmlRootAttribute.ElementName%2A> <xref:System.Xml.Serialization.XmlRootAttribute.Namespace%2A> e <xref:System.Xml.Serialization.XmlRootAttribute.IsNullable%2A> .</span><span class="sxs-lookup"><span data-stu-id="030cd-230">If the type has the `XmlRootAttribute` attribute applied to it, the global element declaration is exported using the following: <xref:System.Xml.Serialization.XmlRootAttribute.ElementName%2A>, <xref:System.Xml.Serialization.XmlRootAttribute.Namespace%2A> and <xref:System.Xml.Serialization.XmlRootAttribute.IsNullable%2A> properties.</span></span> <span data-ttu-id="030cd-231">Os padrões com `XmlRootAttribute` aplicado são o nome do contrato de dados, um namespace em branco e "anulável" são verdadeiros.</span><span class="sxs-lookup"><span data-stu-id="030cd-231">The defaults with `XmlRootAttribute` applied are the data contract name, a blank namespace and "nillable" being true.</span></span>  
  
 <span data-ttu-id="030cd-232">As mesmas regras de declaração de elemento global se aplicam a tipos de conjuntos de conjunto de DataSet.</span><span class="sxs-lookup"><span data-stu-id="030cd-232">The same global element declaration rules apply to legacy dataset types.</span></span> <span data-ttu-id="030cd-233">Observe que o `XmlRootAttribute` não pode substituir as declarações de elemento global adicionadas por meio de código personalizado, adicionadas ao `XmlSchemaSet` usando o método de provedor de esquema ou por meio `GetSchema` de tipos de conjuntos de conjunto de código herdado.</span><span class="sxs-lookup"><span data-stu-id="030cd-233">Note that the `XmlRootAttribute` cannot override global element declarations added through custom code, either added to the `XmlSchemaSet` using the schema provider method or through `GetSchema` for legacy dataset types.</span></span>  
  
### <a name="ixmlserializable-element-types"></a><span data-ttu-id="030cd-234">Tipos de elemento IXmlSerializable</span><span class="sxs-lookup"><span data-stu-id="030cd-234">IXmlSerializable Element Types</span></span>  
 <span data-ttu-id="030cd-235">`IXmlSerializable`os tipos de elemento têm a `IsAny` propriedade definida como `true` ou seu método de provedor de esquema retorna `null` .</span><span class="sxs-lookup"><span data-stu-id="030cd-235">`IXmlSerializable` element types have either the `IsAny` property set to `true` or have their schema provider method return `null`.</span></span>  
  
 <span data-ttu-id="030cd-236">A serialização e a desserialização de um tipo de elemento são muito semelhantes à serialização e à desserialização de um tipo de conteúdo.</span><span class="sxs-lookup"><span data-stu-id="030cd-236">Serializing and deserializing an element type is very similar to serializing and deserializing a content type.</span></span> <span data-ttu-id="030cd-237">No entanto há algumas diferenças importantes:</span><span class="sxs-lookup"><span data-stu-id="030cd-237">However, there are some important differences:</span></span>  
  
- <span data-ttu-id="030cd-238">Espera-se que a `WriteXml` implementação grave exatamente um elemento (que, naturalmente, pode conter vários elementos filho).</span><span class="sxs-lookup"><span data-stu-id="030cd-238">The `WriteXml` implementation is expected to write exactly one element (which could of course contain multiple child elements).</span></span> <span data-ttu-id="030cd-239">Ele não deve estar gravando atributos fora deste único elemento, vários elementos irmãos ou conteúdo misto.</span><span class="sxs-lookup"><span data-stu-id="030cd-239">It should not be writing attributes outside of this single element, multiple sibling elements or mixed content.</span></span> <span data-ttu-id="030cd-240">O elemento pode estar vazio.</span><span class="sxs-lookup"><span data-stu-id="030cd-240">The element may be empty.</span></span>  
  
- <span data-ttu-id="030cd-241">A `ReadXml` implementação não deve ler o elemento wrapper.</span><span class="sxs-lookup"><span data-stu-id="030cd-241">The `ReadXml` implementation should not read the wrapper element.</span></span> <span data-ttu-id="030cd-242">Espera-se que seja lido um elemento que `WriteXml` produz.</span><span class="sxs-lookup"><span data-stu-id="030cd-242">It is expected to read the one element that `WriteXml` produces.</span></span>  
  
- <span data-ttu-id="030cd-243">Ao serializar um tipo de elemento regularmente (por exemplo, como um membro de dados em um contrato de dados), o serializador gera um elemento wrapper antes de chamar `WriteXml` , como com os tipos de conteúdo.</span><span class="sxs-lookup"><span data-stu-id="030cd-243">When serializing an element type regularly (for example, as a data member in a data contract), the serializer outputs a wrapper element before calling `WriteXml`, as with content types.</span></span> <span data-ttu-id="030cd-244">No entanto, ao serializar um tipo de elemento no nível superior, o serializador normalmente não gera um elemento de wrapper em todo o elemento que `WriteXml` grava, a menos que um nome de raiz e um namespace tenham sido explicitamente especificados ao construir o serializador nos `DataContractSerializer` `NetDataContractSerializer` construtores ou.</span><span class="sxs-lookup"><span data-stu-id="030cd-244">However, when serializing an element type at the top level, the serializer does not normally output a wrapper element at all around the element that `WriteXml` writes, unless a root name and namespace were explicitly specified when constructing the serializer in the `DataContractSerializer` or `NetDataContractSerializer` constructors.</span></span> <span data-ttu-id="030cd-245">Para obter mais informações, consulte [serialização e desserialização](serialization-and-deserialization.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-245">For more information, see [Serialization and Deserialization](serialization-and-deserialization.md).</span></span>  
  
- <span data-ttu-id="030cd-246">Ao serializar um tipo de elemento no nível superior sem especificar o nome raiz e o namespace no momento da construção <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteStartObject%2A> e, <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteEndObject%2A> essencialmente, não faz nada e <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteObjectContent%2A> chama `WriteXml` .</span><span class="sxs-lookup"><span data-stu-id="030cd-246">When serializing an element type at the top level without specifying the root name and namespace at construction time, <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteStartObject%2A> and <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteEndObject%2A> essentially does nothing and <xref:System.Runtime.Serialization.XmlObjectSerializer.WriteObjectContent%2A> calls `WriteXml`.</span></span> <span data-ttu-id="030cd-247">Nesse modo, o objeto que está sendo serializado não pode ser nulo e não pode ser polimorficamente atribuído.</span><span class="sxs-lookup"><span data-stu-id="030cd-247">In this mode, the object being serialized cannot be null and cannot be polymorphically assigned.</span></span> <span data-ttu-id="030cd-248">Além disso, a preservação do grafo de objetos não pode ser habilitada e `NetDataContractSerializer` não pode ser usada.</span><span class="sxs-lookup"><span data-stu-id="030cd-248">Also, object graph preservation cannot enabled and the `NetDataContractSerializer` cannot be used.</span></span>  
  
- <span data-ttu-id="030cd-249">Ao desserializar um tipo de elemento no nível superior sem especificar o nome raiz e o namespace no momento da construção, <xref:System.Runtime.Serialization.XmlObjectSerializer.IsStartObject%2A> retorna `true` se ele pode encontrar o início de qualquer elemento.</span><span class="sxs-lookup"><span data-stu-id="030cd-249">When deserializing an element type at the top level without specifying the root name and namespace at construction time, <xref:System.Runtime.Serialization.XmlObjectSerializer.IsStartObject%2A> returns `true` if it can find the start of any element.</span></span> <span data-ttu-id="030cd-250"><xref:System.Runtime.Serialization.XmlObjectSerializer.ReadObject%2A>com o `verifyObjectName` parâmetro definido como se `true` comporta da mesma maneira que `IsStartObject` antes de realmente ler o objeto.</span><span class="sxs-lookup"><span data-stu-id="030cd-250"><xref:System.Runtime.Serialization.XmlObjectSerializer.ReadObject%2A> with the `verifyObjectName` parameter set to `true` behaves in the same way as `IsStartObject` before actually reading the object.</span></span> <span data-ttu-id="030cd-251">`ReadObject`em seguida, passa o controle para o `ReadXml` método.</span><span class="sxs-lookup"><span data-stu-id="030cd-251">`ReadObject` then passes control to `ReadXml` method.</span></span>  
  
 <span data-ttu-id="030cd-252">O esquema exportado para tipos de elementos é o mesmo para o `XmlElement` tipo, conforme descrito em uma seção anterior, exceto pelo fato de que o método de provedor de esquema pode adicionar qualquer esquema adicional ao <xref:System.Xml.Schema.XmlSchemaSet> como com os tipos de conteúdo.</span><span class="sxs-lookup"><span data-stu-id="030cd-252">The schema exported for element types is the same as for the `XmlElement` type as described in an earlier section, except that the schema provider method can add any additional schema to the <xref:System.Xml.Schema.XmlSchemaSet> as with content types.</span></span> <span data-ttu-id="030cd-253">O uso do `XmlRootAttribute` atributo com tipos de elemento não é permitido e as declarações de elemento global nunca são emitidas para esses tipos.</span><span class="sxs-lookup"><span data-stu-id="030cd-253">Using the `XmlRootAttribute` attribute with element types is not allowed, and global element declarations are never emitted for these types.</span></span>  
  
### <a name="differences-from-the-xmlserializer"></a><span data-ttu-id="030cd-254">Diferenças do XmlSerializer</span><span class="sxs-lookup"><span data-stu-id="030cd-254">Differences from the XmlSerializer</span></span>  
 <span data-ttu-id="030cd-255">A `IXmlSerializable` interface e os `XmlSchemaProviderAttribute` `XmlRootAttribute` atributos e também são compreendidos pelo <xref:System.Xml.Serialization.XmlSerializer> .</span><span class="sxs-lookup"><span data-stu-id="030cd-255">The `IXmlSerializable` interface and the `XmlSchemaProviderAttribute` and `XmlRootAttribute` attributes are also understood by the <xref:System.Xml.Serialization.XmlSerializer> .</span></span> <span data-ttu-id="030cd-256">No entanto, há algumas diferenças em como elas são tratadas no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-256">However, there are some differences in how these are treated in the data contract model.</span></span> <span data-ttu-id="030cd-257">As diferenças importantes são resumidas no seguinte:</span><span class="sxs-lookup"><span data-stu-id="030cd-257">The important differences are summarized in the following:</span></span>  
  
- <span data-ttu-id="030cd-258">O método de provedor de esquema deve ser público para ser utilizável no `XmlSerializer` , mas não precisa ser público para ser utilizável no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-258">The schema provider method must be public to be usable in the `XmlSerializer`, but does not have to be public to be usable in the data contract model.</span></span>  
  
- <span data-ttu-id="030cd-259">O método de provedor de esquema é chamado quando `IsAny` é verdadeiro no modelo de contrato de dados, mas não com o `XmlSerializer` .</span><span class="sxs-lookup"><span data-stu-id="030cd-259">The schema provider method is called when `IsAny` is true in the data contract model but not with the `XmlSerializer`.</span></span>  
  
- <span data-ttu-id="030cd-260">Quando o `XmlRootAttribute` atributo não está presente para o conteúdo ou tipos de conjuntos de conjunto de DataSet herdados, o `XmlSerializer` exporta uma declaração de elemento global no namespace em branco.</span><span class="sxs-lookup"><span data-stu-id="030cd-260">When the `XmlRootAttribute` attribute is not present for content or legacy dataset types, the `XmlSerializer` exports a global element declaration in the blank namespace.</span></span> <span data-ttu-id="030cd-261">No modelo de contrato de dados, o namespace usado normalmente é o namespace de contrato de dados, conforme descrito anteriormente.</span><span class="sxs-lookup"><span data-stu-id="030cd-261">In the data contract model, the namespace used is normally the data contract namespace as described earlier.</span></span>  
  
 <span data-ttu-id="030cd-262">Esteja atento a essas diferenças ao criar tipos que são usados com as duas tecnologias de serialização.</span><span class="sxs-lookup"><span data-stu-id="030cd-262">Be aware of these differences when creating types that are used with both serialization technologies.</span></span>  
  
### <a name="importing-ixmlserializable-schema"></a><span data-ttu-id="030cd-263">Importando esquema IXmlSerializable</span><span class="sxs-lookup"><span data-stu-id="030cd-263">Importing IXmlSerializable Schema</span></span>  
 <span data-ttu-id="030cd-264">Ao importar um esquema gerado a partir de `IXmlSerializable` tipos, há algumas possibilidades:</span><span class="sxs-lookup"><span data-stu-id="030cd-264">When importing a schema generated from `IXmlSerializable` types, there are a few possibilities:</span></span>  
  
- <span data-ttu-id="030cd-265">O esquema gerado pode ser um esquema de contrato de dados válido, conforme descrito em [referência de esquema de contrato de dados](data-contract-schema-reference.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-265">The generated schema may be a valid data contract schema as described in [Data Contract Schema Reference](data-contract-schema-reference.md).</span></span> <span data-ttu-id="030cd-266">Nesse caso, o esquema pode ser importado como de costume e os tipos de contrato de dados regulares são gerados.</span><span class="sxs-lookup"><span data-stu-id="030cd-266">In this case, schema can be imported as usual and regular data contract types are generated.</span></span>  
  
- <span data-ttu-id="030cd-267">O esquema gerado pode não ser um esquema de contrato de dados válido.</span><span class="sxs-lookup"><span data-stu-id="030cd-267">The generated schema may not be a valid data contract schema.</span></span> <span data-ttu-id="030cd-268">Por exemplo, o método de provedor de esquema pode gerar um esquema que envolva atributos XML que não têm suporte no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-268">For example, your schema provider method may generate schema that involves XML attributes which are not supported in the data contract model.</span></span> <span data-ttu-id="030cd-269">Nesse caso, você pode importar o esquema como `IXmlSerializable` tipos.</span><span class="sxs-lookup"><span data-stu-id="030cd-269">In this case, you can import the schema as `IXmlSerializable` types.</span></span> <span data-ttu-id="030cd-270">Esse modo de importação não está ativado por padrão, mas pode ser facilmente habilitado – por exemplo, com a `/importXmlTypes` opção de linha de comando para a [ferramenta de utilitário de metadados ServiceModel (svcutil. exe)](../servicemodel-metadata-utility-tool-svcutil-exe.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-270">This import mode is not on by default but can easily be enabled – for example, with the `/importXmlTypes` command-line switch to the [ServiceModel Metadata Utility Tool (Svcutil.exe)](../servicemodel-metadata-utility-tool-svcutil-exe.md).</span></span> <span data-ttu-id="030cd-271">Isso é descrito em detalhes no [esquema de importação para gerar classes](importing-schema-to-generate-classes.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-271">This is described in detail in the [Importing Schema to Generate Classes](importing-schema-to-generate-classes.md).</span></span> <span data-ttu-id="030cd-272">Observe que você deve trabalhar diretamente com o XML para suas instâncias de tipo.</span><span class="sxs-lookup"><span data-stu-id="030cd-272">Note that you must work directly with the XML for your type instances.</span></span> <span data-ttu-id="030cd-273">Você também pode considerar o uso de uma tecnologia de serialização diferente que dá suporte a um grande intervalo de esquema – consulte o tópico sobre como usar o `XmlSerializer` .</span><span class="sxs-lookup"><span data-stu-id="030cd-273">You may also consider using a different serialization technology that supports a wider range of schema – see the topic on using the `XmlSerializer`.</span></span>  
  
- <span data-ttu-id="030cd-274">Talvez você queira reutilizar os `IXmlSerializable` tipos existentes no proxy em vez de gerar novos.</span><span class="sxs-lookup"><span data-stu-id="030cd-274">You may want to reuse your existing `IXmlSerializable` types in the proxy instead of generating new ones.</span></span> <span data-ttu-id="030cd-275">Nesse caso, o recurso de tipos referenciados descrito no tópico importando o esquema para gerar tipos pode ser usado para indicar o tipo a ser reutilizado.</span><span class="sxs-lookup"><span data-stu-id="030cd-275">In this case, the referenced types feature described in the Importing Schema to Generate Types topic can be used to indicate the type to reuse.</span></span> <span data-ttu-id="030cd-276">Isso corresponde ao uso da `/reference` opção em svcutil. exe, que especifica o assembly que contém os tipos a serem reutilizados.</span><span class="sxs-lookup"><span data-stu-id="030cd-276">This corresponds to using the `/reference` switch on svcutil.exe, which specifies the assembly that contains the types to reuse.</span></span>  
  
## <a name="representing-arbitrary-xml-in-data-contracts"></a><span data-ttu-id="030cd-277">Representando XML arbitrário em contratos de dados</span><span class="sxs-lookup"><span data-stu-id="030cd-277">Representing Arbitrary XML in Data Contracts</span></span>  
 <span data-ttu-id="030cd-278">A `XmlElement` matriz e os `XmlNode` `IXmlSerializable` tipos permitem injetar XML arbitrário no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-278">The `XmlElement`, Array of `XmlNode` and `IXmlSerializable` types allow you to inject arbitrary XML into the data contract model.</span></span> <span data-ttu-id="030cd-279">O `DataContractSerializer` e `NetDataContractSerializer` passar esse conteúdo XML para o gravador de XML em uso, sem interferir no processo.</span><span class="sxs-lookup"><span data-stu-id="030cd-279">The `DataContractSerializer` and `NetDataContractSerializer` pass this XML content on to the XML writer in use, without interfering in the process.</span></span> <span data-ttu-id="030cd-280">No entanto, os gravadores XML podem impor determinadas restrições no XML que gravam.</span><span class="sxs-lookup"><span data-stu-id="030cd-280">However, the XML writers may enforce certain restrictions on the XML that they write.</span></span> <span data-ttu-id="030cd-281">Especificamente, aqui estão alguns exemplos importantes:</span><span class="sxs-lookup"><span data-stu-id="030cd-281">Specifically, here are some important examples:</span></span>  
  
- <span data-ttu-id="030cd-282">Os gravadores de XML normalmente não permitem uma declaração de documento XML (por exemplo, \<?xml version=’1.0’ ?> ) no meio da gravação de outro documento.</span><span class="sxs-lookup"><span data-stu-id="030cd-282">The XML writers do not typically allow an XML document declaration (for example, \<?xml version=’1.0’ ?>) in the middle of writing another document.</span></span> <span data-ttu-id="030cd-283">Você não pode usar um documento XML completo e serializá-lo como um `Array` `XmlNode` membro de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-283">You cannot take a full XML document and serialize it as an `Array` of `XmlNode` data member.</span></span> <span data-ttu-id="030cd-284">Para fazer isso, você precisa remover a declaração do documento ou usar seu próprio esquema de codificação para representá-la.</span><span class="sxs-lookup"><span data-stu-id="030cd-284">To do this, you have to either strip out the document declaration or use your own encoding scheme to represent it.</span></span>  
  
- <span data-ttu-id="030cd-285">Todos os gravadores XML fornecidos com o WCF rejeitam instruções de processamento XML ( \<? … ?> ) e definições de tipo de documento ( \<! … > ), porque não são permitidas em mensagens SOAP.</span><span class="sxs-lookup"><span data-stu-id="030cd-285">All of the XML writers supplied with WCF reject XML processing instructions (\<? … ?>) and document type definitions (\<! … >), because they are not allowed in SOAP messages.</span></span> <span data-ttu-id="030cd-286">Novamente, você pode usar seu próprio mecanismo de codificação para contornar essa restrição.</span><span class="sxs-lookup"><span data-stu-id="030cd-286">Again, you can use your own encoding mechanism to get around this restriction.</span></span> <span data-ttu-id="030cd-287">Se você precisar incluí-los em seu XML resultante, poderá escrever um codificador personalizado que usa os gravadores XML que dão suporte a eles.</span><span class="sxs-lookup"><span data-stu-id="030cd-287">If you must include these in your resultant XML, you can write a custom encoder that uses XML writers that support them.</span></span>  
  
- <span data-ttu-id="030cd-288">Ao implementar `WriteXml` , evite chamar <xref:System.Xml.XmlWriter.WriteRaw%2A> o método no gravador de XML.</span><span class="sxs-lookup"><span data-stu-id="030cd-288">When implementing `WriteXml`, avoid calling <xref:System.Xml.XmlWriter.WriteRaw%2A> method on the XML writer.</span></span> <span data-ttu-id="030cd-289">O WCF usa uma variedade de codificações XML (incluindo Binary), é muito difícil ou impossível de usar `WriteRaw` , de modo que o resultado seja utilizável em qualquer codificação.</span><span class="sxs-lookup"><span data-stu-id="030cd-289">WCF uses a variety of XML encodings (including binary), it is very difficult or impossible to use `WriteRaw` such that the result is usable in any encoding.</span></span>  
  
- <span data-ttu-id="030cd-290">Ao implementar `WriteXml` , evite usar os <xref:System.Xml.XmlWriter.WriteEntityRef%2A> <xref:System.Xml.XmlWriter.WriteNmToken%2A> métodos e que não têm suporte nos gravadores de XML fornecidos com o WCF.</span><span class="sxs-lookup"><span data-stu-id="030cd-290">When implementing `WriteXml`, avoid using the <xref:System.Xml.XmlWriter.WriteEntityRef%2A> and <xref:System.Xml.XmlWriter.WriteNmToken%2A> methods that are unsupported on the XML writers supplied with WCF.</span></span>  
  
## <a name="using-dataset-typed-dataset-and-datatable"></a><span data-ttu-id="030cd-291">Usando DataSet, DataSet tipado e DataTable</span><span class="sxs-lookup"><span data-stu-id="030cd-291">Using DataSet, Typed DataSet and DataTable</span></span>  
 <span data-ttu-id="030cd-292">O uso desses tipos tem suporte total no modelo de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="030cd-292">Using these types is fully supported in the data contract model.</span></span> <span data-ttu-id="030cd-293">Ao usar esses tipos, considere os seguintes pontos:</span><span class="sxs-lookup"><span data-stu-id="030cd-293">When using these types, consider the following points:</span></span>  
  
- <span data-ttu-id="030cd-294">O esquema para esses tipos (especialmente <xref:System.Data.DataSet> e suas classes derivadas tipadas) pode não ser interoperável com algumas plataformas não WCF ou pode resultar em má usabilidade quando usado com essas plataformas.</span><span class="sxs-lookup"><span data-stu-id="030cd-294">The schema for these types (especially <xref:System.Data.DataSet> and its typed derived classes) may not be interoperable with some non-WCF platforms, or may result in poor usability when used with these platforms.</span></span> <span data-ttu-id="030cd-295">Além disso, o uso do `DataSet` tipo pode ter implicações de desempenho.</span><span class="sxs-lookup"><span data-stu-id="030cd-295">Additionally, using the `DataSet` type may have performance implications.</span></span> <span data-ttu-id="030cd-296">Por fim, pode dificultar a versão do seu aplicativo no futuro.</span><span class="sxs-lookup"><span data-stu-id="030cd-296">Finally, it may make it more difficult for you to version your application in the future.</span></span> <span data-ttu-id="030cd-297">Considere o uso de tipos de contrato de dados definidos explicitamente em vez de `DataSet` tipos em seus contratos.</span><span class="sxs-lookup"><span data-stu-id="030cd-297">Consider using explicitly defined data contract types instead of `DataSet` types in your contracts.</span></span>  
  
- <span data-ttu-id="030cd-298">Ao importar `DataSet` ou `DataTable` esquema, é importante fazer referência a esses tipos.</span><span class="sxs-lookup"><span data-stu-id="030cd-298">When importing `DataSet` or `DataTable` schema, it is important to reference these types.</span></span> <span data-ttu-id="030cd-299">Com a ferramenta de linha de comando svcutil. exe, isso pode ser feito passando o nome do assembly System. Data. dll para o `/reference` comutador.</span><span class="sxs-lookup"><span data-stu-id="030cd-299">With the Svcutil.exe command-line tool, this can be accomplished by passing the System.Data.dll assembly name to the `/reference` switch.</span></span> <span data-ttu-id="030cd-300">Ao importar o esquema do conjunto de texto tipado, você deve referenciar o tipo do conjunto de texto tipado.</span><span class="sxs-lookup"><span data-stu-id="030cd-300">If importing typed dataset schema, you must reference the typed dataset’s type.</span></span> <span data-ttu-id="030cd-301">Com svcutil. exe, passe o local do assembly do conjunto de módulos tipado para a `/reference` opção.</span><span class="sxs-lookup"><span data-stu-id="030cd-301">With Svcutil.exe, pass the location of the typed dataset’s assembly to the `/reference` switch.</span></span> <span data-ttu-id="030cd-302">Para obter mais informações sobre tipos de referência, consulte o [esquema de importação para gerar classes](importing-schema-to-generate-classes.md).</span><span class="sxs-lookup"><span data-stu-id="030cd-302">For more information about referencing types, see the [Importing Schema to Generate Classes](importing-schema-to-generate-classes.md).</span></span>  
  
 <span data-ttu-id="030cd-303">O suporte para datasets tipados no modelo de contrato de dados é limitado.</span><span class="sxs-lookup"><span data-stu-id="030cd-303">Support for typed DataSets in the data contract model is limited.</span></span> <span data-ttu-id="030cd-304">Os conjuntos de linhas tipados podem ser serializados e desserializados e podem exportar seu esquema.</span><span class="sxs-lookup"><span data-stu-id="030cd-304">Typed DataSets can be serialized and deserialized and can export their schema.</span></span> <span data-ttu-id="030cd-305">No entanto, a importação de esquema de contrato de dados não é capaz de gerar novos tipos de DataSet tipados a partir do esquema, pois ele só pode reutilizar os existentes.</span><span class="sxs-lookup"><span data-stu-id="030cd-305">However, the Data Contract schema import is unable to generate new typed DataSet types from the schema, as it can only reuse existing ones.</span></span> <span data-ttu-id="030cd-306">Você pode apontar para um DataSet tipado existente usando a `/r` opção de svcutil. exe.</span><span class="sxs-lookup"><span data-stu-id="030cd-306">You can point to an existing typed DataSet by using the `/r` switch on Svcutil.exe.</span></span> <span data-ttu-id="030cd-307">Se você tentar usar um svcutil. exe sem a `/r` opção em um serviço que usa um dataset tipado, um serializador alternativo (XmlSerializer) será selecionado automaticamente.</span><span class="sxs-lookup"><span data-stu-id="030cd-307">If you attempt to use a Svcutil.exe without the `/r` switch on a service that uses a typed dataset, an alternative serializer (XmlSerializer) is automatically selected.</span></span> <span data-ttu-id="030cd-308">Se você precisar usar o DataContractSerializer e precisar gerar conjuntos de valores do esquema, poderá usar o seguinte procedimento: gerar os tipos de conjunto de valores tipados (usando a ferramenta xsd. exe com a `/d` opção no serviço), compilar os tipos e, em seguida, apontar para eles usando a `/r` opção svcutil. exe.</span><span class="sxs-lookup"><span data-stu-id="030cd-308">If you must use the DataContractSerializer and must generate DataSets from schema, you can use the following procedure: generate the typed DataSet types (by using the Xsd.exe tool with the `/d` switch on the service), compile the types, and then point to them using the `/r` switch on Svcutil.exe.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="030cd-309">Consulte também</span><span class="sxs-lookup"><span data-stu-id="030cd-309">See also</span></span>

- <xref:System.Runtime.Serialization.DataContractSerializer>
- <xref:System.Xml.Serialization.IXmlSerializable>
- [<span data-ttu-id="030cd-310">Usando contratos de dados</span><span class="sxs-lookup"><span data-stu-id="030cd-310">Using Data Contracts</span></span>](using-data-contracts.md)
- [<span data-ttu-id="030cd-311">Tipos com suporte fornecido pelo serializador de contrato de dados</span><span class="sxs-lookup"><span data-stu-id="030cd-311">Types Supported by the Data Contract Serializer</span></span>](types-supported-by-the-data-contract-serializer.md)
