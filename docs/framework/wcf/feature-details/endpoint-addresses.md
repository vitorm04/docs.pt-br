---
title: Endereços do ponto de extremidade
ms.date: 03/30/2017
helpviewer_keywords:
- addresses [WCF]
- Windows Communication Foundation [WCF], addresses
- WCF [WCF], addresses
ms.assetid: 13f269e3-ebb1-433c-86cf-54fbd866a627
ms.openlocfilehash: cbae03c52f3cc39f7afd422a34b16e99a60d9f3a
ms.sourcegitcommit: 17ee6605e01ef32506f8fdc686954244ba6911de
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/21/2019
ms.locfileid: "74283215"
---
# <a name="endpoint-addresses"></a><span data-ttu-id="c668a-102">Endereços do ponto de extremidade</span><span class="sxs-lookup"><span data-stu-id="c668a-102">Endpoint Addresses</span></span>
<span data-ttu-id="c668a-103">Cada ponto de extremidade tem um endereço associado a ele, que é usado para localizar e identificar o ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c668a-103">Every endpoint has an address associated with it, which is used to locate and identify the endpoint.</span></span> <span data-ttu-id="c668a-104">Esse endereço consiste principalmente em um Uniform Resource Identifier (URI), que especifica o local do ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c668a-104">This address consists primarily of a Uniform Resource Identifier (URI), which specifies the location of the endpoint.</span></span> <span data-ttu-id="c668a-105">O endereço do ponto de extremidade é representado no modelo de programação Windows Communication Foundation (WCF) pela classe <xref:System.ServiceModel.EndpointAddress>, que contém uma propriedade <xref:System.ServiceModel.EndpointAddress.Identity%2A> opcional que permite a autenticação do ponto de extremidade por outros pontos de extremidades que trocam mensagens com ele e um conjunto de propriedades <xref:System.ServiceModel.EndpointAddress.Headers%2A> opcionais, que definem quaisquer outros cabeçalhos SOAP necessários para acessar o serviço.</span><span class="sxs-lookup"><span data-stu-id="c668a-105">The endpoint address is represented in the Windows Communication Foundation (WCF) programming model by the <xref:System.ServiceModel.EndpointAddress> class, which contains an optional <xref:System.ServiceModel.EndpointAddress.Identity%2A> property that enables the authentication of the endpoint by other endpoints that exchange messages with it, and a set of optional <xref:System.ServiceModel.EndpointAddress.Headers%2A> properties, which define any other SOAP headers required to reach the service.</span></span> <span data-ttu-id="c668a-106">Os cabeçalhos opcionais fornecem informações de endereçamento adicionais e mais detalhadas para identificar ou interagir com o ponto de extremidade de serviço.</span><span class="sxs-lookup"><span data-stu-id="c668a-106">The optional headers provide additional and more detailed addressing information to identify or interact with the service endpoint.</span></span> <span data-ttu-id="c668a-107">O endereço de um ponto de extremidade é representado na transmissão como uma referência de ponto de extremidade do WS-Addressing (EPR).</span><span class="sxs-lookup"><span data-stu-id="c668a-107">The address of an endpoint is represented on the wire as a WS-Addressing endpoint reference (EPR).</span></span>  
  
## <a name="uri-structure-of-an-address"></a><span data-ttu-id="c668a-108">Estrutura de URI de um endereço</span><span class="sxs-lookup"><span data-stu-id="c668a-108">URI Structure of an Address</span></span>  
 <span data-ttu-id="c668a-109">O URI de endereço para a maioria dos transportes tem quatro partes.</span><span class="sxs-lookup"><span data-stu-id="c668a-109">The address URI for most transports has four parts.</span></span> <span data-ttu-id="c668a-110">Por exemplo, as quatro partes do URI `http://www.fabrikam.com:322/mathservice.svc/secureEndpoint` podem ser discriminadas da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="c668a-110">For example, the four parts of the URI `http://www.fabrikam.com:322/mathservice.svc/secureEndpoint` can be itemized as follows:</span></span>  
  
- <span data-ttu-id="c668a-111">Esquema: `http:`</span><span class="sxs-lookup"><span data-stu-id="c668a-111">Scheme: `http:`</span></span>
  
- <span data-ttu-id="c668a-112">Computador: `www.fabrikam.com`</span><span class="sxs-lookup"><span data-stu-id="c668a-112">Machine: `www.fabrikam.com`</span></span>  
  
- <span data-ttu-id="c668a-113">adicional Porta: 322</span><span class="sxs-lookup"><span data-stu-id="c668a-113">(optional) Port: 322</span></span>  
  
- <span data-ttu-id="c668a-114">Caminho:/mathservice.svc/secureEndpoint</span><span class="sxs-lookup"><span data-stu-id="c668a-114">Path: /mathservice.svc/secureEndpoint</span></span>  
  
## <a name="defining-an-address-for-a-service"></a><span data-ttu-id="c668a-115">Definindo um endereço para um serviço</span><span class="sxs-lookup"><span data-stu-id="c668a-115">Defining an Address for a Service</span></span>  
 <span data-ttu-id="c668a-116">O endereço do ponto de extremidade para um serviço pode ser especificado de forma imperativa usando código ou declarativamente por meio da configuração.</span><span class="sxs-lookup"><span data-stu-id="c668a-116">The endpoint address for a service can be specified either imperatively using code or declaratively through configuration.</span></span> <span data-ttu-id="c668a-117">A definição de pontos de extremidade no código geralmente não é prática porque as associações e os endereços para um serviço implantado são normalmente diferentes daqueles usados enquanto o serviço está sendo desenvolvido.</span><span class="sxs-lookup"><span data-stu-id="c668a-117">Defining endpoints in code is usually not practical because the bindings and addresses for a deployed service are typically different from those used while the service is being developed.</span></span> <span data-ttu-id="c668a-118">Em geral, é mais prático definir pontos de extremidade de serviço usando a configuração em vez de código.</span><span class="sxs-lookup"><span data-stu-id="c668a-118">Generally, it is more practical to define service endpoints using configuration rather than code.</span></span> <span data-ttu-id="c668a-119">Manter a ligação e as informações de endereçamento do código permite que elas sejam alteradas sem a necessidade de recompilar ou reimplantar o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="c668a-119">Keeping the binding and addressing information out of the code allows them to change without having to recompile or redeploy the application.</span></span>  
  
### <a name="defining-an-address-in-configuration"></a><span data-ttu-id="c668a-120">Definindo um endereço na configuração</span><span class="sxs-lookup"><span data-stu-id="c668a-120">Defining an Address in Configuration</span></span>  
 <span data-ttu-id="c668a-121">Para definir um ponto de extremidade em um arquivo de configuração, use o elemento [\<ponto de extremidade >](../../../../docs/framework/configure-apps/file-schema/wcf/endpoint-element.md) .</span><span class="sxs-lookup"><span data-stu-id="c668a-121">To define an endpoint in a configuration file, use the [\<endpoint>](../../../../docs/framework/configure-apps/file-schema/wcf/endpoint-element.md) element.</span></span> <span data-ttu-id="c668a-122">Para obter detalhes e um exemplo, consulte [especificando um endereço de ponto de extremidade](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span><span class="sxs-lookup"><span data-stu-id="c668a-122">For details and an example, see [Specifying an Endpoint Address](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span></span>  
  
### <a name="defining-an-address-in-code"></a><span data-ttu-id="c668a-123">Definindo um endereço no código</span><span class="sxs-lookup"><span data-stu-id="c668a-123">Defining an Address in Code</span></span>  
 <span data-ttu-id="c668a-124">Um endereço de ponto de extremidade pode ser criado em código com a classe <xref:System.ServiceModel.EndpointAddress>.</span><span class="sxs-lookup"><span data-stu-id="c668a-124">An endpoint address can be created in code with the <xref:System.ServiceModel.EndpointAddress> class.</span></span> <span data-ttu-id="c668a-125">Para obter detalhes e um exemplo, consulte [especificando um endereço de ponto de extremidade](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span><span class="sxs-lookup"><span data-stu-id="c668a-125">For details and an example, see [Specifying an Endpoint Address](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span></span>  
  
### <a name="endpoints-in-wsdl"></a><span data-ttu-id="c668a-126">Pontos de extremidade em WSDL</span><span class="sxs-lookup"><span data-stu-id="c668a-126">Endpoints in WSDL</span></span>  
 <span data-ttu-id="c668a-127">Um endereço de ponto de extremidade também pode ser representado em WSDL como um elemento EPR de endereçamento WS dentro do elemento de `wsdl:port` do ponto de extremidade correspondente.</span><span class="sxs-lookup"><span data-stu-id="c668a-127">An endpoint address can also be represented in WSDL as a WS-Addressing EPR element inside the corresponding endpoint's `wsdl:port` element.</span></span> <span data-ttu-id="c668a-128">O EPR contém o endereço do ponto de extremidade, bem como qualquer propriedade de endereço.</span><span class="sxs-lookup"><span data-stu-id="c668a-128">The EPR contains the endpoint's address as well as any address properties.</span></span> <span data-ttu-id="c668a-129">Para obter detalhes e um exemplo, consulte [especificando um endereço de ponto de extremidade](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span><span class="sxs-lookup"><span data-stu-id="c668a-129">For details and an example, see [Specifying an Endpoint Address](../../../../docs/framework/wcf/specifying-an-endpoint-address.md).</span></span>  
  
## <a name="multiple-iis-binding-support-in-net-framework-35"></a><span data-ttu-id="c668a-130">Suporte a várias associações do IIS no .NET Framework 3,5</span><span class="sxs-lookup"><span data-stu-id="c668a-130">Multiple IIS Binding Support in .NET Framework 3.5</span></span>  
 <span data-ttu-id="c668a-131">Os provedores de serviços de Internet geralmente hospedam muitos aplicativos no mesmo servidor e site para aumentar a densidade do site e reduzir o custo total de propriedade.</span><span class="sxs-lookup"><span data-stu-id="c668a-131">Internet service providers often host many applications on the same server and site to increase the site density and lower total cost of ownership.</span></span> <span data-ttu-id="c668a-132">Esses aplicativos normalmente são associados a endereços de base diferentes.</span><span class="sxs-lookup"><span data-stu-id="c668a-132">These applications are typically bound to different base addresses.</span></span> <span data-ttu-id="c668a-133">Um site da Serviços de Informações da Internet (IIS) pode conter vários aplicativos.</span><span class="sxs-lookup"><span data-stu-id="c668a-133">An Internet Information Services (IIS) Web site can contain multiple applications.</span></span> <span data-ttu-id="c668a-134">Os aplicativos em um site podem ser acessados por meio de uma ou mais associações do IIS.</span><span class="sxs-lookup"><span data-stu-id="c668a-134">The applications in a site can be accessed through one or more IIS bindings.</span></span>  
  
 <span data-ttu-id="c668a-135">As associações do IIS fornecem duas partes de informação: um protocolo de associação e informações de associação.</span><span class="sxs-lookup"><span data-stu-id="c668a-135">IIS bindings provide two pieces of information: a binding protocol, and binding information.</span></span> <span data-ttu-id="c668a-136">O protocolo de associação define o esquema sobre o qual ocorre a comunicação e informações de associação são as informações usadas para acessar o site.</span><span class="sxs-lookup"><span data-stu-id="c668a-136">The binding protocol defines the scheme over which communication occurs, and binding information is the information used to access the site.</span></span>  
  
 <span data-ttu-id="c668a-137">O exemplo a seguir mostra os componentes que podem estar presentes em uma associação do IIS:</span><span class="sxs-lookup"><span data-stu-id="c668a-137">The following example shows the components that can be present in an IIS binding:</span></span>  
  
- <span data-ttu-id="c668a-138">Protocolo de associação: HTTP</span><span class="sxs-lookup"><span data-stu-id="c668a-138">Binding protocol: HTTP</span></span>  
  
- <span data-ttu-id="c668a-139">Informações de associação: endereço IP, porta, cabeçalho de host</span><span class="sxs-lookup"><span data-stu-id="c668a-139">Binding Information: IP Address, Port, Host header</span></span>  
  
 <span data-ttu-id="c668a-140">O IIS pode especificar várias associações para cada site, o que resulta em vários endereços base para cada esquema.</span><span class="sxs-lookup"><span data-stu-id="c668a-140">IIS can specify multiple bindings for each site, which results in multiple base addresses for each scheme.</span></span> <span data-ttu-id="c668a-141">Antes do .NET Framework 3,5, o WCF não oferecia suporte a vários endereços para um esquema e, se eles foram especificados, lançou um <xref:System.ArgumentException> durante a ativação.</span><span class="sxs-lookup"><span data-stu-id="c668a-141">Prior to .NET Framework 3.5, WCF did not support multiple addresses for a schema and, if they were specified, threw a <xref:System.ArgumentException> during activation.</span></span>  
  
 <span data-ttu-id="c668a-142">O .NET Framework 3,5 permite que os provedores de serviços de Internet hospedem vários aplicativos com endereços base diferentes para o mesmo esquema no mesmo site.</span><span class="sxs-lookup"><span data-stu-id="c668a-142">The .NET Framework 3.5 enables Internet service providers to host multiple applications with different base addresses for the same scheme on the same site.</span></span>  
  
 <span data-ttu-id="c668a-143">Por exemplo, um site pode conter os seguintes endereços base:</span><span class="sxs-lookup"><span data-stu-id="c668a-143">For example, a site could contain the following base addresses:</span></span>  
  
- `http://payroll.myorg.com/Service.svc`
  
- `http://shipping.myorg.com/Service.svc`
  
 <span data-ttu-id="c668a-144">Com o .NET Framework 3,5, você especifica um filtro de prefixo no nível do AppDomain no arquivo de configuração.</span><span class="sxs-lookup"><span data-stu-id="c668a-144">With .NET Framework 3.5, you specify a prefix filter at the AppDomain level in the configuration file.</span></span> <span data-ttu-id="c668a-145">Você faz isso com o elemento [\<baseAddressPrefixFilters >](../../../../docs/framework/configure-apps/file-schema/wcf/baseaddressprefixfilters.md) , que contém uma lista de prefixos.</span><span class="sxs-lookup"><span data-stu-id="c668a-145">You do this with the [\<baseAddressPrefixFilters>](../../../../docs/framework/configure-apps/file-schema/wcf/baseaddressprefixfilters.md) element, which contains a list of prefixes.</span></span> <span data-ttu-id="c668a-146">Os endereços base de entrada, fornecidos pelo IIS, são filtrados com base na lista de prefixos opcionais.</span><span class="sxs-lookup"><span data-stu-id="c668a-146">The incoming base addresses, supplied by IIS, are filtered based on the optional prefix list.</span></span> <span data-ttu-id="c668a-147">Por padrão, quando um prefixo não é especificado, todos os endereços são passados.</span><span class="sxs-lookup"><span data-stu-id="c668a-147">By default, when a prefix is not specified, all addresses are passed through.</span></span> <span data-ttu-id="c668a-148">A especificação do prefixo resulta apenas no endereço base correspondente para o qual o esquema será passado.</span><span class="sxs-lookup"><span data-stu-id="c668a-148">Specifying the prefix results in only the matching base address for that scheme to be passed through.</span></span>  
  
 <span data-ttu-id="c668a-149">Veja a seguir um exemplo de código de configuração que usa os filtros de prefixo.</span><span class="sxs-lookup"><span data-stu-id="c668a-149">The following is an example of configuration code that uses the prefix filters.</span></span>  
  
```xml  
<system.serviceModel>  
  <serviceHostingEnvironment>  
     <baseAddressPrefixFilters>  
        <add prefix="net.tcp://payroll.myorg.com:8000"/>  
        <add prefix="http://shipping.myorg.com:8000"/>  
    </baseAddressPrefixFilters>  
  </serviceHostingEnvironment>  
</system.serviceModel>  
```  
  
 <span data-ttu-id="c668a-150">No exemplo anterior, `net.tcp://payroll.myorg.com:8000` e `http://shipping.myorg.com:8000` são os únicos endereços base, para seus respectivos esquemas, que são passados.</span><span class="sxs-lookup"><span data-stu-id="c668a-150">In the preceding example, `net.tcp://payroll.myorg.com:8000` and `http://shipping.myorg.com:8000` are the only base addresses, for their respective schemes, which are passed through.</span></span>  
  
 <span data-ttu-id="c668a-151">O `baseAddressPrefixFilter` não oferece suporte a curingas.</span><span class="sxs-lookup"><span data-stu-id="c668a-151">The `baseAddressPrefixFilter` does not support wildcards.</span></span>  
  
 <span data-ttu-id="c668a-152">Os endereços base fornecidos pelo IIS podem ter endereços associados a outros esquemas que não estão presentes na lista de `baseAddressPrefixFilters`.</span><span class="sxs-lookup"><span data-stu-id="c668a-152">The base addresses supplied by IIS may have addresses bound to other schemes not present in `baseAddressPrefixFilters` list.</span></span> <span data-ttu-id="c668a-153">Esses endereços não são filtrados.</span><span class="sxs-lookup"><span data-stu-id="c668a-153">These addresses are not filtered out.</span></span>  
  
## <a name="multiple-iis-binding-support-in-net-framework-4-and-later"></a><span data-ttu-id="c668a-154">Suporte a várias associações IIS no .NET Framework 4 e posterior</span><span class="sxs-lookup"><span data-stu-id="c668a-154">Multiple IIS Binding Support in .NET Framework 4 and later</span></span>  
 <span data-ttu-id="c668a-155">A partir do .NET 4, você pode habilitar o suporte para várias associações no IIS sem precisar escolher um único endereço base, definindo <xref:System.ServiceModel.ServiceHostingEnvironment><xref:System.ServiceModel.ServiceHostingEnvironment.MultipleSiteBindingsEnabled%2A> configuração como true.</span><span class="sxs-lookup"><span data-stu-id="c668a-155">Starting in .NET 4, you can enable support for multiple bindings in IIS without having to pick a single base address, by setting <xref:System.ServiceModel.ServiceHostingEnvironment>’s <xref:System.ServiceModel.ServiceHostingEnvironment.MultipleSiteBindingsEnabled%2A> setting to true.</span></span> <span data-ttu-id="c668a-156">Esse suporte é limitado aos esquemas de protocolo HTTP.</span><span class="sxs-lookup"><span data-stu-id="c668a-156">This support is limited to HTTP protocol schemes.</span></span>  
  
 <span data-ttu-id="c668a-157">Veja a seguir um exemplo de código de configuração que usa multipleSiteBindingsEnabled em [\<> do ServiceHostingEnvironment](../../../../docs/framework/configure-apps/file-schema/wcf/servicehostingenvironment.md).</span><span class="sxs-lookup"><span data-stu-id="c668a-157">The following is an example of configuration code that uses multipleSiteBindingsEnabled on [\<serviceHostingEnvironment>](../../../../docs/framework/configure-apps/file-schema/wcf/servicehostingenvironment.md).</span></span>  
  
```xml  
<system.serviceModel>  
  <serviceHostingEnvironment multipleSiteBindingsEnabled="true" >  
  </serviceHostingEnvironment>  
</system.serviceModel>  
```  
  
 <span data-ttu-id="c668a-158">Todas as configurações de baseAddressPrefixFilters são ignoradas, para protocolos HTTP e não HTTP, quando várias associações de site são habilitadas usando essa configuração.</span><span class="sxs-lookup"><span data-stu-id="c668a-158">Any baseAddressPrefixFilters settings are ignored, for both HTTP and non-HTTP protocols, when multiple site bindings are enabled using this setting.</span></span>  
  
 <span data-ttu-id="c668a-159">Para obter detalhes e exemplos, consulte [suporte a várias associações de site do IIS](../../../../docs/framework/wcf/feature-details/supporting-multiple-iis-site-bindings.md) e <xref:System.ServiceModel.ServiceHostingEnvironment.MultipleSiteBindingsEnabled%2A>.</span><span class="sxs-lookup"><span data-stu-id="c668a-159">For details and examples, see [Supporting Multiple IIS Site Bindings](../../../../docs/framework/wcf/feature-details/supporting-multiple-iis-site-bindings.md) and <xref:System.ServiceModel.ServiceHostingEnvironment.MultipleSiteBindingsEnabled%2A>.</span></span>  
  
## <a name="extending-addressing-in-wcf-services"></a><span data-ttu-id="c668a-160">Estendendo o endereçamento nos serviços WCF</span><span class="sxs-lookup"><span data-stu-id="c668a-160">Extending Addressing in WCF Services</span></span>  
 <span data-ttu-id="c668a-161">O modelo de endereçamento padrão dos serviços WCF usa o URI do endereço do ponto de extremidade para as seguintes finalidades:</span><span class="sxs-lookup"><span data-stu-id="c668a-161">The default addressing model of WCF services uses the endpoint address URI for the following purposes:</span></span>  
  
- <span data-ttu-id="c668a-162">Para especificar o endereço de escuta do serviço, o local no qual o ponto de extremidade escuta mensagens,</span><span class="sxs-lookup"><span data-stu-id="c668a-162">To specify the service listening address, the location at which the endpoint listens for messages,</span></span>  
  
- <span data-ttu-id="c668a-163">Para especificar o filtro de endereço SOAP, o endereço que um ponto de extremidade espera como um cabeçalho SOAP.</span><span class="sxs-lookup"><span data-stu-id="c668a-163">To specify the SOAP address filter, the address an endpoint expects as a SOAP header.</span></span>  
  
 <span data-ttu-id="c668a-164">Os valores para cada uma dessas finalidades podem ser especificados separadamente, permitindo várias extensões de endereçamento que abrangem cenários úteis:</span><span class="sxs-lookup"><span data-stu-id="c668a-164">The values for each of these purposes can be specified separately, allowing several extensions of addressing that cover useful scenarios:</span></span>  
  
- <span data-ttu-id="c668a-165">Intermediários SOAP: uma mensagem enviada por um cliente atravessa um ou mais serviços adicionais que processam a mensagem antes de atingir seu destino final.</span><span class="sxs-lookup"><span data-stu-id="c668a-165">SOAP intermediaries: a message sent by a client traverses one or more additional services that process the message before it reaches its final destination.</span></span> <span data-ttu-id="c668a-166">Os intermediários SOAP podem executar várias tarefas, como cache, roteamento, balanceamento de carga ou validação de esquema nas mensagens.</span><span class="sxs-lookup"><span data-stu-id="c668a-166">SOAP intermediaries can perform various tasks, such as caching, routing, load-balancing, or schema validation on the messages.</span></span> <span data-ttu-id="c668a-167">Esse cenário é realizado enviando mensagens para um endereço físico separado (`via`) que tem como alvo o intermediário, em vez de apenas um endereço lógico (`wsa:To`) que se destina ao destino final.</span><span class="sxs-lookup"><span data-stu-id="c668a-167">This scenario is accomplished by sending messages to a separate physical address (`via`) that targets the intermediary rather than just to a logical address (`wsa:To`) that targets the ultimate destination.</span></span>  
  
- <span data-ttu-id="c668a-168">O endereço de escuta do ponto de extremidade é um URI privado e é definido com um valor diferente de sua propriedade `listenURI`.</span><span class="sxs-lookup"><span data-stu-id="c668a-168">The listening address of the endpoint is a private URI and is set to a different value than its `listenURI` property.</span></span>  
  
 <span data-ttu-id="c668a-169">O endereço de transporte que o `via` especifica é o local para o qual uma mensagem deve ser enviada inicialmente ao seu caminho para algum outro endereço remoto especificado pelo parâmetro `to` no qual o serviço está localizado.</span><span class="sxs-lookup"><span data-stu-id="c668a-169">The transport address that the `via` specifies is the location to which a message should initially be sent on its way to some other remote address specified by the `to` parameter at which the service is located.</span></span> <span data-ttu-id="c668a-170">Na maioria dos cenários de Internet, o URI de `via` é o mesmo que a propriedade <xref:System.ServiceModel.EndpointAddress.Uri%2A> do endereço de `to` final do serviço.</span><span class="sxs-lookup"><span data-stu-id="c668a-170">In most Internet scenarios, the `via` URI is the same as the <xref:System.ServiceModel.EndpointAddress.Uri%2A> property of the final `to` address of the service.</span></span> <span data-ttu-id="c668a-171">Você só distingue entre esses dois endereços quando deve fazer o roteamento manual.</span><span class="sxs-lookup"><span data-stu-id="c668a-171">You only distinguish between these two addresses when you must do manual routing.</span></span>  
  
### <a name="addressing-headers"></a><span data-ttu-id="c668a-172">Cabeçalhos de endereçamento</span><span class="sxs-lookup"><span data-stu-id="c668a-172">Addressing Headers</span></span>  
 <span data-ttu-id="c668a-173">Um ponto de extremidade pode ser resolvido por um ou mais cabeçalhos SOAP além de seu URI básico.</span><span class="sxs-lookup"><span data-stu-id="c668a-173">An endpoint can be addressed by one or more SOAP headers in addition to its basic URI.</span></span> <span data-ttu-id="c668a-174">Um conjunto de cenários em que isso é útil é um conjunto de cenários intermediários de SOAP em que um ponto de extremidade exige que clientes desse ponto de extremidade incluam cabeçalhos SOAP direcionados a intermediários.</span><span class="sxs-lookup"><span data-stu-id="c668a-174">One set of scenarios where this is useful is a set of SOAP intermediary scenarios where an endpoint requires clients of that endpoint to include SOAP headers targeted at intermediaries.</span></span>  
  
 <span data-ttu-id="c668a-175">Você pode definir cabeçalhos de endereço personalizados de duas maneiras — usando o código ou a configuração:</span><span class="sxs-lookup"><span data-stu-id="c668a-175">You can define custom address headers in two ways—by using either code or configuration:</span></span>  
  
- <span data-ttu-id="c668a-176">No código, crie cabeçalhos de endereço personalizados usando a classe <xref:System.ServiceModel.Channels.AddressHeader> e, em seguida, usado na construção de um <xref:System.ServiceModel.EndpointAddress>.</span><span class="sxs-lookup"><span data-stu-id="c668a-176">In code, create custom address headers by using the <xref:System.ServiceModel.Channels.AddressHeader> class, and then used in the construction of an <xref:System.ServiceModel.EndpointAddress>.</span></span>  
  
- <span data-ttu-id="c668a-177">Na configuração, os [cabeçalhos de\<personalizados >](../../configure-apps/file-schema/wcf/headers.md) são especificados como filhos do elemento [> de ponto de extremidade\<](../../configure-apps/file-schema/wcf/endpoint-of-client.md) .</span><span class="sxs-lookup"><span data-stu-id="c668a-177">In configuration, custom [\<headers>](../../configure-apps/file-schema/wcf/headers.md) are specified as children of the [\<endpoint>](../../configure-apps/file-schema/wcf/endpoint-of-client.md) element.</span></span>  
  
 <span data-ttu-id="c668a-178">A configuração é geralmente preferível ao código, pois permite que você altere os cabeçalhos após a implantação.</span><span class="sxs-lookup"><span data-stu-id="c668a-178">Configuration is generally preferable to code, as it allows you to change the headers after deployment.</span></span>  
  
### <a name="custom-listening-addresses"></a><span data-ttu-id="c668a-179">Endereços de escuta personalizados</span><span class="sxs-lookup"><span data-stu-id="c668a-179">Custom Listening Addresses</span></span>  
 <span data-ttu-id="c668a-180">Você pode definir o endereço de escuta para um valor diferente do URI do ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c668a-180">You can set the listening address to a different value than the endpoint’s URI.</span></span> <span data-ttu-id="c668a-181">Isso é útil em cenários intermediários em que o endereço SOAP a ser exposto é o de um intermediário de SOAP público, enquanto o endereço onde o ponto de extremidade realmente escuta é um endereço de rede privada.</span><span class="sxs-lookup"><span data-stu-id="c668a-181">This is useful in intermediary scenarios where the SOAP address to be exposed is that of a public SOAP intermediary, whereas the address where the endpoint actually listens is a private network address.</span></span>  
  
 <span data-ttu-id="c668a-182">Você pode especificar um endereço de escuta personalizado usando o código ou a configuração:</span><span class="sxs-lookup"><span data-stu-id="c668a-182">You can specify a custom listening address by using either code or configuration:</span></span>  
  
- <span data-ttu-id="c668a-183">No código, especifique um endereço de escuta personalizado adicionando uma classe de <xref:System.ServiceModel.Description.ClientViaBehavior> à coleção de comportamentos do ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c668a-183">In code, specify a custom listening address by adding a <xref:System.ServiceModel.Description.ClientViaBehavior> class to the endpoint’s behavior collection.</span></span>  
  
- <span data-ttu-id="c668a-184">Em configuração, especifique um endereço de escuta personalizado com o atributo `ListenUri` do elemento [> do serviço\<ponto de extremidade](../../configure-apps/file-schema/wcf/endpoint-element.md) .</span><span class="sxs-lookup"><span data-stu-id="c668a-184">In configuration, specify a custom listening address with the `ListenUri` attribute of the service [\<endpoint>](../../configure-apps/file-schema/wcf/endpoint-element.md) element.</span></span>  
  
### <a name="custom-soap-address-filter"></a><span data-ttu-id="c668a-185">Filtro de endereço SOAP personalizado</span><span class="sxs-lookup"><span data-stu-id="c668a-185">Custom SOAP Address Filter</span></span>  
 <span data-ttu-id="c668a-186">O <xref:System.ServiceModel.EndpointAddress.Uri%2A> é usado em conjunto com qualquer propriedade <xref:System.ServiceModel.EndpointAddress.Headers%2A> para definir o filtro de endereço SOAP de um ponto de extremidade (<xref:System.ServiceModel.Dispatcher.EndpointDispatcher.AddressFilter%2A>).</span><span class="sxs-lookup"><span data-stu-id="c668a-186">The <xref:System.ServiceModel.EndpointAddress.Uri%2A> is used in conjunction with any <xref:System.ServiceModel.EndpointAddress.Headers%2A> property to define an endpoint’s SOAP address filter (<xref:System.ServiceModel.Dispatcher.EndpointDispatcher.AddressFilter%2A>).</span></span> <span data-ttu-id="c668a-187">Por padrão, esse filtro verifica se uma mensagem de entrada tem um cabeçalho de mensagem `To` que corresponde ao URI do ponto de extremidade e que todos os cabeçalhos de ponto de extremidade necessários estão presentes na mensagem.</span><span class="sxs-lookup"><span data-stu-id="c668a-187">By default, this filter verifies that an incoming message has a `To` message header that matches the endpoint’s URI and that all of the required endpoint headers are present in the message.</span></span>  
  
 <span data-ttu-id="c668a-188">Em alguns cenários, um ponto de extremidade recebe todas as mensagens que chegam no transporte subjacente, e não apenas aquelas com o cabeçalho de `To` apropriado.</span><span class="sxs-lookup"><span data-stu-id="c668a-188">In some scenarios, an endpoint receives all messages that arrive on the underlying transport, and not just those with the appropriate `To` header.</span></span> <span data-ttu-id="c668a-189">Para habilitar isso, o usuário pode usar a classe <xref:System.ServiceModel.Dispatcher.MatchAllMessageFilter>.</span><span class="sxs-lookup"><span data-stu-id="c668a-189">To enable this, the user can use the <xref:System.ServiceModel.Dispatcher.MatchAllMessageFilter> class.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c668a-190">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c668a-190">See also</span></span>

- [<span data-ttu-id="c668a-191">Especificando um endereço do ponto de extremidade</span><span class="sxs-lookup"><span data-stu-id="c668a-191">Specifying an Endpoint Address</span></span>](../../../../docs/framework/wcf/specifying-an-endpoint-address.md)
- [<span data-ttu-id="c668a-192">Autenticação e identidade de serviço</span><span class="sxs-lookup"><span data-stu-id="c668a-192">Service Identity and Authentication</span></span>](../../../../docs/framework/wcf/feature-details/service-identity-and-authentication.md)
