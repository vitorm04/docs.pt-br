---
title: Visão geral de descoberta do WCF
ms.date: 03/30/2017
ms.assetid: 84fad0e4-23b1-45b5-a2d4-c9cdf90bbb22
ms.openlocfilehash: fce16038b4c9ab65047125be1881be4b86976aee
ms.sourcegitcommit: 68653db98c5ea7744fd438710248935f70020dfb
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 08/22/2019
ms.locfileid: "69931850"
---
# <a name="wcf-discovery-overview"></a><span data-ttu-id="e8e7e-102">Visão geral de descoberta do WCF</span><span class="sxs-lookup"><span data-stu-id="e8e7e-102">WCF Discovery Overview</span></span>
<span data-ttu-id="e8e7e-103">As APIs de descoberta fornecem um modelo de programação unificado para a publicação dinâmica e a descoberta de serviços Web usando o protocolo WS-Discovery.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-103">The Discovery APIs provide a unified programming model for the dynamic publication and discovery of Web services using the WS-Discovery protocol.</span></span> <span data-ttu-id="e8e7e-104">Essas APIs permitem que os serviços se publiquem e os clientes localizem serviços publicados.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-104">These APIs allow services to publish themselves and clients to find published services.</span></span> <span data-ttu-id="e8e7e-105">Depois que um serviço é detectável, o serviço tem a capacidade de enviar mensagens de comunicado, além de escutar e responder às solicitações de descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-105">Once a service is made discoverable, the service has the ability to send announcement messages as well as listen for and respond to discovery requests.</span></span> <span data-ttu-id="e8e7e-106">Os serviços detectáveis podem enviar mensagens de saudação para anunciar sua chegada em uma rede e até mesmo as mensagens para anunciar sua partida de uma rede.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-106">Discoverable services can send Hello messages to announce their arrival on a network and Bye messages to announce their departure from a network.</span></span> <span data-ttu-id="e8e7e-107">Para localizar um serviço, os clientes enviam uma `Probe` solicitação que contém critérios específicos, como tipo de contrato de serviço, palavras-chave e escopo na rede.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-107">To find a service, clients send a `Probe` request that contains specific criteria such as service contract type, keywords, and scope on the network.</span></span> <span data-ttu-id="e8e7e-108">Os serviços recebem `Probe` a solicitação e determinam se elas correspondem aos critérios.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-108">Services receive the `Probe` request and determine whether they match the criteria.</span></span> <span data-ttu-id="e8e7e-109">Se um serviço corresponder, ele responderá enviando uma `ProbeMatch` mensagem de volta ao cliente com as informações necessárias para entrar em contato com o serviço.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-109">If a service matches, it responds by sending a `ProbeMatch` message back to the client with the information necessary to contact the service.</span></span> <span data-ttu-id="e8e7e-110">Os clientes também podem `Resolve` enviar solicitações que permitem encontrar serviços que podem ter alterado seu endereço de ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-110">Clients can also send `Resolve` requests that allow them to find services that may have changed their endpoint address.</span></span> <span data-ttu-id="e8e7e-111">Os serviços correspondentes respondem às `Resolve` solicitações enviando uma `ResolveMatch` mensagem de volta ao cliente.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-111">Matching services respond to `Resolve` requests by sending a `ResolveMatch` message back to the client.</span></span>  
  
## <a name="ad-hoc-and-managed-modes"></a><span data-ttu-id="e8e7e-112">Modos ad-hoc e gerenciados</span><span class="sxs-lookup"><span data-stu-id="e8e7e-112">Ad-Hoc and Managed Modes</span></span>  
 <span data-ttu-id="e8e7e-113">A API de descoberta dá suporte a dois modos diferentes: Gerenciado e ad-hoc.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-113">The Discovery API supports two different modes: Managed and Ad-Hoc.</span></span> <span data-ttu-id="e8e7e-114">No modo gerenciado, há um servidor centralizado chamado proxy de descoberta que mantém informações sobre os serviços disponíveis.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-114">In Managed mode there is a centralized server called a discovery proxy that maintains information about available services.</span></span> <span data-ttu-id="e8e7e-115">O proxy de descoberta pode ser preenchido com informações sobre serviços de várias maneiras.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-115">The discovery proxy can be populated with information about services in a variety of ways.</span></span> <span data-ttu-id="e8e7e-116">Por exemplo, os serviços podem enviar mensagens de comunicado durante a inicialização para o proxy de descoberta ou o proxy pode ler dados de um banco de dado ou de um arquivo de configuração para determinar quais serviços estão disponíveis.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-116">For example, services can send announcement messages during start up to the discovery proxy or the proxy may read data from a database or a configuration file to determine what services are available.</span></span> <span data-ttu-id="e8e7e-117">O modo como o proxy de descoberta é populado é completamente o desenvolvedor.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-117">How the discovery proxy is populated is completely up to the developer.</span></span> <span data-ttu-id="e8e7e-118">Os clientes usam o proxy de descoberta para recuperar informações sobre os serviços disponíveis.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-118">Clients use the discovery proxy to retrieve information about available services.</span></span> <span data-ttu-id="e8e7e-119">Quando um cliente pesquisa um serviço, ele envia uma `Probe` mensagem para o proxy de descoberta e o proxy determina se algum dos serviços que ele sabe corresponde ao serviço que o cliente está procurando.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-119">When a client searches for a service it sends a `Probe` message to the discovery proxy and the proxy determines whether any of the services it knows about match the service the client is searching for.</span></span> <span data-ttu-id="e8e7e-120">Se houver correspondência, o proxy de descoberta enviará uma `ProbeMatch` resposta de volta ao cliente.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-120">If there are matches the discovery proxy sends a `ProbeMatch` response back to the client.</span></span> <span data-ttu-id="e8e7e-121">Em seguida, o cliente pode contatar o serviço diretamente usando as informações de serviço retornadas do proxy.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-121">The client can then contact the service directly using the service information returned from the proxy.</span></span> <span data-ttu-id="e8e7e-122">O princípio-chave por trás do modo gerenciado é que as solicitações de descoberta são enviadas de forma unicast para uma autoridade, o proxy de descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-122">The key principle behind Managed mode is that the discovery requests are sent in a unicast manner to one authority, the discovery proxy.</span></span> <span data-ttu-id="e8e7e-123">O .NET Framework contém os principais componentes que permitem que você crie seu próprio proxy.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-123">The .NET Framework contains key components that allow you to build your own proxy.</span></span> <span data-ttu-id="e8e7e-124">Os clientes e serviços podem localizar o proxy por vários métodos:</span><span class="sxs-lookup"><span data-stu-id="e8e7e-124">Clients and services can locate the proxy by multiple methods:</span></span>  
  
- <span data-ttu-id="e8e7e-125">O proxy pode responder a mensagens ad hoc.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-125">The proxy can respond to ad-hoc messages.</span></span>  
  
- <span data-ttu-id="e8e7e-126">O proxy pode enviar uma mensagem de anúncio durante a inicialização.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-126">The proxy can send an announcement message during start up.</span></span>  
  
- <span data-ttu-id="e8e7e-127">Os clientes e serviços podem ser escritos para procurar um ponto de extremidade conhecido específico.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-127">Clients and services can be written to look for a specific well-known endpoint.</span></span>  
  
 <span data-ttu-id="e8e7e-128">No modo ad hoc, não há nenhum servidor centralizado.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-128">In Ad-Hoc mode, there is no centralized server.</span></span> <span data-ttu-id="e8e7e-129">Todas as mensagens de descoberta, como anúncios de serviço e solicitações de cliente, são enviadas de maneira multicast.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-129">All discovery messages such as service announcements and client requests are sent in a multicast fashion.</span></span> <span data-ttu-id="e8e7e-130">Por padrão, a .NET Framework contém suporte para descoberta ad hoc em relação ao protocolo UDP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-130">By default the .NET Framework contains support for Ad-Hoc discovery over the UDP protocol.</span></span> <span data-ttu-id="e8e7e-131">Por exemplo, se um serviço for configurado para enviar um anúncio de saudação na inicialização, ele o enviará por um endereço de multicast bem conhecido usando o protocolo UDP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-131">For example, if a service is configured to send out a Hello announcement on start up, it sends it out over a well-known, multicast address using the UDP protocol.</span></span> <span data-ttu-id="e8e7e-132">Os clientes precisam escutar ativamente esses anúncios e processá-los de forma adequada.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-132">Clients have to actively listen for these announcements and process them accordingly.</span></span> <span data-ttu-id="e8e7e-133">Quando um cliente envia uma `Probe` mensagem para um serviço, ele é enviado pela rede usando um protocolo de multicast.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-133">When a client sends a `Probe` message for a service it is sent over the network using a multicast protocol.</span></span> <span data-ttu-id="e8e7e-134">Cada serviço que recebe a solicitação determina se ele corresponde aos critérios na `Probe` mensagem e responde diretamente ao cliente com uma `ProbeMatch` mensagem se o serviço corresponder aos critérios especificados na `Probe` mensagem.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-134">Each service that receives the request determines whether it matches the criteria in the `Probe` message and responds directly to the client with a `ProbeMatch` message if the service matches the criteria specified in the `Probe` message.</span></span>  
  
## <a name="benefits-of-using-wcf-discovery"></a><span data-ttu-id="e8e7e-135">Benefícios do uso da descoberta do WCF</span><span class="sxs-lookup"><span data-stu-id="e8e7e-135">Benefits of Using WCF Discovery</span></span>  
 <span data-ttu-id="e8e7e-136">Como a descoberta do WCF é implementada usando o protocolo WS-Discovery, ela é interoperável com outros clientes, serviços e proxies que implementam o WS-Discovery também.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-136">Because WCF Discovery is implemented using the WS-Discovery protocol it is interoperable with other clients, services, and proxies that implement WS-Discovery as well.</span></span> <span data-ttu-id="e8e7e-137">A descoberta do WCF se baseia nas APIs do WCF existentes, o que facilita a adição da funcionalidade de descoberta aos seus serviços e clientes existentes.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-137">WCF Discovery is built upon the existing WCF APIs, which makes it easy to add Discovery functionality to your existing services and clients.</span></span> <span data-ttu-id="e8e7e-138">A descoberta de serviço pode ser facilmente adicionada por meio das definições de configuração do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-138">Service discoverability can be easily added through the application configuration settings.</span></span> <span data-ttu-id="e8e7e-139">Além disso, a descoberta do WCF também dá suporte ao uso do protocolo de descoberta em outros transportes, como o peer net, a sobreposição de nomenclatura e o HTTP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-139">In addition, WCF Discovery also supports using the discovery protocol over other transports such as peer net, naming overlay, and HTTP.</span></span> <span data-ttu-id="e8e7e-140">A descoberta do WCF oferece suporte a um modo gerenciado de operação em que um proxy de descoberta é usado.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-140">WCF Discovery provides support for a Managed mode of operation where a discovery proxy is used.</span></span> <span data-ttu-id="e8e7e-141">Isso pode reduzir o tráfego de rede à medida que as mensagens são enviadas diretamente para o proxy de descoberta em vez de enviar mensagens multicast para toda a rede.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-141">This can reduce network traffic as messages are sent directly to the discovery proxy instead of sending multicast messages to the entire network.</span></span> <span data-ttu-id="e8e7e-142">A descoberta do WCF também permite mais flexibilidade ao trabalhar com serviços Web.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-142">WCF Discovery also allows for more flexibility when working with Web services.</span></span> <span data-ttu-id="e8e7e-143">Por exemplo, você pode alterar o endereço de um serviço sem precisar reconfigurar o cliente ou o serviço.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-143">For example, you can change the address of a service without having to reconfigure the client or the service.</span></span> <span data-ttu-id="e8e7e-144">Quando um cliente deve acessar o serviço, ele pode emitir `Probe` uma mensagem por `Find` meio de uma solicitação e esperar que o serviço responda com seu endereço atual.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-144">When a client must access the service it can issue a `Probe` message through a `Find` request and expect the service to respond with its current address.</span></span> <span data-ttu-id="e8e7e-145">A descoberta do WCF permite que um cliente procure um serviço com base em diferentes critérios, incluindo tipos de contrato, elementos de associação, namespace, escopo e palavras-chave ou números de versão.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-145">WCF Discovery allows a client to search for a service based on different criteria including contract types, binding elements, namespace, scope, and keywords or version numbers.</span></span> <span data-ttu-id="e8e7e-146">A descoberta do WCF permite a descoberta de tempo de execução e de design.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-146">WCF Discovery enables runtime and design time discovery.</span></span> <span data-ttu-id="e8e7e-147">Adicionar descoberta ao seu aplicativo pode ser usado para habilitar outros cenários, como tolerância a falhas e configuração automática.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-147">Adding discovery to your application can be used to enable other scenarios such as fault tolerance and auto configuration.</span></span>  
  
## <a name="service-publication"></a><span data-ttu-id="e8e7e-148">Publicação de serviço</span><span class="sxs-lookup"><span data-stu-id="e8e7e-148">Service Publication</span></span>  
 <span data-ttu-id="e8e7e-149">Para tornar um serviço detectável, um <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> deve ser adicionado ao host de serviço e um ponto de extremidade de descoberta deve ser adicionado para especificar onde ouvir as mensagens de descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-149">To make a service discoverable, a <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> must be added to the service host and a discovery endpoint must be added to specify where to listen for discovery messages.</span></span> <span data-ttu-id="e8e7e-150">O exemplo de código a seguir mostra como um serviço auto-hospedado pode ser modificado para torná-lo detectável.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-150">The following code example shows how a self-hosted service can be modified to make it discoverable.</span></span>  
  
```csharp  
Uri baseAddress = new Uri(string.Format("http://{0}:8000/discovery/scenarios/calculatorservice/{1}/",  
        System.Net.Dns.GetHostName(), Guid.NewGuid().ToString()));

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    serviceHost.Description.Behaviors.Add(new ServiceDiscoveryBehavior());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.AddServiceEndpoint(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
 <span data-ttu-id="e8e7e-151">Uma <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> instância deve ser adicionada a uma descrição de serviço para que o serviço seja detectável.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-151">A <xref:System.ServiceModel.Discovery.ServiceDiscoveryBehavior> instance must be added to a service description for the service to be discoverable.</span></span> <span data-ttu-id="e8e7e-152">Uma <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> instância deve ser adicionada ao host de serviço para informar ao serviço onde ouvir as solicitações de descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-152">A <xref:System.ServiceModel.Discovery.DiscoveryEndpoint> instance must be added to the service host to tell the service where to listen for discovery requests.</span></span> <span data-ttu-id="e8e7e-153">Neste exemplo, um <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (que é derivado de <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>) é adicionado para especificar que o serviço deve escutar solicitações de descoberta por meio do transporte multicast UDP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-153">In this example, a <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> (which is derived from <xref:System.ServiceModel.Discovery.DiscoveryEndpoint>) is added to specify that the service should listen for discovery requests over the UDP multicast transport.</span></span> <span data-ttu-id="e8e7e-154">O <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> é usado para descoberta ad hoc porque todas as mensagens são enviadas de forma multicast.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-154">The <xref:System.ServiceModel.Discovery.UdpDiscoveryEndpoint> is used for Ad-Hoc discovery because all messages are sent in a multicast fashion.</span></span>  
  
## <a name="announcement"></a><span data-ttu-id="e8e7e-155">Nascimento</span><span class="sxs-lookup"><span data-stu-id="e8e7e-155">Announcement</span></span>  
 <span data-ttu-id="e8e7e-156">Por padrão, a publicação do serviço não envia mensagens de comunicado.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-156">By default, service publication does not send out announcement messages.</span></span> <span data-ttu-id="e8e7e-157">O serviço deve ser configurado para enviar mensagens de comunicado.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-157">The service must be configured to send out announcement messages.</span></span> <span data-ttu-id="e8e7e-158">Isso fornece flexibilidade adicional para gravadores de serviço porque eles podem anunciar o serviço separadamente da escuta de mensagens de descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-158">This provides additional flexibility for service writers because they can announce the service separately from listening for discovery messages.</span></span> <span data-ttu-id="e8e7e-159">O comunicado de serviço também pode ser usado como um mecanismo para registrar serviços com um proxy de descoberta ou outros registros de serviço.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-159">Service announcement can also be used as a mechanism for registering services with a discovery proxy or other service registries.</span></span> <span data-ttu-id="e8e7e-160">O código a seguir mostra como configurar um serviço para enviar mensagens de anúncio por uma associação UDP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-160">The following code shows how to configure a service to send announcement messages over a UDP binding.</span></span>  
  
```csharp  
Uri baseAddress = new Uri(string.Format("http://{0}:8000/discovery/scenarios/calculatorservice/{1}/",
        System.Net.Dns.GetHostName(), Guid.NewGuid().ToString()));

// Create a ServiceHost for the CalculatorService type.
using (ServiceHost serviceHost = new ServiceHost(typeof(CalculatorService), baseAddress))
{
    // Add calculator endpoint
    serviceHost.AddServiceEndpoint(typeof(ICalculator), new WSHttpBinding(), string.Empty);

    // ** DISCOVERY ** //
    // Make the service discoverable by adding the discovery behavior
    ServiceDiscoveryBehavior discoveryBehavior = new ServiceDiscoveryBehavior();
    serviceHost.Description.Behaviors.Add(discoveryBehavior);

    // Send announcements on UDP multicast transport
    discoveryBehavior.AnnouncementEndpoints.Add(
      new UdpAnnouncementEndpoint());

    // ** DISCOVERY ** //
    // Add the discovery endpoint that specifies where to publish the services
    serviceHost.Description.Endpoints.Add(new UdpDiscoveryEndpoint());

    // Open the ServiceHost to create listeners and start listening for messages.
    serviceHost.Open();

    // The service can now be accessed.
    Console.WriteLine("Press <ENTER> to terminate service.");
    Console.ReadLine();
}
```  
  
## <a name="service-discovery"></a><span data-ttu-id="e8e7e-161">Descoberta de serviço</span><span class="sxs-lookup"><span data-stu-id="e8e7e-161">Service Discovery</span></span>  
 <span data-ttu-id="e8e7e-162">Um aplicativo cliente pode usar a <xref:System.ServiceModel.Discovery.DiscoveryClient> classe para localizar serviços.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-162">A client application can use the <xref:System.ServiceModel.Discovery.DiscoveryClient> class to find services.</span></span> <span data-ttu-id="e8e7e-163">O desenvolvedor cria uma instância da <xref:System.ServiceModel.Discovery.DiscoveryClient> classe que passa em um ponto de extremidade de descoberta que especifica onde enviar `Resolve` `Probe` ou mensagens.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-163">The developer creates an instance of the <xref:System.ServiceModel.Discovery.DiscoveryClient> class that passes in a discovery endpoint that specifies where to send `Probe` or `Resolve` messages.</span></span> <span data-ttu-id="e8e7e-164">Em seguida, o <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> cliente chama que especifica os critérios <xref:System.ServiceModel.Discovery.FindCriteria> de pesquisa dentro de uma instância.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-164">The client then calls <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> that specifies search criteria within a <xref:System.ServiceModel.Discovery.FindCriteria> instance.</span></span> <span data-ttu-id="e8e7e-165">Se os serviços correspondentes forem encontrados <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> , o retornará uma <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>coleção de.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-165">If matching services are found, <xref:System.ServiceModel.Discovery.DiscoveryClient.Find%2A> returns a collection of <xref:System.ServiceModel.Discovery.EndpointDiscoveryMetadata>.</span></span> <span data-ttu-id="e8e7e-166">O código a seguir mostra como chamar o `Find` método e, em seguida, conectar-se a um serviço descoberto.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-166">The following code shows how to call the `Find` method and then connect to a discovered service.</span></span>  
  
```csharp  
class Client
{
    static EndpointAddress serviceAddress;
  
    static void Main()
    {  
        if (FindService()) 
        {
            InvokeService();
        }
    }  
  
    // ** DISCOVERY ** //  
    static bool FindService()  
    {  
        Console.WriteLine("\nFinding Calculator Service ..");  
        DiscoveryClient discoveryClient =   
            new DiscoveryClient(new UdpDiscoveryEndpoint());  
  
        Collection<EndpointDiscoveryMetadata> calculatorServices =   
            (Collection<EndpointDiscoveryMetadata>)discoveryClient.Find(new FindCriteria(typeof(ICalculator))).Endpoints;  
  
        discoveryClient.Close();  
  
        if (calculatorServices.Count == 0)  
        {  
            Console.WriteLine("\nNo services are found.");  
            return false;  
        }  
        else  
        {  
            serviceAddress = calculatorServices[0].Address;  
            return true;  
        }  
    }  
  
    static void InvokeService()  
    {  
        Console.WriteLine("\nInvoking Calculator Service at {0}\n", serviceAddress);  
  
        // Create a client  
        CalculatorClient client = new CalculatorClient();  
        client.Endpoint.Address = serviceAddress;  
        client.Add(10,3);  
    }
}  
```  
  
## <a name="discovery-and-message-level-security"></a><span data-ttu-id="e8e7e-167">Descoberta e segurança em nível de mensagem</span><span class="sxs-lookup"><span data-stu-id="e8e7e-167">Discovery and Message Level Security</span></span>  
 <span data-ttu-id="e8e7e-168">Ao usar a segurança em nível de mensagem, é necessário <xref:System.ServiceModel.EndpointIdentity> especificar um no ponto de extremidade de descoberta <xref:System.ServiceModel.EndpointIdentity> de serviço e uma correspondência no ponto de extremidade de descoberta do cliente.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-168">When using message level security it is necessary to specify an <xref:System.ServiceModel.EndpointIdentity> on the service discovery endpoint and a matching <xref:System.ServiceModel.EndpointIdentity> on the client discovery endpoint.</span></span> <span data-ttu-id="e8e7e-169">Para obter mais informações sobre segurança de nível de mensagem, consulte [segurança da mensagem](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span><span class="sxs-lookup"><span data-stu-id="e8e7e-169">For more information about message level security, see [Message Security](../../../../docs/framework/wcf/feature-details/message-security-in-wcf.md).</span></span>  
  
## <a name="discovery-and-web-hosted-services"></a><span data-ttu-id="e8e7e-170">Descoberta e serviços hospedados na Web</span><span class="sxs-lookup"><span data-stu-id="e8e7e-170">Discovery and Web Hosted Services</span></span>  
 <span data-ttu-id="e8e7e-171">Para que os serviços WCF sejam detectáveis, eles devem estar em execução.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-171">In order for WCF services to be discoverable they must be running.</span></span> <span data-ttu-id="e8e7e-172">Os serviços WCF hospedados no IIS ou não são executados até que o IIS/WAS receba uma mensagem associada ao serviço, portanto, eles não podem ser detectáveis por padrão.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-172">WCF services hosted under IIS or WAS do not run until IIS/WAS receives a message bound for the service, so they cannot be discoverable by default.</span></span>  <span data-ttu-id="e8e7e-173">Há duas opções para tornar a descoberta de serviços hospedados na Web:</span><span class="sxs-lookup"><span data-stu-id="e8e7e-173">There are two options for making Web-Hosted services discoverable:</span></span>  
  
1. <span data-ttu-id="e8e7e-174">Usar o recurso de início automático do Windows Server AppFabric</span><span class="sxs-lookup"><span data-stu-id="e8e7e-174">Use the Windows Server AppFabric Auto-Start feature</span></span>  
  
2. <span data-ttu-id="e8e7e-175">Usar um proxy de descoberta para se comunicar em nome do serviço</span><span class="sxs-lookup"><span data-stu-id="e8e7e-175">Use a discovery proxy to communicate on behalf of the service</span></span>  
  
 <span data-ttu-id="e8e7e-176">O Windows Server AppFabric tem um recurso de início automático que permitirá que um serviço seja iniciado antes de receber qualquer mensagem.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-176">Windows Server AppFabric has an Auto-Start feature that will allow a service to be started before receiving any messages.</span></span> <span data-ttu-id="e8e7e-177">Com esse conjunto de início automático, um serviço hospedado do IIS/WAS pode ser configurado para ser detectável.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-177">With this Auto-Start set, an IIS/WAS hosted service can be configured to be discoverable.</span></span> <span data-ttu-id="e8e7e-178">Para obter mais informações sobre o recurso de início automático, consulte o [recurso de início automático do Windows Server AppFabric](https://go.microsoft.com/fwlink/?LinkId=205545).</span><span class="sxs-lookup"><span data-stu-id="e8e7e-178">For more information about the Auto-Start feature see, [Windows Server AppFabric Auto-Start Feature](https://go.microsoft.com/fwlink/?LinkId=205545).</span></span> <span data-ttu-id="e8e7e-179">Além de ativar o recurso de início automático, você deve configurar o serviço para descoberta.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-179">Along with turning on the Auto-Start feature, you must configure the service for discovery.</span></span> <span data-ttu-id="e8e7e-180">Para obter mais informações, confira [Como: Adicione programaticamente a detectabilidade a um serviço WCF](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)e cliente Configurando a[descoberta em um arquivo de configuração](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="e8e7e-180">For more information, see [How to: Programmatically Add Discoverability to a WCF Service and Client](../../../../docs/framework/wcf/feature-details/how-to-programmatically-add-discoverability-to-a-wcf-service-and-client.md)[Configuring Discovery in a Configuration File](../../../../docs/framework/wcf/feature-details/configuring-discovery-in-a-configuration-file.md).</span></span>  
  
 <span data-ttu-id="e8e7e-181">Um proxy de descoberta pode ser usado para se comunicar em nome do serviço WCF quando o serviço não estiver em execução.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-181">A discovery proxy can be used to communicate on behalf of the WCF service when the service is not running.</span></span> <span data-ttu-id="e8e7e-182">O proxy pode escutar a investigação ou resolver mensagens e responder ao cliente.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-182">The proxy can listen for probe or resolve messages and respond to the client.</span></span> <span data-ttu-id="e8e7e-183">O cliente pode enviar mensagens diretamente para o serviço.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-183">The client can then send messages directly to the service.</span></span> <span data-ttu-id="e8e7e-184">Quando o cliente envia uma mensagem para o serviço, ele será instanciado para responder à mensagem.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-184">When the client sends a message to the service it will be instantiated to respond to the message.</span></span> <span data-ttu-id="e8e7e-185">Para obter mais informações sobre como implementar um proxy de descoberta, consulte [implementando um proxy de descoberta](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span><span class="sxs-lookup"><span data-stu-id="e8e7e-185">For more information about implementing a discovery proxy see, [Implementing a Discovery Proxy](../../../../docs/framework/wcf/feature-details/implementing-a-discovery-proxy.md).</span></span>  
  
> [!NOTE]
> <span data-ttu-id="e8e7e-186">Para que a descoberta do WCF funcione corretamente, todas as NICs (controlador de interface de rede) só devem ter um endereço IP.</span><span class="sxs-lookup"><span data-stu-id="e8e7e-186">For WCF Discovery to work correctly, all NICs (Network Interface Controller) should only have 1 IP address.</span></span>
