---
title: Utilizando sessões
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- sessions [WCF]
ms.assetid: 864ba12f-3331-4359-a359-6d6d387f1035
ms.openlocfilehash: a879e90aeab7b40529df1f1a60cd1f879c39720a
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79143168"
---
# <a name="using-sessions"></a><span data-ttu-id="eda8e-102">Utilizando sessões</span><span class="sxs-lookup"><span data-stu-id="eda8e-102">Using Sessions</span></span>
<span data-ttu-id="eda8e-103">Nos aplicativos da Windows Communication Foundation (WCF), uma *sessão* correlaciona um grupo de mensagens em uma conversa.</span><span class="sxs-lookup"><span data-stu-id="eda8e-103">In Windows Communication Foundation (WCF) applications, a *session* correlates a group of messages into a conversation.</span></span> <span data-ttu-id="eda8e-104">As sessões wcf são diferentes do objeto de sessão disponível em ASP.NET aplicativos, suportam diferentes comportamentos e são controladas de diferentes maneiras.</span><span class="sxs-lookup"><span data-stu-id="eda8e-104">WCF sessions are different than the session object available in ASP.NET applications, support different behaviors, and are controlled in different ways.</span></span> <span data-ttu-id="eda8e-105">Este tópico descreve os recursos que as sessões permitem em aplicativos WCF e como usá-los.</span><span class="sxs-lookup"><span data-stu-id="eda8e-105">This topic describes the features that sessions enable in WCF applications and how to use them.</span></span>  
  
## <a name="sessions-in-windows-communication-foundation-applications"></a><span data-ttu-id="eda8e-106">Sessões em Aplicativos da Fundação de Comunicação do Windows</span><span class="sxs-lookup"><span data-stu-id="eda8e-106">Sessions in Windows Communication Foundation Applications</span></span>  
 <span data-ttu-id="eda8e-107">Quando um contrato de serviço especifica que requer uma sessão, esse contrato está especificando que todas as chamadas (ou seja, as trocas de mensagens subjacentes que suportam as chamadas) devem fazer parte da mesma conversa.</span><span class="sxs-lookup"><span data-stu-id="eda8e-107">When a service contract specifies that it requires a session, that contract is specifying that all calls (that is, the underlying message exchanges that support the calls) must be part of the same conversation.</span></span> <span data-ttu-id="eda8e-108">Se um contrato especificar que permite sessões, mas não requer uma, os clientes podem se conectar e estabelecer uma sessão ou não estabelecer uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-108">If a contract specifies that it allows sessions but does not require one, clients can connect and either establish a session or not establish a session.</span></span> <span data-ttu-id="eda8e-109">Se a sessão terminar e uma mensagem for enviada pelo mesmo canal, uma exceção será lançada.</span><span class="sxs-lookup"><span data-stu-id="eda8e-109">If the session ends and a message is sent through the same channel an exception is thrown.</span></span>  
  
 <span data-ttu-id="eda8e-110">As sessões de WCF têm as seguintes características conceituais principais:</span><span class="sxs-lookup"><span data-stu-id="eda8e-110">WCF sessions have the following main conceptual features:</span></span>  
  
- <span data-ttu-id="eda8e-111">Eles são explicitamente iniciados e encerrados pelo aplicativo de chamada (o cliente WCF).</span><span class="sxs-lookup"><span data-stu-id="eda8e-111">They are explicitly initiated and terminated by the calling application (the WCF client).</span></span>  
  
- <span data-ttu-id="eda8e-112">As mensagens entregues durante uma sessão são processadas na ordem em que são recebidas.</span><span class="sxs-lookup"><span data-stu-id="eda8e-112">Messages delivered during a session are processed in the order in which they are received.</span></span>  
  
- <span data-ttu-id="eda8e-113">As sessões correlacionam um grupo de mensagens em uma conversa.</span><span class="sxs-lookup"><span data-stu-id="eda8e-113">Sessions correlate a group of messages into a conversation.</span></span> <span data-ttu-id="eda8e-114">Diferentes tipos de correlação são possíveis.</span><span class="sxs-lookup"><span data-stu-id="eda8e-114">Different types of correlation are possible.</span></span> <span data-ttu-id="eda8e-115">Por exemplo, um canal baseado em sessão pode correlacionar mensagens com base em uma conexão de rede compartilhada, enquanto outro canal baseado em sessão pode correlacionar mensagens com base em uma tag compartilhada no corpo de mensagens.</span><span class="sxs-lookup"><span data-stu-id="eda8e-115">For instance, one session-based channel may correlate messages based on a shared network connection while another session-based channel may correlate messages based on a shared tag in the message body.</span></span> <span data-ttu-id="eda8e-116">As características que podem ser derivadas da sessão dependem da natureza da correlação.</span><span class="sxs-lookup"><span data-stu-id="eda8e-116">The features that can be derived from the session depend on the nature of the correlation.</span></span>  
  
- <span data-ttu-id="eda8e-117">Não há um armazenamento geral de dados associado a uma sessão wcf.</span><span class="sxs-lookup"><span data-stu-id="eda8e-117">There is no general data store associated with a WCF session.</span></span>  
  
 <span data-ttu-id="eda8e-118">Se você estiver <xref:System.Web.SessionState.HttpSessionState?displayProperty=nameWithType> familiarizado com a classe em ASP.NET aplicativos e a funcionalidade que ele fornece, você pode notar as seguintes diferenças entre esse tipo de sessão e sessões de WCF:</span><span class="sxs-lookup"><span data-stu-id="eda8e-118">If you are familiar with the <xref:System.Web.SessionState.HttpSessionState?displayProperty=nameWithType> class in ASP.NET applications and the functionality it provides, you might notice the following differences between that kind of session and WCF sessions:</span></span>  
  
- <span data-ttu-id="eda8e-119">ASP.NET sessões são sempre iniciadas pelo servidor.</span><span class="sxs-lookup"><span data-stu-id="eda8e-119">ASP.NET sessions are always server-initiated.</span></span>  
  
- <span data-ttu-id="eda8e-120">ASP.NET sessões são implicitamente desordenadas.</span><span class="sxs-lookup"><span data-stu-id="eda8e-120">ASP.NET sessions are implicitly unordered.</span></span>  
  
- <span data-ttu-id="eda8e-121">ASP.NET sessões fornecem um mecanismo geral de armazenamento de dados entre as solicitações.</span><span class="sxs-lookup"><span data-stu-id="eda8e-121">ASP.NET sessions provide a general data storage mechanism across requests.</span></span>  
  
 <span data-ttu-id="eda8e-122">Este tópico descreve:</span><span class="sxs-lookup"><span data-stu-id="eda8e-122">This topic describes:</span></span>  
  
- <span data-ttu-id="eda8e-123">O comportamento de execução padrão ao usar vinculações baseadas em sessão na camada do modelo de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-123">The default execution behavior when using session-based bindings in the service model layer.</span></span>  
  
- <span data-ttu-id="eda8e-124">Os tipos de recursos que as vinculações fornecidas por sessão wcf, fornecidas pelo sistema.</span><span class="sxs-lookup"><span data-stu-id="eda8e-124">The types of features that the WCF session-based, system-provided bindings provide.</span></span>  
  
- <span data-ttu-id="eda8e-125">Como criar um contrato que declare um requisito de sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-125">How to create a contract that declares a session requirement.</span></span>  
  
- <span data-ttu-id="eda8e-126">Como entender e controlar a criação e o término da sessão e a relação da sessão com a instância de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-126">How to understand and control the creation and termination of the session and the relationship of the session to the service instance.</span></span>  
  
## <a name="default-execution-behavior-using-sessions"></a><span data-ttu-id="eda8e-127">Comportamento de execução padrão usando sessões</span><span class="sxs-lookup"><span data-stu-id="eda8e-127">Default Execution Behavior Using Sessions</span></span>  
 <span data-ttu-id="eda8e-128">Uma vinculação que tenta iniciar uma sessão é chamada de vinculação *baseada em sessão.*</span><span class="sxs-lookup"><span data-stu-id="eda8e-128">A binding that attempts to initiate a session is called a *session-based* binding.</span></span> <span data-ttu-id="eda8e-129">Os contratos de serviço especificam que eles exigem, permitem <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A?displayProperty=nameWithType> ou recusam vinculações baseadas em sessão, definindo a propriedade na interface (ou classe) do contrato de serviço para um dos valores de <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> enumeração.</span><span class="sxs-lookup"><span data-stu-id="eda8e-129">Service contracts specify that they require, permit, or refuse session-based bindings by setting the <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A?displayProperty=nameWithType> property on the service contract interface (or class) to one of the <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> enumeration values.</span></span> <span data-ttu-id="eda8e-130">Por padrão, o valor <xref:System.ServiceModel.SessionMode.Allowed>dessa propriedade é , o que significa que se um cliente usa uma vinculação baseada em sessão com uma implementação de serviço WCF, o serviço estabelece e usa a sessão fornecida.</span><span class="sxs-lookup"><span data-stu-id="eda8e-130">By default, the value of this property is <xref:System.ServiceModel.SessionMode.Allowed>, which means that if a client uses a session-based binding with a WCF service implementation, the service establishes and uses the session provided.</span></span>  
  
 <span data-ttu-id="eda8e-131">Quando um serviço WCF aceita uma sessão de cliente, os seguintes recursos são ativados por padrão:</span><span class="sxs-lookup"><span data-stu-id="eda8e-131">When a WCF service accepts a client session, the following features are enabled by default:</span></span>  
  
1. <span data-ttu-id="eda8e-132">Todas as chamadas entre um objeto cliente WCF são tratadas pela mesma instância de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-132">All calls between a WCF client object are handled by the same service instance.</span></span>  
  
2. <span data-ttu-id="eda8e-133">Diferentes vinculações baseadas em sessão fornecem recursos adicionais.</span><span class="sxs-lookup"><span data-stu-id="eda8e-133">Different session-based bindings provide additional features.</span></span>  
  
## <a name="system-provided-session-types"></a><span data-ttu-id="eda8e-134">Tipos de sessão fornecidos pelo sistema</span><span class="sxs-lookup"><span data-stu-id="eda8e-134">System-Provided Session Types</span></span>  
 <span data-ttu-id="eda8e-135">Uma vinculação baseada em sessão suporta a associação padrão de uma instância de serviço com uma sessão específica.</span><span class="sxs-lookup"><span data-stu-id="eda8e-135">A session-based binding supports the default association of a service instance with a particular session.</span></span> <span data-ttu-id="eda8e-136">No entanto, diferentes vinculações baseadas em sessão suportam diferentes recursos, além de habilitar o controle de instancing baseado em sessão descrito anteriormente.</span><span class="sxs-lookup"><span data-stu-id="eda8e-136">However, different session-based bindings support different features in addition to enabling the session-based instancing control previously described.</span></span>  
  
 <span data-ttu-id="eda8e-137">O WCF fornece os seguintes tipos de comportamento de aplicativo baseado em sessão:</span><span class="sxs-lookup"><span data-stu-id="eda8e-137">WCF provides the following types of session-based application behavior:</span></span>  
  
- <span data-ttu-id="eda8e-138">O <xref:System.ServiceModel.Channels.SecurityBindingElement?displayProperty=nameWithType> suporte a sessões baseadas em segurança, nas quais ambas as extremidades da comunicação concordaram com uma conversa segura específica.</span><span class="sxs-lookup"><span data-stu-id="eda8e-138">The <xref:System.ServiceModel.Channels.SecurityBindingElement?displayProperty=nameWithType> supports security-based sessions, in which both ends of communication have agreed upon a specific secure conversation.</span></span> <span data-ttu-id="eda8e-139">Para obter mais informações, consulte [Protegendo serviços](securing-services.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-139">For more information, see [Securing Services](securing-services.md).</span></span> <span data-ttu-id="eda8e-140">Por exemplo, <xref:System.ServiceModel.WSHttpBinding?displayProperty=nameWithType> a vinculação, que contém suporte para sessões de segurança e sessões confiáveis, por padrão usa apenas uma sessão segura que criptografa e assina digitalmente mensagens.</span><span class="sxs-lookup"><span data-stu-id="eda8e-140">For example, the <xref:System.ServiceModel.WSHttpBinding?displayProperty=nameWithType> binding, which contains support for both security sessions and reliable sessions, by default uses only a secure session that encrypts and digitally signs messages.</span></span>  
  
- <span data-ttu-id="eda8e-141">A <xref:System.ServiceModel.NetTcpBinding?displayProperty=nameWithType> vinculação suporta sessões baseadas em TCP/IP para garantir que todas as mensagens estejam correlacionadas pela conexão no nível do soquete.</span><span class="sxs-lookup"><span data-stu-id="eda8e-141">The <xref:System.ServiceModel.NetTcpBinding?displayProperty=nameWithType> binding supports TCP/IP-based sessions to ensure that all messages are correlated by the connection at the socket level.</span></span>  
  
- <span data-ttu-id="eda8e-142">O <xref:System.ServiceModel.Channels.ReliableSessionBindingElement?displayProperty=nameWithType> elemento, que implementa a especificação WS-ReliableMessaging, fornece suporte para sessões confiáveis nas quais as mensagens podem ser configuradas para serem entregues em ordem e exatamente uma vez, garantindo que as mensagens sejam recebidas mesmo quando as mensagens viajam através de vários nós durante a conversa.</span><span class="sxs-lookup"><span data-stu-id="eda8e-142">The <xref:System.ServiceModel.Channels.ReliableSessionBindingElement?displayProperty=nameWithType> element, which implements the WS-ReliableMessaging specification, provides support for reliable sessions in which messages can be configured to be delivered in order and exactly once, ensuring messages are received even when messages travel across multiple nodes during the conversation.</span></span> <span data-ttu-id="eda8e-143">Para obter mais informações, consulte [Sessões Confiáveis](./feature-details/reliable-sessions.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-143">For more information, see [Reliable Sessions](./feature-details/reliable-sessions.md).</span></span>  
  
- <span data-ttu-id="eda8e-144">A <xref:System.ServiceModel.NetMsmqBinding?displayProperty=nameWithType> vinculação fornece sessões de datagram do MSMQ.</span><span class="sxs-lookup"><span data-stu-id="eda8e-144">The <xref:System.ServiceModel.NetMsmqBinding?displayProperty=nameWithType> binding provides MSMQ datagram sessions.</span></span> <span data-ttu-id="eda8e-145">Para obter mais informações, consulte [Filas no WCF](./feature-details/queues-in-wcf.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-145">For more information, see [Queues in WCF](./feature-details/queues-in-wcf.md).</span></span>  
  
 <span data-ttu-id="eda8e-146">A <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A> definição da propriedade não especifica o tipo de sessão que o contrato requer, apenas que requer uma.</span><span class="sxs-lookup"><span data-stu-id="eda8e-146">Setting the <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A> property does not specify the type of session the contract requires, only that it requires one.</span></span>  
  
## <a name="creating-a-contract-that-requires-a-session"></a><span data-ttu-id="eda8e-147">Criando um contrato que requer uma sessão</span><span class="sxs-lookup"><span data-stu-id="eda8e-147">Creating a Contract That Requires a Session</span></span>  
 <span data-ttu-id="eda8e-148">A criação de um contrato que exija uma sessão afirma que o grupo de operações que o contrato de serviço declara deve ser executado dentro da mesma sessão e que as mensagens devem ser entregues em ordem.</span><span class="sxs-lookup"><span data-stu-id="eda8e-148">Creating a contract that requires a session states that the group of operations that the service contract declares must all be executed within the same session and that messages must be delivered in order.</span></span> <span data-ttu-id="eda8e-149">Para afirmar o nível de suporte de sessão <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A?displayProperty=nameWithType> que um contrato de serviço exige, <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> defina a propriedade na interface ou classe do contrato de serviço para o valor da enumeração para especificar se o contrato:</span><span class="sxs-lookup"><span data-stu-id="eda8e-149">To assert the level of session support that a service contract requires, set the <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A?displayProperty=nameWithType> property on your service contract interface or class to the value of the <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> enumeration to specify whether the contract:</span></span>  
  
- <span data-ttu-id="eda8e-150">Requer uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-150">Requires a session.</span></span>  
  
- <span data-ttu-id="eda8e-151">Permite que um cliente estabeleça uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-151">Allows a client to establish a session.</span></span>  
  
- <span data-ttu-id="eda8e-152">Proíbe uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-152">Prohibits a session.</span></span>  
  
 <span data-ttu-id="eda8e-153">A <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A> definição da propriedade, no entanto, não especifica o tipo de comportamento baseado em sessão que o contrato exige.</span><span class="sxs-lookup"><span data-stu-id="eda8e-153">Setting the <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A> property does not, however, specify the type of session-based behavior the contract requires.</span></span> <span data-ttu-id="eda8e-154">Ele instrui o WCF a confirmar em tempo de execução que a vinculação configurada (que cria o canal de comunicação) para o serviço não faz, ou pode estabelecer uma sessão ao implementar um serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-154">It instructs WCF to confirm at runtime that the configured binding (which creates the communication channel) for the service does, does not, or can establish a session when implementing a service.</span></span> <span data-ttu-id="eda8e-155">Novamente, a vinculação pode satisfazer esse requisito com qualquer tipo de comportamento baseado em sessão que ele escolher — segurança, transporte, confiável ou alguma combinação.</span><span class="sxs-lookup"><span data-stu-id="eda8e-155">Again, the binding can satisfy that requirement with any type of session-based behavior it chooses—security, transport, reliable, or some combination.</span></span> <span data-ttu-id="eda8e-156">O comportamento exato depende <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> do valor selecionado.</span><span class="sxs-lookup"><span data-stu-id="eda8e-156">The exact behavior depends on the <xref:System.ServiceModel.SessionMode?displayProperty=nameWithType> value selected.</span></span> <span data-ttu-id="eda8e-157">Se a vinculação configurada do serviço não <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A>estiver em conformidade com o valor de , uma exceção será lançada.</span><span class="sxs-lookup"><span data-stu-id="eda8e-157">If the configured binding of the service does not conform to the value of <xref:System.ServiceModel.ServiceContractAttribute.SessionMode%2A>, an exception is thrown.</span></span> <span data-ttu-id="eda8e-158">As vinculações e os canais que eles criam dizem ser baseados em sessões.</span><span class="sxs-lookup"><span data-stu-id="eda8e-158">Bindings and the channels they create that support sessions are said to be session-based.</span></span>  
  
 <span data-ttu-id="eda8e-159">O contrato de serviço a seguir `ICalculatorSession` especifica que todas as operações no deve ser trocada dentro de uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-159">The following service contract specifies that all operations in the `ICalculatorSession` must be exchanged within a session.</span></span> <span data-ttu-id="eda8e-160">Nenhuma das operações retorna um valor `Equals` ao chamador, exceto o método.</span><span class="sxs-lookup"><span data-stu-id="eda8e-160">None of the operations returns a value to the caller except the `Equals` method.</span></span> <span data-ttu-id="eda8e-161">No entanto, o `Equals` método não tem parâmetros e, portanto, só pode retornar um valor não-zero dentro de uma sessão em que os dados já foram passados para as outras operações.</span><span class="sxs-lookup"><span data-stu-id="eda8e-161">However, the `Equals` method takes no parameters and, therefore, can only return a non-zero value inside a session in which data has already been passed to the other operations.</span></span> <span data-ttu-id="eda8e-162">Este contrato requer uma sessão para funcionar corretamente.</span><span class="sxs-lookup"><span data-stu-id="eda8e-162">This contract requires a session to function properly.</span></span> <span data-ttu-id="eda8e-163">Sem uma sessão associada a um cliente específico, a instância de serviço não tem como saber quais dados anteriores esse cliente enviou.</span><span class="sxs-lookup"><span data-stu-id="eda8e-163">Without a session associated with a specific client, the service instance has no way of knowing what previous data this client has sent.</span></span>  
  
 [!code-csharp[S_Service_Session#1](../../../samples/snippets/csharp/VS_Snippets_CFX/s_service_session/cs/service.cs#1)]
 [!code-vb[S_Service_Session#1](../../../samples/snippets/visualbasic/VS_Snippets_CFX/s_service_session/vb/service.vb#1)]  
  
 <span data-ttu-id="eda8e-164">Se um serviço permitir uma sessão, uma sessão será estabelecida e usada se o cliente iniciar uma; caso contrário, nenhuma sessão é estabelecida.</span><span class="sxs-lookup"><span data-stu-id="eda8e-164">If a service allows a session, then a session is established and used if the client initiates one; otherwise, no session is established.</span></span>  
  
## <a name="sessions-and-service-instances"></a><span data-ttu-id="eda8e-165">Sessões e Instâncias de Serviço</span><span class="sxs-lookup"><span data-stu-id="eda8e-165">Sessions and Service Instances</span></span>  
 <span data-ttu-id="eda8e-166">Se você usar o comportamento de insporto padrão no WCF, todas as chamadas entre um objeto cliente WCF serão tratadas pela mesma instância de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-166">If you use the default instancing behavior in WCF, all calls between a WCF client object are handled by the same service instance.</span></span> <span data-ttu-id="eda8e-167">Portanto, no nível de aplicação, você pode pensar em uma sessão como habilitando o comportamento do aplicativo semelhante ao comportamento de chamada local.</span><span class="sxs-lookup"><span data-stu-id="eda8e-167">Therefore, at the application level, you can think of a session as enabling application behavior similar to local call behavior.</span></span> <span data-ttu-id="eda8e-168">Por exemplo, quando você cria um objeto local:</span><span class="sxs-lookup"><span data-stu-id="eda8e-168">For example, when you create a local object:</span></span>  
  
- <span data-ttu-id="eda8e-169">Um construtor é chamado.</span><span class="sxs-lookup"><span data-stu-id="eda8e-169">A constructor is called.</span></span>  
  
- <span data-ttu-id="eda8e-170">Todas as chamadas subseqüentes feitas à referência de objeto cliente WCF são processadas pela mesma instância de objeto.</span><span class="sxs-lookup"><span data-stu-id="eda8e-170">All subsequent calls made to the WCF client object reference are processed by the same object instance.</span></span>  
  
- <span data-ttu-id="eda8e-171">Um destruidor é chamado quando a referência do objeto é destruída.</span><span class="sxs-lookup"><span data-stu-id="eda8e-171">A destructor is called when the object reference is destroyed.</span></span>  
  
 <span data-ttu-id="eda8e-172">As sessões permitem um comportamento semelhante entre clientes e serviços, desde que o comportamento padrão da instância de serviço seja usado.</span><span class="sxs-lookup"><span data-stu-id="eda8e-172">Sessions enable a similar behavior between clients and services as long as the default service instance behavior is used.</span></span> <span data-ttu-id="eda8e-173">Se um contrato de serviço exigir ou apoiar sessões, uma ou mais operações de contrato <xref:System.ServiceModel.OperationContractAttribute.IsInitiating%2A> <xref:System.ServiceModel.OperationContractAttribute.IsTerminating%2A> podem ser marcadas como iniciando ou terminando uma sessão definindo as propriedades.</span><span class="sxs-lookup"><span data-stu-id="eda8e-173">If a service contract requires or supports sessions, one or more contract operations can be marked as initiating or terminating a session by setting the <xref:System.ServiceModel.OperationContractAttribute.IsInitiating%2A> and <xref:System.ServiceModel.OperationContractAttribute.IsTerminating%2A> properties.</span></span>  
  
 <span data-ttu-id="eda8e-174">*As operações iniciantes* são aquelas que devem ser chamadas como a primeira operação de uma nova sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-174">*Initiating operations* are those that must be called as the first operation of a new session.</span></span> <span data-ttu-id="eda8e-175">Iniciar operações não pode ser chamado somente após pelo menos uma operação iniciando foi chamada.</span><span class="sxs-lookup"><span data-stu-id="eda8e-175">Non-initiating operations can be called only after at least one initiating operation has been called.</span></span> <span data-ttu-id="eda8e-176">Portanto, você pode criar uma espécie de construtor de sessão para o seu serviço, declarando iniciar operações projetadas para receber a entrada de clientes apropriados para o início da instância de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-176">You can therefore create a kind of session constructor for your service by declaring initiating operations designed to take input from clients appropriate to the beginning of the service instance.</span></span> <span data-ttu-id="eda8e-177">(O estado está associado à sessão, no entanto, e não ao objeto de serviço.)</span><span class="sxs-lookup"><span data-stu-id="eda8e-177">(The state is associated with the session, however, and not the service object.)</span></span>  
  
 <span data-ttu-id="eda8e-178">*As operações de término,* por outro lado, são aquelas que devem ser chamadas como a última mensagem em uma sessão existente.</span><span class="sxs-lookup"><span data-stu-id="eda8e-178">*Terminating operations*, conversely, are those that must be called as the last message in an existing session.</span></span> <span data-ttu-id="eda8e-179">Em casos padrão, WCF recicla o objeto de serviço e seu contexto após a sessão com que o serviço foi associado é fechada.</span><span class="sxs-lookup"><span data-stu-id="eda8e-179">In the default case, WCF recycles the service object and its context after the session with which the service was associated is closed.</span></span> <span data-ttu-id="eda8e-180">Você pode, portanto, criar uma espécie de destruidor declarando operações terminadas projetadas para executar uma função apropriada ao final da instância de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-180">You can, therefore, create a kind of destructor by declaring terminating operations designed to perform a function appropriate to the end of the service instance.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="eda8e-181">Embora o comportamento padrão tenha uma semelhança com construtores e destruidores locais, é apenas uma semelhança.</span><span class="sxs-lookup"><span data-stu-id="eda8e-181">Although the default behavior bears a resemblance to local constructors and destructors, it is only a resemblance.</span></span> <span data-ttu-id="eda8e-182">Qualquer operação de serviço WCF pode ser uma operação de início ou término, ou ambas ao mesmo tempo.</span><span class="sxs-lookup"><span data-stu-id="eda8e-182">Any WCF service operation can be an initiating or terminating operation, or both at the same time.</span></span> <span data-ttu-id="eda8e-183">Além disso, no caso padrão, as operações de início podem ser chamadas várias vezes em qualquer ordem; nenhuma sessão adicional é criada uma vez que a sessão é estabelecida e associada a uma instância, a menos que você controle explicitamente a vida útil da instância de serviço (manipulando o <xref:System.ServiceModel.InstanceContext?displayProperty=nameWithType> objeto).</span><span class="sxs-lookup"><span data-stu-id="eda8e-183">In addition, in the default case, initiating operations can be called any number of times in any order; no additional sessions are created once the session is established and associated with an instance unless you explicitly control the lifetime of the service instance (by manipulating the <xref:System.ServiceModel.InstanceContext?displayProperty=nameWithType> object).</span></span> <span data-ttu-id="eda8e-184">Finalmente, o estado está associado à sessão e não ao objeto de serviço.</span><span class="sxs-lookup"><span data-stu-id="eda8e-184">Finally, the state is associated with the session and not the service object.</span></span>  
  
 <span data-ttu-id="eda8e-185">Por exemplo, `ICalculatorSession` o contrato usado no exemplo anterior exige que `Clear` o objeto cliente WCF ligue primeiro para a operação antes `Equals` de qualquer outra operação e que a sessão com este objeto cliente WCF deve terminar quando ele chamar a operação.</span><span class="sxs-lookup"><span data-stu-id="eda8e-185">For example, the `ICalculatorSession` contract used in the preceding example requires that the WCF client object first call the `Clear` operation prior to any other operation and that the session with this WCF client object should terminate when it calls the `Equals` operation.</span></span> <span data-ttu-id="eda8e-186">O exemplo de código a seguir mostra um contrato que impõe esses requisitos.</span><span class="sxs-lookup"><span data-stu-id="eda8e-186">The following code example shows a contract that enforces these requirements.</span></span> <span data-ttu-id="eda8e-187">`Clear`deve ser chamado primeiro para iniciar uma `Equals` sessão, e essa sessão termina quando é chamada.</span><span class="sxs-lookup"><span data-stu-id="eda8e-187">`Clear` must be called first to initiate a session, and that session ends when `Equals` is called.</span></span>  
  
 [!code-csharp[SCA.IsInitiatingIsTerminating#1](../../../samples/snippets/csharp/VS_Snippets_CFX/sca.isinitiatingisterminating/cs/service.cs#1)]
 [!code-vb[SCA.IsInitiatingIsTerminating#1](../../../samples/snippets/visualbasic/VS_Snippets_CFX/sca.isinitiatingisterminating/vb/service.vb#1)]  
  
 <span data-ttu-id="eda8e-188">Os serviços não iniciam sessões com clientes.</span><span class="sxs-lookup"><span data-stu-id="eda8e-188">Services do not start sessions with clients.</span></span> <span data-ttu-id="eda8e-189">Nos aplicativos clientes WCF, existe uma relação direta entre a vida útil do canal baseado em sessão e a vida útil da própria sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-189">In WCF client applications, a direct relationship exists between the lifetime of the session-based channel and the lifetime of the session itself.</span></span> <span data-ttu-id="eda8e-190">Como tal, os clientes criam novas sessões criando novos canais baseados em sessões e desembaindo as sessões existentes fechando canais baseados em sessões graciosamente.</span><span class="sxs-lookup"><span data-stu-id="eda8e-190">As such, clients create new sessions by creating new session-based channels and tear down existing sessions by closing session-based channels gracefully.</span></span> <span data-ttu-id="eda8e-191">Um cliente inicia uma sessão com um ponto final de serviço ligando para um dos seguintes:</span><span class="sxs-lookup"><span data-stu-id="eda8e-191">A client starts a session with a service endpoint by calling one of the following:</span></span>  
  
- <span data-ttu-id="eda8e-192"><xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType>no canal retornou por <xref:System.ServiceModel.ChannelFactory%601.CreateChannel%2A?displayProperty=nameWithType>uma chamada para .</span><span class="sxs-lookup"><span data-stu-id="eda8e-192"><xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType> on the channel returned by a call to <xref:System.ServiceModel.ChannelFactory%601.CreateChannel%2A?displayProperty=nameWithType>.</span></span>  
  
- <span data-ttu-id="eda8e-193"><xref:System.ServiceModel.ClientBase%601.Open%2A?displayProperty=nameWithType>no objeto cliente WCF gerado pela [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-193"><xref:System.ServiceModel.ClientBase%601.Open%2A?displayProperty=nameWithType> on the WCF client object generated by the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md).</span></span>  
  
- <span data-ttu-id="eda8e-194">Uma operação de início em qualquer tipo de objeto cliente WCF (por padrão, todas as operações estão afiando).</span><span class="sxs-lookup"><span data-stu-id="eda8e-194">An initiating operation on either type of WCF client object (by default, all operations are initiating).</span></span> <span data-ttu-id="eda8e-195">Quando a primeira operação é chamada, o objeto cliente WCF abre automaticamente o canal e inicia uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-195">When the first operation is called, the WCF client object automatically opens the channel and initiates a session.</span></span>  
  
 <span data-ttu-id="eda8e-196">Normalmente, um cliente termina uma sessão com um ponto final de serviço ligando para um dos seguintes:</span><span class="sxs-lookup"><span data-stu-id="eda8e-196">Typically a client ends a session with a service endpoint by calling one of the following:</span></span>  
  
- <span data-ttu-id="eda8e-197"><xref:System.ServiceModel.ICommunicationObject.Close%2A?displayProperty=nameWithType>no canal retornou por <xref:System.ServiceModel.ChannelFactory%601.CreateChannel%2A?displayProperty=nameWithType>uma chamada para .</span><span class="sxs-lookup"><span data-stu-id="eda8e-197"><xref:System.ServiceModel.ICommunicationObject.Close%2A?displayProperty=nameWithType> on the channel returned by a call to <xref:System.ServiceModel.ChannelFactory%601.CreateChannel%2A?displayProperty=nameWithType>.</span></span>  
  
- <span data-ttu-id="eda8e-198"><xref:System.ServiceModel.ClientBase%601.Close%2A?displayProperty=nameWithType>sobre o objeto cliente WCF gerado por Svcutil.exe.</span><span class="sxs-lookup"><span data-stu-id="eda8e-198"><xref:System.ServiceModel.ClientBase%601.Close%2A?displayProperty=nameWithType> on the WCF client object generated by Svcutil.exe.</span></span>  
  
- <span data-ttu-id="eda8e-199">Uma operação de término em qualquer tipo de objeto cliente WCF (por padrão, nenhuma operação está terminando; o contrato deve especificar explicitamente uma operação de rescisão).</span><span class="sxs-lookup"><span data-stu-id="eda8e-199">A terminating operation on either type of WCF client object (by default, no operations are terminating; the contract must explicitly specify a terminating operation).</span></span> <span data-ttu-id="eda8e-200">Quando a primeira operação é chamada, o objeto cliente WCF abre automaticamente o canal e inicia uma sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-200">When the first operation is called, the WCF client object automatically opens the channel and initiates a session.</span></span>  
  
 <span data-ttu-id="eda8e-201">Por exemplo, [veja Como: Criar um serviço que exija sessões,](./feature-details/how-to-create-a-service-that-requires-sessions.md) bem como as amostras [de Comportamento de Serviço Padrão](./samples/default-service-behavior.md) e [Instancing.](./samples/instancing.md)</span><span class="sxs-lookup"><span data-stu-id="eda8e-201">For examples, see [How to: Create a Service That Requires Sessions](./feature-details/how-to-create-a-service-that-requires-sessions.md) as well as the [Default Service Behavior](./samples/default-service-behavior.md) and [Instancing](./samples/instancing.md) samples.</span></span>  
  
 <span data-ttu-id="eda8e-202">Para obter mais informações sobre clientes e sessões, consulte [Serviços de acesso usando um cliente WCF](./feature-details/accessing-services-using-a-client.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-202">For more information about clients and sessions, see [Accessing Services Using a WCF Client](./feature-details/accessing-services-using-a-client.md).</span></span>  
  
## <a name="sessions-interact-with-instancecontext-settings"></a><span data-ttu-id="eda8e-203">As sessões interagem com as configurações do contexto de instância</span><span class="sxs-lookup"><span data-stu-id="eda8e-203">Sessions Interact with InstanceContext Settings</span></span>  
 <span data-ttu-id="eda8e-204">Há uma interação <xref:System.ServiceModel.SessionMode> entre a enumeração <xref:System.ServiceModel.ServiceBehaviorAttribute.InstanceContextMode%2A?displayProperty=nameWithType> em um contrato e o imóvel, que controla a associação entre canais e objetos de serviço específicos.</span><span class="sxs-lookup"><span data-stu-id="eda8e-204">There is an interaction between the <xref:System.ServiceModel.SessionMode> enumeration in a contract and the <xref:System.ServiceModel.ServiceBehaviorAttribute.InstanceContextMode%2A?displayProperty=nameWithType> property, which controls the association between channels and specific service objects.</span></span> <span data-ttu-id="eda8e-205">Para obter mais informações, consulte [Sessões, Instancing e Concurrency](./feature-details/sessions-instancing-and-concurrency.md).</span><span class="sxs-lookup"><span data-stu-id="eda8e-205">For more information, see [Sessions, Instancing, and Concurrency](./feature-details/sessions-instancing-and-concurrency.md).</span></span>  
  
### <a name="sharing-instancecontext-objects"></a><span data-ttu-id="eda8e-206">Compartilhando objetos de contexto de instância</span><span class="sxs-lookup"><span data-stu-id="eda8e-206">Sharing InstanceContext Objects</span></span>  
 <span data-ttu-id="eda8e-207">Você também pode controlar qual canal ou chamada <xref:System.ServiceModel.InstanceContext> baseado em sessão está associado a qual objeto realizando essa associação você mesmo.</span><span class="sxs-lookup"><span data-stu-id="eda8e-207">You can also control which session-based channel or call is associated with which <xref:System.ServiceModel.InstanceContext> object by performing that association yourself.</span></span>
  
## <a name="sessions-and-streaming"></a><span data-ttu-id="eda8e-208">Sessões e Streaming</span><span class="sxs-lookup"><span data-stu-id="eda8e-208">Sessions and Streaming</span></span>  
 <span data-ttu-id="eda8e-209">Quando você tem uma grande quantidade de dados para transferir, o modo de transferência de streaming no WCF é uma alternativa viável ao comportamento padrão de buffering e processamento de mensagens na memória em sua totalidade.</span><span class="sxs-lookup"><span data-stu-id="eda8e-209">When you have a large amount of data to transfer, the streaming transfer mode in WCF is a feasible alternative to the default behavior of buffering and processing messages in memory in their entirety.</span></span> <span data-ttu-id="eda8e-210">Você pode obter um comportamento inesperado durante o streaming de chamadas com uma associação baseada em sessão.</span><span class="sxs-lookup"><span data-stu-id="eda8e-210">You may get unexpected behavior when streaming calls with a session-based binding.</span></span> <span data-ttu-id="eda8e-211">Todas as chamadas de streaming são feitas por um único canal (o canal de datagrama) que não oferece suporte a sessões mesmo que a associação que está sendo usada esteja configurada para usar sessões.</span><span class="sxs-lookup"><span data-stu-id="eda8e-211">All streaming calls are made through a single channel (the datagram channel) that does not support sessions even if the binding being used is configured to use sessions.</span></span> <span data-ttu-id="eda8e-212">Se vários clientes fizerem chamadas de streaming para o mesmo objeto de serviço durante uma vinculação baseada em `PerSession`sessão e o modo de simultâneo do objeto de serviço estiver definido como único e seu modo de contexto de instância estiver definido para, todas as chamadas devem passar pelo canal de datagram e, portanto, apenas uma chamada é processada por vez.</span><span class="sxs-lookup"><span data-stu-id="eda8e-212">If multiple clients make streaming calls to the same service object over a session-based binding, and the service object's concurrency mode is set to single and its instance context mode is set to `PerSession`, all calls must go through the datagram channel and so only one call is processed at a time.</span></span> <span data-ttu-id="eda8e-213">Um ou mais clientes podem, então, cronometrados. Você pode contornar esse problema definindo o `InstanceContextMode` `PerCall` objeto de serviço para ou Concurrency para vários.</span><span class="sxs-lookup"><span data-stu-id="eda8e-213">One or more clients may then time out. You can work around this issue by either setting the service object's `InstanceContextMode` to `PerCall` or Concurrency to multiple.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="eda8e-214">MaxConcurrentSessions não tem efeito neste caso porque há apenas uma "sessão" disponível.</span><span class="sxs-lookup"><span data-stu-id="eda8e-214">MaxConcurrentSessions have no effect in this case because there is only one "session" available.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="eda8e-215">Confira também</span><span class="sxs-lookup"><span data-stu-id="eda8e-215">See also</span></span>

- <xref:System.ServiceModel.OperationContractAttribute.IsInitiating%2A>
- <xref:System.ServiceModel.OperationContractAttribute.IsTerminating%2A>
