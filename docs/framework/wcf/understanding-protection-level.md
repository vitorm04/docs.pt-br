---
title: Noções básicas de nível de proteção
ms.date: 03/30/2017
dev_langs:
- csharp
- vb
helpviewer_keywords:
- WCF, security
- ProtectionLevel property
ms.assetid: 0c034608-a1ac-4007-8287-b1382eaa8bf2
ms.openlocfilehash: 896b75d3dfb5ebace9bef0c410e4a86dfb765bd8
ms.sourcegitcommit: 628e8147ca10187488e6407dab4c4e6ebe0cac47
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/15/2019
ms.locfileid: "72321522"
---
# <a name="understanding-protection-level"></a><span data-ttu-id="a8763-102">Noções básicas de nível de proteção</span><span class="sxs-lookup"><span data-stu-id="a8763-102">Understanding Protection Level</span></span>

<span data-ttu-id="a8763-103">A propriedade `ProtectionLevel` é encontrada em muitas classes diferentes, como as classes <xref:System.ServiceModel.ServiceContractAttribute> e <xref:System.ServiceModel.OperationContractAttribute>.</span><span class="sxs-lookup"><span data-stu-id="a8763-103">The `ProtectionLevel` property is found on many different classes, such as the <xref:System.ServiceModel.ServiceContractAttribute> and the <xref:System.ServiceModel.OperationContractAttribute> classes.</span></span> <span data-ttu-id="a8763-104">A propriedade controla como uma parte (ou todo) de uma mensagem é protegida.</span><span class="sxs-lookup"><span data-stu-id="a8763-104">The property controls how a part (or whole) of a message is protected.</span></span> <span data-ttu-id="a8763-105">Este tópico explica o recurso Windows Communication Foundation (WCF) e como ele funciona.</span><span class="sxs-lookup"><span data-stu-id="a8763-105">This topic explains the Windows Communication Foundation (WCF) feature and how it works.</span></span>

<span data-ttu-id="a8763-106">Para obter instruções sobre como definir o nível de proteção, consulte [como: definir a propriedade ProtectionLevel](how-to-set-the-protectionlevel-property.md).</span><span class="sxs-lookup"><span data-stu-id="a8763-106">For instructions on setting the protection level, see [How to: Set the ProtectionLevel Property](how-to-set-the-protectionlevel-property.md).</span></span>

> [!NOTE]
> <span data-ttu-id="a8763-107">Os níveis de proteção podem ser definidos somente no código, não na configuração.</span><span class="sxs-lookup"><span data-stu-id="a8763-107">Protection levels can be set only in code, not in configuration.</span></span>

## <a name="basics"></a><span data-ttu-id="a8763-108">Algumas</span><span class="sxs-lookup"><span data-stu-id="a8763-108">Basics</span></span>

<span data-ttu-id="a8763-109">Para entender o recurso de nível de proteção, as seguintes instruções básicas se aplicam:</span><span class="sxs-lookup"><span data-stu-id="a8763-109">To understand the protection level feature, the following basic statements apply:</span></span>

- <span data-ttu-id="a8763-110">Existem três níveis básicos de proteção para qualquer parte de uma mensagem.</span><span class="sxs-lookup"><span data-stu-id="a8763-110">Three basic levels of protection exist for any part of a message.</span></span> <span data-ttu-id="a8763-111">A propriedade (onde quer que ela ocorra) é definida como um dos valores de enumeração <xref:System.Net.Security.ProtectionLevel>.</span><span class="sxs-lookup"><span data-stu-id="a8763-111">The property (wherever it occurs) is set to one of the <xref:System.Net.Security.ProtectionLevel> enumeration values.</span></span> <span data-ttu-id="a8763-112">Em ordem crescente de proteção, eles incluem:</span><span class="sxs-lookup"><span data-stu-id="a8763-112">In ascending order of protection, they include:</span></span>

  - <span data-ttu-id="a8763-113">`None`</span><span class="sxs-lookup"><span data-stu-id="a8763-113">`None`.</span></span>

  - <span data-ttu-id="a8763-114">`Sign`</span><span class="sxs-lookup"><span data-stu-id="a8763-114">`Sign`.</span></span> <span data-ttu-id="a8763-115">A parte protegida é assinada digitalmente.</span><span class="sxs-lookup"><span data-stu-id="a8763-115">The protected part is digitally signed.</span></span> <span data-ttu-id="a8763-116">Isso garante a detecção de qualquer violação com a parte da mensagem protegida.</span><span class="sxs-lookup"><span data-stu-id="a8763-116">This ensures detection of any tampering with the protected message part.</span></span>

  - <span data-ttu-id="a8763-117">`EncryptAndSign`</span><span class="sxs-lookup"><span data-stu-id="a8763-117">`EncryptAndSign`.</span></span> <span data-ttu-id="a8763-118">A parte da mensagem é criptografada para garantir a confidencialidade antes de ser assinada.</span><span class="sxs-lookup"><span data-stu-id="a8763-118">The message part is encrypted to ensure confidentiality before it is signed.</span></span>

- <span data-ttu-id="a8763-119">Você pode definir os requisitos de proteção somente para *dados de aplicativo* com esse recurso.</span><span class="sxs-lookup"><span data-stu-id="a8763-119">You can set protection requirements only for *application data* with this feature.</span></span> <span data-ttu-id="a8763-120">Por exemplo, os cabeçalhos WS-Addressing são dados de infraestrutura e, portanto, não são afetados pelo `ProtectionLevel`.</span><span class="sxs-lookup"><span data-stu-id="a8763-120">For example, WS-Addressing headers are infrastructure data and, therefore, are not affected by the `ProtectionLevel`.</span></span>

- <span data-ttu-id="a8763-121">Quando o modo de segurança é definido como `Transport`, toda a mensagem é protegida pelo mecanismo de transporte.</span><span class="sxs-lookup"><span data-stu-id="a8763-121">When the security mode is set to `Transport`, the entire message is protected by the transport mechanism.</span></span> <span data-ttu-id="a8763-122">Portanto, a definição de um nível de proteção separado para diferentes partes de uma mensagem não tem nenhum efeito.</span><span class="sxs-lookup"><span data-stu-id="a8763-122">Therefore, setting a separate protection level for different parts of a message has no effect.</span></span>

- <span data-ttu-id="a8763-123">O `ProtectionLevel` é uma maneira para o desenvolvedor definir o *nível mínimo* com o qual uma associação deve estar em conformidade.</span><span class="sxs-lookup"><span data-stu-id="a8763-123">The `ProtectionLevel` is a way for the developer to set the *minimum level* that a binding must comply with.</span></span> <span data-ttu-id="a8763-124">Quando um serviço é implantado, a associação real especificada na configuração pode ou não dar suporte ao nível mínimo.</span><span class="sxs-lookup"><span data-stu-id="a8763-124">When a service is deployed, the actual binding specified in configuration may or may not support the minimum level.</span></span> <span data-ttu-id="a8763-125">Por exemplo, por padrão, a classe <xref:System.ServiceModel.BasicHttpBinding> não fornece segurança (embora possa ser habilitada).</span><span class="sxs-lookup"><span data-stu-id="a8763-125">For example, by default, the <xref:System.ServiceModel.BasicHttpBinding> class does not supply security (although it can be enabled).</span></span> <span data-ttu-id="a8763-126">Portanto, usá-lo com um contrato que tenha uma configuração diferente de `None` fará com que uma exceção seja gerada.</span><span class="sxs-lookup"><span data-stu-id="a8763-126">Therefore, using it with a contract that has any setting other than `None` will cause an exception to be thrown.</span></span>

- <span data-ttu-id="a8763-127">Se o serviço exigir que o mínimo de `ProtectionLevel` para todas as mensagens seja `Sign`, um cliente (talvez criado por uma tecnologia não WCF) poderá criptografar e assinar todas as mensagens (o que é mais do que o mínimo necessário).</span><span class="sxs-lookup"><span data-stu-id="a8763-127">If the service requires that the minimum `ProtectionLevel` for all messages is `Sign`, a client (perhaps created by a non-WCF technology) can encrypt and sign all messages (which is more than the minimum required).</span></span> <span data-ttu-id="a8763-128">Nesse caso, o WCF não lançará uma exceção, pois o cliente fez mais do que o mínimo.</span><span class="sxs-lookup"><span data-stu-id="a8763-128">In this case, WCF will not throw an exception because the client has done more than the minimum.</span></span> <span data-ttu-id="a8763-129">No entanto, observe que os aplicativos WCF (serviços ou clientes) não protegerão uma parte de mensagem, se possível, mas estarão em conformidade com o nível mínimo.</span><span class="sxs-lookup"><span data-stu-id="a8763-129">Note, however, that WCF applications (services or clients) will not over-secure a message part if possible but will comply with the minimum level.</span></span> <span data-ttu-id="a8763-130">Observe também que, ao usar `Transport` como o modo de segurança, o transporte pode exceder a segurança do fluxo de mensagens, pois é inerentemente impossível proteger em um nível mais granular.</span><span class="sxs-lookup"><span data-stu-id="a8763-130">Also note that when using `Transport` as the security mode, the transport may over-secure the message stream because it is inherently unable to secure at a more granular level.</span></span>

- <span data-ttu-id="a8763-131">Se você definir o `ProtectionLevel` explicitamente para `Sign` ou `EncryptAndSign`, deverá usar uma associação com segurança habilitada ou uma exceção será gerada.</span><span class="sxs-lookup"><span data-stu-id="a8763-131">If you set the `ProtectionLevel` explicitly to either `Sign` or `EncryptAndSign`, then you must use a binding with security enabled or an exception will be thrown.</span></span>

- <span data-ttu-id="a8763-132">Se você selecionar uma associação que habilite a segurança e não definir a propriedade `ProtectionLevel` em qualquer lugar do contrato, todos os dados do aplicativo serão criptografados e assinados.</span><span class="sxs-lookup"><span data-stu-id="a8763-132">If you select a binding that enables security and you do not set the `ProtectionLevel` property anywhere on the contract, all application data will be encrypted and signed.</span></span>

- <span data-ttu-id="a8763-133">Se você selecionar uma associação que não tem segurança habilitada (por exemplo, a classe `BasicHttpBinding` tem segurança desabilitada por padrão) e o `ProtectionLevel` não está definido explicitamente, nenhum dos dados do aplicativo será protegido.</span><span class="sxs-lookup"><span data-stu-id="a8763-133">If you select a binding that does not have security enabled (for example, the `BasicHttpBinding` class has security disabled by default), and the `ProtectionLevel` is not explicitly set, then none of the application data will be protected.</span></span>

- <span data-ttu-id="a8763-134">Se você estiver usando uma associação que aplica segurança no nível de transporte, todos os dados do aplicativo serão protegidos de acordo com os recursos do transporte.</span><span class="sxs-lookup"><span data-stu-id="a8763-134">If you are using a binding that applies security at the transport level, all application data will be secured according to the capabilities of the transport.</span></span>

- <span data-ttu-id="a8763-135">Se você usar uma associação que aplica segurança no nível da mensagem, os dados do aplicativo serão protegidos de acordo com os níveis de proteção definidos no contrato.</span><span class="sxs-lookup"><span data-stu-id="a8763-135">If you use a binding that applies security at the message level, then application data will be secured according to the protection levels set on the contract.</span></span> <span data-ttu-id="a8763-136">Se você não especificar um nível de proteção, todos os dados do aplicativo nas mensagens serão criptografados e assinados.</span><span class="sxs-lookup"><span data-stu-id="a8763-136">If you do not specify a protection level, then all application data in the messages will be encrypted and signed.</span></span>

- <span data-ttu-id="a8763-137">O `ProtectionLevel` pode ser definido em níveis de escopo diferentes.</span><span class="sxs-lookup"><span data-stu-id="a8763-137">The `ProtectionLevel` can be set at different scoping levels.</span></span> <span data-ttu-id="a8763-138">Há uma hierarquia associada ao escopo, que é explicada na próxima seção.</span><span class="sxs-lookup"><span data-stu-id="a8763-138">There is a hierarchy associated with scoping, which is explained in the next section.</span></span>

## <a name="scoping"></a><span data-ttu-id="a8763-139">Definição de escopo</span><span class="sxs-lookup"><span data-stu-id="a8763-139">Scoping</span></span>

<span data-ttu-id="a8763-140">Definir o `ProtectionLevel` na API de nível superior define o nível para todos os níveis abaixo dele.</span><span class="sxs-lookup"><span data-stu-id="a8763-140">Setting the `ProtectionLevel` on the topmost API sets the level for all levels below it.</span></span> <span data-ttu-id="a8763-141">Se o `ProtectionLevel` for definido como um valor diferente em um nível inferior, todas as APIs abaixo desse nível na hierarquia agora serão redefinidas para o novo nível (as APIs acima dela, no entanto, ainda serão afetadas pelo nível mais alto).</span><span class="sxs-lookup"><span data-stu-id="a8763-141">If the `ProtectionLevel` is set to a different value at a lower level, all APIs below that level in the hierarchy will now be reset to the new level (APIs above it, however, will still be affected by the topmost level).</span></span> <span data-ttu-id="a8763-142">A hierarquia é a seguinte:</span><span class="sxs-lookup"><span data-stu-id="a8763-142">The hierarchy is as follows.</span></span> <span data-ttu-id="a8763-143">Os atributos no mesmo nível são os pares.</span><span class="sxs-lookup"><span data-stu-id="a8763-143">Attributes at the same level are peers.</span></span>

- <xref:System.ServiceModel.ServiceContractAttribute>

- <xref:System.ServiceModel.OperationContractAttribute>

- <xref:System.ServiceModel.FaultContractAttribute>

- <xref:System.ServiceModel.MessageContractAttribute>

- <xref:System.ServiceModel.MessageHeaderAttribute>

- <xref:System.ServiceModel.MessageBodyMemberAttribute>

## <a name="programming-protectionlevel"></a><span data-ttu-id="a8763-144">Programando ProtectionLevel</span><span class="sxs-lookup"><span data-stu-id="a8763-144">Programming ProtectionLevel</span></span>

<span data-ttu-id="a8763-145">Para programar o `ProtectionLevel` em qualquer ponto na hierarquia, basta definir a propriedade como um valor apropriado ao aplicar o atributo.</span><span class="sxs-lookup"><span data-stu-id="a8763-145">To program the `ProtectionLevel` at any point in the hierarchy, simply set the property to an appropriate value when applying the attribute.</span></span> <span data-ttu-id="a8763-146">Para obter exemplos, consulte [como: definir a propriedade ProtectionLevel](how-to-set-the-protectionlevel-property.md).</span><span class="sxs-lookup"><span data-stu-id="a8763-146">For examples, see [How to: Set the ProtectionLevel Property](how-to-set-the-protectionlevel-property.md).</span></span>

> [!NOTE]
> <span data-ttu-id="a8763-147">Definir a propriedade em falhas e contratos de mensagem requer a compreensão de como esses recursos funcionam.</span><span class="sxs-lookup"><span data-stu-id="a8763-147">Setting the property on faults and message contracts requires understanding how those features work.</span></span> <span data-ttu-id="a8763-148">Para obter mais informações, consulte [como: definir a propriedade ProtectionLevel](how-to-set-the-protectionlevel-property.md) e [usar contratos de mensagem](./feature-details/using-message-contracts.md).</span><span class="sxs-lookup"><span data-stu-id="a8763-148">For more information, see [How to: Set the ProtectionLevel Property](how-to-set-the-protectionlevel-property.md) and [Using Message Contracts](./feature-details/using-message-contracts.md).</span></span>

## <a name="ws-addressing-dependency"></a><span data-ttu-id="a8763-149">Dependência de WS-Addressing</span><span class="sxs-lookup"><span data-stu-id="a8763-149">WS-Addressing Dependency</span></span>

<span data-ttu-id="a8763-150">Na maioria dos casos, usar a [ferramenta de utilitário de metadados ServiceModel (svcutil. exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) para gerar um cliente garante que os contratos de cliente e serviço sejam idênticos.</span><span class="sxs-lookup"><span data-stu-id="a8763-150">In most cases, using the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) to generate a client ensures that the client and service contracts are identical.</span></span> <span data-ttu-id="a8763-151">No entanto, os contratos aparentemente idênticos podem fazer com que o cliente gere uma exceção.</span><span class="sxs-lookup"><span data-stu-id="a8763-151">However, seemingly identical contracts can cause the client to throw an exception.</span></span> <span data-ttu-id="a8763-152">Isso ocorre sempre que uma associação não dá suporte à especificação WS-Addressing e vários níveis de proteção são especificados no contrato.</span><span class="sxs-lookup"><span data-stu-id="a8763-152">This occurs whenever a binding does not support the WS-Addressing specification and multiple levels of protection are specified on the contract.</span></span> <span data-ttu-id="a8763-153">Por exemplo, a classe <xref:System.ServiceModel.BasicHttpBinding> não oferece suporte à especificação, ou se você criar uma associação personalizada que não ofereça suporte a WS-Addressing.</span><span class="sxs-lookup"><span data-stu-id="a8763-153">For example, the <xref:System.ServiceModel.BasicHttpBinding> class does not support the specification, or if you create a custom binding that does not support WS-Addressing.</span></span> <span data-ttu-id="a8763-154">O recurso `ProtectionLevel` depende da especificação WS-Addressing para habilitar diferentes níveis de proteção em um único contrato.</span><span class="sxs-lookup"><span data-stu-id="a8763-154">The `ProtectionLevel` feature relies on the WS-Addressing specification to enable different protection levels on a single contract.</span></span> <span data-ttu-id="a8763-155">Se a associação não oferecer suporte à especificação WS-Addressing, todos os níveis serão definidos para o mesmo nível de proteção.</span><span class="sxs-lookup"><span data-stu-id="a8763-155">If the binding does not support the WS-Addressing specification, all levels will be set to the same protection level.</span></span> <span data-ttu-id="a8763-156">O nível de proteção efetivo para todos os escopos no contrato será definido como o nível de proteção mais forte usado no contrato.</span><span class="sxs-lookup"><span data-stu-id="a8763-156">The effective protection level for all scopes on the contract will be set to the strongest protection level used on the contract.</span></span>

<span data-ttu-id="a8763-157">Isso pode causar um problema difícil de depurar à primeira vista.</span><span class="sxs-lookup"><span data-stu-id="a8763-157">This may cause a problem that is hard to debug at first glance.</span></span> <span data-ttu-id="a8763-158">É possível criar um contrato de cliente (uma interface) que inclua métodos para mais de um serviço.</span><span class="sxs-lookup"><span data-stu-id="a8763-158">It is possible to create a client contract (an interface) that includes methods for more than one service.</span></span> <span data-ttu-id="a8763-159">Ou seja, a mesma interface é usada para criar um cliente que se comunica com vários serviços e a interface única contém métodos para todos os serviços.</span><span class="sxs-lookup"><span data-stu-id="a8763-159">That is, the same interface is used to create a client that communicates with many services, and the single interface contains methods for all services.</span></span> <span data-ttu-id="a8763-160">O desenvolvedor deve cuidar desse cenário raro para invocar apenas os métodos que são aplicáveis a cada serviço específico.</span><span class="sxs-lookup"><span data-stu-id="a8763-160">The developer must take care in this rare scenario to invoke only those methods that are applicable for each particular service.</span></span> <span data-ttu-id="a8763-161">Se a associação for a classe <xref:System.ServiceModel.BasicHttpBinding>, não haverá suporte para vários níveis de proteção.</span><span class="sxs-lookup"><span data-stu-id="a8763-161">If the binding is the <xref:System.ServiceModel.BasicHttpBinding> class, multiple protection levels cannot be supported.</span></span> <span data-ttu-id="a8763-162">No entanto, um serviço que responde ao cliente pode responder a um cliente com um nível de proteção mais baixo do que o necessário.</span><span class="sxs-lookup"><span data-stu-id="a8763-162">However, a service replying to the client might respond to a client with a lower protection level than required.</span></span> <span data-ttu-id="a8763-163">Nesse caso, o cliente gerará uma exceção porque espera um nível de proteção mais alto.</span><span class="sxs-lookup"><span data-stu-id="a8763-163">In this case, the client will throw an exception because it expects a higher protection level.</span></span>

<span data-ttu-id="a8763-164">Um exemplo do código ilustra esse problema.</span><span class="sxs-lookup"><span data-stu-id="a8763-164">An example of the code illustrates this problem.</span></span> <span data-ttu-id="a8763-165">O exemplo a seguir mostra um serviço e um contrato de cliente.</span><span class="sxs-lookup"><span data-stu-id="a8763-165">The following example shows a service and a client contract.</span></span> <span data-ttu-id="a8763-166">Suponha que a associação seja o elemento [\<basicHttpBinding >](../configure-apps/file-schema/wcf/basichttpbinding.md) .</span><span class="sxs-lookup"><span data-stu-id="a8763-166">Assume that the binding is the [\<basicHttpBinding>](../configure-apps/file-schema/wcf/basichttpbinding.md) element.</span></span> <span data-ttu-id="a8763-167">Portanto, todas as operações em um contrato têm o mesmo nível de proteção.</span><span class="sxs-lookup"><span data-stu-id="a8763-167">Therefore, all operations on a contract have the same protection level.</span></span> <span data-ttu-id="a8763-168">Esse nível de proteção uniforme é determinado como o nível máximo de proteção em todas as operações.</span><span class="sxs-lookup"><span data-stu-id="a8763-168">This uniform protection level is determined as the maximum protection level across all operations.</span></span>

<span data-ttu-id="a8763-169">O contrato de serviço é:</span><span class="sxs-lookup"><span data-stu-id="a8763-169">The service contract is:</span></span>

[!code-csharp[c_ProtectionLevel#7](../../../samples/snippets/csharp/VS_Snippets_CFX/c_protectionlevel/cs/source.cs#7)]
[!code-vb[c_ProtectionLevel#7](../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_protectionlevel/vb/source.vb#7)]

<span data-ttu-id="a8763-170">O código a seguir mostra a interface de contrato do cliente.</span><span class="sxs-lookup"><span data-stu-id="a8763-170">The following code shows the client contract interface.</span></span> <span data-ttu-id="a8763-171">Observe que ele inclui um método `Tax` que se destina a ser usado com um serviço diferente:</span><span class="sxs-lookup"><span data-stu-id="a8763-171">Note that it includes a `Tax` method that is intended to be used with a different service:</span></span>

[!code-csharp[c_ProtectionLevel#8](../../../samples/snippets/csharp/VS_Snippets_CFX/c_protectionlevel/cs/source.cs#8)]
[!code-vb[c_ProtectionLevel#8](../../../samples/snippets/visualbasic/VS_Snippets_CFX/c_protectionlevel/vb/source.vb#8)]

<span data-ttu-id="a8763-172">Quando o cliente chama o método `Price`, ele gera uma exceção quando recebe uma resposta do serviço.</span><span class="sxs-lookup"><span data-stu-id="a8763-172">When the client calls the `Price` method, it throws an exception when it receives a reply from the service.</span></span> <span data-ttu-id="a8763-173">Isso ocorre porque o cliente não especifica um `ProtectionLevel` no `ServiceContractAttribute` e, portanto, o cliente usa o padrão (<xref:System.Net.Security.ProtectionLevel.EncryptAndSign>) para todos os métodos, incluindo o método `Price`.</span><span class="sxs-lookup"><span data-stu-id="a8763-173">This occurs because the client does not specify a `ProtectionLevel` on the `ServiceContractAttribute`, and therefore the client uses the default (<xref:System.Net.Security.ProtectionLevel.EncryptAndSign>) for all methods, including the `Price` method.</span></span> <span data-ttu-id="a8763-174">No entanto, o serviço retorna o valor usando o nível <xref:System.Net.Security.ProtectionLevel.Sign> porque o contrato de serviço define um único método que tem seu nível de proteção definido como <xref:System.Net.Security.ProtectionLevel.Sign>.</span><span class="sxs-lookup"><span data-stu-id="a8763-174">However, the service returns the value using the <xref:System.Net.Security.ProtectionLevel.Sign> level because the service contract defines a single method that has its protection level set to <xref:System.Net.Security.ProtectionLevel.Sign>.</span></span> <span data-ttu-id="a8763-175">Nesse caso, o cliente gerará um erro ao validar a resposta do serviço.</span><span class="sxs-lookup"><span data-stu-id="a8763-175">In this case, the client will throw an error when validating the response from the service.</span></span>

## <a name="see-also"></a><span data-ttu-id="a8763-176">Consulte também</span><span class="sxs-lookup"><span data-stu-id="a8763-176">See also</span></span>

- <xref:System.ServiceModel.ServiceContractAttribute>
- <xref:System.ServiceModel.OperationContractAttribute>
- <xref:System.ServiceModel.FaultContractAttribute>
- <xref:System.ServiceModel.MessageContractAttribute>
- <xref:System.ServiceModel.MessageHeaderAttribute>
- <xref:System.ServiceModel.MessageBodyMemberAttribute>
- <xref:System.Net.Security.ProtectionLevel>
- [<span data-ttu-id="a8763-177">Protegendo serviços</span><span class="sxs-lookup"><span data-stu-id="a8763-177">Securing Services</span></span>](securing-services.md)
- [<span data-ttu-id="a8763-178">Como definir a propriedade ProtectionLevel</span><span class="sxs-lookup"><span data-stu-id="a8763-178">How to: Set the ProtectionLevel Property</span></span>](how-to-set-the-protectionlevel-property.md)
- [<span data-ttu-id="a8763-179">Especificando e lidando com falhas em contratos e serviços</span><span class="sxs-lookup"><span data-stu-id="a8763-179">Specifying and Handling Faults in Contracts and Services</span></span>](specifying-and-handling-faults-in-contracts-and-services.md)
- [<span data-ttu-id="a8763-180">Usando contratos de mensagem</span><span class="sxs-lookup"><span data-stu-id="a8763-180">Using Message Contracts</span></span>](./feature-details/using-message-contracts.md)
