---
title: Controle de versão de serviço
ms.date: 03/30/2017
ms.assetid: 37575ead-d820-4a67-8059-da11a2ab48e2
ms.openlocfilehash: 3f9fd87eacf67a1b23568dcf87df086e935879ba
ms.sourcegitcommit: 14ad34f7c4564ee0f009acb8bfc0ea7af3bc9541
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/01/2019
ms.locfileid: "73423682"
---
# <a name="service-versioning"></a><span data-ttu-id="784c4-102">Controle de versão de serviço</span><span class="sxs-lookup"><span data-stu-id="784c4-102">Service Versioning</span></span>
<span data-ttu-id="784c4-103">Após a implantação inicial e, potencialmente, várias vezes durante o tempo de vida, os serviços (e os pontos de extremidade que eles expõem) talvez precisem ser alterados por vários motivos, como mudanças nas necessidades dos negócios, requisitos de tecnologia da informação ou para resolver outros edições.</span><span class="sxs-lookup"><span data-stu-id="784c4-103">After initial deployment, and potentially several times during their lifetime, services (and the endpoints they expose) may need to be changed for a variety of reasons, such as changing business needs, information technology requirements, or to address other issues.</span></span> <span data-ttu-id="784c4-104">Cada alteração introduz uma nova versão do serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-104">Each change introduces a new version of the service.</span></span> <span data-ttu-id="784c4-105">Este tópico explica como considerar o controle de versão no Windows Communication Foundation (WCF).</span><span class="sxs-lookup"><span data-stu-id="784c4-105">This topic explains how to consider versioning in Windows Communication Foundation (WCF).</span></span>  
  
## <a name="four-categories-of-service-changes"></a><span data-ttu-id="784c4-106">Quatro categorias de alterações de serviço</span><span class="sxs-lookup"><span data-stu-id="784c4-106">Four Categories of Service Changes</span></span>  
 <span data-ttu-id="784c4-107">As alterações nos serviços que podem ser necessárias podem ser classificadas em quatro categorias:</span><span class="sxs-lookup"><span data-stu-id="784c4-107">The changes to services that may be required can be classified into four categories:</span></span>  
  
- <span data-ttu-id="784c4-108">Alterações de contrato: por exemplo, uma operação pode ser adicionada ou um elemento de dados em uma mensagem pode ser adicionado ou alterado.</span><span class="sxs-lookup"><span data-stu-id="784c4-108">Contract changes: For example, an operation might be added, or a data element in a message might be added or changed.</span></span>  
  
- <span data-ttu-id="784c4-109">Alterações de endereço: por exemplo, um serviço é movido para um local diferente em que os pontos de extremidade têm novos endereços.</span><span class="sxs-lookup"><span data-stu-id="784c4-109">Address changes: For example, a service moves to a different location where endpoints have new addresses.</span></span>  
  
- <span data-ttu-id="784c4-110">Alterações de associação: por exemplo, um mecanismo de segurança muda ou suas configurações são alteradas.</span><span class="sxs-lookup"><span data-stu-id="784c4-110">Binding changes: For example, a security mechanism changes or its settings change.</span></span>  
  
- <span data-ttu-id="784c4-111">Alterações de implementação: por exemplo, quando uma implementação de método interno é alterada.</span><span class="sxs-lookup"><span data-stu-id="784c4-111">Implementation changes: For example, when an internal method implementation changes.</span></span>  
  
 <span data-ttu-id="784c4-112">Algumas dessas alterações são chamadas de "quebra" e outras são "não separáveis".</span><span class="sxs-lookup"><span data-stu-id="784c4-112">Some of these changes are called "breaking" and others are "nonbreaking."</span></span> <span data-ttu-id="784c4-113">Uma alteração será não *quebrada* se todas as mensagens que tiverem sido processadas com êxito na versão anterior forem processadas com êxito na nova versão.</span><span class="sxs-lookup"><span data-stu-id="784c4-113">A change is *nonbreaking* if all messages that would have been processed successfully in the previous version are processed successfully in the new version.</span></span> <span data-ttu-id="784c4-114">Qualquer alteração que não atenda a esse critério é uma alteração *significativa* .</span><span class="sxs-lookup"><span data-stu-id="784c4-114">Any change that does not meet that criterion is a *breaking* change.</span></span>  
  
## <a name="service-orientation-and-versioning"></a><span data-ttu-id="784c4-115">Orientação do serviço e controle de versão</span><span class="sxs-lookup"><span data-stu-id="784c4-115">Service Orientation and Versioning</span></span>  
 <span data-ttu-id="784c4-116">Uma das filosofias da orientação do serviço é que os serviços e os clientes são autônomos (ou independentes).</span><span class="sxs-lookup"><span data-stu-id="784c4-116">One of the tenets of service orientation is that services and clients are autonomous (or independent).</span></span> <span data-ttu-id="784c4-117">Entre outras coisas, isso implica que os desenvolvedores de serviço não podem pressupor que eles controlam ou sequer conhecem todos os clientes de serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-117">Among other things, this implies that service developers cannot assume that they control or even know about all service clients.</span></span> <span data-ttu-id="784c4-118">Isso elimina a opção de recompilar e reimplantar todos os clientes quando um serviço altera versões.</span><span class="sxs-lookup"><span data-stu-id="784c4-118">This eliminates the option of rebuilding and redeploying all clients when a service changes versions.</span></span> <span data-ttu-id="784c4-119">Este tópico pressupõe que o serviço atende a essa filosofia e, portanto, deve ser alterado ou "com controle de versão" independente de seus clientes.</span><span class="sxs-lookup"><span data-stu-id="784c4-119">This topic assumes the service adheres to this tenet and therefore must be changed or "versioned" independent of its clients.</span></span>  
  
 <span data-ttu-id="784c4-120">Nos casos em que uma alteração significativa é inesperada e não pode ser evitada, um aplicativo pode optar por ignorar essa filosofia e exigir que os clientes sejam recriados e reimplantados com uma nova versão do serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-120">In cases where a breaking change is unexpected and cannot be avoided, an application may choose to ignore this tenet and require that clients be rebuilt and redeployed with a new version of the service.</span></span>  
  
## <a name="contract-versioning"></a><span data-ttu-id="784c4-121">Controle de versão de contrato</span><span class="sxs-lookup"><span data-stu-id="784c4-121">Contract Versioning</span></span>  
 <span data-ttu-id="784c4-122">Os contratos usados por um cliente não precisam ser iguais aos do contrato usado pelo serviço; Eles precisam apenas ser compatíveis.</span><span class="sxs-lookup"><span data-stu-id="784c4-122">Contracts used by a client do not need to be the same as the contract used by the service; they need only to be compatible.</span></span>  
  
 <span data-ttu-id="784c4-123">Para contratos de serviço, compatibilidade significa que novas operações expostas pelo serviço podem ser adicionadas, mas as operações existentes não podem ser removidas ou alteradas semanticamente.</span><span class="sxs-lookup"><span data-stu-id="784c4-123">For service contracts, compatibility means new operations exposed by the service can be added but existing operations cannot be removed or changed semantically.</span></span>  
  
 <span data-ttu-id="784c4-124">Para contratos de dados, compatibilidade significa que novas definições de tipo de esquema podem ser adicionadas, mas as definições de tipo de esquema existentes não podem ser alteradas de maneiras significativas.</span><span class="sxs-lookup"><span data-stu-id="784c4-124">For data contracts, compatibility means new schema type definitions can be added but existing schema type definitions cannot be changed in breaking ways.</span></span> <span data-ttu-id="784c4-125">Alterações significativas podem incluir a remoção de membros de dados ou a alteração de seu tipo de dados modificada.</span><span class="sxs-lookup"><span data-stu-id="784c4-125">Breaking changes might include removing data members or changing their data type incompatibly.</span></span> <span data-ttu-id="784c4-126">Esse recurso permite que o serviço de alguma latitude altere a versão de seus contratos sem quebrar clientes.</span><span class="sxs-lookup"><span data-stu-id="784c4-126">This feature allows the service some latitude in changing the version of its contracts without breaking clients.</span></span> <span data-ttu-id="784c4-127">As próximas duas seções explicam alterações não separáveis e de interrupção que podem ser feitas em contratos de dados e serviços do WCF.</span><span class="sxs-lookup"><span data-stu-id="784c4-127">The next two sections explain nonbreaking and breaking changes that can be made to WCF data and service contracts.</span></span>  
  
## <a name="data-contract-versioning"></a><span data-ttu-id="784c4-128">Controle de versão de contrato de dados</span><span class="sxs-lookup"><span data-stu-id="784c4-128">Data Contract Versioning</span></span>  
 <span data-ttu-id="784c4-129">Esta seção lida com o controle de versão de dados ao usar as classes <xref:System.Runtime.Serialization.DataContractSerializer> e <xref:System.Runtime.Serialization.DataContractAttribute>.</span><span class="sxs-lookup"><span data-stu-id="784c4-129">This section deals with data versioning when using the <xref:System.Runtime.Serialization.DataContractSerializer> and <xref:System.Runtime.Serialization.DataContractAttribute> classes.</span></span>  
  
### <a name="strict-versioning"></a><span data-ttu-id="784c4-130">Controle de versão estrito</span><span class="sxs-lookup"><span data-stu-id="784c4-130">Strict Versioning</span></span>  
 <span data-ttu-id="784c4-131">Em muitos cenários em que a alteração de versões é um problema, o desenvolvedor de serviço não tem controle sobre os clientes e, portanto, não pode fazer suposições sobre como eles reagirão às alterações no XML ou no esquema da mensagem.</span><span class="sxs-lookup"><span data-stu-id="784c4-131">In many scenarios when changing versions is an issue, the service developer does not have control over the clients and therefore cannot make assumptions about how they would react to changes in the message XML or schema.</span></span> <span data-ttu-id="784c4-132">Nesses casos, você deve garantir que as novas mensagens serão validadas em relação ao esquema antigo, por dois motivos:</span><span class="sxs-lookup"><span data-stu-id="784c4-132">In these cases, you must guarantee that the new messages will validate against the old schema, for two reasons:</span></span>  
  
- <span data-ttu-id="784c4-133">Os clientes antigos foram desenvolvidos com a suposição de que o esquema não será alterado.</span><span class="sxs-lookup"><span data-stu-id="784c4-133">The old clients were developed with the assumption that the schema will not change.</span></span> <span data-ttu-id="784c4-134">Eles podem falhar ao processar as mensagens para as quais nunca foram projetadas.</span><span class="sxs-lookup"><span data-stu-id="784c4-134">They may fail to process messages that they were never designed for.</span></span>  
  
- <span data-ttu-id="784c4-135">Os clientes antigos podem executar a validação de esquema real no esquema antigo antes mesmo de tentar processar as mensagens.</span><span class="sxs-lookup"><span data-stu-id="784c4-135">The old clients may perform actual schema validation against the old schema before even attempting to process the messages.</span></span>  
  
 <span data-ttu-id="784c4-136">A abordagem recomendada em tais cenários é tratar os contratos de dados existentes como imutáveis e criar novos com nomes qualificados XML exclusivos.</span><span class="sxs-lookup"><span data-stu-id="784c4-136">The recommended approach in such scenarios is to treat existing data contracts as immutable and create new ones with unique XML qualified names.</span></span> <span data-ttu-id="784c4-137">O desenvolvedor de serviço então adicionaria novos métodos a um contrato de serviço existente ou criaria um novo contrato de serviço com métodos que usam o novo contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-137">The service developer would then either add new methods to an existing service contract or create a new service contract with methods that use the new data contract.</span></span>  
  
 <span data-ttu-id="784c4-138">Geralmente, será o caso de um desenvolvedor de serviços precisar escrever alguma lógica de negócios que deva ser executada em todas as versões de um contrato de dados mais um código comercial específico da versão para cada versão do contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-138">It will often be the case that a service developer needs to write some business logic that should run within all versions of a data contract plus version-specific business code for each version of the data contract.</span></span> <span data-ttu-id="784c4-139">O apêndice no final deste tópico explica como as interfaces podem ser usadas para atender a essa necessidade.</span><span class="sxs-lookup"><span data-stu-id="784c4-139">The appendix at the end of this topic explains how interfaces can be used to satisfy this need.</span></span>  
  
### <a name="lax-versioning"></a><span data-ttu-id="784c4-140">Controle de versão LAX</span><span class="sxs-lookup"><span data-stu-id="784c4-140">Lax Versioning</span></span>  
 <span data-ttu-id="784c4-141">Em muitos outros cenários, o desenvolvedor de serviços pode tomar a suposição de que adicionar um novo membro opcional ao contrato de dados não interromperá os clientes existentes.</span><span class="sxs-lookup"><span data-stu-id="784c4-141">In many other scenarios, the service developer can make the assumption that adding a new, optional member to the data contract will not break existing clients.</span></span> <span data-ttu-id="784c4-142">Isso requer que o desenvolvedor de serviço investigue se os clientes existentes não estão executando a validação de esquema e se eles ignoram membros de dados desconhecidos.</span><span class="sxs-lookup"><span data-stu-id="784c4-142">This requires the service developer to investigate whether existing clients are not performing schema validation and that they ignore unknown data members.</span></span> <span data-ttu-id="784c4-143">Nesses cenários, é possível tirar proveito dos recursos de contrato de dados para adicionar novos membros de uma maneira não-separável.</span><span class="sxs-lookup"><span data-stu-id="784c4-143">In these scenarios, it is possible to take advantage of data contract features for adding new members in a nonbreaking way.</span></span> <span data-ttu-id="784c4-144">O desenvolvedor do serviço pode tomar essa suposição com confiança se os recursos de contrato de dados para controle de versão já foram usados para a primeira versão do serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-144">The service developer can make this assumption with confidence if the data contract features for versioning were already used for the first version of the service.</span></span>  
  
 <span data-ttu-id="784c4-145">WCF, ASP.NET Web Services e muitas outras pilhas de serviço Web dão suporte ao *controle de versão LAX*: ou seja, eles não geram exceções para novos membros de dados desconhecidos em dados recebidos.</span><span class="sxs-lookup"><span data-stu-id="784c4-145">WCF, ASP.NET Web Services, and many other Web service stacks support *lax versioning*: that is, they do not throw exceptions for new unknown data members in received data.</span></span>  
  
 <span data-ttu-id="784c4-146">É fácil acreditar erroneamente que a adição de um novo membro não interromperá os clientes existentes.</span><span class="sxs-lookup"><span data-stu-id="784c4-146">It is easy to mistakenly believe that adding a new member will not break existing clients.</span></span> <span data-ttu-id="784c4-147">Se você não tiver certeza de que todos os clientes podem lidar com o controle de versão LAX, a recomendação é usar as diretrizes estritas de controle de versão e tratar os contratos de dados como imutáveis.</span><span class="sxs-lookup"><span data-stu-id="784c4-147">If you are unsure that all clients can handle lax versioning, the recommendation is to use the strict versioning guidelines and treat data contracts as immutable.</span></span>  
  
 <span data-ttu-id="784c4-148">Para obter diretrizes detalhadas para versões negligentes e estritas de contratos de dados, consulte [práticas recomendadas: controle de versão de contrato de dados](best-practices-data-contract-versioning.md).</span><span class="sxs-lookup"><span data-stu-id="784c4-148">For detailed guidelines for both lax and strict versioning of data contracts, see [Best Practices: Data Contract Versioning](best-practices-data-contract-versioning.md).</span></span>  
  
### <a name="distinguishing-between-data-contract-and-net-types"></a><span data-ttu-id="784c4-149">Diferenciando entre os tipos de contrato de dados e .NET</span><span class="sxs-lookup"><span data-stu-id="784c4-149">Distinguishing Between Data Contract and .NET Types</span></span>  
 <span data-ttu-id="784c4-150">Uma classe ou estrutura .NET pode ser projetada como um contrato de dados aplicando o atributo <xref:System.Runtime.Serialization.DataContractAttribute> à classe.</span><span class="sxs-lookup"><span data-stu-id="784c4-150">A .NET class or structure can be projected as a data contract by applying the <xref:System.Runtime.Serialization.DataContractAttribute> attribute to the class.</span></span> <span data-ttu-id="784c4-151">O tipo .NET e suas projeções de contrato de dados são duas coisas distintas.</span><span class="sxs-lookup"><span data-stu-id="784c4-151">The .NET type and its data contract projections are two distinct matters.</span></span> <span data-ttu-id="784c4-152">É possível ter vários tipos .NET com a mesma projeção de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-152">It is possible to have multiple .NET types with the same data contract projection.</span></span> <span data-ttu-id="784c4-153">Essa distinção é especialmente útil para permitir que você altere o tipo .NET mantendo o contrato de dados projetado, mantendo a compatibilidade com os clientes existentes, mesmo no sentido estrito da palavra.</span><span class="sxs-lookup"><span data-stu-id="784c4-153">This distinction is especially useful in allowing you to change the .NET type while maintaining the projected data contract, thereby maintaining compatibility with existing clients even in the strict sense of the word.</span></span> <span data-ttu-id="784c4-154">Há duas coisas que você deve fazer sempre para manter essa distinção entre o tipo .NET e o contrato de dados:</span><span class="sxs-lookup"><span data-stu-id="784c4-154">There are two things you should always do to maintain this distinction between .NET type and data contract:</span></span>  
  
- <span data-ttu-id="784c4-155">Especifique um <xref:System.Runtime.Serialization.DataContractAttribute.Name%2A> e <xref:System.Runtime.Serialization.DataContractAttribute.Namespace%2A>.</span><span class="sxs-lookup"><span data-stu-id="784c4-155">Specify a <xref:System.Runtime.Serialization.DataContractAttribute.Name%2A> and <xref:System.Runtime.Serialization.DataContractAttribute.Namespace%2A>.</span></span> <span data-ttu-id="784c4-156">Você sempre deve especificar o nome e o namespace do contrato de dados para impedir que o nome e o namespace do seu tipo .NET sejam expostos no contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-156">You should always specify the name and namespace of your data contract to prevent your .NET type’s name and namespace from being exposed in the contract.</span></span> <span data-ttu-id="784c4-157">Dessa forma, se você decidir posteriormente alterar o namespace .NET ou o nome do tipo, seu contrato de dados permanecerá o mesmo.</span><span class="sxs-lookup"><span data-stu-id="784c4-157">This way, if you decide later to change the .NET namespace or type name, your data contract remains the same.</span></span>  
  
- <span data-ttu-id="784c4-158">Especifique <xref:System.Runtime.Serialization.DataMemberAttribute.Name%2A>.</span><span class="sxs-lookup"><span data-stu-id="784c4-158">Specify <xref:System.Runtime.Serialization.DataMemberAttribute.Name%2A>.</span></span> <span data-ttu-id="784c4-159">Você sempre deve especificar o nome dos seus membros de dados para impedir que o nome do membro do .NET seja exposto no contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-159">You should always specify the name of your data members to prevent your .NET member name from being exposed in the contract.</span></span> <span data-ttu-id="784c4-160">Dessa forma, se você decidir posteriormente alterar o nome do .NET do membro, seu contrato de dados permanecerá o mesmo.</span><span class="sxs-lookup"><span data-stu-id="784c4-160">This way, if you decide later to change the .NET name of the member, your data contract remains the same.</span></span>  
  
### <a name="changing-or-removing-members"></a><span data-ttu-id="784c4-161">Alterando ou removendo membros</span><span class="sxs-lookup"><span data-stu-id="784c4-161">Changing or Removing Members</span></span>  
 <span data-ttu-id="784c4-162">Alterar o nome ou o tipo de dados de um membro ou remover membros de dados é uma alteração significativa, mesmo que o controle de versão incerto seja permitido.</span><span class="sxs-lookup"><span data-stu-id="784c4-162">Changing the name or data type of a member, or removing data members is a breaking change even if lax versioning is allowed.</span></span> <span data-ttu-id="784c4-163">Se isso for necessário, crie um novo contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-163">If this is necessary, create a new data contract.</span></span>  
  
 <span data-ttu-id="784c4-164">Se a compatibilidade de serviço for de alta importância, você pode considerar ignorar membros de dados não utilizados em seu código e deixá-los em vigor.</span><span class="sxs-lookup"><span data-stu-id="784c4-164">If service compatibility is of high importance, you might consider ignoring unused data members in your code and leave them in place.</span></span> <span data-ttu-id="784c4-165">Se estiver dividindo um membro de dados em vários membros, você pode considerar deixar o membro existente em vigor como uma propriedade que pode executar a divisão necessária e a reagregação para clientes de nível inferior (clientes que não são atualizados para a versão mais recente).</span><span class="sxs-lookup"><span data-stu-id="784c4-165">If you are splitting up a data member into multiple members, you might consider leaving the existing member in place as a property that can perform the required splitting and re-aggregation for down-level clients (clients that are not upgraded to the latest version).</span></span>  
  
 <span data-ttu-id="784c4-166">Da mesma forma, as alterações no nome ou namespace do contrato de dados são alterações significativas.</span><span class="sxs-lookup"><span data-stu-id="784c4-166">Similarly, changes to the data contract’s name or namespace are breaking changes.</span></span>  
  
### <a name="round-trips-of-unknown-data"></a><span data-ttu-id="784c4-167">Viagens de ida e volta de dados desconhecidos</span><span class="sxs-lookup"><span data-stu-id="784c4-167">Round-Trips of Unknown Data</span></span>  
 <span data-ttu-id="784c4-168">Em alguns cenários, há a necessidade de "viagem de ida e volta" dados desconhecidos provenientes de membros adicionados em uma nova versão.</span><span class="sxs-lookup"><span data-stu-id="784c4-168">In some scenarios, there is a need to "round-trip" unknown data that comes from members added in a new version.</span></span> <span data-ttu-id="784c4-169">Por exemplo, um serviço "versionNew" envia dados com alguns membros adicionados recentemente a um cliente "versionOld".</span><span class="sxs-lookup"><span data-stu-id="784c4-169">For example, a "versionNew" service sends data with some newly added members to a "versionOld" client.</span></span> <span data-ttu-id="784c4-170">O cliente ignora os membros recém-adicionados ao processar a mensagem, mas reenvia os mesmos dados, incluindo os membros adicionados recentemente, de volta ao serviço versionNew.</span><span class="sxs-lookup"><span data-stu-id="784c4-170">The client ignores the newly added members when processing the message, but it resends that same data, including the newly added members, back to the versionNew service.</span></span> <span data-ttu-id="784c4-171">O cenário típico para isso são as atualizações de dados em que os dados são recuperados do serviço, alterados e retornados.</span><span class="sxs-lookup"><span data-stu-id="784c4-171">The typical scenario for this is data updates where data is retrieved from the service, changed, and returned.</span></span>  
  
 <span data-ttu-id="784c4-172">Para habilitar a ida e volta para um tipo específico, o tipo deve implementar a interface <xref:System.Runtime.Serialization.IExtensibleDataObject>.</span><span class="sxs-lookup"><span data-stu-id="784c4-172">To enable round-tripping for a particular type, the type must implement the <xref:System.Runtime.Serialization.IExtensibleDataObject> interface.</span></span> <span data-ttu-id="784c4-173">A interface contém uma propriedade, <xref:System.Runtime.Serialization.IExtensibleDataObject.ExtensionData%2A> que retorna o tipo de <xref:System.Runtime.Serialization.ExtensionDataObject>.</span><span class="sxs-lookup"><span data-stu-id="784c4-173">The interface contains one property, <xref:System.Runtime.Serialization.IExtensibleDataObject.ExtensionData%2A> that returns the <xref:System.Runtime.Serialization.ExtensionDataObject> type.</span></span> <span data-ttu-id="784c4-174">A propriedade é usada para armazenar quaisquer dados de versões futuras do contrato de dados que são desconhecidos para a versão atual.</span><span class="sxs-lookup"><span data-stu-id="784c4-174">The property is used to store any data from future versions of the data contract that is unknown to the current version.</span></span> <span data-ttu-id="784c4-175">Esses dados são opacos para o cliente, mas quando a instância é serializada, o conteúdo da propriedade <xref:System.Runtime.Serialization.IExtensibleDataObject.ExtensionData%2A> é escrito com o restante dos dados dos membros do contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-175">This data is opaque to the client, but when the instance is serialized, the content of the <xref:System.Runtime.Serialization.IExtensibleDataObject.ExtensionData%2A> property is written with the rest of the data contract members' data.</span></span>  
  
 <span data-ttu-id="784c4-176">É recomendável que todos os seus tipos implementem essa interface para acomodar Membros futuros novos e desconhecidos.</span><span class="sxs-lookup"><span data-stu-id="784c4-176">It is recommended that all your types implement this interface to accommodate new and unknown future members.</span></span>  
  
### <a name="data-contract-libraries"></a><span data-ttu-id="784c4-177">Bibliotecas de contratos de dados</span><span class="sxs-lookup"><span data-stu-id="784c4-177">Data Contract Libraries</span></span>  
 <span data-ttu-id="784c4-178">Pode haver bibliotecas de contratos de dados em que um contrato é publicado em um repositório central, e implementadores de serviço e tipo implementam e expõem contratos de dados desse repositório.</span><span class="sxs-lookup"><span data-stu-id="784c4-178">There may be libraries of data contracts where a contract is published to a central repository, and service and type implementers implement and expose data contracts from that repository.</span></span> <span data-ttu-id="784c4-179">Nesse caso, quando você publica um contrato de dados no repositório, não tem controle sobre quem cria tipos que o implementam.</span><span class="sxs-lookup"><span data-stu-id="784c4-179">In that case, when you publish a data contract to the repository, you have no control over who creates types that implement it.</span></span> <span data-ttu-id="784c4-180">Portanto, você não pode modificar o contrato depois que ele é publicado, renderizando-o de forma efetiva.</span><span class="sxs-lookup"><span data-stu-id="784c4-180">Thus, you cannot modify the contract once it is published, rendering it effectively immutable.</span></span>  
  
### <a name="when-using-the-xmlserializer"></a><span data-ttu-id="784c4-181">Ao usar o XmlSerializer</span><span class="sxs-lookup"><span data-stu-id="784c4-181">When Using the XmlSerializer</span></span>  
 <span data-ttu-id="784c4-182">Os mesmos princípios de controle de versão se aplicam ao usar a classe <xref:System.Xml.Serialization.XmlSerializer>.</span><span class="sxs-lookup"><span data-stu-id="784c4-182">The same versioning principles apply when using the <xref:System.Xml.Serialization.XmlSerializer> class.</span></span> <span data-ttu-id="784c4-183">Quando o controle de versão estrito é necessário, trate os contratos de dados como imutáveis e crie novos contratos de dados com nomes qualificados exclusivos para as novas versões.</span><span class="sxs-lookup"><span data-stu-id="784c4-183">When strict versioning is required, treat data contracts as immutable and create new data contracts with unique, qualified names for the new versions.</span></span> <span data-ttu-id="784c4-184">Quando tiver certeza de que o controle de versão negligente pode ser usado, você pode adicionar novos membros serializáveis em novas versões, mas não alterar ou remover membros existentes.</span><span class="sxs-lookup"><span data-stu-id="784c4-184">When you are sure that lax versioning can be used, you can add new serializable members in new versions but not change or remove existing members.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="784c4-185">O <xref:System.Xml.Serialization.XmlSerializer> usa os atributos <xref:System.Xml.Serialization.XmlAnyElementAttribute> e <xref:System.Xml.Serialization.XmlAnyAttributeAttribute> para dar suporte à ida e volta de dados desconhecidos.</span><span class="sxs-lookup"><span data-stu-id="784c4-185">The <xref:System.Xml.Serialization.XmlSerializer> uses the <xref:System.Xml.Serialization.XmlAnyElementAttribute> and <xref:System.Xml.Serialization.XmlAnyAttributeAttribute> attributes to support round-tripping of unknown data.</span></span>  
  
## <a name="message-contract-versioning"></a><span data-ttu-id="784c4-186">Controle de versão do contrato de mensagem</span><span class="sxs-lookup"><span data-stu-id="784c4-186">Message Contract Versioning</span></span>  
 <span data-ttu-id="784c4-187">As diretrizes para controle de versão de contrato de mensagem são muito semelhantes ao controle de versão de contratos de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-187">The guidelines for message contract versioning are very similar to versioning data contracts.</span></span> <span data-ttu-id="784c4-188">Se o controle de versão estrito for necessário, você não deverá alterar o corpo da mensagem, mas, em vez disso, criar um novo contrato de mensagem com um nome qualificado exclusivo.</span><span class="sxs-lookup"><span data-stu-id="784c4-188">If strict versioning is required, you should not change your message body but instead create a new message contract with a unique qualified name.</span></span> <span data-ttu-id="784c4-189">Se você sabe que pode usar o controle de versão negligente, você pode adicionar novas partes de corpo de mensagem, mas não alterar ou remover as existentes.</span><span class="sxs-lookup"><span data-stu-id="784c4-189">If you know that you can use lax versioning, you can add new message body parts but not change or remove existing ones.</span></span> <span data-ttu-id="784c4-190">Essa orientação se aplica a contratos de mensagem Bare-and-Wrapped.</span><span class="sxs-lookup"><span data-stu-id="784c4-190">This guidance applies both to bare and wrapped message contracts.</span></span>  
  
 <span data-ttu-id="784c4-191">Os cabeçalhos de mensagem sempre podem ser adicionados, mesmo se o controle de versão estrito estiver em uso.</span><span class="sxs-lookup"><span data-stu-id="784c4-191">Message headers can always be added, even if strict versioning is in use.</span></span> <span data-ttu-id="784c4-192">O sinalizador MustUnderstand pode afetar o controle de versão.</span><span class="sxs-lookup"><span data-stu-id="784c4-192">The MustUnderstand flag may affect versioning.</span></span> <span data-ttu-id="784c4-193">Em geral, o modelo de controle de versão para cabeçalhos no WCF é conforme descrito na especificação SOAP.</span><span class="sxs-lookup"><span data-stu-id="784c4-193">In general, the versioning model for headers in WCF is as described in the SOAP specification.</span></span>  
  
## <a name="service-contract-versioning"></a><span data-ttu-id="784c4-194">Controle de versão do contrato de serviço</span><span class="sxs-lookup"><span data-stu-id="784c4-194">Service Contract Versioning</span></span>  
 <span data-ttu-id="784c4-195">Semelhante ao controle de versão de contrato de dados, o controle de versão de contrato de serviço também envolve a adição, alteração e remoção de operações.</span><span class="sxs-lookup"><span data-stu-id="784c4-195">Similar to data contract versioning, service contract versioning also involves adding, changing, and removing operations.</span></span>  
  
### <a name="specifying-name-namespace-and-action"></a><span data-ttu-id="784c4-196">Especificando o nome, o namespace e a ação</span><span class="sxs-lookup"><span data-stu-id="784c4-196">Specifying Name, Namespace, and Action</span></span>  
 <span data-ttu-id="784c4-197">Por padrão, o nome de um contrato de serviço é o nome da interface.</span><span class="sxs-lookup"><span data-stu-id="784c4-197">By default, the name of a service contract is the name of the interface.</span></span> <span data-ttu-id="784c4-198">Seu namespace padrão é "http://tempuri.org" e a ação de cada operação é "http://tempuri.org/contractname/methodname".</span><span class="sxs-lookup"><span data-stu-id="784c4-198">Its default namespace is "http://tempuri.org", and each operation’s action is "http://tempuri.org/contractname/methodname".</span></span> <span data-ttu-id="784c4-199">É recomendável especificar explicitamente um nome e um namespace para o contrato de serviço, e uma ação para cada operação para evitar o uso de "http://tempuri.org" e impedir que os nomes de interface e de método sejam expostos no contrato do serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-199">It is recommended that you explicitly specify a name and namespace for the service contract, and an action for each operation to avoid using "http://tempuri.org" and to prevent interface and method names from being exposed in the service’s contract.</span></span>  
  
### <a name="adding-parameters-and-operations"></a><span data-ttu-id="784c4-200">Adicionando parâmetros e operações</span><span class="sxs-lookup"><span data-stu-id="784c4-200">Adding Parameters and Operations</span></span>  
 <span data-ttu-id="784c4-201">A adição de operações de serviço expostas pelo serviço é uma alteração sem interrupção porque os clientes existentes não precisam se preocupar com essas novas operações.</span><span class="sxs-lookup"><span data-stu-id="784c4-201">Adding service operations exposed by the service is a nonbreaking change because existing clients need not be concerned about those new operations.</span></span>  
  
> [!NOTE]
> <span data-ttu-id="784c4-202">Adicionar operações a um contrato de retorno de chamada duplex é uma alteração significativa.</span><span class="sxs-lookup"><span data-stu-id="784c4-202">Adding operations to a duplex callback contract is a breaking change.</span></span>  
  
### <a name="changing-operation-parameter-or-return-types"></a><span data-ttu-id="784c4-203">Alterando o parâmetro de operação ou tipos de retorno</span><span class="sxs-lookup"><span data-stu-id="784c4-203">Changing Operation Parameter or Return Types</span></span>  
 <span data-ttu-id="784c4-204">A alteração de parâmetros ou tipos de retorno geralmente é uma alteração significativa, a menos que o novo tipo implemente o mesmo contrato de dados implementado pelo tipo antigo.</span><span class="sxs-lookup"><span data-stu-id="784c4-204">Changing parameter or return types generally is a breaking change unless the new type implements the same data contract implemented by the old type.</span></span> <span data-ttu-id="784c4-205">Para fazer essa alteração, adicione uma nova operação ao contrato de serviço ou defina um novo contrato de serviço.</span><span class="sxs-lookup"><span data-stu-id="784c4-205">To make such a change, add a new operation to the service contract or define a new service contract.</span></span>  
  
### <a name="removing-operations"></a><span data-ttu-id="784c4-206">Removendo operações</span><span class="sxs-lookup"><span data-stu-id="784c4-206">Removing Operations</span></span>  
 <span data-ttu-id="784c4-207">A remoção de operações também é uma alteração significativa.</span><span class="sxs-lookup"><span data-stu-id="784c4-207">Removing operations is also a breaking change.</span></span> <span data-ttu-id="784c4-208">Para fazer essa alteração, defina um novo contrato de serviço e expor-o em um novo ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="784c4-208">To make such a change, define a new service contract and expose it on a new endpoint.</span></span>  
  
### <a name="fault-contracts"></a><span data-ttu-id="784c4-209">Contratos de falha</span><span class="sxs-lookup"><span data-stu-id="784c4-209">Fault Contracts</span></span>  
 <span data-ttu-id="784c4-210">O atributo <xref:System.ServiceModel.FaultContractAttribute> permite que um desenvolvedor de contrato de serviço Especifique informações sobre falhas que podem ser retornadas das operações do contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-210">The <xref:System.ServiceModel.FaultContractAttribute> attribute enables a service contract developer to specify information about faults that can be returned from the contract's operations.</span></span>  
  
 <span data-ttu-id="784c4-211">A lista de falhas descritas no contrato de um serviço não é considerada exaustiva.</span><span class="sxs-lookup"><span data-stu-id="784c4-211">The list of faults described in a service's contract is not considered exhaustive.</span></span> <span data-ttu-id="784c4-212">A qualquer momento, uma operação pode retornar falhas que não estão descritas em seu contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-212">At any time, an operation may return faults that are not described in its contract.</span></span> <span data-ttu-id="784c4-213">Portanto, a alteração do conjunto de falhas descritas no contrato não é considerada uma interrupção.</span><span class="sxs-lookup"><span data-stu-id="784c4-213">Therefore changing the set of faults described in the contract is not considered breaking.</span></span> <span data-ttu-id="784c4-214">Por exemplo, adicionar uma nova falha ao contrato usando o <xref:System.ServiceModel.FaultContractAttribute> ou remover uma falha existente do contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-214">For example, adding a new fault to the contract using the <xref:System.ServiceModel.FaultContractAttribute> or removing an existing fault from the contract.</span></span>  
  
### <a name="service-contract-libraries"></a><span data-ttu-id="784c4-215">Bibliotecas de contratos de serviço</span><span class="sxs-lookup"><span data-stu-id="784c4-215">Service Contract Libraries</span></span>  
 <span data-ttu-id="784c4-216">As organizações podem ter bibliotecas de contratos em que um contrato é publicado em um repositório central e implementadores de serviço implementam contratos desse repositório.</span><span class="sxs-lookup"><span data-stu-id="784c4-216">Organizations may have libraries of contracts where a contract is published to a central repository and service implementers implement contracts from that repository.</span></span> <span data-ttu-id="784c4-217">Nesse caso, quando você publica um contrato de serviço no repositório, não tem controle sobre quem cria serviços que o implementam.</span><span class="sxs-lookup"><span data-stu-id="784c4-217">In this case, when you publish a service contract to the repository you have no control over who creates services that implement it.</span></span> <span data-ttu-id="784c4-218">Portanto, você não pode modificar o contrato de serviço depois de publicado, renderizando-o efetivamente imutável.</span><span class="sxs-lookup"><span data-stu-id="784c4-218">Therefore, you cannot modify the service contract once published, rendering it effectively immutable.</span></span> <span data-ttu-id="784c4-219">O WCF dá suporte à herança de contrato, que pode ser usada para criar um novo contrato que estende os contratos existentes.</span><span class="sxs-lookup"><span data-stu-id="784c4-219">WCF supports contract inheritance, which can be used to create a new contract that extends existing contracts.</span></span> <span data-ttu-id="784c4-220">Para usar esse recurso, defina uma nova interface de contrato de serviço que herda da interface antiga do contrato de serviço e, em seguida, adicione métodos à nova interface.</span><span class="sxs-lookup"><span data-stu-id="784c4-220">To use this feature, define a new service contract interface that inherits from the old service contract interface, then add methods to the new interface.</span></span> <span data-ttu-id="784c4-221">Em seguida, altere o serviço que implementa o contrato antigo para implementar o novo contrato e altere a definição de ponto de extremidade "versionOld" para usar o novo contrato.</span><span class="sxs-lookup"><span data-stu-id="784c4-221">You then change the service that implements the old contract to implement the new contract and change the "versionOld" endpoint definition to use the new contract.</span></span> <span data-ttu-id="784c4-222">Para clientes "versionOld", o ponto de extremidade continuará a aparecer como expondo o contrato "versionOld"; para clientes "versionNew", o ponto de extremidade será exibido para expor o contrato "versionNew".</span><span class="sxs-lookup"><span data-stu-id="784c4-222">To "versionOld" clients, the endpoint will continue to appear as exposing the "versionOld" contract; to "versionNew" clients, the endpoint will appear to expose the "versionNew" contract.</span></span>  
  
## <a name="address-and-binding-versioning"></a><span data-ttu-id="784c4-223">Controle de versão de endereço e ligação</span><span class="sxs-lookup"><span data-stu-id="784c4-223">Address and Binding Versioning</span></span>  
 <span data-ttu-id="784c4-224">As alterações no endereço de ponto de extremidade e na associação são alterações significativas, a menos que os clientes possam descobrir dinamicamente o novo endereço de ponto de extremidade ou associação.</span><span class="sxs-lookup"><span data-stu-id="784c4-224">Changes to endpoint address and binding are breaking changes unless clients are capable of dynamically discovering the new endpoint address or binding.</span></span> <span data-ttu-id="784c4-225">Um mecanismo para implementar esse recurso é usando um registro UDDI (descrição de descoberta universal) e o padrão de invocação UDDI, em que um cliente tenta se comunicar com um ponto de extremidade e, após a falha, consulta um UDDI bem conhecido registro para os metadados do ponto de extremidade atual.</span><span class="sxs-lookup"><span data-stu-id="784c4-225">One mechanism for implementing this capability is by using a Universal Discovery Description and Integration (UDDI) registry and the UDDI Invocation Pattern where a client attempts to communicate with an endpoint and, upon failure, queries a well-known UDDI registry for the current endpoint metadata.</span></span> <span data-ttu-id="784c4-226">Em seguida, o cliente usa o endereço e a associação desses metadados para se comunicar com o ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="784c4-226">The client then uses the address and binding from this metadata to communicate with the endpoint.</span></span> <span data-ttu-id="784c4-227">Se essa comunicação for realizada com sucesso, o cliente armazenará em cache o endereço e as informações de associação para uso futuro.</span><span class="sxs-lookup"><span data-stu-id="784c4-227">If this communication succeeds, the client caches the address and binding information for future use.</span></span>  
  
## <a name="routing-service-and-versioning"></a><span data-ttu-id="784c4-228">Serviço de roteamento e controle de versão</span><span class="sxs-lookup"><span data-stu-id="784c4-228">Routing Service and Versioning</span></span>  
 <span data-ttu-id="784c4-229">Se as alterações feitas em um serviço forem alterações significativas e você precisar ter duas ou mais versões diferentes de um serviço em execução simultaneamente, você poderá usar o serviço de roteamento do WCF para rotear mensagens para a instância de serviço apropriada.</span><span class="sxs-lookup"><span data-stu-id="784c4-229">If the changes made to a service are breaking changes and you need to have two or more different versions of a service running simultaneously you can use the WCF Routing Service to route messages to the appropriate service instance.</span></span> <span data-ttu-id="784c4-230">O serviço de roteamento do WCF usa o roteamento baseado em conteúdo, em outras palavras, ele usa informações dentro da mensagem para determinar onde rotear a mensagem.</span><span class="sxs-lookup"><span data-stu-id="784c4-230">The WCF Routing Service uses content-based routing, in other words, it uses information within the message to determine where to route the message.</span></span> <span data-ttu-id="784c4-231">Para obter mais informações sobre o serviço de roteamento do WCF, consulte [serviço de roteamento](./feature-details/routing-service.md).</span><span class="sxs-lookup"><span data-stu-id="784c4-231">For more information about the WCF Routing Service see [Routing Service](./feature-details/routing-service.md).</span></span> <span data-ttu-id="784c4-232">Para obter um exemplo de como usar o serviço de roteamento do WCF para controle de versão de serviço, consulte [como: controle de versão de serviço](./feature-details/how-to-service-versioning.md).</span><span class="sxs-lookup"><span data-stu-id="784c4-232">For an example of how to use the WCF Routing Service for service versioning see [How To: Service Versioning](./feature-details/how-to-service-versioning.md).</span></span>  
  
## <a name="appendix"></a><span data-ttu-id="784c4-233">Anexo</span><span class="sxs-lookup"><span data-stu-id="784c4-233">Appendix</span></span>  
 <span data-ttu-id="784c4-234">As diretrizes de controle de versão de contrato de dados gerais quando o controle de versão estrito é necessário é tratar os contratos de dados como imutáveis e criar novos quando forem necessárias alterações.</span><span class="sxs-lookup"><span data-stu-id="784c4-234">The general data contract versioning guidance when strict versioning is needed is to treat data contracts as immutable and create new ones when changes are required.</span></span> <span data-ttu-id="784c4-235">Uma nova classe precisa ser criada para cada novo contrato de dados, portanto, um mecanismo é necessário para evitar a necessidade de usar o código existente que foi escrito em termos da classe antiga de contrato de dados e reescrevê-lo em termos da nova classe de contrato de dados.</span><span class="sxs-lookup"><span data-stu-id="784c4-235">A new class needs to be created for each new data contract, so a mechanism is needed to avoid having to take existing code that was written in terms of the old data contract class and rewrite it in terms of the new data contract class.</span></span>  
  
 <span data-ttu-id="784c4-236">Um desses mecanismos é usar interfaces para definir os membros de cada contrato de dados e escrever o código de implementação interno em termos das interfaces em vez das classes de contrato de dados que implementam as interfaces.</span><span class="sxs-lookup"><span data-stu-id="784c4-236">One such mechanism is to use interfaces to define the members of each data contract and write internal implementation code in terms of the interfaces rather than the data contract classes that implement the interfaces.</span></span> <span data-ttu-id="784c4-237">O código a seguir para a versão 1 de um serviço mostra uma interface `IPurchaseOrderV1` e um `PurchaseOrderV1`:</span><span class="sxs-lookup"><span data-stu-id="784c4-237">The following code for version 1 of a service shows an `IPurchaseOrderV1` interface and a `PurchaseOrderV1`:</span></span>  
  
```csharp  
public interface IPurchaseOrderV1  
{  
    string OrderId { get; set; }  
    string CustomerId { get; set; }  
}  
  
[DataContract(  
Name = "PurchaseOrder",  
Namespace = "http://examples.microsoft.com/WCF/2005/10/PurchaseOrder")]  
public class PurchaseOrderV1 : IPurchaseOrderV1  
{  
    [DataMember(...)]  
    public string OrderId {...}  
    [DataMember(...)]  
    public string CustomerId {...}  
}  
```  
  
 <span data-ttu-id="784c4-238">Embora as operações do contrato de serviço sejam gravadas em termos de `PurchaseOrderV1`, a lógica de negócios real seria em termos de `IPurchaseOrderV1`.</span><span class="sxs-lookup"><span data-stu-id="784c4-238">While the service contract’s operations would be written in terms of `PurchaseOrderV1`, the actual business logic would be in terms of `IPurchaseOrderV1`.</span></span> <span data-ttu-id="784c4-239">Em seguida, na versão 2, haveria uma nova interface `IPurchaseOrderV2` e uma nova classe `PurchaseOrderV2`, conforme mostrado no código a seguir:</span><span class="sxs-lookup"><span data-stu-id="784c4-239">Then, in version 2, there would be a new `IPurchaseOrderV2` interface and a new `PurchaseOrderV2` class as shown in the following code:</span></span>  
  
```csharp
public interface IPurchaseOrderV2  
{  
    DateTime OrderDate { get; set; }  
}

[DataContract(   
Name = "PurchaseOrder",  
Namespace = "http://examples.microsoft.com/WCF/2006/02/PurchaseOrder")]  
public class PurchaseOrderV2 : IPurchaseOrderV1, IPurchaseOrderV2  
{  
    [DataMember(...)]  
    public string OrderId {...}  
    [DataMember(...)]  
    public string CustomerId {...}  
    [DataMember(...)]  
    public DateTime OrderDate { ... }  
}  
```  
  
 <span data-ttu-id="784c4-240">O contrato de serviço seria atualizado para incluir novas operações que são gravadas em termos de `PurchaseOrderV2`.</span><span class="sxs-lookup"><span data-stu-id="784c4-240">The service contract would be updated to include new operations that are written in terms of `PurchaseOrderV2`.</span></span> <span data-ttu-id="784c4-241">A lógica de negócios existente escrita em termos de `IPurchaseOrderV1` continuaria a funcionar para o `PurchaseOrderV2` e a nova lógica de negócios que precisa da propriedade `OrderDate` seria gravada em termos de `IPurchaseOrderV2`.</span><span class="sxs-lookup"><span data-stu-id="784c4-241">Existing business logic written in terms of `IPurchaseOrderV1` would continue to work for `PurchaseOrderV2` and new business logic that needs the `OrderDate` property would be written in terms of `IPurchaseOrderV2`.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="784c4-242">Consulte também</span><span class="sxs-lookup"><span data-stu-id="784c4-242">See also</span></span>

- <xref:System.Runtime.Serialization.DataContractSerializer>
- <xref:System.Runtime.Serialization.DataContractAttribute>
- <xref:System.Runtime.Serialization.DataContractAttribute.Name%2A>
- <xref:System.Runtime.Serialization.DataContractAttribute.Namespace%2A>
- <xref:System.Runtime.Serialization.DataMemberAttribute.Order%2A>
- <xref:System.Runtime.Serialization.DataMemberAttribute.IsRequired%2A>
- <xref:System.Runtime.Serialization.IExtensibleDataObject>
- <xref:System.Runtime.Serialization.ExtensionDataObject>
- <xref:System.Runtime.Serialization.IExtensibleDataObject.ExtensionData%2A>
- <xref:System.Xml.Serialization.XmlSerializer>
- [<span data-ttu-id="784c4-243">Equivalência de contrato de dados</span><span class="sxs-lookup"><span data-stu-id="784c4-243">Data Contract Equivalence</span></span>](./feature-details/data-contract-equivalence.md)
- [<span data-ttu-id="784c4-244">Retornos de chamada de serialização tolerantes à versão</span><span class="sxs-lookup"><span data-stu-id="784c4-244">Version-Tolerant Serialization Callbacks</span></span>](./feature-details/version-tolerant-serialization-callbacks.md)
