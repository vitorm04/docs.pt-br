---
title: "Programação de nível por canal de serviço"
ms.custom: 
ms.date: 03/30/2017
ms.prod: .net-framework
ms.reviewer: 
ms.suite: 
ms.technology: dotnet-clr
ms.tgt_pltfrm: 
ms.topic: article
dev_langs:
- csharp
- vb
ms.assetid: 8d8dcd85-0a05-4c44-8861-4a0b3b90cca9
caps.latest.revision: "9"
author: dotnet-bot
ms.author: dotnetcontent
manager: wpickett
ms.openlocfilehash: 718f3c8fd2d6ed3434f7231e3ccaf8cf1a663875
ms.sourcegitcommit: ce279f2d7fe2220e6ea0a25a8a7a5370ddf8d9f0
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/02/2017
---
# <a name="service-channel-level-programming"></a><span data-ttu-id="d1e3e-102">Programação de nível por canal de serviço</span><span class="sxs-lookup"><span data-stu-id="d1e3e-102">Service Channel-Level Programming</span></span>
<span data-ttu-id="d1e3e-103">Este tópico descreve como escrever um [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] aplicativo de serviço sem usar o <xref:System.ServiceModel.ServiceHost?displayProperty=nameWithType> e seu modelo de objeto associado.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-103">This topic describes how to write a [!INCLUDE[indigo1](../../../../includes/indigo1-md.md)] service application without using the <xref:System.ServiceModel.ServiceHost?displayProperty=nameWithType> and its associated object model.</span></span>  
  
## <a name="receiving-messages"></a><span data-ttu-id="d1e3e-104">Recebendo mensagens</span><span class="sxs-lookup"><span data-stu-id="d1e3e-104">Receiving Messages</span></span>  
 <span data-ttu-id="d1e3e-105">Para estar pronto para receber e processar mensagens, são necessárias as seguintes etapas:</span><span class="sxs-lookup"><span data-stu-id="d1e3e-105">To be ready to receive and process messages, the following steps are required:</span></span>  
  
1.  <span data-ttu-id="d1e3e-106">Crie uma associação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-106">Create a binding.</span></span>  
  
2.  <span data-ttu-id="d1e3e-107">Crie um ouvinte de canal.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-107">Build a channel listener.</span></span>  
  
3.  <span data-ttu-id="d1e3e-108">Abra a escuta de canal.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-108">Open the channel listener.</span></span>  
  
4.  <span data-ttu-id="d1e3e-109">Ler a solicitação e enviar uma resposta.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-109">Read the request and send a reply.</span></span>  
  
5.  <span data-ttu-id="d1e3e-110">Feche todos os objetos de canal.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-110">Close all channel objects.</span></span>  
  
#### <a name="creating-a-binding"></a><span data-ttu-id="d1e3e-111">Criar uma associação</span><span class="sxs-lookup"><span data-stu-id="d1e3e-111">Creating a Binding</span></span>  
 <span data-ttu-id="d1e3e-112">A primeira etapa na escutando e recebimento de mensagens está criando uma associação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-112">The first step in listening for and receiving messages is creating a binding.</span></span> [!INCLUDE[indigo2](../../../../includes/indigo2-md.md)]<span data-ttu-id="d1e3e-113">é fornecido com várias ligações internas ou fornecido pelo sistema que podem ser usados diretamente, criando um deles.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-113"> ships with several built-in or system-provided bindings that can be used directly by instantiating one of them.</span></span> <span data-ttu-id="d1e3e-114">Além disso, você também pode criar sua própria associação personalizada criando uma instância de uma classe CustomBinding que é o que faz o código na listagem 1.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-114">In addition, you can also create your own custom binding by instantiating a CustomBinding class which is what the code in listing 1 does.</span></span>  
  
 <span data-ttu-id="d1e3e-115">O exemplo de código a seguir cria uma instância de <xref:System.ServiceModel.Channels.CustomBinding?displayProperty=nameWithType> e adiciona um <xref:System.ServiceModel.Channels.HttpTransportBindingElement?displayProperty=nameWithType> à sua coleção de elementos que é uma coleção de elementos que são usados para criar a pilha de canais de associação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-115">The code example below creates an instance of <xref:System.ServiceModel.Channels.CustomBinding?displayProperty=nameWithType> and adds an <xref:System.ServiceModel.Channels.HttpTransportBindingElement?displayProperty=nameWithType> to its Elements collection which is a collection of binding elements that are used to build the channel stack.</span></span> <span data-ttu-id="d1e3e-116">Neste exemplo, porque a coleção de elementos tem apenas o <xref:System.ServiceModel.Channels.HttpTransportBindingElement>, a pilha de canais resultante tem o canal de transporte HTTP.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-116">In this example, because the elements collection has only the <xref:System.ServiceModel.Channels.HttpTransportBindingElement>, the resulting channel stack has only the HTTP transport channel.</span></span>  
  
#### <a name="building-a-channellistener"></a><span data-ttu-id="d1e3e-117">Criando ChannelListener</span><span class="sxs-lookup"><span data-stu-id="d1e3e-117">Building a ChannelListener</span></span>  
 <span data-ttu-id="d1e3e-118">Depois de criar uma associação, podemos chamar <!--zz<xref:System.ServiceModel.Channels.Binding.BuildChannelListener%601%2A?displayProperty=nameWithType>--> `System.ServiceModel.Channels.Binding.BuildChannelListener` para criar o ouvinte de canal em que o parâmetro de tipo é a forma de canal para criar.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-118">After creating a binding, we call <!--zz<xref:System.ServiceModel.Channels.Binding.BuildChannelListener%601%2A?displayProperty=nameWithType>--> `System.ServiceModel.Channels.Binding.BuildChannelListener` to build the channel listener where the type parameter is the channel shape to create.</span></span> <span data-ttu-id="d1e3e-119">Neste exemplo estamos usando <xref:System.ServiceModel.Channels.IReplyChannel?displayProperty=nameWithType> como queremos escutar mensagens de entrada em um padrão de troca de mensagem de solicitação/resposta.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-119">In this example we are using <xref:System.ServiceModel.Channels.IReplyChannel?displayProperty=nameWithType> because we want to listen for incoming messages in a request/reply message exchange pattern.</span></span>  
  
 <span data-ttu-id="d1e3e-120"><xref:System.ServiceModel.Channels.IReplyChannel>é usado para receber mensagens e retornando mensagens de resposta de solicitação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-120"><xref:System.ServiceModel.Channels.IReplyChannel> is used for receiving request messages and sending back reply messages.</span></span> <span data-ttu-id="d1e3e-121">Chamando <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A?displayProperty=nameWithType> retorna um <xref:System.ServiceModel.Channels.IRequestChannel?displayProperty=nameWithType>, que pode ser usado para receber a mensagem de solicitação e enviar uma mensagem de resposta de volta.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-121">Calling <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A?displayProperty=nameWithType> returns an <xref:System.ServiceModel.Channels.IRequestChannel?displayProperty=nameWithType>, which can be used to receive the request message and to send back a reply message.</span></span>  
  
 <span data-ttu-id="d1e3e-122">Ao criar o ouvinte, passamos o endereço de rede no qual ele escuta, nesse caso `http://localhost:8080/channelapp`.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-122">When creating the listener, we pass the network address on which it listens, in this case `http://localhost:8080/channelapp`.</span></span> <span data-ttu-id="d1e3e-123">Em geral, cada canal de transporte dá suporte a uma ou possivelmente vários esquemas de endereço, por exemplo, o transporte HTTP dá suporte a esquemas http e https.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-123">In general, each transport channel supports one or possibly several address schemes, for example, the HTTP transport supports both http and https schemes.</span></span>  
  
 <span data-ttu-id="d1e3e-124">Também passamos vazio <xref:System.ServiceModel.Channels.BindingParameterCollection?displayProperty=nameWithType> ao criar o ouvinte.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-124">We also pass an empty <xref:System.ServiceModel.Channels.BindingParameterCollection?displayProperty=nameWithType> when creating the listener.</span></span> <span data-ttu-id="d1e3e-125">Um parâmetro de associação é um mecanismo para passar os parâmetros que controlam como o ouvinte deve ser criado.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-125">A binding parameter is a mechanism to pass parameters that control how the listener should be built.</span></span> <span data-ttu-id="d1e3e-126">Em nosso exemplo, estamos não usando qualquer esses parâmetros para que podemos passar uma coleção vazia.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-126">In our example, we are not using any such parameters so we pass an empty collection.</span></span>  
  
#### <a name="listening-for-incoming-messages"></a><span data-ttu-id="d1e3e-127">Escutar mensagens de entrada</span><span class="sxs-lookup"><span data-stu-id="d1e3e-127">Listening for Incoming Messages</span></span>  
 <span data-ttu-id="d1e3e-128">Em seguida, chamamos <xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType> no ouvinte e aceitando os canais de início.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-128">We then call <xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType> on the listener and start accepting channels.</span></span> <span data-ttu-id="d1e3e-129">O comportamento de <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A?displayProperty=nameWithType> depende se o transporte é orientado a conexão ou sem conexão.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-129">The behavior of <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A?displayProperty=nameWithType> depends on whether the transport is connection-oriented or connection-less.</span></span> <span data-ttu-id="d1e3e-130">Para os transportes orientado a conexão, <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A> bloqueia até que uma nova solicitação de conexão é fornecido no ponto em que ele retorna um novo canal que representa essa nova conexão.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-130">For connection-oriented transports, <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A> blocks until a new connection request comes in at which point it returns a new channel that represents that new connection.</span></span> <span data-ttu-id="d1e3e-131">Para os transportes de conexão menor, como HTTP, <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A> retornará imediatamente com um e apenas o canal que cria o ouvinte de transporte.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-131">For connection-less transports, such as HTTP, <xref:System.ServiceModel.Channels.IChannelListener%601.AcceptChannel%2A> returns immediately with the one and only channel that the transport listener creates.</span></span>  
  
 <span data-ttu-id="d1e3e-132">Neste exemplo, o ouvinte retorna um canal que implemente <xref:System.ServiceModel.Channels.IReplyChannel>.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-132">In this example, the listener returns a channel that implements <xref:System.ServiceModel.Channels.IReplyChannel>.</span></span> <span data-ttu-id="d1e3e-133">Para receber de mensagens neste canal, primeira chamada <xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType> nele para colocá-lo em um estado pronto para comunicação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-133">To receive messages on this channel we first call <xref:System.ServiceModel.ICommunicationObject.Open%2A?displayProperty=nameWithType> on it to place it in a state ready for communication.</span></span> <span data-ttu-id="d1e3e-134">Em seguida, chamamos <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A> quais blocos até que uma mensagem chega.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-134">We then call <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A> which blocks until a message arrives.</span></span>  
  
#### <a name="reading-the-request-and-sending-a-reply"></a><span data-ttu-id="d1e3e-135">Ler a solicitação e enviar uma resposta</span><span class="sxs-lookup"><span data-stu-id="d1e3e-135">Reading the Request and Sending a Reply</span></span>  
 <span data-ttu-id="d1e3e-136">Quando <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A> retorna um <xref:System.ServiceModel.Channels.RequestContext>, obtemos a mensagem recebida usando seu <xref:System.ServiceModel.Channels.RequestContext.RequestMessage%2A> propriedade.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-136">When <xref:System.ServiceModel.Channels.IReplyChannel.ReceiveRequest%2A> returns a <xref:System.ServiceModel.Channels.RequestContext>, we get the received message using its <xref:System.ServiceModel.Channels.RequestContext.RequestMessage%2A> property.</span></span> <span data-ttu-id="d1e3e-137">Podemos gravar o conteúdo da mensagem ação e o corpo, (que vamos supor que é uma cadeia de caracteres).</span><span class="sxs-lookup"><span data-stu-id="d1e3e-137">We write out the message’s action and body content, (which we assume is a string).</span></span>  
  
 <span data-ttu-id="d1e3e-138">Para enviar uma resposta, criamos uma nova mensagem de resposta nesse caso transmitindo de volta os dados de cadeia de caracteres que é recebido na solicitação.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-138">To send a reply, we create a new reply message in this case passing back the string data we received in the request.</span></span> <span data-ttu-id="d1e3e-139">Em seguida, chamamos <xref:System.ServiceModel.Channels.RequestContext.Reply%2A> para enviar a mensagem de resposta.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-139">We then call <xref:System.ServiceModel.Channels.RequestContext.Reply%2A> to send the reply message.</span></span>  
  
#### <a name="closing-objects"></a><span data-ttu-id="d1e3e-140">Fechando objetos</span><span class="sxs-lookup"><span data-stu-id="d1e3e-140">Closing Objects</span></span>  
 <span data-ttu-id="d1e3e-141">Para evitar o vazamento de recursos, é muito importante fechar objetos usados nas comunicações quando eles não são mais necessários.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-141">To avoid leaking resources, it is very important to close objects used in communications when they are no longer required.</span></span> <span data-ttu-id="d1e3e-142">Neste exemplo, podemos fechar a mensagem de solicitação, o contexto de solicitação, o canal e o ouvinte.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-142">In this example we close the request message, the request context, the channel and the listener.</span></span>  
  
 <span data-ttu-id="d1e3e-143">O exemplo de código a seguir mostra um serviço básico no qual um ouvinte de canal recebe apenas uma mensagem.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-143">The following code example shows a basic service in which a channel listener receives only one message.</span></span> <span data-ttu-id="d1e3e-144">Um serviço real mantém aceitando canais e receber mensagens até que o serviço será encerrado.</span><span class="sxs-lookup"><span data-stu-id="d1e3e-144">A real service keeps accepting channels and receiving messages until the service exits.</span></span>  
  
 [!code-csharp[ChannelProgrammingBasic#1](../../../../samples/snippets/csharp/VS_Snippets_CFX/channelprogrammingbasic/cs/serviceprogram.cs#1)]
 [!code-vb[ChannelProgrammingBasic#1](../../../../samples/snippets/visualbasic/VS_Snippets_CFX/channelprogrammingbasic/vb/serviceprogram.vb#1)]
