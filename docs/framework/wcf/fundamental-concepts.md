---
title: Conceitos fundamentais do Windows Communication Foundation
ms.date: 03/30/2017
helpviewer_keywords:
- WCF [WCF], concepts
- concepts [WCF]
- fundamentals [WCF]
- Windows Communication Foundation [WCF], concepts
ms.assetid: 3e7e0afd-7913-499d-bafb-eac7caacbc7a
ms.openlocfilehash: 9dcaa5f73dd8a4ec1943cb7fc840feee889563b8
ms.sourcegitcommit: 628e8147ca10187488e6407dab4c4e6ebe0cac47
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/15/2019
ms.locfileid: "72319843"
---
# <a name="fundamental-windows-communication-foundation-concepts"></a><span data-ttu-id="c893a-102">Conceitos fundamentais do Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="c893a-102">Fundamental Windows Communication Foundation Concepts</span></span>

<span data-ttu-id="c893a-103">Este documento fornece uma exibição de alto nível da arquitetura do Windows Communication Foundation (WCF).</span><span class="sxs-lookup"><span data-stu-id="c893a-103">This document provides a high-level view of the Windows Communication Foundation (WCF) architecture.</span></span> <span data-ttu-id="c893a-104">Ele destina-se a explicar os principais conceitos e como eles se adaptam entre si.</span><span class="sxs-lookup"><span data-stu-id="c893a-104">It is intended to explain key concepts and how they fit together.</span></span> <span data-ttu-id="c893a-105">Para obter um tutorial sobre como criar a versão mais simples de um serviço e cliente do WCF, consulte [introdução tutorial](getting-started-tutorial.md).</span><span class="sxs-lookup"><span data-stu-id="c893a-105">For a tutorial on creating the simplest version of a WCF service and client, see [Getting Started Tutorial](getting-started-tutorial.md).</span></span> <span data-ttu-id="c893a-106">Para aprender sobre a programação do WCF, consulte [programação básica do WCF](basic-wcf-programming.md).</span><span class="sxs-lookup"><span data-stu-id="c893a-106">To learn WCF programming, see [Basic WCF Programming](basic-wcf-programming.md).</span></span>

## <a name="wcf-fundamentals"></a><span data-ttu-id="c893a-107">Princípios básicos do WCF</span><span class="sxs-lookup"><span data-stu-id="c893a-107">WCF Fundamentals</span></span>

<span data-ttu-id="c893a-108">O WCF é um tempo de execução e um conjunto de APIs para criar sistemas que enviam mensagens entre serviços e clientes.</span><span class="sxs-lookup"><span data-stu-id="c893a-108">WCF is a runtime and a set of APIs for creating systems that send messages between services and clients.</span></span> <span data-ttu-id="c893a-109">A mesma infraestrutura e APIs são usados para criar aplicativos que se comunicam com outros aplicativos no mesmo sistema de computador ou em um sistema que reside em outra empresa e é acessado pela Internet.</span><span class="sxs-lookup"><span data-stu-id="c893a-109">The same infrastructure and APIs are used to create applications that communicate with other applications on the same computer system or on a system that resides in another company and is accessed over the Internet.</span></span>

### <a name="messaging-and-endpoints"></a><span data-ttu-id="c893a-110">Mensagem e pontos de extremidade</span><span class="sxs-lookup"><span data-stu-id="c893a-110">Messaging and Endpoints</span></span>

<span data-ttu-id="c893a-111">O WCF é baseado na noção de comunicação baseada em mensagem, e qualquer coisa que possa ser modelada como uma mensagem (por exemplo, uma solicitação HTTP ou uma mensagem de enfileiramento de mensagens (também conhecida como MSMQ) pode ser representada de forma uniforme no modelo de programação.</span><span class="sxs-lookup"><span data-stu-id="c893a-111">WCF is based on the notion of message-based communication, and anything that can be modeled as a message (for example, an HTTP request or a Message Queuing (also known as MSMQ) message) can be represented in a uniform way in the programming model.</span></span> <span data-ttu-id="c893a-112">Isso permite uma API unificada em mecanismos de transporte diferentes.</span><span class="sxs-lookup"><span data-stu-id="c893a-112">This enables a unified API across different transport mechanisms.</span></span>

<span data-ttu-id="c893a-113">O modelo distingue entre _clientes_, que são aplicativos que iniciam a comunicação e os _Serviços_, que são aplicativos que esperam que os clientes se comuniquem com eles e respondam a essa comunicação.</span><span class="sxs-lookup"><span data-stu-id="c893a-113">The model distinguishes between _clients_, which are applications that initiate communication, and _services_, which are applications that wait for clients to communicate with them and respond to that communication.</span></span> <span data-ttu-id="c893a-114">Um único aplicativo pode atuar como um cliente e um serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-114">A single application can act as both a client and a service.</span></span> <span data-ttu-id="c893a-115">Para obter exemplos, consulte [Serviços duplex](./feature-details/duplex-services.md) e [rede ponto a ponto](./feature-details/peer-to-peer-networking.md).</span><span class="sxs-lookup"><span data-stu-id="c893a-115">For examples, see [Duplex Services](./feature-details/duplex-services.md) and [Peer-to-Peer Networking](./feature-details/peer-to-peer-networking.md).</span></span>

<span data-ttu-id="c893a-116">As mensagens são enviadas entre pontos de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-116">Messages are sent between endpoints.</span></span> <span data-ttu-id="c893a-117">Os _pontos de extremidade_ são locais onde as mensagens são enviadas ou recebidas (ou ambas) e definem todas as informações necessárias para a troca de mensagens.</span><span class="sxs-lookup"><span data-stu-id="c893a-117">_Endpoints_ are places where messages are sent or received (or both), and they define all the information required for the message exchange.</span></span> <span data-ttu-id="c893a-118">Um serviço expõe um ou mais pontos de extremidade do aplicativo (assim como zero ou mais pontos de extremidade de infraestrutura) e o cliente gera um ponto de extremidade que é compatível com um dos pontos de extremidade do serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-118">A service exposes one or more application endpoints (as well as zero or more infrastructure endpoints), and the client generates an endpoint that is compatible with one of the service's endpoints.</span></span>

<span data-ttu-id="c893a-119">Um _ponto de extremidade_ descreve de forma padrão o local em que as mensagens devem ser enviadas, como elas devem ser enviadas e como devem ser as mensagens.</span><span class="sxs-lookup"><span data-stu-id="c893a-119">An _endpoint_ describes in a standard-based way where messages should be sent, how they should be sent, and what the messages should look like.</span></span> <span data-ttu-id="c893a-120">Um serviço pode expor essas informações como metadados que os clientes podem processar para gerar clientes WCF apropriados e _pilhas_de comunicação.</span><span class="sxs-lookup"><span data-stu-id="c893a-120">A service can expose this information as metadata that clients can process to generate appropriate WCF clients and communication _stacks_.</span></span>

### <a name="communication-protocols"></a><span data-ttu-id="c893a-121">Protocolos de comunicação</span><span class="sxs-lookup"><span data-stu-id="c893a-121">Communication Protocols</span></span>

<span data-ttu-id="c893a-122">Um elemento obrigatório da pilha de comunicação é o _protocolo de transporte_.</span><span class="sxs-lookup"><span data-stu-id="c893a-122">One required element of the communication stack is the _transport protocol_.</span></span> <span data-ttu-id="c893a-123">As mensagens podem ser enviados pela Internet e por intranets usando transportes comuns, como HTTP e TCP.</span><span class="sxs-lookup"><span data-stu-id="c893a-123">Messages can be sent over intranets and the Internet using common transports, such as HTTP and TCP.</span></span> <span data-ttu-id="c893a-124">Estão incluídos outros transportes que dão suporte à comunicação com aplicativos de Enfileiramento de Mensagens e nós em uma malha de rede par.</span><span class="sxs-lookup"><span data-stu-id="c893a-124">Other transports are included that support communication with Message Queuing applications and nodes on a Peer Networking mesh.</span></span> <span data-ttu-id="c893a-125">Mais mecanismos de transporte podem ser adicionados usando os pontos de extensão internos do WCF.</span><span class="sxs-lookup"><span data-stu-id="c893a-125">More transport mechanisms can be added using the built-in extension points of WCF.</span></span>

<span data-ttu-id="c893a-126">Outro elemento necessário na pilha de comunicação é a codificação que especifica como qualquer mensagem determinada é formatada.</span><span class="sxs-lookup"><span data-stu-id="c893a-126">Another required element in the communication stack is the encoding that specifies how any given message is formatted.</span></span> <span data-ttu-id="c893a-127">O WCF fornece as seguintes codificações:</span><span class="sxs-lookup"><span data-stu-id="c893a-127">WCF provides the following encodings:</span></span>

- <span data-ttu-id="c893a-128">Codificação de texto, uma codificação interoperável.</span><span class="sxs-lookup"><span data-stu-id="c893a-128">Text encoding, an interoperable encoding.</span></span>

- <span data-ttu-id="c893a-129">Codificação MTOM (Mecanismo de otimização de transmissão de mensagens), que é uma maneira interoperável de enviar com eficiência dados binários não estruturados para e de um serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-129">Message Transmission Optimization Mechanism (MTOM) encoding, which is an interoperable way for efficiently sending unstructured binary data to and from a service.</span></span>

- <span data-ttu-id="c893a-130">Codificação binária para a transferência eficiente.</span><span class="sxs-lookup"><span data-stu-id="c893a-130">Binary encoding for efficient transfer.</span></span>

<span data-ttu-id="c893a-131">Mais mecanismos de codificação (por exemplo, uma codificação de compactação) podem ser adicionados usando os pontos de extensão internos do WCF.</span><span class="sxs-lookup"><span data-stu-id="c893a-131">More encoding mechanisms (for example, a compression encoding) can be added using the built-in extension points of WCF.</span></span>

### <a name="message-patterns"></a><span data-ttu-id="c893a-132">Padrões de mensagem</span><span class="sxs-lookup"><span data-stu-id="c893a-132">Message Patterns</span></span>

<span data-ttu-id="c893a-133">O WCF dá suporte a vários padrões de mensagens, incluindo comunicação de solicitação-resposta, unidirecional e duplex.</span><span class="sxs-lookup"><span data-stu-id="c893a-133">WCF supports several messaging patterns, including request-reply, one-way, and duplex communication.</span></span> <span data-ttu-id="c893a-134">Os transportes diferentes dão suporte a padrões diferentes de mensagem e, portanto, afetam os tipos de interações que eles suportam.</span><span class="sxs-lookup"><span data-stu-id="c893a-134">Different transports support different messaging patterns, and thus affect the types of interactions that they support.</span></span> <span data-ttu-id="c893a-135">As APIs e o tempo de execução do WCF também ajudam você a enviar mensagens de forma segura e confiável.</span><span class="sxs-lookup"><span data-stu-id="c893a-135">The WCF APIs and runtime also help you to send messages securely and reliably.</span></span>

## <a name="wcf-terms"></a><span data-ttu-id="c893a-136">Termos do WCF</span><span class="sxs-lookup"><span data-stu-id="c893a-136">WCF Terms</span></span>

<span data-ttu-id="c893a-137">Outros conceitos e termos usados na documentação do WCF incluem o seguinte:</span><span class="sxs-lookup"><span data-stu-id="c893a-137">Other concepts and terms used in the WCF documentation include the following:</span></span>

<span data-ttu-id="c893a-138">**Message**</span><span class="sxs-lookup"><span data-stu-id="c893a-138">**Message**</span></span>  
 <span data-ttu-id="c893a-139">Uma unidade independente de dados que pode ser formada por várias partes, incluindo um corpo e cabeçalhos.</span><span class="sxs-lookup"><span data-stu-id="c893a-139">A self-contained unit of data that can consist of several parts, including a body and headers.</span></span>

<span data-ttu-id="c893a-140">**Serviço**</span><span class="sxs-lookup"><span data-stu-id="c893a-140">**Service**</span></span>  
 <span data-ttu-id="c893a-141">Uma construção que expõe um ou mais pontos de extremidade, com cada ponto de extremidade expondo uma ou mais operações de serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-141">A construct that exposes one or more endpoints, with each endpoint exposing one or more service operations.</span></span>

<span data-ttu-id="c893a-142">**Ponto de extremidade**</span><span class="sxs-lookup"><span data-stu-id="c893a-142">**Endpoint**</span></span>  
 <span data-ttu-id="c893a-143">Uma construção na qual as mensagens são enviadas ou recebidas (ou ambos).</span><span class="sxs-lookup"><span data-stu-id="c893a-143">A construct at which messages are sent or received (or both).</span></span> <span data-ttu-id="c893a-144">Ele consiste em um local (um endereço) que define onde as mensagens podem ser enviadas, uma especificação do mecanismo de comunicação (uma associação) que descreve como as mensagens devem ser enviadas e uma definição para um conjunto de mensagens que podem ser enviadas ou recebidas (ou ambas) nesse local (um contrato de serviço) que descreve qual mensagem pode ser enviada.</span><span class="sxs-lookup"><span data-stu-id="c893a-144">It comprises a location (an address) that defines where messages can be sent, a specification of the communication mechanism (a binding) that describes how messages should be sent, and a definition for a set of messages that can be sent or received (or both) at that location (a service contract) that describes what message can be sent.</span></span>

<span data-ttu-id="c893a-145">Um serviço WCF é exposto ao mundo como uma coleção de pontos de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-145">A WCF service is exposed to the world as a collection of endpoints.</span></span>

<span data-ttu-id="c893a-146">**Ponto de extremidade do aplicativo**</span><span class="sxs-lookup"><span data-stu-id="c893a-146">**Application endpoint**</span></span>  
 <span data-ttu-id="c893a-147">Um ponto de extremidade exposto pelo aplicativo e que corresponde a um contrato de serviço implementado pelo aplicativo.</span><span class="sxs-lookup"><span data-stu-id="c893a-147">An endpoint exposed by the application and that corresponds to a service contract implemented by the application.</span></span>

<span data-ttu-id="c893a-148">**Ponto de extremidade de infraestrutura**</span><span class="sxs-lookup"><span data-stu-id="c893a-148">**Infrastructure endpoint**</span></span>  
 <span data-ttu-id="c893a-149">Um ponto de extremidade que é exposto pela infraestrutura para facilitar a funcionalidade que é necessária ou fornecida pelo serviço que não está relacionado a um contrato de serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-149">An endpoint that is exposed by the infrastructure to facilitate functionality that is needed or provided by the service that does not relate to a service contract.</span></span> <span data-ttu-id="c893a-150">Por exemplo, um serviço pode ter um ponto de extremidade de infraestrutura que fornece informações de metadados.</span><span class="sxs-lookup"><span data-stu-id="c893a-150">For example, a service might have an infrastructure endpoint that provides metadata information.</span></span>

<span data-ttu-id="c893a-151">**Endereço**</span><span class="sxs-lookup"><span data-stu-id="c893a-151">**Address**</span></span>  
 <span data-ttu-id="c893a-152">Especifica o local onde as mensagens são recebidas.</span><span class="sxs-lookup"><span data-stu-id="c893a-152">Specifies the location where messages are received.</span></span> <span data-ttu-id="c893a-153">É especificado como um URI (Uniform Resource Identifier).</span><span class="sxs-lookup"><span data-stu-id="c893a-153">It is specified as a Uniform Resource Identifier (URI).</span></span> <span data-ttu-id="c893a-154">A parte do esquema URI nomeia o mecanismo de transporte a ser usado para alcançar o endereço, por exemplo, HTTP e TCP.</span><span class="sxs-lookup"><span data-stu-id="c893a-154">The URI schema part names the transport mechanism to use to reach the address, such as HTTP and TCP.</span></span> <span data-ttu-id="c893a-155">A parte hierárquica do URI contém um local exclusivo cujo formato é dependente do mecanismo de transporte.</span><span class="sxs-lookup"><span data-stu-id="c893a-155">The hierarchical part of the URI contains a unique location whose format is dependent on the transport mechanism.</span></span>

<span data-ttu-id="c893a-156">O endereço do ponto de extremidade permite que você crie endereços exclusivos de ponto de extremidade para cada ponto de extremidade em um serviço, ou, sob determinadas condições, para compartilhar um endereço através de pontos de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-156">The endpoint address enables you to create unique endpoint addresses for each endpoint in a service or, under certain conditions, to share an address across endpoints.</span></span> <span data-ttu-id="c893a-157">O exemplo a seguir mostra um endereço usando o protocolo HTTPS com uma porta não padrão:</span><span class="sxs-lookup"><span data-stu-id="c893a-157">The following example shows an address using the HTTPS protocol with a non-default port:</span></span>

```http
HTTPS://cohowinery:8005/ServiceModelSamples/CalculatorService
```

<span data-ttu-id="c893a-158">**Associação**</span><span class="sxs-lookup"><span data-stu-id="c893a-158">**Binding**</span></span>  
 <span data-ttu-id="c893a-159">Define como um ponto de extremidade comunica-se com o mundo.</span><span class="sxs-lookup"><span data-stu-id="c893a-159">Defines how an endpoint communicates to the world.</span></span> <span data-ttu-id="c893a-160">Ele é construído de um conjunto de componentes chamados elementos de associação que “empilham” um sobre o outro para criar a infraestrutura de comunicação.</span><span class="sxs-lookup"><span data-stu-id="c893a-160">It is constructed of a set of components called binding elements that "stack" one on top of the other to create the communication infrastructure.</span></span> <span data-ttu-id="c893a-161">No mínimo, uma associação define o transporte (como HTTP ou TCP) e a codificação que está sendo usada (como texto ou binário).</span><span class="sxs-lookup"><span data-stu-id="c893a-161">At the very least, a binding defines the transport (such as HTTP or TCP) and the encoding being used (such as text or binary).</span></span> <span data-ttu-id="c893a-162">Uma associação pode conter elementos de associação que especificam detalhes como mecanismos de segurança usados para proteger mensagens, ou o padrão de mensagem usado por um ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-162">A binding can contain binding elements that specify details like the security mechanisms used to secure messages, or the message pattern used by an endpoint.</span></span> <span data-ttu-id="c893a-163">Para obter mais informações, consulte [Configurando serviços](configuring-services.md).</span><span class="sxs-lookup"><span data-stu-id="c893a-163">For more information, see [Configuring Services](configuring-services.md).</span></span>

<span data-ttu-id="c893a-164">**Elemento Binding**</span><span class="sxs-lookup"><span data-stu-id="c893a-164">**Binding element**</span></span>  
 <span data-ttu-id="c893a-165">Representa uma parte específica da associação, como um transporte, uma codificação, uma implementação de um protocolo de nível de infraestrutura (como WS-ReliableMessaging), ou qualquer outro componente da pilha de comunicação.</span><span class="sxs-lookup"><span data-stu-id="c893a-165">Represents a particular piece of the binding, such as a transport, an encoding, an implementation of an infrastructure-level protocol (such as WS-ReliableMessaging), or any other component of the communication stack.</span></span>

<span data-ttu-id="c893a-166">**Comportamentos**</span><span class="sxs-lookup"><span data-stu-id="c893a-166">**Behaviors**</span></span>  
 <span data-ttu-id="c893a-167">Um componente que controla vários aspectos de tempo de execução de um serviço, um ponto de extremidade, uma operação específico ou um cliente.</span><span class="sxs-lookup"><span data-stu-id="c893a-167">A component that controls various run-time aspects of a service, an endpoint, a particular operation, or a client.</span></span> <span data-ttu-id="c893a-168">Os comportamentos são agrupados de acordo com o escopo: os comportamentos comuns afetam todos os pontos de extremidade globalmente, os aspectos de serviço afetam somente aspectos relacionados a serviço, os comportamentos de ponto de extremidade afetam somente as propriedades relacionadas a ponto de extremidade e os comportamentos de nível de operação afetam operações específicas.</span><span class="sxs-lookup"><span data-stu-id="c893a-168">Behaviors are grouped according to scope: common behaviors affect all endpoints globally, service behaviors affect only service-related aspects, endpoint behaviors affect only endpoint-related properties, and operation-level behaviors affect particular operations.</span></span> <span data-ttu-id="c893a-169">Por exemplo, um comportamento do serviço é a limitação, que especifica como um serviço reage quando um excesso de mensagens ameaça sobrecarregar seus recursos de manipulação.</span><span class="sxs-lookup"><span data-stu-id="c893a-169">For example, one service behavior is throttling, which specifies how a service reacts when an excess of messages threaten to overwhelm its handling capabilities.</span></span> <span data-ttu-id="c893a-170">Um comportamento de ponto de extremidade, por outro lado, controla somente os aspectos que são relevantes para os pontos de extremidade, por exemplo, como e onde localizar uma credencial de segurança.</span><span class="sxs-lookup"><span data-stu-id="c893a-170">An endpoint behavior, on the other hand, controls only aspects that are relevant to endpoints, such as how and where to find a security credential.</span></span>

<span data-ttu-id="c893a-171">**Associações fornecidas pelo sistema**</span><span class="sxs-lookup"><span data-stu-id="c893a-171">**System-provided bindings**</span></span>  
 <span data-ttu-id="c893a-172">WCF inclui um número o sistema forneceu associações.</span><span class="sxs-lookup"><span data-stu-id="c893a-172">WCF includes a number of system-provided bindings.</span></span> <span data-ttu-id="c893a-173">Essas são coleções de elementos de associação que são otimizados para cenários específicos.</span><span class="sxs-lookup"><span data-stu-id="c893a-173">These are collections of binding elements that are optimized for specific scenarios.</span></span> <span data-ttu-id="c893a-174">Por exemplo, o <xref:System.ServiceModel.WSHttpBinding> foi projetado para interoperabilidade com serviços que implementam várias especificações WS-\*.</span><span class="sxs-lookup"><span data-stu-id="c893a-174">For example, the <xref:System.ServiceModel.WSHttpBinding> is designed for interoperability with services that implement various WS-\* specifications.</span></span> <span data-ttu-id="c893a-175">Essas associações predefinidas economizam tempo apresentando somente essas opções que podem ser corretamente aplicadas ao cenário específico.</span><span class="sxs-lookup"><span data-stu-id="c893a-175">These predefined bindings save time by presenting only those options that can be correctly applied to the specific scenario.</span></span> <span data-ttu-id="c893a-176">Se uma associação predefinida não atender aos requisitos, você poderá criar sua própria associação personalizada.</span><span class="sxs-lookup"><span data-stu-id="c893a-176">If a predefined binding does not meet your requirements, you can create your own custom binding.</span></span>

<span data-ttu-id="c893a-177">**Configuração versus codificação**</span><span class="sxs-lookup"><span data-stu-id="c893a-177">**Configuration versus coding**</span></span>  
 <span data-ttu-id="c893a-178">O controle de um aplicativo pode ser feito por meio de codificação, configuração ou uma combinação de ambos.</span><span class="sxs-lookup"><span data-stu-id="c893a-178">Control of an application can be done either through coding, through configuration, or through a combination of both.</span></span> <span data-ttu-id="c893a-179">A configuração tem a vantagem de permitir que alguém que não seja o desenvolvedor (por exemplo, um administrador de rede) defina o cliente e os parâmetros de serviço após o código ser escrito e sem ter que recompilar.</span><span class="sxs-lookup"><span data-stu-id="c893a-179">Configuration has the advantage of allowing someone other than the developer (for example, a network administrator) to set client and service parameters after the code is written and without having to recompile.</span></span> <span data-ttu-id="c893a-180">A configuração permite que você não apenas defina os valores como endereços de ponto de extremidade, mas também permite um controle adicional para adicionar pontos de extremidade, associações e comportamentos.</span><span class="sxs-lookup"><span data-stu-id="c893a-180">Configuration not only enables you to set values like endpoint addresses, but also allows further control by enabling you to add endpoints, bindings, and behaviors.</span></span> <span data-ttu-id="c893a-181">A codificação permite que o desenvolvedor mantenha o controle restrito sobre todos os componentes de serviço ou cliente, e as configurações feitas podem ser inspecionadas e, se necessário, substituídas pelo código.</span><span class="sxs-lookup"><span data-stu-id="c893a-181">Coding allows the developer to retain strict control over all components of the service or client, and any settings done through the configuration can be inspected and if needed overridden by the code.</span></span>

<span data-ttu-id="c893a-182">**Operação de serviço**</span><span class="sxs-lookup"><span data-stu-id="c893a-182">**Service operation**</span></span>  
 <span data-ttu-id="c893a-183">Um procedimento definido no código de um serviço que implementa a funcionalidade para uma operação.</span><span class="sxs-lookup"><span data-stu-id="c893a-183">A procedure defined in a service's code that implements the functionality for an operation.</span></span> <span data-ttu-id="c893a-184">Esta operação é exposto aos clientes como métodos em um cliente do windows.</span><span class="sxs-lookup"><span data-stu-id="c893a-184">This operation is exposed to clients as methods on a WCF client.</span></span> <span data-ttu-id="c893a-185">O método pode retornar um valor e pode utilizar um número opcional de argumentos, ou não utilizar argumentos, e não retornar nenhuma resposta.</span><span class="sxs-lookup"><span data-stu-id="c893a-185">The method can return a value, and can take an optional number of arguments, or take no arguments, and return no response.</span></span> <span data-ttu-id="c893a-186">Por exemplo, uma operação que funciona como um simples “Hello” pode ser usada como uma notificação da presença de um cliente e iniciar uma série de operações.</span><span class="sxs-lookup"><span data-stu-id="c893a-186">For example, an operation that functions as a simple "Hello" can be used as a notification of a client's presence and to begin a series of operations.</span></span>

<span data-ttu-id="c893a-187">**Contrato de serviço**</span><span class="sxs-lookup"><span data-stu-id="c893a-187">**Service contract**</span></span>  
 <span data-ttu-id="c893a-188">Agrupa várias operações relacionadas em uma única unidade funcional.</span><span class="sxs-lookup"><span data-stu-id="c893a-188">Ties together multiple related operations into a single functional unit.</span></span> <span data-ttu-id="c893a-189">O contrato pode definir configurações de nível de serviço, como o namespace do serviço, um contrato correspondente de retorno de chamada e outras configurações semelhantes.</span><span class="sxs-lookup"><span data-stu-id="c893a-189">The contract can define service-level settings, such as the namespace of the service, a corresponding callback contract, and other such settings.</span></span> <span data-ttu-id="c893a-190">Na maioria dos casos, o contrato é definido criando uma interface na linguagem de programação de sua escolha e aplicando o atributo <xref:System.ServiceModel.ServiceContractAttribute> à interface.</span><span class="sxs-lookup"><span data-stu-id="c893a-190">In most cases, the contract is defined by creating an interface in the programming language of your choice and applying the <xref:System.ServiceModel.ServiceContractAttribute> attribute to the interface.</span></span> <span data-ttu-id="c893a-191">O código real do serviço é gerado implementando a interface.</span><span class="sxs-lookup"><span data-stu-id="c893a-191">The actual service code results by implementing the interface.</span></span>

<span data-ttu-id="c893a-192">**Contrato de operação**</span><span class="sxs-lookup"><span data-stu-id="c893a-192">**Operation contract**</span></span>  
 <span data-ttu-id="c893a-193">Um contrato de operação define os parâmetros e o tipo de retorno de uma operação.</span><span class="sxs-lookup"><span data-stu-id="c893a-193">An operation contract defines the parameters and return type of an operation.</span></span> <span data-ttu-id="c893a-194">Ao criar uma interface que define o contrato de serviço, você significa um contrato de operação aplicando o atributo <xref:System.ServiceModel.OperationContractAttribute> a cada definição do método que faz parte do contrato.</span><span class="sxs-lookup"><span data-stu-id="c893a-194">When creating an interface that defines the service contract, you signify an operation contract by applying the <xref:System.ServiceModel.OperationContractAttribute> attribute to each method definition that is part of the contract.</span></span> <span data-ttu-id="c893a-195">As operações podem ser modeladas como utilizar uma única mensagem e retornar uma única mensagem, ou como utilizar um conjunto de tipos e retornar um tipo.</span><span class="sxs-lookup"><span data-stu-id="c893a-195">The operations can be modeled as taking a single message and returning a single message, or as taking a set of types and returning a type.</span></span> <span data-ttu-id="c893a-196">Nesse último caso, o sistema determinará o formato das mensagens que precisam ser trocadas para essa operação.</span><span class="sxs-lookup"><span data-stu-id="c893a-196">In the latter case, the system will determine the format for the messages that need to be exchanged for that operation.</span></span>

<span data-ttu-id="c893a-197">**Contrato de mensagem**</span><span class="sxs-lookup"><span data-stu-id="c893a-197">**Message contract**</span></span>  
 <span data-ttu-id="c893a-198">Descreve o formato de uma mensagem.</span><span class="sxs-lookup"><span data-stu-id="c893a-198">Describes the format of a message.</span></span> <span data-ttu-id="c893a-199">Por exemplo, isso declara se os elementos da mensagem devem estar presentes em cabeçalhos ou no corpo, qual nível de segurança deve ser aplicado para quais elementos de mensagem e assim por diante.</span><span class="sxs-lookup"><span data-stu-id="c893a-199">For example, it declares whether message elements should go in headers versus the body, what level of security should be applied to what elements of the message, and so on.</span></span>

<span data-ttu-id="c893a-200">**Contrato de falha**</span><span class="sxs-lookup"><span data-stu-id="c893a-200">**Fault contract**</span></span>  
 <span data-ttu-id="c893a-201">Pode estar associado com uma operação de serviço para denotar os erros que podem ser retornados para o chamador.</span><span class="sxs-lookup"><span data-stu-id="c893a-201">Can be associated with a service operation to denote errors that can be returned to the caller.</span></span> <span data-ttu-id="c893a-202">Uma operação pode ter zero ou mais falhas associadas a ela.</span><span class="sxs-lookup"><span data-stu-id="c893a-202">An operation can have zero or more faults associated with it.</span></span> <span data-ttu-id="c893a-203">Esses erros são falhas SOAP que são modeladas como exceções no modelo de programação.</span><span class="sxs-lookup"><span data-stu-id="c893a-203">These errors are SOAP faults that are modeled as exceptions in the programming model.</span></span>

<span data-ttu-id="c893a-204">**Contrato de dados**</span><span class="sxs-lookup"><span data-stu-id="c893a-204">**Data contract**</span></span>  
 <span data-ttu-id="c893a-205">As descrições nos metadados dos tipos de dados que um serviço usa.</span><span class="sxs-lookup"><span data-stu-id="c893a-205">The descriptions in metadata of the data types that a service uses.</span></span> <span data-ttu-id="c893a-206">Isso permite que outros interoperem com o serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-206">This enables others to interoperate with the service.</span></span> <span data-ttu-id="c893a-207">Os tipos de dados podem ser usados em qualquer parte de uma mensagem, por exemplo, como parâmetros ou tipos de retorno.</span><span class="sxs-lookup"><span data-stu-id="c893a-207">The data types can be used in any part of a message, for example, as parameters or return types.</span></span> <span data-ttu-id="c893a-208">Se o serviço estiver usando somente tipos simples, não há necessidade de usar explicitamente contratos de dados.</span><span class="sxs-lookup"><span data-stu-id="c893a-208">If the service is using only simple types, there is no need to explicitly use data contracts.</span></span>

<span data-ttu-id="c893a-209">**Hospedagem**</span><span class="sxs-lookup"><span data-stu-id="c893a-209">**Hosting**</span></span>  
 <span data-ttu-id="c893a-210">Um serviço deve ser hospedado em algum processo.</span><span class="sxs-lookup"><span data-stu-id="c893a-210">A service must be hosted in some process.</span></span> <span data-ttu-id="c893a-211">Um _host_ é um aplicativo que controla o tempo de vida do serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-211">A _host_ is an application that controls the lifetime of the service.</span></span> <span data-ttu-id="c893a-212">Os serviços podem ser auto-hospedados ou gerenciados por um processo de hospedagem existente.</span><span class="sxs-lookup"><span data-stu-id="c893a-212">Services can be self-hosted or managed by an existing hosting process.</span></span>

<span data-ttu-id="c893a-213">**Serviço auto-hospedado**</span><span class="sxs-lookup"><span data-stu-id="c893a-213">**Self-hosted service**</span></span>  
 <span data-ttu-id="c893a-214">Um serviço que é executado dentro de um aplicativo de processo que o desenvolvedor criou.</span><span class="sxs-lookup"><span data-stu-id="c893a-214">A service that runs within a process application that the developer created.</span></span> <span data-ttu-id="c893a-215">O desenvolvedor controla o tempo de vida, define as propriedades do serviço, abre o serviço (que o define em um modo escuta) e fecha o serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-215">The developer controls its lifetime, sets the properties of the service, opens the service (which sets it into a listening mode), and closes the service.</span></span>

<span data-ttu-id="c893a-216">**Processo de hospedagem**</span><span class="sxs-lookup"><span data-stu-id="c893a-216">**Hosting process**</span></span>  
 <span data-ttu-id="c893a-217">Um aplicativo que é criado para hospedar serviços.</span><span class="sxs-lookup"><span data-stu-id="c893a-217">An application that is designed to host services.</span></span> <span data-ttu-id="c893a-218">São eles: IIS (Serviços de Informações da Internet), WAS (Serviços de Ativação do Windows) e Serviços do Windows.</span><span class="sxs-lookup"><span data-stu-id="c893a-218">These include Internet Information Services (IIS), Windows Activation Services (WAS), and Windows Services.</span></span> <span data-ttu-id="c893a-219">Nesses cenários hospedados, o host controla o tempo de vida do serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-219">In these hosted scenarios, the host controls the lifetime of the service.</span></span> <span data-ttu-id="c893a-220">Por exemplo, usando o IIS, você pode configurar um diretório virtual que contém o assembly do serviço e o arquivo de configuração.</span><span class="sxs-lookup"><span data-stu-id="c893a-220">For example, using IIS you can set up a virtual directory that contains the service assembly and configuration file.</span></span> <span data-ttu-id="c893a-221">Quando uma mensagem é recebida, o IIS inicia o serviço e controla o tempo de vida.</span><span class="sxs-lookup"><span data-stu-id="c893a-221">When a message is received, IIS starts the service and controls its lifetime.</span></span>

<span data-ttu-id="c893a-222">**Instanciação**</span><span class="sxs-lookup"><span data-stu-id="c893a-222">**Instancing**</span></span>  
 <span data-ttu-id="c893a-223">Um serviço tem um modelo de instanciação.</span><span class="sxs-lookup"><span data-stu-id="c893a-223">A service has an instancing model.</span></span> <span data-ttu-id="c893a-224">Há três modelos de instanciação: “simples”, no qual um único objeto CLR atende a todos os clientes; “por chamada”, no qual um novo objeto CLR é criado para manipular cada chamada de cliente; e “por sessão” no qual um conjunto de objetos CLR é criado, um para cada sessão separada.</span><span class="sxs-lookup"><span data-stu-id="c893a-224">There are three instancing models: "single," in which a single CLR object services all the clients; "per call," in which a new CLR object is created to handle each client call; and "per session," in which a set of CLR objects is created, one for each separate session.</span></span> <span data-ttu-id="c893a-225">A escolha de um modelo de instanciação depende dos requisitos de aplicativo e o padrão de uso esperado do serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-225">The choice of an instancing model depends on the application requirements and the expected usage pattern of the service.</span></span>

<span data-ttu-id="c893a-226">**Aplicativo cliente**</span><span class="sxs-lookup"><span data-stu-id="c893a-226">**Client application**</span></span>  
 <span data-ttu-id="c893a-227">Um programa que troca mensagens com um ou mais pontos de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-227">A program that exchanges messages with one or more endpoints.</span></span> <span data-ttu-id="c893a-228">O aplicativo cliente começa criando uma instância de um cliente de WCF e chamando métodos de cliente do windows.</span><span class="sxs-lookup"><span data-stu-id="c893a-228">The client application begins by creating an instance of a WCF client and calling methods of the WCF client.</span></span> <span data-ttu-id="c893a-229">É importante observar que um único aplicativo pode ser um cliente e um serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-229">It is important to note that a single application can be both a client and a service.</span></span>

<span data-ttu-id="c893a-230">**Canal**</span><span class="sxs-lookup"><span data-stu-id="c893a-230">**Channel**</span></span>  
 <span data-ttu-id="c893a-231">Uma implementação concreta de um elemento de associação.</span><span class="sxs-lookup"><span data-stu-id="c893a-231">A concrete implementation of a binding element.</span></span> <span data-ttu-id="c893a-232">A associação representa a configuração e o canal é a implementação associada com essa configuração.</span><span class="sxs-lookup"><span data-stu-id="c893a-232">The binding represents the configuration, and the channel is the implementation associated with that configuration.</span></span> <span data-ttu-id="c893a-233">Portanto, há um canal associado com cada elemento de associação.</span><span class="sxs-lookup"><span data-stu-id="c893a-233">Therefore, there is a channel associated with each binding element.</span></span> <span data-ttu-id="c893a-234">O canais empilham-se uns sobre os outros para criar a implementação concreta da associação: a pilha do canal.</span><span class="sxs-lookup"><span data-stu-id="c893a-234">Channels stack on top of each other to create the concrete implementation of the binding: the channel stack.</span></span>

<span data-ttu-id="c893a-235">**Cliente WCF**</span><span class="sxs-lookup"><span data-stu-id="c893a-235">**WCF client**</span></span>  
 <span data-ttu-id="c893a-236">Uma construção de aplicativo cliente que expõe as operações de serviço como métodos (na linguagem de programação .NET Framework de sua escolha, como Visual Basic ou Visual C#).</span><span class="sxs-lookup"><span data-stu-id="c893a-236">A client-application construct that exposes the service operations as methods (in the .NET Framework programming language of your choice, such as Visual Basic or Visual C#).</span></span> <span data-ttu-id="c893a-237">Qualquer aplicativo pode hospedar um cliente de WCF, incluindo um aplicativo que hospeda um serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-237">Any application can host a WCF client, including an application that hosts a service.</span></span> <span data-ttu-id="c893a-238">Portanto, é possível criar um serviço que inclui clientes de WCF de outros serviços.</span><span class="sxs-lookup"><span data-stu-id="c893a-238">Therefore, it is possible to create a service that includes WCF clients of other services.</span></span>

<span data-ttu-id="c893a-239">Um cliente WCF pode ser gerado automaticamente usando a [ferramenta de utilitário de metadados ServiceModel (svcutil. exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) e apontando-o para um serviço em execução que publica metadados.</span><span class="sxs-lookup"><span data-stu-id="c893a-239">A WCF client can be automatically generated by using the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) and pointing it at a running service that publishes metadata.</span></span>

<span data-ttu-id="c893a-240">**Metadados**</span><span class="sxs-lookup"><span data-stu-id="c893a-240">**Metadata**</span></span>  
 <span data-ttu-id="c893a-241">Em um serviço, descreve as características do serviço que uma entidade externa precisa entender para se comunicar com o serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-241">In a service, describes the characteristics of the service that an external entity needs to understand to communicate with the service.</span></span> <span data-ttu-id="c893a-242">Os metadados podem ser consumidos pela [ferramenta de utilitário de metadados ServiceModel (svcutil. exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) para gerar um cliente WCF e acompanhar a configuração que um aplicativo cliente pode usar para interagir com o serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-242">Metadata can be consumed by the [ServiceModel Metadata Utility Tool (Svcutil.exe)](servicemodel-metadata-utility-tool-svcutil-exe.md) to generate a WCF client and accompanying configuration that a client application can use to interact with the service.</span></span>

<span data-ttu-id="c893a-243">Os metadados expostos pelo serviço incluem os documentos XML do esquema, que definem o contrato de dados do serviço, e os documentos WSDL, que descrevem os métodos do serviço.</span><span class="sxs-lookup"><span data-stu-id="c893a-243">The metadata exposed by the service includes XML schema documents, which define the data contract of the service, and WSDL documents, which describe the methods of the service.</span></span>

<span data-ttu-id="c893a-244">Quando ativados, os metadados para o serviço são gerados automaticamente pela inspecionando o serviço e seus pontos de extremidade.</span><span class="sxs-lookup"><span data-stu-id="c893a-244">When enabled, metadata for the service is automatically generated by WCF by inspecting the service and its endpoints.</span></span> <span data-ttu-id="c893a-245">Para publicar metadados de um serviço, você deverá explicitamente ativar o comportamento dos metadados.</span><span class="sxs-lookup"><span data-stu-id="c893a-245">To publish metadata from a service, you must explicitly enable the metadata behavior.</span></span>

<span data-ttu-id="c893a-246">**Security**</span><span class="sxs-lookup"><span data-stu-id="c893a-246">**Security**</span></span>  
 <span data-ttu-id="c893a-247">No WCF, inclui confidencialidade (criptografia de mensagens para evitar a espionagem), integridade (o meio de detecção de violação com a mensagem), autenticação (o meio de validação de servidores e clientes) e autorização (o controle de acesso a recursos).</span><span class="sxs-lookup"><span data-stu-id="c893a-247">In WCF, includes confidentiality (encryption of messages to prevent eavesdropping), integrity (the means for detection of tampering with the message), authentication (the means for validation of servers and clients), and authorization (the control of access to resources).</span></span> <span data-ttu-id="c893a-248">Essas funções são fornecidas aproveitando os mecanismos de segurança existentes, como o TLS sobre HTTP (também conhecido como HTTPS), ou implementando uma ou mais das várias especificações de segurança WS-\*.</span><span class="sxs-lookup"><span data-stu-id="c893a-248">These functions are provided by either leveraging existing security mechanisms, such as TLS over HTTP (also known as HTTPS), or by implementing one or more of the various WS-\* security specifications.</span></span>

<span data-ttu-id="c893a-249">**Modo de segurança de transporte**</span><span class="sxs-lookup"><span data-stu-id="c893a-249">**Transport security mode**</span></span>  
 <span data-ttu-id="c893a-250">Especifica que a confidencialidade, a integridade e a autenticação são fornecidas pelos mecanismos da camada de transporte (como HTTPS).</span><span class="sxs-lookup"><span data-stu-id="c893a-250">Specifies that confidentiality, integrity, and authentication are provided by the transport layer mechanisms (such as HTTPS).</span></span> <span data-ttu-id="c893a-251">Ao usar um transporte como HTTPS, esse modo tem a vantagem de ser eficiente no desempenho, além de bem-compreendido devido à sua predominância na Internet.</span><span class="sxs-lookup"><span data-stu-id="c893a-251">When using a transport like HTTPS, this mode has the advantage of being efficient in its performance, and well understood because of its prevalence on the Internet.</span></span> <span data-ttu-id="c893a-252">A desvantagem é que esse tipo de segurança é aplicado separadamente em cada salto no caminho de comunicação, tornando a comunicação suscetível a um ataque de intermediários.</span><span class="sxs-lookup"><span data-stu-id="c893a-252">The disadvantage is that this kind of security is applied separately on each hop in the communication path, making the communication susceptible to a "man in the middle" attack.</span></span>

<span data-ttu-id="c893a-253">**Modo de segurança da mensagem**</span><span class="sxs-lookup"><span data-stu-id="c893a-253">**Message security mode**</span></span>  
 <span data-ttu-id="c893a-254">Especifica que a segurança é fornecida pela implementação de uma ou mais especificações de segurança, como a especificação chamada [especificação Web Services Security: segurança de mensagem SOAP](https://go.microsoft.com/fwlink/?LinkId=94684).</span><span class="sxs-lookup"><span data-stu-id="c893a-254">Specifies that security is provided by implementing one or more of the security specifications, such as the specification named [Web Services Security: SOAP Message Security](https://go.microsoft.com/fwlink/?LinkId=94684).</span></span> <span data-ttu-id="c893a-255">Cada mensagem contém os mecanismos necessários para fornecer segurança durante seu trânsito e ativar os destinatários para detectar violação e descriptografar as mensagens.</span><span class="sxs-lookup"><span data-stu-id="c893a-255">Each message contains the necessary mechanisms to provide security during its transit, and to enable the receivers to detect tampering and to decrypt the messages.</span></span> <span data-ttu-id="c893a-256">Nesse sentido, a segurança é encapsulada dentro de cada mensagem, fornecendo segurança de ponta a ponta em vários saltos.</span><span class="sxs-lookup"><span data-stu-id="c893a-256">In this sense, the security is encapsulated within every message, providing end-to-end security across multiple hops.</span></span> <span data-ttu-id="c893a-257">Como as informações de segurança se tornam parte da mensagem, também é possível incluir vários tipos de credenciais com a mensagem (elas são chamadas de _declarações_).</span><span class="sxs-lookup"><span data-stu-id="c893a-257">Because security information becomes part of the message, it is also possible to include multiple kinds of credentials with the message (these are referred to as _claims_).</span></span> <span data-ttu-id="c893a-258">Essa abordagem também tem a vantagem de permitir que a mensagem viaje com segurança em qualquer transporte, incluindo vários transportes entre sua origem e o destino.</span><span class="sxs-lookup"><span data-stu-id="c893a-258">This approach also has the advantage of enabling the message to travel securely over any transport, including multiple transports between its origin and destination.</span></span> <span data-ttu-id="c893a-259">A desvantagem dessa abordagem é a complexidade dos mecanismos de criptografia empregados, resultando em implicações de desempenho.</span><span class="sxs-lookup"><span data-stu-id="c893a-259">The disadvantage of this approach is the complexity of the cryptographic mechanisms employed, resulting in performance implications.</span></span>

<span data-ttu-id="c893a-260">**Transporte com o modo de segurança de credencial da mensagem**</span><span class="sxs-lookup"><span data-stu-id="c893a-260">**Transport with message credential security mode**</span></span>  
 <span data-ttu-id="c893a-261">Especifica o uso da camada de transporte para fornecer confidencialidade, autenticação e integridade de mensagens, embora cada uma das mensagens possa conter várias credenciais (reivindicações) exigidas pelos destinatários da mensagem.</span><span class="sxs-lookup"><span data-stu-id="c893a-261">Specifies the use of the transport layer to provide confidentiality, authentication, and integrity of the messages, while each of the messages can contain multiple credentials (claims) required by the receivers of the message.</span></span>

<span data-ttu-id="c893a-262">**WS-\***</span><span class="sxs-lookup"><span data-stu-id="c893a-262">**WS-\***</span></span>  
 <span data-ttu-id="c893a-263">Taquigrafia para o conjunto crescente de especificações de (WS) de serviço Web, como WS- segurança, WS - ReliableMessaging, e assim por diante, que são implementadas em windows.</span><span class="sxs-lookup"><span data-stu-id="c893a-263">Shorthand for the growing set of Web Service (WS) specifications, such as WS-Security, WS-ReliableMessaging, and so on, that are implemented in WCF.</span></span>

## <a name="see-also"></a><span data-ttu-id="c893a-264">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c893a-264">See also</span></span>

- [<span data-ttu-id="c893a-265">O que é o Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="c893a-265">What Is Windows Communication Foundation</span></span>](whats-wcf.md)
- [<span data-ttu-id="c893a-266">Arquitetura do Windows Communication Foundation</span><span class="sxs-lookup"><span data-stu-id="c893a-266">Windows Communication Foundation Architecture</span></span>](architecture.md)
