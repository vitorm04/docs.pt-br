---
title: Cenários assíncronos usando HTTP, TCP ou pipe nomeado
ms.date: 03/30/2017
ms.assetid: a4d62402-43a4-48a4-9ced-220633ebc4ce
ms.openlocfilehash: 6ae96c0aac5010adf37eb78ed57d1549885ece58
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/12/2020
ms.locfileid: "79185779"
---
# <a name="asynchronous-scenarios-using-http-tcp-or-named-pipe"></a><span data-ttu-id="e75b1-102">Cenários assíncronos usando HTTP, TCP ou pipe nomeado</span><span class="sxs-lookup"><span data-stu-id="e75b1-102">Asynchronous Scenarios using HTTP, TCP, or Named-Pipe</span></span>
<span data-ttu-id="e75b1-103">Este tópico descreve as atividades e transferências para diferentes cenários de solicitação/resposta assíncronas, com solicitações multithreaded usando HTTP, TCP ou pipe nomeado.</span><span class="sxs-lookup"><span data-stu-id="e75b1-103">This topic describes the activities and transfers for different asynchronous request/reply scenarios, with multithreaded requests using HTTP, TCP, or named pipe.</span></span>  
  
## <a name="asynchronous-requestreply-without-errors"></a><span data-ttu-id="e75b1-104">Solicitação/Resposta assíncrona sem erros</span><span class="sxs-lookup"><span data-stu-id="e75b1-104">Asynchronous Request/Reply without Errors</span></span>  
 <span data-ttu-id="e75b1-105">Esta seção descreve as atividades e transferências para um cenário assíncrono de solicitação/resposta, com clientes multithreaded.</span><span class="sxs-lookup"><span data-stu-id="e75b1-105">This section describes the activities and transfers for an asynchronous request/reply scenario, with multithreaded clients.</span></span>  
  
 <span data-ttu-id="e75b1-106">A atividade do `beginCall` chamador `endCall` termina quando retorna e retorna.</span><span class="sxs-lookup"><span data-stu-id="e75b1-106">The caller activity terminates when `beginCall` returns, and `endCall` returns.</span></span> <span data-ttu-id="e75b1-107">Se um retorno de chamada for chamado, o retorno de chamada retorna.</span><span class="sxs-lookup"><span data-stu-id="e75b1-107">If a callback is called, the callback returns.</span></span>  
  
 <span data-ttu-id="e75b1-108">A atividade chamada `beginCall` termina `endCall` quando retorna, retorna ou quando o retorno de chamada retorna se for chamado dessa atividade.</span><span class="sxs-lookup"><span data-stu-id="e75b1-108">The called activity terminates when `beginCall` returns, `endCall` returns, or when the callback returns if it was called from that activity.</span></span>  
  
### <a name="asynchronous-client-without-callback"></a><span data-ttu-id="e75b1-109">Cliente assíncrono sem Retorno de Chamada</span><span class="sxs-lookup"><span data-stu-id="e75b1-109">Asynchronous Client without Callback</span></span>  
  
#### <a name="propagation-is-enabled-on-both-sides-using-http"></a><span data-ttu-id="e75b1-110">A propagação é habilitada em ambos os lados, usando HTTP</span><span class="sxs-lookup"><span data-stu-id="e75b1-110">Propagation is Enabled on Both Sides, using HTTP</span></span>  
 ![Cliente assíncrono sem retorno de chamada onde propagaçãoActivity é definido como verdadeiro em ambos os lados.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-client-no-callback.gif)
  
 <span data-ttu-id="e75b1-112">Se `propagateActivity=true`, ProcessMessage indicar para qual atividade processAction será transferida.</span><span class="sxs-lookup"><span data-stu-id="e75b1-112">If `propagateActivity=true`, ProcessMessage indicates which ProcessAction activity to transfer to.</span></span>  
  
 <span data-ttu-id="e75b1-113">Para cenários baseados em HTTP, o ReceiveBytes é invocado na primeira mensagem a ser enviada e existe durante toda a vida útil da solicitação.</span><span class="sxs-lookup"><span data-stu-id="e75b1-113">For HTTP-based scenarios, ReceiveBytes is invoked on the first message to send, and exists for the lifetime of the request.</span></span>  
  
#### <a name="propagation-is-disabled-on-either-sides-using-http"></a><span data-ttu-id="e75b1-114">A propagação é desativada em ambos os lados, usando HTTP</span><span class="sxs-lookup"><span data-stu-id="e75b1-114">Propagation is Disabled on Either Sides, using HTTP</span></span>  
 <span data-ttu-id="e75b1-115">Se `propagateActivity=false` em ambos os lados, processMessage não indicar para qual atividade processAction será transferida.</span><span class="sxs-lookup"><span data-stu-id="e75b1-115">If `propagateActivity=false` on either side, ProcessMessage does not indicate which ProcessAction activity to transfer to.</span></span> <span data-ttu-id="e75b1-116">Portanto, uma nova atividade temporária processAction com um novo ID é invocada.</span><span class="sxs-lookup"><span data-stu-id="e75b1-116">Therefore, a new temporary ProcessAction activity with a new ID is invoked.</span></span> <span data-ttu-id="e75b1-117">Quando a resposta assíncrona é compatível com a solicitação no código ServiceModel, o ID de atividade pode ser recuperado do contexto local.</span><span class="sxs-lookup"><span data-stu-id="e75b1-117">When the asynchronous response is matched to the request in ServiceModel code, the Activity ID can be retrieved from the local context.</span></span> <span data-ttu-id="e75b1-118">A atividade processaction real pode ser transferida para com esse ID.</span><span class="sxs-lookup"><span data-stu-id="e75b1-118">The actual ProcessAction activity can be transferred to with that ID.</span></span>  
  
 ![Cliente assíncrono sem retorno de chamada onde propagaatividade é definido como falso em ambos os lados.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-scenario-propagation-disabled-either-side.gif)  

 <span data-ttu-id="e75b1-120">Para cenários baseados em HTTP, o ReceiveBytes é invocado na primeira mensagem a ser enviada e existe durante toda a vida útil da solicitação.</span><span class="sxs-lookup"><span data-stu-id="e75b1-120">For HTTP-based scenarios, ReceiveBytes is invoked on the first message to send, and exists for the lifetime of the request.</span></span>  
  
 <span data-ttu-id="e75b1-121">Uma atividade process action é criada em um `propagateActivity=false` cliente assíncrono quando no chamador ou no callee, e quando a mensagem de resposta não inclui um cabeçalho de ação.</span><span class="sxs-lookup"><span data-stu-id="e75b1-121">A Process Action activity is created on an asynchronous client when `propagateActivity=false` at the caller or callee, and when the response message does not include an Action header.</span></span>  
  
#### <a name="propagation-is-enabled-on-both-sides-using-tcp-or-named-pipe"></a><span data-ttu-id="e75b1-122">A propagação está ativada em ambos os lados, usando TCP ou Pipe nomeado</span><span class="sxs-lookup"><span data-stu-id="e75b1-122">Propagation is Enabled on Both Sides, using TCP or Named Pipe</span></span>  
 ![Cliente assíncrono sem retorno de chamada onde propagaçãoAAtividade é definida como verdadeira em ambos os lados e chamada pipe/TCP.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-scenario-propagation-enabled-using-tcp.gif)  
  
 <span data-ttu-id="e75b1-124">Para um cenário baseado em Pipe ou TCP, o ReceiveBytes é invocado quando o cliente é aberto e existe durante toda a vida útil da conexão.</span><span class="sxs-lookup"><span data-stu-id="e75b1-124">For a Named-Pipe or TCP-based scenario, ReceiveBytes is invoked when the client is opened, and exists for the lifetime of the connection.</span></span>  
  
 <span data-ttu-id="e75b1-125">Semelhante à primeira imagem, se `propagateActivity=true`, ProcessMessage indica para qual atividade processAction transferir.</span><span class="sxs-lookup"><span data-stu-id="e75b1-125">Similar to the first image, if `propagateActivity=true`, ProcessMessage indicates which ProcessAction activity to transfer to.</span></span>  
  
#### <a name="propagation-is-disabled-on-either-sides-using-tcp-or-named-pipe"></a><span data-ttu-id="e75b1-126">A propagação é desativada em ambos os lados, usando TCP ou Pipe nomeado</span><span class="sxs-lookup"><span data-stu-id="e75b1-126">Propagation is Disabled on Either Sides, using TCP or Named Pipe</span></span>  
 <span data-ttu-id="e75b1-127">Para um cenário baseado em Pipe ou TCP, o ReceiveBytes é invocado quando o cliente é aberto e existe durante toda a vida útil da conexão.</span><span class="sxs-lookup"><span data-stu-id="e75b1-127">For a Named-Pipe or TCP-based scenario, ReceiveBytes is invoked when the client is opened, and exists for the lifetime of the connection.</span></span>  
  
 <span data-ttu-id="e75b1-128">Semelhante à segunda imagem, se `propagateActivity=false` em ambos os lados, processMessage não indica qual atividade processAction transferir.</span><span class="sxs-lookup"><span data-stu-id="e75b1-128">Similar to the second image, if `propagateActivity=false` on either side, ProcessMessage does not indicate which ProcessAction activity to transfer to.</span></span> <span data-ttu-id="e75b1-129">Portanto, uma nova atividade temporária processAction com um novo ID é invocada.</span><span class="sxs-lookup"><span data-stu-id="e75b1-129">Therefore, a new temporary ProcessAction activity with a new ID is invoked.</span></span> <span data-ttu-id="e75b1-130">Quando a resposta assíncrona é compatível com a solicitação no código ServiceModel, o ID de atividade pode ser recuperado do contexto local.</span><span class="sxs-lookup"><span data-stu-id="e75b1-130">When the asynchronous response is matched to the request in ServiceModel code, the Activity ID can be retrieved from the local context.</span></span> <span data-ttu-id="e75b1-131">A atividade processaction real pode ser transferida para com esse ID.</span><span class="sxs-lookup"><span data-stu-id="e75b1-131">The actual ProcessAction activity can be transferred to with that ID.</span></span>  
  
 ![Cliente assíncrono sem retorno de chamada onde propagaçãoActivity é definido como falso em ambos os lados e chamado pipe/TCP.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-scenario-propagation-disabled-using-tcp.gif)  

### <a name="asynchronous-client-with-callback"></a><span data-ttu-id="e75b1-133">Cliente assíncrono com Callback</span><span class="sxs-lookup"><span data-stu-id="e75b1-133">Asynchronous client with Callback</span></span>  
 <span data-ttu-id="e75b1-134">Este cenário adiciona atividades G e A', para o retorno de chamada e `endCall`, e suas transferências para dentro/para fora.</span><span class="sxs-lookup"><span data-stu-id="e75b1-134">This scenario adds activities G and A’, for the callback and `endCall`, and their transfers in/out.</span></span>  
  
 <span data-ttu-id="e75b1-135">Esta seção só `propagateActivity` = `true`demonstra o uso de HTTP com .</span><span class="sxs-lookup"><span data-stu-id="e75b1-135">This section only demonstrates using HTTP with `propagateActivity`=`true`.</span></span> <span data-ttu-id="e75b1-136">No entanto, as atividades adicionais e transferências `propagateActivity` = `false`também se aplicam aos outros casos (isto é, usando TCP ou Named-Pipe).</span><span class="sxs-lookup"><span data-stu-id="e75b1-136">However, the additional activities and transfers also apply to the other cases (that is, `propagateActivity`=`false`, using TCP or Named-Pipe).</span></span>  
  
 <span data-ttu-id="e75b1-137">O retorno de chamada cria uma nova atividade (G) quando o cliente liga para o código do usuário para notificar que os resultados estão prontos.</span><span class="sxs-lookup"><span data-stu-id="e75b1-137">The callback creates a new activity (G) when the client calls user code to notify that results are ready.</span></span> <span data-ttu-id="e75b1-138">Em seguida, `endCall` o código do usuário chama dentro do retorno de chamada (como mostrado na Figura 5) ou fora do retorno de chamada (Figura 6).</span><span class="sxs-lookup"><span data-stu-id="e75b1-138">User code then calls `endCall` within the callback (as shown in Figure 5) or outside the callback (Figure 6).</span></span> <span data-ttu-id="e75b1-139">Como não se sabe `endCall` de qual atividade do usuário `A’`está sendo chamada, essa atividade é rotulada .</span><span class="sxs-lookup"><span data-stu-id="e75b1-139">Because it is not known which user activity `endCall` is being called from, this activity is labeled `A’`.</span></span> <span data-ttu-id="e75b1-140">É possível que A' possa ser idêntico ou diferente de A.</span><span class="sxs-lookup"><span data-stu-id="e75b1-140">It is possible that A’ can be identical to or different from A.</span></span>  
  
 ![Mostra um cliente assíncrono com retorno de chamada, chamada final em retorno de chamada.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-client-callback-endcall-in-callback.gif)  

 ![Mostra um cliente assíncrono com retorno de chamada, chamada de chamada externa.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-client-callback-endcall-outside-callback.gif)  

### <a name="asynchronous-server-with-callback"></a><span data-ttu-id="e75b1-143">Servidor assíncrono com retorno de chamada</span><span class="sxs-lookup"><span data-stu-id="e75b1-143">Asynchronous Server with Callback</span></span>  
 ![Mostra um servidor assíncrono com retorno de chamada.](./media/asynchronous-scenarios-using-http-tcp-or-named-pipe/asynchronous-server-callback.gif)  

 <span data-ttu-id="e75b1-145">A pilha de canais chama de volta o cliente no Message Receive: os rastreamentos para esse processamento são emitidos na própria atividade ProcessRequest.</span><span class="sxs-lookup"><span data-stu-id="e75b1-145">The channel stack calls back the client on Message Receive: traces for this processing are emitted in the ProcessRequest activity itself.</span></span>  
  
## <a name="asynchronous-requestreply-with-errors"></a><span data-ttu-id="e75b1-146">Solicitação/resposta assíncrona com erros</span><span class="sxs-lookup"><span data-stu-id="e75b1-146">Asynchronous Request/Reply with Errors</span></span>  
 <span data-ttu-id="e75b1-147">Erros de mensagem `endCall`de falha são recebidos durante .</span><span class="sxs-lookup"><span data-stu-id="e75b1-147">Fault message errors are received during `endCall`.</span></span> <span data-ttu-id="e75b1-148">Caso contrário, as atividades e transferências são semelhantes aos cenários anteriores.</span><span class="sxs-lookup"><span data-stu-id="e75b1-148">Otherwise, activities and transfers are similar to previous scenarios.</span></span>  
  
## <a name="asynchronous-one-way-with-or-without-errors"></a><span data-ttu-id="e75b1-149">Caminho síncrono unidirecional com ou sem erros</span><span class="sxs-lookup"><span data-stu-id="e75b1-149">Asynchronous One-Way with or without Errors</span></span>  
 <span data-ttu-id="e75b1-150">Nenhuma resposta ou falha é devolvida ao cliente.</span><span class="sxs-lookup"><span data-stu-id="e75b1-150">No response or fault is returned to the client.</span></span>
