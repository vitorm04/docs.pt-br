---
title: Atividades de fluxo de controle em WF
description: Este artigo resume as .NET Framework atividades 4.6.1 para controlar o fluxo de execução em um fluxo de trabalho.
ms.date: 03/30/2017
ms.assetid: 6892885b-f7c5-4aea-8f5e-28863fb4ae75
ms.openlocfilehash: 18ff982d3f215e3fd46108eb2411f3d1a5ab9745
ms.sourcegitcommit: 9a4488a3625866335e83a20da5e9c5286b1f034c
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 05/15/2020
ms.locfileid: "83420065"
---
# <a name="control-flow-activities-in-wf"></a><span data-ttu-id="017ea-103">Atividades de fluxo de controle em WF</span><span class="sxs-lookup"><span data-stu-id="017ea-103">Control Flow Activities in WF</span></span>
<span data-ttu-id="017ea-104">[!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] fornece várias atividades para o fluxo de controle de execução dentro de um fluxo de trabalho.</span><span class="sxs-lookup"><span data-stu-id="017ea-104">The [!INCLUDE[netfx_current_long](../../../includes/netfx-current-long-md.md)] provides several activities for controlling flow of execution within a workflow.</span></span> <span data-ttu-id="017ea-105">Algumas dessas atividades (como `Switch` e `If` ) implementam estruturas de controle de fluxo semelhantes àquelas em ambientes de programação, como o Visual C#, enquanto outras (como `Pick` ) modelam novas estruturas de programação.</span><span class="sxs-lookup"><span data-stu-id="017ea-105">Some of these activities (such as `Switch` and `If`) implement flow control structures similar to those in programming environments such as Visual C#, while others (such as `Pick`) model new programming structures.</span></span>  
  
 <span data-ttu-id="017ea-106">Observe que quando atividades como `Parallel` e atividades filhos de cronograma de atividades de `ParallelForEach` várias para a execução simultaneamente, somente um único segmento é usado para um fluxo de trabalho.</span><span class="sxs-lookup"><span data-stu-id="017ea-106">Note that while activities such as the `Parallel` and `ParallelForEach` activities schedule multiple child activities for execution simultaneously, only a single thread is used for a workflow.</span></span> <span data-ttu-id="017ea-107">Cada atividade filho dessas atividades executa seqüencialmente e as atividades sucessivas não executam até atividades anteriores completa ou vão ociosa.</span><span class="sxs-lookup"><span data-stu-id="017ea-107">Each child activity of these activities executes sequentially and successive activities do not execute until previous activities either complete or go idle.</span></span> <span data-ttu-id="017ea-108">Como resultado, essas atividades são mais úteis para aplicativos que potencialmente em vários bloquear atividades deve executar em uma forma intercalada.</span><span class="sxs-lookup"><span data-stu-id="017ea-108">As a result, these activities are most useful for applications in which several potentially blocking activities must execute in an interleaved fashion.</span></span> <span data-ttu-id="017ea-109">Se nenhuma das atividades filhos dessas atividades vão ociosa, uma atividade de `Parallel` apenas executa como uma atividade de `Sequence` , e uma atividade de `ParallelForEach` apenas executa como uma atividade de `ForEach` .</span><span class="sxs-lookup"><span data-stu-id="017ea-109">If none of the child activities of these activities go idle, a `Parallel` activity executes just like a `Sequence` activity, and a `ParallelForEach` activity executes just like a `ForEach` activity.</span></span> <span data-ttu-id="017ea-110">Se, no entanto, as atividades assíncronos (como atividades que derivam de <xref:System.Activities.AsyncCodeActivity>) ou as atividades de mensagem são usadas, o controle irá passar a ramificação seguir quando as espera por filhos de atividade para que a mensagem a ser recebidos ou seu trabalho assíncrono está concluído.</span><span class="sxs-lookup"><span data-stu-id="017ea-110">If, however, asynchronous activities (such as activities that derive from <xref:System.Activities.AsyncCodeActivity>) or messaging activities are used, control will pass to the next branch while the child activity waits for its message to be received or its asynchronous work to be completed.</span></span>  
  
## <a name="flow-control-activities"></a><span data-ttu-id="017ea-111">Atividades de controle de fluxo</span><span class="sxs-lookup"><span data-stu-id="017ea-111">Flow control activities</span></span>  
  
|<span data-ttu-id="017ea-112">Atividade</span><span class="sxs-lookup"><span data-stu-id="017ea-112">Activity</span></span>|<span data-ttu-id="017ea-113">Descrição</span><span class="sxs-lookup"><span data-stu-id="017ea-113">Description</span></span>|  
|--------------|-----------------|  
|<xref:System.Activities.Statements.DoWhile>|<span data-ttu-id="017ea-114">Executa as atividades contidas uma vez e continua a fazer isso quando a condição é `true`.</span><span class="sxs-lookup"><span data-stu-id="017ea-114">Executes the contained activities once and continues to do so while a condition is `true`.</span></span>|  
|<xref:System.Activities.Statements.ForEach%601>|<span data-ttu-id="017ea-115">Executa uma declaração inserido em ordem para cada elemento em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="017ea-115">Executes an embedded statement in sequence for each element in a collection.</span></span> <span data-ttu-id="017ea-116"><xref:System.Activities.Statements.ForEach%601> é semelhante a palavra-chave `foreach`, mas é implementado como uma atividade em vez de uma declaração de idioma.</span><span class="sxs-lookup"><span data-stu-id="017ea-116"><xref:System.Activities.Statements.ForEach%601> is similar to the keyword `foreach`, but is implemented as an activity rather than a language statement.</span></span>|  
|<xref:System.Activities.Statements.If>|<span data-ttu-id="017ea-117">Executes continha atividades se uma condição é `true`, e pode executar as atividades contidas na propriedade de <xref:System.Activities.Statements.If.Else%2A> se a condição for `false`.</span><span class="sxs-lookup"><span data-stu-id="017ea-117">Executes contained activities if a condition is `true`, and can execute activities contained in the <xref:System.Activities.Statements.If.Else%2A> property if the condition is `false`.</span></span>|  
|<xref:System.Activities.Statements.Parallel>|<span data-ttu-id="017ea-118">Executes continha atividades paralelamente.</span><span class="sxs-lookup"><span data-stu-id="017ea-118">Executes contained activities in parallel.</span></span>|  
|<xref:System.Activities.Statements.ParallelForEach%601>|<span data-ttu-id="017ea-119">Executa uma declaração inserido paralelamente para cada elemento em uma coleção.</span><span class="sxs-lookup"><span data-stu-id="017ea-119">Executes an embedded statement in parallel for each element in a collection.</span></span>|  
|<xref:System.Activities.Statements.Pick>|<span data-ttu-id="017ea-120">Fornece etapas de eventos com base modelagem de fluxo de controle.</span><span class="sxs-lookup"><span data-stu-id="017ea-120">Provides event-based control flow modeling.</span></span>|  
|<xref:System.Activities.Statements.PickBranch>|<span data-ttu-id="017ea-121">Representa um caminho potencial de execução em uma atividade de <xref:System.Activities.Statements.Pick> .</span><span class="sxs-lookup"><span data-stu-id="017ea-121">Represents a potential path of execution in a <xref:System.Activities.Statements.Pick> activity.</span></span>|  
|<xref:System.Activities.Statements.Sequence>|<span data-ttu-id="017ea-122">Executes continha atividades em ordem.</span><span class="sxs-lookup"><span data-stu-id="017ea-122">Executes contained activities in sequence.</span></span>|  
|<xref:System.Activities.Statements.Switch%601>|<span data-ttu-id="017ea-123">Seleciona uma opção de um número de atividades para executar, com base no valor de uma expressão fornecida.</span><span class="sxs-lookup"><span data-stu-id="017ea-123">Selects one choice from a number of activities to execute, based on the value of a given expression.</span></span>|  
|<xref:System.Activities.Statements.While>|<span data-ttu-id="017ea-124">Executes continha atividades enquanto uma condição for `true`.</span><span class="sxs-lookup"><span data-stu-id="017ea-124">Executes contained activities while a condition is `true`.</span></span>|
