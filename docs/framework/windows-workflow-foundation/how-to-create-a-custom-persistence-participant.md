---
title: 'Como: criar um participante de persistência personalizado'
ms.date: 03/30/2017
ms.assetid: 1d9cc47a-8966-4286-94d5-4221403d9c06
ms.openlocfilehash: 47283375b618422d91a6279ee9049fae469f540a
ms.sourcegitcommit: 005980b14629dfc193ff6cdc040800bc75e0a5a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 09/14/2019
ms.locfileid: "70989674"
---
# <a name="how-to-create-a-custom-persistence-participant"></a><span data-ttu-id="e86ae-102">Como: criar um participante de persistência personalizado</span><span class="sxs-lookup"><span data-stu-id="e86ae-102">How to: Create a Custom Persistence Participant</span></span>
<span data-ttu-id="e86ae-103">O seguinte procedimento tem as etapas para criar um participante de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-103">The following procedure has steps to create a persistence participant.</span></span> <span data-ttu-id="e86ae-104">Consulte o tópico [participando da persistência](https://go.microsoft.com/fwlink/?LinkID=177735) e a [extensibilidade de armazenamento](store-extensibility.md) para obter exemplos de implementações de participantes de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-104">See the [Participating in Persistence](https://go.microsoft.com/fwlink/?LinkID=177735) sample and [Store Extensibility](store-extensibility.md) topic for sample implementations of persistence participants.</span></span>  
  
1. <span data-ttu-id="e86ae-105">Crie uma classe que deriva de <xref:System.Activities.Persistence.PersistenceParticipant> ou classe de <xref:System.Activities.Persistence.PersistenceIOParticipant> .</span><span class="sxs-lookup"><span data-stu-id="e86ae-105">Create a class deriving from the <xref:System.Activities.Persistence.PersistenceParticipant> or the <xref:System.Activities.Persistence.PersistenceIOParticipant> class.</span></span> <span data-ttu-id="e86ae-106">A classe PersistenceIOParticipant oferece os mesmos pontos de extensibilidade que a classe PersistenceParticipant, além de ser capaz de participar de operações de e/s.</span><span class="sxs-lookup"><span data-stu-id="e86ae-106">The PersistenceIOParticipant class offers the same extensibility points as the PersistenceParticipant class in addition to being able to participate in I/O operations.</span></span> <span data-ttu-id="e86ae-107">Execute uma ou mais das seguintes etapas.</span><span class="sxs-lookup"><span data-stu-id="e86ae-107">Follow one or more of the following steps.</span></span>  
  
2. <span data-ttu-id="e86ae-108">Implementar o método de <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> .</span><span class="sxs-lookup"><span data-stu-id="e86ae-108">Implement the <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> method.</span></span> <span data-ttu-id="e86ae-109">O método **CollectValues** tem dois parâmetros de dicionário, um para armazenar valores de leitura/gravação e o outro para armazenar valores somente gravação (usados posteriormente em consultas).</span><span class="sxs-lookup"><span data-stu-id="e86ae-109">The **CollectValues** method has two dictionary parameters, one for storing read/write values and the other one for storing write-only values (used later in queries).</span></span> <span data-ttu-id="e86ae-110">Nesse método, você deve preencher esses dicionários com dados que são específicos para um participante de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-110">In this method, you should populate these dictionaries with data that is specific to a persistence participant.</span></span> <span data-ttu-id="e86ae-111">Cada dicionário contém o nome do valor como a chave e o valor próprio como um objeto de <xref:System.Runtime.DurableInstancing.InstanceValue> .</span><span class="sxs-lookup"><span data-stu-id="e86ae-111">Each dictionary contains the name of the value as the key and the value itself as an <xref:System.Runtime.DurableInstancing.InstanceValue> object.</span></span>  
  
    <span data-ttu-id="e86ae-112">Os valores no dicionário readWriteValues são empacotados como objetos **InstanceValue** .</span><span class="sxs-lookup"><span data-stu-id="e86ae-112">The values in the readWriteValues dictionary are packaged as **InstanceValue** objects.</span></span> <span data-ttu-id="e86ae-113">Os valores no dicionário somente gravação são empacotados como objetos **InstanceValue** com InstanceValueOptions. optional e InstanceValueOption. WriteOnly definido.</span><span class="sxs-lookup"><span data-stu-id="e86ae-113">The values in the write-only dictionary are packaged as **InstanceValue** objects with InstanceValueOptions.Optional and InstanceValueOption.WriteOnly set.</span></span> <span data-ttu-id="e86ae-114">Cada **InstanceValue** fornecida pelas implementações de **CollectValues** em todos os participantes de persistência deve ter um nome exclusivo.</span><span class="sxs-lookup"><span data-stu-id="e86ae-114">Each **InstanceValue** provided by the **CollectValues** implementations across all persistence participants must have a unique name.</span></span>
  
    ```csharp  
    protected virtual void CollectValues(out IDictionary<XName,Object> readWriteValues, out IDictionary<XName,Object> writeOnlyValues)
    {
    }
    ```  
  
3. <span data-ttu-id="e86ae-115">Implementar o método de <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> .</span><span class="sxs-lookup"><span data-stu-id="e86ae-115">Implement the <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> method.</span></span> <span data-ttu-id="e86ae-116">O método **MapValues** usa dois parâmetros semelhantes aos parâmetros que o método **CollectValues** recebe.</span><span class="sxs-lookup"><span data-stu-id="e86ae-116">The **MapValues** method takes two parameters that are similar to the parameters that the **CollectValues** method receives.</span></span> <span data-ttu-id="e86ae-117">Todos os valores coletados no estágio **CollectValues** são passados por meio desses parâmetros de dicionário.</span><span class="sxs-lookup"><span data-stu-id="e86ae-117">All the values collected in the **CollectValues** stage are passed through these dictionary parameters.</span></span> <span data-ttu-id="e86ae-118">Os novos valores adicionados pelo estágio **MapValues** são adicionados aos valores somente gravação.</span><span class="sxs-lookup"><span data-stu-id="e86ae-118">The new values added by the **MapValues** stage are added to the write-only values.</span></span>  <span data-ttu-id="e86ae-119">O dicionário somente gravação é usado para fornecer dados para uma fonte externa associada não diretamente com os valores da instância.</span><span class="sxs-lookup"><span data-stu-id="e86ae-119">The write-only dictionary is used to provide data to an external source not directly associated with the instance values.</span></span> <span data-ttu-id="e86ae-120">Cada valor fornecido pelas implementações do método **MapValues** em todos os participantes da persistência deve ter um nome exclusivo.</span><span class="sxs-lookup"><span data-stu-id="e86ae-120">Each value provided by implementations of the **MapValues** method across all persistence participants must have a unique name.</span></span>  
  
    ```csharp  
    protected virtual IDictionary<XName,Object> MapValues(IDictionary<XName,Object> readWriteValues,IDictionary<XName,Object> writeOnlyValues)
    {
    }
    ```  
  
     <span data-ttu-id="e86ae-121">O método de <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> fornece funcionalidade que o <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> contrário, que permite uma dependência em outro valor fornecido por outro participante de persistência que não é processado pelo <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> ainda.</span><span class="sxs-lookup"><span data-stu-id="e86ae-121">The <xref:System.Activities.Persistence.PersistenceParticipant.MapValues%2A> method provides functionality that <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> does not, in that it allows for a dependency on another value provided by another persistence participant that hasn’t been processed by <xref:System.Activities.Persistence.PersistenceParticipant.CollectValues%2A> yet.</span></span>  
  
4. <span data-ttu-id="e86ae-122">Implemente o método **PublishValues** .</span><span class="sxs-lookup"><span data-stu-id="e86ae-122">Implement the **PublishValues** method.</span></span> <span data-ttu-id="e86ae-123">O método **PublishValues** recebe um dicionário contendo todos os valores carregados do repositório de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-123">The **PublishValues** method receives a dictionary containing all the values loaded from the persistence store.</span></span>  
  
    ```csharp  
    protected virtual void PublishValues(IDictionary<XName,Object> readWriteValues)
    {
    }
    ```  
  
5. <span data-ttu-id="e86ae-124">Implemente o método **BeginOnSave** se o participante for um participante de e/s de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-124">Implement the **BeginOnSave** method if the participant is a persistence I/O participant.</span></span> <span data-ttu-id="e86ae-125">Este método é chamado durante uma operação de salvar.</span><span class="sxs-lookup"><span data-stu-id="e86ae-125">This method is called during a Save operation.</span></span> <span data-ttu-id="e86ae-126">Nesse método, você deve executar suplemento de e/s para as instâncias de fluxo de trabalho persistentes (salvando).</span><span class="sxs-lookup"><span data-stu-id="e86ae-126">In this method, you should perform I/O adjunct to the persisting (saving) workflow instances.</span></span>  <span data-ttu-id="e86ae-127">Se o host está usando uma transação para o comando correspondente de persistência, a mesma transação é fornecida em Transaction.Current.</span><span class="sxs-lookup"><span data-stu-id="e86ae-127">If the host is using a transaction for the corresponding persistence command, the same transaction is provided in Transaction.Current.</span></span>  <span data-ttu-id="e86ae-128">Além disso, PersistenceIOParticipants pode anunciar um requisito transacional de consistência, nesse caso o host cria uma transação para o episódio de persistência se não seria usado de outra maneira.</span><span class="sxs-lookup"><span data-stu-id="e86ae-128">Additionally, PersistenceIOParticipants may advertise a transactional consistency requirement, in which case the host creates a transaction for the persistence episode if one would not otherwise be used.</span></span>  
  
    ```csharp  
    protected virtual IAsyncResult BeginOnSave(IDictionary<XName,Object> readWriteValues, IDictionary<XName,Object> writeOnlyValues, TimeSpan timeout, AsyncCallback callback, Object state)
    {
    }
    ```  
  
6. <span data-ttu-id="e86ae-129">Implemente o método **BeginOnLoad** se o participante for um participante de e/s de persistência.</span><span class="sxs-lookup"><span data-stu-id="e86ae-129">Implement the **BeginOnLoad** method if the participant is a persistence I/O participant.</span></span> <span data-ttu-id="e86ae-130">Este método é chamado durante uma operação de carregamento.</span><span class="sxs-lookup"><span data-stu-id="e86ae-130">This method is called during a Load operation.</span></span> <span data-ttu-id="e86ae-131">Nesse método, você deve executar suplemento de e/s para o carregamento de instâncias de fluxo de trabalho.</span><span class="sxs-lookup"><span data-stu-id="e86ae-131">In this method, you should perform I/O adjunct to the loading of workflow instances.</span></span> <span data-ttu-id="e86ae-132">Se o host está usando uma transação para o comando correspondente de persistência, a mesma transação é fornecida em Transaction.Current.</span><span class="sxs-lookup"><span data-stu-id="e86ae-132">If the host is using a transaction for the corresponding persistence command, the same transaction is provided in Transaction.Current.</span></span> <span data-ttu-id="e86ae-133">Além disso, os participantes de e/s de persistência podem anunciar um requisito de consistência transacional; nesse caso, o host criará uma transação para o episódio de persistência se, de outra forma, não for usado.</span><span class="sxs-lookup"><span data-stu-id="e86ae-133">Additionally, Persistence I/O participants may advertise a transactional consistency requirement, in which case the host creates a transaction for the persistence episode if one would not otherwise be used.</span></span>  
  
    ```csharp  
    protected virtual IAsyncResult BeginOnLoad(IDictionary<XName,Object> readWriteValues, TimeSpan timeout, AsyncCallback callback, Object state)
    {
    }
    ```
