---
title: Operadores de consulta padrão em consultas LINQ to Entities
ms.date: 08/21/2018
ms.assetid: 7fa55a9b-6219-473d-b1e5-2884a32dcdff
ms.openlocfilehash: 5c666bad40d0e433ee5f8d2b1155e881d7042a85
ms.sourcegitcommit: 9b552addadfb57fab0b9e7852ed4f1f1b8a42f8e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/23/2019
ms.locfileid: "61797716"
---
# <a name="standard-query-operators-in-linq-to-entities-queries"></a><span data-ttu-id="d5c4d-102">Operadores de consulta padrão em consultas LINQ to Entities</span><span class="sxs-lookup"><span data-stu-id="d5c4d-102">Standard Query Operators in LINQ to Entities Queries</span></span>
<span data-ttu-id="d5c4d-103">Em uma consulta, você especifica as informações que deseja recuperar da fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-103">In a query, you specify the information that you want to retrieve from the data source.</span></span> <span data-ttu-id="d5c4d-104">Uma consulta também pode especificar como essas informações devem ser classificadas, agrupadas e moldadas antes de serem retornadas.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-104">A query can also specify how that information should be sorted, grouped, and shaped before it is returned.</span></span> <span data-ttu-id="d5c4d-105">A sintaxe de consulta LINQ fornece um conjunto de métodos de consulta padrão que você pode usar em uma consulta.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-105">LINQ provides a set of standard query methods that you can use in a query.</span></span> <span data-ttu-id="d5c4d-106">A maioria desses métodos opera em sequências; Nesse contexto, uma sequência é um objeto cujo tipo implementa a <xref:System.Collections.Generic.IEnumerable%601> interface ou o <xref:System.Linq.IQueryable%601> interface.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-106">Most of these methods operate on sequences; in this context, a sequence is an object whose type implements the <xref:System.Collections.Generic.IEnumerable%601> interface or the <xref:System.Linq.IQueryable%601> interface.</span></span> <span data-ttu-id="d5c4d-107">A funcionalidade de consulta de operadores de consulta padrão inclui filtragem, projeção, agregação, classificação, agrupamento, paginação e muito mais.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-107">The standard query operators query functionality includes filtering, projection, aggregation, sorting, grouping, paging, and more.</span></span> <span data-ttu-id="d5c4d-108">Alguns dos operadores de consulta padrão mais usados possuem sintaxe de palavra-chave dedicada para que possam ser chamados por meio da sintaxe da expressão de consulta.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-108">Some of the more frequently used standard query operators have dedicated keyword syntax so that they can be called by using query expression syntax.</span></span> <span data-ttu-id="d5c4d-109">Uma expressão de consulta é uma maneira diferente e mais legível de expressar uma consulta do que o equivalente baseado em método.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-109">A query expression is a different, more readable way to express a query than the method-based equivalent.</span></span> <span data-ttu-id="d5c4d-110">As cláusulas de expressão de consulta são convertidas em chamadas para os métodos de consulta em tempo de compilação.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-110">Query expression clauses are translated into calls to the query methods at compile time.</span></span> <span data-ttu-id="d5c4d-111">Para obter uma lista de operadores de consulta padrão que têm cláusulas de expressão de consulta equivalente, consulte [visão geral de operadores de consulta padrão](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120)).</span><span class="sxs-lookup"><span data-stu-id="d5c4d-111">For a list of standard query operators that have equivalent query expression clauses, see [Standard Query Operators Overview](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120)).</span></span>  
  
 <span data-ttu-id="d5c4d-112">Nem todos os operadores de consulta padrão têm suporte em consultas [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)].</span><span class="sxs-lookup"><span data-stu-id="d5c4d-112">Not all of the standard query operators are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] queries.</span></span> <span data-ttu-id="d5c4d-113">Para obter mais informações, consulte [com suporte e os métodos LINQ (LINQ to Entities)](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="d5c4d-113">For more information, see [Supported and Unsupported LINQ Methods (LINQ to Entities)](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md).</span></span> <span data-ttu-id="d5c4d-114">Este tópico fornece informações sobre os operadores de consulta padrão que são específicas para [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)].</span><span class="sxs-lookup"><span data-stu-id="d5c4d-114">This topic provides information about the standard query operators that is specific to [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)].</span></span> <span data-ttu-id="d5c4d-115">Para obter mais informações sobre problemas conhecidos [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] consultas, consulte [problemas conhecidos e considerações no LINQ to Entities](../../../../../../docs/framework/data/adonet/ef/language-reference/known-issues-and-considerations-in-linq-to-entities.md).</span><span class="sxs-lookup"><span data-stu-id="d5c4d-115">For more information about known issues in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)] queries, see [Known Issues and Considerations in LINQ to Entities](../../../../../../docs/framework/data/adonet/ef/language-reference/known-issues-and-considerations-in-linq-to-entities.md).</span></span>  
  
## <a name="projection-and-filtering-methods"></a><span data-ttu-id="d5c4d-116">Métodos de projeção e filtragem</span><span class="sxs-lookup"><span data-stu-id="d5c4d-116">Projection and Filtering Methods</span></span>  
 <span data-ttu-id="d5c4d-117">*Projeção* refere-se a transformação de elementos de um conjunto de resultados em um formulário desejado.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-117">*Projection* refers to transforming the elements of a result set into a desired form.</span></span> <span data-ttu-id="d5c4d-118">Por exemplo, você pode projetar um subconjunto das propriedades de que precisa de cada objeto do conjunto de resultados, projetar uma propriedade e efetuar um cálculo matemático nela ou projetar o objeto inteiro do conjunto de resultados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-118">For example, you can project a subset of the properties you need from each object in the result set, you can project a property and perform a mathematical calculation on it, or you can project the entire object from the result set.</span></span> <span data-ttu-id="d5c4d-119">Os métodos de projeção são `Select` e `SelectMany`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-119">The projection methods are `Select` and `SelectMany`.</span></span>  
  
 <span data-ttu-id="d5c4d-120">*Filtragem* refere-se para a operação de restringir o conjunto de resultados para conter apenas os elementos que correspondem a uma condição especificada.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-120">*Filtering* refers to the operation of restricting the result set to contain only those elements that match a specified condition.</span></span> <span data-ttu-id="d5c4d-121">O método de filtragem é `Where`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-121">The filtering method is `Where`.</span></span>  
  
 <span data-ttu-id="d5c4d-122">A maioria das sobrecargas dos métodos de projeção e de filtragem tem suporte em [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], exceto aquelas que aceitam um argumento posicional.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-122">Most overloads of the projection and filtering methods are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], with the exception of those that accept a positional argument.</span></span>  
  
## <a name="join-methods"></a><span data-ttu-id="d5c4d-123">Métodos de junção</span><span class="sxs-lookup"><span data-stu-id="d5c4d-123">Join Methods</span></span>  
 <span data-ttu-id="d5c4d-124">A junção é uma operação importante em consultas que usam fontes de dados de destino que não têm nenhuma relação navegável entre si.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-124">Joining is an important operation in queries that target data sources that have no navigable relationships to each other.</span></span> <span data-ttu-id="d5c4d-125">Uma junção de duas fontes de dados é a associação de objetos em uma fonte de dados com objetos em outra fonte de dados que compartilham um atributo ou uma propriedade comum.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-125">A join of two data sources is the association of objects in one data source with objects in the other data source that share a common attribute or property.</span></span> <span data-ttu-id="d5c4d-126">Os métodos de junção são `Join` e `GroupJoin`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-126">The join methods are `Join` and `GroupJoin`.</span></span>  
  
 <span data-ttu-id="d5c4d-127">A maioria das sobrecargas dos métodos de junção tem suporte, exceto aquelas que usam <xref:System.Collections.Generic.IEqualityComparer%601>.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-127">Most overloads of the join methods are supported, with the exception of those that use a <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="d5c4d-128">Isso ocorre porque o comparador não pode ser convertido na fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-128">This is because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="set-methods"></a><span data-ttu-id="d5c4d-129">Definir métodos</span><span class="sxs-lookup"><span data-stu-id="d5c4d-129">Set Methods</span></span>  
 <span data-ttu-id="d5c4d-130">As operações de definição em LINQ são operações de consulta que baseiam seus conjuntos de resultados na presença ou na ausência de elementos equivalentes na mesma ou em outra coleção (ou em um conjunto).</span><span class="sxs-lookup"><span data-stu-id="d5c4d-130">Set operations in LINQ are query operations that base their result sets on the presence or absence of equivalent elements within the same or in another collection (or set).</span></span> <span data-ttu-id="d5c4d-131">Os métodos de definição são `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect` e `Union`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-131">The set methods are `All`, `Any`, `Concat`, `Contains`, `DefaultIfEmpty`, `Distinct`, `EqualAll`, `Except`, `Intersect`, and `Union`.</span></span>  
  
 <span data-ttu-id="d5c4d-132">A maioria das sobrecargas dos métodos de definição tem suporte em [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], embora existam algumas diferenças de comportamento em relação a LINQ to Objects.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-132">Most overloads of the set methods are supported in [!INCLUDE[linq_entities](../../../../../../includes/linq-entities-md.md)], though there are some differences in behavior compared to LINQ to Objects.</span></span> <span data-ttu-id="d5c4d-133">No entanto, definir métodos que usam um <xref:System.Collections.Generic.IEqualityComparer%601> não têm suporte porque o comparer não pode ser convertido para a fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-133">However, set methods that use an <xref:System.Collections.Generic.IEqualityComparer%601> are not supported because the comparer cannot be translated to the data source.</span></span>  
  
## <a name="ordering-methods"></a><span data-ttu-id="d5c4d-134">Métodos de ordenação</span><span class="sxs-lookup"><span data-stu-id="d5c4d-134">Ordering Methods</span></span>  
 <span data-ttu-id="d5c4d-135">Ordenação ou Classificação é a operação de ordenar os elementos de um conjunto de resultados com base em um ou mais atributos.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-135">Ordering, or sorting, refers to the ordering the elements of a result set based on one or more attributes.</span></span> <span data-ttu-id="d5c4d-136">Ao especificar mais de um critério de classificação, você pode romper os vínculos em um grupo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-136">By specifying more than one sort criterion, you can break ties within a group.</span></span>  
  
 <span data-ttu-id="d5c4d-137">Há suporte para a maioria dos métodos de ordenação, exceto para aqueles que usam <xref:System.Collections.Generic.IComparer%601>.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-137">Most overloads of the ordering methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IComparer%601>.</span></span> <span data-ttu-id="d5c4d-138">Isso ocorre porque o comparador não pode ser convertido na fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-138">This is because the comparer cannot be translated to the data source.</span></span> <span data-ttu-id="d5c4d-139">Os métodos de ordenação são `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending` e `Reverse`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-139">The ordering methods are `OrderBy`, `OrderByDescending`, `ThenBy`, `ThenByDescending`, and `Reverse`.</span></span>  
  
 <span data-ttu-id="d5c4d-140">Como a consulta é executada na fonte de dados, o comportamento de ordenação pode diferir das consultas executadas em CLR.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-140">Because the query is executed on the data source, the ordering behavior may differ from queries executed in the CLR.</span></span> <span data-ttu-id="d5c4d-141">Isso ocorre porque as opções de ordenação, por exemplo, a ordenação de casos, a ordenação de kanji e a ordenação de valores nulos, podem ser definidas na fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-141">This is because ordering options, such as case ordering, kanji ordering, and null ordering, can be set in the data source.</span></span> <span data-ttu-id="d5c4d-142">Dependendo da fonte de dados, essas opções de ordenação podem produzir diferentes resultados em relação a CLR.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-142">Depending on the data source, these ordering options might produce different results than in the CLR.</span></span>  
  
 <span data-ttu-id="d5c4d-143">Se você especificar o mesmo seletor de chave em mais de uma operação de ordenação, uma ordenação duplicada será produzida.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-143">If you specify the same key selector in more than one ordering operation, a duplicate ordering will be produced.</span></span> <span data-ttu-id="d5c4d-144">Isso não é válido e uma exceção será gerada.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-144">This is not valid and an exception will be thrown.</span></span>  
  
## <a name="grouping-methods"></a><span data-ttu-id="d5c4d-145">Métodos de agrupamento</span><span class="sxs-lookup"><span data-stu-id="d5c4d-145">Grouping Methods</span></span>  
 <span data-ttu-id="d5c4d-146">O agrupamento é a colocação de dados em grupos de modo que os elementos em cada grupo compartilhem um atributo comum.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-146">Grouping refers to placing data into groups so that the elements in each group share a common attribute.</span></span> <span data-ttu-id="d5c4d-147">O método de agrupamento é `GroupBy`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-147">The grouping method is `GroupBy`.</span></span>  
  
 <span data-ttu-id="d5c4d-148">Há suporte para a maioria dos métodos de agrupamento, exceto para aqueles que usam <xref:System.Collections.Generic.IEqualityComparer%601>.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-148">Most overloads of the grouping methods are supported, with the exception of those that use an <xref:System.Collections.Generic.IEqualityComparer%601>.</span></span> <span data-ttu-id="d5c4d-149">Isso ocorre porque o comparador não pode ser convertido na fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-149">This is because the comparer cannot be translated to the data source.</span></span>  
  
 <span data-ttu-id="d5c4d-150">Os métodos de agrupamento são mapeados para a fonte de dados usando uma subconsulta distinta para o seletor de chave.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-150">The grouping methods are mapped to the data source using a distinct sub-query for the key selector.</span></span> <span data-ttu-id="d5c4d-151">A subconsulta de comparação de seletor de chave é executada usando a semântica da fonte de dados, incluindo os problemas relacionados à comparação de valores `null`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-151">The key selector comparison sub-query is executed by using the semantics of the data source, including issues related to comparing `null` values.</span></span>  
  
## <a name="aggregate-methods"></a><span data-ttu-id="d5c4d-152">Métodos de agregação</span><span class="sxs-lookup"><span data-stu-id="d5c4d-152">Aggregate Methods</span></span>  
 <span data-ttu-id="d5c4d-153">Uma operação de agregação computa um único valor de uma coleção de valores.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-153">An aggregation operation computes a single value from a collection of values.</span></span> <span data-ttu-id="d5c4d-154">Por exemplo, calcular a temperatura média diária a partir dos valores de temperatura diária de um mês é uma operação de agregação.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-154">For example, calculating the average daily temperature from a month's worth of daily temperature values is an aggregation operation.</span></span> <span data-ttu-id="d5c4d-155">Os métodos de agregação são `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min` e `Sum`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-155">The aggregate methods are `Aggregate`, `Average`, `Count`, `LongCount`, `Max`, `Min`, and `Sum`.</span></span>  
  
 <span data-ttu-id="d5c4d-156">Há suporte para a maioria das sobrecargas dos métodos de agregação.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-156">Most overloads of the aggregate methods are supported.</span></span> <span data-ttu-id="d5c4d-157">No que se refere ao comportamento de valores nulos, os métodos de agregação usam a semântica de fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-157">For behavior related to null values, the aggregate methods use the data source semantics.</span></span> <span data-ttu-id="d5c4d-158">Quando valores nulos estão envolvidos, o comportamento dos métodos de agregação pode ser diferente, dependendo da fonte de dados back-end em uso.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-158">The behavior of the aggregation methods when null values are involved might be different, depending on which back-end data source is being used.</span></span> <span data-ttu-id="d5c4d-159">O comportamento do método de agregação que usa a semântica da fonte de dados também pode ser diferente daquele que é esperado dos métodos CLR.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-159">Aggregate method behavior using the semantics of the data source might also be different from what is expected from CLR methods.</span></span> <span data-ttu-id="d5c4d-160">Por exemplo, o comportamento padrão do método `Sum` no SQL Server é ignorar todos os valores nulos, em vez de gerar uma exceção.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-160">For example, the default behavior for the `Sum` method on SQL Server is to ignore any null values instead of throwing an exception.</span></span>  
  
 <span data-ttu-id="d5c4d-161">Todas as exceções resultantes de agregação, como um estouro da função `Sum`, são geradas como exceções da fonte de dados ou como exceções de Entity Framework durante a materialização dos resultados da consulta.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-161">Any exceptions that result from aggregation, such as an overflow from the `Sum` function, are thrown as data source exceptions or Entity Framework exceptions during the materialization of the query results.</span></span>  
  
 <span data-ttu-id="d5c4d-162">Para esses métodos que envolvem um cálculo em uma sequência, como `Sum` ou `Average`, o cálculo real é efetuado no servidor.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-162">For those methods that involve a calculation over a sequence, such as `Sum` or `Average`, the actual calculation is performed on the server.</span></span> <span data-ttu-id="d5c4d-163">Como resultado, podem ocorrer conversões de tipo e perda de precisão no servidor, e os resultados podem diferir daquele que é esperado usando a semântica CLR.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-163">As a result, type conversions and loss of precision might occur on the server, and the results might differ from what is expected using CLR semantics.</span></span>  
  
 <span data-ttu-id="d5c4d-164">O comportamento padrão dos métodos de agregação para valores nulos/não nulos é mostrado na seguinte tabela:</span><span class="sxs-lookup"><span data-stu-id="d5c4d-164">The default behavior of the aggregate methods for null/non-null values is shown in the following table:</span></span>  
  
|<span data-ttu-id="d5c4d-165">Método</span><span class="sxs-lookup"><span data-stu-id="d5c4d-165">Method</span></span>|<span data-ttu-id="d5c4d-166">Nenhum dado</span><span class="sxs-lookup"><span data-stu-id="d5c4d-166">No data</span></span>|<span data-ttu-id="d5c4d-167">Todos os valores nulos</span><span class="sxs-lookup"><span data-stu-id="d5c4d-167">All null values</span></span>|<span data-ttu-id="d5c4d-168">Alguns valores nulos</span><span class="sxs-lookup"><span data-stu-id="d5c4d-168">Some null values</span></span>|<span data-ttu-id="d5c4d-169">Nenhum valor nulo</span><span class="sxs-lookup"><span data-stu-id="d5c4d-169">No null values</span></span>|  
|------------|-------------|---------------------|----------------------|--------------------|  
|`Average`|<span data-ttu-id="d5c4d-170">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-170">Returns null.</span></span>|<span data-ttu-id="d5c4d-171">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-171">Returns null.</span></span>|<span data-ttu-id="d5c4d-172">Retorna a média dos valores não nulos em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-172">Returns the average of the non-null values in a sequence.</span></span>|<span data-ttu-id="d5c4d-173">Computa a média de uma sequência de valores numéricos.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-173">Computes the average of a sequence of numeric values.</span></span>|  
|`Count`|<span data-ttu-id="d5c4d-174">Retorna 0</span><span class="sxs-lookup"><span data-stu-id="d5c4d-174">Returns 0</span></span>|<span data-ttu-id="d5c4d-175">Retorna o número de valores nulos na sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-175">Returns the number of null values in the sequence.</span></span>|<span data-ttu-id="d5c4d-176">Retorna o número de valores nulos e não nulos na sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-176">Returns the number of null and non-null values in the sequence.</span></span>|<span data-ttu-id="d5c4d-177">Retorna o número de elementos na sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-177">Returns the number of elements in the sequence.</span></span>|  
|`Max`|<span data-ttu-id="d5c4d-178">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-178">Returns null.</span></span>|<span data-ttu-id="d5c4d-179">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-179">Returns null.</span></span>|<span data-ttu-id="d5c4d-180">Retorna o valor não nulo máximo em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-180">Returns the maximum non-null value in a sequence.</span></span>|<span data-ttu-id="d5c4d-181">Retorna o valor máximo em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-181">Returns the maximum value in a sequence.</span></span>|  
|`Min`|<span data-ttu-id="d5c4d-182">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-182">Returns null.</span></span>|<span data-ttu-id="d5c4d-183">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-183">Returns null.</span></span>|<span data-ttu-id="d5c4d-184">Retorna o valor não nulo mínimo em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-184">Returns the minimum non-null value in a sequence.</span></span>|<span data-ttu-id="d5c4d-185">Retorna o valor mínimo em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-185">Returns the minimum value in a sequence.</span></span>|  
|`Sum`|<span data-ttu-id="d5c4d-186">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-186">Returns null.</span></span>|<span data-ttu-id="d5c4d-187">Retorna um valor nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-187">Returns null.</span></span>|<span data-ttu-id="d5c4d-188">Retorna a soma do valor não nulo em uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-188">Returns the sum of the non-null value in a sequence.</span></span>|<span data-ttu-id="d5c4d-189">Computa a soma de uma sequência de valores numéricos.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-189">Computes the sum of a sequence of numeric values.</span></span>|  
  
## <a name="type-methods"></a><span data-ttu-id="d5c4d-190">Métodos de tipo</span><span class="sxs-lookup"><span data-stu-id="d5c4d-190">Type Methods</span></span>  
 <span data-ttu-id="d5c4d-191">Os dois métodos LINQ que lidam com conversão de tipo e de teste têm suporte no contexto do Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-191">The two LINQ methods that deal with type conversion and testing are both supported in the context of the Entity Framework.</span></span> <span data-ttu-id="d5c4d-192">Isso significa que os únicos tipos com suporte são tipos que são mapeados para o tipo apropriado do Entity Framework.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-192">This means that the only supported types are types that map to the appropriate Entity Framework type.</span></span> <span data-ttu-id="d5c4d-193">Para obter uma lista desses tipos, consulte [tipos de modelo conceituais (CSDL)](/ef/ef6/modeling/designer/advanced/edmx/csdl-spec#conceptual-model-types-csdl).</span><span class="sxs-lookup"><span data-stu-id="d5c4d-193">For a list of these types, see [Conceptual Model Types (CSDL)](/ef/ef6/modeling/designer/advanced/edmx/csdl-spec#conceptual-model-types-csdl).</span></span> <span data-ttu-id="d5c4d-194">Os métodos de tipo são `Convert` e `OfType`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-194">The type methods are `Convert` and `OfType`.</span></span>  
  
 <span data-ttu-id="d5c4d-195">Há suporte para `OfType` em tipos de entidade.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-195">`OfType` is supported for entity types.</span></span> <span data-ttu-id="d5c4d-196">Há suporte para `Convert` em tipos primitivos de modelo conceitual.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-196">`Convert` is supported for conceptual model primitive types.</span></span>  <span data-ttu-id="d5c4d-197">Também há suporte para os métodos C# `is` e `as`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-197">The C# `is` and `as` methods are also supported.</span></span>  
  
## <a name="paging-methods"></a><span data-ttu-id="d5c4d-198">Métodos de paginação</span><span class="sxs-lookup"><span data-stu-id="d5c4d-198">Paging Methods</span></span>  
 <span data-ttu-id="d5c4d-199">Operações de paginação retornam um único elemento ou em vários elementos de uma sequência.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-199">Paging operations return a single element or multiple elements from a sequence.</span></span> <span data-ttu-id="d5c4d-200">Os métodos de paginação com suporte são `First`, `FirstOrDefault`, `Single`, `SingleOrDefault`, `Skip`, e `Take`.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-200">The supported paging methods are `First`, `FirstOrDefault`, `Single`, `SingleOrDefault`, `Skip`, and `Take`.</span></span>  
  
 <span data-ttu-id="d5c4d-201">Um número de métodos de paginação não têm suporte, devido à impossibilidade de mapear funções para a fonte de dados ou à falta de ordenação implícita de conjuntos na fonte de dados.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-201">A number of paging methods are not supported, due either to the inability to map functions to the data source or to the lack of implicit ordering of sets on the data source.</span></span> <span data-ttu-id="d5c4d-202">Os métodos que retornam um valor padrão são restritos a tipos primitivos de modelo conceitual e a tipos de referência com valores nulos como padrão.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-202">Methods that return a default value are restricted to conceptual model primitive types and reference types with null defaults.</span></span> <span data-ttu-id="d5c4d-203">Os métodos de paginação executados em uma sequência vazia retornam nulo.</span><span class="sxs-lookup"><span data-stu-id="d5c4d-203">Paging methods that are executed on an empty sequence will return null.</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="d5c4d-204">Consulte também</span><span class="sxs-lookup"><span data-stu-id="d5c4d-204">See also</span></span>

- [<span data-ttu-id="d5c4d-205">Métodos LINQ com e sem suporte (LINQ to Entities)</span><span class="sxs-lookup"><span data-stu-id="d5c4d-205">Supported and Unsupported LINQ Methods (LINQ to Entities)</span></span>](../../../../../../docs/framework/data/adonet/ef/language-reference/supported-and-unsupported-linq-methods-linq-to-entities.md)
- <span data-ttu-id="d5c4d-206">[Visão Geral de Operadores de Consulta Padrão](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120))</span><span class="sxs-lookup"><span data-stu-id="d5c4d-206">[Standard Query Operators Overview](https://docs.microsoft.com/previous-versions/visualstudio/visual-studio-2013/bb397896(v=vs.120))</span></span>
