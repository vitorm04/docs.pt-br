---
title: Método ICorProfilerInfo2::DoStackSnapshot
ms.date: 03/30/2017
api_name:
- ICorProfilerInfo2.DoStackSnapshot
api_location:
- mscorwks.dll
api_type:
- COM
f1_keywords:
- ICorProfilerInfo2::DoStackSnapshot
helpviewer_keywords:
- ICorProfilerInfo2::DoStackSnapshot method [.NET Framework profiling]
- DoStackSnapshot method [.NET Framework profiling]
ms.assetid: 287b11e9-7c52-4a13-ba97-751203fa97f4
topic_type:
- apiref
ms.openlocfilehash: 64bcf6ee58d743a26e31c49a425f36cc808b5080
ms.sourcegitcommit: 9a39f2a06f110c9c7ca54ba216900d038aa14ef3
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 11/23/2019
ms.locfileid: "74426831"
---
# <a name="icorprofilerinfo2dostacksnapshot-method"></a><span data-ttu-id="c7582-102">Método ICorProfilerInfo2::DoStackSnapshot</span><span class="sxs-lookup"><span data-stu-id="c7582-102">ICorProfilerInfo2::DoStackSnapshot Method</span></span>
<span data-ttu-id="c7582-103">Percorre os quadros gerenciados na pilha para o thread especificado e envia informações para o criador de perfil por meio de um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="c7582-103">Walks the managed frames on the stack for the specified thread, and sends information to the profiler through a callback.</span></span>  
  
## <a name="syntax"></a><span data-ttu-id="c7582-104">Sintaxe</span><span class="sxs-lookup"><span data-stu-id="c7582-104">Syntax</span></span>  
  
```cpp  
HRESULT DoStackSnapshot(  
    [in] ThreadID thread,  
    [in] StackSnapshotCallback *callback,  
    [in] ULONG32 infoFlags,  
    [in] void *clientData,  
    [in, size_is(contextSize), length_is(contextSize)] BYTE context[],  
    [in] ULONG32 contextSize);  
```  
  
## <a name="parameters"></a><span data-ttu-id="c7582-105">Parâmetros</span><span class="sxs-lookup"><span data-stu-id="c7582-105">Parameters</span></span>  
 `thread`  
 <span data-ttu-id="c7582-106">no A ID do thread de destino.</span><span class="sxs-lookup"><span data-stu-id="c7582-106">[in] The ID of the target thread.</span></span>  
  
 <span data-ttu-id="c7582-107">Passar NULL em `thread` gera um instantâneo do thread atual.</span><span class="sxs-lookup"><span data-stu-id="c7582-107">Passing null in `thread` yields a snapshot of the current thread.</span></span> <span data-ttu-id="c7582-108">Se um `ThreadID` de um thread diferente for passado, o Common Language Runtime (CLR) suspenderá esse thread, executará o instantâneo e será retomado.</span><span class="sxs-lookup"><span data-stu-id="c7582-108">If a `ThreadID` of a different thread is passed, the common language runtime (CLR) suspends that thread, performs the snapshot, and resumes.</span></span>  
  
 `callback`  
 <span data-ttu-id="c7582-109">no Um ponteiro para a implementação do método [StackSnapshotCallback](../../../../docs/framework/unmanaged-api/profiling/stacksnapshotcallback-function.md) , que é chamado pelo CLR para fornecer ao criador de perfil informações sobre cada quadro gerenciado e cada execução de quadros não gerenciados.</span><span class="sxs-lookup"><span data-stu-id="c7582-109">[in] A pointer to the implementation of the [StackSnapshotCallback](../../../../docs/framework/unmanaged-api/profiling/stacksnapshotcallback-function.md) method, which is called by the CLR to provide the profiler with information on each managed frame and each run of unmanaged frames.</span></span>  
  
 <span data-ttu-id="c7582-110">O método `StackSnapshotCallback` é implementado pelo gravador do criador de perfil.</span><span class="sxs-lookup"><span data-stu-id="c7582-110">The `StackSnapshotCallback` method is implemented by the profiler writer.</span></span>  
  
 `infoFlags`  
 <span data-ttu-id="c7582-111">no Um valor da enumeração [COR_PRF_SNAPSHOT_INFO](../../../../docs/framework/unmanaged-api/profiling/cor-prf-snapshot-info-enumeration.md) , que especifica a quantidade de dados a serem passados de volta para cada quadro por `StackSnapshotCallback`.</span><span class="sxs-lookup"><span data-stu-id="c7582-111">[in] A value of the [COR_PRF_SNAPSHOT_INFO](../../../../docs/framework/unmanaged-api/profiling/cor-prf-snapshot-info-enumeration.md) enumeration, which specifies the amount of data to be passed back for each frame by `StackSnapshotCallback`.</span></span>  
  
 `clientData`  
 <span data-ttu-id="c7582-112">no Um ponteiro para os dados do cliente, que é passado diretamente para a função de retorno de chamada `StackSnapshotCallback`.</span><span class="sxs-lookup"><span data-stu-id="c7582-112">[in] A pointer to the client data, which is passed straight through to the `StackSnapshotCallback` callback function.</span></span>  
  
 `context`  
 <span data-ttu-id="c7582-113">no Um ponteiro para uma estrutura de `CONTEXT` do Win32, que é usado para propagar a movimentação da pilha.</span><span class="sxs-lookup"><span data-stu-id="c7582-113">[in] A pointer to a Win32 `CONTEXT` structure, which is used to seed the stack walk.</span></span> <span data-ttu-id="c7582-114">A estrutura de `CONTEXT` do Win32 contém valores dos registros de CPU e representa o estado da CPU em um determinado momento no tempo.</span><span class="sxs-lookup"><span data-stu-id="c7582-114">The Win32 `CONTEXT` structure contains values of the CPU registers and represents the state of the CPU at a particular moment in time.</span></span>  
  
 <span data-ttu-id="c7582-115">A semente ajuda o CLR a determinar onde começar a movimentação da pilha, se a parte superior da pilha for um código auxiliar não gerenciado; caso contrário, a semente será ignorada.</span><span class="sxs-lookup"><span data-stu-id="c7582-115">The seed helps the CLR determine where to begin the stack walk, if the top of the stack is unmanaged helper code; otherwise, the seed is ignored.</span></span> <span data-ttu-id="c7582-116">Uma semente deve ser fornecida para uma movimentação assíncrona.</span><span class="sxs-lookup"><span data-stu-id="c7582-116">A seed must be supplied for an asynchronous walk.</span></span> <span data-ttu-id="c7582-117">Se você estiver fazendo uma movimentação síncrona, nenhuma semente será necessária.</span><span class="sxs-lookup"><span data-stu-id="c7582-117">If you are doing a synchronous walk, no seed is necessary.</span></span>  
  
 <span data-ttu-id="c7582-118">O parâmetro `context` é válido somente se o sinalizador COR_PRF_SNAPSHOT_CONTEXT foi passado no parâmetro `infoFlags`.</span><span class="sxs-lookup"><span data-stu-id="c7582-118">The `context` parameter is valid only if the COR_PRF_SNAPSHOT_CONTEXT flag was passed in the `infoFlags` parameter.</span></span>  
  
 `contextSize`  
 <span data-ttu-id="c7582-119">no O tamanho da estrutura de `CONTEXT`, que é referenciada pelo parâmetro `context`.</span><span class="sxs-lookup"><span data-stu-id="c7582-119">[in] The size of the `CONTEXT` structure, which is referenced by the `context` parameter.</span></span>  
  
## <a name="remarks"></a><span data-ttu-id="c7582-120">Comentários</span><span class="sxs-lookup"><span data-stu-id="c7582-120">Remarks</span></span>  
 <span data-ttu-id="c7582-121">Passar NULL por `thread` produz um instantâneo do thread atual.</span><span class="sxs-lookup"><span data-stu-id="c7582-121">Passing null for `thread` yields a snapshot of the current thread.</span></span> <span data-ttu-id="c7582-122">Os instantâneos podem ser tirados de outros threads somente se o thread de destino for suspenso no momento.</span><span class="sxs-lookup"><span data-stu-id="c7582-122">Snapshots can be taken of other threads only if the target thread is suspended at the time.</span></span>  
  
 <span data-ttu-id="c7582-123">Quando o criador de perfil deseja percorrer a pilha, ele chama `DoStackSnapshot`.</span><span class="sxs-lookup"><span data-stu-id="c7582-123">When the profiler wants to walk the stack, it calls `DoStackSnapshot`.</span></span> <span data-ttu-id="c7582-124">Antes que o CLR retorne dessa chamada, ele chama o `StackSnapshotCallback` várias vezes, uma vez para cada quadro gerenciado (ou execução de quadros não gerenciados) na pilha.</span><span class="sxs-lookup"><span data-stu-id="c7582-124">Before the CLR returns from that call, it calls your `StackSnapshotCallback` several times, once for each managed frame (or run of unmanaged frames) on the stack.</span></span> <span data-ttu-id="c7582-125">Quando são encontrados quadros não gerenciados, você mesmo deve acompanhá-los.</span><span class="sxs-lookup"><span data-stu-id="c7582-125">When unmanaged frames are encountered, you must walk them yourself.</span></span>  
  
 <span data-ttu-id="c7582-126">A ordem na qual a pilha é movimentada é o inverso de como os quadros foram enviados para a pilha: quadro (último a ser enviado) primeiro, principal (primeiro a ser enviado).</span><span class="sxs-lookup"><span data-stu-id="c7582-126">The order in which the stack is walked is the reverse of how the frames were pushed onto the stack: leaf (last-pushed) frame first, main (first-pushed) frame last.</span></span>  
  
 <span data-ttu-id="c7582-127">Para obter mais informações sobre como programar o criador de perfil para movimentar pilhas gerenciadas, consulte [movimentação de pilha do profiler no .NET Framework 2,0: Noções básicas e além disso](https://go.microsoft.com/fwlink/?LinkId=73638).</span><span class="sxs-lookup"><span data-stu-id="c7582-127">For more information about how to program the profiler to walk managed stacks, see [Profiler Stack Walking in the .NET Framework 2.0: Basics and Beyond](https://go.microsoft.com/fwlink/?LinkId=73638).</span></span>  
  
 <span data-ttu-id="c7582-128">Uma movimentação de pilha pode ser síncrona ou assíncrona, conforme explicado nas seções a seguir.</span><span class="sxs-lookup"><span data-stu-id="c7582-128">A stack walk can be synchronous or asynchronous, as explained in the following sections.</span></span>  
  
## <a name="synchronous-stack-walk"></a><span data-ttu-id="c7582-129">Movimentação de pilha síncrona</span><span class="sxs-lookup"><span data-stu-id="c7582-129">Synchronous Stack Walk</span></span>  
 <span data-ttu-id="c7582-130">Uma movimentação de pilha síncrona envolve percorrer a pilha do thread atual em resposta a um retorno de chamada.</span><span class="sxs-lookup"><span data-stu-id="c7582-130">A synchronous stack walk involves walking the stack of the current thread in response to a callback.</span></span> <span data-ttu-id="c7582-131">Ele não requer propagação ou suspensão.</span><span class="sxs-lookup"><span data-stu-id="c7582-131">It does not require seeding or suspending.</span></span>  
  
 <span data-ttu-id="c7582-132">Você faz uma chamada síncrona quando, em resposta ao CLR chamando um dos métodos [ICorProfilerCallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) (ou [ICorProfilerCallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md)) de seu criador de perfil, você chama `DoStackSnapshot` para percorrer a pilha do thread atual.</span><span class="sxs-lookup"><span data-stu-id="c7582-132">You make a synchronous call when, in response to the CLR calling one of your profiler's [ICorProfilerCallback](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-interface.md) (or [ICorProfilerCallback2](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback2-interface.md)) methods, you call `DoStackSnapshot` to walk the stack of the current thread.</span></span> <span data-ttu-id="c7582-133">Isso é útil quando você deseja ver qual é a aparência da pilha em uma notificação como [ICorProfilerCallback:: ObjectAllocated](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-objectallocated-method.md).</span><span class="sxs-lookup"><span data-stu-id="c7582-133">This is useful when you want to see what the stack looks like at a notification such as [ICorProfilerCallback::ObjectAllocated](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-objectallocated-method.md).</span></span> <span data-ttu-id="c7582-134">Basta chamar `DoStackSnapshot` de dentro de seu método `ICorProfilerCallback`, passando NULL nos parâmetros `context` e `thread`.</span><span class="sxs-lookup"><span data-stu-id="c7582-134">You just call `DoStackSnapshot` from within your `ICorProfilerCallback` method, passing null in the `context` and `thread` parameters.</span></span>  
  
## <a name="asynchronous-stack-walk"></a><span data-ttu-id="c7582-135">Movimentação de pilha assíncrona</span><span class="sxs-lookup"><span data-stu-id="c7582-135">Asynchronous Stack Walk</span></span>  
 <span data-ttu-id="c7582-136">Uma movimentação de pilha assíncrona envolve a movimentação da pilha de um thread diferente ou a movimentação da pilha do thread atual, não em resposta a um retorno de chamada, mas ao seqüestrar o ponteiro de instrução do thread atual.</span><span class="sxs-lookup"><span data-stu-id="c7582-136">An asynchronous stack walk entails walking the stack of a different thread, or walking the stack of the current thread, not in response to a callback, but by hijacking the current thread's instruction pointer.</span></span> <span data-ttu-id="c7582-137">Uma movimentação assíncrona exigirá uma semente se a parte superior da pilha for um código não gerenciado que não faz parte de uma chamada de plataforma (PInvoke) ou de chamadas COM, mas o código auxiliar no próprio CLR.</span><span class="sxs-lookup"><span data-stu-id="c7582-137">An asynchronous walk requires a seed if the top of the stack is unmanaged code that is not part of a platform invoke (PInvoke) or COM call, but helper code in the CLR itself.</span></span> <span data-ttu-id="c7582-138">Por exemplo, o código que faz compilação JIT (just-in-time) ou coleta de lixo é o código auxiliar.</span><span class="sxs-lookup"><span data-stu-id="c7582-138">For example, code that does just-in-time (JIT) compiling or garbage collection is helper code.</span></span>  
  
 <span data-ttu-id="c7582-139">Você Obtém uma semente suspendendo diretamente o thread-alvo e movimentando sua pilha por conta própria até encontrar o quadro gerenciado superior.</span><span class="sxs-lookup"><span data-stu-id="c7582-139">You obtain a seed by directly suspending the target thread and walking its stack yourself, until you find the topmost managed frame.</span></span> <span data-ttu-id="c7582-140">Depois que o thread de destino for suspenso, obtenha o contexto de registro atual do thread de destino.</span><span class="sxs-lookup"><span data-stu-id="c7582-140">After the target thread is suspended, get the target thread's current register context.</span></span> <span data-ttu-id="c7582-141">Em seguida, determine se o contexto de registro aponta para código não gerenciado chamando [ICorProfilerInfo:: GetFunctionFromIP](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-getfunctionfromip-method.md) — se ele retornar um `FunctionID` igual a zero, o quadro será um código não gerenciado.</span><span class="sxs-lookup"><span data-stu-id="c7582-141">Next, determine whether the register context points to unmanaged code by calling [ICorProfilerInfo::GetFunctionFromIP](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-getfunctionfromip-method.md) — if it returns a `FunctionID` equal to zero, the frame is unmanaged code.</span></span> <span data-ttu-id="c7582-142">Agora, percorra a pilha até atingir o primeiro quadro gerenciado e, em seguida, calcule o contexto de semente com base no contexto de registro desse quadro.</span><span class="sxs-lookup"><span data-stu-id="c7582-142">Now, walk the stack until you reach the first managed frame, and then calculate the seed context based on the register context for that frame.</span></span>  
  
 <span data-ttu-id="c7582-143">Chame `DoStackSnapshot` com o contexto de semente para iniciar a movimentação de pilha assíncrona.</span><span class="sxs-lookup"><span data-stu-id="c7582-143">Call `DoStackSnapshot` with your seed context to begin the asynchronous stack walk.</span></span> <span data-ttu-id="c7582-144">Se você não fornecer uma semente, `DoStackSnapshot` poderá ignorar os quadros gerenciados na parte superior da pilha e, consequentemente, fornecerá uma movimentação de pilha incompleta.</span><span class="sxs-lookup"><span data-stu-id="c7582-144">If you do not supply a seed, `DoStackSnapshot` might skip managed frames at the top of the stack and, consequently, will give you an incomplete stack walk.</span></span> <span data-ttu-id="c7582-145">Se você fornecer uma semente, ele deverá apontar para código gerado por JIT ou gerador de imagem nativa (NGen. exe); caso contrário, `DoStackSnapshot` retornará o código de falha, CORPROF_E_STACKSNAPSHOT_UNMANAGED_CTX.</span><span class="sxs-lookup"><span data-stu-id="c7582-145">If you do supply a seed, it must point to JIT-compiled or Native Image Generator (Ngen.exe)-generated code; otherwise, `DoStackSnapshot` returns the failure code, CORPROF_E_STACKSNAPSHOT_UNMANAGED_CTX.</span></span>  
  
 <span data-ttu-id="c7582-146">As movimentações assíncronas podem causar facilmente deadlocks ou violações de acesso, a menos que você siga estas diretrizes:</span><span class="sxs-lookup"><span data-stu-id="c7582-146">Asynchronous stack walks can easily cause deadlocks or access violations, unless you follow these guidelines:</span></span>  
  
- <span data-ttu-id="c7582-147">Quando você suspende threads diretamente, lembre-se de que apenas um thread que nunca executou código gerenciado pode suspender outro thread.</span><span class="sxs-lookup"><span data-stu-id="c7582-147">When you directly suspend threads, remember that only a thread that has never run managed code can suspend another thread.</span></span>  
  
- <span data-ttu-id="c7582-148">Bloquear sempre em seu retorno de chamada [ICorProfilerCallback:: ThreadDestroyed](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-threaddestroyed-method.md) até que a movimentação da pilha desse thread seja concluída.</span><span class="sxs-lookup"><span data-stu-id="c7582-148">Always block in your [ICorProfilerCallback::ThreadDestroyed](../../../../docs/framework/unmanaged-api/profiling/icorprofilercallback-threaddestroyed-method.md) callback until that thread's stack walk is complete.</span></span>  
  
- <span data-ttu-id="c7582-149">Não mantenha um bloqueio enquanto o criador de perfil chamar uma função CLR que possa disparar uma coleta de lixo.</span><span class="sxs-lookup"><span data-stu-id="c7582-149">Do not hold a lock while your profiler calls into a CLR function that can trigger a garbage collection.</span></span> <span data-ttu-id="c7582-150">Ou seja, não mantenha um bloqueio se o thread de propriedade puder fazer uma chamada que dispara uma coleta de lixo.</span><span class="sxs-lookup"><span data-stu-id="c7582-150">That is, do not hold a lock if the owning thread might make a call that triggers a garbage collection.</span></span>  
  
 <span data-ttu-id="c7582-151">Também há um risco de deadlock se você chamar `DoStackSnapshot` de um thread que seu criador de perfil criou para que você possa percorrer a pilha de um thread-alvo separado.</span><span class="sxs-lookup"><span data-stu-id="c7582-151">There is also a risk of deadlock if you call `DoStackSnapshot` from a thread that your profiler has created so that you can walk the stack of a separate target thread.</span></span> <span data-ttu-id="c7582-152">Na primeira vez que o thread que você criou insere determinados métodos de `ICorProfilerInfo*` (incluindo `DoStackSnapshot`), o CLR executará a inicialização específica do CLR por thread nesse thread.</span><span class="sxs-lookup"><span data-stu-id="c7582-152">The first time the thread you created enters certain `ICorProfilerInfo*` methods (including `DoStackSnapshot`), the CLR will perform per-thread, CLR-specific initialization on that thread.</span></span> <span data-ttu-id="c7582-153">Se o seu criador de perfil tiver suspenso o thread de destino cuja pilha você está tentando percorrer e se esse thread de destino tiver causado um bloqueio necessário para executar essa inicialização por thread, ocorrerá um deadlock.</span><span class="sxs-lookup"><span data-stu-id="c7582-153">If your profiler has suspended the target thread whose stack you are trying to walk, and if that target thread happened to own a lock necessary for performing this per-thread initialization, a deadlock will occur.</span></span> <span data-ttu-id="c7582-154">Para evitar esse deadlock, faça uma chamada inicial em `DoStackSnapshot` de seu thread criado pelo criador de perfil para percorrer um thread de destino separado, mas não suspenda o thread-alvo primeiro.</span><span class="sxs-lookup"><span data-stu-id="c7582-154">To avoid this deadlock, make an initial call into `DoStackSnapshot` from your profiler-created thread to walk a separate target thread, but do not suspend the target thread first.</span></span> <span data-ttu-id="c7582-155">Essa chamada inicial garante que a inicialização por thread possa ser concluída sem deadlock.</span><span class="sxs-lookup"><span data-stu-id="c7582-155">This initial call ensures that the per-thread initialization can complete without deadlock.</span></span> <span data-ttu-id="c7582-156">Se `DoStackSnapshot` tiver sucesso e reportar pelo menos um quadro, depois desse ponto, será seguro que esse thread criado pelo criador de perfil suspenda qualquer thread de destino e chame `DoStackSnapshot` para percorrer a pilha do thread-alvo.</span><span class="sxs-lookup"><span data-stu-id="c7582-156">If `DoStackSnapshot` succeeds and reports at least one frame, after that point, it will be safe for that profiler-created thread to suspend any target thread and call `DoStackSnapshot` to walk the stack of that target thread.</span></span>  
  
## <a name="requirements"></a><span data-ttu-id="c7582-157">Requisitos</span><span class="sxs-lookup"><span data-stu-id="c7582-157">Requirements</span></span>  
 <span data-ttu-id="c7582-158">**Plataformas:** confira [Requisitos do sistema](../../../../docs/framework/get-started/system-requirements.md).</span><span class="sxs-lookup"><span data-stu-id="c7582-158">**Platforms:** See [System Requirements](../../../../docs/framework/get-started/system-requirements.md).</span></span>  
  
 <span data-ttu-id="c7582-159">**Cabeçalho:** CorProf. idl, CorProf. h</span><span class="sxs-lookup"><span data-stu-id="c7582-159">**Header:** CorProf.idl, CorProf.h</span></span>  
  
 <span data-ttu-id="c7582-160">**Biblioteca:** CorGuids.lib</span><span class="sxs-lookup"><span data-stu-id="c7582-160">**Library:** CorGuids.lib</span></span>  
  
 <span data-ttu-id="c7582-161">**Versões do .NET Framework:** [!INCLUDE[net_current_v20plus](../../../../includes/net-current-v20plus-md.md)]</span><span class="sxs-lookup"><span data-stu-id="c7582-161">**.NET Framework Versions:** [!INCLUDE[net_current_v20plus](../../../../includes/net-current-v20plus-md.md)]</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="c7582-162">Consulte também</span><span class="sxs-lookup"><span data-stu-id="c7582-162">See also</span></span>

- [<span data-ttu-id="c7582-163">Interface ICorProfilerInfo</span><span class="sxs-lookup"><span data-stu-id="c7582-163">ICorProfilerInfo Interface</span></span>](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo-interface.md)
- [<span data-ttu-id="c7582-164">Interface ICorProfilerInfo2</span><span class="sxs-lookup"><span data-stu-id="c7582-164">ICorProfilerInfo2 Interface</span></span>](../../../../docs/framework/unmanaged-api/profiling/icorprofilerinfo2-interface.md)
