---
title: WSTrustChannelFactory e WSTrustChannel
ms.date: 03/30/2017
ms.assetid: 96cec467-e963-4132-b18b-7d0b3a2e979f
author: BrucePerlerMS
ms.openlocfilehash: ecc62292b2b064219127c369f43141a31ffe606d
ms.sourcegitcommit: ea00c05e0995dae928d48ead99ddab6296097b4c
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/02/2018
ms.locfileid: "48034708"
---
# <a name="wstrustchannelfactory-and-wstrustchannel"></a><span data-ttu-id="ea2b7-102">WSTrustChannelFactory e WSTrustChannel</span><span class="sxs-lookup"><span data-stu-id="ea2b7-102">WSTrustChannelFactory and WSTrustChannel</span></span>
<span data-ttu-id="ea2b7-103">Se você já estiver familiarizado com o WCF (Windows Communication Foundation), saberá que um cliente WCF já é baseado em federação.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-103">If you are already familiar with Windows Communication Foundation (WCF), you know that a WCF client is already federation aware.</span></span> <span data-ttu-id="ea2b7-104">Configurando um cliente WCF com uma <xref:System.ServiceModel.WSFederationHttpBinding> ou uma associação personalizada semelhante, você pode habilitar a autenticação federada em um serviço.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-104">By configuring a WCF client with a <xref:System.ServiceModel.WSFederationHttpBinding> or similar custom binding, you can enable federated authentication to a service.</span></span>

 <span data-ttu-id="ea2b7-105">O WCF obtém o token emitido pelo STS (serviço de token de segurança) nos bastidores e usa esse token para se autenticar no serviço.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-105">WCF obtains the token that is issued by the security token service (STS) behind the scenes and uses this token to authenticate to the service.</span></span> <span data-ttu-id="ea2b7-106">A principal limitação dessa abordagem é que não há nenhuma visibilidade na comunicação do cliente com o servidor.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-106">The main limitation to this approach is that there is no visibility into the client’s communications with the server.</span></span> <span data-ttu-id="ea2b7-107">O WCF gera o RST (token de segurança de solicitação) automaticamente para o STS, com base nos parâmetros do token emitido na associação.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-107">WCF automatically generates the request security token (RST) to the STS based on the issued token parameters on the binding.</span></span> <span data-ttu-id="ea2b7-108">Isso significa que o cliente não pode variar os parâmetros do RST por solicitação, inspecionar a RSTR (resposta do token de segurança de solicitação) para obter informações como as declarações de exibição nem armazenar o token em cache para uso futuro.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-108">This means that the client cannot vary the RST parameters per request, inspect the request security token response (RSTR) to get information such as display claims, or cache the token for future use.</span></span>

 <span data-ttu-id="ea2b7-109">No momento, o cliente WCF é adequado para cenários de federação básica.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-109">Currently, the WCF client is suitable for basic federation scenarios.</span></span> <span data-ttu-id="ea2b7-110">No entanto, um dos principais cenários ao qual o WIF (Windows Identity Foundation) dá suporte exige controle sobre o RST em um nível que o WCF não permite facilmente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-110">However, one of the major scenarios that Windows Identity Foundation (WIF) supports requires control over the RST at a level that WCF does not easily allow.</span></span> <span data-ttu-id="ea2b7-111">Portanto, o WIF adiciona recursos que proporcionam mais controle sobre a comunicação com o STS.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-111">Therefore, WIF adds features that give you more control over communication with the STS.</span></span>

 <span data-ttu-id="ea2b7-112">O WIF dá suporte aos seguintes cenários de federação:</span><span class="sxs-lookup"><span data-stu-id="ea2b7-112">WIF supports the following federation scenarios:</span></span>

- <span data-ttu-id="ea2b7-113">Uso de um cliente WCF sem nenhuma dependência do WIF para se autenticar em um serviço federado</span><span class="sxs-lookup"><span data-stu-id="ea2b7-113">Using a WCF client without any WIF dependencies to authenticate to a federated service</span></span>

- <span data-ttu-id="ea2b7-114">Habilitação do WIF em um cliente WCF para inserir um elemento ActAs ou OnBehalfOf no RST para o STS</span><span class="sxs-lookup"><span data-stu-id="ea2b7-114">Enabling WIF on a WCF client to insert an ActAs or OnBehalfOf element into the RST to the STS</span></span>

- <span data-ttu-id="ea2b7-115">Uso do WIF sozinho para obter um token do STS e, em seguida, permissão para que um cliente WCF se autentique com esse token.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-115">Using WIF alone to obtain a token from the STS and then enable a WCF client to authenticate with this token.</span></span> <span data-ttu-id="ea2b7-116">Para obter mais informações, consulte a amostra [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406).</span><span class="sxs-lookup"><span data-stu-id="ea2b7-116">For more information, see [ClaimsAwareWebService](https://go.microsoft.com/fwlink/?LinkID=248406) sample.</span></span>

 <span data-ttu-id="ea2b7-117">O primeiro cenário é autoexplicativo: os clientes WCF existentes continuarão funcionando com terceiras partes confiáveis e STSs do WIF.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-117">The first scenario is self-explanatory: Existing WCF clients will continue to work with WIF relying parties and STSs.</span></span> <span data-ttu-id="ea2b7-118">Este tópico aborda os dois cenários restantes.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-118">This topic discusses the remaining two scenarios.</span></span>

## <a name="enhancing-an-existing-wcf-client-with-actas--onbehalfof"></a><span data-ttu-id="ea2b7-119">Melhorando um cliente WCF existente com ActAs/OnBehalfOf</span><span class="sxs-lookup"><span data-stu-id="ea2b7-119">Enhancing an Existing WCF Client with ActAs / OnBehalfOf</span></span>
<span data-ttu-id="ea2b7-120">Em um cenário típico de delegação de identidade, um cliente chama um serviço de camada intermediária, que, em seguida, chama um serviço back-end.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-120">In a typical identity delegation scenario, a client calls a middle-tier service, which then calls a back-end service.</span></span> <span data-ttu-id="ea2b7-121">O serviço de camada intermediária atua como ou em nome do cliente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-121">The middle-tier service acts as, or acts on behalf of, the client.</span></span>

> [!TIP]
> <span data-ttu-id="ea2b7-122">Qual é a diferença entre ActAs e OnBehalfOf?</span><span class="sxs-lookup"><span data-stu-id="ea2b7-122">What is the difference between ActAs and OnBehalfOf?</span></span>
>
> <span data-ttu-id="ea2b7-123">Do ponto de vista de protocolo WS-Trust:</span><span class="sxs-lookup"><span data-stu-id="ea2b7-123">From the WS-Trust protocol standpoint:</span></span>
>
> 1. <span data-ttu-id="ea2b7-124">Um elemento RST ActAs indica que o solicitante deseja obter um token que contém declarações sobre duas entidades distintas: o solicitante e uma entidade externa representada pelo token no elemento ActAs.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-124">An ActAs RST element indicates that the requestor wants a token that contains claims about two distinct entities: the requestor, and an external entity represented by the token in the ActAs element.</span></span>
> 2. <span data-ttu-id="ea2b7-125">Um elemento RST OnBehalfOf indica que o solicitante deseja obter um token que contém declarações somente sobre uma entidade: a entidade externa representada pelo token no elemento OnBehalfOf.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-125">An OnBehalfOf RST element indicates that the requestor wants a token that contains claims only about one entity: the external entity represented by the token in the OnBehalfOf element.</span></span>
>
> <span data-ttu-id="ea2b7-126">O recurso ActAs normalmente é usado em cenários que exigem a delegação de composição, em que o destinatário final do token emitido pode inspecionar a cadeia inteira de delegação e ver não apenas o cliente, mas todos os intermediários.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-126">The ActAs feature is typically used in scenarios that require composite delegation, where the final recipient of the issued token can inspect the entire delegation chain and see not just the client, but all intermediaries.</span></span> <span data-ttu-id="ea2b7-127">Isso permite que ele execute o controle de acesso, a auditoria e outras atividades relacionadas, com base na cadeia inteira de delegação de identidade.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-127">This lets it perform access control, auditing and other related activities based on the entire identity delegation chain.</span></span> <span data-ttu-id="ea2b7-128">O recurso ActAs normalmente é usado em sistemas de várias camadas para autenticar e transmitir informações sobre identidades entre as camadas, sem precisar passar essas informações na camada do aplicativo/lógica de negócios.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-128">The ActAs feature is commonly used in multi-tiered systems to authenticate and pass information about identities between the tiers without having to pass this information at the application/business logic layer.</span></span>
>
> <span data-ttu-id="ea2b7-129">O recurso OnBehalfOf é usado em cenários em que somente a identidade do cliente original é importante e é praticamente a mesma que o recurso de representação de identidade disponível no Windows.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-129">The OnBehalfOf feature is used in scenarios where only the identity of the original client is important and is effectively the same as the identity impersonation feature available in Windows.</span></span> <span data-ttu-id="ea2b7-130">Quando OnBehalfOf é usado, o destinatário final do token emitido somente pode ver declarações sobre o cliente original e as informações sobre intermediários não são preservadas.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-130">When OnBehalfOf is used, the final recipient of the issued token can only see claims about the original client, and the information about intermediaries is not preserved.</span></span> <span data-ttu-id="ea2b7-131">Um padrão comum em que o recurso OnBehalfOf é usado é o padrão de proxy, no qual o cliente não pode acessar o STS diretamente, mas se comunica por meio de um gateway de proxy.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-131">One common pattern where the OnBehalfOf feature is used is the proxy pattern where the client cannot access the STS directly but instead communicates through a proxy gateway.</span></span> <span data-ttu-id="ea2b7-132">O gateway de proxy autentica o chamador e coloca informações sobre o chamador no elemento OnBehalfOf da mensagem RST que ele então envia para o STS real para processamento.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-132">The proxy gateway authenticates the caller and puts information about the caller into the OnBehalfOf element of the RST message that it then sends to the real STS for processing.</span></span> <span data-ttu-id="ea2b7-133">O token resultante contém apenas declarações relacionadas ao cliente do proxy, tornando o proxy completamente transparente para o receptor do token emitido. Observe que o WIF não dá suporte a \<wsse:SecurityTokenReference> nem a \<wsa:EndpointReferences> como filho de \<wst:OnBehalfOf>.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-133">The resulting token contains only claims related to the client of the proxy, making the proxy completely transparent to the receiver of the issued token.Note that WIF does not support \<wsse:SecurityTokenReference> or \<wsa:EndpointReferences> as a child of \<wst:OnBehalfOf>.</span></span> <span data-ttu-id="ea2b7-134">A especificação WS-Trust permite três maneiras de identificar o solicitante original (em nome de quem o proxy está atuando).</span><span class="sxs-lookup"><span data-stu-id="ea2b7-134">The WS-Trust specification allows for three ways to identify the original requestor (on behalf of whom the proxy is acting).</span></span> <span data-ttu-id="ea2b7-135">Elas são:</span><span class="sxs-lookup"><span data-stu-id="ea2b7-135">These are:</span></span>
>
> - <span data-ttu-id="ea2b7-136">Referência de token de segurança.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-136">Security token reference.</span></span> <span data-ttu-id="ea2b7-137">Uma referência a um token, na mensagem ou possivelmente recuperada fora de banda.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-137">A reference to a token, either in the message, or possibly retrieved out of band).</span></span>
> - <span data-ttu-id="ea2b7-138">Referência de ponto de extremidade.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-138">Endpoint reference.</span></span> <span data-ttu-id="ea2b7-139">Usada como uma chave para pesquisar dados, novamente fora de banda.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-139">Used as a key to look up data, again out of band.</span></span>
> - <span data-ttu-id="ea2b7-140">Token de segurança.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-140">Security token.</span></span> <span data-ttu-id="ea2b7-141">Identifica o solicitante original diretamente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-141">Identifies the original requestor directly.</span></span>
>
> <span data-ttu-id="ea2b7-142">O WIF dá suporte apenas a tokens de segurança, criptografados ou não criptografados, como um elemento filho direto de \<wst:OnBehalfOf>.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-142">WIF supports only security tokens, either encrypted or unencrypted, as a direct child element of \<wst:OnBehalfOf>.</span></span>

 <span data-ttu-id="ea2b7-143">Essas informações são transmitidas para um emissor do WS-Trust usando os elementos de token ActAs e OnBehalfOf no RST.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-143">This information is conveyed to a WS-Trust issuer using the ActAs and OnBehalfOf token elements in the RST.</span></span>

 <span data-ttu-id="ea2b7-144">O WCF expõe um ponto de extensibilidade na associação que permite que os elementos XML arbitrários sejam adicionados ao RST.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-144">WCF exposes an extensibility point on the binding that allows arbitrary XML elements to be added to the RST.</span></span> <span data-ttu-id="ea2b7-145">No entanto, como o ponto de extensibilidade está vinculado à associação, os cenários que exigem que o conteúdo do RST varie por chamada devem recriar o cliente para cada chamada, o que diminui o desempenho.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-145">However, because the extensibility point is tied to the binding, scenarios that require the RST contents to vary per call must re-create the client for every call, which decreases performance.</span></span> <span data-ttu-id="ea2b7-146">O WIF usa métodos de extensão na classe `ChannelFactory` para permitir aos desenvolvedores anexar qualquer token obtido fora de banda ao RST.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-146">WIF uses extension methods on the `ChannelFactory` class to allow developers to attach any token that is obtained out of band to the RST.</span></span> <span data-ttu-id="ea2b7-147">O exemplo de código a seguir mostra como usar um token que representa o cliente (como um X.509, nome de usuário ou token SAML [Security Assertion Markup Language]) e anexá-lo ao RST enviado para o emissor.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-147">The following code example shows how to take a token that represents the client (such as an X.509, username, or Security Assertion Markup Language (SAML) token) and attach it to the RST that is sent to the issuer.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelActingAs<IHelloService>(clientSamlToken);
serviceChannel.Hello("Hi!");
```

 <span data-ttu-id="ea2b7-148">O WIF oferece os seguintes benefícios:</span><span class="sxs-lookup"><span data-stu-id="ea2b7-148">WIF provides the following benefits:</span></span>

- <span data-ttu-id="ea2b7-149">O RST pode ser modificado por canal. Portanto, os serviços de camada intermediária não precisam recriar a fábrica de canais para cada cliente, o que melhora o desempenho.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-149">The RST can be modified per channel; therefore, middle-tier services do not have to re-create the channel factory for each client, which improves performance.</span></span>

- <span data-ttu-id="ea2b7-150">Isso funciona com os clientes WCF existentes, que possibilita um caminho de upgrade fácil para os serviços de camada intermediária do WCF existentes que desejam habilitar a semântica de delegação de identidade.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-150">This works with existing WCF clients, which makes an easy upgrade path possible for existing WCF middle-tier services that want to enable identity delegation semantics.</span></span>

 <span data-ttu-id="ea2b7-151">No entanto, ainda não há nenhuma visibilidade na comunicação do cliente com o STS.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-151">However, there is still no visibility into the client’s communication with the STS.</span></span> <span data-ttu-id="ea2b7-152">Examinaremos isso no terceiro cenário.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-152">We’ll examine this in the third scenario.</span></span>

## <a name="communicating-directly-with-an-issuer-and-using-the-issued-token-to-authenticate"></a><span data-ttu-id="ea2b7-153">Comunicando-se diretamente com um emissor e usando o token emitido para autenticação</span><span class="sxs-lookup"><span data-stu-id="ea2b7-153">Communicating Directly with an Issuer and Using the Issued Token to Authenticate</span></span>
<span data-ttu-id="ea2b7-154">Para alguns cenários avançados, a melhoria de um cliente WCF não é suficiente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-154">For some advanced scenarios, enhancing a WCF client is not enough.</span></span> <span data-ttu-id="ea2b7-155">Os desenvolvedores que usam apenas o WCF normalmente usam contratos de Mensagem de Entrada/Mensagem de Saída e administram a análise do lado do cliente do emissor manualmente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-155">Developers who use only WCF typically use Message In / Message Out contracts and handle client-side parsing of the issuer response manually.</span></span>

<span data-ttu-id="ea2b7-156">O WIF introduz as classes <xref:System.ServiceModel.Security.WSTrustChannelFactory> e <xref:System.ServiceModel.Security.WSTrustChannel> para permitir que o cliente se comunique diretamente com um emissor do WS-Trust.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-156">WIF introduces the <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes to let the client communicate directly with a WS-Trust issuer.</span></span> <span data-ttu-id="ea2b7-157">As classes <xref:System.ServiceModel.Security.WSTrustChannelFactory> e <xref:System.ServiceModel.Security.WSTrustChannel> permitem que objetos RST e RSTR fortemente tipados fluam entre o cliente e o emissor, conforme mostrado no exemplo de código a seguir.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-157">The <xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> classes enable strongly typed RST and RSTR objects to flow between the client and issuer, as shown in the following code example.</span></span>

```csharp
WSTrustChannelFactory trustChannelFactory = new WSTrustChannelFactory(stsBinding, stsAddress);
WSTrustChannel channel = (WSTrustChannel) trustChannelFactory.CreateChannel();
RequestSecurityToken rst = new RequestSecurityToken(RequestTypes.Issue);
rst.AppliesTo = new EndpointAddress(serviceAddress);
RequestSecurityTokenResponse rstr = null;
SecurityToken token = channel.Issue(rst, out rstr);
```

<span data-ttu-id="ea2b7-158">Observe que o parâmetro `out` no método <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> permite o acesso ao RSTR para a inspeção do lado do cliente.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-158">Note that the `out` parameter on the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method allows access to the RSTR for client-side inspection.</span></span>

<span data-ttu-id="ea2b7-159">Até agora, você apenas viu como obter um token.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-159">So far, you’ve only seen how to obtain a token.</span></span> <span data-ttu-id="ea2b7-160">O token retornado do objeto <xref:System.ServiceModel.Security.WSTrustChannel> é um `GenericXmlSecurityToken` que contém todas as informações necessárias para a autenticação em uma terceira parte confiável.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-160">The token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel> object is a `GenericXmlSecurityToken` that contains all of the information that is necessary for authentication to a relying party.</span></span> <span data-ttu-id="ea2b7-161">O exemplo de código a seguir mostra como usar esse token.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-161">The following code example shows how to use this token.</span></span>

```csharp
IHelloService serviceChannel = channelFactory.CreateChannelWithIssuedToken<IHelloService>( token );
serviceChannel.Hello("Hi!");
```

<span data-ttu-id="ea2b7-162">O método de extensão <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> no objeto `ChannelFactory` indica ao WIF que você obteve um token fora de banda e que ele deve interromper a chamada normal do WCF ao emissor e, em vez disso, usar o token obtido para se autenticar na terceira parte confiável.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-162">The <xref:System.ServiceModel.ChannelFactory%601.CreateChannelWithIssuedToken%2A> extension method on the `ChannelFactory` object indicates to WIF that you have obtained a token out of band, and that it should stop the normal WCF call to the issuer and instead use the token that you obtained to authenticate to the relying party.</span></span> <span data-ttu-id="ea2b7-163">Isso traz os seguintes benefícios:</span><span class="sxs-lookup"><span data-stu-id="ea2b7-163">This has the following benefits:</span></span>

- <span data-ttu-id="ea2b7-164">Proporciona controle total sobre o processo de emissão de token.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-164">It gives you complete control over the token issuance process.</span></span>

- <span data-ttu-id="ea2b7-165">Dá suporte a cenários de ActAs/OnBehalfOf com a configuração direta dessas propriedades no RST de saída.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-165">It supports ActAs / OnBehalfOf scenarios by directly setting these properties on the outgoing RST.</span></span>

- <span data-ttu-id="ea2b7-166">Permite que decisões dinâmicas de confiança do lado do cliente sejam feitas com base no conteúdo do RSTR.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-166">It enables dynamic client-side trust decisions to be made based on the contents of the RSTR.</span></span>

- <span data-ttu-id="ea2b7-167">Permite armazenar em cache e reutilizar o token retornado do método <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A>.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-167">It lets you cache and reuse the token that is returned from the <xref:System.ServiceModel.Security.WSTrustChannel.Issue%2A> method.</span></span>

- <span data-ttu-id="ea2b7-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> e <xref:System.ServiceModel.Security.WSTrustChannel> permitem o controle do cache de canal, de falhas e da semântica de recuperação de acordo com as melhores práticas do WCF.</span><span class="sxs-lookup"><span data-stu-id="ea2b7-168"><xref:System.ServiceModel.Security.WSTrustChannelFactory> and <xref:System.ServiceModel.Security.WSTrustChannel> allow for control of channel caching, fault, and recovery semantics according to WCF best practices.</span></span>

## <a name="see-also"></a><span data-ttu-id="ea2b7-169">Consulte também</span><span class="sxs-lookup"><span data-stu-id="ea2b7-169">See also</span></span>

- [<span data-ttu-id="ea2b7-170">Recursos do WIF</span><span class="sxs-lookup"><span data-stu-id="ea2b7-170">WIF Features</span></span>](../../../docs/framework/security/wif-features.md)