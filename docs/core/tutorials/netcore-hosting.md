---
title: Hospedando o .NET Core
description: "Hospedando o tempo de execução do .NET Core com base no código nativo"
keywords: .NET, .NET Core, Hospedagem, Hospedando o .NET Core
author: mjrousos
ms.author: mikerou
ms.date: 2/3/2017
ms.topic: article
ms.prod: .net-core
ms.devlang: dotnet
ms.assetid: 13edec8b-614d-47ed-9e95-ed6d3b94ec0c
ms.openlocfilehash: 1f0983b909244dda7270d3eff01dc302383639a5
ms.sourcegitcommit: bd1ef61f4bb794b25383d3d72e71041a5ced172e
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 10/18/2017
---
# <a name="hosting-net-core"></a><span data-ttu-id="9a76e-104">Hospedando o .NET Core</span><span class="sxs-lookup"><span data-stu-id="9a76e-104">Hosting .NET Core</span></span>

<span data-ttu-id="9a76e-105">Como todo código gerenciado, os aplicativos .NET Core são executados por um host.</span><span class="sxs-lookup"><span data-stu-id="9a76e-105">Like all managed code, .NET Core applications are executed by a host.</span></span> <span data-ttu-id="9a76e-106">O host é responsável por iniciar o tempo de execução (incluindo componentes como JIT e coletor de lixo), criar AppDomains e invocar pontos de entrada gerenciados.</span><span class="sxs-lookup"><span data-stu-id="9a76e-106">The host is responsible for starting the runtime (including components like the JIT and garbage collector), creating AppDomains, and invoking managed entry points.</span></span>

<span data-ttu-id="9a76e-107">Hospedar o tempo de execução do .NET Core é um cenário avançado e, na maioria dos casos, os desenvolvedores do .NET Core não precisam se preocupar com a hospedagem, pois os processos de build do .NET Core fornecem um host padrão para executar aplicativos .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9a76e-107">Hosting the .NET Core runtime is an advanced scenario and, in most cases, .NET Core developers don't need to worry about hosting because .NET Core build processes provide a default host to run .NET Core applications.</span></span> <span data-ttu-id="9a76e-108">No entanto, em algumas circunstâncias especializadas, pode ser útil hospedar explicitamente o tempo de execução do .NET Core, como uma maneira de invocar o código gerenciado em um processo nativo ou para ter mais controle sobre o funcionamento do tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="9a76e-108">In some specialized circumstances, though, it can be useful to explicitly host the .NET Core runtime, either as a means of invoking managed code in a native process or in order to gain more control over how the runtime works.</span></span>

<span data-ttu-id="9a76e-109">Este artigo fornece uma visão geral das etapas necessárias para iniciar o tempo de execução do .NET Core com base no código nativo, criar um domínio do aplicativo inicial (<xref:System.AppDomain>) e executar o código gerenciado nele.</span><span class="sxs-lookup"><span data-stu-id="9a76e-109">This article gives an overview of the steps necessary to start the .NET Core runtime from native code, create an initial application domain (<xref:System.AppDomain>), and execute managed code in it.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="9a76e-110">Pré-requisitos</span><span class="sxs-lookup"><span data-stu-id="9a76e-110">Prerequisites</span></span>

<span data-ttu-id="9a76e-111">Como os hosts são aplicativos nativos, este tutorial abordará a construção de um aplicativo C++ para hospedar o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="9a76e-111">Because hosts are native applications, this tutorial will cover constructing a C++ application to host .NET Core.</span></span> <span data-ttu-id="9a76e-112">Você precisará de um ambiente de desenvolvimento do C++ (como aquele fornecido pelo [Visual Studio](https://www.visualstudio.com/downloads/)).</span><span class="sxs-lookup"><span data-stu-id="9a76e-112">You will need a C++ development environment (such as that provided by [Visual Studio](https://www.visualstudio.com/downloads/)).</span></span>

<span data-ttu-id="9a76e-113">Você também desejará ter um aplicativo .NET Core simples com o qual testará o host e, portanto, deverá instalar o [SDK do .NET Core](https://www.microsoft.com/net/core) e [compilar um aplicativo .NET Core de teste pequeno](../../core/tutorials/with-visual-studio.md) (como um aplicativo “Olá, Mundo”).</span><span class="sxs-lookup"><span data-stu-id="9a76e-113">You will also want a simple .NET Core application to test the host with, so you should install the [.NET Core SDK](https://www.microsoft.com/net/core) and [build a small .NET Core test app](../../core/tutorials/with-visual-studio.md) (such as a 'Hello World' app).</span></span> <span data-ttu-id="9a76e-114">O aplicativo “Olá, Mundo” criado pelo novo modelo de projeto de console do .NET Core é suficiente.</span><span class="sxs-lookup"><span data-stu-id="9a76e-114">The 'Hello World' app created by the new .NET Core console project template is sufficient.</span></span>

<span data-ttu-id="9a76e-115">Este tutorial e seu exemplo associado criam um host do Windows, consulte as notas ao final deste artigo sobre a hospedagem no UNIX.</span><span class="sxs-lookup"><span data-stu-id="9a76e-115">This tutorial and its associated sample build a Windows host; see the notes at the end of this article about hosting on Unix.</span></span>

## <a name="creating-the-host"></a><span data-ttu-id="9a76e-116">Criando o host</span><span class="sxs-lookup"><span data-stu-id="9a76e-116">Creating the host</span></span>

<span data-ttu-id="9a76e-117">Um [host de exemplo](https://github.com/dotnet/docs/tree/master/samples/core/hosting) que demonstra as etapas descritas neste artigo está disponível no repositório dotnet/docs do GitHub.</span><span class="sxs-lookup"><span data-stu-id="9a76e-117">A [sample host](https://github.com/dotnet/docs/tree/master/samples/core/hosting) demonstrating the steps outlined in this article is available in the dotnet/docs GitHub repository.</span></span> <span data-ttu-id="9a76e-118">Os comentários no arquivo *host.cpp* do exemplo associam claramente as etapas numeradas deste tutorial aos pontos em que são executadas no exemplo.</span><span class="sxs-lookup"><span data-stu-id="9a76e-118">Comments in the sample's *host.cpp* file clearly associate the numbered steps from this tutorial with where they're performed in the sample.</span></span> <span data-ttu-id="9a76e-119">Para obter instruções de download, consulte [Exemplos e tutoriais](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span><span class="sxs-lookup"><span data-stu-id="9a76e-119">For download instructions, see [Samples and Tutorials](../../samples-and-tutorials/index.md#viewing-and-downloading-samples).</span></span>

<span data-ttu-id="9a76e-120">Lembre-se de que o host de exemplo destina-se a ser usado para fins de aprendizado e, portanto, não leva a sério a verificação de erros e é projetado para enfatizar a legibilidade em detrimento da eficiência.</span><span class="sxs-lookup"><span data-stu-id="9a76e-120">Keep in mind that the sample host is meant to be used for learning purposes, so it is light on error checking and is designed to emphasize readability over efficiency.</span></span> <span data-ttu-id="9a76e-121">Mais amostras de host do mundo real estão disponíveis no repositório [dotnet/coreclr](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts).</span><span class="sxs-lookup"><span data-stu-id="9a76e-121">More real-world host samples are available in the [dotnet/coreclr](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts) repository.</span></span> <span data-ttu-id="9a76e-122">O [host CoreRun](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun), em particular, é um bom host de uso geral para ser estudado depois de ler toda a amostra mais simples.</span><span class="sxs-lookup"><span data-stu-id="9a76e-122">The [CoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts/corerun), in particular, is a good general-purpose host to study after reading through the simpler sample.</span></span>

### <a name="a-note-about-mscoreeh"></a><span data-ttu-id="9a76e-123">Uma observação sobre mscoree.h</span><span class="sxs-lookup"><span data-stu-id="9a76e-123">A note about mscoree.h</span></span>
<span data-ttu-id="9a76e-124">A interface de hospedagem primária do .NET Core (`ICLRRuntimeHost2`) é definida em [MSCOREE.IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span><span class="sxs-lookup"><span data-stu-id="9a76e-124">The primary .NET Core hosting interface (`ICLRRuntimeHost2`) is defined in [MSCOREE.IDL](https://github.com/dotnet/coreclr/blob/master/src/inc/MSCOREE.IDL).</span></span> <span data-ttu-id="9a76e-125">Uma versão de cabeçalho deste arquivo (mscoree.h), que o host precisará referenciar, é produzida por meio da MIDL durante o build do [tempo de execução do .NET Core](https://github.com/dotnet/coreclr/).</span><span class="sxs-lookup"><span data-stu-id="9a76e-125">A header version of this file (mscoree.h), which your host will need to reference, is produced via MIDL when the [.NET Core runtime](https://github.com/dotnet/coreclr/) is built.</span></span> <span data-ttu-id="9a76e-126">Se você não desejar compilar o tempo de execução do .NET Core, mscoree.h também estará disponível como um [cabeçalho predefinido](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) no repositório dotnet/coreclr.</span><span class="sxs-lookup"><span data-stu-id="9a76e-126">If you do not want to build the .NET Core runtime, mscoree.h is also available as a [pre-built header](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc) in the dotnet/coreclr repository.</span></span> <span data-ttu-id="9a76e-127">Encontre [instruções sobre como compilar o tempo de execução do .NET Core](https://github.com/dotnet/coreclr#building-the-repository) no repositório GitHub.</span><span class="sxs-lookup"><span data-stu-id="9a76e-127">[Instructions on building the .NET Core runtime](https://github.com/dotnet/coreclr#building-the-repository) can be found in its GitHub repository.</span></span> 

### <a name="step-1---identify-the-managed-entry-point"></a><span data-ttu-id="9a76e-128">Etapa 1 – Identificar o ponto de entrada gerenciado</span><span class="sxs-lookup"><span data-stu-id="9a76e-128">Step 1 - Identify the managed entry point</span></span>
<span data-ttu-id="9a76e-129">Depois de referenciar os cabeçalhos necessários ([mscoree.h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) e stdio.h, por exemplo), uma das primeiras coisas que um host do .NET Core deverá fazer é localizar o ponto de entrada gerenciado que será usado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-129">After referencing necessary headers ([mscoree.h](https://github.com/dotnet/coreclr/tree/master/src/pal/prebuilt/inc/mscoree.h) and stdio.h, for example), one of the first things a .NET Core host must do is locate the managed entry point it will be using.</span></span> <span data-ttu-id="9a76e-130">Em nosso host de exemplo, isso é feito apenas usando o primeiro argumento de linha de comando de nosso host como o caminho para um binário gerenciado cujo método `main` será executado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-130">In our sample host, this is done by just taking the first command line argument to our host as the path to a managed binary whose `main` method will be executed.</span></span>

[!code-cpp[NetCoreHost#1](../../../samples/core/hosting/host.cpp#1)]

### <a name="step-2---find-and-load-coreclrdll"></a><span data-ttu-id="9a76e-131">Etapa 2 – Localizar e carregar CoreCLR.dll</span><span class="sxs-lookup"><span data-stu-id="9a76e-131">Step 2 - Find and load CoreCLR.dll</span></span>
<span data-ttu-id="9a76e-132">As APIs do tempo de execução do .NET Core estão localizadas em *CoreCLR.dll* (no Windows).</span><span class="sxs-lookup"><span data-stu-id="9a76e-132">The .NET Core runtime APIs are in *CoreCLR.dll* (on Windows).</span></span> <span data-ttu-id="9a76e-133">Para obter nossa interface de hospedagem (`ICLRRuntimeHost2`), é necessário localizar e carregar *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="9a76e-133">To get our hosting interface (`ICLRRuntimeHost2`), it's necessary to find and load *CoreCLR.dll*.</span></span> <span data-ttu-id="9a76e-134">É responsabilidade do host definir uma convenção de como ele localizará *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="9a76e-134">It is up to the host to define a convention for how it will locate *CoreCLR.dll*.</span></span> <span data-ttu-id="9a76e-135">Alguns hosts esperam que o arquivo esteja presente em um local conhecido em todo o computador (como %programfiles%\dotnet\shared\Microsoft.NETCore.App\1.1.0).</span><span class="sxs-lookup"><span data-stu-id="9a76e-135">Some hosts expect the file to be present in a well-known machine-wide location (such as %programfiles%\dotnet\shared\Microsoft.NETCore.App\1.1.0).</span></span> <span data-ttu-id="9a76e-136">Outros esperam que *CoreCLR.dll* seja carregado de um local próximo do próprio host ou do aplicativo a ser hospedado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-136">Others expect that *CoreCLR.dll* will be loaded from a location next to either the host itself or the app to be hosted.</span></span> <span data-ttu-id="9a76e-137">Outros ainda poderão consultar uma variável de ambiente para localizar a biblioteca.</span><span class="sxs-lookup"><span data-stu-id="9a76e-137">Still others might consult an environment variable to find the library.</span></span>

<span data-ttu-id="9a76e-138">No Linux ou no Mac, a biblioteca básica em tempo de execução é *libcoreclr.so* ou *libcoreclr.dylib*, respectivamente.</span><span class="sxs-lookup"><span data-stu-id="9a76e-138">On Linux or Mac, the core runtime library is *libcoreclr.so* or *libcoreclr.dylib*, respectively.</span></span>

<span data-ttu-id="9a76e-139">Nosso host de exemplo investiga alguns locais comuns para *CoreCLR.dll*.</span><span class="sxs-lookup"><span data-stu-id="9a76e-139">Our sample host probes a few common locations for *CoreCLR.dll*.</span></span> <span data-ttu-id="9a76e-140">Depois de encontrado, ele deverá ser carregado por meio de `LoadLibrary` (ou `dlopen` no Linux/Mac).</span><span class="sxs-lookup"><span data-stu-id="9a76e-140">Once found, it must be loaded via `LoadLibrary` (or `dlopen` on Linux/Mac).</span></span>

[!code-cpp[NetCoreHost#2](../../../samples/core/hosting/host.cpp#2)]

### <a name="step-3---get-an-iclrruntimehost2-instance"></a><span data-ttu-id="9a76e-141">Etapa 3 – Obter uma instância de ICLRRuntimeHost2</span><span class="sxs-lookup"><span data-stu-id="9a76e-141">Step 3 - Get an ICLRRuntimeHost2 Instance</span></span>
<span data-ttu-id="9a76e-142">A interface de hospedagem `ICLRRuntimeHost2` é recuperada pela chamada a `GetProcAddress` (ou `dlsym` no Linux/Mac) em `GetCLRRuntimeHost` e, em seguida, pela invocação dessa função.</span><span class="sxs-lookup"><span data-stu-id="9a76e-142">The `ICLRRuntimeHost2` hosting interface is retrieved by calling `GetProcAddress` (or `dlsym` on Linux/Mac) on `GetCLRRuntimeHost`, and then invoking that function.</span></span> 

[!code-cpp[NetCoreHost#3](../../../samples/core/hosting/host.cpp#3)]

### <a name="step-4---setting-startup-flags-and-starting-the-runtime"></a><span data-ttu-id="9a76e-143">Etapa 4 – Configurando sinalizadores de inicialização e iniciando o tempo de execução</span><span class="sxs-lookup"><span data-stu-id="9a76e-143">Step 4 - Setting startup flags and starting the runtime</span></span>
<span data-ttu-id="9a76e-144">Com um `ICLRRuntimeHost2` em mãos, agora podemos especificar sinalizadores de inicialização em todo o tempo de execução e iniciar o tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="9a76e-144">With an `ICLRRuntimeHost2` in-hand, we can now specify runtime-wide startup flags and start the runtime.</span></span> <span data-ttu-id="9a76e-145">Os sinalizadores de inicialização determinarão qual GC (coletor de lixo) será usado (simultâneo ou de servidor), se usaremos um AppDomain único ou vários AppDomains e qual política de otimização do carregador será usada (para o carregamento de domínio neutro de assemblies).</span><span class="sxs-lookup"><span data-stu-id="9a76e-145">Startup flags will determine which garbage collector (GC) to use (concurrent or server), whether we will use a single AppDomain or multiple AppDomains, and what loader optimization policy to use (for domain-neutral loading of assemblies).</span></span>

[!code-cpp[NetCoreHost#4](../../../samples/core/hosting/host.cpp#4)]

<span data-ttu-id="9a76e-146">O tempo de execução é iniciado com uma chamada à função `Start`.</span><span class="sxs-lookup"><span data-stu-id="9a76e-146">The runtime is started with a call to the `Start` function.</span></span>

```C++
hr = runtimeHost->Start();
```

### <a name="step-5---preparing-appdomain-settings"></a><span data-ttu-id="9a76e-147">Etapa 5 – Preparando as configurações de AppDomain</span><span class="sxs-lookup"><span data-stu-id="9a76e-147">Step 5 - Preparing AppDomain settings</span></span>
<span data-ttu-id="9a76e-148">Depois que o tempo de execução for iniciado, é recomendável configurar um AppDomain.</span><span class="sxs-lookup"><span data-stu-id="9a76e-148">Once the runtime is started, we will want to set up an AppDomain.</span></span> <span data-ttu-id="9a76e-149">Há várias opções que devem ser especificadas durante a criação de um AppDomain do .NET. No entanto, é necessário prepará-lo primeiro.</span><span class="sxs-lookup"><span data-stu-id="9a76e-149">There are a number of options that must be specified when creating a .NET AppDomain, however, so it's necessary to prepare those first.</span></span>

<span data-ttu-id="9a76e-150">Os sinalizadores de AppDomain especificam comportamentos de AppDomain relacionados à segurança e interoperabilidade.</span><span class="sxs-lookup"><span data-stu-id="9a76e-150">AppDomain flags specify AppDomain behaviors related to security and interop.</span></span> <span data-ttu-id="9a76e-151">Os hosts mais antigos do Silverlight usavam essas configurações para restringir o código do usuário a uma área restrita, porém, os hosts do .NET Core mais modernos executam o código do usuário como confiança total e habilitam a interoperabilidade.</span><span class="sxs-lookup"><span data-stu-id="9a76e-151">Older Silverlight hosts used these settings to sandbox user code, but most modern .NET Core hosts run user code as full trust and enable interop.</span></span>

[!code-cpp[NetCoreHost#5](../../../samples/core/hosting/host.cpp#5)]

<span data-ttu-id="9a76e-152">Depois de decidir quais sinalizadores AppDomain serão usados, as propriedades de AppDomain deverão ser definidas.</span><span class="sxs-lookup"><span data-stu-id="9a76e-152">After deciding which AppDomain flags to use, AppDomain properties must be defined.</span></span> <span data-ttu-id="9a76e-153">As propriedades são pares chave-valor de cadeias de caracteres.</span><span class="sxs-lookup"><span data-stu-id="9a76e-153">The properties are key/value pairs of strings.</span></span> <span data-ttu-id="9a76e-154">Muitas das propriedades estão relacionadas a como o AppDomain carregará os assemblies.</span><span class="sxs-lookup"><span data-stu-id="9a76e-154">Many of the properties relate to how the AppDomain will load assemblies.</span></span>

<span data-ttu-id="9a76e-155">As propriedades comuns de AppDomain incluem:</span><span class="sxs-lookup"><span data-stu-id="9a76e-155">Common AppDomain properties include:</span></span>

* <span data-ttu-id="9a76e-156">`TRUSTED_PLATFORM_ASSEMBLIES` Essa é uma lista de caminhos de assembly (delimitada por “;” no Windows e por “:” no Unix) para os quais o AppDomain deverá priorizar o carregamento e fornecer confiança total (mesmo em domínios de confiança parcial).</span><span class="sxs-lookup"><span data-stu-id="9a76e-156">`TRUSTED_PLATFORM_ASSEMBLIES` This is a list of assembly paths (delimited by ';' on Windows and ':' on Unix) which the AppDomain should prioritize loading and give full trust to (even in partially-trusted domains).</span></span> <span data-ttu-id="9a76e-157">Essa lista deve conter assemblies “Framework” e outros módulos confiáveis, semelhante ao GAC em cenários do .NET Framework.</span><span class="sxs-lookup"><span data-stu-id="9a76e-157">This list is meant to contain 'Framework' assemblies and other trusted modules, similar to the GAC in .NET Framework scenarios.</span></span> <span data-ttu-id="9a76e-158">Alguns hosts colocarão uma biblioteca próxima a *coreclr.dll* nesta lista, enquanto outros têm manifestos embutidos em código que listam os assemblies confiáveis para suas finalidades.</span><span class="sxs-lookup"><span data-stu-id="9a76e-158">Some hosts will put any library next to *coreclr.dll* on this list, others have hard-coded manifests listing trusted assemblies for their purposes.</span></span>
* <span data-ttu-id="9a76e-159">`APP_PATHS` Essa é uma lista de caminhos a serem investigados quanto a um assembly se ele não for encontrado na lista de TPA (assemblies de plataforma confiáveis).</span><span class="sxs-lookup"><span data-stu-id="9a76e-159">`APP_PATHS` This is a list of paths to probe in for an assembly if it can't be found in the trusted platform assemblies (TPA) list.</span></span> <span data-ttu-id="9a76e-160">Esses caminhos devem ser os locais em que os assemblies dos usuários podem ser encontrados.</span><span class="sxs-lookup"><span data-stu-id="9a76e-160">These paths are meant to be the locations where users' assemblies can be found.</span></span> <span data-ttu-id="9a76e-161">Em um AppDomain em área restrita, os assemblies carregados desses caminhos só receberão confiança parcial.</span><span class="sxs-lookup"><span data-stu-id="9a76e-161">In a sandboxed AppDomain, assemblies loaded from these paths will only be granted partial trust.</span></span> <span data-ttu-id="9a76e-162">Caminhos APP_PATH comuns incluem o caminho do qual o aplicativo de destino foi carregado ou outros locais em que os ativos do usuário são conhecidos como dinâmicos.</span><span class="sxs-lookup"><span data-stu-id="9a76e-162">Common APP_PATH paths include the path the target app was loaded from or other locations where user assets are known to live.</span></span>
*  <span data-ttu-id="9a76e-163">`APP_NI_PATHS` Essa lista é muito semelhante a APP_PATHS, com exceção de que foi projetada para serem caminhos que serão investigados quanto a imagens nativas.</span><span class="sxs-lookup"><span data-stu-id="9a76e-163">`APP_NI_PATHS` This list is very similar to APP_PATHS except that it's meant to be paths that will be probed for native images.</span></span>
*  <span data-ttu-id="9a76e-164">`NATIVE_DLL_SEARCH_DIRECTORIES` Essa propriedade é uma lista de caminhos que o carregador deverá investigar ao procurar DLLs nativas chamadas por meio de p/invoke.</span><span class="sxs-lookup"><span data-stu-id="9a76e-164">`NATIVE_DLL_SEARCH_DIRECTORIES` This property is a list of paths the loader should probe when looking for native DLLs called via p/invoke.</span></span>
*  <span data-ttu-id="9a76e-165">`PLATFORM_RESOURCE_ROOTS` Essa lista inclui caminhos a serem investigados quanto a assemblies satélite de recursos (em subdiretórios específicos à cultura).</span><span class="sxs-lookup"><span data-stu-id="9a76e-165">`PLATFORM_RESOURCE_ROOTS` This list includes paths to probe in for resource satellite assemblies (in culture-specific sub-directories).</span></span>
*  <span data-ttu-id="9a76e-166">`AppDomainCompatSwitch` Essa cadeia de caracteres especifica quais particularidades de compatibilidade deverão ser usadas para assemblies sem um Moniker de Estrutura de Destino explícito (um atributo no nível de assembly que indica em qual Estrutura um assembly deve ser executado).</span><span class="sxs-lookup"><span data-stu-id="9a76e-166">`AppDomainCompatSwitch` This string specifies which compatibility quirks should be used for assemblies without an explicit Target Framework Moniker (an assembly-level attribute indicating which Framework an assembly is meant to run against).</span></span> <span data-ttu-id="9a76e-167">Normalmente, isso deve ser definido como `"UseLatestBehaviorWhenTFMNotSpecified"`, mas alguns hosts podem preferir obter particularidades de compatibilidade do Silverlight ou do Windows Phone mais antigas.</span><span class="sxs-lookup"><span data-stu-id="9a76e-167">Typically, this should be set to `"UseLatestBehaviorWhenTFMNotSpecified"` but some hosts may prefer to get older Silverlight or Windows Phone compatibility quirks, instead.</span></span>

<span data-ttu-id="9a76e-168">Em nosso [host de exemplo simples](https://github.com/dotnet/docs/tree/master/samples/core/hosting), essas propriedades são configuradas da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="9a76e-168">In our [simple sample host](https://github.com/dotnet/docs/tree/master/samples/core/hosting), these properties are set up as follows:</span></span>

[!code-cpp[NetCoreHost#6](../../../samples/core/hosting/host.cpp#6)]

### <a name="step-6---create-the-appdomain"></a><span data-ttu-id="9a76e-169">Etapa 6 – Criar o AppDomain</span><span class="sxs-lookup"><span data-stu-id="9a76e-169">Step 6 - Create the AppDomain</span></span>
<span data-ttu-id="9a76e-170">Depois que todos os sinalizadores e as propriedades de AppDomain forem preparados, `ICLRRuntimeHost2::CreateAppDomainWithManager` poderá ser usado para configurar o AppDomain.</span><span class="sxs-lookup"><span data-stu-id="9a76e-170">Once all AppDomain flags and properties are prepared, `ICLRRuntimeHost2::CreateAppDomainWithManager` can be used to set up the AppDomain.</span></span> <span data-ttu-id="9a76e-171">Opcionalmente, essa função usa um nome de assembly totalmente qualificado e um nome de tipo a ser usado como o gerenciador de AppDomain do domínio.</span><span class="sxs-lookup"><span data-stu-id="9a76e-171">This function optionally takes a fully qualified assembly name and type name to use as the domain's AppDomain manager.</span></span> <span data-ttu-id="9a76e-172">Um gerenciador de AppDomain pode permitir que um host controle alguns aspectos do comportamento de AppDomain e pode fornecer pontos de entrada para iniciar o código gerenciado se o host não pretende invocar o código do usuário diretamente.</span><span class="sxs-lookup"><span data-stu-id="9a76e-172">An AppDomain manager can allow a host to control some aspects of AppDomain behavior and may provide entry points for launching managed code if the host doesn't intend to invoke user code directly.</span></span>   

[!code-cpp[NetCoreHost#7](../../../samples/core/hosting/host.cpp#7)]

### <a name="step-7---run-managed-code"></a><span data-ttu-id="9a76e-173">Etapa 7 – Executar o código gerenciado</span><span class="sxs-lookup"><span data-stu-id="9a76e-173">Step 7 - Run managed code!</span></span>
<span data-ttu-id="9a76e-174">Com um AppDomain em execução, agora o host pode começar a execução do código gerenciado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-174">With an AppDomain up and running, the host can now start executing managed code.</span></span> <span data-ttu-id="9a76e-175">A maneira mais fácil de fazer isso é usar `ICLRRuntimeHost2::ExecuteAssembly` para invocar um método de ponto de entrada de um assembly gerenciado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-175">The easiest way to do this is to use `ICLRRuntimeHost2::ExecuteAssembly` to invoke a managed assembly's entry point method.</span></span> <span data-ttu-id="9a76e-176">Observe que essa função funciona apenas em cenários de domínio único.</span><span class="sxs-lookup"><span data-stu-id="9a76e-176">Note that this function only works in single-domain scenarios.</span></span>

[!code-cpp[NetCoreHost#8](../../../samples/core/hosting/host.cpp#8)]

<span data-ttu-id="9a76e-177">Outra opção, caso `ExecuteAssembly` não atenda às necessidades do host, é usar `CreateDelegate` para criar um ponteiro de função para um método estático gerenciado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-177">Another option, if `ExecuteAssembly` doesn't meet your host's needs, is to use `CreateDelegate` to create a function pointer to a static managed method.</span></span> <span data-ttu-id="9a76e-178">Isso exige que o host conheça a assinatura do método na qual ele está chamando (para criar o tipo de ponteiro de função), mas permite aos hosts a flexibilidade de invocar um código diferente de um ponto de entrada de um assembly.</span><span class="sxs-lookup"><span data-stu-id="9a76e-178">This requires the host to know the signature of the method it is calling into (in order to create the function pointer type) but allows hosts the flexibility to invoke code other than an assembly's entry point.</span></span>

```C++
void *pfnDelegate = NULL;
hr = runtimeHost->CreateDelegate(
  domainId,
  L"HW, Version=1.0.0.0, Culture=neutral",  // Target managed assembly
  L"ConsoleApplication.Program",            // Target managed type
  L"Main",                                  // Target entry point (static method)
  (INT_PTR*)&pfnDelegate);

((MainMethodFp*)pfnDelegate)(NULL);
```

### <a name="step-8---clean-up"></a><span data-ttu-id="9a76e-179">Etapa 8 – Limpar</span><span class="sxs-lookup"><span data-stu-id="9a76e-179">Step 8 - Clean up</span></span>
<span data-ttu-id="9a76e-180">Por fim, o host deverá executar uma limpeza descarregando AppDomains, interrompendo o tempo de execução e liberando a referência `ICLRRuntimeHost2`.</span><span class="sxs-lookup"><span data-stu-id="9a76e-180">Finally, the host should clean up after itself by unloading AppDomains, stopping the runtime, and releasing the `ICLRRuntimeHost2` reference.</span></span>

[!code-cpp[NetCoreHost#9](../../../samples/core/hosting/host.cpp#9)]

## <a name="about-hosting-net-core-on-unix"></a><span data-ttu-id="9a76e-181">Sobre a hospedagem do .NET Core no Unix</span><span class="sxs-lookup"><span data-stu-id="9a76e-181">About Hosting .NET Core on Unix</span></span>
<span data-ttu-id="9a76e-182">O .NET Core é um produto de plataforma cruzada, em execução em sistemas operacionais Windows, Linux e Mac.</span><span class="sxs-lookup"><span data-stu-id="9a76e-182">.NET Core is a cross-platform product, running on Windows, Linux, and Mac operating systems.</span></span> <span data-ttu-id="9a76e-183">No entanto, como aplicativos nativos, os hosts de diferentes plataformas terão algumas diferenças entre si.</span><span class="sxs-lookup"><span data-stu-id="9a76e-183">As native applications, though, hosts for different platforms will have some differences between them.</span></span> <span data-ttu-id="9a76e-184">O processo descrito acima de como usar `ICLRRuntimeHost2` para iniciar o tempo de execução, criar um AppDomain e executar o código gerenciado deve funcionar em qualquer sistema operacional com suporte.</span><span class="sxs-lookup"><span data-stu-id="9a76e-184">The process described above of using `ICLRRuntimeHost2` to start the runtime, create an AppDomain, and execute managed code, should work on any supported operating system.</span></span> <span data-ttu-id="9a76e-185">No entanto, as interfaces definidas em mscoree.h podem ser difíceis de serem trabalhadas em plataformas Unix, já que mscoree faz várias suposições do Win32.</span><span class="sxs-lookup"><span data-stu-id="9a76e-185">However, the interfaces defined in mscoree.h can be cumbersome to work with on Unix platforms since mscoree makes many Win32 assumptions.</span></span>

<span data-ttu-id="9a76e-186">Para facilitar a hospedagem em plataformas Unix, um conjunto de wrappers de API de hospedagem mais de plataforma neutra está disponível em [coreclrhost.h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h).</span><span class="sxs-lookup"><span data-stu-id="9a76e-186">To make hosting on Unix platforms easier, a set of more platform-neutral hosting API wrappers are available in [coreclrhost.h](https://github.com/dotnet/coreclr/blob/master/src/coreclr/hosts/inc/coreclrhost.h).</span></span>

<span data-ttu-id="9a76e-187">Veja um exemplo de como usar coreclrhost.h (em vez de usar mscoree.h diretamente) no [host UnixCoreRun](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts).</span><span class="sxs-lookup"><span data-stu-id="9a76e-187">An example of using coreclrhost.h (instead of mscoree.h directly) can be seen in the [UnixCoreRun host](https://github.com/dotnet/coreclr/tree/master/src/coreclr/hosts).</span></span> <span data-ttu-id="9a76e-188">As etapas para usar as APIs de coreclrhost.h para hospedar o tempo de execução são semelhantes às etapas de como usar mscoree.h:</span><span class="sxs-lookup"><span data-stu-id="9a76e-188">The steps to use the APIs from coreclrhost.h to host the runtime are similar to the steps when using mscoree.h:</span></span>

1. <span data-ttu-id="9a76e-189">Identifique o código gerenciado a ser executado (em parâmetros de linha de comando, por exemplo).</span><span class="sxs-lookup"><span data-stu-id="9a76e-189">Identify the managed code to execute (from command line parameters, for example).</span></span> 
2. <span data-ttu-id="9a76e-190">Carregue a biblioteca de CoreCLR.</span><span class="sxs-lookup"><span data-stu-id="9a76e-190">Load the CoreCLR library.</span></span>
    1. `dlopen("./libcoreclr.so", RTLD_NOW | RTLD_LOCAL);` 
3. <span data-ttu-id="9a76e-191">Obtenha ponteiros de função para as funções `coreclr_initialize`, `coreclr_create_delegate`, `coreclr_execute_assembly` e `coreclr_shutdown` de CoreCLR usando `dlsym`</span><span class="sxs-lookup"><span data-stu-id="9a76e-191">Get function pointers to CoreCLR's `coreclr_initialize`, `coreclr_create_delegate`, `coreclr_execute_assembly`, and `coreclr_shutdown` functions using `dlsym`</span></span>
    1. `coreclr_initialize_ptr coreclr_initialize = (coreclr_initialize_ptr)dlsym(coreclrLib, "coreclr_initialize");`
4. <span data-ttu-id="9a76e-192">Configure as propriedades de AppDomain (como a lista de TPA).</span><span class="sxs-lookup"><span data-stu-id="9a76e-192">Set up AppDomain properties (such as the TPA list).</span></span> <span data-ttu-id="9a76e-193">Isso é o mesmo que a etapa 5 do fluxo de trabalho de mscoree acima.</span><span class="sxs-lookup"><span data-stu-id="9a76e-193">This is the same as step 5 from the mscoree workflow, above.</span></span>
5. <span data-ttu-id="9a76e-194">Use `coreclr_initialize` para iniciar o tempo de execução e criar um AppDomain.</span><span class="sxs-lookup"><span data-stu-id="9a76e-194">Use `coreclr_initialize` to start the runtime and create an AppDomain.</span></span> <span data-ttu-id="9a76e-195">Isso também criará um ponteiro `hostHandle` que será usado em futuras chamadas de hospedagem.</span><span class="sxs-lookup"><span data-stu-id="9a76e-195">This will also create a `hostHandle` pointer that will be used in future hosting calls.</span></span>
    1. <span data-ttu-id="9a76e-196">Observe que essa função executa as funções das etapas 4 e 6 do fluxo de trabalho anterior.</span><span class="sxs-lookup"><span data-stu-id="9a76e-196">Note that this function performs the roles of both steps 4 and 6 from the previous workflow.</span></span> 
6. <span data-ttu-id="9a76e-197">Use `coreclr_execute_assembly` ou `coreclr_create_delegate` para executar o código gerenciado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-197">Use either `coreclr_execute_assembly` or `coreclr_create_delegate` to execute managed code.</span></span> <span data-ttu-id="9a76e-198">Essas funções são análogas às funções `ExecuteAssembly` e `CreateDelegate` de mscoree da etapa 7 do fluxo de trabalho anterior.</span><span class="sxs-lookup"><span data-stu-id="9a76e-198">These functions are analogous to mscoree's `ExecuteAssembly` and `CreateDelegate` functions from step 7 of the previous workflow.</span></span>
7. <span data-ttu-id="9a76e-199">Use `coreclr_shutdown` para descarregar o AppDomain e desligar o tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="9a76e-199">Use `coreclr_shutdown` to unload the AppDomain and shut down the runtime.</span></span> 

## <a name="conclusion"></a><span data-ttu-id="9a76e-200">Conclusão</span><span class="sxs-lookup"><span data-stu-id="9a76e-200">Conclusion</span></span>
<span data-ttu-id="9a76e-201">Depois de compilar o host, é possível testá-lo executando-o por meio da linha de comando e passando argumentos (como o aplicativo gerenciado a ser executado) esperados pelo host.</span><span class="sxs-lookup"><span data-stu-id="9a76e-201">Once your host is built, it can be tested by running it from the command line and passing any arguments (like the managed app to run) the host expects.</span></span> <span data-ttu-id="9a76e-202">Ao especificar o aplicativo .NET Core a ser executado pelo host, lembre-se de usar o arquivo .dll produzido por `dotnet build`.</span><span class="sxs-lookup"><span data-stu-id="9a76e-202">When specifying the .NET Core app for the host to run, be sure to use the .dll that is produced by `dotnet build`.</span></span> <span data-ttu-id="9a76e-203">Executáveis produzidos por `dotnet publish` para aplicativos independentes são, na verdade, o host padrão do .NET Core (para que o aplicativo possa ser iniciado diretamente na linha de comando nos principais cenários); o código de usuário é compilado em uma dll com o mesmo nome.</span><span class="sxs-lookup"><span data-stu-id="9a76e-203">Executables produced by `dotnet publish` for self-contained applications are actually the default .NET Core host (so that the app can be launched directly from the command line in mainline scenarios); user code is compiled into a dll of the same name.</span></span> 

<span data-ttu-id="9a76e-204">Se as coisas não funcionarem no começo, verifique novamente se *coreclr.dll* está disponível no local esperado pelo host, se todas as bibliotecas do Framework necessárias estão na lista de TPA e se o número de bit de CoreCLR (32 ou 64 bits) corresponde a como o host foi compilado.</span><span class="sxs-lookup"><span data-stu-id="9a76e-204">If things don't work initially, double-check that *coreclr.dll* is available in the location expected by the host, that all necessary Framework libraries are in the TPA list, and that CoreCLR's bitness (32- or 64-bit) matches how the host was built.</span></span>

<span data-ttu-id="9a76e-205">A hospedagem do tempo de execução do .NET Core é um cenário avançado do qual muitos desenvolvedores não precisarão. No entanto, para aqueles que precisam inicializar o código gerenciado de um processo nativo ou que precisam de mais controle sobre o comportamento do tempo de execução do .NET Core, ela pode ser muito útil.</span><span class="sxs-lookup"><span data-stu-id="9a76e-205">Hosting the .NET Core runtime is an advanced scenario that many developers won't require, but for those who need to launch managed code from a native process, or who need more control over the .NET Core runtime's behavior, it can be very useful.</span></span> <span data-ttu-id="9a76e-206">Como o .NET Core pode ser executado lado a lado consigo mesmo, é possível até mesmo criar hosts que inicializam e iniciam várias versões do tempo de execução do .NET Core e executam aplicativos em todos eles no mesmo processo.</span><span class="sxs-lookup"><span data-stu-id="9a76e-206">Because .NET Core is able to run side-by-side with itself, it's even possible to create hosts which initialize and start multiple versions of the .NET Core runtime and execute apps on all of them in the same process.</span></span> 
