---
title: "Implantação de aplicativos .NET Core com o Visual Studio"
description: "Aprenda a implantação de aplicativos .NET Core com o Visual Studio"
keywords: "Implantação do .NET e .NET Core"
author: rpetrusha
ms.author: ronpet
ms.date: 04/18/2017
ms.topic: article
ms.prod: .net-core
ms.devlang: dotnet
ms.assetid: 01049a21-fd50-4419-9ab2-0e4a2e091050
ms.workload: dotnetcore
ms.openlocfilehash: a2706aecb80f079e6e735310b09c1062a6953901
ms.sourcegitcommit: e7f04439d78909229506b56935a1105a4149ff3d
ms.translationtype: HT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/23/2017
---
# <a name="deploying-net-core-apps-with-visual-studio"></a><span data-ttu-id="5727d-104">Implantando aplicativos .NET Core com o Visual Studio</span><span class="sxs-lookup"><span data-stu-id="5727d-104">Deploying .NET Core apps with Visual Studio</span></span>

<span data-ttu-id="5727d-105">Você pode implantar um aplicativo .NET Core como uma *implantação dependente de estrutura*, que inclui os binários do seu aplicativo, mas depende da presença do .NET Core no sistema de destino, ou como uma *implantação autocontida*, que inclui os binários do seu aplicativo e do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="5727d-105">You can deploy a .NET Core application either as a *framework-dependent deployment*, which includes your application binaries but depends on the presence of .NET Core on the target system, or as a *self-contained deployment*, which includes both your application and .NET Core binaries.</span></span> <span data-ttu-id="5727d-106">Para obter uma visão geral da implantação de aplicativos .NET Core, consulte [Implantação de aplicativos .NET Core](index.md).</span><span class="sxs-lookup"><span data-stu-id="5727d-106">For an overview of .NET Core application deployment, see [.NET Core Application Deployment](index.md).</span></span>

<span data-ttu-id="5727d-107">As seções a seguir mostram como usar o Microsoft Visual Studio para criar os seguintes tipos de implantações:</span><span class="sxs-lookup"><span data-stu-id="5727d-107">The following sections show how to use Microsoft Visual Studio to create the following kinds of deployments:</span></span>

- <span data-ttu-id="5727d-108">Implantação dependente de estrutura</span><span class="sxs-lookup"><span data-stu-id="5727d-108">Framework-dependent deployment</span></span>
- <span data-ttu-id="5727d-109">Implantação dependente de estrutura com dependências de terceiros</span><span class="sxs-lookup"><span data-stu-id="5727d-109">Framework-dependent deployment with third-party dependencies</span></span>
- <span data-ttu-id="5727d-110">Implantação autocontida</span><span class="sxs-lookup"><span data-stu-id="5727d-110">Self-contained deployment</span></span>
- <span data-ttu-id="5727d-111">Implantação autocontida com dependências de terceiros</span><span class="sxs-lookup"><span data-stu-id="5727d-111">Self-contained deployment with third-party dependencies</span></span>

<span data-ttu-id="5727d-112">Para obter informações sobre como usar o Visual Studio para desenvolver aplicativos .NET Core, consulte [Pré-requisitos para .NET Core no Windows](../windows-prerequisites.md#prerequisites-with-visual-studio-2017).</span><span class="sxs-lookup"><span data-stu-id="5727d-112">For information on using Visual Studio to develop .NET Core applications, see [Prerequisites for .NET Core on Windows](../windows-prerequisites.md#prerequisites-with-visual-studio-2017).</span></span>

## <a name="framework-dependent-deployment"></a><span data-ttu-id="5727d-113">Implantação dependente de estrutura</span><span class="sxs-lookup"><span data-stu-id="5727d-113">Framework-dependent deployment</span></span>

<span data-ttu-id="5727d-114">Implantar uma implantação dependente de estrutura sem dependências de terceiros significa simplesmente compilar, testar e publicar o aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-114">Deploying a framework-dependent deployment with no third-party dependencies simply involves building, testing, and publishing the app.</span></span> <span data-ttu-id="5727d-115">Um exemplo simples criado em C# ilustra o processo.</span><span class="sxs-lookup"><span data-stu-id="5727d-115">A simple example written in C# illustrates the process.</span></span>  

1. <span data-ttu-id="5727d-116">Crie o projeto.</span><span class="sxs-lookup"><span data-stu-id="5727d-116">Create the project.</span></span>

   <span data-ttu-id="5727d-117">Selecione **Arquivo** > **Novo** > **Projeto**.</span><span class="sxs-lookup"><span data-stu-id="5727d-117">Select **File** > **New** > **Project**.</span></span> <span data-ttu-id="5727d-118">Na caixa de diálogo **Novo Projeto**, selecione **.NET Core** no painel de tipos de projeto **Instalados** e selecione o modelo **Aplicativo de Console (.NET Core)** no painel central.</span><span class="sxs-lookup"><span data-stu-id="5727d-118">In the **New Project** dialog, select **.NET Core** in the **Installed** project types pane, and select the **Console App (.NET Core)** template in the center pane.</span></span> <span data-ttu-id="5727d-119">Insira um nome de projeto, como "FDD" na caixa de texto **Nome**.</span><span class="sxs-lookup"><span data-stu-id="5727d-119">Enter a project name, such as "FDD", in the **Name** text box.</span></span> <span data-ttu-id="5727d-120">Selecione o botão **OK**.</span><span class="sxs-lookup"><span data-stu-id="5727d-120">Select the **OK** button.</span></span>

1. <span data-ttu-id="5727d-121">Adicione o código-fonte do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-121">Add the application's source code.</span></span>

   <span data-ttu-id="5727d-122">Abra o arquivo *Program.cs* no editor e substitua o código gerado automaticamente pelo código a seguir.</span><span class="sxs-lookup"><span data-stu-id="5727d-122">Open the *Program.cs* file in the editor and replace the auto-generated code with the following code.</span></span> <span data-ttu-id="5727d-123">Ele solicitará que o usuário insira texto e exibirá as palavras individuais inseridas pelo usuário.</span><span class="sxs-lookup"><span data-stu-id="5727d-123">It prompts the user to enter text and displays the individual words entered by the user.</span></span> <span data-ttu-id="5727d-124">Ele usa a expressão regular `\w+` para separar as palavras no texto de entrada.</span><span class="sxs-lookup"><span data-stu-id="5727d-124">It uses the regular expression `\w+` to separate the words in the input text.</span></span>

   [!code-csharp[deployment#1](../../../samples/snippets/core/deploying/deployment-example.cs)]

1. <span data-ttu-id="5727d-125">Crie um build de depuração do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-125">Create a Debug build of your app.</span></span>

   <span data-ttu-id="5727d-126">Selecione **Compilar** > **Compilar Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-126">Select **Build** > **Build Solution**.</span></span> <span data-ttu-id="5727d-127">Você também pode compilar e executar o build de Depuração do aplicativo selecionando **Depurar** > **Iniciar Depuração**.</span><span class="sxs-lookup"><span data-stu-id="5727d-127">You can also compile and run the Debug build of your application by selecting **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="5727d-128">Implante seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-128">Deploy your app.</span></span>

   <span data-ttu-id="5727d-129">Depois de ter depurado e testado o programa, crie os arquivos a serem implantados com seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-129">After you've debugged and tested the program, create the files to be deployed with your app.</span></span> <span data-ttu-id="5727d-130">Para publicar do Visual Studio, faça o seguinte:</span><span class="sxs-lookup"><span data-stu-id="5727d-130">To publish from Visual Studio, do the following:</span></span>

      1. <span data-ttu-id="5727d-131">Altere a configuração da solução de **Depuração** para **Lançamento** na barra de ferramentas para compilar uma versão de lançamento (em vez de uma de depuração) do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-131">Change the solution configuration from **Debug** to **Release** on the toolbar to build a Release (rather than a Debug) version of your app.</span></span>

      1. <span data-ttu-id="5727d-132">Clique com o botão direito do mouse no projeto (e não na solução) no **Gerenciador de Soluções** e selecione **Publicar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-132">Right-click on the project (not the solution) in **Solution Explorer**, and select **Publish**.</span></span>

      1. <span data-ttu-id="5727d-133">Na guia **Publicar**, selecione **Publicar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-133">In the **Publish** tab, select **Publish**.</span></span> <span data-ttu-id="5727d-134">O Visual Studio grava os arquivos que compõem seu aplicativo no sistema de arquivos local.</span><span class="sxs-lookup"><span data-stu-id="5727d-134">Visual Studio writes the files that comprise your application to the local file system.</span></span>

      1. <span data-ttu-id="5727d-135">A guia **Publicar** agora mostra um único perfil **FolderProfile**.</span><span class="sxs-lookup"><span data-stu-id="5727d-135">The **Publish** tab now shows a single profile, **FolderProfile**.</span></span> <span data-ttu-id="5727d-136">As configurações do perfil são mostradas na seção **Resumo** da guia.</span><span class="sxs-lookup"><span data-stu-id="5727d-136">The profile's configuration settings are shown in the **Summary** section of the tab.</span></span>

   <span data-ttu-id="5727d-137">Os arquivos resultantes são colocados em um diretório chamado `PublishOutput` que está em um subdiretório de *.\bin\release* do projeto.</span><span class="sxs-lookup"><span data-stu-id="5727d-137">The resulting files are placed in a directory named `PublishOutput` that is in a subdirectory of your project's *.\bin\release* subdirectory.</span></span>

<span data-ttu-id="5727d-138">Junto com os arquivos do aplicativo, o processo de publicação emite um arquivo de banco de dados do programa (.pdb) que contém informações de depuração sobre seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-138">Along with your application's files, the publishing process emits a program database (.pdb) file that contains debugging information about your app.</span></span> <span data-ttu-id="5727d-139">O arquivo é útil principalmente para exceções de depuração.</span><span class="sxs-lookup"><span data-stu-id="5727d-139">The file is useful primarily for debugging exceptions.</span></span> <span data-ttu-id="5727d-140">Você pode optar por não empacotá-lo com os arquivos do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-140">You can choose not to package it with your application's files.</span></span> <span data-ttu-id="5727d-141">No entanto, você deve salvá-lo no caso de desejar depurar o build de lançamento para seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-141">You should, however, save it in the event that you want to debug the Release build of your app.</span></span>

<span data-ttu-id="5727d-142">Implante o conjunto completo de arquivos de aplicativo da maneira que desejar.</span><span class="sxs-lookup"><span data-stu-id="5727d-142">Deploy the complete set of application files in any way you like.</span></span> <span data-ttu-id="5727d-143">Por exemplo, você pode empacotá-los em um arquivo zip, usar um simples comando `copy` ou implantá-los com qualquer pacote de instalação de sua escolha.</span><span class="sxs-lookup"><span data-stu-id="5727d-143">For example, you can package them in a Zip file, use a simple `copy` command, or deploy them with any installation package of your choice.</span></span> <span data-ttu-id="5727d-144">Uma vez instalado, os usuários podem executar seu aplicativo usando o comando `dotnet` e fornecendo o nome de arquivo de aplicativo, como `dotnet fdd.dll`.</span><span class="sxs-lookup"><span data-stu-id="5727d-144">Once installed, users can then execute your application by using the `dotnet` command and providing the application filename, such as `dotnet fdd.dll`.</span></span>

<span data-ttu-id="5727d-145">Além dos binários do aplicativo, o instalador deverá também agrupar o instalador da estrutura compartilhada ou procurar por ele como um pré-requisito como parte da instalação do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-145">In addition to the application binaries, your installer should also either bundle the shared framework installer or check for it as a prerequisite as part of the application installation.</span></span>  <span data-ttu-id="5727d-146">A instalação da estrutura compartilhada requer acesso de Administrador/raiz, pois se trata de todo o computador.</span><span class="sxs-lookup"><span data-stu-id="5727d-146">Installation of the shared framework requires Administrator/root access since it is machine-wide.</span></span>

## <a name="framework-dependent-deployment-with-third-party-dependencies"></a><span data-ttu-id="5727d-147">Implantação dependente de estrutura com dependências de terceiros</span><span class="sxs-lookup"><span data-stu-id="5727d-147">Framework-dependent deployment with third-party dependencies</span></span>

<span data-ttu-id="5727d-148">Implantar uma implantação dependente de estrutura com uma ou mais dependências de terceiros requer que todas as dependências estejam disponíveis para seu projeto.</span><span class="sxs-lookup"><span data-stu-id="5727d-148">Deploying a framework-dependent deployment with one or more third-party dependencies requires that any dependencies be available to your project.</span></span> <span data-ttu-id="5727d-149">As etapas adicionais a seguir são necessárias antes de ser possível compilar seu aplicativo:</span><span class="sxs-lookup"><span data-stu-id="5727d-149">The following additional steps are required before you can build your app:</span></span>

1. <span data-ttu-id="5727d-150">Use o **Gerenciador de Pacotes NuGet** para adicionar uma referência a um pacote NuGet ao projeto e se o pacote ainda não estiver disponível no sistema, instale-o.</span><span class="sxs-lookup"><span data-stu-id="5727d-150">Use the **NuGet Package Manager** to add a reference to a NuGet package to your project; and if the package is not already available on your system, install it.</span></span> <span data-ttu-id="5727d-151">Para abrir o gerenciador de pacotes, selecione **Ferramentas** > **Gerenciador de Pacotes NuGet** > **Gerenciar Pacotes NuGet para a Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-151">To open the package manager, select **Tools** > **NuGet Package Manager** > **Manage NuGet Packages for Solution**.</span></span>

1. <span data-ttu-id="5727d-152">Confirme se `Newtonsoft.Json` está instalado em seu sistema e, se não estiver, instale-o.</span><span class="sxs-lookup"><span data-stu-id="5727d-152">Confirm that `Newtonsoft.Json` is installed on your system and, if it is not, install it.</span></span> <span data-ttu-id="5727d-153">A guia **Instalados** lista os pacotes NuGet instalados no sistema.</span><span class="sxs-lookup"><span data-stu-id="5727d-153">The **Installed** tab lists NuGet packages installed on your system.</span></span> <span data-ttu-id="5727d-154">Se `Newtonsoft.Json` não estiver listado, selecione a guia **Procurar** e insira "Newtonsoft.Json" na caixa de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="5727d-154">If `Newtonsoft.Json` is not listed there, select the **Browse** tab and enter "Newtonsoft.Json" in the search box.</span></span> <span data-ttu-id="5727d-155">Selecione `Newtonsoft.Json` e, no painel direito, selecione seu projeto antes de selecionar **Instalar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-155">Select `Newtonsoft.Json` and, in the right pane, select your project before selecting **Install**.</span></span>

1. <span data-ttu-id="5727d-156">Se `Newtonsoft.Json` já estiver instalado no sistema, adicione-o ao projeto selecionando o projeto no painel direito da guia **Gerenciar Pacotes para a Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-156">If `Newtonsoft.Json` is already installed on your system, add it to your project by selecting your project in the right pane of hte **Manage Packages for Solution** tab.</span></span>

<span data-ttu-id="5727d-157">Observe que uma implantação dependente de estrutura com dependências de terceiros tem a mesma portabilidade que suas dependências de terceiros.</span><span class="sxs-lookup"><span data-stu-id="5727d-157">Note that a framework-dependent deployment with third-party dependencies is only as portable as its third-party dependencies.</span></span> <span data-ttu-id="5727d-158">Por exemplo, se uma biblioteca de terceiros der suporte apenas a macOS, o aplicativo não será portátil para sistemas Windows.</span><span class="sxs-lookup"><span data-stu-id="5727d-158">For example, if a third-party library only supports macOS, the app isn't portable to Windows systems.</span></span> <span data-ttu-id="5727d-159">Isso acontecerá se a dependência de terceiros em si depender do código nativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-159">This happens if the third-party dependency itself depends on native code.</span></span> <span data-ttu-id="5727d-160">Um bom exemplo disso é o [servidor Kestrel](http://docs.microsoft.com/aspnet/core/fundamentals/servers/kestrel), que requer uma dependência nativa no [libuv](https://github.com/libuv/libuv).</span><span class="sxs-lookup"><span data-stu-id="5727d-160">A good example of this is [Kestrel server](http://docs.microsoft.com/aspnet/core/fundamentals/servers/kestrel), which requires a native dependency on [libuv](https://github.com/libuv/libuv).</span></span> <span data-ttu-id="5727d-161">Quando uma FDD é criada para um aplicativo com esse tipo de dependência de terceiros, a saída publicada contém uma pasta para cada [RID (Identificador de Tempo de Execução)](../rid-catalog.md) que dá suporte a dependência nativa (e que existe em seu pacote NuGet).</span><span class="sxs-lookup"><span data-stu-id="5727d-161">When an FDD is created for an application with this kind of third-party dependency, the published output contains a folder for each [Runtime Identifier (RID)](../rid-catalog.md) that the native dependency supports (and that exists in its NuGet package).</span></span>

## <a name="simpleSelf"></a> <span data-ttu-id="5727d-162">Implantação autocontida sem dependências de terceiros</span><span class="sxs-lookup"><span data-stu-id="5727d-162">Self-contained deployment without third-party dependencies</span></span>

<span data-ttu-id="5727d-163">Implantar uma implantação autocontida sem dependências de terceiros inclui a criação do projeto, a modificação do arquivo *csproj*, a compilação, os testes e a publicação do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-163">Deploying a self-contained deployment with no third-party dependencies involves creating the project, modifying the *csproj* file, building, testing, and publishing the app.</span></span> <span data-ttu-id="5727d-164">Um exemplo simples criado em C# ilustra o processo.</span><span class="sxs-lookup"><span data-stu-id="5727d-164">A simple example written in C# illustrates the process.</span></span> 

1. <span data-ttu-id="5727d-165">Crie o projeto.</span><span class="sxs-lookup"><span data-stu-id="5727d-165">Create the project.</span></span>

   <span data-ttu-id="5727d-166">Selecione **Arquivo** > **Novo** > **Projeto**.</span><span class="sxs-lookup"><span data-stu-id="5727d-166">Select **File** > **New** > **Project**.</span></span> <span data-ttu-id="5727d-167">Na caixa de diálogo **Adicionar Novo Projeto**, selecione **.NET Core** no painel de tipos de projeto **Instalados** e selecione o modelo **Aplicativo de Console (.NET Core)** no painel central.</span><span class="sxs-lookup"><span data-stu-id="5727d-167">In the **Add New Project** dialog, select **.NET Core** in the **Installed** project types pane, and select the **Console App (.NET Core)** template in the center pane.</span></span> <span data-ttu-id="5727d-168">Insira um nome de projeto, como "SCD" na caixa de texto **Nome** e selecione o botão **OK**.</span><span class="sxs-lookup"><span data-stu-id="5727d-168">Enter a project name, such as "SCD", in the **Name** text box, and select the **OK** button.</span></span>

1. <span data-ttu-id="5727d-169">Adicione o código-fonte do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-169">Add the application's source code.</span></span>

   <span data-ttu-id="5727d-170">Abra o arquivo *Program.cs* no editor e substitua o código gerado automaticamente pelo código a seguir.</span><span class="sxs-lookup"><span data-stu-id="5727d-170">Open the *Program.cs* file in your editor, and replace the auto-generated code with the following code.</span></span> <span data-ttu-id="5727d-171">Ele solicitará que o usuário insira texto e exibirá as palavras individuais inseridas pelo usuário.</span><span class="sxs-lookup"><span data-stu-id="5727d-171">It prompts the user to enter text and displays the individual words entered by the user.</span></span> <span data-ttu-id="5727d-172">Ele usa a expressão regular `\w+` para separar as palavras no texto de entrada.</span><span class="sxs-lookup"><span data-stu-id="5727d-172">It uses the regular expression `\w+` to separate the words in the input text.</span></span>

   [!code-csharp[deployment#1](../../../samples/snippets/core/deploying/deployment-example.cs)]

1. <span data-ttu-id="5727d-173">Defina as plataformas às quais seu aplicativo se destinará.</span><span class="sxs-lookup"><span data-stu-id="5727d-173">Define the platforms that your app will target.</span></span>

   1. <span data-ttu-id="5727d-174">Clique com o botão direito do mouse no projeto (e não na solução) no **Gerenciador de Soluções** e selecione **Editar SCD.csproj**.</span><span class="sxs-lookup"><span data-stu-id="5727d-174">Right-click on your project (not the solution) In **Solution Explorer**, and select **Edit SCD.csproj**.</span></span>

   1. <span data-ttu-id="5727d-175">Crie uma marcação `<RuntimeIdentifiers>` na seção `<PropertyGroup>` de seu arquivo *csproj* que define as plataformas de destino do seu aplicativo e especifique o RID (identificador de tempo de execução) de cada plataforma que você selecionar.</span><span class="sxs-lookup"><span data-stu-id="5727d-175">Create a `<RuntimeIdentifiers>` tag in the `<PropertyGroup>` section of your *csproj* file that defines the platforms your app targets, and specify the runtime identifier (RID) of each platform that you target.</span></span> <span data-ttu-id="5727d-176">Observe que você também precisa adicionar um ponto e vírgula para separar os RIDs.</span><span class="sxs-lookup"><span data-stu-id="5727d-176">Note that you also need to add a semicolon to separate the RIDs.</span></span> <span data-ttu-id="5727d-177">Consulte o [Catálogo de Identificador de Tempo de Execução](../rid-catalog.md) para obter uma lista de identificadores de tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="5727d-177">See [Runtime IDentifier catalog](../rid-catalog.md) for a list of runtime identifiers.</span></span> 

   <span data-ttu-id="5727d-178">Por exemplo, o exemplo a seguir indica que o aplicativo é executado em sistemas operacionais Windows 10 de 64 bits e no sistema de operacional OS X Versão 10.11 de 64 bits.</span><span class="sxs-lookup"><span data-stu-id="5727d-178">For example, the following example indicates that the app runs on 64-bit Windows 10 operating systems and the 64-bit OS X Version 10.11 operating system.</span></span>

```xml
<PropertyGroup>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
</PropertyGroup>
```
   <span data-ttu-id="5727d-179">Observe que o elemento `<RuntimeIdentifiers>` pode entrar em qualquer `<PropertyGroup>` que você tenha em seu arquivo *csproj*.</span><span class="sxs-lookup"><span data-stu-id="5727d-179">Note that the `<RuntimeIdentifiers>` element can go into any `<PropertyGroup>` that you have in your *csproj* file.</span></span> <span data-ttu-id="5727d-180">Um arquivo *csproj* de exemplo completo aparece mais adiante nesta seção.</span><span class="sxs-lookup"><span data-stu-id="5727d-180">A complete sample *csproj* file appears later in this section.</span></span>

1. <span data-ttu-id="5727d-181">Crie um build de depuração do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-181">Create a Debug build of your app.</span></span>

   <span data-ttu-id="5727d-182">Selecione **Compilar** > **Compilar Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-182">Select **Build** > **Build Solution**.</span></span> <span data-ttu-id="5727d-183">Você também pode compilar e executar o build de Depuração do aplicativo selecionando **Depurar** > **Iniciar Depuração**.</span><span class="sxs-lookup"><span data-stu-id="5727d-183">You can also compile and run the Debug build of your application by selecting **Debug** > **Start Debugging**.</span></span>

1. <span data-ttu-id="5727d-184">Publique seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-184">Publish your app.</span></span>

   <span data-ttu-id="5727d-185">Depois de ter depurado e testado o programa, crie os arquivos a serem implantados com seu aplicativo para cada plataforma à qual ele se destina.</span><span class="sxs-lookup"><span data-stu-id="5727d-185">After you've debugged and tested the program, create the files to be deployed with your app for each platform that it targets.</span></span>

   <span data-ttu-id="5727d-186">Para publicar seu aplicativo do Visual Studio, faça o seguinte:</span><span class="sxs-lookup"><span data-stu-id="5727d-186">To publish your app from Visual Studio, do the following:</span></span>

      1. <span data-ttu-id="5727d-187">Altere a configuração da solução de **Depuração** para **Lançamento** na barra de ferramentas para compilar uma versão de lançamento (em vez de uma de depuração) do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-187">Change the solution configuration from **Debug** to **Release** on the toolbar to build a Release (rather than a Debug) version of your app.</span></span>

      1. <span data-ttu-id="5727d-188">Clique com o botão direito do mouse no projeto (e não na solução) no **Gerenciador de Soluções** e selecione **Publicar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-188">Right-click on the project (not the solution) in **Solution Explorer** and select **Publish**.</span></span> 

      1. <span data-ttu-id="5727d-189">Na guia **Publicar**, selecione **Publicar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-189">In the **Publish** tab, select **Publish**.</span></span> <span data-ttu-id="5727d-190">O Visual Studio grava os arquivos que compõem seu aplicativo no sistema de arquivos local.</span><span class="sxs-lookup"><span data-stu-id="5727d-190">Visual Studio writes the files that comprise your application to the local file system.</span></span>

      1. <span data-ttu-id="5727d-191">A guia **Publicar** agora mostra um único perfil **FolderProfile**.</span><span class="sxs-lookup"><span data-stu-id="5727d-191">The **Publish** tab now shows a single profile, **FolderProfile**.</span></span> <span data-ttu-id="5727d-192">As configurações do perfil são mostradas na seção **Resumo** da guia. **Tempo de Execução de Destino** identifica qual tempo de execução foi publicado e **Local de Destino** identifica o local em que os arquivos da implantação autocontida foram gravados.</span><span class="sxs-lookup"><span data-stu-id="5727d-192">The profile's configuration settings are shown in the **Summary** section of the tab. **Target Runtime** identifies which runtime has been published, and **Target Location** identifies where the files for the self-contained deployment were written.</span></span>

      1. <span data-ttu-id="5727d-193">Por padrão, o Visual Studio grava todos os arquivos publicados em um único diretório.</span><span class="sxs-lookup"><span data-stu-id="5727d-193">Visual Studio by default writes all published files to a single directory.</span></span> <span data-ttu-id="5727d-194">Para sua conveniência, é melhor criar perfis separados para cada tempo de execução de destino e colocar os arquivos publicados em um diretório específico da plataforma.</span><span class="sxs-lookup"><span data-stu-id="5727d-194">For convenience, it's best to create separate profiles for each target runtime and to place published files in a platform-specific directory.</span></span> <span data-ttu-id="5727d-195">Isso envolve a criação de um perfil de publicação separado para cada plataforma de destino.</span><span class="sxs-lookup"><span data-stu-id="5727d-195">This involves creating a separate publishing profile for each target platform.</span></span> <span data-ttu-id="5727d-196">Agora recompile o aplicativo para cada plataforma fazendo o seguinte:</span><span class="sxs-lookup"><span data-stu-id="5727d-196">So now rebuild the application for each platform by doing the following:</span></span>

         1. <span data-ttu-id="5727d-197">Selecione **Criar novo perfil** na caixa de diálogo **Publicar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-197">Select **Create new profile** in the **Publish** dialog.</span></span>

         1. <span data-ttu-id="5727d-198">Na caixa de diálogo **Escolher um destino de publicação**, altere o local de **Escolher uma pasta** para *bin\Release\PublishOutput\win10-x64*.</span><span class="sxs-lookup"><span data-stu-id="5727d-198">In the **Pick a publish target** dialog, change the **Choose a folder** location to *bin\Release\PublishOutput\win10-x64*.</span></span> <span data-ttu-id="5727d-199">Selecione **OK**.</span><span class="sxs-lookup"><span data-stu-id="5727d-199">Select **OK**.</span></span>

         1. <span data-ttu-id="5727d-200">Selecione o novo perfil (**FolderProfile1**) na lista de perfis e certifique-se de que o **Tempo de Execução de Destino** é `win10-x64`.</span><span class="sxs-lookup"><span data-stu-id="5727d-200">Select the new profile (**FolderProfile1**) in the list of profiles, and make sure that the **Target Runtime** is `win10-x64`.</span></span> <span data-ttu-id="5727d-201">Se não for, selecione **Configurações**.</span><span class="sxs-lookup"><span data-stu-id="5727d-201">If it isn't, select **Settings**.</span></span> <span data-ttu-id="5727d-202">Na caixa de diálogo **Configurações de Perfil**, altere o **Tempo de Execução de Destino** para `win10-x64` e selecione **Salvar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-202">In the **Profile Settings** dialog, change the **Target Runtime** to `win10-x64` and select **Save**.</span></span> <span data-ttu-id="5727d-203">Caso contrário, selecione **Cancelar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-203">Otherwise, select **Cancel**.</span></span>

         1. <span data-ttu-id="5727d-204">Selecione **Publicar** para publicar seu aplicativo para plataformas Windows 10 de 64 bits.</span><span class="sxs-lookup"><span data-stu-id="5727d-204">Select **Publish** to publish your app for 64-bit Windows 10 platforms.</span></span>

         1. <span data-ttu-id="5727d-205">Siga as etapas anteriores novamente para criar um perfil para a plataforma `osx.10.11-x64`.</span><span class="sxs-lookup"><span data-stu-id="5727d-205">Follow the previous steps again to create a profile for the `osx.10.11-x64` platform.</span></span> <span data-ttu-id="5727d-206">O **Local de Destino** é *bin\Release\PublishOutput\osx.10.11-x64* e o **Tempo de Execução de Destino** é `osx.10.11-x64`.</span><span class="sxs-lookup"><span data-stu-id="5727d-206">The **Target Location** is *bin\Release\PublishOutput\osx.10.11-x64*, and the **Target Runtime** is `osx.10.11-x64`.</span></span> <span data-ttu-id="5727d-207">O nome que o Visual Studio atribui a este perfil é **FolderProfile2**.</span><span class="sxs-lookup"><span data-stu-id="5727d-207">The name that Visual Studio assigns to this profile is **FolderProfile2**.</span></span>

      <span data-ttu-id="5727d-208">Observe que cada local de destino contém o conjunto completo de arquivos (arquivos do seu aplicativo e todos os arquivos do .NET Core) necessários para iniciar seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-208">Note that each target location contains the complete set of files (both your app files and all .NET Core files) needed to launch your app.</span></span>

<span data-ttu-id="5727d-209">Junto com os arquivos do aplicativo, o processo de publicação emite um arquivo de banco de dados do programa (.pdb) que contém informações de depuração sobre seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-209">Along with your application's files, the publishing process emits a program database (.pdb) file that contains debugging information about your app.</span></span> <span data-ttu-id="5727d-210">O arquivo é útil principalmente para exceções de depuração.</span><span class="sxs-lookup"><span data-stu-id="5727d-210">The file is useful primarily for debugging exceptions.</span></span> <span data-ttu-id="5727d-211">Você pode optar por não empacotá-lo com os arquivos do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-211">You can choose not to package it with your application's files.</span></span> <span data-ttu-id="5727d-212">No entanto, você deve salvá-lo no caso de desejar depurar o build de lançamento para seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-212">You should, however, save it in the event that you want to debug the Release build of your app.</span></span>

<span data-ttu-id="5727d-213">Implante os arquivos publicados na maneira que desejar.</span><span class="sxs-lookup"><span data-stu-id="5727d-213">Deploy the published files in any way you like.</span></span> <span data-ttu-id="5727d-214">Por exemplo, você pode empacotá-los em um arquivo zip, usar um simples comando `copy` ou implantá-los com qualquer pacote de instalação de sua escolha.</span><span class="sxs-lookup"><span data-stu-id="5727d-214">For example, you can package them in a Zip file, use a simple `copy` command, or deploy them with any installation package of your choice.</span></span>

<span data-ttu-id="5727d-215">A seguir está o arquivo *csproj* completo para esse projeto.</span><span class="sxs-lookup"><span data-stu-id="5727d-215">The following is the complete *csproj* file for this project.</span></span>

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp1.1</TargetFramework>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
  </PropertyGroup>
</Project>
```

## <a name="self-contained-deployment-with-third-party-dependencies"></a><span data-ttu-id="5727d-216">Implantação autocontida com dependências de terceiros</span><span class="sxs-lookup"><span data-stu-id="5727d-216">Self-contained deployment with third-party dependencies</span></span>

<span data-ttu-id="5727d-217">Implantar uma implantação autocontida com uma ou mais dependências de terceiros envolve adicionar as dependências.</span><span class="sxs-lookup"><span data-stu-id="5727d-217">Deploying a self-contained deployment with one or more third-party dependencies involves adding the dependencies.</span></span> <span data-ttu-id="5727d-218">As etapas adicionais a seguir são necessárias antes de ser possível compilar seu aplicativo:</span><span class="sxs-lookup"><span data-stu-id="5727d-218">The following additional steps are required before you can build your app:</span></span>

1. <span data-ttu-id="5727d-219">Use o **Gerenciador de Pacotes NuGet** para adicionar uma referência a um pacote NuGet ao projeto e se o pacote ainda não estiver disponível no sistema, instale-o.</span><span class="sxs-lookup"><span data-stu-id="5727d-219">Use the **NuGet Package Manager** to add a reference to a NuGet package to your project; and if the package is not already available on your system, install it.</span></span> <span data-ttu-id="5727d-220">Para abrir o gerenciador de pacotes, selecione **Ferramentas** > **Gerenciador de Pacotes NuGet** > **Gerenciar Pacotes NuGet para a Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-220">To open the package manager, select **Tools** > **NuGet Package Manager** > **Manage NuGet Packages for Solution**.</span></span>

1. <span data-ttu-id="5727d-221">Confirme se `Newtonsoft.Json` está instalado em seu sistema e, se não estiver, instale-o.</span><span class="sxs-lookup"><span data-stu-id="5727d-221">Confirm that `Newtonsoft.Json` is installed on your system and, if it is not, install it.</span></span> <span data-ttu-id="5727d-222">A guia **Instalados** lista os pacotes NuGet instalados no sistema.</span><span class="sxs-lookup"><span data-stu-id="5727d-222">The **Installed** tab lists NuGet packages installed on your system.</span></span> <span data-ttu-id="5727d-223">Se `Newtonsoft.Json` não estiver listado, selecione a guia **Procurar** e insira "Newtonsoft.Json" na caixa de pesquisa.</span><span class="sxs-lookup"><span data-stu-id="5727d-223">If `Newtonsoft.Json` is not listed there, select the **Browse** tab and enter "Newtonsoft.Json" in the search box.</span></span> <span data-ttu-id="5727d-224">Selecione `Newtonsoft.Json` e, no painel direito, selecione seu projeto antes de selecionar **Instalar**.</span><span class="sxs-lookup"><span data-stu-id="5727d-224">Select `Newtonsoft.Json` and, in the right pane, select your project before selecting **Install**.</span></span>

1. <span data-ttu-id="5727d-225">Se `Newtonsoft.Json` já estiver instalado no sistema, adicione-o ao projeto selecionando o projeto no painel direito da guia **Gerenciar Pacotes para a Solução**.</span><span class="sxs-lookup"><span data-stu-id="5727d-225">If `Newtonsoft.Json` is already installed on your system, add it to your project by selecting your project in the right pane of the **Manage Packages for Solution** tab.</span></span>

<span data-ttu-id="5727d-226">A seguir está o arquivo *csproj* completo para esse projeto:</span><span class="sxs-lookup"><span data-stu-id="5727d-226">The following is the complete *csproj* file for this project:</span></span>

```xml
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>netcoreapp1.1</TargetFramework>
    <RuntimeIdentifiers>win10-x64;osx.10.11-x64</RuntimeIdentifiers>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="10.0.2" />
  </ItemGroup>
</Project>
```

<span data-ttu-id="5727d-227">Quando você implanta seu aplicativo, todas as dependências de terceiros usadas em seu aplicativo também contém os arquivos do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="5727d-227">When you deploy your application, any third-party dependencies used in your app are also contained with your application files.</span></span> <span data-ttu-id="5727d-228">As bibliotecas de terceiros não são necessárias no sistema em que o aplicativo está em execução.</span><span class="sxs-lookup"><span data-stu-id="5727d-228">Third-party libraries aren't required on the system on which the app is running.</span></span>

<span data-ttu-id="5727d-229">Observe que você só pode implantar uma implantação autocontida com uma biblioteca de terceiros para plataformas compatíveis com essa biblioteca.</span><span class="sxs-lookup"><span data-stu-id="5727d-229">Note that you can only deploy a self-contained deployment with a third-party library to platforms supported by that library.</span></span> <span data-ttu-id="5727d-230">Isso é semelhante a ter dependências de terceiros com dependências nativas em sua implantação dependente de estrutura, em que as dependências nativas não existem na plataforma de destino a menos que elas tenham sido instaladas anteriormente.</span><span class="sxs-lookup"><span data-stu-id="5727d-230">This is similar to having third-party dependencies with native dependencies in your framework-dependent deployment, where the native dependencies won't exist on the target platform unless they were previously installed there.</span></span>

# <a name="see-also"></a><span data-ttu-id="5727d-231">Consulte também</span><span class="sxs-lookup"><span data-stu-id="5727d-231">See also</span></span>
<span data-ttu-id="5727d-232">[Implantação de aplicativos .NET Core](index.md) </span><span class="sxs-lookup"><span data-stu-id="5727d-232">[.NET Core Application Deployment](index.md) </span></span>  
[<span data-ttu-id="5727d-233">Catálogo do Identificador de Tempo de Execução do .NET Core</span><span class="sxs-lookup"><span data-stu-id="5727d-233">.NET Core Runtime IDentifier (RID) catalog</span></span>](../rid-catalog.md)   
