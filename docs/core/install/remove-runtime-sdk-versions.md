---
title: Remover o SDK e o runtime do .NET Core
description: Este artigo descreve como determinar quais versões do runtime e do SDK do .NET Core estão instaladas e, em seguida, como removê-las no Windows, Mac e Linux.
author: adegeo
ms.author: adegeo
ms.date: 04/28/2020
zone_pivot_groups: operating-systems-set-one
ms.openlocfilehash: 1e4a846cf5e3d0209f5ade873520ef64abc12e7c
ms.sourcegitcommit: dc2feef0794cf41dbac1451a13b8183258566c0e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/24/2020
ms.locfileid: "85324650"
---
# <a name="how-to-remove-the-net-core-runtime-and-sdk"></a><span data-ttu-id="feb41-103">Como remover o SDK e o Runtime do .NET Core</span><span class="sxs-lookup"><span data-stu-id="feb41-103">How to remove the .NET Core Runtime and SDK</span></span>

<span data-ttu-id="feb41-104">Ao longo do tempo, ao instalar as versões atualizadas do runtime e do SDK do .NET Core, você poderá desejar remover versões desatualizadas do .NET Core do seu computador.</span><span class="sxs-lookup"><span data-stu-id="feb41-104">Over time, as you install updated versions of the .NET Core runtime and SDK, you may want to remove outdated versions of .NET Core from your machine.</span></span> <span data-ttu-id="feb41-105">Remover versões mais antigas do runtime pode mudar o runtime escolhido para executar aplicativos de estrutura compartilhada, conforme detalhado no artigo [Seleção de versão do .NET Core](../versions/selection.md).</span><span class="sxs-lookup"><span data-stu-id="feb41-105">Removing older versions of the runtime may change the runtime chosen to run shared framework applications, as detailed in the article on [.NET Core version selection](../versions/selection.md).</span></span>

## <a name="should-i-remove-a-version"></a><span data-ttu-id="feb41-106">Devo remover uma versão?</span><span class="sxs-lookup"><span data-stu-id="feb41-106">Should I remove a version?</span></span>

<span data-ttu-id="feb41-107">Os comportamentos de [seleção de versão do .NET Core](../versions/selection.md) e a compatibilidade do runtime do .NET Core com as atualizações permitem a remoção segura de versões anteriores.</span><span class="sxs-lookup"><span data-stu-id="feb41-107">The [.NET Core version selection](../versions/selection.md) behaviors and the runtime compatibility of .NET Core across updates enables safe removal of previous versions.</span></span> <span data-ttu-id="feb41-108">As atualizações de runtime do .NET core são compatíveis com uma “banda” de versão principal como 1.x e 2.x.</span><span class="sxs-lookup"><span data-stu-id="feb41-108">.NET Core runtime updates are compatible within a major version 'band' such as 1.x and 2.x.</span></span> <span data-ttu-id="feb41-109">Além disso, versões mais recentes do SDK do .NET Core geralmente mantêm a capacidade de compilar aplicativos destinados a versões anteriores do runtime de uma maneira compatível.</span><span class="sxs-lookup"><span data-stu-id="feb41-109">Additionally, newer releases of the .NET Core SDK generally maintain the ability to build applications that target previous versions of the runtime in a compatible manner.</span></span>

<span data-ttu-id="feb41-110">Em geral, você precisa apenas do SDK mais recente e da versão de patch mais recente dos runtimes necessários para seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="feb41-110">In general, you only need the latest SDK and latest patch version of the runtimes required for your application.</span></span> <span data-ttu-id="feb41-111">As instâncias em que você talvez queira manter versões mais antigas do SDK ou *do*Runtime incluem manterproject.jsaplicativos baseados em.</span><span class="sxs-lookup"><span data-stu-id="feb41-111">Instances where you might want to keep older SDK or runtime versions include maintaining *project.json*-based applications.</span></span> <span data-ttu-id="feb41-112">A menos que seu aplicativo tenha motivos específicos para runtimes ou SDKs anteriores, você pode remover com segurança as versões mais antigas.</span><span class="sxs-lookup"><span data-stu-id="feb41-112">Unless your application has specific reasons for earlier SDKs or runtimes, you may safely remove older versions.</span></span>

## <a name="determine-what-is-installed"></a><span data-ttu-id="feb41-113">Determinar o que está instalado</span><span class="sxs-lookup"><span data-stu-id="feb41-113">Determine what is installed</span></span>

<span data-ttu-id="feb41-114">Começando com o .NET Core 2.1, a CLI do .NET tem as opções que você pode usar para listar as versões do SDK e do runtime que estão instaladas em seu computador.</span><span class="sxs-lookup"><span data-stu-id="feb41-114">Starting with .NET Core 2.1, the .NET CLI has options you can use to list the versions of the SDK and runtime that are installed on your machine.</span></span>  <span data-ttu-id="feb41-115">Use [`dotnet --list-sdks`](../tools/dotnet.md#options) para ver a lista de SDKs instalados em seu computador.</span><span class="sxs-lookup"><span data-stu-id="feb41-115">Use [`dotnet --list-sdks`](../tools/dotnet.md#options) to see the list of SDKs installed on your machine.</span></span> <span data-ttu-id="feb41-116">Use [`dotnet --list-runtimes`](../tools/dotnet.md#options) para ver a lista de tempos de execução instalados em seu computador.</span><span class="sxs-lookup"><span data-stu-id="feb41-116">Use [`dotnet --list-runtimes`](../tools/dotnet.md#options) to see the list of runtimes installed on your machine.</span></span> <span data-ttu-id="feb41-117">Para obter mais informações, consulte [como verificar se o .NET Core já está instalado](how-to-detect-installed-versions.md).</span><span class="sxs-lookup"><span data-stu-id="feb41-117">For more information, see [How to check that .NET Core is already installed](how-to-detect-installed-versions.md).</span></span>

## <a name="uninstall-net-core"></a><span data-ttu-id="feb41-118">Desinstalar o .NET Core</span><span class="sxs-lookup"><span data-stu-id="feb41-118">Uninstall .NET Core</span></span>

::: zone pivot="os-windows"

<span data-ttu-id="feb41-119">O .NET Core usa a caixa de diálogo **recursos de & de aplicativos** do Windows para remover versões do SDK e do tempo de execução do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-119">.NET Core uses the Windows **Apps & features** dialog to remove versions of the .NET Core runtime and SDK.</span></span> <span data-ttu-id="feb41-120">A figura a seguir mostra a caixa de diálogo **aplicativos & recursos** .</span><span class="sxs-lookup"><span data-stu-id="feb41-120">The following figure shows the **Apps & features** dialog.</span></span> <span data-ttu-id="feb41-121">Você pode pesquisar o **SDK do core** para filtrar e mostrar as versões instaladas do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-121">You can search for **core sdk** to filter and show installed versions of .NET Core.</span></span>

![Adicionar ou remover programas para remover o .NET Core](./media/remove-runtime-sdk-versions/programs-and-features.png)

<span data-ttu-id="feb41-123">Selecione todas as versões que você deseja remover do seu computador e clique em **Desinstalar**.</span><span class="sxs-lookup"><span data-stu-id="feb41-123">Select any versions you want to remove from your machine and click **Uninstall**.</span></span>

::: zone-end

::: zone pivot="os-linux"

<span data-ttu-id="feb41-124">Há mais opções para desinstalar o .NET Core (SDK ou runtime) no Linux.</span><span class="sxs-lookup"><span data-stu-id="feb41-124">There are more options to uninstall .NET Core (either SDK or runtime) on Linux.</span></span> <span data-ttu-id="feb41-125">A melhor maneira para desinstalar o .NET Core é espelhar a ação que você usou para instalar o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-125">The best way for you to uninstall .NET Core is to mirror the action you used to install .NET Core.</span></span> <span data-ttu-id="feb41-126">As especificações dependem de sua distribuição escolhida e do método de instalação.</span><span class="sxs-lookup"><span data-stu-id="feb41-126">The specifics depend on your chosen distribution and the installation method.</span></span>

> [!IMPORTANT]
> <span data-ttu-id="feb41-127">Para instalações do Red Hat, consulte o [Guia de introdução do Red Hat](https://access.redhat.com/documentation/en-us/net_core/2.0/html/getting_started_guide/gs_install_dotnet#install_register_rehel) para obter informações sobre como instalar e desinstalar o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-127">For Red Hat installations, consult the [Red Hat Getting Started Guide](https://access.redhat.com/documentation/en-us/net_core/2.0/html/getting_started_guide/gs_install_dotnet#install_register_rehel) for information on installing and uninstalling .NET Core.</span></span>

<span data-ttu-id="feb41-128">A partir do .NET Core 2,1, não é necessário desinstalar o SDK do .NET Core ao atualizá-lo usando um Gerenciador de pacotes.</span><span class="sxs-lookup"><span data-stu-id="feb41-128">Starting with .NET Core 2.1, there's no need to uninstall the .NET Core SDK when upgrading it using a package manager.</span></span> <span data-ttu-id="feb41-129">Os comandos `update` ou `refresh` do gerenciador de pacotes removerão automaticamente a versão mais antiga após a instalação bem-sucedida de uma versão mais recente.</span><span class="sxs-lookup"><span data-stu-id="feb41-129">The package manager `update` or `refresh` commands will automatically remove the older version upon the successful installation of a newer version.</span></span>

<span data-ttu-id="feb41-130">Se instalou o .NET Core usando um gerenciador de pacotes, você usará esse mesmo gerenciador de pacotes para desinstalar o SDK ou o runtime do .NET.</span><span class="sxs-lookup"><span data-stu-id="feb41-130">If you installed .NET Core using a package manager, you use that same package manager to uninstall .NET SDK or runtime.</span></span> <span data-ttu-id="feb41-131">As instalações do .NET core dão suporte a gerenciadores de pacotes mais populares.</span><span class="sxs-lookup"><span data-stu-id="feb41-131">.NET Core installations support most popular package managers.</span></span> <span data-ttu-id="feb41-132">Consulte a documentação do Gerenciador de pacotes de distribuição para obter a sintaxe precisa em seu ambiente:</span><span class="sxs-lookup"><span data-stu-id="feb41-132">Consult the documentation for your distribution's package manager for the precise syntax in your environment:</span></span>

- <span data-ttu-id="feb41-133">[apt-get(8)](https://linux.die.net/man/8/apt-get) é usado por sistemas baseados em Debian, incluindo o Ubuntu.</span><span class="sxs-lookup"><span data-stu-id="feb41-133">[apt-get(8)](https://linux.die.net/man/8/apt-get) is used by Debian based systems, including Ubuntu.</span></span>
- <span data-ttu-id="feb41-134">[yum(8)](https://linux.die.net/man/8/yum) é usado no Fedora, CentOS e Oracle Linux.</span><span class="sxs-lookup"><span data-stu-id="feb41-134">[yum(8)](https://linux.die.net/man/8/yum) is used on Fedora, CentOS, and Oracle Linux.</span></span>
- <span data-ttu-id="feb41-135">[zypper(8)](https://en.opensuse.org/SDB:Zypper_manual_(plain)) é usado no openSUSE e no SLES (SUSE Linux Enterprise System).</span><span class="sxs-lookup"><span data-stu-id="feb41-135">[zypper(8)](https://en.opensuse.org/SDB:Zypper_manual_(plain)) is used on openSUSE and SUSE Linux Enterprise System (SLES).</span></span>
- <span data-ttu-id="feb41-136">[dnf(8)](https://dnf.readthedocs.io/en/latest/command_ref.html) é usado no Fedora.</span><span class="sxs-lookup"><span data-stu-id="feb41-136">[dnf(8)](https://dnf.readthedocs.io/en/latest/command_ref.html) is used on Fedora.</span></span>

<span data-ttu-id="feb41-137">Em quase todos os casos, o comando para remover um pacote é `remove`.</span><span class="sxs-lookup"><span data-stu-id="feb41-137">In almost all cases, the command to remove a package is `remove`.</span></span>

<span data-ttu-id="feb41-138">O nome do pacote para a instalação do SDK do .NET Core para a maioria dos gerenciadores de pacotes é `dotnet-sdk`, seguido pelo número de versão.</span><span class="sxs-lookup"><span data-stu-id="feb41-138">The package name for the .NET Core SDK installation for most package managers is `dotnet-sdk`, followed by the version number.</span></span> <span data-ttu-id="feb41-139">Começando com a versão 2.1.300 do SDK do .NET Core e a versão `2.1` do runtime, somente os números de versão principal e secundária são necessários: por exemplo, o SDK do .NET Core versão 2.1.300 pode ser referenciado como o pacote `dotnet-sdk-2.1`.</span><span class="sxs-lookup"><span data-stu-id="feb41-139">Starting with the version 2.1.300 of the .NET Core SDK and version `2.1` of the runtime, only the major and minor version numbers are necessary: for example, the .NET Core SDK version 2.1.300 can be referenced as the package `dotnet-sdk-2.1`.</span></span> <span data-ttu-id="feb41-140">As versões anteriores exigem a cadeia de caracteres de versão inteira: por exemplo, `dotnet-sdk-2.1.200` seria necessária para a versão 2.1.200 do SDK do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-140">Prior versions require the entire version string: for example, `dotnet-sdk-2.1.200` would be required for version 2.1.200 of the .NET Core SDK.</span></span>

<span data-ttu-id="feb41-141">Para computadores apenas com o runtime instalado, e não o SDK, o nome do pacote é `dotnet-runtime-<version>`, para o runtime do .NET Core, e `aspnetcore-runtime-<version>`, para a pilha em runtime inteira.</span><span class="sxs-lookup"><span data-stu-id="feb41-141">For machines that have installed only the runtime, and not the SDK, the package name is `dotnet-runtime-<version>` for the .NET Core runtime, and `aspnetcore-runtime-<version>` for the entire runtime stack.</span></span>

<span data-ttu-id="feb41-142">As instalações do .NET Core anteriores a 2,0 não desinstalaram o aplicativo host quando o SDK foi desinstalado usando o Gerenciador de pacotes.</span><span class="sxs-lookup"><span data-stu-id="feb41-142">.NET Core installations earlier than 2.0 didn't uninstall the host application when the SDK was uninstalled using the package manager.</span></span> <span data-ttu-id="feb41-143">Usando `apt-get`, o comando é:</span><span class="sxs-lookup"><span data-stu-id="feb41-143">Using `apt-get`, the command is:</span></span>

```bash
apt-get remove dotnet-host
```

<span data-ttu-id="feb41-144">Observe que não há nenhuma versão anexada a `dotnet-host` .</span><span class="sxs-lookup"><span data-stu-id="feb41-144">Note that there's no version attached to `dotnet-host`.</span></span>

<span data-ttu-id="feb41-145">Se instalou usando um tarball, você deverá remover o .NET Core usando o método manual.</span><span class="sxs-lookup"><span data-stu-id="feb41-145">If you installed using a tarball, you must remove .NET Core using the manual method.</span></span>

<span data-ttu-id="feb41-146">No Linux, você deve remover os SDKs e tempos de execução separadamente, removendo os diretórios com controle de versão.</span><span class="sxs-lookup"><span data-stu-id="feb41-146">On Linux, you must remove the SDKs and runtimes separately, by removing the versioned directories.</span></span> <span data-ttu-id="feb41-147">Removê-los exclui o SDK e o tempo de execução do disco.</span><span class="sxs-lookup"><span data-stu-id="feb41-147">Removing them deletes the SDK and runtime from disk.</span></span> <span data-ttu-id="feb41-148">Por exemplo, para remover o SDK 1.0.1 e o runtime, você usaria os seguintes comandos de bash:</span><span class="sxs-lookup"><span data-stu-id="feb41-148">For example, to remove the 1.0.1 SDK and runtime, you would use the following bash commands:</span></span>

```bash
version="1.0.1"
sudo rm -rf /usr/local/share/dotnet/sdk/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.NETCore.App/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.AspNetCore.All/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.AspNetCore.App/$version
sudo rm -rf /usr/local/share/dotnet/host/fxr/$version
```

<span data-ttu-id="feb41-149">Os diretórios pais para o SDK e o runtime são listados na saída dos comandos `dotnet --list-sdks` e `dotnet --list-runtimes`, conforme mostrado na tabela anterior.</span><span class="sxs-lookup"><span data-stu-id="feb41-149">The parent directories for the SDK and runtime are listed in the output from the `dotnet --list-sdks` and `dotnet --list-runtimes` command, as shown in the earlier table.</span></span>

::: zone-end

::: zone pivot="os-macos"

<span data-ttu-id="feb41-150">No Mac, você deve remover os SDKs e tempos de execução separadamente, removendo os diretórios com controle de versão.</span><span class="sxs-lookup"><span data-stu-id="feb41-150">On Mac, you must remove the SDKs and runtimes separately, by removing the versioned directories.</span></span> <span data-ttu-id="feb41-151">Removê-los exclui o SDK e o tempo de execução do disco.</span><span class="sxs-lookup"><span data-stu-id="feb41-151">Removing them deletes the SDK and runtime from disk.</span></span> <span data-ttu-id="feb41-152">Por exemplo, para remover o SDK 1.0.1 e o runtime, você usaria os seguintes comandos de bash:</span><span class="sxs-lookup"><span data-stu-id="feb41-152">For example, to remove the 1.0.1 SDK and runtime, you would use the following bash commands:</span></span>

```bash
version="1.0.1"
sudo rm -rf /usr/local/share/dotnet/sdk/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.NETCore.App/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.AspNetCore.All/$version
sudo rm -rf /usr/local/share/dotnet/shared/Microsoft.AspNetCore.App/$version
sudo rm -rf /usr/local/share/dotnet/host/fxr/$version
```

<span data-ttu-id="feb41-153">Os diretórios pais para o SDK e o runtime são listados na saída dos comandos `dotnet --list-sdks` e `dotnet --list-runtimes`, conforme mostrado na tabela anterior.</span><span class="sxs-lookup"><span data-stu-id="feb41-153">The parent directories for the SDK and runtime are listed in the output from the `dotnet --list-sdks` and `dotnet --list-runtimes` command, as shown in the earlier table.</span></span>

::: zone-end

## <a name="net-core-uninstall-tool"></a><span data-ttu-id="feb41-154">Ferramenta de Desinstalação do .NET Core</span><span class="sxs-lookup"><span data-stu-id="feb41-154">.NET Core Uninstall Tool</span></span>

<span data-ttu-id="feb41-155">A [ferramenta de desinstalação do .NET Core](../additional-tools/uninstall-tool.md) ( `dotnet-core-uninstall` ) permite que você remova os SDKs do .NET Core e os tempos de execução de um sistema.</span><span class="sxs-lookup"><span data-stu-id="feb41-155">The [.NET Core Uninstall Tool](../additional-tools/uninstall-tool.md) (`dotnet-core-uninstall`) lets you remove .NET Core SDKs and runtimes from a system.</span></span> <span data-ttu-id="feb41-156">Uma coleção de opções está disponível para especificar quais versões devem ser desinstaladas.</span><span class="sxs-lookup"><span data-stu-id="feb41-156">A collection of options is available to specify which versions should be uninstalled.</span></span>

## <a name="visual-studio-dependency-on-net-core-sdk-versions"></a><span data-ttu-id="feb41-157">Dependência do Visual Studio em versões de SDK do .NET Core</span><span class="sxs-lookup"><span data-stu-id="feb41-157">Visual Studio dependency on .NET Core SDK versions</span></span>

<span data-ttu-id="feb41-158">Antes do Visual Studio 2019 versão 16,3, os instaladores do Visual Studio chamaram o instalador de SDK do .NET Core autônomo.</span><span class="sxs-lookup"><span data-stu-id="feb41-158">Before Visual Studio 2019 version 16.3, Visual Studio installers called the standalone .NET Core SDK installer.</span></span> <span data-ttu-id="feb41-159">Como resultado, as versões do SDK são exibidas na caixa de diálogo **recursos de & de aplicativos** do Windows.</span><span class="sxs-lookup"><span data-stu-id="feb41-159">As a result, the SDK versions appear in the Windows **Apps & features** dialog.</span></span> <span data-ttu-id="feb41-160">Remover SDKs do .NET Core que foram instalados pelo Visual Studio usando o instalador autônomo pode interromper o Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="feb41-160">Removing .NET Core SDKs that were installed by Visual Studio using the standalone installer may break Visual Studio.</span></span> <span data-ttu-id="feb41-161">Se o Visual Studio tiver problemas depois de desinstalar os SDKs, execute o reparo nessa versão específica do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="feb41-161">If Visual Studio has problems after you uninstall SDKs, run Repair on that specific version of Visual Studio.</span></span> <span data-ttu-id="feb41-162">A tabela a seguir mostra algumas das dependências do Visual Studio em SDK do .NET Core versões:</span><span class="sxs-lookup"><span data-stu-id="feb41-162">The following table shows some of the Visual Studio dependencies on .NET Core SDK versions:</span></span>

| <span data-ttu-id="feb41-163">Versão do Visual Studio</span><span class="sxs-lookup"><span data-stu-id="feb41-163">Visual Studio version</span></span>           | <span data-ttu-id="feb41-164">Versão do SDK do .NET Core</span><span class="sxs-lookup"><span data-stu-id="feb41-164">.NET Core SDK version</span></span>          |
|---------------------------------|--------------------------------|
| <span data-ttu-id="feb41-165">Visual Studio 2019 versão 16.2</span><span class="sxs-lookup"><span data-stu-id="feb41-165">Visual Studio 2019 version 16.2</span></span> | <span data-ttu-id="feb41-166">SDK do .NET Core 2.2.4 XX, 2.1.8 XX</span><span class="sxs-lookup"><span data-stu-id="feb41-166">.NET Core SDK 2.2.4xx, 2.1.8xx</span></span> |
| <span data-ttu-id="feb41-167">Visual Studio 2019 versão 16.1</span><span class="sxs-lookup"><span data-stu-id="feb41-167">Visual Studio 2019 version 16.1</span></span> | <span data-ttu-id="feb41-168">SDK do .NET Core 2.2.3 XX, 2.1.7 XX</span><span class="sxs-lookup"><span data-stu-id="feb41-168">.NET Core SDK 2.2.3xx, 2.1.7xx</span></span> |
| <span data-ttu-id="feb41-169">Visual Studio 2019 versão 16,0</span><span class="sxs-lookup"><span data-stu-id="feb41-169">Visual Studio 2019 version 16.0</span></span> | <span data-ttu-id="feb41-170">SDK do .NET Core 2.2.2 XX, 2.1.6 XX</span><span class="sxs-lookup"><span data-stu-id="feb41-170">.NET Core SDK 2.2.2xx, 2.1.6xx</span></span> |
| <span data-ttu-id="feb41-171">Visual Studio 2017 versão 15,9</span><span class="sxs-lookup"><span data-stu-id="feb41-171">Visual Studio 2017 version 15.9</span></span> | <span data-ttu-id="feb41-172">SDK do .NET Core 2.2.1 XX, 2.1.5 XX</span><span class="sxs-lookup"><span data-stu-id="feb41-172">.NET Core SDK 2.2.1xx, 2.1.5xx</span></span> |
| <span data-ttu-id="feb41-173">Visual Studio 2017 versão 15.8</span><span class="sxs-lookup"><span data-stu-id="feb41-173">Visual Studio 2017 version 15.8</span></span> | <span data-ttu-id="feb41-174">SDK do .NET Core 2.1.4 XX</span><span class="sxs-lookup"><span data-stu-id="feb41-174">.NET Core SDK 2.1.4xx</span></span>          |

<span data-ttu-id="feb41-175">A partir do Visual Studio 2019 versão 16,3, o Visual Studio é responsável por sua própria cópia do SDK do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="feb41-175">Starting with Visual Studio 2019 version 16.3, Visual Studio is in charge of its own copy of the .NET Core SDK.</span></span> <span data-ttu-id="feb41-176">Por esse motivo, você não vê mais essas versões do SDK na caixa de diálogo **aplicativos & recursos** .</span><span class="sxs-lookup"><span data-stu-id="feb41-176">For that reason, you no longer see those SDK versions in the **Apps & features** dialog.</span></span>

## <a name="remove-the-nuget-fallback-folder"></a><span data-ttu-id="feb41-177">Remover a pasta de fallback do NuGet</span><span class="sxs-lookup"><span data-stu-id="feb41-177">Remove the NuGet fallback folder</span></span>

<span data-ttu-id="feb41-178">Antes do SDK do .NET Core 3,0, os instaladores de SDK do .NET Core usaram uma pasta chamada *NuGetFallbackFolder* para armazenar um cache de pacotes NuGet.</span><span class="sxs-lookup"><span data-stu-id="feb41-178">Before .NET Core 3.0 SDK, the .NET Core SDK installers used a folder named *NuGetFallbackFolder* to store a cache of NuGet packages.</span></span> <span data-ttu-id="feb41-179">Esse cache foi usado durante operações como `dotnet restore` ou `dotnet build /t:Restore` .</span><span class="sxs-lookup"><span data-stu-id="feb41-179">This cache was used during operations such as `dotnet restore` or `dotnet build /t:Restore`.</span></span> <span data-ttu-id="feb41-180">O *NuGetFallbackFolder* está localizado em *C:\Program Files\dotnet\sdk* no Windows e em */usr/local/share/dotnet/SDK* no MacOS.</span><span class="sxs-lookup"><span data-stu-id="feb41-180">The *NuGetFallbackFolder* is located at *C:\Program Files\dotnet\sdk* on Windows and at */usr/local/share/dotnet/sdk* on macOS.</span></span>

<span data-ttu-id="feb41-181">Talvez você queira remover essa pasta, se:</span><span class="sxs-lookup"><span data-stu-id="feb41-181">You may want to remove this folder, if:</span></span>

- <span data-ttu-id="feb41-182">Você está desenvolvendo apenas usando o SDK do .NET Core 3,0 ou versões posteriores.</span><span class="sxs-lookup"><span data-stu-id="feb41-182">You're only developing using .NET Core 3.0 SDK or later versions.</span></span>
- <span data-ttu-id="feb41-183">Você está desenvolvendo usando versões SDK do .NET Core anteriores a 3,0, mas pode trabalhar online.</span><span class="sxs-lookup"><span data-stu-id="feb41-183">You're developing using .NET Core SDK versions earlier than 3.0, but you can work online.</span></span>

<span data-ttu-id="feb41-184">Se você quiser remover a pasta de fallback do NuGet, poderá excluí-la, mas precisará de privilégios de administrador para fazer isso.</span><span class="sxs-lookup"><span data-stu-id="feb41-184">If you want to remove the NuGet fallback folder, you can delete it, but you'll need admin privileges to do so.</span></span>

<span data-ttu-id="feb41-185">Não é recomendável excluir a pasta *dotnet* .</span><span class="sxs-lookup"><span data-stu-id="feb41-185">It's not recommended to delete the *dotnet* folder.</span></span> <span data-ttu-id="feb41-186">Isso removeria todas as ferramentas globais que você instalou anteriormente.</span><span class="sxs-lookup"><span data-stu-id="feb41-186">Doing so would remove any global tools you've previously installed.</span></span> <span data-ttu-id="feb41-187">Além disso, no Windows:</span><span class="sxs-lookup"><span data-stu-id="feb41-187">Also, on Windows:</span></span>

- <span data-ttu-id="feb41-188">Você interromperá o Visual Studio 2019 versão 16,3 e versões posteriores.</span><span class="sxs-lookup"><span data-stu-id="feb41-188">You'll break Visual Studio 2019 version 16.3 and later versions.</span></span> <span data-ttu-id="feb41-189">Você pode executar o **reparo** para recuperar.</span><span class="sxs-lookup"><span data-stu-id="feb41-189">You can run **Repair** to recover.</span></span>
- <span data-ttu-id="feb41-190">Se houver SDK do .NET Core entradas na caixa de diálogo **aplicativos & recursos** , eles ficarão órfãos.</span><span class="sxs-lookup"><span data-stu-id="feb41-190">If there are .NET Core SDK entries in the **Apps & features** dialog, they'll be orphaned.</span></span>
