---
title: Trabalhando com o notarization Catalina do macOS
description: Como lidar com problemas de notarization e certificado com o macOS ao instalar o .NET Core Runtime, o SDK e os aplicativos criados com o .NET Core.
author: adegeo
ms.author: adegeo
ms.date: 02/14/2020
ms.openlocfilehash: cd3886b2e772a182156d212aefb9705a3fb5e17c
ms.sourcegitcommit: dc2feef0794cf41dbac1451a13b8183258566c0e
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 06/24/2020
ms.locfileid: "85324619"
---
# <a name="macos-catalina-notarization-and-the-impact-on-net-core-downloads-and-projects"></a><span data-ttu-id="8ec55-103">macOS Catalina notarization e o impacto sobre os downloads e projetos do .NET Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-103">macOS Catalina Notarization and the impact on .NET Core downloads and projects</span></span>

<span data-ttu-id="8ec55-104">A partir do macOS Catalina (versão 10,15), todo software criado após 1º de junho de 2019 e distribuído com a ID do desenvolvedor deve ser notarized.</span><span class="sxs-lookup"><span data-stu-id="8ec55-104">Beginning with macOS Catalina (version 10.15), all software built after June 1, 2019, and distributed with Developer ID, must be notarized.</span></span> <span data-ttu-id="8ec55-105">Esse requisito se aplica ao tempo de execução do .NET Core, SDK do .NET Core e software criado com o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="8ec55-105">This requirement applies to the .NET Core runtime, .NET Core SDK, and software created with .NET Core.</span></span> <span data-ttu-id="8ec55-106">Este artigo descreve os cenários comuns que você pode enfrentar com o .NET Core e o macOS notarization.</span><span class="sxs-lookup"><span data-stu-id="8ec55-106">This article describes the common scenarios you may face with .NET Core and macOS notarization.</span></span>

## <a name="installing-net-core"></a><span data-ttu-id="8ec55-107">Instalando o .NET Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-107">Installing .NET Core</span></span>

<span data-ttu-id="8ec55-108">Os instaladores do .NET Core (Runtime e SDK) versões 3,1, 3,0 e 2,1, foram notarizeddos desde 18 de fevereiro de 2020.</span><span class="sxs-lookup"><span data-stu-id="8ec55-108">The installers for .NET Core (both runtime and SDK) versions 3.1, 3.0, and 2.1, have been notarized since February 18, 2020.</span></span> <span data-ttu-id="8ec55-109">Versões anteriores liberadas não são notarized.</span><span class="sxs-lookup"><span data-stu-id="8ec55-109">Prior released versions aren't notarized.</span></span> <span data-ttu-id="8ec55-110">Você pode instalar manualmente uma versão não notarized do .NET Core baixando primeiro o instalador e, em seguida, usando o `sudo installer` comando.</span><span class="sxs-lookup"><span data-stu-id="8ec55-110">You can manually install a non-notarized version of .NET Core by first downloading the installer, and then using the `sudo installer` command.</span></span> <span data-ttu-id="8ec55-111">Para obter mais informações, consulte [baixar e instalar manualmente para o MacOS](sdk.md?pivots=os-macos#download-and-manually-install).</span><span class="sxs-lookup"><span data-stu-id="8ec55-111">For more information, see [Download and manually install for macOS](sdk.md?pivots=os-macos#download-and-manually-install).</span></span>

<span data-ttu-id="8ec55-112">A partir das seguintes versões, os instaladores do .NET Core são notarized:</span><span class="sxs-lookup"><span data-stu-id="8ec55-112">Beginning with the following versions, the .NET Core installers are notarized:</span></span>

- <span data-ttu-id="8ec55-113">Runtime do .NET Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-113">.NET Core Runtime</span></span>
  - <span data-ttu-id="8ec55-114">2.1.16</span><span class="sxs-lookup"><span data-stu-id="8ec55-114">2.1.16</span></span>
  - <span data-ttu-id="8ec55-115">3.0.3</span><span class="sxs-lookup"><span data-stu-id="8ec55-115">3.0.3</span></span>
  - <span data-ttu-id="8ec55-116">3.1.2</span><span class="sxs-lookup"><span data-stu-id="8ec55-116">3.1.2</span></span>

- <span data-ttu-id="8ec55-117">SDK do .Net Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-117">.NET Core SDK</span></span>
  - <span data-ttu-id="8ec55-118">2.1.512</span><span class="sxs-lookup"><span data-stu-id="8ec55-118">2.1.512</span></span>
  - <span data-ttu-id="8ec55-119">3.0.103</span><span class="sxs-lookup"><span data-stu-id="8ec55-119">3.0.103</span></span>
  - <span data-ttu-id="8ec55-120">3.1.102</span><span class="sxs-lookup"><span data-stu-id="8ec55-120">3.1.102</span></span>

## <a name="apphost-is-disabled-by-default"></a><span data-ttu-id="8ec55-121">appHost está desabilitado por padrão</span><span class="sxs-lookup"><span data-stu-id="8ec55-121">appHost is disabled by default</span></span>

<span data-ttu-id="8ec55-122">Por padrão, as versões não notarized do SDK do .NET Core 3,0 e superior produzem um executável de Mach-O nativo (conhecido como **appHost**) quando seu projeto é compilado, publicado ou é executado.</span><span class="sxs-lookup"><span data-stu-id="8ec55-122">By default, non-notarized versions of the .NET Core SDK 3.0 and above produce a native Mach-O executable (known as the **appHost**) when your project compiles, publishes, or is run.</span></span> <span data-ttu-id="8ec55-123">Esse executável é uma maneira conveniente de executar seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="8ec55-123">This executable is a convenient way to run your app.</span></span> <span data-ttu-id="8ec55-124">Caso contrário, seu aplicativo deve ser iniciado executando `dotnet <filename.dll>` .</span><span class="sxs-lookup"><span data-stu-id="8ec55-124">Otherwise, your app must be started by running `dotnet <filename.dll>`.</span></span> <span data-ttu-id="8ec55-125">Quando o appHost está habilitado, o `dotnet run` comando é invocado no contexto do appHost.</span><span class="sxs-lookup"><span data-stu-id="8ec55-125">When the appHost is enabled, the `dotnet run` command is invoked in the context of the appHost.</span></span> <span data-ttu-id="8ec55-126">Para obter mais informações, consulte [contexto do appHost](#context-of-the-apphost).</span><span class="sxs-lookup"><span data-stu-id="8ec55-126">For more information, see [Context of the appHost](#context-of-the-apphost).</span></span>

<span data-ttu-id="8ec55-127">A partir das versões notarized do SDK do .NET Core 3,0 e posterior, o executável appHost não é gerado por padrão.</span><span class="sxs-lookup"><span data-stu-id="8ec55-127">Starting with the notarized versions of the .NET Core SDK 3.0 and above, the appHost executable isn't generated by default.</span></span> <span data-ttu-id="8ec55-128">Você pode ativar a geração de appHost com a [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) configuração booliana no arquivo de projeto.</span><span class="sxs-lookup"><span data-stu-id="8ec55-128">You can turn on appHost generation with the [`UseAppHost`](../project-sdk/msbuild-props.md#useapphost) boolean setting in the project file.</span></span> <span data-ttu-id="8ec55-129">Você também pode alternar o appHost com o `-p:UseAppHost` parâmetro na linha de comando para o `dotnet` comando específico executado:</span><span class="sxs-lookup"><span data-stu-id="8ec55-129">You can also toggle the appHost with the `-p:UseAppHost` parameter on the command line for the specific `dotnet` command you run:</span></span>

- <span data-ttu-id="8ec55-130">Arquivo de projeto</span><span class="sxs-lookup"><span data-stu-id="8ec55-130">Project file</span></span>

  ```xml
  <PropertyGroup>
    <UseAppHost>true</UseAppHost>
  </PropertyGroup>
  ```

- <span data-ttu-id="8ec55-131">Parâmetro de linha de comando</span><span class="sxs-lookup"><span data-stu-id="8ec55-131">Command-line parameter</span></span>

  ```dotnetcli
  dotnet run -p:UseAppHost=true
  ```

<span data-ttu-id="8ec55-132">Um appHost é sempre criado quando você publica [seu aplicativo independente](../deploying/index.md#publish-self-contained).</span><span class="sxs-lookup"><span data-stu-id="8ec55-132">An appHost is always created when you publish your app [self-contained](../deploying/index.md#publish-self-contained).</span></span>

<span data-ttu-id="8ec55-133">Para obter mais informações sobre a `UseAppHost` configuração, consulte [Propriedades do MSBuild para Microsoft. net. SDK](../project-sdk/msbuild-props.md#useapphost).</span><span class="sxs-lookup"><span data-stu-id="8ec55-133">For more information about the `UseAppHost` setting, see [MSBuild properties for Microsoft.NET.Sdk](../project-sdk/msbuild-props.md#useapphost).</span></span>

### <a name="context-of-the-apphost"></a><span data-ttu-id="8ec55-134">Contexto do appHost</span><span class="sxs-lookup"><span data-stu-id="8ec55-134">Context of the appHost</span></span>

<span data-ttu-id="8ec55-135">Quando o appHost está habilitado em seu projeto e você usa o `dotnet run` comando para executar seu aplicativo, o aplicativo é invocado no contexto do appHost e não no host padrão (o host padrão é o `dotnet` comando).</span><span class="sxs-lookup"><span data-stu-id="8ec55-135">When the appHost is enabled in your project, and you use the `dotnet run` command to run your app, the app is invoked in the context of the appHost and not the default host (the default host is the `dotnet` command).</span></span> <span data-ttu-id="8ec55-136">Se o appHost estiver desabilitado em seu projeto, o `dotnet run` comando executará seu aplicativo no contexto do host padrão.</span><span class="sxs-lookup"><span data-stu-id="8ec55-136">If the appHost is disabled in your project, the `dotnet run` command runs your app in the context of the default host.</span></span> <span data-ttu-id="8ec55-137">Mesmo que o appHost esteja desabilitado, a publicação de seu aplicativo como independente gera um executável appHost e os usuários usam esse executável para executar seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="8ec55-137">Even if the appHost is disabled, publishing your app as self-contained generates an appHost executable, and users use that executable to run your app.</span></span> <span data-ttu-id="8ec55-138">Executar seu aplicativo com `dotnet <filename.dll>` invoca o aplicativo com o host padrão, o tempo de execução compartilhado.</span><span class="sxs-lookup"><span data-stu-id="8ec55-138">Running your app with `dotnet <filename.dll>` invokes the app with the default host, the shared runtime.</span></span>

<span data-ttu-id="8ec55-139">Quando um aplicativo que usa o appHost é invocado, a partição de certificado acessada pelo aplicativo é diferente do host padrão notarized.</span><span class="sxs-lookup"><span data-stu-id="8ec55-139">When an app using the appHost is invoked, the certificate partition accessed by the app is different from the notarized default host.</span></span> <span data-ttu-id="8ec55-140">Se seu aplicativo precisar acessar os certificados instalados por meio do host padrão, use o `dotnet run` comando para executar seu aplicativo de seu arquivo de projeto ou use o `dotnet <filename.dll>` comando para iniciar o aplicativo diretamente.</span><span class="sxs-lookup"><span data-stu-id="8ec55-140">If your app must access the certificates installed through the default host, use the `dotnet run` command to run your app from its project file, or use the `dotnet <filename.dll>` command to start the app directly.</span></span>

<span data-ttu-id="8ec55-141">Mais informações sobre esse cenário são fornecidas na seção [ASP.NET Core e MacOS e certificados](#aspnet-core-and-macos-and-certificates) .</span><span class="sxs-lookup"><span data-stu-id="8ec55-141">More information about this scenario is provided in the [ASP.NET Core and macOS and certificates](#aspnet-core-and-macos-and-certificates) section.</span></span>

## <a name="aspnet-core-and-macos-and-certificates"></a><span data-ttu-id="8ec55-142">ASP.NET Core e macOS e certificados</span><span class="sxs-lookup"><span data-stu-id="8ec55-142">ASP.NET Core and macOS and certificates</span></span>

<span data-ttu-id="8ec55-143">O .NET Core fornece a capacidade de gerenciar certificados no conjunto de chaves do macOS com a <xref:System.Security.Cryptography.X509Certificates> classe.</span><span class="sxs-lookup"><span data-stu-id="8ec55-143">.NET Core provides the ability to manage certificates in the macOS Keychain with the <xref:System.Security.Cryptography.X509Certificates> class.</span></span> <span data-ttu-id="8ec55-144">O acesso ao conjunto de chaves do macOS usa a identidade de aplicativos como a chave primária ao decidir qual partição deve ser considerada.</span><span class="sxs-lookup"><span data-stu-id="8ec55-144">Access to the macOS Keychain uses the applications identity as the primary key when deciding which partition to consider.</span></span> <span data-ttu-id="8ec55-145">Por exemplo, aplicativos não assinados armazenam segredos na partição não assinada, mas os aplicativos assinados armazenam seus segredos em partições somente que podem acessar.</span><span class="sxs-lookup"><span data-stu-id="8ec55-145">For example, unsigned applications store secrets in the unsigned partition, but signed applications store their secrets in partitions only they can access.</span></span> <span data-ttu-id="8ec55-146">A origem da execução que invoca seu aplicativo decide qual partição usar.</span><span class="sxs-lookup"><span data-stu-id="8ec55-146">The source of execution that invokes your app decides which partition to use.</span></span>

<span data-ttu-id="8ec55-147">O .NET Core fornece três fontes de execução: [appHost](#apphost-is-disabled-by-default), host padrão (o `dotnet` comando) e um host personalizado.</span><span class="sxs-lookup"><span data-stu-id="8ec55-147">.NET Core provides three sources of execution: [appHost](#apphost-is-disabled-by-default), default host (the `dotnet` command), and a custom host.</span></span> <span data-ttu-id="8ec55-148">Cada modelo de execução pode ter identidades diferentes, assinadas ou não, e tem acesso a diferentes partições dentro do conjunto de chaves.</span><span class="sxs-lookup"><span data-stu-id="8ec55-148">Each execution model may have different identities, either signed or unsigned, and has access to different partitions within the Keychain.</span></span> <span data-ttu-id="8ec55-149">Os certificados importados por um modo podem não estar acessíveis a partir de outro.</span><span class="sxs-lookup"><span data-stu-id="8ec55-149">Certificates imported by one mode may not be accessible from another.</span></span> <span data-ttu-id="8ec55-150">Por exemplo, as versões notarized do .NET Core têm um host padrão assinado.</span><span class="sxs-lookup"><span data-stu-id="8ec55-150">For example, the notarized versions of .NET Core have a default host that is signed.</span></span> <span data-ttu-id="8ec55-151">Os certificados são importados para uma partição segura com base em sua identidade.</span><span class="sxs-lookup"><span data-stu-id="8ec55-151">Certificates are imported into a secure partition based on its identity.</span></span> <span data-ttu-id="8ec55-152">Esses certificados não estão acessíveis de um appHost gerado, pois o appHost não está assinado.</span><span class="sxs-lookup"><span data-stu-id="8ec55-152">These certificates aren't accessible from a generated appHost, as the appHost is unsigned.</span></span>

<span data-ttu-id="8ec55-153">Outro exemplo, por padrão, ASP.NET Core importa um certificado SSL padrão por meio do host padrão.</span><span class="sxs-lookup"><span data-stu-id="8ec55-153">Another example, by default, ASP.NET Core imports a default SSL certificate through the default host.</span></span> <span data-ttu-id="8ec55-154">ASP.NET Core aplicativos que usam um appHost não terão acesso a esse certificado e receberão um erro quando o .NET Core detectar que o certificado não está acessível.</span><span class="sxs-lookup"><span data-stu-id="8ec55-154">ASP.NET Core applications that use an appHost won't have access to this certificate and will receive an error when .NET Core detects the certificate isn't accessible.</span></span> <span data-ttu-id="8ec55-155">A mensagem de erro fornece instruções sobre como corrigir esse problema.</span><span class="sxs-lookup"><span data-stu-id="8ec55-155">The error message provides instructions on how to fix this problem.</span></span>

<span data-ttu-id="8ec55-156">Se o compartilhamento de certificados for necessário, o macOS fornecerá opções de configuração com o `security` utilitário.</span><span class="sxs-lookup"><span data-stu-id="8ec55-156">If certificate sharing is required, macOS provides configuration options with the `security` utility.</span></span>

<span data-ttu-id="8ec55-157">Para obter mais informações sobre como solucionar problemas de ASP.NET Core de certificado, consulte [impor HTTPS no ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span><span class="sxs-lookup"><span data-stu-id="8ec55-157">For more information on how to troubleshoot ASP.NET Core certificate issues, see [Enforce HTTPS in ASP.NET Core](/aspnet/core/security/enforcing-ssl?view=aspnetcore-3.1&tabs=visual-studio#troubleshoot-certificate-problems).</span></span>

## <a name="default-entitlements"></a><span data-ttu-id="8ec55-158">Direitos padrão</span><span class="sxs-lookup"><span data-stu-id="8ec55-158">Default entitlements</span></span>

<span data-ttu-id="8ec55-159">O host padrão do .NET Core (o `dotnet` comando) tem um conjunto de direitos padrão.</span><span class="sxs-lookup"><span data-stu-id="8ec55-159">.NET Core’s default host (the `dotnet` command) has a set of default entitlements.</span></span> <span data-ttu-id="8ec55-160">Esses direitos são necessários para a operação adequada do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="8ec55-160">These entitlements are required for proper operation of .NET Core.</span></span> <span data-ttu-id="8ec55-161">É possível que seu aplicativo possa precisar de direitos adicionais. nesse caso, você precisará gerar e usar um [appHost](#apphost-is-disabled-by-default) e, em seguida, adicionar os direitos necessários localmente.</span><span class="sxs-lookup"><span data-stu-id="8ec55-161">It's possible that your application may need additional entitlements, in which case you'll need to generate and use an [appHost](#apphost-is-disabled-by-default) and then add the necessary entitlements locally.</span></span>

<span data-ttu-id="8ec55-162">Conjunto padrão de direitos para o .NET Core:</span><span class="sxs-lookup"><span data-stu-id="8ec55-162">Default set of entitlements for .NET Core:</span></span>

- `com.apple.security.cs.allow-jit`
- `com.apple.security.cs.allow-unsigned-executable-memory`
- `com.apple.security.cs.allow-dyld-environment-variables`
- `com.apple.security.cs.disable-library-validation`

## <a name="notarize-a-net-core-app"></a><span data-ttu-id="8ec55-163">Notarize um aplicativo .NET Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-163">Notarize a .NET Core app</span></span>

<span data-ttu-id="8ec55-164">Se você quiser que seu aplicativo seja executado no macOS (versão 10,15) ou superior, convém notarize seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="8ec55-164">If you want your application to run on macOS Catalina (version 10.15) or higher, you'll want to notarize your app.</span></span> <span data-ttu-id="8ec55-165">O appHost que você envia com seu aplicativo para notarization deve ser usado com pelo menos os mesmos [direitos padrão](#default-entitlements) para o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="8ec55-165">The appHost you submit with your application for notarization should be used with at least the same [default entitlements](#default-entitlements) for .NET Core.</span></span>

## <a name="next-steps"></a><span data-ttu-id="8ec55-166">Próximas etapas</span><span class="sxs-lookup"><span data-stu-id="8ec55-166">Next steps</span></span>

- <span data-ttu-id="8ec55-167">[Dependências e requisitos do .NET Core](dependencies.md).</span><span class="sxs-lookup"><span data-stu-id="8ec55-167">[.NET Core dependencies and requirements](dependencies.md).</span></span>
- <span data-ttu-id="8ec55-168">[Instale o SDK do .NET Core](sdk.md).</span><span class="sxs-lookup"><span data-stu-id="8ec55-168">[Install the .NET Core SDK](sdk.md).</span></span>
- [<span data-ttu-id="8ec55-169">Instalar o tempo de execução do .NET Core</span><span class="sxs-lookup"><span data-stu-id="8ec55-169">Install the .NET Core Runtime</span></span>](runtime.md)
