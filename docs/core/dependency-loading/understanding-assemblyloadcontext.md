---
title: Entendendo AssemblyLoadContext - .NET Core
description: Conceitos-chave para entender o propósito e o comportamento do AssemblyLoadContext no .NET Core.
ms.date: 08/09/2019
author: sdmaclea
ms.author: stmaclea
ms.openlocfilehash: 43fb0d792ddeb20b8a141af452a86dd50f37ba43
ms.sourcegitcommit: 79b0dd8bfc63f33a02137121dd23475887ecefda
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/01/2020
ms.locfileid: "80523606"
---
# <a name="understanding-systemruntimeloaderassemblyloadcontext"></a><span data-ttu-id="c6097-103">Entendendo System.Runtime.Loader.AssemblyLoadContext</span><span class="sxs-lookup"><span data-stu-id="c6097-103">Understanding System.Runtime.Loader.AssemblyLoadContext</span></span>

<span data-ttu-id="c6097-104">A <xref:System.Runtime.Loader.AssemblyLoadContext> classe é exclusiva do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="c6097-104">The <xref:System.Runtime.Loader.AssemblyLoadContext> class is unique to .NET Core.</span></span> <span data-ttu-id="c6097-105">Este artigo tenta <xref:System.Runtime.Loader.AssemblyLoadContext> complementar a documentação da API com informações conceituais.</span><span class="sxs-lookup"><span data-stu-id="c6097-105">This article attempts to supplement the <xref:System.Runtime.Loader.AssemblyLoadContext> API documentation with conceptual information.</span></span>

<span data-ttu-id="c6097-106">Este artigo é relevante para desenvolvedores que implementam carregamento dinâmico, especialmente desenvolvedores de estruturas de carregamento dinâmico.</span><span class="sxs-lookup"><span data-stu-id="c6097-106">This article is relevant to developers implementing dynamic loading, especially dynamic loading framework developers.</span></span>

## <a name="what-is-the-assemblyloadcontext"></a><span data-ttu-id="c6097-107">O que é o AssemblyLoadContext?</span><span class="sxs-lookup"><span data-stu-id="c6097-107">What is the AssemblyLoadContext?</span></span>

<span data-ttu-id="c6097-108">Cada aplicativo .NET Core <xref:System.Runtime.Loader.AssemblyLoadContext>usa implicitamente o .</span><span class="sxs-lookup"><span data-stu-id="c6097-108">Every .NET Core application implicitly uses the <xref:System.Runtime.Loader.AssemblyLoadContext>.</span></span>
<span data-ttu-id="c6097-109">É o provedor de tempo de execução para localizar e carregar dependências.</span><span class="sxs-lookup"><span data-stu-id="c6097-109">It's the runtime's provider for locating and loading dependencies.</span></span> <span data-ttu-id="c6097-110">Sempre que uma dependência é <xref:System.Runtime.Loader.AssemblyLoadContext> carregada, uma instância é invocada para localizá-la.</span><span class="sxs-lookup"><span data-stu-id="c6097-110">Whenever a dependency is loaded, an <xref:System.Runtime.Loader.AssemblyLoadContext> instance is invoked to locate it.</span></span>

- <span data-ttu-id="c6097-111">Ele fornece um serviço de localização, carregamento e cache de conjuntos gerenciados e outras dependências.</span><span class="sxs-lookup"><span data-stu-id="c6097-111">It provides a service of locating, loading, and caching managed assemblies and other dependencies.</span></span>

- <span data-ttu-id="c6097-112">Para suportar o carregamento e descarga de códigodinâmico, ele cria um <xref:System.Runtime.Loader.AssemblyLoadContext> contexto isolado para o código de carregamento e suas dependências em sua própria instância.</span><span class="sxs-lookup"><span data-stu-id="c6097-112">To support dynamic code loading and unloading, it creates an isolated context for loading code and its dependencies in their own <xref:System.Runtime.Loader.AssemblyLoadContext> instance.</span></span>

## <a name="when-do-you-need-multiple-assemblyloadcontext-instances"></a><span data-ttu-id="c6097-113">Quando você precisa de várias instâncias assemblyLoadContext?</span><span class="sxs-lookup"><span data-stu-id="c6097-113">When do you need multiple AssemblyLoadContext instances?</span></span>

<span data-ttu-id="c6097-114">Uma <xref:System.Runtime.Loader.AssemblyLoadContext> única instância está limitada a <xref:System.Reflection.Assembly> carregar exatamente uma <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType>versão de um nome de montagem simples, .</span><span class="sxs-lookup"><span data-stu-id="c6097-114">A single <xref:System.Runtime.Loader.AssemblyLoadContext> instance is limited to loading exactly one version of an <xref:System.Reflection.Assembly> per simple assembly name, <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType>.</span></span>

<span data-ttu-id="c6097-115">Essa restrição pode se tornar um problema ao carregar módulos de código dinamicamente.</span><span class="sxs-lookup"><span data-stu-id="c6097-115">This restriction can become a problem when loading code modules dynamically.</span></span> <span data-ttu-id="c6097-116">Cada módulo é compilado independentemente e pode <xref:System.Reflection.Assembly>depender de versões diferentes de um .</span><span class="sxs-lookup"><span data-stu-id="c6097-116">Each module is independently compiled and may depend on different versions of an <xref:System.Reflection.Assembly>.</span></span> <span data-ttu-id="c6097-117">Esse problema geralmente ocorre quando diferentes módulos dependem de versões diferentes de uma biblioteca comumente usada.</span><span class="sxs-lookup"><span data-stu-id="c6097-117">This problem commonly occurs when different modules depend on different versions of a commonly used library.</span></span>

<span data-ttu-id="c6097-118">Para suportar o código <xref:System.Runtime.Loader.AssemblyLoadContext> de carregamento dinâmico, a API prevê o carregamento de versões conflitantes de um <xref:System.Reflection.Assembly> no mesmo aplicativo.</span><span class="sxs-lookup"><span data-stu-id="c6097-118">To support dynamically loading code, the <xref:System.Runtime.Loader.AssemblyLoadContext> API provides for loading conflicting versions of an <xref:System.Reflection.Assembly> in the same application.</span></span> <span data-ttu-id="c6097-119">Cada <xref:System.Runtime.Loader.AssemblyLoadContext> instância fornece um <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType> mapeamento de <xref:System.Reflection.Assembly> dicionário único para uma instância específica.</span><span class="sxs-lookup"><span data-stu-id="c6097-119">Each <xref:System.Runtime.Loader.AssemblyLoadContext> instance provides a unique dictionary mapping each <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType> to a specific <xref:System.Reflection.Assembly> instance.</span></span>

<span data-ttu-id="c6097-120">Ele também fornece um mecanismo conveniente para agrupar dependências relacionadas a um módulo de código para descarregar posteriormente.</span><span class="sxs-lookup"><span data-stu-id="c6097-120">It also provides a convenient mechanism for grouping dependencies related to a code module for later unload.</span></span>

## <a name="what-is-special-about-the-assemblyloadcontextdefault-instance"></a><span data-ttu-id="c6097-121">O que há de especial na instância AssemblyLoadContext.Default?</span><span class="sxs-lookup"><span data-stu-id="c6097-121">What is special about the AssemblyLoadContext.Default instance?</span></span>

<span data-ttu-id="c6097-122">A <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> instância é preenchida automaticamente pelo tempo de execução na inicialização.</span><span class="sxs-lookup"><span data-stu-id="c6097-122">The <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> instance is automatically populated by the runtime at startup.</span></span>  <span data-ttu-id="c6097-123">Ele usa [sondagem padrão](default-probing.md) para localizar e encontrar todas as dependências estáticas.</span><span class="sxs-lookup"><span data-stu-id="c6097-123">It uses [default probing](default-probing.md) to locate and find all static dependencies.</span></span>

<span data-ttu-id="c6097-124">Resolve os cenários de carregamento de dependência mais comuns.</span><span class="sxs-lookup"><span data-stu-id="c6097-124">It solves the most common dependency loading scenarios.</span></span>

## <a name="how-does-assemblyloadcontext-support-dynamic-dependencies"></a><span data-ttu-id="c6097-125">Como o AssemblyLoadContext suporta dependências dinâmicas?</span><span class="sxs-lookup"><span data-stu-id="c6097-125">How does AssemblyLoadContext support dynamic dependencies?</span></span>

<span data-ttu-id="c6097-126"><xref:System.Runtime.Loader.AssemblyLoadContext>tem vários eventos e funções virtuais que podem ser substituídos.</span><span class="sxs-lookup"><span data-stu-id="c6097-126"><xref:System.Runtime.Loader.AssemblyLoadContext> has various events and virtual functions that can be overridden.</span></span>

<span data-ttu-id="c6097-127">A <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> instância só suporta sobrepor os eventos.</span><span class="sxs-lookup"><span data-stu-id="c6097-127">The <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> instance only supports overriding the events.</span></span>

<span data-ttu-id="c6097-128">Os artigos [Algoritmo de carregamento de montagem gerenciado,](loading-managed.md)algoritmo de carregamento de montagem de [satélite](loading-resources.md)e algoritmo de carregamento de biblioteca não [gerenciado (nativo)](loading-unmanaged.md) referem-se a todos os eventos e funções virtuais disponíveis.</span><span class="sxs-lookup"><span data-stu-id="c6097-128">The articles [Managed assembly loading algorithm](loading-managed.md), [Satellite assembly loading algorithm](loading-resources.md), and [Unmanaged (native) library loading algorithm](loading-unmanaged.md) refer to all the available events and virtual functions.</span></span>  <span data-ttu-id="c6097-129">Os artigos mostram a posição relativa de cada evento e função nos algoritmos de carregamento.</span><span class="sxs-lookup"><span data-stu-id="c6097-129">The articles show each event and function's relative position in the loading algorithms.</span></span> <span data-ttu-id="c6097-130">Este artigo não reproduz essa informação.</span><span class="sxs-lookup"><span data-stu-id="c6097-130">This article doesn't reproduce that information.</span></span>

<span data-ttu-id="c6097-131">Esta seção abrange os princípios gerais para os eventos e funções relevantes.</span><span class="sxs-lookup"><span data-stu-id="c6097-131">This section covers the general principles for the relevant events and functions.</span></span>

- <span data-ttu-id="c6097-132">**Seja repetível**.</span><span class="sxs-lookup"><span data-stu-id="c6097-132">**Be repeatable**.</span></span> <span data-ttu-id="c6097-133">Uma consulta para uma dependência específica deve sempre resultar na mesma resposta.</span><span class="sxs-lookup"><span data-stu-id="c6097-133">A query for a specific dependency must always result in the same response.</span></span> <span data-ttu-id="c6097-134">A mesma instância de dependência carregada deve ser devolvida.</span><span class="sxs-lookup"><span data-stu-id="c6097-134">The same loaded dependency instance must be returned.</span></span> <span data-ttu-id="c6097-135">Esse requisito é fundamental para a consistência do cache.</span><span class="sxs-lookup"><span data-stu-id="c6097-135">This requirement is fundamental  for cache consistency.</span></span> <span data-ttu-id="c6097-136">Para montagens gerenciadas em particular, <xref:System.Reflection.Assembly> estamos criando um cache.</span><span class="sxs-lookup"><span data-stu-id="c6097-136">For managed assemblies in particular, we're creating a <xref:System.Reflection.Assembly> cache.</span></span> <span data-ttu-id="c6097-137">A chave de cache é <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType>um nome de montagem simples, .</span><span class="sxs-lookup"><span data-stu-id="c6097-137">The cache key is a simple assembly name, <xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType>.</span></span>
- <span data-ttu-id="c6097-138">**Normalmente não jogam.**</span><span class="sxs-lookup"><span data-stu-id="c6097-138">**Typically don't throw**.</span></span>  <span data-ttu-id="c6097-139">Espera-se que essas funções retornem `null` em vez de serem jogadas quando não conseguem encontrar a dependência solicitada.</span><span class="sxs-lookup"><span data-stu-id="c6097-139">It's expected that these functions return `null` rather than throw when unable to find the requested dependency.</span></span> <span data-ttu-id="c6097-140">O arremesso acabará prematuramente com a busca e será propagado uma exceção ao interlocutor.</span><span class="sxs-lookup"><span data-stu-id="c6097-140">Throwing will prematurely end the search and be propagate an exception to the caller.</span></span> <span data-ttu-id="c6097-141">O arremesso deve ser restrito a erros inesperados, como um conjunto corrompido ou uma condição fora da memória.</span><span class="sxs-lookup"><span data-stu-id="c6097-141">Throwing should be restricted to unexpected errors like a corrupted assembly or an out of memory condition.</span></span>
- <span data-ttu-id="c6097-142">**Evite a recursão.**</span><span class="sxs-lookup"><span data-stu-id="c6097-142">**Avoid recursion**.</span></span> <span data-ttu-id="c6097-143">Esteja ciente de que essas funções e manipuladores implementam as regras de carregamento para localizar dependências.</span><span class="sxs-lookup"><span data-stu-id="c6097-143">Be aware that these functions and handlers implement the loading rules for locating dependencies.</span></span> <span data-ttu-id="c6097-144">Sua implementação não deve chamar APIs que acionam a recursão.</span><span class="sxs-lookup"><span data-stu-id="c6097-144">Your implementation shouldn't call APIs that trigger recursion.</span></span> <span data-ttu-id="c6097-145">Seu código normalmente deve chamar funções de carga **AssemblyLoadContext** que requerem um argumento específico de referência de caminho ou memória.</span><span class="sxs-lookup"><span data-stu-id="c6097-145">Your code should typically call **AssemblyLoadContext** load functions that require a specific path or memory reference argument.</span></span>
- <span data-ttu-id="c6097-146">**Carregar no AssemblyLoadContext correto**.</span><span class="sxs-lookup"><span data-stu-id="c6097-146">**Load into the correct AssemblyLoadContext**.</span></span> <span data-ttu-id="c6097-147">A escolha de onde carregar dependências é específica do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="c6097-147">The choice of where to load dependencies is application-specific.</span></span>  <span data-ttu-id="c6097-148">A escolha é implementada por esses eventos e funções.</span><span class="sxs-lookup"><span data-stu-id="c6097-148">The choice is implemented by these events and functions.</span></span> <span data-ttu-id="c6097-149">Quando o código chama as funções de carga por caminho **AssemblyLoadContext,** chame-as na instância em que você deseja que o código seja carregado.</span><span class="sxs-lookup"><span data-stu-id="c6097-149">When your code calls **AssemblyLoadContext** load-by-path functions call them on the instance where you want the code loaded.</span></span> <span data-ttu-id="c6097-150">Em algum `null` momento, <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> retornar e deixar o cabo da carga pode ser a opção mais simples.</span><span class="sxs-lookup"><span data-stu-id="c6097-150">Sometime returning `null` and letting the <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> handle the load may be the simplest option.</span></span>
- <span data-ttu-id="c6097-151">**Esteja atento às corridas de linha.**</span><span class="sxs-lookup"><span data-stu-id="c6097-151">**Be aware of thread races**.</span></span> <span data-ttu-id="c6097-152">O carregamento pode ser acionado por vários segmentos.</span><span class="sxs-lookup"><span data-stu-id="c6097-152">Loading can be triggered by multiple threads.</span></span> <span data-ttu-id="c6097-153">O AssemblyLoadContext lida com corridas de segmento adicionando atomicamente conjuntos ao seu cache.</span><span class="sxs-lookup"><span data-stu-id="c6097-153">The AssemblyLoadContext handles thread races by atomically adding assemblies to its cache.</span></span> <span data-ttu-id="c6097-154">A instância do perdedor da corrida está descartada.</span><span class="sxs-lookup"><span data-stu-id="c6097-154">The race loser's instance is discarded.</span></span> <span data-ttu-id="c6097-155">Em sua lógica de implementação, não adicione lógica extra que não manuseie vários segmentos corretamente.</span><span class="sxs-lookup"><span data-stu-id="c6097-155">In your implementation logic, don't add extra logic that doesn't handle multiple threads properly.</span></span>

## <a name="how-are-dynamic-dependencies-isolated"></a><span data-ttu-id="c6097-156">Como as dependências dinâmicas são isoladas?</span><span class="sxs-lookup"><span data-stu-id="c6097-156">How are dynamic dependencies isolated?</span></span>

<span data-ttu-id="c6097-157">Cada <xref:System.Runtime.Loader.AssemblyLoadContext> instância representa um <xref:System.Reflection.Assembly> escopo <xref:System.Type> único para instâncias e definições.</span><span class="sxs-lookup"><span data-stu-id="c6097-157">Each <xref:System.Runtime.Loader.AssemblyLoadContext> instance represents a unique scope for <xref:System.Reflection.Assembly> instances and <xref:System.Type> definitions.</span></span>

<span data-ttu-id="c6097-158">Não há isolamento binário entre essas dependências.</span><span class="sxs-lookup"><span data-stu-id="c6097-158">There's no binary isolation between these dependencies.</span></span> <span data-ttu-id="c6097-159">Eles só estão isolados por não se encontrarem pelo nome.</span><span class="sxs-lookup"><span data-stu-id="c6097-159">They're only isolated by not finding each other by name.</span></span>

<span data-ttu-id="c6097-160">Em <xref:System.Runtime.Loader.AssemblyLoadContext>cada um:</span><span class="sxs-lookup"><span data-stu-id="c6097-160">In each <xref:System.Runtime.Loader.AssemblyLoadContext>:</span></span>

- <span data-ttu-id="c6097-161"><xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType>pode se referir <xref:System.Reflection.Assembly> a uma instância diferente.</span><span class="sxs-lookup"><span data-stu-id="c6097-161"><xref:System.Reflection.AssemblyName.Name?displayProperty=nameWithType> may refer to a different <xref:System.Reflection.Assembly> instance.</span></span>
- <span data-ttu-id="c6097-162"><xref:System.Type.GetType%2A?displayProperty=nameWithType>pode retornar uma instância de `name`tipo diferente para o mesmo tipo .</span><span class="sxs-lookup"><span data-stu-id="c6097-162"><xref:System.Type.GetType%2A?displayProperty=nameWithType> may return a different type instance for the same type `name`.</span></span>

## <a name="how-are-dependencies-shared"></a><span data-ttu-id="c6097-163">Como as dependências são compartilhadas?</span><span class="sxs-lookup"><span data-stu-id="c6097-163">How are dependencies shared?</span></span>

<span data-ttu-id="c6097-164">As dependências podem ser <xref:System.Runtime.Loader.AssemblyLoadContext> facilmente compartilhadas entre instâncias.</span><span class="sxs-lookup"><span data-stu-id="c6097-164">Dependencies can easily be shared between <xref:System.Runtime.Loader.AssemblyLoadContext> instances.</span></span> <span data-ttu-id="c6097-165">O modelo geral <xref:System.Runtime.Loader.AssemblyLoadContext> é para se carregar uma dependência.</span><span class="sxs-lookup"><span data-stu-id="c6097-165">The general model is for one <xref:System.Runtime.Loader.AssemblyLoadContext> to load a dependency.</span></span>  <span data-ttu-id="c6097-166">O outro compartilha a dependência usando uma referência ao conjunto carregado.</span><span class="sxs-lookup"><span data-stu-id="c6097-166">The other shares the dependency by using a reference to the loaded assembly.</span></span>

<span data-ttu-id="c6097-167">Este compartilhamento é necessário para as assembléias em tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="c6097-167">This sharing is required of the runtime assemblies.</span></span> <span data-ttu-id="c6097-168">Estas assembléias só podem <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType>ser carregadas no .</span><span class="sxs-lookup"><span data-stu-id="c6097-168">These assemblies can only be loaded into the <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType>.</span></span> <span data-ttu-id="c6097-169">O mesmo é necessário `ASP.NET`para `WPF`estruturas `WinForms`como, ou .</span><span class="sxs-lookup"><span data-stu-id="c6097-169">The same is required for frameworks like `ASP.NET`, `WPF`, or `WinForms`.</span></span>

<span data-ttu-id="c6097-170">Recomenda-se que as dependências compartilhadas sejam <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType>carregadas em .</span><span class="sxs-lookup"><span data-stu-id="c6097-170">It's recommended that shared dependencies should be loaded into <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType>.</span></span> <span data-ttu-id="c6097-171">Esse compartilhamento é o padrão de design comum.</span><span class="sxs-lookup"><span data-stu-id="c6097-171">This sharing is the common design pattern.</span></span>

<span data-ttu-id="c6097-172">O compartilhamento é implementado na <xref:System.Runtime.Loader.AssemblyLoadContext> codificação da instância personalizada.</span><span class="sxs-lookup"><span data-stu-id="c6097-172">Sharing is implemented in the coding of the custom <xref:System.Runtime.Loader.AssemblyLoadContext> instance.</span></span> <span data-ttu-id="c6097-173"><xref:System.Runtime.Loader.AssemblyLoadContext>tem vários eventos e funções virtuais que podem ser substituídos.</span><span class="sxs-lookup"><span data-stu-id="c6097-173"><xref:System.Runtime.Loader.AssemblyLoadContext> has various events and virtual functions that can be overridden.</span></span> <span data-ttu-id="c6097-174">Quando qualquer uma dessas funções <xref:System.Reflection.Assembly> retorna uma referência a <xref:System.Runtime.Loader.AssemblyLoadContext> uma <xref:System.Reflection.Assembly> instância que foi carregada em outra instância, a instância é compartilhada.</span><span class="sxs-lookup"><span data-stu-id="c6097-174">When any of these functions return a reference to an <xref:System.Reflection.Assembly> instance that was loaded in another <xref:System.Runtime.Loader.AssemblyLoadContext> instance, the <xref:System.Reflection.Assembly> instance is shared.</span></span> <span data-ttu-id="c6097-175">O algoritmo de carga <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> padrão adia para o carregamento para simplificar o padrão de compartilhamento comum.</span><span class="sxs-lookup"><span data-stu-id="c6097-175">The standard load algorithm defers to <xref:System.Runtime.Loader.AssemblyLoadContext.Default?displayProperty=nameWithType> for loading to simplify the common sharing pattern.</span></span>  <span data-ttu-id="c6097-176">Consulte [algoritmo de carregamento de montagem gerenciado](loading-managed.md).</span><span class="sxs-lookup"><span data-stu-id="c6097-176">See [Managed assembly loading algorithm](loading-managed.md).</span></span>

## <a name="complications"></a><span data-ttu-id="c6097-177">Complicações</span><span class="sxs-lookup"><span data-stu-id="c6097-177">Complications</span></span>

### <a name="type-conversion-issues"></a><span data-ttu-id="c6097-178">Problemas de conversão de tipo</span><span class="sxs-lookup"><span data-stu-id="c6097-178">Type conversion issues</span></span>

<span data-ttu-id="c6097-179">Quando <xref:System.Runtime.Loader.AssemblyLoadContext> duas instâncias contêm `name`definições de tipo com o mesmo, elas não são do mesmo tipo.</span><span class="sxs-lookup"><span data-stu-id="c6097-179">When two <xref:System.Runtime.Loader.AssemblyLoadContext> instances contain type definitions with the same `name`, they're not the same type.</span></span> <span data-ttu-id="c6097-180">Eles são do mesmo tipo se e somente <xref:System.Reflection.Assembly> se eles vêm da mesma instância.</span><span class="sxs-lookup"><span data-stu-id="c6097-180">They're the same type if and only if they come from the same <xref:System.Reflection.Assembly> instance.</span></span>

<span data-ttu-id="c6097-181">Para complicar as coisas, mensagens de exceção sobre esses tipos incompatíveis podem ser confusas.</span><span class="sxs-lookup"><span data-stu-id="c6097-181">To complicate matters, exception messages about these mismatched types can be confusing.</span></span> <span data-ttu-id="c6097-182">Os tipos são referidos nas mensagens de exceção por seus nomes de tipos simples.</span><span class="sxs-lookup"><span data-stu-id="c6097-182">The types are referred to in the exception messages by their simple type names.</span></span> <span data-ttu-id="c6097-183">A mensagem de exceção comum neste caso seria do formulário:</span><span class="sxs-lookup"><span data-stu-id="c6097-183">The common exception message in this case would be of the form:</span></span>

> <span data-ttu-id="c6097-184">O objeto do tipo 'IsolatedType' não pode ser convertido para digitar 'IsolatedType'.</span><span class="sxs-lookup"><span data-stu-id="c6097-184">Object of type 'IsolatedType' cannot be converted to type 'IsolatedType'.</span></span>

### <a name="debugging-type-conversion-issues"></a><span data-ttu-id="c6097-185">Problemas de conversão de tipo de depuração</span><span class="sxs-lookup"><span data-stu-id="c6097-185">Debugging type conversion issues</span></span>

<span data-ttu-id="c6097-186">Dado um par de tipos incompatíveis, é importante também saber:</span><span class="sxs-lookup"><span data-stu-id="c6097-186">Given a pair of mismatched types it's important to also know:</span></span>

- <span data-ttu-id="c6097-187">Cada tipo<xref:System.Type.Assembly?displayProperty=nameWithType></span><span class="sxs-lookup"><span data-stu-id="c6097-187">Each type's <xref:System.Type.Assembly?displayProperty=nameWithType></span></span>
- <span data-ttu-id="c6097-188">Cada tipo <xref:System.Runtime.Loader.AssemblyLoadContext>é, que pode ser <xref:System.Runtime.Loader.AssemblyLoadContext.GetLoadContext(System.Reflection.Assembly)?displayProperty=nameWithType> obtido através da função.</span><span class="sxs-lookup"><span data-stu-id="c6097-188">Each type's <xref:System.Runtime.Loader.AssemblyLoadContext>, which can be obtained via the <xref:System.Runtime.Loader.AssemblyLoadContext.GetLoadContext(System.Reflection.Assembly)?displayProperty=nameWithType> function.</span></span>

<span data-ttu-id="c6097-189">Dado `a` dois `b`objetos e, avaliando o seguinte no depurador será útil:</span><span class="sxs-lookup"><span data-stu-id="c6097-189">Given two objects `a` and `b`, evaluating the following in the debugger will be helpful:</span></span>

```csharp
// In debugger look at each assembly's instance, Location, and FullName
a.GetType().Assembly
b.GetType().Assembly
// In debugger look at each AssemblyLoadContext's instance and name
System.Runtime.Loader.AssemblyLoadContext.GetLoadContext(a.GetType().Assembly)
System.Runtime.Loader.AssemblyLoadContext.GetLoadContext(b.GetType().Assembly)
```

### <a name="resolving-type-conversion-issues"></a><span data-ttu-id="c6097-190">Resolução de problemas de conversão de tipo</span><span class="sxs-lookup"><span data-stu-id="c6097-190">Resolving type conversion issues</span></span>

<span data-ttu-id="c6097-191">Existem dois padrões de design para resolver esses problemas de conversão de tipo.</span><span class="sxs-lookup"><span data-stu-id="c6097-191">There are two design patterns for solving these type conversion issues.</span></span>

1. <span data-ttu-id="c6097-192">Use tipos comuns compartilhados.</span><span class="sxs-lookup"><span data-stu-id="c6097-192">Use common shared types.</span></span> <span data-ttu-id="c6097-193">Este tipo compartilhado pode ser um tipo primitivo de tempo de execução, ou pode envolver a criação de um novo tipo compartilhado em um conjunto compartilhado.</span><span class="sxs-lookup"><span data-stu-id="c6097-193">This shared type can either be a primitive runtime type, or it can involve creating a new shared type in a shared assembly.</span></span>  <span data-ttu-id="c6097-194">Muitas vezes, o tipo compartilhado é uma [interface](../../csharp/language-reference/keywords/interface.md) definida em um conjunto de aplicativos.</span><span class="sxs-lookup"><span data-stu-id="c6097-194">Often the shared type is an [interface](../../csharp/language-reference/keywords/interface.md) defined in an application assembly.</span></span> <span data-ttu-id="c6097-195">Veja também: [Como as dependências são compartilhadas?](#how-are-dependencies-shared).</span><span class="sxs-lookup"><span data-stu-id="c6097-195">See also: [How are dependencies shared?](#how-are-dependencies-shared).</span></span>

2. <span data-ttu-id="c6097-196">Use técnicas de empacotamento para converter de um tipo para outro.</span><span class="sxs-lookup"><span data-stu-id="c6097-196">Use marshaling techniques to convert from one type to another.</span></span>
