---
title: Migrando C++projetos/CLI para o .NET Core
description: Saiba mais sobre como C++portar projetos de/CLI para o .NET Core.
author: mjrousos
ms.date: 01/10/2020
ms.openlocfilehash: eb03f2a5ff42e8279fd3ebd6ee6fb6d955f6798d
ms.sourcegitcommit: c01c18755bb7b0f82c7232314ccf7955ea7834db
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 01/15/2020
ms.locfileid: "75964926"
---
# <a name="how-to-port-a-ccli-project-to-net-core"></a><span data-ttu-id="f515a-103">Como portar um C++projeto/CLI para o .NET Core</span><span class="sxs-lookup"><span data-stu-id="f515a-103">How to port a C++/CLI project to .NET Core</span></span>

<span data-ttu-id="f515a-104">A partir do .NET Core 3,1 e do Visual Studio 2019 versão 16,4, [ C++os projetos/CLI](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) podem ter como destino o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-104">Beginning with .NET Core 3.1 and Visual Studio 2019 version 16.4, [C++/CLI projects](/cpp/dotnet/dotnet-programming-with-cpp-cli-visual-cpp) can target .NET Core.</span></span> <span data-ttu-id="f515a-105">Esse suporte torna possível a porta de aplicativos da área de C++trabalho do Windows com camadas de interoperabilidade/CLI para o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-105">This support makes it possible to port Windows desktop applications with C++/CLI interop layers to .NET Core.</span></span> <span data-ttu-id="f515a-106">Este artigo descreve como portar C++projetos da/cli do .NET Framework para o .net Core 3,1.</span><span class="sxs-lookup"><span data-stu-id="f515a-106">This article describes how to port C++/CLI projects from .NET Framework to .NET Core 3.1.</span></span>

## <a name="ccli-net-core-limitations"></a><span data-ttu-id="f515a-107">C++Limitações do .NET Core/CLI</span><span class="sxs-lookup"><span data-stu-id="f515a-107">C++/CLI .NET Core limitations</span></span>

<span data-ttu-id="f515a-108">Há algumas limitações importantes para portar projetos C++/CLI para o .NET Core em comparação com outras linguagens:</span><span class="sxs-lookup"><span data-stu-id="f515a-108">There are some important limitations to porting C++/CLI projects to .NET Core compared to other languages:</span></span>

* <span data-ttu-id="f515a-109">C++O suporte a/CLI para .NET Core é somente Windows.</span><span class="sxs-lookup"><span data-stu-id="f515a-109">C++/CLI support for .NET Core is Windows only.</span></span>
* <span data-ttu-id="f515a-110">C++Os projetos/CLI não podem ter como destino .NET Standard, somente o .NET Core (ou .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="f515a-110">C++/CLI projects can't target .NET Standard, only .NET Core (or .NET Framework).</span></span>
* <span data-ttu-id="f515a-111">C++Os projetos/CLI não dão suporte ao novo formato de arquivo de projeto em estilo SDK.</span><span class="sxs-lookup"><span data-stu-id="f515a-111">C++/CLI projects don't support the new SDK-style project file format.</span></span> <span data-ttu-id="f515a-112">Em vez disso, mesmo ao direcionar C++o .NET Core, os projetos/CLI usam o formato de arquivo vcxproj existente.</span><span class="sxs-lookup"><span data-stu-id="f515a-112">Instead, even when targeting .NET Core, C++/CLI projects use the existing vcxproj file format.</span></span>
* <span data-ttu-id="f515a-113">C++Projetos/CLI não podem ter vários destinos em várias plataformas .NET.</span><span class="sxs-lookup"><span data-stu-id="f515a-113">C++/CLI projects can't multitarget multiple .NET platforms.</span></span> <span data-ttu-id="f515a-114">Se você precisar criar um C++projeto/CLI para o .NET Framework e o .NET Core, use arquivos de projeto separados.</span><span class="sxs-lookup"><span data-stu-id="f515a-114">If you need to build a C++/CLI project for both .NET Framework and .NET Core, use separate project files.</span></span>
* <span data-ttu-id="f515a-115">O .NET Core não dá suporte à compilação `-clr:pure` ou `-clr:safe`, somente a nova opção `-clr:netcore` (que é equivalente a `-clr` para .NET Framework).</span><span class="sxs-lookup"><span data-stu-id="f515a-115">.NET Core doesn't support `-clr:pure` or `-clr:safe` compilation, only the new `-clr:netcore` option (which is equivalent to `-clr` for .NET Framework).</span></span>

## <a name="port-a-ccli-project"></a><span data-ttu-id="f515a-116">Porta um C++projeto/CLI</span><span class="sxs-lookup"><span data-stu-id="f515a-116">Port a C++/CLI project</span></span>

<span data-ttu-id="f515a-117">Para portar C++um projeto/CLI para o .NET Core, faça as seguintes alterações no arquivo vcxproj.</span><span class="sxs-lookup"><span data-stu-id="f515a-117">To port a C++/CLI project to .NET Core, make the following changes to the vcxproj file.</span></span> <span data-ttu-id="f515a-118">Essas etapas de migração diferem das etapas necessárias para outros tipos C++de projeto porque os projetos/CLI não usam arquivos de projeto em estilo SDK.</span><span class="sxs-lookup"><span data-stu-id="f515a-118">These migration steps differ from the steps needed for other project types because C++/CLI projects don't use SDK-style project files.</span></span>

1. <span data-ttu-id="f515a-119">Substitua `<CLRSupport>true</CLRSupport>` Propriedades por `<CLRSupport>NetCore</CLRSupport>`.</span><span class="sxs-lookup"><span data-stu-id="f515a-119">Replace `<CLRSupport>true</CLRSupport>` properties with `<CLRSupport>NetCore</CLRSupport>`.</span></span> <span data-ttu-id="f515a-120">Essa propriedade geralmente está em grupos de Propriedades específicos da configuração, portanto, talvez seja necessário substituí-la em vários locais.</span><span class="sxs-lookup"><span data-stu-id="f515a-120">This property is often in configuration-specific property groups, so you may need to replace it in multiple places.</span></span>
2. <span data-ttu-id="f515a-121">Substitua `<TargetFrameworkVersion>` Propriedades por `<TargetFramework>netcoreapp3.1</TargetFramework>`.</span><span class="sxs-lookup"><span data-stu-id="f515a-121">Replace `<TargetFrameworkVersion>` properties with `<TargetFramework>netcoreapp3.1</TargetFramework>`.</span></span>
3. <span data-ttu-id="f515a-122">Remova todas as referências de .NET Framework (como `<Reference Include="System" />`).</span><span class="sxs-lookup"><span data-stu-id="f515a-122">Remove any .NET Framework references (like `<Reference Include="System" />`).</span></span> <span data-ttu-id="f515a-123">SDK do .NET Core assemblies são automaticamente referenciados ao usar `<CLRSupport>NetCore</CLRSupport>`.</span><span class="sxs-lookup"><span data-stu-id="f515a-123">.NET Core SDK assemblies are automatically referenced when using `<CLRSupport>NetCore</CLRSupport>`.</span></span>
4. <span data-ttu-id="f515a-124">Atualize o uso da API em arquivos CPP, conforme necessário, para remover APIs indisponíveis para o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-124">Update API usage in cpp files, as necessary, to remove APIs unavailable to .NET Core.</span></span> <span data-ttu-id="f515a-125">Como C++os projetos/CLI tendem a ser bastante finos camadas de interoperabilidade, muitas vezes não há muitas alterações necessárias.</span><span class="sxs-lookup"><span data-stu-id="f515a-125">Because C++/CLI projects tend to be fairly thin interop layers, there are often not many changes needed.</span></span> <span data-ttu-id="f515a-126">O [.net Portability Analyzer](../../standard/analyzers/portability-analyzer.md) pode ser usado para identificar APIs .NET sem suporte usadas por C++binários/CLI, assim como com binários puramente gerenciados.</span><span class="sxs-lookup"><span data-stu-id="f515a-126">The [.NET Portability Analyzer](../../standard/analyzers/portability-analyzer.md) can be used to identify unsupported .NET APIs used by C++/CLI binaries just as with purely managed binaries.</span></span> <span data-ttu-id="f515a-127">As diretrizes para determinar a portabilidade do código e a atualização de projetos para trabalhar com as APIs do .NET Core estão disponíveis nas [diretrizes de portabilidade da biblioteca](./libraries.md#determine-portability).</span><span class="sxs-lookup"><span data-stu-id="f515a-127">Guidelines for determining code portability and updating projects to work with .NET Core APIs are available in the [library porting guidance](./libraries.md#determine-portability).</span></span>

### <a name="wpf-and-windows-forms-usage"></a><span data-ttu-id="f515a-128">Uso do WPF e do Windows Forms</span><span class="sxs-lookup"><span data-stu-id="f515a-128">WPF and Windows Forms usage</span></span>

<span data-ttu-id="f515a-129">Os projetos C++do .NET Core/CLI podem usar Windows Forms e APIs do WPF.</span><span class="sxs-lookup"><span data-stu-id="f515a-129">.NET Core C++/CLI projects can use Windows Forms and WPF APIs.</span></span> <span data-ttu-id="f515a-130">Para usar essas APIs de área de trabalho do Windows, você precisa adicionar referências de estrutura explícitas às bibliotecas de interface do usuário.</span><span class="sxs-lookup"><span data-stu-id="f515a-130">To use these Windows desktop APIs, you need to add explicit framework references to the UI libraries.</span></span> <span data-ttu-id="f515a-131">Os projetos no estilo SDK que usam as APIs da área de trabalho do Windows fazem referência automaticamente às bibliotecas de estrutura necessárias usando o SDK do `Microsoft.NET.Sdk.WindowsDesktop`.</span><span class="sxs-lookup"><span data-stu-id="f515a-131">SDK-style projects that use Windows desktop APIs reference the necessary framework libraries automatically by using the `Microsoft.NET.Sdk.WindowsDesktop` SDK.</span></span> <span data-ttu-id="f515a-132">Como C++os projetos/CLI não usam o formato de projeto no estilo SDK, eles precisam adicionar referências de estrutura explícitas ao direcionar o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-132">Because C++/CLI projects don't use the SDK-style project format, they need to add explicit framework references when targeting .NET Core.</span></span>

<span data-ttu-id="f515a-133">Para usar Windows Forms APIs, adicione essa referência ao arquivo vcxproj:</span><span class="sxs-lookup"><span data-stu-id="f515a-133">To use Windows Forms APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of Windows Forms -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WindowsForms" />
```

<span data-ttu-id="f515a-134">Para usar APIs do WPF, adicione essa referência ao arquivo vcxproj:</span><span class="sxs-lookup"><span data-stu-id="f515a-134">To use WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference all of WPF -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App.WPF" />
```

<span data-ttu-id="f515a-135">Para usar as APIs Windows Forms e WPF, adicione essa referência ao arquivo vcxproj:</span><span class="sxs-lookup"><span data-stu-id="f515a-135">To use both Windows Forms and WPF APIs, add this reference to the vcxproj file:</span></span>

```xml
<!-- Reference the entirety of the Windows desktop framework:
     Windows Forms, WPF, and the types that provide integration between them -->
<FrameworkReference Include="Microsoft.WindowsDesktop.App" />
```

<span data-ttu-id="f515a-136">Atualmente, não é possível adicionar essas referências usando o Gerenciador de referências do Visual Studio.</span><span class="sxs-lookup"><span data-stu-id="f515a-136">Currently, it's not possible to add these references using Visual Studio's reference manager.</span></span> <span data-ttu-id="f515a-137">Em vez disso, atualize o arquivo de projeto manualmente.</span><span class="sxs-lookup"><span data-stu-id="f515a-137">Instead, update the project file manually.</span></span> <span data-ttu-id="f515a-138">Essa atualização pode ser feita no Visual Studio descarregando o projeto e editando o arquivo de projeto.</span><span class="sxs-lookup"><span data-stu-id="f515a-138">This update can be done in Visual Studio by unloading the project and then editing the project file.</span></span> <span data-ttu-id="f515a-139">Você também pode usar outro editor como VS Code.</span><span class="sxs-lookup"><span data-stu-id="f515a-139">You can also use another editor like VS Code.</span></span>

## <a name="build-without-msbuild"></a><span data-ttu-id="f515a-140">Compilar sem MSBuild</span><span class="sxs-lookup"><span data-stu-id="f515a-140">Build without MSBuild</span></span>

<span data-ttu-id="f515a-141">Também é possível criar C++projetos/CLI sem usar o MSBuild.</span><span class="sxs-lookup"><span data-stu-id="f515a-141">It's also possible to build C++/CLI projects without using MSBuild.</span></span> <span data-ttu-id="f515a-142">Siga estas etapas para criar um C++projeto/CLI para o .NET Core diretamente com *CL. exe* e *link. exe*:</span><span class="sxs-lookup"><span data-stu-id="f515a-142">Follow these steps to build a C++/CLI project for .NET Core directly with *cl.exe* and *link.exe*:</span></span>

1. <span data-ttu-id="f515a-143">Ao compilar, passe `-clr:netcore` para *CL. exe*.</span><span class="sxs-lookup"><span data-stu-id="f515a-143">When compiling, pass `-clr:netcore` to *cl.exe*.</span></span>
2. <span data-ttu-id="f515a-144">Referência dos assemblies de referência do .NET Core necessários.</span><span class="sxs-lookup"><span data-stu-id="f515a-144">Reference necessary .NET Core reference assemblies.</span></span>
3. <span data-ttu-id="f515a-145">Ao vincular, forneça o diretório do host do aplicativo .NET Core como um `LibPath` (para que *ijwhost. lib* possa ser encontrado).</span><span class="sxs-lookup"><span data-stu-id="f515a-145">When linking, provide the .NET Core app host directory as a `LibPath` (so that *ijwhost.lib* can be found).</span></span>
4. <span data-ttu-id="f515a-146">Copie *ijwhost. dll* (do diretório de host do aplicativo .NET Core) para o diretório de saída do projeto.</span><span class="sxs-lookup"><span data-stu-id="f515a-146">Copy *ijwhost.dll* (from the .NET Core app host directory) to the project's output directory.</span></span>
5. <span data-ttu-id="f515a-147">Verifique se um arquivo [runtimeconfig. JSON](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) existe para o primeiro componente do aplicativo que executará o código gerenciado.</span><span class="sxs-lookup"><span data-stu-id="f515a-147">Make sure a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file exists for the first component of the application that will run managed code.</span></span> <span data-ttu-id="f515a-148">Se o aplicativo tiver um ponto de entrada gerenciado, um arquivo de `runtime.config` será criado e copiado automaticamente.</span><span class="sxs-lookup"><span data-stu-id="f515a-148">If the application has a managed entry point, a `runtime.config` file will be created and copied automatically.</span></span> <span data-ttu-id="f515a-149">No entanto, se o aplicativo tiver um ponto de entrada nativo, você precisará criar um arquivo `runtimeconfig.json` C++para a primeira biblioteca/CLI usar o tempo de execução do .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-149">If the application has a native entry point, though, you need to create a `runtimeconfig.json` file for the first C++/CLI library to use the .NET Core runtime.</span></span>

## <a name="known-issues"></a><span data-ttu-id="f515a-150">Problemas conhecidos</span><span class="sxs-lookup"><span data-stu-id="f515a-150">Known issues</span></span>

<span data-ttu-id="f515a-151">Há alguns problemas conhecidos a serem verificados ao trabalhar com C++projetos/CLI visando o .NET Core.</span><span class="sxs-lookup"><span data-stu-id="f515a-151">There are a couple known issues to look out for when working with C++/CLI projects targeting .NET Core.</span></span>

* <span data-ttu-id="f515a-152">Uma referência de estrutura do WPF em C++projetos do .NET Core/CLI atualmente faz com que alguns avisos estranhos não conseguirem importar símbolos.</span><span class="sxs-lookup"><span data-stu-id="f515a-152">A WPF framework reference in .NET Core C++/CLI projects currently causes some extraneous warnings about being unable to import symbols.</span></span> <span data-ttu-id="f515a-153">Esses avisos podem ser ignorados com segurança e devem ser corrigidos em breve.</span><span class="sxs-lookup"><span data-stu-id="f515a-153">These warnings can be safely ignored and should be fixed soon.</span></span>
* <span data-ttu-id="f515a-154">Se o aplicativo tiver um ponto de entrada nativo, C++a biblioteca/CLI que primeiro executa o código gerenciado precisa de um arquivo [runtimeconfig. JSON](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) .</span><span class="sxs-lookup"><span data-stu-id="f515a-154">If the application has a native entry point, the C++/CLI library that first executes managed code needs a [runtimeconfig.json](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md) file.</span></span> <span data-ttu-id="f515a-155">Esse arquivo de configuração é usado quando o tempo de execução do .NET Core é iniciado.</span><span class="sxs-lookup"><span data-stu-id="f515a-155">This config file is used when the .NET Core runtime starts.</span></span> <span data-ttu-id="f515a-156">C++Os projetos/CLI não criam `runtimeconfig.json` arquivos automaticamente no momento da compilação, portanto, o arquivo deve ser gerado manualmente.</span><span class="sxs-lookup"><span data-stu-id="f515a-156">C++/CLI projects don't create `runtimeconfig.json` files automatically at build time yet, so the file must be generated manually.</span></span> <span data-ttu-id="f515a-157">Se uma C++biblioteca/CLI for chamada de um ponto de entrada gerenciado, a C++biblioteca/CLI não precisará de um arquivo de `runtimeconfig.json` (já que o assembly de ponto de entrada terá um que é usado ao iniciar o tempo de execução).</span><span class="sxs-lookup"><span data-stu-id="f515a-157">If a C++/CLI library is called from a managed entry point, then the C++/CLI library doesn't need a `runtimeconfig.json` file (since the entry point assembly will have one that is used when starting the runtime).</span></span> <span data-ttu-id="f515a-158">Um arquivo de `runtimeconfig.json` de exemplo simples é mostrado abaixo.</span><span class="sxs-lookup"><span data-stu-id="f515a-158">A simple sample `runtimeconfig.json` file is shown below.</span></span> <span data-ttu-id="f515a-159">Para obter mais informações, consulte a [especificação no GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span><span class="sxs-lookup"><span data-stu-id="f515a-159">For more information, see the [spec on GitHub](https://github.com/dotnet/cli/blob/master/Documentation/specs/runtime-configuration-file.md).</span></span>

    ```json
    {
          "runtimeOptions": {
             "tfm": "netcoreapp3.1",
             "framework": {
                "name": "Microsoft.NETCore.App",
                "version": "3.1.0"
             }
          }
    }
    ```
