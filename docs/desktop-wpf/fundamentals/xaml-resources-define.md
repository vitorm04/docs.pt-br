---
title: Definir recursos XAML
description: Saiba mais sobre os recursos XAML no WPF para .NET Core. Entenda os tipos de recursos XAML e saiba como definir recursos XAML.
author: thraka
ms.author: adegeo
ms.date: 08/21/2019
ms.openlocfilehash: b278bb92afc308578d60e347871e0150b26a95db
ms.sourcegitcommit: f8c36054eab877de4d40a705aacafa2552ce70e9
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 12/31/2019
ms.locfileid: "82071279"
---
# <a name="overview-of-xaml-resources"></a><span data-ttu-id="eaae3-104">Visão geral dos recursos XAML</span><span class="sxs-lookup"><span data-stu-id="eaae3-104">Overview of XAML resources</span></span>

<span data-ttu-id="eaae3-105">Um recurso é um objeto que pode ser reutilizado em locais diferentes em seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-105">A resource is an object that can be reused in different places in your app.</span></span> <span data-ttu-id="eaae3-106">Exemplos de recursos incluem pincéis e estilos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-106">Examples of resources include brushes and styles.</span></span> <span data-ttu-id="eaae3-107">Esta visão geral descreve como usar os recursos no Extensible Application Markup Language (XAML).</span><span class="sxs-lookup"><span data-stu-id="eaae3-107">This overview describes how to use resources in Extensible Application Markup Language (XAML).</span></span> <span data-ttu-id="eaae3-108">Você também pode criar e acessar recursos usando código.</span><span class="sxs-lookup"><span data-stu-id="eaae3-108">You can also create and access resources by using code.</span></span>

> [!NOTE]
> <span data-ttu-id="eaae3-109">Os recursos XAML descritos neste artigo são diferentes dos *recursos de aplicativo* que geralmente são arquivos adicionados a um aplicativo, como conteúdo, dados ou arquivos inseridos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-109">XAML resources described in this article are different from *app resources* which are generally files added to an app, such as content, data, or embedded files.</span></span>

<!-- TODO: File redirect from docs\framework\wpf\advanced\xaml-resources.md -->

[!INCLUDE [desktop guide under construction](../../../includes/desktop-guide-preview-note.md)]

## <a name="using-resources-in-xaml"></a><span data-ttu-id="eaae3-110">Usando recursos em XAML</span><span class="sxs-lookup"><span data-stu-id="eaae3-110">Using resources in XAML</span></span>

<span data-ttu-id="eaae3-111">O exemplo a seguir define <xref:System.Windows.Media.SolidColorBrush> um como um recurso no elemento raiz de uma página.</span><span class="sxs-lookup"><span data-stu-id="eaae3-111">The following example defines a <xref:System.Windows.Media.SolidColorBrush> as a resource on the root element of a page.</span></span> <span data-ttu-id="eaae3-112">O exemplo faz referência ao recurso e o usa para definir as propriedades de vários elementos filho, incluindo <xref:System.Windows.Shapes.Ellipse>um, <xref:System.Windows.Controls.TextBlock>um e um <xref:System.Windows.Controls.Button>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-112">The example then references the resource and uses it to set properties of several child elements, including an <xref:System.Windows.Shapes.Ellipse>, a <xref:System.Windows.Controls.TextBlock>, and a <xref:System.Windows.Controls.Button>.</span></span>

[!code-xaml[FEResourceSH_snip#XAML](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page1.xaml#xaml)]

<span data-ttu-id="eaae3-113">Cada elemento de nível de estrutura<xref:System.Windows.FrameworkElement> ( <xref:System.Windows.FrameworkContentElement>ou) tem <xref:System.Windows.FrameworkElement.Resources%2A> uma propriedade, que é <xref:System.Windows.ResourceDictionary> um tipo que contém recursos definidos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-113">Every framework-level element (<xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement>) has a <xref:System.Windows.FrameworkElement.Resources%2A> property, which is a <xref:System.Windows.ResourceDictionary> type that contains defined resources.</span></span> <span data-ttu-id="eaae3-114">Você pode definir recursos em qualquer elemento, como um <xref:System.Windows.Controls.Button>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-114">You can define resources on any element, such as a <xref:System.Windows.Controls.Button>.</span></span> <span data-ttu-id="eaae3-115">No entanto, os recursos são definidos com mais frequência no elemento raiz <xref:System.Windows.Controls.Page> , que está no exemplo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-115">However, resources are most often defined on the root element, which is <xref:System.Windows.Controls.Page> in the example.</span></span>

<span data-ttu-id="eaae3-116">Cada recurso em um dicionário de recursos deve ter uma chave exclusiva.</span><span class="sxs-lookup"><span data-stu-id="eaae3-116">Each resource in a resource dictionary must have a unique key.</span></span> <span data-ttu-id="eaae3-117">Ao definir recursos na marcação, você atribui a chave exclusiva por meio da [diretiva x:Key](../xaml-services/xkey-directive.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-117">When you define resources in markup, you assign the unique key through the [x:Key Directive](../xaml-services/xkey-directive.md).</span></span> <span data-ttu-id="eaae3-118">Normalmente, a chave é uma cadeia de caracteres; no entanto, você pode também a definir com outros tipos de objeto usando as extensões de marcação apropriadas.</span><span class="sxs-lookup"><span data-stu-id="eaae3-118">Typically, the key is a string; however, you can also set it to other object types by using the appropriate markup extensions.</span></span> <span data-ttu-id="eaae3-119">Chaves de não cadeia de caracteres para recursos são usadas por determinadas áreas de recursos no WPF, especialmente para estilos, recursos de componente e estilo de dados.</span><span class="sxs-lookup"><span data-stu-id="eaae3-119">Non-string keys for resources are used by certain feature areas in WPF, notably for styles, component resources, and data styling.</span></span>

<span data-ttu-id="eaae3-120">Você pode usar um recurso definido com a sintaxe de extensão de marcação de recurso que especifica o nome da chave do recurso.</span><span class="sxs-lookup"><span data-stu-id="eaae3-120">You can use a defined resource with the resource markup extension syntax that specifies the key name of the resource.</span></span> <span data-ttu-id="eaae3-121">Por exemplo, use o recurso como o valor de uma propriedade em outro elemento.</span><span class="sxs-lookup"><span data-stu-id="eaae3-121">For example, use the resource as the value of a property on another element.</span></span>

[!code-xaml[FEResourceSH_snip#KeyNameUsage](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page2.xaml#keynameusage)]

<span data-ttu-id="eaae3-122">No exemplo anterior, quando o carregador XAML processa o `{StaticResource MyBrush}` valor da <xref:System.Windows.Controls.Control.Background%2A> Propriedade on <xref:System.Windows.Controls.Button>, a lógica de pesquisa de recurso primeiro verifica o dicionário de recursos para <xref:System.Windows.Controls.Button> o elemento.</span><span class="sxs-lookup"><span data-stu-id="eaae3-122">In the preceding example, when the XAML loader processes the value `{StaticResource MyBrush}` for the <xref:System.Windows.Controls.Control.Background%2A> property on <xref:System.Windows.Controls.Button>, the resource lookup logic first checks the resource dictionary for the <xref:System.Windows.Controls.Button> element.</span></span> <span data-ttu-id="eaae3-123">Se <xref:System.Windows.Controls.Button> não tiver uma definição da chave `MyBrush` de recurso (nesse exemplo, ela não é; sua coleção de recursos está vazia), a pesquisa em seguida verifica o <xref:System.Windows.Controls.Button>elemento pai de <xref:System.Windows.Controls.Page>, que é.</span><span class="sxs-lookup"><span data-stu-id="eaae3-123">If <xref:System.Windows.Controls.Button> doesn't have a definition of the resource key `MyBrush` (in that example it doesn't; its resource collection is empty), the lookup next checks the parent element of <xref:System.Windows.Controls.Button>, which is <xref:System.Windows.Controls.Page>.</span></span> <span data-ttu-id="eaae3-124">Se você definir um recurso no elemento <xref:System.Windows.Controls.Page> raiz, todos os elementos na árvore lógica do <xref:System.Windows.Controls.Page> poderão acessá-lo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-124">If you define a resource on the <xref:System.Windows.Controls.Page> root element, all the elements in the logical tree of the <xref:System.Windows.Controls.Page> can access it.</span></span> <span data-ttu-id="eaae3-125">E você pode reutilizar o mesmo recurso para definir o valor de qualquer propriedade que aceite o mesmo tipo que o recurso representa.</span><span class="sxs-lookup"><span data-stu-id="eaae3-125">And you can reuse the same resource for setting the value of any property that accepts the same type that the resource represents.</span></span> <span data-ttu-id="eaae3-126">No exemplo anterior, o `MyBrush` mesmo recurso define duas propriedades diferentes: o <xref:System.Windows.Controls.Control.Background%2A> de <xref:System.Windows.Controls.Button>a e o <xref:System.Windows.Shapes.Shape.Fill%2A> de a. <xref:System.Windows.Shapes.Rectangle></span><span class="sxs-lookup"><span data-stu-id="eaae3-126">In the previous example, the same `MyBrush` resource sets two different properties: the <xref:System.Windows.Controls.Control.Background%2A> of a <xref:System.Windows.Controls.Button>, and the <xref:System.Windows.Shapes.Shape.Fill%2A> of a <xref:System.Windows.Shapes.Rectangle>.</span></span>

## <a name="static-and-dynamic-resources"></a><span data-ttu-id="eaae3-127">Recursos estáticos e dinâmicos</span><span class="sxs-lookup"><span data-stu-id="eaae3-127">Static and dynamic resources</span></span>

<span data-ttu-id="eaae3-128">Um recurso pode ser referenciado como estático ou dinâmico.</span><span class="sxs-lookup"><span data-stu-id="eaae3-128">A resource can be referenced as either static or dynamic.</span></span> <span data-ttu-id="eaae3-129">As referências são criadas usando a [extensão de marcação StaticResource](../../framework/wpf/advanced/staticresource-markup-extension.md) ou a [extensão de marcação DynamicResource](../../framework/wpf/advanced/dynamicresource-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-129">References are created by using either the [StaticResource Markup Extension](../../framework/wpf/advanced/staticresource-markup-extension.md) or the [DynamicResource Markup Extension](../../framework/wpf/advanced/dynamicresource-markup-extension.md).</span></span> <span data-ttu-id="eaae3-130">Uma extensão de marcação é um recurso XAML que permite especificar uma referência de objeto fazendo com que a extensão de marcação processe a cadeia de caracteres de atributo e retorne o objeto para um carregador XAML.</span><span class="sxs-lookup"><span data-stu-id="eaae3-130">A markup extension is a XAML feature that lets you specify an object reference by having the markup extension process the attribute string and return the object to a XAML loader.</span></span> <span data-ttu-id="eaae3-131">Para obter mais informações sobre o comportamento de extensão de marcação, consulte [Markup Extensions e WPF XAML](../../framework/wpf/advanced/markup-extensions-and-wpf-xaml.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-131">For more information about markup extension behavior, see [Markup Extensions and WPF XAML](../../framework/wpf/advanced/markup-extensions-and-wpf-xaml.md).</span></span>

<span data-ttu-id="eaae3-132">Quando você usa uma extensão de marcação, normalmente fornece um ou mais parâmetros no formulário de cadeia de caracteres que são processados por essa extensão de marcação específica.</span><span class="sxs-lookup"><span data-stu-id="eaae3-132">When you use a markup extension, you typically provide one or more parameters in string form that are processed by that particular markup extension.</span></span> <span data-ttu-id="eaae3-133">A [extensão de marcação StaticResource](../../framework/wpf/advanced/staticresource-markup-extension.md) processa uma chave procurando o valor dessa chave em todos os dicionários de recursos disponíveis.</span><span class="sxs-lookup"><span data-stu-id="eaae3-133">The [StaticResource Markup Extension](../../framework/wpf/advanced/staticresource-markup-extension.md) processes a key by looking up the value for that key in all available resource dictionaries.</span></span> <span data-ttu-id="eaae3-134">O processamento ocorre durante o carregamento, que é quando o processo de carregamento precisa atribuir o valor da propriedade.</span><span class="sxs-lookup"><span data-stu-id="eaae3-134">Processing happens during load, which is when the loading process needs to assign the property value.</span></span> <span data-ttu-id="eaae3-135">Em vez disso, a [extensão de marcação DynamicResource](../../framework/wpf/advanced/dynamicresource-markup-extension.md) processa uma chave criando uma expressão, e essa expressão permanece não avaliada até que o aplicativo seja executado, no momento em que a expressão é avaliada e fornece um valor.</span><span class="sxs-lookup"><span data-stu-id="eaae3-135">The [DynamicResource Markup Extension](../../framework/wpf/advanced/dynamicresource-markup-extension.md) instead processes a key by creating an expression, and that expression remains unevaluated until the app runs, at which time the expression is evaluated and provides a value.</span></span>

<span data-ttu-id="eaae3-136">Quando você referencia um recurso, as seguintes considerações podem influenciar se uma referência de recurso estático ou dinâmico será usada:</span><span class="sxs-lookup"><span data-stu-id="eaae3-136">When you reference a resource, the following considerations can influence whether you use a static resource reference or a dynamic resource reference:</span></span>

- <span data-ttu-id="eaae3-137">Ao determinar o design geral de como você cria os recursos para seu aplicativo (por página, no aplicativo, em XAML flexível ou em um assembly somente de recursos), considere o seguinte:</span><span class="sxs-lookup"><span data-stu-id="eaae3-137">When determining the overall design of how you create the resources for your app (per page, in the app, in loose XAML, or in a resource-only assembly), consider the following:</span></span>

- <span data-ttu-id="eaae3-138">A funcionalidade do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-138">The app's functionality.</span></span> <span data-ttu-id="eaae3-139">Está atualizando os recursos em tempo real de seus requisitos de aplicativo?</span><span class="sxs-lookup"><span data-stu-id="eaae3-139">Are updating resources in real-time part of your app requirements?</span></span>
- <span data-ttu-id="eaae3-140">O respectivo comportamento de pesquisa desse tipo de referência de recurso.</span><span class="sxs-lookup"><span data-stu-id="eaae3-140">The respective lookup behavior of that resource reference type.</span></span>
- <span data-ttu-id="eaae3-141">A propriedade ou o tipo de recurso específico e o comportamento nativo desses tipos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-141">The particular property or resource type, and the native behavior of those types.</span></span>

## <a name="static-resources"></a><span data-ttu-id="eaae3-142">Recursos estáticos</span><span class="sxs-lookup"><span data-stu-id="eaae3-142">Static resources</span></span>

<span data-ttu-id="eaae3-143">As referências de recursos estáticos funcionam melhor nas seguintes circunstâncias:</span><span class="sxs-lookup"><span data-stu-id="eaae3-143">Static resource references work best for the following circumstances:</span></span>

- <span data-ttu-id="eaae3-144">Seu design de aplicativo concentra a maioria de seus recursos em dicionários de recursos em nível de página ou aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-144">Your app design concentrates most of its resources into page or application-level resource dictionaries.</span></span> <span data-ttu-id="eaae3-145">As referências a recursos estáticos não são reavaliadas com base em comportamentos de tempo de execução, como recarregar uma página.</span><span class="sxs-lookup"><span data-stu-id="eaae3-145">Static resource references aren't reevaluated based on runtime behaviors, such as reloading a page.</span></span> <span data-ttu-id="eaae3-146">Portanto, pode haver algum benefício de desempenho para evitar um grande número de referências de recursos dinâmicos quando elas não são necessárias com base no seu recurso e no design de aplicativos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-146">So there can be some performance benefit to avoiding large numbers of dynamic resource references when they aren't necessary based on your resource and app design.</span></span>

- <span data-ttu-id="eaae3-147">Você está definindo o valor de uma propriedade que não está em <xref:System.Windows.DependencyObject> um ou <xref:System.Windows.Freezable>um.</span><span class="sxs-lookup"><span data-stu-id="eaae3-147">You're setting the value of a property that isn't on a <xref:System.Windows.DependencyObject> or a <xref:System.Windows.Freezable>.</span></span>

- <span data-ttu-id="eaae3-148">Você está criando um dicionário de recursos que será compilado em uma DLL e empacotado como parte do aplicativo ou compartilhado entre aplicativos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-148">You're creating a resource dictionary that will be compiled into a DLL and packaged as part of the app or shared between apps.</span></span>

- <span data-ttu-id="eaae3-149">Você está criando um tema para um controle personalizado e está definindo recursos que são usados dentro dos temas.</span><span class="sxs-lookup"><span data-stu-id="eaae3-149">You're creating a theme for a custom control and are defining resources that are used within the themes.</span></span> <span data-ttu-id="eaae3-150">Para esse caso, você normalmente não deseja o comportamento de pesquisa de referência de recurso dinâmico; em vez disso, você deseja o comportamento de referência de recurso estático para que a pesquisa seja previsível e independente para o tema.</span><span class="sxs-lookup"><span data-stu-id="eaae3-150">For this case, you typically do not want the dynamic resource reference lookup behavior; you instead want the static resource reference behavior so that the lookup is predictable and self-contained to the theme.</span></span> <span data-ttu-id="eaae3-151">Com uma referência de recurso dinâmico, até mesmo uma referência dentro de um tema é deixada não avaliada até o tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="eaae3-151">With a dynamic resource reference, even a reference within a theme is left unevaluated until run-time.</span></span> <span data-ttu-id="eaae3-152">e há uma chance de que, quando o tema for aplicado, algum elemento local redefinirá uma chave que seu tema está tentando referenciar, e o elemento local cairá antes do próprio tema na pesquisa.</span><span class="sxs-lookup"><span data-stu-id="eaae3-152">and there is a chance that when the theme is applied, some local element will redefine a key that your theme is trying to reference, and the local element will fall prior to the theme itself in the lookup.</span></span> <span data-ttu-id="eaae3-153">Se isso acontecer, seu tema não se comportará conforme o esperado.</span><span class="sxs-lookup"><span data-stu-id="eaae3-153">If that happens, your theme will not behave as expected.</span></span>

- <span data-ttu-id="eaae3-154">Você está usando recursos para definir um grande número de propriedades de dependência.</span><span class="sxs-lookup"><span data-stu-id="eaae3-154">You're using resources to set large numbers of dependency properties.</span></span> <span data-ttu-id="eaae3-155">As propriedades de dependência têm o cache de valor efetivo como habilitado pelo sistema de propriedades, portanto, se você fornecer um valor para uma propriedade de dependência que possa ser avaliada no tempo de carregamento, a propriedade de dependência não precisará verificar se há uma expressão reavaliada e retornar o último valor efetivo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-155">Dependency properties have effective value caching as enabled by the property system, so if you provide a value for a dependency property that can be evaluated at load time, the dependency property doesn't have to check for a reevaluated expression and can return the last effective value.</span></span> <span data-ttu-id="eaae3-156">Essa técnica pode ser uma vantagem de desempenho.</span><span class="sxs-lookup"><span data-stu-id="eaae3-156">This technique can be a performance benefit.</span></span>

- <span data-ttu-id="eaae3-157">Você deseja alterar o recurso subjacente para todos os consumidores ou deseja manter instâncias graváveis separadas para cada consumidor usando o [atributo x:Shared](../xaml-services/xshared-attribute.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-157">You want to change the underlying resource for all consumers, or you want to maintain separate writable instances for each consumer by using the [x:Shared Attribute](../xaml-services/xshared-attribute.md).</span></span>

### <a name="static-resource-lookup-behavior"></a><span data-ttu-id="eaae3-158">Comportamento de pesquisa de recursos estáticos</span><span class="sxs-lookup"><span data-stu-id="eaae3-158">Static resource lookup behavior</span></span>

<span data-ttu-id="eaae3-159">O seguinte descreve o processo de pesquisa que ocorre automaticamente quando um recurso estático é referenciado por uma propriedade ou um elemento:</span><span class="sxs-lookup"><span data-stu-id="eaae3-159">The following describes the lookup process that automatically happens when a static resource is referenced by a property or element:</span></span>

01. <span data-ttu-id="eaae3-160">O processo de pesquisa verifica a chave solicitada no dicionário de recursos definido pelo elemento que define a propriedade.</span><span class="sxs-lookup"><span data-stu-id="eaae3-160">The lookup process checks for the requested key within the resource dictionary defined by the element that sets the property.</span></span>

01. <span data-ttu-id="eaae3-161">O processo de pesquisa então percorre a árvore lógica para cima até o elemento pai e seu dicionário de recursos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-161">The lookup process then traverses the logical tree upward to the parent element and its resource dictionary.</span></span> <span data-ttu-id="eaae3-162">Esse processo continua até que o elemento raiz seja atingido.</span><span class="sxs-lookup"><span data-stu-id="eaae3-162">This process continues until the root element is reached.</span></span>

01. <span data-ttu-id="eaae3-163">Os recursos do aplicativo são verificados.</span><span class="sxs-lookup"><span data-stu-id="eaae3-163">App resources are checked.</span></span> <span data-ttu-id="eaae3-164">Os recursos de aplicativo são aqueles no dicionário de recursos que é definido pelo <xref:System.Windows.Application> objeto para seu aplicativo do WPF.</span><span class="sxs-lookup"><span data-stu-id="eaae3-164">App resources are those resources within the resource dictionary that is defined by the <xref:System.Windows.Application> object for your WPF app.</span></span>

<span data-ttu-id="eaae3-165">As referências de recursos estáticos em um dicionário de recursos devem referenciar um recurso que já foi definido lexicalmente antes da referência ao recurso.</span><span class="sxs-lookup"><span data-stu-id="eaae3-165">Static resource references from within a resource dictionary must reference a resource that has already been defined lexically before the resource reference.</span></span> <span data-ttu-id="eaae3-166">As referências de encaminhamento não podem ser resolvidas por uma referência de recursos estáticos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-166">Forward references cannot be resolved by a static resource reference.</span></span> <span data-ttu-id="eaae3-167">Por esse motivo, crie sua estrutura de dicionário de recursos, de modo que os recursos sejam definidos no início de cada dicionário de recursos ou próximo dele.</span><span class="sxs-lookup"><span data-stu-id="eaae3-167">For this reason, design your resource dictionary structure such that resources are defined at or near the beginning of each respective resource dictionary.</span></span>

<span data-ttu-id="eaae3-168">A pesquisa de recursos estáticos pode se estender a temas ou a recursos do sistema, mas essa pesquisa só tem suporte porque o carregador XAML adia a solicitação.</span><span class="sxs-lookup"><span data-stu-id="eaae3-168">Static resource lookup can extend into themes or into system resources, but this lookup is supported only because the XAML loader defers the request.</span></span> <span data-ttu-id="eaae3-169">O adiamento é necessário para que o tema de tempo de execução no momento em que a página é carregada se aplique corretamente ao aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-169">The deferral is necessary so that the runtime theme at the time the page loads applies properly to the app.</span></span> <span data-ttu-id="eaae3-170">No entanto, as referências a recursos estáticos para chaves que são conhecidas apenas em temas ou como recursos do sistema não são recomendadas, pois essas referências não são reavaliadas se o tema é alterado pelo usuário em tempo real.</span><span class="sxs-lookup"><span data-stu-id="eaae3-170">However, static resource references to keys that are known to only exist in themes or as system resources aren't recommended, because such references aren't reevaluated if the theme is changed by the user in real time.</span></span> <span data-ttu-id="eaae3-171">Uma referência de recurso dinâmico é mais confiável quando você solicita recursos do sistema ou de tema.</span><span class="sxs-lookup"><span data-stu-id="eaae3-171">A dynamic resource reference is more reliable when you request theme or system resources.</span></span> <span data-ttu-id="eaae3-172">A exceção é quando um próprio elemento de tema solicita outro recurso.</span><span class="sxs-lookup"><span data-stu-id="eaae3-172">The exception is when a theme element itself requests another resource.</span></span> <span data-ttu-id="eaae3-173">Essas referências devem ser referências de recursos estáticos, pelas razões mencionadas anteriormente.</span><span class="sxs-lookup"><span data-stu-id="eaae3-173">These references should be static resource references, for the reasons mentioned earlier.</span></span>

<span data-ttu-id="eaae3-174">O comportamento da exceção se uma referência de recurso estático não for encontrada varia.</span><span class="sxs-lookup"><span data-stu-id="eaae3-174">The exception behavior if a static resource reference isn't found varies.</span></span> <span data-ttu-id="eaae3-175">Se o recurso foi adiado, a exceção ocorre em runtime.</span><span class="sxs-lookup"><span data-stu-id="eaae3-175">If the resource was deferred, then the exception occurs at runtime.</span></span> <span data-ttu-id="eaae3-176">Se o recurso não foi adiado, a exceção ocorre em tempo de carregamento.</span><span class="sxs-lookup"><span data-stu-id="eaae3-176">If the resource was not deferred, the exception occurs at load time.</span></span>

## <a name="dynamic-resources"></a><span data-ttu-id="eaae3-177">Recursos dinâmicos</span><span class="sxs-lookup"><span data-stu-id="eaae3-177">Dynamic resources</span></span>

<span data-ttu-id="eaae3-178">Os recursos dinâmicos funcionam melhor quando:</span><span class="sxs-lookup"><span data-stu-id="eaae3-178">Dynamic resources work best when:</span></span>

- <span data-ttu-id="eaae3-179">O valor do recurso, incluindo recursos do sistema ou recursos que, de outra forma, o usuário configurável, depende de condições que não são conhecidas até o tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="eaae3-179">The value of the resource, including system resources, or resources that are otherwise user settable, depends on conditions that aren't known until runtime.</span></span> <span data-ttu-id="eaae3-180">Por exemplo, você pode criar valores setter que se referem às propriedades do sistema <xref:System.Windows.SystemColors>como <xref:System.Windows.SystemFonts>expostas por, ou <xref:System.Windows.SystemParameters>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-180">For example, you can create setter values that refer to system properties as exposed by <xref:System.Windows.SystemColors>, <xref:System.Windows.SystemFonts>, or <xref:System.Windows.SystemParameters>.</span></span> <span data-ttu-id="eaae3-181">Esses valores são realmente dinâmicos, pois, basicamente, são obtidos do ambiente em runtime do usuário e do sistema operacional.</span><span class="sxs-lookup"><span data-stu-id="eaae3-181">These values are truly dynamic because they ultimately come from the runtime environment of the user and operating system.</span></span> <span data-ttu-id="eaae3-182">Você também pode ter temas em nível do aplicativo que podem ser alterados, em que o acesso aos recursos em nível de página também deve captar a alteração.</span><span class="sxs-lookup"><span data-stu-id="eaae3-182">You might also have application-level themes that can change, where page-level resource access must also capture the change.</span></span>

- <span data-ttu-id="eaae3-183">Você está criando ou referenciando estilos de tema para um controle personalizado.</span><span class="sxs-lookup"><span data-stu-id="eaae3-183">You're creating or referencing theme styles for a custom control.</span></span>

- <span data-ttu-id="eaae3-184">Você pretende ajustar o conteúdo de um <xref:System.Windows.ResourceDictionary> durante um tempo de vida do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-184">You intend to adjust the contents of a <xref:System.Windows.ResourceDictionary> during an app lifetime.</span></span>

- <span data-ttu-id="eaae3-185">Você tem uma estrutura de recursos complicada que tem interdependências, em que uma referência de encaminhamento pode ser necessária.</span><span class="sxs-lookup"><span data-stu-id="eaae3-185">You have a complicated resource structure that has interdependencies, where a forward reference may be required.</span></span> <span data-ttu-id="eaae3-186">As referências de recursos estáticos não dão suporte a referências de encaminhamento, mas as referências a recursos dinâmicos dão suporte a elas porque o recurso não precisa ser avaliado até o tempo de execução e as referências de encaminhamento não são, portanto, um conceito relevante.</span><span class="sxs-lookup"><span data-stu-id="eaae3-186">Static resource references do not support forward references, but dynamic resource references do support them because the resource doesn't need to be evaluated until runtime, and forward references are therefore not a relevant concept.</span></span>

- <span data-ttu-id="eaae3-187">Você está fazendo referência a um recurso que é grande da perspectiva de um conjunto de trabalho ou de compilação, e o recurso pode não ser usado imediatamente quando a página é carregada.</span><span class="sxs-lookup"><span data-stu-id="eaae3-187">You're referencing a resource that is large from the perspective of a compile or working set, and the resource might not be used immediately when the page loads.</span></span> <span data-ttu-id="eaae3-188">As referências a recursos estáticos sempre são carregadas do XAML quando a página é carregada.</span><span class="sxs-lookup"><span data-stu-id="eaae3-188">Static resource references always load from XAML when the page loads.</span></span> <span data-ttu-id="eaae3-189">No entanto, uma referência de recurso dinâmico não é carregada até que seja usada.</span><span class="sxs-lookup"><span data-stu-id="eaae3-189">However, a dynamic resource reference doesn't load until it's used.</span></span>

- <span data-ttu-id="eaae3-190">Você está criando um estilo em que os valores setter podem vir de outros valores que são influenciados por temas ou outras configurações de usuário.</span><span class="sxs-lookup"><span data-stu-id="eaae3-190">You're creating a style where setter values might come from other values that are influenced by themes or other user settings.</span></span>

- <span data-ttu-id="eaae3-191">Você está aplicando recursos a elementos que podem ser repais na árvore lógica durante o tempo de vida do aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-191">You're applying resources to elements that might be reparented in the logical tree during app lifetime.</span></span> <span data-ttu-id="eaae3-192">Potencialmente, a alteração do pai também altera o escopo de pesquisa de recursos; portanto, se você desejar que o recurso de um elemento reassociado seja reavaliado com base no novo escopo, sempre use uma referência de recursos dinâmicos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-192">Changing the parent also potentially changes the resource lookup scope, so if you want the resource for a reparented element to be reevaluated based on the new scope, always use a dynamic resource reference.</span></span>

### <a name="dynamic-resource-lookup-behavior"></a><span data-ttu-id="eaae3-193">Comportamento de pesquisa de recursos dinâmicos</span><span class="sxs-lookup"><span data-stu-id="eaae3-193">Dynamic resource lookup behavior</span></span>

<span data-ttu-id="eaae3-194">O comportamento de pesquisa de recurso para uma referência de recurso dinâmico paraleliza o comportamento de pesquisa em seu <xref:System.Windows.FrameworkElement.FindResource%2A> código <xref:System.Windows.FrameworkElement.SetResourceReference%2A>se você chamar ou:</span><span class="sxs-lookup"><span data-stu-id="eaae3-194">Resource lookup behavior for a dynamic resource reference parallels the lookup behavior in your code if you call <xref:System.Windows.FrameworkElement.FindResource%2A> or <xref:System.Windows.FrameworkElement.SetResourceReference%2A>:</span></span>

01. <span data-ttu-id="eaae3-195">A pesquisa verifica a chave solicitada dentro do dicionário de recursos definido pelo elemento que define a propriedade:</span><span class="sxs-lookup"><span data-stu-id="eaae3-195">The lookup checks for the requested key within the resource dictionary defined by the element that sets the property:</span></span>

    - <span data-ttu-id="eaae3-196">Se o elemento definir uma <xref:System.Windows.FrameworkElement.Style%2A> Propriedade, o <xref:System.Windows.FrameworkElement.Style?displayProperty=fullName> do elemento terá seu <xref:System.Windows.Style.Resources> dicionário marcado.</span><span class="sxs-lookup"><span data-stu-id="eaae3-196">If the element defines a <xref:System.Windows.FrameworkElement.Style%2A> property, the <xref:System.Windows.FrameworkElement.Style?displayProperty=fullName> of the element has its <xref:System.Windows.Style.Resources> dictionary checked.</span></span>

    - <span data-ttu-id="eaae3-197">Se o elemento definir uma <xref:System.Windows.Controls.Control.Template%2A> Propriedade, o <xref:System.Windows.FrameworkTemplate.Resources?displayProperty=fullName> dicionário do elemento será verificado.</span><span class="sxs-lookup"><span data-stu-id="eaae3-197">If the element defines a <xref:System.Windows.Controls.Control.Template%2A> property, the <xref:System.Windows.FrameworkTemplate.Resources?displayProperty=fullName> dictionary of the element is checked.</span></span>

01. <span data-ttu-id="eaae3-198">A pesquisa percorre a árvore lógica para cima até o elemento pai e seu dicionário de recursos.</span><span class="sxs-lookup"><span data-stu-id="eaae3-198">The lookup traverses the logical tree upward to the parent element and its resource dictionary.</span></span> <span data-ttu-id="eaae3-199">Esse processo continua até que o elemento raiz seja atingido.</span><span class="sxs-lookup"><span data-stu-id="eaae3-199">This process continues until the root element is reached.</span></span>

01. <span data-ttu-id="eaae3-200">Os recursos do aplicativo são verificados.</span><span class="sxs-lookup"><span data-stu-id="eaae3-200">App resources are checked.</span></span> <span data-ttu-id="eaae3-201">Os recursos de aplicativo são aqueles no dicionário de recursos que são definidos pelo <xref:System.Windows.Application> objeto para seu aplicativo do WPF.</span><span class="sxs-lookup"><span data-stu-id="eaae3-201">App resources are those resources within the resource dictionary that are defined by the <xref:System.Windows.Application> object for your WPF app.</span></span>

01. <span data-ttu-id="eaae3-202">O dicionário de recursos do tema é verificado para o tema ativo no momento.</span><span class="sxs-lookup"><span data-stu-id="eaae3-202">The theme resource dictionary is checked for the currently active theme.</span></span> <span data-ttu-id="eaae3-203">Se o tema for alterado em runtime, o valor será reavaliado.</span><span class="sxs-lookup"><span data-stu-id="eaae3-203">If the theme changes at runtime, the value is reevaluated.</span></span>

01. <span data-ttu-id="eaae3-204">Os recursos do sistema são verificados.</span><span class="sxs-lookup"><span data-stu-id="eaae3-204">System resources are checked.</span></span>

<span data-ttu-id="eaae3-205">O comportamento de exceção (se houver) varia:</span><span class="sxs-lookup"><span data-stu-id="eaae3-205">Exception behavior (if any) varies:</span></span>

- <span data-ttu-id="eaae3-206">Se um recurso foi solicitado por uma <xref:System.Windows.FrameworkElement.FindResource%2A> chamada e não foi encontrado, uma exceção será lançada.</span><span class="sxs-lookup"><span data-stu-id="eaae3-206">If a resource was requested by a <xref:System.Windows.FrameworkElement.FindResource%2A> call and was not found, an exception is thrown.</span></span>

- <span data-ttu-id="eaae3-207">Se um recurso foi solicitado por uma <xref:System.Windows.FrameworkElement.TryFindResource%2A> chamada e não foi encontrado, nenhuma exceção será lançada e o valor retornado será `null`.</span><span class="sxs-lookup"><span data-stu-id="eaae3-207">If a resource was requested by a <xref:System.Windows.FrameworkElement.TryFindResource%2A> call and was not found, no exception is thrown, and the returned value is `null`.</span></span> <span data-ttu-id="eaae3-208">Se a propriedade que está sendo definida `null`não aceitar, ainda será possível que uma exceção mais profunda seja gerada, dependendo da propriedade individual que está sendo definida.</span><span class="sxs-lookup"><span data-stu-id="eaae3-208">If the property being set doesn't accept `null`, then it's still possible that a deeper exception will be thrown, depending on the individual property being set.</span></span>

- <span data-ttu-id="eaae3-209">Se um recurso foi solicitado por uma referência de recurso dinâmico em XAML e não foi encontrado, o comportamento dependerá do sistema de propriedades geral.</span><span class="sxs-lookup"><span data-stu-id="eaae3-209">If a resource was requested by a dynamic resource reference in XAML and was not found, then the behavior depends on the general property system.</span></span> <span data-ttu-id="eaae3-210">O comportamento geral é como se nenhuma operação de configuração de propriedade ocorreu no nível em que o recurso existe.</span><span class="sxs-lookup"><span data-stu-id="eaae3-210">The general behavior is as if no property setting operation occurred at the level where the resource exists.</span></span> <span data-ttu-id="eaae3-211">Por exemplo, se você tentar definir o plano de fundo em um elemento de botão individual usando um recurso que não pôde ser avaliado, nenhum valor definido resultará, mas o valor efetivo ainda poderá vir de outros participantes no sistema de propriedades e precedência de valor.</span><span class="sxs-lookup"><span data-stu-id="eaae3-211">For instance, if you attempt to set the background on an individual button element using a resource that could not be evaluated, then no value set results, but the effective value can still come from other participants in the property system and value precedence.</span></span> <span data-ttu-id="eaae3-212">Por exemplo, o valor de plano de fundo ainda pode vir de um estilo de botão definido localmente ou do estilo de tema.</span><span class="sxs-lookup"><span data-stu-id="eaae3-212">For instance, the background value might still come from a locally defined button style or from the theme style.</span></span> <span data-ttu-id="eaae3-213">Para propriedades que não são definidas por estilos de tema, o valor efetivo após uma avaliação de recurso com falha pode vir do valor padrão nos metadados de propriedade.</span><span class="sxs-lookup"><span data-stu-id="eaae3-213">For properties that aren't defined by theme styles, the effective value after a failed resource evaluation might come from the default value in the property metadata.</span></span>

### <a name="restrictions"></a><span data-ttu-id="eaae3-214">Restrições</span><span class="sxs-lookup"><span data-stu-id="eaae3-214">Restrictions</span></span>

<span data-ttu-id="eaae3-215">As referências de recursos dinâmicos têm algumas restrições importantes.</span><span class="sxs-lookup"><span data-stu-id="eaae3-215">Dynamic resource references have some notable restrictions.</span></span> <span data-ttu-id="eaae3-216">Pelo menos uma das seguintes condições deve ser verdadeira:</span><span class="sxs-lookup"><span data-stu-id="eaae3-216">At least one of the following conditions must be true:</span></span>

- <span data-ttu-id="eaae3-217">A propriedade que está sendo definida deve ser uma propriedade <xref:System.Windows.FrameworkElement> em <xref:System.Windows.FrameworkContentElement>um ou.</span><span class="sxs-lookup"><span data-stu-id="eaae3-217">The property being set must be a property on a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement>.</span></span> <span data-ttu-id="eaae3-218">Essa propriedade deve ser apoiada por um <xref:System.Windows.DependencyProperty>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-218">That property must be backed by a <xref:System.Windows.DependencyProperty>.</span></span>

- <span data-ttu-id="eaae3-219">A referência é para um valor dentro de `StyleSetter`um.</span><span class="sxs-lookup"><span data-stu-id="eaae3-219">The reference is for a value within a `StyleSetter`.</span></span>

- <span data-ttu-id="eaae3-220">A propriedade que está sendo definida deve ser uma propriedade <xref:System.Windows.Freezable> em um que é fornecida como um valor de <xref:System.Windows.FrameworkElement> uma <xref:System.Windows.FrameworkContentElement> propriedade ou, ou <xref:System.Windows.Setter> um valor.</span><span class="sxs-lookup"><span data-stu-id="eaae3-220">The property being set must be a property on a <xref:System.Windows.Freezable> that is provided as a value of either a <xref:System.Windows.FrameworkElement> or <xref:System.Windows.FrameworkContentElement> property, or a <xref:System.Windows.Setter> value.</span></span>

<span data-ttu-id="eaae3-221">Como a propriedade que está sendo definida deve <xref:System.Windows.DependencyProperty> ser <xref:System.Windows.Freezable> uma propriedade ou, a maioria das alterações de propriedade pode ser propagada para a interface do usuário porque uma alteração de propriedade (o valor de recurso dinâmico alterado) é confirmada pelo sistema de propriedades.</span><span class="sxs-lookup"><span data-stu-id="eaae3-221">Because the property being set must be a <xref:System.Windows.DependencyProperty> or <xref:System.Windows.Freezable> property, most property changes can propagate to the UI because a property change (the changed dynamic resource value) is acknowledged by the property system.</span></span> <span data-ttu-id="eaae3-222">A maioria dos controles inclui lógica que forçará outro layout de um controle <xref:System.Windows.DependencyProperty> se uma alteração e essa propriedade puderem afetar o layout.</span><span class="sxs-lookup"><span data-stu-id="eaae3-222">Most controls include logic that will force another layout of a control if a <xref:System.Windows.DependencyProperty> changes and that property might affect layout.</span></span> <span data-ttu-id="eaae3-223">No entanto, nem todas as propriedades que têm uma [extensão de marcação DynamicResource](../../framework/wpf/advanced/dynamicresource-markup-extension.md) como seu valor têm a garantia de fornecer atualizações em tempo real na interface do usuário.</span><span class="sxs-lookup"><span data-stu-id="eaae3-223">However, not all properties that have a [DynamicResource Markup Extension](../../framework/wpf/advanced/dynamicresource-markup-extension.md) as their value are guaranteed to provide real time updates in the UI.</span></span> <span data-ttu-id="eaae3-224">Essa funcionalidade ainda pode variar dependendo da propriedade, bem como dependendo do tipo que possui a propriedade, ou até mesmo da estrutura lógica do seu aplicativo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-224">That functionality still might vary depending on the property, as well as depending on the type that owns the property, or even the logical structure of your app.</span></span>

## <a name="styles-datatemplates-and-implicit-keys"></a><span data-ttu-id="eaae3-225">Estilos, DataTemplates e chaves implícitas</span><span class="sxs-lookup"><span data-stu-id="eaae3-225">Styles, DataTemplates, and implicit keys</span></span>

<span data-ttu-id="eaae3-226">Embora todos os itens em <xref:System.Windows.ResourceDictionary> um devam ter uma chave, isso não significa que todos os recursos devem `x:Key`ter um explícito.</span><span class="sxs-lookup"><span data-stu-id="eaae3-226">Although all items in a <xref:System.Windows.ResourceDictionary> must have a key, that doesn't mean that all resources must have an explicit `x:Key`.</span></span> <span data-ttu-id="eaae3-227">Vários tipos de objeto dão suporte a uma chave implícita quando definidos como um recurso, em que o valor da chave é vinculado ao valor de outra propriedade.</span><span class="sxs-lookup"><span data-stu-id="eaae3-227">Several object types support an implicit key when defined as a resource, where the key value is tied to the value of another property.</span></span> <span data-ttu-id="eaae3-228">Esse tipo de chave é conhecido como uma chave implícita, enquanto que `x:Key` um atributo é uma chave explícita.</span><span class="sxs-lookup"><span data-stu-id="eaae3-228">This type of key is known as an implicit key, whereas an `x:Key` attribute is an explicit key.</span></span> <span data-ttu-id="eaae3-229">Você pode substituir uma chave implícita especificando uma chave explícita.</span><span class="sxs-lookup"><span data-stu-id="eaae3-229">You can overwrite any implicit key by specifying an explicit key.</span></span>

<span data-ttu-id="eaae3-230">Um cenário importante para os recursos é quando você define <xref:System.Windows.Style>um.</span><span class="sxs-lookup"><span data-stu-id="eaae3-230">One important scenario for resources is when you define a <xref:System.Windows.Style>.</span></span> <span data-ttu-id="eaae3-231">Na verdade, um <xref:System.Windows.Style> é quase sempre definido como uma entrada em um dicionário de recursos, porque os estilos são inerentemente destinados à reutilização.</span><span class="sxs-lookup"><span data-stu-id="eaae3-231">In fact, a <xref:System.Windows.Style> is almost always defined as an entry in a resource dictionary, because styles are inherently intended for reuse.</span></span> <span data-ttu-id="eaae3-232">Para obter mais informações sobre estilos, consulte [estilização e modelagem](styles-templates-overview.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-232">For more information about styles, see [Styling and Templating](styles-templates-overview.md).</span></span>

<span data-ttu-id="eaae3-233">Os estilos de controles podem ser criados com uma chave implícita e referenciados com ela.</span><span class="sxs-lookup"><span data-stu-id="eaae3-233">Styles for controls can be both created with and referenced with an implicit key.</span></span> <span data-ttu-id="eaae3-234">Os estilos de tema que definem a aparência padrão de um controle se baseiam nessa chave implícita.</span><span class="sxs-lookup"><span data-stu-id="eaae3-234">The theme styles that define the default appearance of a control rely on this implicit key.</span></span> <span data-ttu-id="eaae3-235">Do ponto de vista da solicitação, a chave implícita é o <xref:System.Type> do próprio controle.</span><span class="sxs-lookup"><span data-stu-id="eaae3-235">From the standpoint of requesting it, the implicit key is the <xref:System.Type> of the control itself.</span></span> <span data-ttu-id="eaae3-236">Do ponto de vista da definição dos recursos, a chave implícita é <xref:System.Windows.Style.TargetType%2A> o do estilo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-236">From the standpoint of defining the resources, the implicit key is the <xref:System.Windows.Style.TargetType%2A> of the style.</span></span> <span data-ttu-id="eaae3-237">Portanto, se você estiver criando temas para controles personalizados ou criando estilos que interagem com estilos de tema existentes, não será necessário especificar uma [diretiva x:Key](../xaml-services/xkey-directive.md) para isso <xref:System.Windows.Style>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-237">Therefore, if you're creating themes for custom controls or creating styles that interact with existing theme styles, you do not need to specify an [x:Key Directive](../xaml-services/xkey-directive.md) for that <xref:System.Windows.Style>.</span></span> <span data-ttu-id="eaae3-238">Além disso, você desejar usar os estilos de tema, não precisará especificar nenhum estilo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-238">And if you want to use the themed styles, you do not need to specify any style at all.</span></span> <span data-ttu-id="eaae3-239">Por exemplo, a definição de estilo a seguir funciona, embora <xref:System.Windows.Style> o recurso não pareça ter uma chave:</span><span class="sxs-lookup"><span data-stu-id="eaae3-239">For instance, the following style definition works, even though the <xref:System.Windows.Style> resource doesn't appear to have a key:</span></span>

[!code-xaml[FEResourceSH_snip#ImplicitStyle](~/samples/snippets/csharp/VS_Snippets_Wpf/FEResourceSH_snip/CS/page2.xaml#implicitstyle)]

<span data-ttu-id="eaae3-240">Esse estilo realmente tem uma chave: a chave `typeof(System.Windows.Controls.Button)`implícita.</span><span class="sxs-lookup"><span data-stu-id="eaae3-240">That style really does have a key: the implicit key `typeof(System.Windows.Controls.Button)`.</span></span> <span data-ttu-id="eaae3-241">Na marcação, você pode especificar um <xref:System.Windows.Style.TargetType%2A> diretamente como o nome do tipo (ou opcionalmente, você pode usar [{x:Type...}](../xaml-services/xtype-markup-extension.md)</span><span class="sxs-lookup"><span data-stu-id="eaae3-241">In markup, you can specify a <xref:System.Windows.Style.TargetType%2A> directly as the type name (or you can optionally use [{x:Type...}](../xaml-services/xtype-markup-extension.md)</span></span> <span data-ttu-id="eaae3-242">para retornar um <xref:System.Type>.</span><span class="sxs-lookup"><span data-stu-id="eaae3-242">to return a <xref:System.Type>.</span></span>

<span data-ttu-id="eaae3-243">Por meio dos mecanismos de estilo de tema padrão usados pelo WPF, esse estilo é aplicado como o estilo <xref:System.Windows.Controls.Button> de tempo de execução de a na <xref:System.Windows.Controls.Button> página, mesmo que a si <xref:System.Windows.FrameworkElement.Style%2A> não tente especificar sua propriedade ou uma referência de recurso específica ao estilo.</span><span class="sxs-lookup"><span data-stu-id="eaae3-243">Through the default theme style mechanisms used by WPF, that style is applied as the runtime style of a <xref:System.Windows.Controls.Button> on the page, even though the <xref:System.Windows.Controls.Button> itself doesn't attempt to specify its <xref:System.Windows.FrameworkElement.Style%2A> property or a specific resource reference to the style.</span></span> <span data-ttu-id="eaae3-244">Seu estilo definido na página é encontrado anteriormente na sequência de pesquisa do que o estilo de dicionário do tema, usando a mesma chave que o estilo do dicionário do tema.</span><span class="sxs-lookup"><span data-stu-id="eaae3-244">Your style defined in the page is found earlier in the lookup sequence than the theme dictionary style, using the same key that the theme dictionary style has.</span></span> <span data-ttu-id="eaae3-245">Você poderia apenas especificar `<Button>Hello</Button>` qualquer lugar na página, e o estilo que você <xref:System.Windows.Style.TargetType%2A> definiu `Button` a ser aplicado a esse botão.</span><span class="sxs-lookup"><span data-stu-id="eaae3-245">You could just specify `<Button>Hello</Button>` anywhere in the page, and the style you defined with <xref:System.Windows.Style.TargetType%2A> of `Button` would apply to that button.</span></span> <span data-ttu-id="eaae3-246">Se desejar, você ainda poderá chavear explicitamente o estilo com o mesmo valor de tipo <xref:System.Windows.Style.TargetType%2A> para fins de clareza na sua marcação, mas isso é opcional.</span><span class="sxs-lookup"><span data-stu-id="eaae3-246">If you want, you can still explicitly key the style with the same type value as <xref:System.Windows.Style.TargetType%2A> for clarity in your markup, but that is optional.</span></span>

<span data-ttu-id="eaae3-247">As chaves implícitas para estilos não se aplicam a <xref:System.Windows.FrameworkElement.OverridesDefaultStyle%2A> um `true`controle se for.</span><span class="sxs-lookup"><span data-stu-id="eaae3-247">Implicit keys for styles do not apply on a control if <xref:System.Windows.FrameworkElement.OverridesDefaultStyle%2A> is `true`.</span></span> <span data-ttu-id="eaae3-248">(Observe também que <xref:System.Windows.FrameworkElement.OverridesDefaultStyle%2A> pode ser definido como parte do comportamento nativo da classe Control, em vez de explicitamente em uma instância do controle.) Além disso, para dar suporte a chaves implícitas para cenários de classe derivada, o <xref:System.Windows.FrameworkElement.DefaultStyleKey%2A> controle deve substituir (todos os controles existentes fornecidos como parte do WPF incluem essa substituição).</span><span class="sxs-lookup"><span data-stu-id="eaae3-248">(Also note that <xref:System.Windows.FrameworkElement.OverridesDefaultStyle%2A> might be set as part of native behavior for the control class, rather than explicitly on an instance of the control.) Also, in order to support implicit keys for derived class scenarios, the control must override <xref:System.Windows.FrameworkElement.DefaultStyleKey%2A> (all existing controls provided as part of WPF include this override).</span></span> <span data-ttu-id="eaae3-249">Para obter mais informações sobre estilos, temas e design de controle, consulte [diretrizes para criar controles stylable](../../framework/wpf/controls/guidelines-for-designing-stylable-controls.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-249">For more information about styles, themes, and control design, see [Guidelines for Designing Stylable Controls](../../framework/wpf/controls/guidelines-for-designing-stylable-controls.md).</span></span>

<span data-ttu-id="eaae3-250"><xref:System.Windows.DataTemplate>também tem uma chave implícita.</span><span class="sxs-lookup"><span data-stu-id="eaae3-250"><xref:System.Windows.DataTemplate> also has an implicit key.</span></span> <span data-ttu-id="eaae3-251">A chave implícita para um <xref:System.Windows.DataTemplate> é o <xref:System.Windows.DataTemplate.DataType%2A> valor da propriedade.</span><span class="sxs-lookup"><span data-stu-id="eaae3-251">The implicit key for a <xref:System.Windows.DataTemplate> is the <xref:System.Windows.DataTemplate.DataType%2A> property value.</span></span> <span data-ttu-id="eaae3-252"><xref:System.Windows.DataTemplate.DataType%2A>também pode ser especificado como o nome do tipo em vez de explicitamente usar [{x:Type...}](../xaml-services/xtype-markup-extension.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-252"><xref:System.Windows.DataTemplate.DataType%2A> can also be specified as the name of the type rather than explicitly using [{x:Type...}](../xaml-services/xtype-markup-extension.md).</span></span> <span data-ttu-id="eaae3-253">Para obter detalhes, consulte [visão geral de modelagem de dados](../../framework/wpf/data/data-templating-overview.md).</span><span class="sxs-lookup"><span data-stu-id="eaae3-253">For details, see [Data Templating Overview](../../framework/wpf/data/data-templating-overview.md).</span></span>

## <a name="see-also"></a><span data-ttu-id="eaae3-254">Confira também</span><span class="sxs-lookup"><span data-stu-id="eaae3-254">See also</span></span>

- <xref:System.Windows.ResourceDictionary>
- [<span data-ttu-id="eaae3-255">Recursos do aplicativo</span><span class="sxs-lookup"><span data-stu-id="eaae3-255">Application resources</span></span>](../../framework/wpf/advanced/optimizing-performance-application-resources.md)
- [<span data-ttu-id="eaae3-256">Recursos e código</span><span class="sxs-lookup"><span data-stu-id="eaae3-256">Resources and code</span></span>](../../framework/wpf/advanced/resources-and-code.md)
- [<span data-ttu-id="eaae3-257">Definir e referenciar um recurso</span><span class="sxs-lookup"><span data-stu-id="eaae3-257">Define and reference a resource</span></span>](../../framework/wpf/advanced/how-to-define-and-reference-a-resource.md)
- [<span data-ttu-id="eaae3-258">Visão geral do gerenciamento de aplicativos</span><span class="sxs-lookup"><span data-stu-id="eaae3-258">Application management overview</span></span>](../../framework/wpf/app-development/application-management-overview.md)
- [<span data-ttu-id="eaae3-259">extensão de marcação x:Type</span><span class="sxs-lookup"><span data-stu-id="eaae3-259">x:Type markup extension</span></span>](../xaml-services/xtype-markup-extension.md)
- [<span data-ttu-id="eaae3-260">Extensão de marcação StaticResource</span><span class="sxs-lookup"><span data-stu-id="eaae3-260">StaticResource markup extension</span></span>](../../framework/wpf/advanced/staticresource-markup-extension.md)
- [<span data-ttu-id="eaae3-261">Extensão de marcação DynamicResource</span><span class="sxs-lookup"><span data-stu-id="eaae3-261">DynamicResource markup extension</span></span>](../../framework/wpf/advanced/dynamicresource-markup-extension.md)
