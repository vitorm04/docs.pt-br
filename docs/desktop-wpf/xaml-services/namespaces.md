---
title: XAML Namespaces para serviços .NET XAML
ms.date: 03/30/2017
ms.assetid: e4f15f13-c420-4c1e-aeab-9b6f50212047
ms.openlocfilehash: 2ae57d08fade85c59fea2a54b653a2f775b57415
ms.sourcegitcommit: 7588136e355e10cbc2582f389c90c127363c02a5
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/12/2020
ms.locfileid: "82071839"
---
# <a name="xaml-namespaces-for-net-xaml-services"></a><span data-ttu-id="49710-102">XAML Namespaces para serviços .NET XAML</span><span class="sxs-lookup"><span data-stu-id="49710-102">XAML Namespaces for .NET XAML Services</span></span>
<span data-ttu-id="49710-103">Um namespace XAML é um conceito que se expande na definição de um namespace XML.</span><span class="sxs-lookup"><span data-stu-id="49710-103">A XAML namespace is a concept that expands on the definition of an XML namespace.</span></span> <span data-ttu-id="49710-104">Semelhante a um namespace XML, você pode definir `xmlns` um namespace XAML usando um atributo na marcação.</span><span class="sxs-lookup"><span data-stu-id="49710-104">Similar to an XML namespace, you can define a XAML namespace using an `xmlns` attribute in markup.</span></span> <span data-ttu-id="49710-105">Os namespaces XAML também estão representados no fluxo de nós XAML e em outras APIs de Serviços XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-105">XAML namespaces are also represented in the XAML node stream and other XAML Services APIs.</span></span> <span data-ttu-id="49710-106">Este tópico define o conceito de namespace XAML e descreve como os namespaces XAML podem ser definidos e são usados por contextos de esquema XAML e outros aspectos dos Serviços .NET XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-106">This topic defines the XAML namespace concept, and describes how XAML namespaces can be defined and are used by XAML schema contexts and other aspects of .NET XAML Services.</span></span>  
  
## <a name="xml-namespace-and-xaml-namespace"></a><span data-ttu-id="49710-107">Espaço de nome XML e Espaço de Nome XAML</span><span class="sxs-lookup"><span data-stu-id="49710-107">XML Namespace and XAML Namespace</span></span>  
 <span data-ttu-id="49710-108">Um namespace XAML é um namespace XML especializado, assim como XAML é uma forma especializada de XML e usa a forma xml básica para sua marcação.</span><span class="sxs-lookup"><span data-stu-id="49710-108">A XAML namespace is a specialized XML namespace, just as XAML is a specialized form of XML and uses the basic XML form for its markup.</span></span> <span data-ttu-id="49710-109">Na marcação, você declara um namespace XAML e seu mapeamento através de um `xmlns` atributo aplicado a um elemento.</span><span class="sxs-lookup"><span data-stu-id="49710-109">In markup, you declare a XAML namespace and its mapping through an `xmlns` attribute applied to an element.</span></span> <span data-ttu-id="49710-110">A `xmlns` declaração pode ser feita para o mesmo elemento em que o namespace XAML é declarado.</span><span class="sxs-lookup"><span data-stu-id="49710-110">The `xmlns` declaration can be made to the same element that the XAML namespace is declared in.</span></span> <span data-ttu-id="49710-111">Uma declaração de namespace XAML feita a um elemento é válida para esse elemento, todos os atributos desse elemento, e todos os filhos desse elemento.</span><span class="sxs-lookup"><span data-stu-id="49710-111">A XAML namespace declaration made to an element is valid for that element, all attributes of that element, and all children of that element.</span></span> <span data-ttu-id="49710-112">Os atributos podem usar um namespace XAML que não é o mesmo que o elemento que contém o atributo, desde que o próprio nome do atributo faça referência ao prefixo como parte de seu nome de atributo na marcação.</span><span class="sxs-lookup"><span data-stu-id="49710-112">Attributes can use a XAML namespace that is not the same as the element that contains the attribute, so long as the attribute name itself references the prefix as part of its attribute name in markup.</span></span>  
  
 <span data-ttu-id="49710-113">A distinção de um namespace XAML versus um namespace XML é que um namespace XML pode ser usado para referenciar um esquema ou simplesmente para diferenciar entidades.</span><span class="sxs-lookup"><span data-stu-id="49710-113">The distinction of a XAML namespace versus an XML namespace is that an XML namespace might be used to reference a schema or simply to differentiate entities.</span></span> <span data-ttu-id="49710-114">Para XAML, os tipos e membros usados no XAML devem finalmente ser resolvidos para tipos de backup, e os conceitos de esquema XML não se aplicam bem a esse recurso.</span><span class="sxs-lookup"><span data-stu-id="49710-114">For XAML, the types and members as used in XAML must ultimately be resolved to backing types, and XML schema concepts do not apply well to this capability.</span></span> <span data-ttu-id="49710-115">O namespace XAML contém informações que o contexto do esquema XAML deve ter disponível para realizar esse mapeamento de tipo.</span><span class="sxs-lookup"><span data-stu-id="49710-115">The XAML namespace contains information that the XAML schema context must have available in order to perform this type mapping.</span></span>  
  
## <a name="xaml-namespace-components"></a><span data-ttu-id="49710-116">Componentes de namespace xaml</span><span class="sxs-lookup"><span data-stu-id="49710-116">XAML Namespace Components</span></span>  
 <span data-ttu-id="49710-117">A definição de namespace XAML tem dois componentes: um prefixo e um identificador.</span><span class="sxs-lookup"><span data-stu-id="49710-117">The XAML namespace definition has two components: a prefix, and an identifier.</span></span> <span data-ttu-id="49710-118">Cada um desses componentes está presente quando um namespace XAML é declarado na marcação, ou definido no sistema do tipo XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-118">Each of these components is present when a XAML namespace is declared in markup, or defined in the XAML type system.</span></span>  
  
 <span data-ttu-id="49710-119">O prefixo pode ser qualquer string permitida pelos [Namespaces W3C na especificação XML 1.0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="49710-119">The prefix can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="49710-120">Por convenção, os prefixos são tipicamente strings curtas, porque o prefixo é repetido muitas vezes em um arquivo de marcação típico.</span><span class="sxs-lookup"><span data-stu-id="49710-120">By convention, the prefixes are typically short strings, because the prefix is repeated many times in a typical markup file.</span></span> <span data-ttu-id="49710-121">Certos namespaces XAML que devem ser usados em várias implementações XAML usam prefixos convencionais específicos.</span><span class="sxs-lookup"><span data-stu-id="49710-121">Certain XAML namespaces that are intended to be used in multiple XAML implementations use particular conventional prefixes.</span></span> <span data-ttu-id="49710-122">Por exemplo, o espaço de nome XAML da linguagem `x`XAML é tipicamente mapeado usando o prefixo .</span><span class="sxs-lookup"><span data-stu-id="49710-122">For example, the XAML language XAML namespace is typically mapped using the prefix `x`.</span></span> <span data-ttu-id="49710-123">Você pode definir um namespace XAML padrão, onde o prefixo não é dado na definição, mas é representado como uma seqüência vazia se definida ou consultada by.NET API de Serviços XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-123">You can define a default XAML namespace, where the prefix is not given in the definition but is represented as an empty string if defined or queried by.NET XAML Services API.</span></span> <span data-ttu-id="49710-124">Normalmente, o namespace XAML padrão é deliberadamente escolhido para promover uma quantidade maximizada de marcação de prefixo por uma tecnologia de implementação xaml e seus cenários e vocabulários.</span><span class="sxs-lookup"><span data-stu-id="49710-124">Typically, the default XAML namespace is deliberately chosen in order to promote a maximized amount of prefix-omitting markup by a XAML-implementing technology and its scenarios and vocabularies.</span></span>  
  
 <span data-ttu-id="49710-125">O identificador pode ser qualquer string permitida pelos [Namespaces W3C na especificação XML 1.0](https://www.w3.org/TR/REC-xml-names/).</span><span class="sxs-lookup"><span data-stu-id="49710-125">The identifier can be any string as allowed by the [W3C Namespaces in XML 1.0 specification](https://www.w3.org/TR/REC-xml-names/).</span></span> <span data-ttu-id="49710-126">Por convenção, os identificadores para espaços de nome XML ou namespaces XAML são frequentemente dados na forma URI, tipicamente como um URI absoluto qualificado para protocolo.</span><span class="sxs-lookup"><span data-stu-id="49710-126">By convention, identifiers for either XML namespaces or XAML namespaces are often given in URI form, typically as a protocol-qualified absolute URI.</span></span> <span data-ttu-id="49710-127">Muitas vezes, as informações da versão que define um vocabulário XAML em particular estão implícitas como parte da seqüência de caminhos.</span><span class="sxs-lookup"><span data-stu-id="49710-127">Often, version information that defines a particular XAML vocabulary is implied as part of the path string.</span></span> <span data-ttu-id="49710-128">Os namespaces XAML adicionam uma convenção de identificadores adicionais além da convenção XML URI.</span><span class="sxs-lookup"><span data-stu-id="49710-128">XAML namespaces add an additional identifier convention beyond the XML URI convention.</span></span> <span data-ttu-id="49710-129">Para namespaces XAML, o identificador comunica informações necessárias por um contexto de esquema XAML para resolver os tipos especificados como elementos sob esse namespace XAML ou para resolver atributos aos membros.</span><span class="sxs-lookup"><span data-stu-id="49710-129">For XAML namespaces, the identifier communicates information that is needed by a XAML schema context in order to resolve the types that are specified as elements under that XAML namespace, or to resolve attributes to members.</span></span>  
  
 <span data-ttu-id="49710-130">Para fins de comunicação de informações a um contexto de esquema XAML, o identificador de um namespace XAML ainda pode estar no formulário URI.</span><span class="sxs-lookup"><span data-stu-id="49710-130">For purposes of communicating information to a XAML schema context, the identifier for a XAML namespace might still be in URI form.</span></span> <span data-ttu-id="49710-131">No entanto, neste caso, o URI também é declarado como um identificador correspondente em uma assembléia específica ou lista de assembléias.</span><span class="sxs-lookup"><span data-stu-id="49710-131">However, in this case the URI is also declared as a matching identifier in a particular assembly or list of assemblies.</span></span> <span data-ttu-id="49710-132">Isso é feito em assembléias atribuindo <xref:System.Windows.Markup.XmlnsDefinitionAttribute>a montagem com .</span><span class="sxs-lookup"><span data-stu-id="49710-132">This is done in assemblies by attributing the assembly with <xref:System.Windows.Markup.XmlnsDefinitionAttribute>.</span></span> <span data-ttu-id="49710-133">Este método de identificar o namespace XAML e suportar um comportamento de resolução de tipo baseado em CLR no conjunto atribuído é suportado pelo contexto padrão do esquema XAML em Serviços XAML .NET.</span><span class="sxs-lookup"><span data-stu-id="49710-133">This method of identifying the XAML namespace and supporting a CLR-based type resolution behavior in the attributed assembly is supported by the default XAML schema context in .NET XAML Services.</span></span> <span data-ttu-id="49710-134">De forma mais geral, esta convenção pode ser usada para casos em que o contexto do esquema XAML incorpora a CLR ou se baseia no contexto padrão do esquema XAML, que é necessário para ler atributos CLR de assembléias CLR.</span><span class="sxs-lookup"><span data-stu-id="49710-134">More generally, this convention can be used for cases where the XAML schema context incorporates the CLR or is based on the default XAML schema context, which is necessary in order to read CLR attributes from CLR assemblies.</span></span>  
  
 <span data-ttu-id="49710-135">Os namespaces XAML também podem ser identificados por uma convenção que comunica um namespace CLR e um conjunto de definição de tipo.</span><span class="sxs-lookup"><span data-stu-id="49710-135">XAML namespaces also can be identified by a convention that communicates a CLR namespace and a type-defining assembly.</span></span> <span data-ttu-id="49710-136">Esta convenção é usada <xref:System.Windows.Markup.XmlnsDefinitionAttribute> nos casos em que não existe atribuição nas assembléias que contêm tipos.</span><span class="sxs-lookup"><span data-stu-id="49710-136">This convention is used in cases where no <xref:System.Windows.Markup.XmlnsDefinitionAttribute> attribution exists in the assemblies that contain types.</span></span> <span data-ttu-id="49710-137">Esta convenção é potencialmente mais complexa do que a convenção uri, e também tem o potencial de ambiguidade e duplicação, porque há múltiplas maneiras de se referir a uma assembléia.</span><span class="sxs-lookup"><span data-stu-id="49710-137">This convention is potentially more complex than the URI convention, and also has the potential for ambiguity and duplication, because there are multiple ways of referring to an assembly.</span></span>  
  
 <span data-ttu-id="49710-138">A forma mais básica de um identificador que usa o namespace clr e a convenção de montagem é a seguinte:</span><span class="sxs-lookup"><span data-stu-id="49710-138">The most basic form of an identifier that uses the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyShortName`
  
 <span data-ttu-id="49710-139">`clr-namespace:`e `; assembly=` são componentes literais da sintaxe.</span><span class="sxs-lookup"><span data-stu-id="49710-139">`clr-namespace:` and `; assembly=` are literal components of the syntax.</span></span>  
  
 <span data-ttu-id="49710-140">*clrnsName* é o nome da seqüência que identifica um namespace CLR.</span><span class="sxs-lookup"><span data-stu-id="49710-140">*clrnsName* is the string name that identifies a CLR namespace.</span></span> <span data-ttu-id="49710-141">Este nome de seqüência inclui quaisquer caracteres de dot interno (.) que fornecem dicas sobre o namespace clr e sua relação com outros namespaces CLR.</span><span class="sxs-lookup"><span data-stu-id="49710-141">This string name includes any internal dot characters (.) that provide hints about the CLR namespace and its relation to other CLR namespaces.</span></span>
  
 <span data-ttu-id="49710-142">*assemblyShortName* é o nome da seqüência de um conjunto que define tipos que são úteis em XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-142">*assemblyShortName* is the string name of an assembly that defines types that are useful in XAML.</span></span> <span data-ttu-id="49710-143">Espera-se que os tipos a serem acessados através do namespace XAML declarado sejam definidos pela montagem e sejam declarados dentro do namespace CLR especificado por *clrnsName*.</span><span class="sxs-lookup"><span data-stu-id="49710-143">The types to be accessed through the declared XAML namespace are expected to be defined by the assembly and to be declared within the CLR namespace specified by *clrnsName*.</span></span> <span data-ttu-id="49710-144">Este nome de seqüência normalmente <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>faz um paralelo com as informações relatadas por .</span><span class="sxs-lookup"><span data-stu-id="49710-144">This string name typically parallels the information as reported by <xref:System.Reflection.AssemblyName.Name%2A?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="49710-145">Uma definição mais completa do namespace e da convenção de montagem da CLR é a seguinte:</span><span class="sxs-lookup"><span data-stu-id="49710-145">A more complete definition of the CLR namespace and assembly convention is as follows:</span></span>  
  
 `clr-namespace:clrnsName; assembly=assemblyName`
  
 <span data-ttu-id="49710-146">*assemblyName* representa qualquer string que <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> seja legal como entrada.</span><span class="sxs-lookup"><span data-stu-id="49710-146">*assemblyName* represents any string that is legal as an <xref:System.Reflection.Assembly.Load%28System.String%29?displayProperty=nameWithType> input.</span></span> <span data-ttu-id="49710-147">Essa string pode incluir cultura, chave pública ou informações de versão (definições desses conceitos são definidas no tópico de referência para <xref:System.Reflection.Assembly>).</span><span class="sxs-lookup"><span data-stu-id="49710-147">This string can include culture, public key, or version information (definitions of these concepts are defined in the reference topic for <xref:System.Reflection.Assembly>).</span></span> <span data-ttu-id="49710-148">O formato e a evidência COFF <xref:System.Reflection.Assembly.Load%2A>(como usado por outras sobrecargas de ) não são relevantes para fins de carregamento de montagem XAML; todas as informações de carga devem ser apresentadas como uma string.</span><span class="sxs-lookup"><span data-stu-id="49710-148">COFF format and evidence (as used by other overloads of <xref:System.Reflection.Assembly.Load%2A>) are not relevant for XAML assembly loading purposes; all load information must be presented as a string.</span></span>  
  
 <span data-ttu-id="49710-149">Especificar uma chave pública para o conjunto é uma técnica útil para a segurança XAML ou para remover possíveis ambiguidades que podem existir se os conjuntos forem carregados por nome simples ou pré-existirem em um cache ou domínio de aplicativo.</span><span class="sxs-lookup"><span data-stu-id="49710-149">Specifying a public key for the assembly is a useful technique for XAML security, or for removing possible ambiguity that can exist if assemblies are loaded by simple name, or pre-exist in a cache or application domain.</span></span> <span data-ttu-id="49710-150">Para obter mais informações, consulte [Considerações de segurança XAML](security-considerations.md).</span><span class="sxs-lookup"><span data-stu-id="49710-150">For more information, see [XAML Security Considerations](security-considerations.md).</span></span>  
  
## <a name="xaml-namespace-declarations-in-the-xaml-services-api"></a><span data-ttu-id="49710-151">Declarações de namespace xaml na API de serviços XAML</span><span class="sxs-lookup"><span data-stu-id="49710-151">XAML Namespace Declarations in the XAML Services API</span></span>  
 <span data-ttu-id="49710-152">Na API de serviços XAML, uma declaração <xref:System.Xaml.NamespaceDeclaration> de namespace XAML é representada por um objeto.</span><span class="sxs-lookup"><span data-stu-id="49710-152">In the XAML Services API, a XAML namespace declaration is represented by a <xref:System.Xaml.NamespaceDeclaration> object.</span></span> <span data-ttu-id="49710-153">Se você estiver declarando um namespace XAML <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> em código, você chamará o construtor.</span><span class="sxs-lookup"><span data-stu-id="49710-153">If you are declaring a XAML namespace in code, you call the <xref:System.Xaml.NamespaceDeclaration.%23ctor%28System.String%2CSystem.String%29> constructor.</span></span> <span data-ttu-id="49710-154">Os `ns` `prefix` parâmetros e parâmetros são especificados como strings, e a entrada para fornecer esses parâmetros corresponde à definição do identificador de namespace XAML e do prefixo xaml namespace, conforme fornecido anteriormente neste tópico.</span><span class="sxs-lookup"><span data-stu-id="49710-154">The `ns` and `prefix` parameters are specified as strings, and the input to provide for these parameters corresponds to the definition of XAML namespace identifier and XAML namespace prefix as provided previously in this topic.</span></span>  
  
 <span data-ttu-id="49710-155">Se você estiver examinando as informações do namespace XAML como parte de um fluxo de <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> nós XAML ou através <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> de outro acesso ao sistema do tipo XAML, informa o identificador de namespace XAML e reporta o prefixo de namespace XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-155">If you are examining XAML namespace information as part of a XAML node stream or through other access to the XAML type system, <xref:System.Xaml.NamespaceDeclaration.Namespace%2A?displayProperty=nameWithType> reports the XAML namespace identifier, and <xref:System.Xaml.NamespaceDeclaration.Prefix%2A?displayProperty=nameWithType> reports the XAML namespace prefix.</span></span>  
  
 <span data-ttu-id="49710-156">Em um fluxo de nó XAML, as informações do namespace XAML podem aparecer como um nó XAML que precede a entidade à qual ele se aplica.</span><span class="sxs-lookup"><span data-stu-id="49710-156">In a XAML node stream, the XAML namespace information can appear as a XAML node that precedes the entity to which it applies.</span></span> <span data-ttu-id="49710-157">Isso inclui casos em que as informações de `StartObject` namespace XAML precedem o elemento raiz XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-157">This includes cases where the XAML namespace information precedes the `StartObject` of the XAML root element.</span></span> <span data-ttu-id="49710-158">Para obter mais informações, consulte [Entendendo estruturas e conceitos de fluxo de nó XAML](understanding-xaml-node-stream-structures-and-concepts.md).</span><span class="sxs-lookup"><span data-stu-id="49710-158">For more information, see [Understanding XAML Node Stream Structures and Concepts](understanding-xaml-node-stream-structures-and-concepts.md).</span></span>  
  
 <span data-ttu-id="49710-159">Para muitos cenários que usam a API de Serviços XAML .NET, espera-se que pelo menos uma declaração de namespace XAML exista, e a declaração deve conter ou se referir às informações exigidas por um contexto de esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-159">For many scenarios that use .NET XAML Services API, at least one XAML namespace declaration is expected to exist, and the declaration must either contain or refer to information that is required by a XAML schema context.</span></span> <span data-ttu-id="49710-160">Os namespaces XAML devem especificar conjuntos a serem carregados ou ajudar a resolver tipos específicos dentro de espaços de nomes e conjuntos que já estão carregados ou conhecidos pelo contexto do esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-160">The XAML namespaces must either specify assemblies to be loaded, or assist in resolving specific types within namespaces and assemblies that are already loaded or known by the XAML schema context.</span></span>  
  
 <span data-ttu-id="49710-161">Para gerar um fluxo de nó XAML, as informações do tipo XAML devem estar disponíveis, através do contexto do esquema XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-161">In order to generate a XAML node stream, XAML type information must be available, through the XAML schema context.</span></span> <span data-ttu-id="49710-162">As informações do tipo XAML não podem ser determinadas sem antes determinar o namespace XAML relevante para cada nó a ser criado.</span><span class="sxs-lookup"><span data-stu-id="49710-162">The XAML type information cannot be determined without first determining the relevant XAML namespace for each node to create.</span></span> <span data-ttu-id="49710-163">Neste ponto, ainda não foram criadas instâncias de tipos, mas o contexto do esquema XAML pode precisar procurar informações do tipo de montagem e respaldo definidor.</span><span class="sxs-lookup"><span data-stu-id="49710-163">At this point, no instances of types are created yet, but the XAML schema context may need to look up information from the defining assembly and backing type.</span></span> <span data-ttu-id="49710-164">Por exemplo, para processar a `<Party><PartyFavor/></Party>`marcação, o contexto do esquema XAML deve ser `ContentProperty` `Party`capaz de determinar o nome e o `Party` tipo `PartyFavor`do de , e, portanto, também deve conhecer as informações de namespace XAML para e .</span><span class="sxs-lookup"><span data-stu-id="49710-164">For example, in order to process the markup `<Party><PartyFavor/></Party>`, the XAML schema context must be able to determine the name and type of the `ContentProperty` of `Party`, and thus also must know the XAML namespace information for `Party` and `PartyFavor`.</span></span> <span data-ttu-id="49710-165">No caso do contexto padrão do esquema XAML, a reflexão estática relata grande parte das informações do sistema do tipo XAML necessárias para gerar nonos do tipo XAML no fluxo de nó.</span><span class="sxs-lookup"><span data-stu-id="49710-165">In the case of the default XAML schema context, static reflection reports much of the XAML type system information that is needed to generate XAML type nodes in the node stream.</span></span>  
  
 <span data-ttu-id="49710-166">Para gerar um gráfico de objeto a partir de um fluxo de nó XAML, as declarações de namespace XAML devem existir para cada prefixo XAML usado na marcação original e gravado no fluxo de nó XAML.</span><span class="sxs-lookup"><span data-stu-id="49710-166">In order to generate an object graph from a XAML node stream, XAML namespace declarations must exist for each XAML prefix used in the original markup and recorded in the XAML node stream.</span></span> <span data-ttu-id="49710-167">Neste ponto, instâncias estão sendo criadas, e ocorre um verdadeiro comportamento de mapeamento de tipos.</span><span class="sxs-lookup"><span data-stu-id="49710-167">At this point, instances are being created, and true type-mapping behavior occurs.</span></span>  
  
 <span data-ttu-id="49710-168">Se você precisar prepreencher informações de namespace XAML, nos casos em que o namespace XAML que você pretende que o contexto do esquema XAML <xref:System.Xml.XmlParserContext> use <xref:System.Xml.XmlReader>não esteja definido na marcação, uma técnica que você pode usar é declarar declarações de namespace XML no para um .</span><span class="sxs-lookup"><span data-stu-id="49710-168">If you need to prepopulate XAML namespace information, in cases where the XAML namespace you intend the XAML schema context to use is not defined in the markup, one technique you can use is to declare XML namespace declarations in the <xref:System.Xml.XmlParserContext> for an <xref:System.Xml.XmlReader>.</span></span> <span data-ttu-id="49710-169">Em seguida, use isso <xref:System.Xml.XmlReader> como entrada para <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>um construtor de leitor XAML, ou .</span><span class="sxs-lookup"><span data-stu-id="49710-169">Then use that <xref:System.Xml.XmlReader> as input for a XAML reader constructor, or <xref:System.Xaml.XamlServices.Load%28System.Xml.XmlReader%29?displayProperty=nameWithType>.</span></span>  
  
 <span data-ttu-id="49710-170">Duas outras APIs relevantes para o manuseio de namespace XAML <xref:System.Windows.Markup.XmlnsDefinitionAttribute> em <xref:System.Windows.Markup.XmlnsPrefixAttribute>Serviços XAML .NET são os atributos e .</span><span class="sxs-lookup"><span data-stu-id="49710-170">Two other APIs that are relevant for XAML namespace handling in .NET XAML Services are the attributes <xref:System.Windows.Markup.XmlnsDefinitionAttribute> and <xref:System.Windows.Markup.XmlnsPrefixAttribute>.</span></span> <span data-ttu-id="49710-171">Esses atributos se aplicam às assembléias.</span><span class="sxs-lookup"><span data-stu-id="49710-171">These attributes apply to assemblies.</span></span> <span data-ttu-id="49710-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute>é usado por um contexto de esquema XAML para interpretar qualquer declaração de namespace XAML que inclua um URI.</span><span class="sxs-lookup"><span data-stu-id="49710-172"><xref:System.Windows.Markup.XmlnsDefinitionAttribute> is used by a XAML schema context to interpret any XAML namespace declaration that includes a URI.</span></span> <span data-ttu-id="49710-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute>é usado por ferramentas que emitem XAML para que um espaço de nome XAML em particular possa ser serializado com um prefixo previsível.</span><span class="sxs-lookup"><span data-stu-id="49710-173"><xref:System.Windows.Markup.XmlnsPrefixAttribute> is used by tools that emit XAML so that a particular XAML namespace can be serialized with a predictable prefix.</span></span> <span data-ttu-id="49710-174">Para obter mais informações, consulte [Atributos CLR relacionados à XAML para tipos e bibliotecas personalizados](clr-attributes-with-custom-types-and-libraries.md).</span><span class="sxs-lookup"><span data-stu-id="49710-174">For more information, see [XAML-Related CLR Attributes for Custom Types and Libraries](clr-attributes-with-custom-types-and-libraries.md).</span></span>  
  
## <a name="see-also"></a><span data-ttu-id="49710-175">Confira também</span><span class="sxs-lookup"><span data-stu-id="49710-175">See also</span></span>

- [<span data-ttu-id="49710-176">Noções básicas sobre estruturas e conceitos do fluxo de nó XAML</span><span class="sxs-lookup"><span data-stu-id="49710-176">Understanding XAML Node Stream Structures and Concepts</span></span>](understanding-xaml-node-stream-structures-and-concepts.md)
